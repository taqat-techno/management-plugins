# Global Lessons Learned — Cross-Project Best Practices

> Accumulated from real project sessions. Apply these across ALL projects.
> Last updated: April 14, 2026 (386 lessons, 67 categories)
>

## Report Writing

1. **Be specific, not vague** — "Udemy training completed" > "Core training completed". Name the platform, tool, or system explicitly.
2. **Reports must stand alone** — The reader (CEO, Operation Manager, Engineering Manager) shouldn't need the author in the room to explain what a cell means.
3. **Every cell should be self-explanatory** — If someone reads a table cell and asks "what does this mean?", rewrite it.
4. **Same state = same label** — Use identical wording across all rows/departments for the same status. Don't say "Phase completed" for one and "Training done" for another.
186. **Pearl Pixels Quarterly Report and Q1 Report are complementary, not duplicates** — The quarterly report contains unique PM observations (execution-vs-planning mode, untracked study hours, QA validation gaps, zero-activity team members) not in the main Q1 report. Always read both when building Q2 plans or executive summaries.
187. **"Split" means separate into individual entries, never remove** — When user says "split" two items combined in one row, create separate entries for each. "Split" preserves all items, just separates them. Never interpret it as removing the second item.
227. **Verify team roles from the authoritative team directory before assigning action item owners** — Never assume roles from email CC lists or job title signatures. RC Hosting Investigation assigned backend tasks to an Implementation team member (El-afify) and an unlisted person (Badawy) based on email CC assumptions instead of checking DevOps_Teams.md. Always cross-reference the team directory first.
253. **Deep-study all repos and wikis before creating a dashboard** — A shallow pull-and-list misses wiki content (SOPs, architecture decisions, project plans, test reports). Clone wikis, read every file, and explore all branches before claiming a "complete" report. The Taqat dashboard missed 35 wiki pages (5,461 lines) on first pass.

## Status Labels & Completion Markers

5. **"Ongoing" is a lazy status** — Replace with specific next steps: "Phase 2: To Be Defined", "Certification prep continues independently (lab practice & exam)". Show intentional planning, not vagueness.
6. **Completion markers must be consistent** — If one department shows "Training completed" in a phase, ALL completed departments must show it. Never leave blank or `--` for a completed item.
7. **Explain what "independently" means** — If something continues outside the plan scope, say what specifically continues (e.g., "lab practice & exam"), not just "continues independently".

## Bilingual Documents (EN/AR)

8. **Always update both languages for every text change** — Never edit English without updating the Arabic span. Easy to forget on small fixes.
9. **Verify RTL visually** — Arabic layout can break borders, alignment, and table padding. Always open the HTML and toggle to Arabic after changes.
10. **Check both languages after toggle** — A change that looks fine in English might overflow or misalign in Arabic RTL mode.

## Stakeholder Communication & Emails

11. **Give email options, not just one draft** — Provide 3 versions: formal (detailed), concise (professional), action-oriented (direct). Different audiences need different tones.
12. **"Dear All" for multiple managers** — Avoids hierarchy issues. Let the To/CC fields handle the addressing.
13. **Always include what's attached and why** — Don't just say "please find attached". List what the attachment contains (bullet points).
282. **Record email directives into deliverables immediately** — Semir's Mar 16 email changed Flow 2 ownership (Mohammed Agha → Ashik Mohamed/TaqaTechno) but sat unrecorded for 3 weeks, leaving the V2 summary stale with wrong RACI. Always update execution documents when new email directives arrive.
283. **Always verify and use correct job titles in formal documents** — "IT Project Manager" vs "Project Manager & Scrum Master" is a small detail but a big difference in formal deliverables. Cross-check titles against the latest org chart or email signatures before publishing.

## HTML Deliverables

14. **Open HTML after every change** — Code edits can look correct but render wrong. Always verify in the browser.
15. **Version control in filenames** — Use `_v2`, `_v3` suffixes. Keep old versions in versioned folders (`v1/`, `v2/`, `V3/`).
277. **Assessment files for candidates must NOT contain evaluator content** — When cloning assessment tabs into standalone HTML for email, exclude scoring rubrics, reference solutions, interview questions, scenarios, and PM decisions. Candidates see only requirements + deliverables. Evaluator content stays internal. Cost: leaking the scoring rubric lets candidates game the assessment.
284. **Changelog sections are internal tracking, not for stakeholder deliverables** — V2→V3 diff tables help the PM track what changed, but management-facing HTML should not include version changelogs. Remove them from final outputs presented to the board or stakeholders.
285. **Always reference organization identity explicitly in deliverables** — Include the Azure DevOps org name (TaqaTechno) and URL (dev.azure.com/TaqaTechno) in HTML reports. Never leave organization references generic when the audience needs to know which tenant/org is affected.
308. **Hardcoded candidate names make assessment pages non-reusable** — team-assessment-phase0.html had "Shehab & Hala" in 30+ places. When new candidates join, every reference breaks. Use generic terms ("candidates", "current backend team") from the start. Names belong in runtime data (contenteditable fields), not static HTML.
309. **Every HTML deliverable must include a favicon** — Missing favicons make browser tabs show a generic icon, making it hard to identify dashboards when multiple tabs are open. Use inline SVG data URIs (`<link rel="icon" type="image/svg+xml">`) so favicons work without external files. Check all linked HTML files during dashboard reviews.

## Data Validation

16. **Always validate email/source dates first** — Before merging versions, check if source files actually changed. File size comparison catches identical files.
17. **Cost model is the #1 change trigger** — Major version changes are usually driven by pricing/subscription model changes, not content changes.
18. **Use PowerShell COM for .msg extraction** — `Outlook.Application` works but files must not be open in Outlook.
286. **No assumptions on source data — always read the actual files** — Never assume PDF/MD content matches what you remember. The V3 Recommendation had exact user lists (14 users, 2 teams), cost breakdowns (3 scenarios), and 6 role definitions that all had to match the email summary precisely. Always re-read the source before generating derivative documents.
292. **Always verify live system data before finalizing deliverables** — The V3 recommendation (Feb 3) said 24 users. Live Azure DevOps API check (Apr 7) showed 33 users — a 37% growth in 2 months that no document reflected. Always query the live system before publishing user counts, costs, or scope.

## OKR & KPI Dashboards

19. **OKR scoring must be automatic** — Don't rely on manual status entry for KR progress. Use editable "Current" fields that auto-calculate progress percentage, bar color, and status badge. Manual scoring drifts and becomes inconsistent.
20. **Inverse metrics need special handling** — KPIs where lower is better (bug escape rate, carry-over tasks) need inverse progress calculation: if current <= target, it's 100%. Don't use the same formula as "higher is better" metrics.
21. **Separate OKR (strategic) from KPI (operational)** — OKRs are quarterly aspirational goals with key results. KPIs are ongoing team metrics. Mixing them in one table confuses the audience. Use separate tabs/sections.
183. **Dashboard team rosters must stay in sync with actual assignments** — When a team member is added or moved between teams in DevOps, the dashboard roster (HTML arrays, WIQL member lists, team card tables, member counts, and header totals) must ALL be updated together. A member missing from any one array silently drops their metrics from queries. Check all data arrays — dashboards often have 3-4 redundant member lists that must match.

## Search Index Maintenance

22. **Rebuild search index after every pull** — When new HTML pages are added to a repo with a client-side search engine, the search index (e.g., `search-index.js`) won't include them automatically. Always check if new pages need to be added to the file list AND rebuild the index.
23. **New pages must be registered in build script** — Adding a new HTML file to the repo doesn't make it searchable. The file must also be added to the FILES array in the build script (e.g., `build-index.js`). This is a two-step process: (1) add to list, (2) rebuild.
312. **New root-level HTML files are not auto-indexed in the Sessions Index** — Creating a new HTML file in a project folder does not make it searchable in the Claude Sessions Index. A session card must be manually added to both `Claude_Sessions_Index.html` and `.md` with the correct path, badges, and date for the file to appear in search results.

## Project Estimation & Story Points

24. **Always verify project duration across all files** — Different documents (backlog, proposal, basis of calculation) may show conflicting durations. Cross-check all sources after every pull — the latest versioned file (v2) is the source of truth.
25. **Story points need a documented scale** — If a backlog uses SP without defining what each value means, derive the scale from actual story examples. Group stories by SP value, analyze complexity patterns, and document the scale for team alignment.
26. **Person-months ≠ calendar months** — Always calculate effective person-months (allocation % × duration) not headcount × months. A team of 11 at mixed allocations (20%-100%) may only deliver 15.93 person-months in 4 calendar months.
27. **AI acceleration changes all estimates** — When a project uses AI-assisted development, traditional effort estimates don't apply. Document the multiplier per role (QA gets highest ~4x, PM gets lowest ~1.5x) so stakeholders understand the compressed timeline.

## DevOps API Integration

28. **Use real DevOps states, not made-up labels** — Dashboard metrics must match the exact states in Azure DevOps (To Do, In Progress, Resolved, Done, Closed). Using generic terms like "Active" or "Carry-over" creates confusion when comparing with the DevOps board. Always query actual states first with the States API before building queries.
29. **Query by Assigned To when Area Paths don't exist** — If a DevOps project doesn't have team-specific area paths (Backend, Frontend, etc.), query by `[System.AssignedTo] CONTAINS 'member name'` across all projects. This is more flexible than area paths and works cross-project.
30. **Always provide a Data Source tab** — When building dashboards that fetch from APIs, include a transparent "Source" tab showing the exact queries used, results returned, and verification links. The user should never have to trust the numbers blindly — they need to verify against the source system independently.
31. **Discover projects via API before hardcoding** — Don't assume which DevOps projects exist based on email analysis or conversation context. Use `/_apis/projects` to discover all projects, then let the user confirm which ones to include.
32. **Never expose PAT tokens in chat or files** — PAT tokens should only be entered in browser modals and stored in localStorage. Never save them in code, memory files, or conversation. If a user shares one, remind them not to.
33. **Arabic toggle must be planned from day one** — Every PM dashboard needs EN/AR toggle. Adding it after the fact requires touching every text element. Plan for `data-i18n` attributes from the start to avoid rework.
34. **"Last updated" means fetch time, not current time** — Dashboard timestamps should show when data was last retrieved from the API, not a live clock. A live clock gives false confidence that data is current when it may be hours old.
35. **Show full project names, not abbreviations** — In dashboards for CEO/Board, use full project names (e.g., "KhairGate (BMS)") instead of abbreviations (e.g., "BMS"). Abbreviations are internal shorthand that external stakeholders won't recognize.
293. **Azure DevOps API does not store job titles** — The user list API (`az devops user list/show`) only returns display name, email, and license type. Job titles must come from the user, HR system, or Microsoft Entra ID. Don't assume you can pull titles from DevOps.

## Session Workflow & Git Hygiene (Mar 16, 2026)

36. **Always pull before analyzing** — Never analyze or report on data without pulling latest from remote first. Stale local data leads to wrong conclusions (e.g., reporting 9-month plan when v2 already shows 4 months).
37. **URL.txt is the session start ritual** — For repos with a URL.txt, always run it first: fetch → compare → pull if needed → open HTML. This prevents working on outdated files.
38. **Check git diff after pull, not just commit message** — Commit messages like "design modifications" are vague. Always check `git diff --stat` to know exactly which files changed and how much, so you can assess impact.
39. **Version files may have internal inconsistencies** — A v2 file may still contain v1 data in some sections (e.g., team member durations showing 9 months in a 4-month plan). Flag these to the user rather than silently assuming one is correct.
278. **Remove project-specific context from standalone assessment files unless PM explicitly wants it** — When creating assessment HTML for email, candidates need technical requirements — not PM governance context (project names, phase numbers, sprint references, hiring gaps). Only include project context if the PM explicitly confirms it belongs in the candidate-facing file.
306. **Always run git pull first, not just git log** — `git log` shows fetched commits but doesn't update the working tree. Today's session checked `git log` (saw 4 commits) but didn't pull, leaving local 2 commits behind. The drift fix was applied correctly but the repo was stale. Always `git fetch` + `git pull` as the first action.

## Data Analysis & Presentation (Mar 16, 2026)

40. **Derive what's not documented** — If a backlog has story points but no scale definition, reverse-engineer it from the data. Count distribution, group by SP value, read actual stories at each level, then present the derived scale for validation.
41. **Show the math, not just the result** — When presenting person-months, show: Allocation % × Duration = Person-Months for each member. Stakeholders trust calculations they can verify.
42. **Compare before and after** — When two versions of a plan exist (v1 vs v2), always present a variance table. Decision-makers need to see what changed and by how much (e.g., -56% duration, -66% effort).
43. **Define jargon when asked** — Terms like "person-month", "story point", "sprint velocity" are not obvious to all stakeholders. When the user asks "what does X mean?", give a concrete example from their own project data, not a textbook definition.

## Memory & Lessons Discipline (Mar 17, 2026)

44. **Save lessons at end of every session** — Don't wait for the user to ask. At the end of each working session, proactively capture lessons learned into `global_lessons.md` and project-specific memory files.
45. **Memory files are for future conversations, not current tasks** — Don't save ephemeral task details (current debug state, temp file paths). Save things that help future-you understand the project: team composition, estimation methodology, key decisions, file relationships.
46. **Update session log after every pull** — The session log in MEMORY.md is the project timeline. Record: date, commit range, new/updated files, key actions taken. This helps trace when changes were introduced.

## Consolidation & Multi-Source Architecture (Mar 24, 2026)

47. **Never modify source files in consolidation projects** — Keep all source analyses untouched (e.g., S1: 468 files, S2: 21 files). Build an independent consolidated output (HUB v3.0). This preserves original analysis integrity for audit trails.
48. **Track conflicts explicitly with IDs and side-by-side display** — Number conflicts (CF-01 to CF-12) and show both positions without auto-resolution. Let governance decide. Transparent conflict management builds stakeholder trust.
49. **Color-coded source attribution on every data point** — Use consistent color tags (S1=blue, S2=orange, Merged=purple, New=green) so readers can trace any claim to its origin. Critical for multi-source projects.
50. **3-tier gap classification prevents analysis paralysis** — Classify gaps as Blocking / Important / Deferrable. Simple 3-tier system distinguishes urgency without overcomplicating prioritization.

## Portal & Pipeline Architecture (Mar 24, 2026)

51. **Zero-dependency static HTML is the most durable deliverable** — No build system, no bundler, no server. Works offline, on any machine, forever. Essential for board presentations and long-term archival.
52. **Modular CSS from day one saves rework** — Separate CSS files (variables, base, layout, nav, components, RTL, print) allow targeted changes without breaking unrelated pages. Invest in CSS architecture early.
53. **Numbered scripts enforce execution order** — Name pipeline scripts 01→22 to make execution order self-documenting. Anyone can run them in sequence without reading a README.
54. **Large JSON intermediates enable independent downstream scripts** — Files like `db_objects.json` (20MB) are large but let any downstream script work without re-extracting from source. The storage cost is worth the decoupling.
55. **FOLDER_OBJECTIVE_MAP.html serves as the project atlas** — A visual map of all folders and their purposes is essential when projects exceed 10 subfolders. New team members can orient themselves immediately.
56. **Recommendations stay as recommendations until governance approves** — Never auto-apply analysis recommendations to source files. Present findings; let the board decide. Technical teams provide analysis; governance decides.

## Dashboard Maintenance & Adoption (Mar 24, 2026)

57. **Maintenance burden kills dashboards** — If a dashboard requires > 30 manual inputs per week for one person, it won't be used. v1 had ~113 manual inputs and was never populated. v2 cut to ~24 by auto-fetching everything possible from DevOps APIs. Design rule: if an API can provide it, never ask the PM to type it.
58. **KRs must be outcomes, not activities** — "Weekly agendas with DevOps metrics pre-populated" is a task, not a key result. "Decision turnaround time < 3 days" is a measurable outcome. Before finalizing OKRs, test each KR: "Could someone else observe and verify this was achieved?" If not, rewrite it.
59. **PMs need their own scorecard for CEO visibility** — Team KPIs (sprint velocity, bug rate) measure the team, not the PM. Add a dedicated PM Scorecard with metrics the CEO evaluates the PM on: decision turnaround time, scope change control, meeting compliance, stakeholder satisfaction pulse. Without this, the PM is invisible in their own dashboard.
60. **localStorage snapshots enable trend tracking without a backend** — Save a weekly snapshot object into a history array on every "Save" click. Use inline SVG sparklines (no Chart.js dependency) to show week-over-week progress. Cap at 13 weeks (~500KB). Add Export/Import JSON for backup. This gives trend visibility with zero infrastructure.

## Enhanced Standalone Auto-Updater Rules (Mar 24, 2026)

61. **Surface updates are NOT enough** — After every Hub V2 pull, audit internal content (Document Control tables, assumptions, risk descriptions, internal text references), not just headers/footers/KPIs. V5-V10 missed this completely.
62. **Each pull with drift = new standalone folder** — Never edit existing version in place. Copy folder, increment version, apply fixes. Each version is a frozen snapshot.
63. **Create own reports, never copy Hub V2's** — PM creates own variance/story reports based on previous template. Hub V2 reports are Mohamed's internal documents with different branding.
64. **Hub V2 pages don't belong in standalone** — Only PM-created artifacts go in the standalone folder. Hub V2 pages (health-dashboard, etc.) are linked via nav, not copied.
65. **Document Control tables must match folder version** — Version field, Updated date, Data Source (with commit hash) must be current in every file after every update.
66. **Footer must show specific date** — "Modified: DD Mon YYYY" not generic "March 2026". Every file, every version.
67. **Phase 0 vs full program must be distinguished** — Budget, Timeline, Hiring show full 22-month program but kickoff is Phase 0 only. Both must be shown.
68. **Old reports don't belong in kickoff package** — Only latest variance + story variance versions. Remove historical snapshots (v10, v11, v1, v2).
69. **BA Review is a loop, not one-shot** — Mahmoud reviews → Mohamed fixes → Mahmoud verifies. Loop until no critical issues. Track pass number.
70. **Read lessons BEFORE executing** — At start of every session, read feedback files before touching any code. Prevents repeating past mistakes.
71. **Never assume git author = current user** — Check git log. MahmoudEl-Afify ≠ Mahmoud Elshahed.
72. **Validate Hub V2 claims against actual data** — Don't trust page counts, story counts, or status claims. Count actual files. Read actual KPI strips.
73. **Interactive buttons must sync via same localStorage key** — All files using status buttons must share `kg-action-progress` key. Labels must match across files (3 states for ADRs).
74. **Resolved sections should be collapsed by default** — `<details>` without `open` for ALL RESOLVED / CLOSED sections. Focus on pending items.

## OKR Dashboard v2 Lessons (Mar 25, 2026)

75. **Tab-based navigation beats sidebar for dashboards under 10 views** — v1 used 12 sidebar items; v2 consolidated to 7 tabs. Fewer navigation choices = faster cognitive load for executives. Only use sidebar when you have 10+ sections or need persistent visibility of all options.
76. **Bilingual i18n must use data attributes, not duplicate HTML** — v2 uses `data-i18n` keys with a JS translation map (~223 keys). Duplicating entire pages for EN/AR doubles maintenance and guarantees drift. Plan for `data-i18n` from the start so every text element is toggle-ready.
77. **Print CSS must show all tabs, not just the active one** — Dashboard print styles that hide inactive tabs produce blank pages. Use `display: block !important` on all `.tab-content`, add `page-break-before: always` between sections, and inject section titles via `content: attr(data-tab-title)` pseudo-elements. Always test Ctrl+P after adding print styles.
78. **Metric cards should be clickable drill-downs, not dead-end numbers** — Showing "To Do: 12" is useless without knowing who owns what. Every aggregate metric card should open a per-member or per-project breakdown modal on click (`showTeamDetailModal`). This was the key UX improvement in Team Pulse v2 — all 6 teams, 4 states each, 24 clickable drill-downs.
79. **Declare cached variables BEFORE loadData() runs** — JS `var` hoisting hoists the declaration but not the `= {}` initializer. If `var cache = {}` appears after `loadData()`, the initializer overwrites whatever `loadData()` restored from localStorage. Always declare cache variables before any load function that populates them.
80. **Never duplicate team members across teams** — Double-counting inflates metrics silently and makes drill-downs misleading. One person = one team. If they work across teams, pick their primary. OKR v2 had 4 members in both Mobile and Qatar — split them to eliminate overlap.
81. **WIQL returns work item IDs for free — don't discard them** — `data.workItems` from Azure DevOps contains `[{id, url}]`. The original `runWIQL` threw away IDs and returned only `.length`. `runWIQLWithIds` preserves them at zero extra API cost. Cache IDs in localStorage so drill-down links survive page reloads.
82. **Dashboards need a built-in user guide** — A PM won't use a dashboard they don't understand. Embed a step-by-step weekly workflow, a timeline with deadlines, and an auto-vs-manual field reference directly in the dashboard. Don't rely on external documentation — the guide must live inside the HTML.
83. **Always open HTML after edits — no exceptions** — Never leave the user to open files manually. Run `Start-Process` or equivalent as the final step after every HTML edit session. This was explicitly called out as a failure and saved as a feedback memory.

## Auto-Updater & UX Redesign Lessons (Mar 25, 2026)

84. **PowerShell em-dash crash** — Unicode characters (em-dash, curly quotes) in PowerShell string literals cause silent parse failures. The window closes instantly with no error. Always use ASCII alternatives (`--` not `—`, straight quotes not curly).
85. **Always provide .bat launcher for PS scripts** — "Right-click → Run with PowerShell" closes the window on any error. Create a `.bat` file with `powershell -ExecutionPolicy Bypass -NoExit -File script.ps1` so errors stay visible.
86. **Disable update buttons when no drift** — "Full Update" when already up-to-date is misleading and risky. Separate into 3 buttons: Check (always available), Pull+Update (only when drift), Audit Only (always available). Grey out unavailable actions.
87. **Automate recurring failures** — If a check is missed 3+ times across sessions, build a tool. The auto-updater script exists because manual drift fixes repeatedly missed Document Control tables, assumptions, and internal content. Human checklists fail; scripts don't forget.
88. **3 page types define nav UX** — Action pages (green, interactive buttons for decisions), Reference pages (blue, read-only for board review), Audit pages (purple, health history snapshots). Organize navigation by type, not by arbitrary categories. Directors need to know: "where do I click?" vs "where do I read?"
89. **Merge overlapping pages** — Two pages covering the same topic (KICKOFF_READINESS_GUIDE + KICKOFF_READINESS_VALIDATION) confuse the Director. One source of truth per topic. If you create a summary page, merge it into the original rather than adding another file.

## Cross-File Consistency & Audit Lessons (Mar 25, 2026)

90. **Deleting a file breaks all relative links to it** — When we deleted root-level HUB_PULL_HISTORY.html, 14 V11 sidebar nav links (`../HUB_PULL_HISTORY.html`) broke silently. Always grep for all references BEFORE deleting any file, and fix links in the same commit.
91. **Don't duplicate files across locations** — Having HUB_PULL_HISTORY at root AND inside V11 caused staleness (root was March 18, V11 was current). One copy, one location. If other files need it, link to it — don't copy it.
92. **Location-specific terms don't belong in hiring risks** — "Qatar market" implies on-site only. Hiring may be hybrid/remote. Use skill-based language instead: "Odoo 19 skill scarcity" not "Qatar market scarcity". Applies to all R-08 references across PM_RISK_REGISTER, ADR_APPROVAL_REQUEST, and any hiring-related text.
93. **Health dashboard verdict must reference cards by name** — A flat list of 7 pending items is unreadable. Group pending items under the card they belong to (e.g., "POC Readiness V.2 — ADR-1, Budget, Odoo hiring"). Board can then click the right card directly.
94. **Visual badges (ACTION NEEDED / CLEAR) save board reading time** — 7 cards with no visual priority = Board reads all 7. Amber badge + green badge = Board reads only the 3 that matter. Always add status badges when cards have mixed states.
95. **Track review loops explicitly** — The BA review between El-Afify and Mohamed had no visible status on the dashboard. Pass 2 was pending but nowhere documented. Add "Review Loop Status" tracking boxes to both the health dashboard card and the readiness page.
96. **Purpose tips in headers orient the reader** — Every page header should have a one-line description of what this page does. Board members open 15+ files — a tip below the title saves them from scrolling to understand context. Use `.summary` div pattern.
97. **Auto-updater covers KPIs but not consistency** — The PS1 server catches number drift (stories, SP) but misses text mismatches ("Qatar market" in 2 files), broken nav links, file count disagreements, and version tag staleness. Build a 6-check audit: KPIs + text + nav + file count + status sync + version tags.
98. **Sprint-plan-phase0 and phase0-poc-readiness are a pair, not duplicates** — Sprint plan = developer execution guide (Mohamed's, 2,196 lines). POC Readiness = PM status dashboard (ours, 227 lines). Different audiences, same data. Keep both but verify data consistency between them.
99. **Session handoff must include tracking file updates** — HUB_PULL_HISTORY, FOLDER_OBJECTIVE_MAP, and MEMORY.md must be updated at end of every session. Save a "FIRST TASKS Next Session" section in memory if context runs out.
203. **Supporting documents drift silently during version bumps** — Approval emails, cover letters, and non-artifact HTML files are not included in version-bump agent scope. KICKOFF_APPROVAL_EMAIL had V5 data (commit 2f321a3, March 9) while all 13 PM artifacts were at V13 (March 30). Stale numbers (18 vs 19 risks, 21 vs 28 actions, 200+ vs 937 stories) went unnoticed for 4 versions. Include ALL HTML files in audit-check scope.
204. **KPI smart cards must match source file buttons and KPI strips** — ADR_APPROVAL_REQUEST showed "1 Approved / 2 Pending" in KPI cards while all 3 ADR buttons were already "Approved." Manual review missed this; only the audit-check agent caught it. Smart card values are high-visibility — board sees them first — so mismatches create credibility risk.
205. **Internal summary text drifts independently from KPI cards in the same file** — PROJECT_STARTUP_CHECKLIST summary said "21 actions / 3 phases" while its own KPI cards correctly showed "28 / 4." PM_RISK_REGISTER meta said "18 Risks" while its KPI card showed "19." Always audit both the KPI strip AND the subtitle/meta/section headers — they are updated by different processes and diverge silently.
206. **Never expose internal PM governance file names on Hub V2 pages** — V13 Standalone file names (PM_RACI_MATRIX.html, HIRING_TEAM_PLAN.html) were added to phase0-poc-readiness.html on Hub V2. The team sees Hub V2 only; V13 references leak authority structure and create confusion about which files are canonical. Always use Hub V2 paths on Hub V2 pages.
207. **Hub V2 technical files created by engineering are NOT PM governance files** — sprint-plan-phase0 Key Contacts table (4 people) was treated as the project team structure. It's only a Phase 0 quick-reference. Hub V2 raci-matrix.html and hiring-team-plan.html are NOT approved by IT PM. Always confirm approval status before treating any file as authoritative for roles and responsibilities.
208. **Discrepancy reports need owner assignments + clickable source file links** — Review summary of sprint-plan-phase0 found 9 issues but initially had no owner or file references. Adding "Responsible: Mohamed Basha" + hyperlinked source files made it actionable. Without these, findings sit unresolved because nobody knows who should fix what or where.
209. **AI-generated placeholder names in Hub V2 files must be caught and flagged** — pbi-validation-phase0.html listed "Dr. Sarah Pemberton (BA)" as reviewer — a fabricated name. The actual reviewer is Mahmoud Afifiy (El-Afify). Placeholder names erode credibility if they reach board review and can cause confusion in formal audit trails.
210. **Separate internal evaluator content into local-only files before pushing** — Reference solutions (code answers) and interview questions (with expected answers) must NOT be in files pushed to shared repos. Candidates can see anything in the repo. Extract to V13-only files (e.g., ASSESSMENT_REFERENCE_SOLUTION_INTERNAL.html) and replace with locked placeholder in Hub V2 version.
211. **When moving files between folders, grep for ALL relative path references** — Moving team-assessment-phase0.html from Hub V2 to Team Assessment folder broke 8 CSS paths, 4 JS paths, and 3 cross-file links silently. Page rendered unstyled. Always grep for the filename across ALL files and fix paths in the same step as the move.
219. **After a drift fix, re-fetch origin before pushing** — Hub V2 moves fast. The E19 fix took hours, during which E2 expanded (+1 story, +3 SP), making V14 stale before it was even pushed. Always re-check for new commits after completing a drift fix.
220. **version-bump agent misses body-text prose with version numbers** — It handles version-tag spans, headers, footers, but NOT inline text like "all 5 operational docs complete in v13.0". Always grep for the old version in body text after version-bump completes and fix manually.
221. **Review summary fixes must be on SOURCE pages, not the tracking dashboard** — Basha marked C1-C5/H1-H4/D1-D3 resolved on phase0-poc-readiness (our PM dashboard) but sprint-plan-phase0 and pbi-validation-phase0 (source files) were not updated. Dashboard fixes are cosmetic — source files remain broken.
307. **Check all cross-tab dependencies before adding content to one tab** — Adding .gitignore, screen recording, and AI policy to Tab 1 (Assessment) required updates in Tab 3 (Scorecard scoring rows), Tab 4 (Deep Review anti-patterns), Tab 5 (Interview AI policy note), and Overview (description). Editing one tab without mapping dependencies first means follow-up edits that could be missed.

## HR & Recruitment (Mar 31, 2026)

212. **Always check the email thread for ALL active JDs before evaluating any candidate** — A single recruitment folder ("Senior Odoo Developer") contained TWO separate active positions: (1) Senior Odoo Developer and (2) Sr. Backend Engineer — Django & Python. First evaluation scored Amr Deep against the Odoo JD (15% match); the correct Django JD yielded 35-40%. Cost: entire interview form had to be rewritten. Always read the .msg files to identify ALL open positions before building any evaluation.
213. **One recruitment folder can contain multiple positions with separate criteria** — Don't assume one folder = one role. The Basha email added a second position (Django & Python) alongside the original Odoo role. Both are active simultaneously with different JDs, different criteria, and different candidate pipelines. Always ask which position a candidate is being evaluated for.
214. **Color-code interview forms by role type for visual distinction** — Odoo interviews use purple (#714B67), Django/Python interviews use blue (#1a5276). When multiple role types share a recruitment folder, visual distinction prevents confusion during panel reviews.
215. **"Revised" or "new JD" doesn't always mean replacing the old one** — When an email says "refine position JD not to be Odoo BE developer, instead it should be Sr. Backend", it may mean adding a second position, not killing the first. Both the Odoo and Django roles were active simultaneously. Always confirm: is this a replacement or an addition? Assuming replacement when it's an addition means you stop evaluating valid candidates for the original role.
216. **Match each candidate to their target position at intake, not during evaluation** — When a new CV arrives, the first question should be "which of our open positions does this candidate map to?" before any scoring begins. Amr Deep's CV was initially scored against the wrong JD because the position mapping wasn't established upfront. Establish position → candidate mapping before building any evaluation artifact.

## Git, Branching & Deployment Lessons (Mar 26, 2026)

100. **`git rm -rf .` deletes actual files, not just tracking** — When creating orphan branches inside the working folder, `git rm -rf .` permanently deleted V12 files. All session changes had to be re-applied from scratch. Always work from a DIFFERENT directory when creating orphan branches, or use `git worktree`.
101. **PowerShell arrays of hashtables don't serialize with ConvertTo-Json** — `@() += @{...}` pattern produces empty JSON arrays. Use `[System.Collections.ArrayList]::new()` with `.Add()` instead. Also increase `-Depth` to 10+ for nested structures.
102. **Wrong Hub V2 path = wrong KPIs** — `KhairGate-Hub-Analysis` (V1, stale) vs `KhairGate-Hub-Analysis-V2` (current). The auto-updater pointed to V1 for months without anyone noticing. Always verify the exact folder name when configuring paths to data sources.
103. **Greedy regex causes false positives in audits** — `Financial Readiness.*100%` matched "To reach 100%" text instead of the actual 80% value on the same line. Fix: use tight patterns that match specific HTML structure (e.g., `progress-label` div value), not greedy wildcards across the whole line.
104. **Multilingual regex needs wider context windows** — Arabic "العودة" (fallback) was 40+ chars before "Odoo 18" but the 20-char lookback missed it. Expand context windows to 80+ chars when checking multilingual content for contextual exclusions.
105. **Historical files must be excluded from audit checks** — HUB_PULL_HISTORY, variance reports, and ADR contain intentional references to old tech (Camunda, Odoo 18, USD). They document history, not current state. Audit rules that flag "stale content" must have an exclusion list for historical/audit files.
106. **Hub V2 i18n uses data-i18n attributes + inline JS dictionary** — Not JSON files. ~550+ terms in a JS object (`var T = {...}`) inside i18n.js. `toggleLang()` swaps `textContent` via `querySelectorAll('[data-i18n]')`. New pages need both the data-i18n attributes AND the dictionary entries to translate.
107. **Push ALL repos after changes, not just the one you're focused on** — Hub V2 changes (Arabic translations) were committed but not pushed until the user asked. When a session touches multiple repos, push all of them before ending.
108. **Orphan branches preserve version snapshots** — Each Enhanced Standalone version (v1-v12) as its own orphan branch with descriptive commit messages preserves the full evolution history. Use `git checkout --orphan v{N}` pattern.
109. **Auto-update lessons from audit findings with dedup** — Build the pipeline to write back to global_lessons.md automatically. Check for existing keywords before appending to avoid duplicates. Stale lesson files are worse than no lesson files.
217. **Create git branch in the SAME step as pushing a new standalone version** — V14 was pushed to main but v13/v14 branches were not created until the next day. Branches are the team's way to access frozen versions. Always: push commit → create branch → push branch — one atomic step.
218. **Archive the old version folder in the SAME step as creating the new one** — V13 folder couldn't be moved to archive for 2 days because .git was locked by VS Code. If deferred, file locks accumulate. Always: create V14 → move V13 to archive → verify — before any other work.
254. **Never assume repo state — verify with actual commands** — When asked "do we need to pull?", don't rely on environment metadata saying "not a git repo". Read reference files (URL.txt), check actual folders, run git status. The Taqat folder had 10 cloned repos inside it despite the root not being a repo.

## Dashboard & KPI Design Lessons (Mar 26, 2026)

110. **Separate work item types for accurate metrics** — Grouping Bug + Enhancement under "Task" hides defect trends. Each type needs its own WIQL query, KPI card, and column in drill-down tables. Without separation, you can't answer "how many bugs are open?" from the dashboard.
111. **Auto-calculate derived KPIs — don't let users manually set what can be computed** — Overall health = worst-of(Schedule, Quality, Scope) + blocker penalty. Manual "Overall" toggles drift from reality. Formula: min(S,Q,Scope), downgrade if blockers > 0, force Red if blockers > 3.
112. **Merge tiered tables into one unified view** — Tier 1 (full G/A/R) + Tier 2 (active items only) confused readers. One table, same columns, all 11 projects. Tiers are for prioritization logic, not UI separation.
113. **Column tooltips explain scoring criteria to non-technical readers** — Board members don't know what "Quality = Amber" means. Add `title` attributes: "Consider bug count, rework rate, QA pass rate." Every subjective column needs a tooltip.
114. **localStorage migration between dashboard versions** — v2→v3 key change (`okr-kpi-q2-2026-v2` → `-v3`) requires auto-migration logic on load. Users lose data without it. Always check for old key, transform data shape, write to new key, delete old.
294. **Cost scenario cards must show the phased ramp, not just the final number** — "$1,704/year" means nothing without showing it ramps $70→$106→$142/month across 3 batches. Management needs to understand cash flow timing per batch, not just the annual total.

## Document Consolidation Lessons (Mar 26, 2026)

115. **Identify the primary source before merging** — When 3 files cover the same meeting, one is always the most comprehensive. Use it as the skeleton; others are supplements for gaps only. Don't merge line-by-line — that creates duplicates.
116. **Email-ready HTML beats DOCX for meeting distribution** — Inline CSS, no attachment issues, renders in every mail client. For MoM/agenda distribution, HTML is the final format. DOCX is the input, not the output.
295. **Implementation roadmaps must evolve with the execution plan** — A 6-phase single-batch roadmap (Feb) became stale when execution shifted to 3-batch phased rollout (Apr). Both the summary tab and the recommendation tab must stay in sync — if one changes structure, the other must follow.

## Modal, Audit & i18n Lessons (Mar 26, 2026)

117. **Modal popups beat inline results for audit output** — Inline `<div id="results">` pushes page content down and gets lost on scroll. Modal overlay with backdrop = focused attention, dismissible, no layout shift. Include summary bar (pass/fail counts) + individual check cards.
118. **Remove vendor names from architecture documents** — "Camunda" → "external BPM tool". Docs survive tool changes without rewrites. Historical files (ADR, variance reports) keep vendor names as they document decisions made at that time. Only current-state docs use generic terms.
119. **Title consistency is a cross-file audit check** — "Project Manager" vs "IT Project Manager" appeared in 6+ files across V12. Title mismatches erode credibility with board readers. Add title verification to the audit engine. Exclude external stakeholder entries (e.g., Semir's role stays as-is).
120. **Batch i18n: attributes + dictionary in one commit** — Adding `data-i18n` attributes to HTML without updating the JS dictionary (`var T = {...}`) means keys resolve to raw key names. Always add both in the same commit. For Hub V2, that's 9 HTML files + i18n.js = one atomic commit.

## OKR & KPI Dashboard v3 Lessons (Mar 26, 2026)

121. **Numbers must reconcile across dashboard views** — If Executive Summary shows 26 Bugs total, Team Pulse must also total 26. Add an "Unassigned" card to catch work items with no owner. Formula: Project total = Sum(all teams) + Unassigned. If they don't match, users lose trust in the dashboard.
122. **Consistent drill-down pattern across all metrics** — Every clickable number must follow the same flow: Card number → Breakdown table → Work Items popup (with DevOps links) → Back button. Don't skip levels for Bugs/Enhancements while keeping them for To Do/In Progress. Inconsistency confuses users.
123. **Modal max-height prevents off-screen Close button** — A project with 18+ work items pushes the Close button below the viewport. Always set `max-height: 85vh; overflow: auto` on modal content. The Close and Back buttons must always be reachable.
124. **Don't add redundant summary strips** — A live data strip in the header repeating KPI card values adds clutter, not clarity. Use header space for report purpose/benefits instead. Data lives in KPI cards; identity lives in the header.
125. **G/A/R toggles need deselect capability** — Click to select, click again to deselect back to "none". Without this, once you set a value you can never clear it. Check `el.classList.contains('selected')` before toggling.
126. **Separate work item types in WIQL queries** — Mixing Task+Bug in `IN ('Task', 'Bug')` hides bug counts. Use separate queries for Bug-only and Enhancement-only counts. This enables auto-populating KR2.1 (production bugs) and gives PM visibility into defect vs improvement work.
127. **Persist in-memory data to localStorage** — `lastProjectData` was only in JS memory — lost on page reload, breaking detail modals ("Click Fetch first"). Save runtime data structures to localStorage in `saveData()` and restore in `loadData()` if the UI depends on them.
128. **Use the Blocked field, not tags** — Azure DevOps has a dedicated `Microsoft.VSTS.CMMI.Blocked` field (Yes/No). Don't rely on `[System.Tags] CONTAINS 'Blocked'` which requires manual tagging. The field is built into the work item form.
129. **Move action buttons to sidebar footer** — Fixed bottom bars (Save/Export/Clear) waste 50px of vertical space on every page. Sidebar footer is always visible and doesn't consume main content area. Also move Fetch/PAT buttons there.
130. **Two-column header: purpose left, identity right** — Title + subtitle + benefits on the left; name + org + quarter + meta tags stacked right-aligned on the right. No wasted horizontal space. AR toggle goes top-right.
131. **Always add tooltips and legends for Board audience** — G/A/R buttons, column headers (Schedule/Quality/Scope), KPI cards — every element needs hover `title` explanation. Board members won't guess what "Quality = Amber" means. Add a visible legend box above the table.
132. **Spell out acronyms in the title** — "OKR & KPI" means nothing to a CEO. Show "Objectives & Key Results / Key Performance Indicators" beside the short title. The abbreviated form is for navigation (sidebar), the full form is for comprehension (header).
133. **Use correct job titles consistently** — "IT Project Manager" not "PM" or "Project Manager". Check every occurrence: header, print summary, PM Scorecard title, meta tags. Title mismatches erode credibility in Board-facing reports.

## Email Analysis & Weekly Reporting Lessons (Mar 26, 2026)

134. **Verify topic categorization by participants, not keywords** — "Terms and Conditions (English)" was placed under Relief Center based on keyword match. Checking participants (Dr. Bahaa, Hacene, Syed) immediately revealed it belongs to BMS/KhairGate. Always check the participant list in topic_summary.csv before assigning new topics to project categories.
135. **Email volume ≠ project status — never set badges from data alone** — A project with 12 new emails might be on track; a project with zero emails might be critically blocked. Status badges (URGENT, ACTIVE, MEDIUM, etc.) must come from the PM's manual input, not automated email counts. Email data provides trends and counts; human judgment provides status.
136. **Checkboxes without persistence are decoration** — HTML checkboxes that reset on page refresh provide no tracking value. Either add localStorage persistence or accept that status tracking is manual. For weekly snapshot reports, manual Thursday review is sufficient — don't over-engineer interactivity.
137. **Embed the update process in the deliverable itself** — If a report has a recurring update workflow, document the process steps inside the report (e.g., info card on the Dashboard tab). Anyone opening the report should understand how it gets updated without needing external documentation.

## File Management & Deletion Safety Lessons (Mar 27, 2026)

138. **Never use bash `rm` on Windows — always use Recycle Bin** — Bash `rm` permanently deletes files with no recovery. Always use PowerShell safe delete: `[Microsoft.VisualBasic.FileIO.FileSystem]::DeleteFile('path', 'OnlyErrorDialogs', 'SendToRecycleBin')`. This applies to all user files, no exceptions. Lost 3 Skilljar HTML source files (~4 MB total) because `rm` was used instead of Recycle Bin.
139. **Always confirm which specific files to delete before any deletion** — When asked to "delete files not needed," never assume which files the user means. List all files with numbers and ask the user to pick. Assumption led to deleting original source files instead of generated ones.
140. **Source/original files are sacred — never delete originals** — Downloaded files, user-created content, and original sources may be irreplaceable. Only delete generated/derived files (and only after explicit confirmation). Generated files can always be recreated; originals cannot.
141. **Separate destructive actions from other work** — When a request includes both "delete X" and "do Y," handle the deletion as a standalone step with its own confirmation. Chaining "delete + open" led to rushing through deletion without proper verification.
222. **Archive old versions, keep only the latest in the root folder** — When versioned deliverables accumulate (v1–v12+), move older versions to a dedicated archive subfolder and keep only the current version in the working directory. This reduces clutter, prevents accidental edits to old versions, and makes the current deliverable immediately identifiable. Apply to all projects with iterative HTML/DOCX/MD deliverables.

## Course Dashboard & Content Extraction Lessons (Mar 27, 2026)

142. **Verify stats from the actual source HTML, not assumptions** — Course landing pages have exact lecture/video/quiz counts in `<span class="clp__stat-value">` tags. Claude Code in Action was 15 lectures (not 91 as assumed). Always read the HTML stats before hardcoding numbers. Wrong numbers propagated across 14 dashboard versions before being caught.
143. **Benefit cards must contain actionable content, not marketing language** — "Master context management strategies" is useless. `/init`, `CLAUDE.md` (3 locations), `#`, `@filename`, `/compact`, `/clear` — these are what the user actually needs. Every bullet should be a command, technique, or decision rule they can apply immediately.
144. **Always auto-open HTML deliverables in browser after creation or update** — Users want to visually verify every change immediately. Don't wait for them to ask "open it." This applies to new versions AND edits to existing versions. Code edits can look correct but render wrong.
145. **Course URL pages are the source of truth for stats, downloaded files are for content extraction** — The Skilljar URL page shows correct lecture count, video hours, and quiz count. The downloaded lecture HTMLs in subfolders are for extracting "What You'll Actually Use" content and "Lecture Breakdown" detail cards. Don't conflate the two sources.
146. **Track completion status accurately — "In progress" if any lecture is downloaded** — If even one lecture file exists for a course, the status is "In progress," not "Not started." Status should reflect actual evidence from the file system, not assumptions about user activity.
147. **All lessons in a completed course must show "Saved" tag** — When a course is marked Completed with a certificate, every lesson in that course's detail card must have the "Saved" badge, even if the individual lecture HTML wasn't downloaded as a separate file. The user completed the course = they consumed all content. Never leave the last lesson without a "Saved" tag in a completed course.
148. **Never trust sidebar stats in saved HTML pages** — Skilljar pages render OTHER courses' stats in JS sidebars. Claude Code in Action showed "15 lectures" which was actually a different course's stats. The real count was 21. MCP showed 16 but was actually 14. Always verify from the actual Skilljar URL page or user-provided screenshots.
149. **YouTube playlists are the source for video durations, not Skilljar sidebar data** — Subagents course showed "0.4h" (~24 min) from Skilljar sidebar, but the actual YouTube playlist totaled 15 min (4:44 + 3:42 + 2:49 + 3:45). Always check the video source directly.
150. **"Estimated time" on Skilljar = reading time, not video duration** — Each lecture page shows two separate values: "Estimated time: 20 minutes" (total reading time) and "(4 minutes)" below the heading (video duration only). Don't confuse or merge them.
151. **Keep the Recommended Learning Path section in sync with course status** — When a course is marked Completed or In Progress in the Course Comparison table, the Learning Path cards must be updated too (green border + "COMPLETED" or blue border + "IN PROGRESS"). This was missed for Course 2 and Course 3 initially.
152. **Use full course names everywhere in the dashboard** — Tab names, Topic Coverage Matrix headers, Course Comparison headers must all show the full official course name. Abbreviated names like "Claude API" or "MCP" cause confusion. All course name headers should also be hyperlinks to the official Skilljar URL.
153. **Record certificate earned date/time in 3 places** — When user reports a certificate: (1) Learning Path card with timestamp, (2) Certificate row in Course Comparison, (3) Status row in Course Comparison. Use the time the user reported it as the timestamp.
154. **Every recurring scan must complete the full update checklist** — Scan → compare → if changes: create version → update folder_snapshot.md → update CLAUDE.md → open browser. Skipping any step (especially snapshot and CLAUDE.md) creates drift that compounds across versions.
155. **Dashboard versions go in a subfolder, latest copy in root** — Store all versions in `Claude_Courses_Dashboard/` subfolder for history. Keep only the latest version copy in the root folder. When creating a new version: create in subfolder → copy to root → recycle previous root copy.
156. **Add external resource links (YouTube playlists, docs) to benefit cards** — When a course has associated YouTube playlists or reference URLs, add them as clickable links at the bottom of the "What You'll Actually Use" benefit card. Users need quick access to these resources.
157. **Hide redundant tracking rows from comparison tables** — If download/progress status is already visible in each course tab (Saved tags + lecture breakdown count), don't duplicate it in the Course Comparison table. Redundant information clutters the overview.
158. **Use Playwright for dynamic web data, not WebFetch** — Platforms like Skilljar hide duration/quiz stats behind dynamic rendering. WebFetch returns empty static HTML. Playwright sees the actual rendered page with all stats (lectures, hours, quizzes). Go straight to Playwright for any learning platform data extraction.
159. **Keep team columns alongside role columns in recommendation matrices** — Teams (Qatar, Mobile, Flutter) represent real organizational units with specific people who need personalized course guidance. Don't remove them — each team member needs to see their column to know which courses to take. Map team recommendations based on the actual roles of team members.
160. **Transpose wide tables — items as rows, not columns** — Any table with 10+ columns causes horizontal scroll and is hard to read. Flip it: put items (courses, projects, etc.) as rows and attributes/roles as columns. A 15-row × 8-column table is far cleaner than an 8-row × 15-column table.
161. **Never leave placeholder dashes when real data exists** — If a course page shows "1.1 hours of video" and "2 quizzes," the catalog must show those numbers, not "—". Leaving placeholders makes deliverables look incomplete and undermines credibility. Always scrape ALL available metadata before publishing.
162. **Sort comparison tables by difficulty level, not enrollment status** — Foundation → Beginner → Intermediate → Advanced with section headers. This shows the natural progression path at a glance. Grouping by "enrolled vs not enrolled" is self-centered; grouping by level is useful for everyone reading the catalog.
163. **Scan on demand, not auto-timer** — User prefers manual "scan" command over 15-minute auto-loop (/loop). Auto-scanning creates noise when the user is actively downloading lectures and wants control over when updates happen. Cancel the cron and wait for explicit "scan" instructions.
164. **Screenshots contain knowledge for benefit cards, not file tracking** — When a user downloads screenshots from course videos, READ the image content and add useful information to the "What You'll Actually Use" section. Do NOT track screenshots as per-lecture download badges — the user doesn't care about the file, only the knowledge in it. Only track actual downloadable resources (notebooks, zips, PDFs) per lecture.
165. **Quiz questions and answers are actionable knowledge — add to benefit cards** — Every quiz question + correct answer is a tested, practical rule that belongs in the "What You'll Actually Use" section. They're not just test results — they're the distilled, exam-worthy knowledge from the course. Include them as "Quiz Insights" in the benefit card.
166. **Scan before quiz, not after** — Always update the dashboard with new lecture downloads BEFORE the user takes a quiz. The user wants to see their progress current before starting the assessment. Don't wait for quiz completion to scan.
167. **In-place dashboard updates are OK during active learning sessions** — When rapidly iterating (downloading lectures, taking quizzes back-to-back), updating the same version (e.g., v31) throughout the day is fine and preferred over creating v32, v33, v34 for each small change. Create new version numbers only for structural changes.
168. **Track ALL downloadable file types, not just notebooks** — Scan for .ipynb, .zip, .pdf, .csv, .md, .py, .txt — any file the user downloaded separately as a course resource. Add them as blue badges (+1 notebook, +1 zip) on the relevant lecture with a "Download:" line showing the filename. Use code-tag styling for filenames.
169. **Course completion requires a full update checklist** — When a course is completed with certificate: (1) Course header → Completed + certificate timestamp, (2) Learning Path card → green border + COMPLETED, (3) Course Comparison table → status + certificate rows, (4) Page 2 catalog → status updated, (5) Stats bar → lectures saved count, (6) CLAUDE.md → version entry updated. Missing any step creates inconsistency.
170. **Deduplicate multi-language course listings** — When a course is offered in 10 languages, show it as ONE row with a "Languages" column listing all variants, not 10 separate rows. Keeps tables readable and prevents false inflation of course counts. Note the primary/English version link.
171. **Complement technical courses with PM-perspective courses** — Technical courses (Anthropic: API, tools, MCP) + PM courses (PMI: governance, ethics, adoption) together create a complete skillset. When building course catalogs, include both perspectives and explain how they complement each other.

### March 29, 2026 — Audit Engine Fix Session (KhairGate V13)

172. **Verify both sides of a PS1↔HTML API contract** — When PowerShell returns `items` but HTML reads `check.details`, the data silently disappears. Always grep both the server response field names AND the client rendering field names before declaring an API "working." Cost: 183 false warnings displayed to user.
173. **Never pre-escape AND use [regex]::Escape()** — PowerShell's `[regex]::Escape()` already handles special chars. If the whitelist array contains `v13\.0` (pre-escaped) and then `Escape()` runs again, it produces `v13\\.0` which matches a literal backslash, not a dot. Use plain strings in the array: `@('v13.0')`.
174. **Case sensitivity across language boundaries** — PowerShell returns `"PASS"` (uppercase) but JavaScript checks `==='pass'` (lowercase). Cross-language APIs must normalize case. Add `.toLowerCase()` on the JS side or return lowercase from PS1.
175. **Health dashboard push requires FULL content sync, not just badges** — Workflow G rule: when pushing to Hub V2 after a drift fix, update ALL health-dashboard content (stories, SP, prerequisites, dates, cards) — not just the verdict line. Partial updates create inconsistency that erodes board trust. Cost: 3 follow-up commits to fix what should have been 1.
176. **Add automated date consistency to audit engine** — Document Control "Updated" dates drifted 4 days behind footer "Modified" dates across 7 files without anyone noticing. Automate: extract footer date, compare with DocControl date, flag if DocControl is older. Added to Check 6 (Cross-File Data Consistency).
177. **SP/Story regex must use global matching with "any match" logic** — `-match` returns only the FIRST occurrence. Files with Phase 0 subset values (133 SP) before the total (3,882 SP) trigger false positives. Use `[regex]::Matches()` to find ALL occurrences, then flag only if NONE match the expected value. Also exclude files that legitimately contain only subset numbers.
178. **Status buttons must persist to HTML file, not just localStorage** — Board members opening the standalone file see DEFAULT states, not the PM's reviewed states. Solution: updater server `/api/save-status` endpoint writes `st-X` class + button text directly to the HTML file. Graceful degradation: if server not running, localStorage still works. Arabic text must be extracted from the file's own `data-labels-ar` attribute (not the JSON payload) to avoid encoding corruption.
179. **Bilingual span count must exclude CSS style blocks** — Audit Check 3 (bilingual balance) was falsely flagging 7 files with 10-27% AR > EN gap. Root cause: CSS selectors like `[lang-ar]{display:none}` and `body.ar div[lang-ar]` contain ~14 extra `lang-ar` references per file for the bilingual toggle. Fix: strip `<style>` blocks before counting. After stripping, all files are under 2.5% gap.
180. **Readiness progress cards must match status button states** — KICKOFF_READINESS_GUIDE showed "Technical Readiness 0%, 2 ADRs pending" while ADR buttons were all `st-1` (Approved). Stale readiness cards contradict the button states and mislead the Board. Added to audit-check agent Check 7: verify readiness card text matches button states.
181. **Hub V2 sync needs a dedicated subagent** — Manually syncing health-dashboard + phase0-poc-readiness after every drift fix caused 7 follow-up commits in one session (ADR-2 missing, prerequisites wrong, reports count wrong, R-08 not updated, variance reports stale). Created `hub-sync` agent to automate: reads V13 state, updates both Hub V2 pages + copies variance reports, returns diff summary.
182. **Browser caches PS1 server responses** — After updating the PS1 audit engine (6→11 checks), the Audit Only button still showed 6 checks even after server restart. Root cause: browser cached the old HTML page. Fix: Ctrl+Shift+R (hard refresh) after every server restart. Consider adding cache-busting headers to the PS1 HTTP response.

## Team Roster & Data Deduplication Lessons (Mar 30, 2026)

184. **One row per person in team rosters — combine groups into a single column** — When a team member belongs to multiple groups (e.g., Qatar Team + Mobile Team + Flutter), list them once with all groups in a comma-separated "Groups" column. Repeated rows inflate counts, cause confusion, and make updates error-prone. If you change a name, you'd have to find every duplicate row. Cost: DevOps_Teams.md had 15 rows reduced to 9 after deduplication.
260. **Always cross-reference team roles across ALL sources before answering** — The same person can have different titles in V15 RACI, Hub V2 pages, and DevOps Teams file. Example: El-Afify = "Odoo Consultant" (V15) vs "Business Analyst" (Hub V2) vs "Implementation" (DevOps Teams). Always read project_team_roster.md first — never answer from a single source. Cost: Multiple incorrect role attributions across sessions before full cross-reference was built.
261. **Maintain a team alias map for name fragments** — Team members are referenced by different name fragments across project files. Key aliases: "Basha"/"Salah"/"Mohamed Basha" = Mohamed Salah Basha (Tech Lead/EM), "El-Afify"/"Afifiy"/"Mahmoud Afify" = Mahmoud El-Afify, "Hatem"/"xhatem" = Hatem Abdelaziz, "Jassem" = Jassim Mohamed. Without the map, commit attribution and role questions produce wrong answers.
276. **Map every point in a team member's commit against PM artifacts before responding** — When reviewing a commit that proposes changes (like Hatem's Proposed Plan with 21 points), map each point against the original PM documents. Missing even one conflict (like Scenario C: BLOCKED vs Foundation proceeds) weakens the PM response and lets scope overreach go unchallenged.

## Localhost Server Management (Mar 30, 2026)

185. **Assign unique ports per project server and maintain a port map** — Multiple localhost PowerShell servers across projects (Outlook, KhairGate, Source-Root) can silently collide if they share the same port. Since many servers expose `/api/status`, an HTML dashboard will "connect" to whichever server grabbed the port first, then fail on project-specific endpoints. Maintain a port map (5050=Outlook, 5051=KhairGate, 5052=Scan, 5053+ next). Before creating a new server, grep all `.ps1` files for `$Port =` to verify no conflicts. Cost: Scan button returned "Unknown endpoint" because KhairGate server was on the same port.

## Executive Reports & One-Pagers (Mar 30, 2026)

188. **Pull from ALL data sources upfront when building executive reports** — Check OKR Dashboard, Outlook analysis, HR folder, DevOps audit, Source-Root index, Monthly Reports, and project-specific folders (e.g., Enhanced Standalone for HUB). Missing a single source means missing context. Cost: 4 additional sources (Source-Root, DevOps, HR, Pearl Pixels Quarterly Report) were missed in the first draft and had to be added iteratively.
189. **Two questions = two visually separate answer blocks** — When a director asks two questions in one request, structure the document with distinct labeled sections (Q1, Q2) and a visual divider between them. Don't blend answers into a single narrative — each question deserves its own clearly labeled block so the reader finds the answer immediately.
190. **Never duplicate data across sections in the same document** — Before finalizing, audit every section — each fact should appear in exactly one place. OKR targets stay in OKR section only. Portfolio items stay in portfolio table. Team development items covered by OKRs don't need their own section. Duplicates waste space and look unprofessional on a one-pager.
191. **Director-facing documents show only pending and planned, not completed** — Filter every item: if it's done, exclude it. The director wants a forward-looking view to understand what's coming, not a retrospective of what happened. Completed work belongs in quarterly reports, not in Q2 plans.
192. **"Hiring and assignment" — not just "hiring"** — When discussing resource needs, always consider both new hires AND reassignment of existing team members. The user corrected "Pending hiring" to "Pending hiring and assignment" for HUB project because some roles may be filled by moving existing staff.
193. **Verify position titles and candidate names with the user** — Don't assume role titles from earlier data. User corrected "Sr. Odoo Developer" to "Sr. Backend — Django & Python" and added Moqbel Elseaedy alongside Yousef Ashraf. Always confirm exact titles and full candidate lists.
194. **Handover completion doesn't mean process compliance** — When told "handover is done", the real issue may shift to process discipline. Hacene's handover was completed, but the actual problem was pushing to production and creating features without team alignment. Ask: "What's the current issue?" not just "Is it done?"
195. **OKR sections for directors need full KR numbers and measurable targets** — Compressed OKR summaries ("RC launch, BMS Sprint 5") lose the measurable specifics directors need. Show all 12 KRs with KR numbers (KR1.1, KR2.3) and exact targets (< 5 bugs, 80%, By Jun 30). The dashboard already has this data — don't compress it away.
196. **Check who's NOT on a platform — the gap matters more than who IS** — When user said "DevOps is mandatory", the key insight was naming the 5 specific people NOT on DevOps (Hossam, Muram, Walied, Ajay, Hacene). Listing the gap by name creates accountability. Don't just say "some members need onboarding."
197. **Risk levels must be confirmed with the user, not assumed** — I set Taqat Property to "Low" risk because docs were ready, but user corrected to "Medium." Risk perception depends on business context the PM understands better. Always default to user's assessment.
198. **8 iterations is normal for director-facing documents — plan for it** — The Q2 PM plan went through v1→v8 with corrections at each step. Each version fixed real issues (wrong status, missing projects, duplicated data, missing OKR details). Don't try to get it perfect in one shot — iterate, but track what changed in each version.
199. **Always save lessons to global_lessons.md, not just memory files** — Memory files are project-scoped and conversation-scoped. Global lessons persist across all projects and all future sessions. If a lesson applies broadly (report building, stakeholder communication, process discipline), it belongs in global_lessons.md.

## Cross-Project Dashboard & Query Copy (Mar 30, 2026)

200. **Use az CLI Bearer auth instead of PAT tokens for DevOps automation scripts** — PAT tokens expire, require manual rotation, and are easy to leak. `az account get-access-token --resource 499b84ac-1321-427f-aa17-267ca6975798` gives a short-lived Bearer token using existing login. Add a `-UseAzCli` switch to scripts so both auth methods are supported. Cost: Every script run previously required pasting a PAT; now it's zero-friction with az CLI.
201. **Copy queries BEFORE dashboard widgets — widgets depend on query IDs** — Dashboard chart widgets reference query GUIDs in their settings. If you copy widgets first, they'll point to non-existent queries and show errors. Always: (1) copy/create queries, (2) capture new query IDs, (3) replace source query IDs in widget settings, (4) then create widgets. Cost: Widgets with wrong query IDs show "Last time you checked, there were no results" permanently.
202. **For targeted widget additions, use direct az REST calls instead of the full copy script** — The full `Query-and-Dashboard-Copy.ps1` copies ALL queries and ALL widgets. When you only need 3 specific charts (e.g., adding frontend team to an existing dashboard), direct `az rest` POST calls are faster, more precise, and don't risk duplicating existing content. Use the script for new project setup; use direct API for incremental additions.
204. **Dashboard API URL requires project-level path for project-scoped dashboards** — Team-scoped dashboards use `/{project}/{team}/_apis/dashboard/dashboards/{id}` but project-scoped dashboards only need `/{project}/_apis/dashboard/dashboards/{id}` (no team segment). If a dashboard returns 404, try removing the team context from the URL before assuming it was deleted. When replicating widgets across dashboards in the same project, create team-specific queries first (update the group filter in WIQL), then update all embedded query IDs in widget settings (`groupKey`, `filter`, `queryId`).

## OKR & KPI Dashboards (Apr 1, 2026)

223. **Never fabricate project-specific data in OKR/KPI dashboards** — Sprint numbers, deadlines, milestone names, and scope definitions must come from the user or a verified source (Azure DevOps, project documents, emails). If the information wasn't explicitly provided, ask — don't invent it. Cost: BMS "Sprint 5" with a "June 15" deadline was assumed and placed as KR1.2; neither the sprint number nor the date was verified, requiring correction after delivery.
224. **Show all active projects by default, then recommend — not the other way around** — When building OKRs or dashboards, first list ALL projects with their real DevOps data (iterations, dates, status). Then recommend which ones are critical. Never cherry-pick projects based on assumptions about which are "biggest" or "most active." The user decides the scope, not Claude. Cost: O1 originally had only 3 KRs (RC, BMS, Aqraboon+Wallet) when 8 projects had active Q2 sprints — 5 projects were invisible.
225. **Never merge unrelated projects into a single KR** — Each project has its own sprints, teams, and timelines. Combining "Aqraboon + Wallet" into one KR because they seemed "secondary" hid the fact that they have completely different cadences (Aqraboon Sprint 1 ends Jun 2, Wallet Sprint 6 ends Apr 23). One KR = one project = one measurable milestone.
226. **Always fetch iteration dates from Azure DevOps `_settings/work` before setting KR deadlines** — The source of truth for sprint start/end dates is `https://dev.azure.com/TaqaTechno/{ProjectName}/_settings/work`. Never assume sprint numbers or dates. Fetch first, then build the dashboard.
310. **Adding a new team to OKR dashboard requires 7 atomic updates** — When adding a new team (not just a member), update all 7 locations together: (1) version tag, (2) stats bar team/member counts, (3) CSS header gradient class, (4) HTML team card with full metrics grid + expandable member table, (5) teamDefs array, (6) memberDefs array, (7) teamInfoMap. Missing any one silently breaks drill-downs or WIQL queries. Always create a new version file — never edit in place.

## Executive Summary Reports (Apr 1, 2026)

227. **Trust DevOps closure dates — never second-guess or flag as "carried over"** — The `Custom.ClosureDate` field is the single source of truth for which month a work item belongs to. If the closure date says March, it's March — even if the item appeared in an earlier report. Do NOT cross-reference against previous report contents or create special "Carried Over / Re-dated" categories. Categorize items by their work type only. Cost: Flagged 4 items as "re-dated" and proposed a "Carried Over" category, delaying the March 2026 report and requiring user correction.

## HTML Dashboard Search (Apr 1, 2026)

228. **Search boxes must use multi-word AND matching, not exact phrase matching** — When a user types "Executive Summary Aya 2026", they expect results where ALL words appear anywhere in the content — not as one contiguous substring. Using `text.includes(fullQuery)` fails when words are scattered across different parts of the same card or section. Always split input into words and check each independently: `words.every(w => text.includes(w))`. Cost: Claude Sessions Index search returned zero results for "Executive Summary for Aya Allah Nady Shapan for 2026" even though the data was present in the same card — the words just weren't adjacent.
229. **HTML dashboards that summarize CSV data lose searchability — bridge the gap** — When an HTML report shows curated summaries but the raw data lives in CSV files, any detail not manually written into the HTML becomes invisible to search. Solution: add a "Deep Search" mode that queries the CSV files via a local server endpoint (e.g., localhost:5050/api/search). This keeps the HTML lightweight while making all raw data searchable on demand. Cost: 4 email records about Aya Allah Nady Shapan existed in cat_other_projects.csv but were completely unfindable from the Outlook v12 HTML search panel.
255. **Search results should replace content, not stack on top** — When a dashboard has many sections, showing search results while keeping all other content visible creates noise. Enter key should hide everything (stats, diagrams, tabs) and show only results. Escape or clicking a result restores the normal view. Implemented via a CSS class toggle on the container.

## OKR & KPI Trend Charts (Apr 1, 2026)

230. **Trend charts must render with 1+ data points, not 2+** — A single-week baseline is still valuable data. Use dots for 1 point, lines for 2+. Never gate chart rendering behind a minimum week count without asking the user. Cost: User clicked Save and saw empty charts with misleading placeholder text.
231. **Add data tables below every SVG chart** — SVG dots/lines are hard to read precisely. A small HTML table below each chart with exact values per week makes the data board-ready and accessible when chart labels overlap.
232. **Heatmap cells need text labels (G/A/R), not just colors** — Color-only heatmaps are ambiguous (yellow could mean "at risk" or "no data"). Always include text inside cells and use gray (#e8e8e8) for "no data" to distinguish from amber/yellow status.

## KhairGate Drift Fix & Autonomous Execution (Apr 1, 2026)

233. **When user says "yes do it" — execute autonomously** — Don't ask "should I do it before or after X?" Follow the workflow rules, decide the approach, execute, and present the result for approval. Asking at every fork wastes time and frustrates the user.
234. **Re-fetch origin AFTER completing a drift fix, BEFORE pushing** — Hub V2 moves faster than drift fixes. V14 was built for E19 (947/3,928) but E2 arrived during the fix (+1/+3), making V14 stale before it was even pushed. Required immediate V15.
235. **If version-bump subagent fails, run sed commands directly** — The version-bump agent for V15 failed because it couldn't execute Bash. Don't relaunch — just run the sed replacements manually. Wasted time on a failed agent is worse than 30 seconds of direct sed.
262. **"No drift" Hub V2 pulls still need a HUB_PULL_HISTORY entry** — Even when Hub V2 commits don't affect standalone PM artifacts, document every commit in HUB_PULL_HISTORY.html with a "NO DRIFT" badge. Every pull must be tracked regardless of impact level. This maintains audit completeness and prevents gaps in the commit trail.

## Portfolio Health Scoring Methodology (Apr 1, 2026)

236. **Always cite framework references before recommending a scoring approach** — When recommending a methodology (e.g., worst-case vs. weighted scoring for RAG aggregation), verify which PM standards actually endorse it before presenting it as a recommendation. PRINCE2 and SAFe endorse worst-case; PMI Portfolio Standard endorses weighted scoring; PMBOK prescribes nothing. Never recommend an approach and attribute it to a standard without verification. Cost: Initially recommended "Average + Floor Rule" as the best approach with no standard backing — had to retract and re-research when challenged for references.
237. **When changing a calculation formula, audit every consumer of its output** — Changing the Portfolio Health Overall formula from worst-case to PMI Weighted Scoring caused the badge text to change from `'On Track'` to `'On Track (2.65)'`. Every JavaScript comparison using exact-match (`=== 'On Track'`) broke silently — Projects On Track counter, weekly snapshot saves, and PM Scorecard roll-ups all failed. Always grep for every reference to the output value and update comparisons (e.g., use `indexOf` instead of `===`). Cost: 3 broken counters across Executive Summary, PM Scorecard, and Save/History that would have shown 0/11 projects on track despite all being green.
238. **Bilingual dashboards must translate tooltips (title attributes), not just text content** — The `data-i18n` translation system only handles `textContent` via `el.textContent = T[key]`. HTML `title` attributes (hover tooltips) are invisible to this system and remain English-only when switching to Arabic. Solution: add a parallel `data-tip-i18n` attribute system and extend `applyLang()` to swap `title` attributes, storing originals in `data-tip-original`. Cost: 20 tooltips across Executive Summary, Work Item Flow, PM Highlights, and Portfolio Health headers stayed in English while all surrounding text was Arabic — inconsistent bilingual experience for CEO presentation.

## PM Framework Coverage Gaps (Apr 1, 2026)

239. **PM²-Agile covers individual project reporting, not portfolio-level health aggregation** — The PM²-Agile course materials (7 sessions, 33 templates) provide Status Report templates (#19, #20) and Development Status Reports but do not prescribe how to aggregate multiple RAG dimensions (Schedule/Quality/Scope) into an Overall score, or how to roll up health across a portfolio. Don't assume a PM course covers every PM topic — verify before citing it as a reference.
240. **PMBOK does not prescribe a specific RAG aggregation formula** — PMBOK 6th Ed Section 4.5.3 (Work Performance Reports) describes traffic-light reporting but explicitly leaves the aggregation methodology to "organizational process assets." When a stakeholder asks "what standard backs this?", PMBOK is not a valid answer for the specific calculation — only for the concept of using RAG status. Cite PRINCE2 Ch.12 for worst-case or PMI Portfolio Standard for weighted scoring instead.

## Quarterly Report Build & Data Pipeline (Apr 2, 2026)

241. **Read the actual template file before replacing values** — The Q2 Performance Report template had v7 KPI values (3,934.51) not v11 values (4,341.61). The replacement script searched for wrong numbers and left stale data in the KPIs. Always read the target file's actual values before writing a replacement script.
242. **Replace entire HTML blocks, never individual numbers** — Replacing "932" globally hit 9 unrelated locations (bar charts, commit counts, table cells). Use regex to match the full containing element (e.g., the entire KPI grid div) and replace it as a block. Bare number replacement is never safe in HTML.
243. **Regenerate every data section in one pass, not incrementally** — Replacing tables but missing trend bars, chart.js arrays, employee cards, and insight cards produces a broken hybrid of Q1 and Q2 data. Build a single script that regenerates all data-dependent sections from the JSON source in one execution.
244. **Check the data before asking the user** — Questions like "Is Water for Mosques paused?" and "Is IndoQat real?" are answerable from the query data. Don't ask the user to validate what the data already shows. Only ask about decisions (team structure, merge rules) not facts.
245. **Verify team structure from the latest authoritative source before building** — Q2 has 3 teams (Egypt/Qatar/Mobile per OKR/KPI v5), not the Q1 structure of 2 teams. Building reports with wrong team grouping required a full rebuild. Always check the current team definitions from the OKR/KPI dashboard or DevOps_Teams.md before starting.
246. **Get answers before generating — not after** — Generating reports with DRAFT placeholders, wrong team structure, and stale content, then asking for review wastes time on both sides. Collect all inputs (team roster, project status, achievements) before running the build script.
247. **Use MCP tools as PAT fallback, not just PowerShell** — When the PAT expired mid-session, MCP tools (`wit_get_query_results_by_id` + `wit_get_work_items_batch_by_ids`) successfully fetched all 366 items. Always have two data paths: PowerShell script (fast, bulk) and MCP tools (auth-independent, always available).
248. **Never show a report to the user without self-validation first** — If the KPI says 3,934 but the JSON sums to 1,386, that's the builder's bug to catch. Run a validation check (KPI values vs JSON aggregates, no Q1 remnants, correct employee count) before opening the report.
249. **Pre-build checklist is mandatory before any quarterly report** — Before generating: (1) read OKR/KPI for current team roster, (2) check who left/joined from task data, (3) confirm new projects from query, (4) read actual template values, (5) confirm team structure with user. Skipping this caused 1.5 hours of rework in Q2 build.
250. **Patching HTML is always slower than full generation** — Incremental find-replace across a 500K HTML file caused 5 rebuild cycles (wrong KPIs, orphan elements, stale bars, stale cards). A single full-generation script that builds all sections from JSON in one pass eliminates this class of errors entirely.
251. **When user says "okay" or "sure", execute immediately** — "Okay build it" means build it now, not "I designed the concept, want me to build it?" Asking for re-confirmation after the user already approved wastes time. This happened twice in the Q2 session (skill creation and refresh explanation).
252. **Read all 3 PM reports before working on any of them** — Monthly Performance, OKR/KPI Dashboard, and Quarter Plan share interconnected PM insights (risks ↔ KRs ↔ data). Working on one without reading the others produces disconnected insights. Always glob for latest version from each folder — never hardcode version numbers.
253. **ClosureDate is PM action, not developer completion** — PM closes items every Wednesday/Thursday. Developer completion = state changed to "Done" (from revision history). All time metrics (lead time, cycle time) must use the Done date from revisions, NOT ClosureDate/ClosedDate.
254. **Fetch form fields, not query columns** — Query columns can be hidden or changed by the user at any time. Always request fields directly in the API call. This makes the data pipeline independent of query display configuration.
255. **Exclude training/meeting items from time metrics** — Items with predetermined durations (training, courses, meetings, onboarding, KT sessions) have perfect estimation ratios by definition. Including them inflates accuracy scores and skews lead time averages. Filter by title keywords.
256. **Sprint length and iteration settings are per-project in Azure DevOps** — Don't assume 2-week sprints globally. Check `https://dev.azure.com/TaqaTechno/{PROJECT_NAME}/_settings/work` per project (or use MCP `work_get_team_settings`/`work_list_iterations`). Sprint definitions affect velocity calculations, carry-over detection, and completion rate thresholds.
257. **Don't duplicate content across sections in the same report** — If Challenges & Lessons and Key Insights both exist, merge into one section. Creating both caused confusion about where metrics-driven insights belong. One section per purpose, no overlap. In the CEO report, Key Insights is the single home for all PM observations.
258. **Tooltips need bilingual support in every HTML deliverable** — The HTML `title` attribute doesn't switch with `switchLang()`. Use `data-en-title` and `data-ar-title` pairs, and add tooltip switching logic to `switchLang()`. Applies to every bilingual HTML report with tooltips and EN/AR toggle.

## Tool & Environment (Apr 2, 2026)

256. **Paths with special characters break Windows shell open commands** — The `&` character in folder names (e.g., "Taqat Trading & Business Solutions") gets escaped as `^&` causing "file not found" errors in cmd/PowerShell/bash. Always copy files to a clean path (e.g., D:\tmp) before opening programmatically. This failed 3 times before the workaround was established.

## Translation & i18n (Apr 5, 2026)

259. **Hub V2 i18n uses textContent, not innerHTML** — The `applyLang()` function replaces `el.textContent`, so nested HTML tags (bold, links, code) get stripped when toggling to Arabic. For elements with mixed HTML, either put `data-i18n` on text-only spans to preserve formatting, or accept plain-text Arabic. Always check how the i18n system works before adding translations to a new page.

## Claude Automation (Apr 5, 2026)

263. **Subagents for large-scale HTML edits plan instead of executing** — When delegating bulk translation or multi-section HTML modifications to background agents, they consistently produce plans rather than making edits. After 4 failed agent launches in one session, the work had to be done directly. For large HTML modification tasks (100+ elements), do the work directly in the main conversation instead of delegating.
279. **Always verify i18n.js syntax with `node -c` after adding translation entries** — A single missing comma breaks the entire translation dictionary and no Arabic toggle works on any page. Run `node -c path/to/i18n.js` after every edit. Cost: missing comma on line 918 broke all Arabic translations across both assessment and POC readiness pages until caught by syntax check.

## Version Archive & History Preservation (Apr 5, 2026)

264. **Never delete old standalone versions — always MOVE to the archive folder** — When user says "remove" or "delete" a version, MOVE it to `Enhanced standalone version/` archive folder, never `rm -rf`. All versions are pushed to GitHub repo (`melshahedpearl/KhairGate-HUB--Kickoff`) for history. Deleting locally loses the local copy even though git has it. Cost: V14 was permanently deleted, had to be recovered from GitHub. The archive folder preserves V1-V14+ for local reference.

## File Organization & Versioning Hygiene (Apr 6, 2026)

265. **Only the latest version stays in the root folder — archive previous versions immediately** — When creating a new Outlook report version (or any versioned HTML deliverable), move the previous version into the archive subfolder (e.g., `Outlook Email Analysis Report/`) before or right after generating the new one. Only the current/latest version should live in the root working directory. Prevents clutter and confusion about which version is active. Cost: v12 was left in root alongside v13, breaking the v1-v11 archival pattern.
287. **Each document version gets its own Created date — no history chain needed** — Each versioned file (v1, v2, v3, v4) is a standalone snapshot with its own "Created" timestamp. Don't add version history tables tracking all previous versions' dates — the file system and CLAUDE.md already track that. Keep the document clean.
298. **Remove departing employees before provisioning, not after** — Eslam Hafez was leaving in 1 week. If all 17 users had been provisioned at once, a $6/month license would have been wasted. Phased batching naturally catches this — always verify the roster against HR status before each batch.
300. **Dashboard cards with version numbers go stale silently** — The Access Upgrade Summary card pointed to v2 while v5 existed. No one notices until they click and see outdated content. Every time a new version is created, update all referencing dashboards in the same session.

## Phased Rollout & License Management (Apr 7, 2026)

299. **Phased rollout splitting factors are dependency-based, not cost-based** — Cost is the same $142/month regardless of batching. The real factors for splitting: who blocks others (dependency), who trains others (role), who is senior vs junior (seniority), does each team have coverage (team balance), and risk reduction (validate licensing channel with small group first).
302. **Users on different platforms don't need the same tool license** — Walid uses GitHub for task tracking, not Azure DevOps. Don't assume every team member needs the same license. Ask about their actual workflow before including them in an upgrade plan.
303. **Running totals cascade on every single user change** — Removing 1 user from Batch 2 changed: user count, Basic count, monthly cost, annual cost, all 3 scenario cards, all batch running totals, and all 4 modals. One person = 20+ edits across the file. Always use a systematic checklist.

## Dashboard Data Integrity (Apr 6, 2026)

266. **Never assume project status — always pull from the actual source** — Assumptions about Relief Center being "Behind" and BMS being "At Risk" were wrong. Real OKR v5 Portfolio Health data showed both ON TRACK (3.00). Always read the source dashboard before assigning status labels. Cost: had to redo all 12 project badges after user caught the mismatch.
267. **Verify sprint counts from DevOps API, don't estimate** — Initial count said "6 active sprints" but querying all 27 projects with `work_list_team_iterations` revealed **13** had current sprints (timeFrame=1). The first query only checked 6 projects. Cost: presented wrong number to PM before meeting.
268. **One project, one name — check for duplicates before displaying** — "Taqat Property" and "Property Management" are the SAME project (DevOps name vs display name). Dashboard showed both as separate entries with different badges. Always verify project name mapping against DevOps project list before creating cards.
295. **Data gaps are more damaging than bad numbers** — Tasks without Estimated Time or Actual Time trigger direct management questions. Management doesn't look for perfect numbers — they look for understandable, complete numbers.

## Leadership Presentation Design (Apr 6, 2026)

269. **Keep leadership dashboards to one page with expandable cards** — Removing 15+ project tabs and using expandable cards on a single dashboard page was the right call. During a meeting, clicking through tabs is slow and loses the audience. Click-to-expand keeps context visible while allowing drill-down.
270. **Align status badges across all dashboards shown in the same meeting** — If OKR Dashboard v5 says "G — On Track (3.00)", the presentation dashboard must say exactly the same. Using different labels (e.g., "HIGH RISK" vs "On Track") for the same project creates confusion and undermines credibility with management.
271. **Understand WHY you clone a UI template before building** — v12 Outlook Email Analysis Report was chosen for its sidebar+cards+search pattern. Understanding the pattern's strengths (navigation, search, badges, expandable sections) led to better adaptation for the meeting dashboard than blindly copying HTML.
292. **Management evaluates the presenter, not just the presentation** — Semir wasn't just reviewing dashboards — he was assessing whether Mahmoud controls the numbers or just displays them. Every management meeting is a dual evaluation (content + competence).
293. **Use measurement language, never justification language in management presentations** — "Deviation" / "time gap" / "corrective plan" vs. "people" / "circumstances" / "pressure." The words you choose signal whether you're in control or making excuses.
294. **The golden rule for management impression: numbers + understanding + plan** — A beautiful dashboard means nothing. Management wants a manager who controls the numbers, understands the deviations, and has a corrective plan.

## Chrome & Local File Access (Apr 6, 2026)

272. **Chrome blocks file:/// links even from other file:/// pages** — `window.open('file:///')`, `<a href="file:///" target="_blank">`, and `--allow-file-access-from-files` flag all fail in Chrome. Workarounds: (1) programmatic `<a>` element click, (2) `window.location.href` for same-tab navigation, (3) localhost HTTP server (`python -m http.server`). Edge handles file:/// links natively.
273. **Delete large commented-out HTML blocks — don't rely on HTML comments** — Old project tabs inside `<!-- -->` leaked visible text because the content contained `-->` in DevOps URL attributes, breaking the comment boundary. HTML comments cannot be nested. Always delete unused code entirely rather than commenting it out.

## Azure DevOps Dashboard Architecture (Apr 6, 2026)

274. **Azure DevOps dashboards exist at two levels — project and team** — Each of the 12 projects has an "Overview" dashboard (project-level, visible at `/{Project}/_dashboards/`). Additionally, Pearl Pixels has 8 team-level dashboards (Backend, Frontend, Implementation, QA-QC, Dashboard, Details, Monthly Report, Project Changed Date) visible at the dashboards directory. Present both levels separately in governance reviews.
275. **Always cross-reference new dashboards with existing deliverables** — The meeting dashboard must be consistent with OKR v5 (Portfolio Health scores), Q2 PM Plan (milestones/risks), and Performance Report (metrics). If badges, numbers, or project names conflict across dashboards shown in the same meeting, management loses trust. Cost: 3 correction cycles during dashboard build.

## Meeting Documentation & Extraction (Apr 7, 2026)

276. **Meeting chat extraction alone captures zero value when all discussion is verbal** — Teams meeting chat was completely empty; 100% of decisions and actions came from the transcript. Always request transcript extraction alongside chat highlights, or you risk missing all meeting outcomes.
277. **AI-generated meeting notes need a "decisions vs. discussion" split** — The Meeting Result doc mixed 17 discussion points + 4 follow-up tasks in one stream. The Transcript extraction properly separated decisions only. Always extract decisions, actions, and discussion notes as separate outputs to avoid burying actionable items in narrative.
278. **Multiple AI extractions from the same meeting produce overlapping but non-identical outputs** — The Next Actions doc and the Follow-Up Tasks from the Meeting Result doc overlap (e.g., SPI/CPI tracking, bi-weekly reviews) but use different wording and granularity. Consolidate into one dashboard but preserve both sources without merging/deduplicating to avoid losing nuance.
279. **Post-meeting action items must distinguish "what to build" from "what to present"** — Actions like "prepare executive presentation" and "prepare for next presentation in 2 weeks" are presentation tasks; actions like "activate SPI/CPI indicators" and "prepare employee performance reports" are system/report-building tasks. Mixing them without categorization makes prioritization unclear.
290. **Always extract management signals separately from meeting decisions** — Decisions tell you "what was agreed"; signals tell you "what management really expects but didn't formally state." The impression analysis revealed the shift expectation from "Dashboard Operator → Owner of Control & Metrics" which wasn't in any formal decision list.
291. **A Before/During/After checklist should follow every management review meeting** — Preparing for the next presentation starts immediately after the current one ends. The 3-phase structure (data readiness → delivery → control) ensures no gap between meetings.

## Data Extraction & Automation (Apr 7, 2026)

280. **Python DOCX with Arabic content fails on cp1252 Windows console encoding** — Python's python-docx library reads UTF-8 fine but Windows console defaults to cp1252, crashing on Arabic characters. Always wrap sys.stdout with io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8') before printing Arabic text from DOCX extraction scripts.

## Dashboard & HTML Architecture (Apr 7, 2026)

281. **Meeting results HTML should be a separate file from the meeting preparation dashboard** — The pre-meeting dashboard (project status, team data, KPIs) serves a different purpose than post-meeting results (decisions, actions, follow-ups). Don't merge outcomes into the preparation file — create a dedicated results file to keep both artifacts clean and independently shareable.
288. **Self-contained deliverables over external references** — Embed the full recommendation as a second tab in the same HTML rather than referencing a separate PDF. One file = complete picture. The audience shouldn't need to open multiple files to understand the full context.
289. **Use clickable popups for drill-down data in dashboards** — Counts like "14 users" or "$130/month for 13 Basic + 1 Test Plans" should be clickable modals showing the full user list with names, emails, and roles. Keeps the main view clean while providing detail on demand without scrolling to another section.
296. **Collapsible sections (details/summary) beat side-by-side cards for detailed data** — 3 batch cards in columns were too cramped for tables with emails and roles. Collapsible rows give full-width tables that expand on demand. Use `<details open>` for the first item, collapsed for the rest.
297. **Popups must match the document's grouping logic** — When the document splits users by batch (not by team), all popups must also split by batch. Showing team-based grouping in popups while the main view uses batch-based grouping confuses the reader.
301. **CLAUDE.md version headers drift from the actual entries below them** — The header said "v3 is the latest" but v4 and v5 entries were already documented below it. Always update the header line when adding new version entries — don't just append.
313. **Scan buttons should auto-copy server CMD and auto-retry connection** — Requiring users to manually start a server, then click scan separately, creates unnecessary friction. Combine "Copy CMD" + "Wait for server" + "Auto-scan" into a single button click so the user only needs to paste in terminal.

## QC Task Management (Apr 8, 2026)

302. **KhairGate Wallet uses 2 QC templates per PBI — always create both** — When a PBI moves to "Ready For QC", create both [QC Test Execution] and [QC Bug Fixing] sub-tasks assigned to Mostafa Ahmed (mahmed@pearlpixels.com). Never create just one; the pair ensures testing and bug-fix tracking are separate work items from the start.
303. **Create QC Bug Fixing tasks proactively, not reactively** — The [QC Bug Fixing] task is created upfront alongside [QC Test Execution] when the PBI enters "Ready For QC", not after bugs are found. This gives the QC team a ready container for tracking fixes without waiting for task creation mid-cycle.
304. **Batch QC sub-task creation for multiple PBIs in the same state** — When several PBIs reach "Ready For QC" simultaneously, create all sub-tasks in parallel and link them to parents in a single batch call. For 4 PBIs × 2 templates = 8 tasks, this saves time vs. creating one-by-one.
305. **Always fetch project task templates before creating tasks — never assume template count** — Each project may have a different number of templates per role (e.g., Relief Center has 2 QC templates: [QC Test Execution] + [QC Bug Fixing], not one generic "QC" task). Always call the templates API first and confirm with the user which templates to use, rather than guessing the structure from role names alone.

## Team Onboarding (Apr 8, 2026)

306. **New team member onboarding requires three updates — memory, teams file, and Azure DevOps** — When adding a new member, update all three: (1) team roster memory for future conversation context, (2) DevOps_Teams.md as the authoritative team directory, and (3) Azure DevOps project team membership via security group API. Skipping any one creates drift between systems.
307. **Use security group membership API to add Azure DevOps team members — not `az devops team member add`** — The CLI does not have a direct `team member add` command. Instead, retrieve the team's security group descriptor via `az devops security group list`, then use `az devops security group membership add` with the group descriptor and the user's identity ID.
308. **Always confirm which Azure DevOps project when names are ambiguous** — "BMS Wallet" could mean KhairGate (BMS) or KhairGate Wallet — two separate projects. Never assume; ask the user to pick the correct one before making changes.

## Access Upgrade & Multi-Reference Document Updates (Apr 8, 2026)

310. **Adding one user to an access upgrade plan requires 20+ cross-reference updates** — A single user addition (Ayush Sakalley to Batch 1) required updating: batch counts, batch costs, running totals, 3 cost scenarios, next steps, implementation timeline, success criteria, 4 modals, user numbering across batches, Tab 2 team counts, appendix summary, action items, footer, and version header. Always enumerate every reference before starting edits to avoid partial updates.
311. **Always collect email address before creating a versioned document — never guess from naming patterns** — Email domains vary across the same organization (e.g., @Taqat.qa vs @taqatechno.com vs @pearlpixels.com). Guessing based on role or team leads to wrong entries that propagate across all modals and tables. Confirm the exact email first, then create the version.

## SLA & Service Agreement Creation (Apr 9, 2026)

314. **Extract all governance facts from source documents before drafting an SLA — never assume metrics, roles, or workflows** — The Relief Center SLA required reading 3 source documents (email + 2 MOMs) to extract 4 provider roles, 4 client roles, authorized requesters, and approval flows. 13 gaps were identified that needed explicit user input before any content could be written. Assumptions in contract documents create disputes later.
315. **When multiple meetings discuss the same topic, cross-reference all sources for contradictions before committing** — The Semir meeting focused on scope tracking and Shafik's bridge role; the Ola meeting defined authorized requesters and reporting lines. Both covered governance but from different angles. Merging without cross-checking would have missed that Ahmad Al-Jamal is the SPOC while Dr. Ola is the approver — two distinct roles confirmed in separate meetings.
316. **Always present multiple SLA metric options when no numbers were explicitly agreed — never commit to a single set** — The Relief Center meetings discussed service categories but zero response/resolution times. Presenting Option A (Aggressive) and Option B (Maximum Commitment) with a selection note lets both parties negotiate rather than the PM unilaterally committing to targets the team cannot meet.
317. **Logo positioning in bilingual HTML must use absolute positioning with RTL override, not centered layout** — A centered logo works for monolingual documents but looks unprofessional in bilingual SLAs. Use `position: absolute; left: 30px` for LTR and `html[dir="rtl"] { left: auto; right: 30px }` to flip the logo to the correct corner per language direction.
318. **UI elements with the same background color as their container become invisible — always use contrasting colors for fixed-position controls** — The EN/AR language toggle had `background: var(--primary)` matching the dark blue header, making it disappear visually. Fixed by switching to white background with dark text. Any fixed-position button overlaying a colored section needs explicit contrast.
319. **When a column has the same value in every row, lift it out into a banner above the table** — Relief Center SLA v6 had "Initial Response: 1 hour" repeated across all 5 priority rows. Moving it to a single info banner above the table reduced visual noise, freed column width, and made the unified commitment more prominent. Rule: a column constant across all rows is a header note, not a column. Applies to SLA tables, KPI dashboards, and status reports.
320. **"Logo" is ambiguous — confirm header logo vs browser favicon vs footer mark before building** — Spent a full round building an inline-SVG header logo when the user actually meant the browser-tab favicon. Three different artifacts (header brand mark, footer mark, favicon), three different sizes, three different file formats. Always ask which one before designing any graphic asset.
321. **Title prefixes (Eng./Dr./م.) must be applied in BOTH languages AND every occurrence — grep both Latin and Arabic name forms before declaring done** — "Eng. Semir Worku" needed updating in 4 places: Section 1 EN, Section 1 AR, Section 12 EN, Section 12 AR. The Arabic equivalent "م." is easy to miss because it's a different character set. When adding/changing any title prefix, search both name spellings before claiming the change is complete.
322. **Client-facing cover emails should avoid the word "support" / "الدعم" — frame the relationship as engagement or collaboration, not a help-desk transaction** — The Relief Center cover email originally read "post-sales support… during the support period". The client preferred a partnership tone, so all four occurrences (2 EN + 2 AR) were rewritten to "engagement period" / "فترة التعاون". Also avoid the substitute "post-sales services" — it still echoes the support framing. Always grep both languages (`support`, `الدعم`) and any euphemisms before delivering. Versioned as `Email_to_Client_SLA_Bilingual_v2.html`.

## Team Assessment & Hiring (Apr 12, 2026)

323. **Reduce follow-up assessment duration when prerequisites are passed and AI tools are permitted** — When candidates have already passed a prior level (e.g., Junior → Mid-Level), they've proven setup familiarity (project structure, Django CLI, models). Combined with AI-assisted coding, boilerplate (forms, views, URLs, auth) is fast. Cut Mid-Level from 2 working days to 5 hours. The real evaluation is the interview where candidates explain every line — duration should reflect coding complexity minus known skills, not a generous buffer.

## Source Attribution & PM Deliverable Rigor (Apr 12, 2026)

324. **Never invent a person's name — grep the repo or ask before putting it in a deliverable** — In the Al-Aqraboon PM Summary popup, I wrote "Ahmed El-Afify" twice (header + validation gate). The correct name is "Mahmoud El-Afify". I had conflated him with "Ahmed Lakosha" (an unrelated commenter in the source file). One `grep "El-Afify"` across the repo would have caught the guess. Rule: before any name ships in a deliverable, verify against a source file or ask the user — don't pull from conversational memory alone. Names in PM artifacts get quoted in meetings and cascade into other documents; the cost of correction is never zero.
325. **Display before push on shared repos — preview is mandatory for client-visible deliverables** — Mid-session on Al-Aqraboon, the user said "display before you push". By then two commits had already landed (121f128, aea60c2). For any edit to a file under version control that's visible to stakeholders (dashboards, proposals, email drafts, HTML reports), the flow must be: draft → show content in chat → wait for explicit approval → commit → push. Even small corrections. The cost of previewing is 30 seconds; the cost of a wrong name or wrong number landing in front of a client is far higher.
326. **Flag unverifiable numbers instead of passing them through as if sourced** — The Al-Aqraboon PM Summary showed "10 Days Stalled" in a KPI card. Arithmetic from the review date (2026-03-26) to today (2026-04-12) was 17 days, not 10. The number was passed through in the first answer without flagging the mismatch. Rule: if a headline number can't be traced to a computation you can show, say so explicitly in the answer ("this number is from the dashboard; I can't verify the start date"). Don't launder unverified counts into authoritative answers — stakeholders will cite them.
327. **Quoted verdicts beat paraphrases when the owner will cite them upward** — The Al-Aqraboon remediation popup embeds an exact quote from El-Afify's Section 11: *"This backlog CANNOT enter any sprint. Estimated remediation: 3–4 weeks of dedicated BA/PO work before Sprint 0."* Originally it was a paraphrase. The verbatim quote is more useful because the owner (Mohammed Salah) can cite it in meetings without re-verifying. Rule: for accountability items where an owner must defend a timeline or scope decision, embed a verbatim quote + source attribution, not a cleaned-up summary.

## HTML Asset Loading & Image Fallbacks (Apr 12, 2026)

328. **Always null-guard `onerror` on `<img>` fallback handlers — `onerror="this.src='fallback.jpg'"` is an infinite-loop bug** — Relief Center SLA v8 had `<img src="../Logo.jpg" onerror="this.src='../Logo -.jpeg'">`. When the primary failed, `onerror` reassigned `src`; if the fallback also failed (or the reassignment re-triggered the error), the handler kept firing because `onerror` was never cleared. The browser entered a load→fail→reassign loop, producing rapid flicker (logo appearing and disappearing multiple times per second). **Rule**: every `onerror` fallback MUST clear itself first — `onerror="this.onerror=null;this.src='fallback.jpg'"`. Apply to all `<img>`, `<script>`, `<link>` fallback patterns.
329. **Inline critical visual assets as base64 data URIs in client-facing HTML deliverables** — Relative paths like `../Logo.jpg` break when (a) the parent folder name contains spaces (`Relief Center` → URL-encoding inconsistency across browsers/file:// contexts), (b) the file is moved or emailed, or (c) the client opens it from a different location. Embedding the logo as `data:image/jpeg;base64,...` directly in the `<img src>` makes the HTML self-contained and portable. Cost: +44 KB on a single-file deliverable — negligible. Benefit: zero broken images regardless of where the file lives. Apply to: logos, signature marks, small icons in any standalone HTML deliverable sent to clients.
330. **Spaces in parent folder names silently break relative asset paths in file:// HTML** — Even when the file exists at the resolved path, some browser/OS combinations fail to load `../Logo.jpg` from a folder named `Relief Center` because of inconsistent space handling (`%20` vs raw space vs `+`). The failure is intermittent and hard to reproduce, which is the worst kind. **Two fixes**: (1) URL-encode spaces explicitly in `src` attributes (`../Logo%20-.jpeg`), or (2) inline the asset as base64 to eliminate the path entirely. Prefer #2 for client deliverables.
331. **When patching a versioned deliverable, also patch every co-existing sibling copy carrying the same bug — `glob` the folder before declaring done** — The Relief Center SLA logo flicker was fixed in `Relief_Center_SLA_v9.html`, but a parallel client-named copy `Relief Center & Pearl Pixels Services level Agreement (SLA).html` lived in the same folder with the identical buggy `<img>` tag. The user opened the client-named copy (not v9), the loop ran for hours in their browser tab, and 111,138 requests accumulated before they reported it again. **Rule**: after fixing a bug in a versioned file, immediately `grep` the entire deliverable folder for the buggy pattern (`onerror`, the broken path, the bad function call) and patch every match in the same batch. Versioning the canonical file does NOT propagate fixes to client-named or email-attachment copies. Applies to: SLA documents, dashboards, proposals, any folder where multiple naming conventions co-exist.
332. **Browser tabs with infinite-loop bugs keep running until the tab is killed — fixing the file on disk does not stop the loop** — After patching the buggy HTML on disk, the user's browser tab was still racking up requests because the page in memory was the pre-fix version. Reload alone may not be enough if the tab is hung; the fix must include "close the tab completely, then reopen". When reporting any infinite-loop fix to the user, the verification instructions MUST start with "close the existing tab" — never assume a refresh is sufficient.

## PM Authority & Tone (Apr 12, 2026)

333. **PM does not "request" review windows from EM — PM defines and informs** — A Project Manager and an Engineering Manager are peers in the project chain, not subordinate/superior. Replace "I am formally requesting a 3–5 day review window" with "I will align with the PM Director on the brief in parallel." The PM defines the validation cycle and informs other stakeholders; they do not ask permission to do their job. In the Al-Aqraboon kickoff reply, the v3 draft asked Basha for a review window — the user rejected it because the tone made the PM look subordinate to the EM. The v4 draft removed the request and replaced it with a definitional statement.

334. **Frame quality gates as validation cycles, not approval requests** — Instead of "until the brief is approved by me and the PM Director", say "once the scope is stabilized through BA, FL, and QA validation". This frames the gate as a quality engineering practice (BA → FL → QA pipeline) that protects delivery, not as a top-down approval bottleneck that blocks the project. Validation language is constructive; approval language is obstructive. Same gate, different perception.

335. **Engage the QA gate AFTER BA finalizes scope, never in parallel or after sprint start** — When a Business Analyst is rebuilding scope from scratch, the QA/QC engineer must be brought into the cycle once the BA completes the rebuild — to identify business gaps and edge cases before the brief is locked. Skipping QA at this gate (or running QA in parallel with BA) lets gaps surface in later sprints, where they cost 10x more to fix. In Al-Aqraboon, Mostafa Ahmed was named in the brief's team table but had zero engagement when Basha sent the kickoff — the PM's reply explicitly called this out as a delivery quality risk and committed to engaging Mostafa after the BA's 3 May milestone.

336. **Don't acknowledge binding commitments before formally reviewing them** — Lines like "the 4-month commitment is noted" or "scope is acknowledged" implicitly accept a timeline, scope, or risk profile that has not been validated. Remove acknowledgment phrases for any commitment that is part of an unreviewed brief. Use neutral openers ("Thank you for the formal kickoff") that thank the sender without binding the PM to anything in the attachment. Implicit acceptance of unreviewed commitments is the #1 way PMs get trapped into impossible deliveries.

## Stakeholder Communication & Emails (continued — Apr 12, 2026)

337. **Never Reply-All on a sensitive governance message — manually narrow recipients** — When replying to a kickoff or commitment email that contains scope concerns, pushback, or naming of underperforming team members, manually set TO=sender + CC=chain-of-authority only. Reply-All exposes the sender publicly to peers/reports and names internal team gaps to people who don't need to know. In the Al-Aqraboon kickoff reply, Reply-All would have aired "scope not clear to the team" and "Mostafa not yet engaged" to 5 unrelated recipients (Hatem, Amr, Jassim, Agha, Hamza), embarrassing Basha publicly and exposing Mostafa by name. Send privately, with PM Director on CC for authority closure.

338. **Conclusion-first for executive emails — under 200 words, fits one mobile screen** — Lead with the PM observation (what's wrong + evidence), not formalities or section breaks. The Al-Aqraboon v3 draft (700+ words, 7 numbered sections, governance precedent paragraph, items-needed list) was rejected as "more huge for management to read on email." The v4 draft (under 200 words, conclusion-first, two-bullet evidence) was accepted. Management reads on mobile in 30 seconds — design for that. Long analytical sections belong in the supporting HTML file, not in the email body.

339. **First-time receipt disclosure is a governance baseline statement** — When a binding document (brief, contract, scope, commitment) is shared with the PM for the first time, explicitly state "this email is the first time X has been shared with me." It establishes the legal/operational baseline that the PM had no input during drafting and protects against implicit acceptance. Distinguish carefully from "the project is new to me" — the project may be 6 weeks old in the repo, but the formal binding document is new. State the receipt fact precisely.

## Memory & Cross-Project Verification (continued — Apr 12, 2026)

340. **Always grep ALL project memories for team roles before answering — not just the current project's memory** — When asked about a person's role or responsibility, run `Grep pattern="<name1>|<name2>" path="C:\Users\DELL\.claude\projects" glob="**/memory/*.md"` BEFORE answering. The master roster lives at `d--KhairGate--HUB--xhatem-Github\memory\project_team_roster.md` and covers all KhairGate + Taqat + Pearl Pixels people across projects. Single-project memory grep is not sufficient — many people appear only in other projects' memories. The Al-Aqraboon Email session required two corrections ("deep search your memory") because the first answer was guessed from email domains instead of cross-project memory grep. Reinforces lesson 227.

341. **Read project-specific status files before drafting any reply email** — When drafting a response to a project email, search the project repository for current status files (`pm_summary.html`, `Project_Readiness_Report.html`, `orientation_gaps_v2.html`, `backlog_validation.html`). These contain the team's most recent verified work and completely change the response framing. In Al-Aqraboon, the BA's 3-wave plan (Mar 30) and FL's 20 findings (Apr 12, same day as kickoff) transformed the reply from "acknowledge and stall" to "acknowledge and cite active progress." Drafting without reading these files produces a generic, weak response that misses the strategic angle.

## Document Sensitivity & Internal Numbers (continued — Apr 12, 2026)

342. **Never quote internal harsh numbers (readiness scores, gap counts, effort variance) in public replies** — Internal readiness scores (e.g., 1.5/10 backlog), gap counts (+190 missing stories), and effort variance estimates (40-50% underestimate) belong in private 1:1s with the PM Director, not in Reply-All emails to the engineering manager. Save those numbers for a private attachment to the PM Director, alongside the full readiness report. Public replies should reference team work (BA plan, FL findings) as constructive evidence instead. Quoting harsh internal numbers publicly humiliates the document owner and poisons the working relationship.

343. **First time you see an attachment ≠ first time the project exists — phrase the disclosure precisely** — Distinguish between "I'm seeing this brief for the first time" (governance fact about the document) and "the project is new to me" (which would be wrong if the project has prior history). The Al-Aqraboon project had 6 weeks of repository history (20 commits, 48 pages) when the formal brief was first shared with the PM. State the receipt fact precisely without implying ignorance of the project, otherwise the disclosure becomes self-undermining.

## Iterative Email Drafting (Apr 12, 2026)

344. **Complex governance reply emails require 4+ iterations with strategic feedback — plan for refinement, not one-shot drafts** — A complex Al-Aqraboon kickoff reply went through: v1 (generic acknowledgment, no context) → v2 (Apr 9 BA-rebuild context integrated) → v3 (brief approval gate framing, governance precedent paragraph) → v4 (executive short, PM-tone shift, BA/FL progress evidence). Each iteration was driven by user feedback I had not had context for at the previous step. Save each version's design intent as an inline callout so the user can compare. Don't try to nail it in v1; plan for iterative refinement. The user reveals the most important context (e.g., "I'm seeing this for the first time", "the team is rebuilding scope from scratch") incrementally as they review your draft.

## Counts vs Itemized Lists (Apr 12, 2026)

345. **Never fabricate itemized lists from cited counts — quote the count, name the source artifact, flag the gap** — When a status report cites a count ("10 remediation actions", "31 dependencies", "+190 missing stories") but does NOT enumerate the items, never invent the list to satisfy a stakeholder request. Quote only what the source literally says (count + buckets/categories), name the missing artifact by filename + commit hash (e.g., `backlog_validation_v2.html` @ `7f6eb9d`), and offer to fetch the real source before the user acts. **Why:** Stakeholders act on whatever you hand them. A guessed list of "10 remediation actions" becomes a fake commitment tracker — worse than saying "I don't have it." Triggered by Apr 12, 2026 Al-Aqraboon Readiness Report review where Salah asked for the 10 backlog remediation items; the report named only the count (10) and four buckets (acceptance criteria, SOP mapping, dependencies, DoR), and the itemized list lived in a file outside the working directory. **How to apply:** (1) Quote the count + buckets verbatim. (2) Name the source artifact precisely (file + commit). (3) Offer to fetch before answering definitively. (4) Never paraphrase a count into an enumeration.

## Source-Root Index & Project Context Discipline (Apr 12, 2026)

346. **Treat the user-visible index as the canonical drift signal, not the JSON baseline** — Index drift can hide when `scan_baseline.json` is fresher than the user-facing `Claude_Sessions_Index.md/.html`. On Apr 12, 2026 the baseline already contained Afriqat, IndoQat, Herbal-Gardens, and AI Call Center, but the index never picked them up — the gap was invisible if you only diffed baseline vs disk. **Why:** The baseline is a staging file, not a deliverable. Stakeholders, future sessions, and the dashboard read the .md/.html index. **How to apply:** After every D:\ scan, diff index counts against the baseline (not just baseline against disk). Any folder in the baseline missing from the index is drift, even if disk and baseline agree.

347. **Every project root needs a tailored CLAUDE.md — no exceptions, no boilerplate** — On Apr 12, 2026 only 1 of 18 indexed project folders (`Water for Mosques`) had a root CLAUDE.md. The other 17 forced every new session to rediscover purpose, key subfolders, and standing rules from scratch. **Why:** Without root CLAUDE.md, Claude burns turns re-exploring things the index already knows, and standing rules (e.g., "Department Workflows = HTML only, never DOCX") get violated by accident. **How to apply:** Every top-level project folder gets its own CLAUDE.md with purpose, key subfolders, standing rules, current file counts, and links to sibling/sub CLAUDE.md files. Tailored from the index entry — never a generic template.

348. **Latest version subfolder gets a CLAUDE.md; historical versions stay frozen** — When a project has versioned subfolders (Udemy v1/v2/V3, Enhanced Standalone v2–v15, khairworld V.1/V.4, Claude_Courses_Dashboard v1–v31), only the *current* version needs its own CLAUDE.md. Adding context files to v1/v2/old snapshots invites editing frozen artifacts and breaks the "version = immutable snapshot" contract. **Why:** Historical versions are reference material, not active workspaces. **How to apply:** When creating per-version context files, scan for the highest-numbered/latest folder and create CLAUDE.md only there. Mention older versions by path with a "frozen, do not edit" note inside the latest version's CLAUDE.md.

349. **Two folders differing only in punctuation need explicit cross-reference CLAUDE.md files** — `D:\KhairGate (Wallet)\` and `D:\KhairGate Wallet\` are different projects with different owners (Sponsorship Module vs. Team Aya), but the index treated them as one for months. **Why:** Filesystem-similar names create governance hazards: edits land in the wrong folder, MEMORY entries collide, drift compounds silently. **How to apply:** Whenever two folders differ only in punctuation, parens, casing, or spacing, both must have CLAUDE.md files that explicitly cross-reference each other ("Not to be confused with X, which is owned by Y"). Update the Source-Root index to disambiguate them as separate sessions.

350. **When you find drift mid-task, write the flag into the artifact, not just the chat** — While creating the v15 Enhanced Standalone CLAUDE.md, I discovered the Source-Root index only tracked v9–v12. The flag was added as an "⚠️ Index Drift" section *inside* the v15 CLAUDE.md, not just verbally to the user. **Why:** Chat messages disappear; future sessions reading the artifact get the warning automatically without needing to re-discover the gap. **How to apply:** When drift is discovered as a side-effect of another task, embed a short "⚠️ Drift" callout in the file you're already creating/editing. Reference the canonical index location and what needs reconciling.

351. **Plan mode can re-engage mid-session — verify state before each non-trivial edit** — On Apr 12, 2026 a `Claude_Sessions_Index.md` edit slipped through after plan mode had silently re-activated. The edit was correct content but violated plan mode's read-only contract. **Why:** Plan mode toggles are easy to miss inside long tool-use chains, especially after a system reminder. **How to apply:** After any system reminder mentioning "Re-entering Plan Mode" or "Plan mode is active", explicitly confirm state in your next thought before any write. Treat plan mode like a transactional flag: re-check, don't assume.

## PM Dashboard Layout & Role Semantics (Apr 12, 2026)

352. **Arrow notation `A → B` in owner/responsibility labels needs semantic agreement before shipping** — I interpreted `A → B` as *"A reports/flows output to B"*. The PM's actual semantic was *"work flows FROM source A TO the person who must act B"*. Same glyph, opposite meaning. Shipping Action #3 with `Mohammed Salah → PM + Ahmed Lakosha` was wrong — the correct form was `Ahmed Lakosha → Mohammed Salah` (Lakosha issued the findings; Salah must review them). The PM called it out directly: "you don't understand the status". Rule: before any arrow goes in a PM label, state the semantic explicitly to the user (flow-of-work vs reporting direction) and confirm in one word. The glyph is free; the ambiguity is expensive.
353. **Nested reply threading inside a table cell beats adding columns for 3-party feedback** — When Basha posted 11 counter-comments to El-Afify's 10 remediation items and the PM then replied to each, the cleanest UI was a light-blue indented block with a `↳ PM Reply — [Name]` label sitting directly under the reviewer's quoted comment inside the same `<td>`. Reads as a conversation thread, no horizontal real estate lost, bilingual-safe, degrades gracefully on mobile. Rule: for 3-party feedback (author → reviewer → PM) inside a data table, use nested reply blocks in the same cell before reaching for extra columns.
354. **Restructure by role-owner beats grouped sub-blocks when a section contains inputs from multiple roles** — When the "Team Inputs Requiring Integration" section had Block A (Lakosha/FL), Block B (El-Afify/BA), and a Readiness Summary all stacked inside one `<div class="sec">`, the cleanest fix was splitting them into two parallel sections — one per role-owner — with distinct color coding (BA=red, FL=purple). Each section then has one owner, one source, one status, one action. Rule: when a section contains inputs from 2+ distinct role-owners, promote each to its own top-level section with its own color identity before the audience has to ask "who owns what".
355. **Replace an unverifiable KPI card instead of defending it** — The "10 Days Stalled" KPI was the counter that triggered the whole Basha review chain, but the arithmetic (2026-03-26 → 2026-04-12 = 17 days, not 10) meant the number couldn't be traced to any source. Rather than hiding it or keeping it and flagging, the right move was replacing it with a factual count: "30 Open Findings" (20 Lakosha + 10 El-Afify). Same card slot, same urgency color, sourced from two real files. Rule: when a dashboard KPI can't be traced to a computation you can show, replace the card with a factual count from a known source rather than defending the unverifiable number or deleting the card entirely.
356. **Disambiguate same-first-name collisions explicitly in client-visible UI** — Al-Aqraboon has two people named "Mahmoud" — *Mahmoud Elshahed* (PM) and *Mahmoud El-Afify* (Senior BA) — plus *Mohamed Basha = Mohammed Salah* (same person, two names in use). Every label that references them must be fully surname-qualified, and where a person has two names in daily use, both must appear (e.g. `Mohamed Basha (Mohammed Salah) — Engineering Manager`). Ambiguity in a single name cost one push cycle today when a validator was labeled "Ahmed El-Afify" instead of "Mahmoud El-Afify". Rule: in any PM-owned artifact that goes to stakeholders, never refer to a team member by first name alone. Surname + role at first mention, consistent through the document, dual identity shown if applicable.

## External Deliverables vs Repository Canonicalization (Apr 13, 2026)

357. **External deliverables drift silently from the repo — variance-check before distribution, never after** — On 2026-04-13, Al-Aqraboon's external brief (`D:\Alaqraboon\Email\AQ_project_brief.html`) was authored by the same solution architect who maintains the repo, but from an earlier snapshot. Headline totals matched (244 stories, 1,111 SP, 6 epics, 12-member team, 4 months) — enough to pass a cursory glance. Six epic-level SP distributions had drifted (E0 −10, E1 −35, E2 −10 stories/−151 SP, E3 −34, E4 −10 stories/−15 SP, E5 +14), and the brief carried business-case data the repo never had (60→10 min processing KPI, 1,200/840 capacity gap, QAR approval tiers, Sandi integration). None of this was visible without a line-by-line diff. **Rule:** any external deliverable authored outside the version-controlled repo must be variance-checked against repo v2 files before distribution. Compare at minimum: story totals, SP totals, per-epic breakdown, team roster, integration list, workflow count, approval tiers, tech stack, headline KPIs. Declare the repo as canonical source of truth. Document the variance in a dedicated bilingual HTML report committed to the repo alongside the deliverable. **Why:** headline-total matches create false confidence; the damage only surfaces when a stakeholder quotes a number the team can't defend.

358. **Git log is free, defensible evidence — use it for provenance tables in trust-critical reports** — When building a variance or audit report, every "source" claim gains defensibility if it carries `git log --follow --format="%ai %h %s"` dates (created + last-updated + commit hash). On 2026-04-13 the Alaqraboon variance report added a Section 12 "Evidence Pages — Source Dates" table with 8 rows, showing that the oldest evidence file (Al_Aqraboon_Workflows.html, 2026-02-25) predated the brief by nearly 2 months, and the newest repo updates (2026-04-12) were 1 day before the report. This single table shifts the report from "Mahmoud's opinion" to "git's verdict" — the reader cannot dismiss the timeline. **Rule:** any report that argues "source A is more current/authoritative than source B" must include a git-sourced provenance table. Cost: one `git log --follow` per file. Benefit: the argument becomes machine-verifiable. Applies to variance reports, audit reports, readiness reports, brief-vs-repo comparisons, scope reconciliation docs.

359. **Bilingual footer stamps use direction-neutral styling — border-top, not border-left/right — to avoid RTL override hell** — On 2026-04-13 we added `Created / Last Updated / Commit` footer stamps to 8 Alaqraboon pages. The stamps use inline-styled `<div>` with `border-top:2px solid #1565C0` instead of `border-left/right`, so no RTL override is needed. Text uses `data-en` / `data-ar` attributes which every page's existing `toggleLang()` function picks up automatically (confirmed working with both `textContent`-style toggles and `innerHTML`-style toggles). Inline styles avoid CSS variable dependencies, so the stamp works regardless of the host page's theme. **Rule:** for any cross-cutting UI element added to pages with different CSS themes and bilingual requirements, prefer (a) direction-neutral CSS properties (border-top/bottom, text-align:center, no margin-left/right), (b) inline styles (no class dependencies), (c) `data-en`/`data-ar` attributes (picked up by existing toggle handlers). Test on at least one page using `innerHTML`-style toggling AND one using `textContent`-style toggling before declaring the pattern safe.

360. **When adding a 3rd dropdown to a 2-way toggle handler, upgrade to N-way arrays — don't hardcode pairs** — Alaqraboon `nav.js` originally had `['v1-dd', 'v2-dd'].forEach(...)` with `otherId = ddId === 'v1-dd' ? 'v2-dd' : 'v1-dd'` hardcoded. Adding a 3rd dropdown (`it-pm-dd`) with the same pattern would have required 3 pairwise branches. The right fix was to promote the list to a single `DD_IDS = ['v1-dd', 'v2-dd', 'it-pm-dd']` array and loop: on click, toggle current, then close all others in the array. Adding a 4th dropdown now requires one array entry — zero logic change. **Rule:** when any binary pair is about to become a triple, stop and generalize to an array. Pairwise special-casing is a maintenance debt that compounds with each addition.

361. **Plan mode re-enters silently after a browser review break — stage files survive but commit is blocked until ExitPlanMode** — On 2026-04-13, after I opened the variance report in the user's browser for visual review (via `start ""`), the harness re-entered plan mode. The user then approved the commit message, and I ran `git add` (which succeeded — the hook fired and staged 12 files). But `git commit` was still blocked by plan mode's read-only contract; I had to call `ExitPlanMode` mid-flow to complete the commit. The plan file was outdated (described the pre-review state), so I also had to edit it before exiting. **Rule:** after any user interaction that might trigger a plan-mode re-entry (opening files in external apps, long pauses, approval breaks, tool-use chains spanning hours), explicitly check for plan-mode state before the next write operation. If plan mode is active, edit the plan file to reflect current state FIRST, then call `ExitPlanMode` to request re-approval. Never try to commit or push while plan mode is active — the harness will not stop you on `git add` but will stop you on `git commit`.

362. **Commit messages: drop narrative framing, keep structural sections — shorter beats rhetorical** — On 2026-04-13 I drafted a commit message with an opening justification paragraph ("Reports that the external brief ... diverges ... Flags repo v2 as canonical source of truth"), followed by 5 structural sections (New deliverable / Navigation / Search / Git provenance stamps / Housekeeping). The user refined it to drop the justification paragraph, drop Git provenance stamps, drop Housekeeping — keeping only subject line + 3 structural sections (New deliverable / Navigation / Search) + co-author trailer. The final message was shorter, purely mechanical, and readable as a changelog entry. **Rule:** commit message bodies should describe WHAT changed (files, categories, counts) in structural bullet sections, not WHY it changed (the "why" belongs in the PR description, the variance report, or the linked issue). Narrative framing ages poorly in `git log`. Keep the subject line for the headline and the body for a changelog-style breakdown. If you find yourself writing a paragraph, move it to the deliverable itself.

## Source-Root RAG Sync & Live-Scan Discipline (Apr 13, 2026)

363. **"No new folders" is one check out of three, not the whole coverage audit** — On 2026-04-13 I answered "no new folders on D:\" when the user asked if RAG needed updating. That was technically correct but missed two other classes of drift: (a) existing indexed folders missing their root `<folder-name>.md` because the last batch script skipped edge cases (see lesson 365), and (b) new *sub-folders* created inside already-indexed parents since the last baseline scan — which is what held up `D:\Project Management\Project management Quarter Plan\Projects Handling Review Meeting 6-4-2026` that day. **Rule:** every RAG coverage audit must run all three checks: (1) new top-level folders vs `scan_baseline.json`, (2) every HTML-bearing folder's root `<folder-name>.md` via Glob, (3) diff the live RAG list (`curl /ui/projects/list`) against the current index sessions — not just the baseline file. Skipping any of the three yields false-negative answers that look thorough but aren't.

364. **RAG entries are per sub-session, not per top-level folder — map accordingly, never add root entries on top of sub-entries** — On 2026-04-13, checking the RAG at `http://127.0.0.1:21420/ui/projects/list` revealed 42 entries organized per sub-session (KhairGate HUB = 11 entries, Project Management = 9 entries, DevOps = 4, HR = 2, Personal = 3), not one-per-D:\-folder. After creating a new root-level `D:\KhairGate (HUB)\KhairGate (HUB).md` to close a gap from the Apr 12 batch, I initially planned to register it as a new RAG project — but that would have duplicated chunks already covered by the 11 existing sub-entries, bloating the index and degrading search relevance. **Rule:** before adding any new RAG entry, check whether its path is already covered recursively by an existing entry. If yes, the folder MD becomes documentation-only (for humans and future Claude sessions), not a RAG entry. Map RAG granularity to `Claude_Sessions_Index.md` sub-section level, never to top-level folder.

365. **WebFetch auto-upgrades HTTP→HTTPS — it WILL fail on any localhost dev server** — On 2026-04-13, `WebFetch http://127.0.0.1:21420/projects` returned `ECONNREFUSED` despite the RAG server being up and the URL loading fine in Chrome. The tool silently upgrades HTTP to HTTPS (documented in its description), and local dev servers don't bind HTTPS. Switching to `curl -s -m 5 http://127.0.0.1:21420/...` via Bash worked immediately. **Rule:** for any HTTP-only localhost service (RAG UI 21420, Outlook Update Server 5050, Standalone Update 5051, Taqat 5052, D Drive Scan 5053, or any PowerShell HttpListener), always use `curl` via Bash, never WebFetch. Document the port-to-service map in MEMORY.md so future sessions don't waste turns rediscovering this.

366. **Baselines go stale the day they're written — always cross-check against the live source** — `scan_baseline.json` last updated Apr 12, 2026 missed a folder created Apr 13 at 06:50 (20 hours later). Any "compare against baseline" audit on Apr 13+ gives false negatives for same-day additions. **Rule:** treat `scan_baseline.json` as a fixed point-in-time snapshot, not a live truth source. Every coverage check must also query the live state: `ls D:/<folder>` for disk truth, `curl /ui/projects/list` for RAG truth, `grep Claude_Sessions_Index.md` for index truth. Reconcile disagreements by trusting the live source and updating the baseline last. Never answer "nothing new" based on a baseline older than ~24 hours without live verification.

367. **Batch scripts that operate on folder names silently skip names with parentheses, spaces, `&`, or Arabic characters** — On 2026-04-12 a batch created 37 `<folder-name>.md` files across every indexed session folder. On 2026-04-13 a coverage check revealed `D:\KhairGate (HUB)\KhairGate (HUB).md` was missing — almost certainly because the script's glob or regex didn't escape the parentheses, and the folder was silently skipped without raising an error. **Rule:** any batch operation over the filesystem must be tested against edge-case folder names *before* the batch runs (parens, spaces, `&`, single quotes, Arabic characters like `D:\Global Lessons\مشاريع`). Add an explicit post-run verification step that counts output files against expected-input count and reports any gap — silent skips are the worst failure mode because nothing surfaces until a coverage audit weeks later.

368. **Remove-then-re-add for RAG updates; direct-add for new entries** — On 2026-04-13 the user clarified the rule: when a RAG entry already exists and its source MD has changed, remove the existing entry first then re-add, because RAG caches metadata and in-place updates can serve stale chunks. This rule applies only to *existing* entries — brand-new entries (like the `project-management-quarter-plan` added the same day) go straight through `/ui/projects/add` with no removal needed. **Rule:** (a) before any RAG add operation, check via `curl /ui/projects/list` whether the target path or ID already exists; (b) if it exists, DELETE first via `/ui/projects/<id>/remove`, confirm deletion, then POST the add; (c) if it doesn't exist, POST directly. Never update in-place. Document both the remove and re-add in the session changelog so future sessions can trace what was re-indexed when.

## Publish vs Memory Classification (Apr 13, 2026)

369. **Every piece of user information has one of two destinations — classify before acting, default to memory, ask when unclear** — On 2026-04-13 while building the Al-Aqraboon Change Request HTML (CR-2026-04-13), I embedded an internal PM governance rule (about which files are "PM governance artifacts" vs "scope sources") directly into Section 2 of the team-facing CR document. The rule was true and useful as internal discipline, but it was NOT supposed to be visible to team owners reading the CR — it was background context for me, not content for the deliverable. The user had to explicitly correct me: *"remove this word ... it's for your memory only, not to publish."* Then the user taught me the meta-rule: *"Let's agree that there are things that can be published and that there are other things to teach you and provide you with information to keep in your memory. You should keep that in mind when I talk to you."* **Rule:** Every piece of information the user shares during a conversation has one of two destinations — (a) **Publish:** goes into a deliverable (HTML report, email, commit message, public document) where stakeholders will see it; or (b) **Memory only:** stays in internal memory files, plan notes, or mental context, never appears in any user-visible artifact. **How to apply:** (1) When user provides context or rationale → default to **memory only** unless they explicitly say "add this to the file" or the context is obviously part of the deliverable being built. (2) When user requests a specific content change → default to **publish**, but confirm whether associated reasoning is meant to be published alongside. (3) When I discover a justification or exclusion rule during exploration → treat it as memory by default; surface it in chat for user review but do NOT auto-embed it in the deliverable. (4) When user says "remove X from the file" → interpret it as "keep X in memory, don't show stakeholders" unless they explicitly say "forget X entirely". (5) When in doubt, **ASK** — better to confirm "is this for the file or for your memory?" than to over-publish internal context or under-publish intentional content. **Publish signals:** add / include / show / mention / note in the document. **Memory signals:** remember / keep in mind / for context / understand that / to teach you / so you know. **Ambiguous — always ask:** "note that" / "by the way" / "also" / "additionally". Applies across ALL projects and sessions. Saved to `feedback_publish_vs_memory.md` in Alaqraboon project memory and as a standing rule in both `d:\Alaqraboon\CLAUDE.md` and `C:\Users\DELL\.claude\CLAUDE.md`.

## PM Governance Boundaries & Assumption Discipline (Apr 13, 2026)

370. **PM orchestrates, never authors team-owned artifacts** — On 2026-04-13 I proposed that the PM directly edit `backlog_v2.html`, `architecture.html`, `technical_proposal_v2.html`, `proposal_v2.html`, `Al_Aqraboon_Workflows.html`, and `basis_of_calculation_v2.html` to backport business-case data and fix title references. The user correctly pushed back: *"you need me as IT project manager to make these changes on the pages that team created and updated on repo, right?"* The answer is no. Each of those files has a specific authoring owner (BA, FL, SA, EM) who maintains them through their own PR workflow. When the PM edits a team-owned artifact directly, it (a) overrides the owner's authority on their own file, (b) violates the canonical-source rule ("repo v2 is team-authored"), and (c) creates future drift when the real owner next touches the file and overwrites or disagrees with the PM's changes. **Rule:** PM's role on team-owned files is **flag → request → review**, never author. Use a formal Change Request document (HTML or Markdown) to assign items to owners with acceptance criteria; owners push their own PRs; PM reviews each. PM may directly author only: (a) files the PM created (PM Summary, variance reports, change requests, dashboards), (b) governance / memory / CLAUDE.md / plan files, (c) files where the author explicitly delegates edits back to PM. The Change Request pattern from 2026-04-13 (`CHANGE_REQUEST_2026-04-13.html`) is the canonical example — it tells each owner WHAT to edit, WHERE to edit it, WITH what acceptance criteria, and nothing more. PM does not embed owner's content inside the Change Request.

371. **Never propagate PM assumptions across multiple documents — caught early or corrected with discipline** — On 2026-04-13 I invented deadlines (5 / 10 / 15 working days) for the 6 CR items when drafting `CHANGE_REQUEST_2026-04-13.html`. Those invented numbers then propagated into `pm_summary.html` (3 locations), the variance report Section 7, `change_request_status.md` memory, and the plan file. The user eventually caught them: *"who is add the Deadline for every topic? me or someone of the team or your assumption?"* Full admission + removal across every document took a dedicated commit (`001489a`) to undo. **Rule:** Any numeric commitment — deadlines, effort estimates, story points, dollar amounts, capacity figures, dates — MUST come from a real decision-maker (PM, BA, owner, customer) OR be explicitly labeled `TBD — owner to confirm` at first mention. Never invent AND never propagate invented numbers across documents where they become load-bearing. **Detection signal:** if you can't answer "where did this number come from?" with a specific person + date, it's an invented number and needs either (a) removal, (b) TBD marker, or (c) a rationale + disclaimer. **Prevention:** when drafting governance documents (CRs, RFCs, scope summaries), separate "facts known from source files" from "values that need decisions." Mark the second category clearly BEFORE any other file references them.

372. **Verify the team's communication model before designing handoffs** — On 2026-04-13 I offered to draft Slack / Teams / email handoff messages for the CR items, assuming push-style communication. The user clarified: *"the team have access on the report and they get pull and do his works"* — the team pulls the repo and reads `pm_summary.html` + `CHANGE_REQUEST_2026-04-13.html` directly. The repo IS the communication channel. This required a new commit to fix pm_summary.html Next Actions item 9 and add a "Pull-based Workflow" info-box. **Rule:** before designing any handoff / notification / coordination mechanism, explicitly ask or verify HOW the team receives information. Three models exist: (a) **Push** — PM sends messages via Slack/Teams/email and team reacts, (b) **Pull** — team checks the repo/dashboard on their schedule and reads updates there, (c) **Sync** — updates happen only at standups/meetings and the team brings back notes. Each model dictates different content placement: push needs standalone messages with context, pull needs centralized hub pages that the team bookmarks, sync needs agenda items and meeting minutes. Wrong model = wrong content structure = extra commits to fix later. **How to verify:** ask "does the team see repo changes automatically, or do I need to tell them?" The answer determines whether content belongs in a message OR in the repo.

373. **Silent fix patterns extend by category — maintain consistency once established** — On 2026-04-13 the user established "silent fix" as the norm for Mohammed Salah title corrections (pm_summary.html lines 270 + 354). Over the next several turns, the silent pattern extended organically to `basis_of_calculation_v2.html` (3 locations), `AQ_project_brief.html` (Section 3 table + Action #11), variance report Section 7 (the Action cell for Salah's row), variance report line 169 author label, and `pm_summary.html` line 440 (role enumeration). Each extension was a user decision, but the user expected consistency — if I had marked one of them explicit while others stayed silent, the git log would become inconsistent and confusing. **Rule:** once a content category is marked "silent" (commit message does not name the change, even though the diff shows it), all future work in that same category stays silent unless the user explicitly reverses the policy. Track the silent-fix category in memory (e.g., `silent_fix_category_title_corrections = [pm_summary.html, basis_of_calculation_v2.html, AQ_project_brief.html, Brief_vs_Repo_Variance_Report_v1.html]`) and check against it before naming any future fix. **Why consistency matters:** git log readers infer intent from patterns. If 5 silent fixes are followed by 1 explicit one, the explicit one looks anomalous and raises questions the PM may not want asked.

374. **"What about X?" questions need antecedent clarification, not interpretation** — On 2026-04-13 the user asked *"what about if highlighted this in the file pm_summary.html?"* with no clear antecedent for "this" (could have meant: the pending task list, the handoff instructions, a specific CR item, a summary status, or something I couldn't guess). I jumped to interpretation based on the most recent topic in the conversation. Multiple clarification rounds followed before the real intent surfaced: integrate CR items into the existing Section 1 BA Stream, Section 2 FL Stream, and Next Actions. **Rule:** when a user question contains ambiguous pronouns ("this", "that", "it") without a clear antecedent, ASK — don't interpret. Offer 3–4 candidate interpretations via `AskUserQuestion` tool or plain text. Cost of asking: 1 turn. Cost of guessing wrong: 2–4 turns of backtracking + potentially wasted file edits. **Signal phrases:** "what about this", "should we add that", "maybe do X" — all antecedent-light, all should trigger a clarification before action.

## Commit Discipline & Content Governance (Apr 13, 2026)

375. **Git diff ≠ commit message — "silent" means unnamed, not invisible** — On 2026-04-13 when silently fixing Mohammed Salah's title, I framed the action as "silent" because the commit message did not name the change. But the git diff still showed every modified character. When the user asked "will owners see this?", the honest answer was YES in the diff (anyone running `git show <hash>` sees it) but NO in the one-line summary. "Silent" is a property of the commit MESSAGE, not the commit CONTENT. **Rule:** when discussing silent edits with a user, always clarify: silence applies only to the commit subject line and body text. The diff remains fully visible to anyone viewing the commit. Silence is for de-emphasis, not for concealment. If the user wants true concealment (changes that don't appear anywhere), that requires NOT committing at all — which defeats the purpose of version control. Be explicit about this distinction the first time silence is proposed in a session.

376. **Squash related small commits BEFORE the first push, not after** — On 2026-04-13 three small commits (`db3f3d2` silent line 440 fix, `001489a` deadline removal + CR highlights, `3fefee3` pull-workflow reframe) stacked locally before any push. The user requested squash-then-push, which was trivial because nothing had reached `origin/master` yet (`git reset --soft` + recommit, zero force-push risk). If those three commits had each been pushed individually first, the squash would require `git push --force` — a dangerous operation on `master`. **Rule:** when related work in a single session produces multiple small commits touching overlapping files, offer to squash them into one clean commit BEFORE the first push. Check with the user: "these 3 commits are all on the same topic — squash into one before push, or keep separate?" The answer determines whether the final git log tells a clean story (1 commit = 1 feature) or a messy one (3 tiny commits that the team has to read through). **Gate:** squash is safe only before push. After push, never squash without explicit force-push authorization.

377. **Commit messages with explicit "Rationale:" paragraphs are self-documenting — adopt as default for non-trivial explicit commits** — On 2026-04-13 three commits (`001489a`, `3fefee3`, the final squashed `92bcd78`) each ended with a `Rationale:` paragraph explaining WHY the change was made, not just WHAT changed. Example: *"Rationale: Team has direct repo access and pulls regularly. Previous phrasing incorrectly assumed push-style communication. Original CR deadlines (5/10/15 working days) were PM placeholder estimates never validated with owners and have been removed per no-assumptions rule; owners negotiate delivery dates at kickoff."* This style makes the commit self-documenting — a future session reading `git log` doesn't need external context to understand why the change happened. It's especially valuable when the change reverses or corrects an earlier commit (like removing deadlines after introducing them). **Rule:** for any non-trivial explicit commit where the WHY matters (policy reversal, governance change, assumption correction, scope refinement), add a `Rationale:` paragraph at the end of the commit message body. Keep the rationale under 4 sentences. Skip the rationale for (a) silent commits — they should stay silent in the message, (b) trivial mechanical changes (typo fixes, formatting), (c) commits where the subject line alone is self-explanatory. When in doubt, add it — the cost is negligible and the future value is high.

## Process Discipline — Rule Lookup & Version Archival (Apr 13, 2026)

378. **"No rule exists" requires a 3-location search, not 1** — Before telling the user a rule or convention doesn't exist, grep ALL three locations: (a) project-scoped memory folder `C:\Users\DELL\.claude\projects\<sanitized-cwd>\memory\`, (b) `D:\Global Lessons\global_lessons.md`, (c) global CLAUDE.md at `C:\Users\DELL\.claude\CLAUDE.md`. Use broad keywords — the rule may use different wording than the user's question (e.g., rule says "version" when user asks about "archive", rule says "standalone folder" when user asks about "per-file versioning"). **Why**: Apr 13, 2026 — told user "no rule about archiving old versions into a subfolder" after grepping only the project memory folder; lesson #222 ("Archive old versions, keep only the latest in the root folder") was sitting in global_lessons.md line 272 the whole time, plus #218 for same-step discipline, #264 for "move not delete", and #155 for the Claude Courses precedent. A confident negative answer based on one search location is worse than saying "let me check" because the user trusts it and acts on it. **How to apply**: whenever the user says "as we have in memory, right?" or "isn't there a rule for this?" or "should we do X as usual?", run parallel greps across all three locations before answering. Cite the lesson number when one is found. If no match in any location, say "I checked memory, global_lessons, and CLAUDE.md — no rule found" — mention what you searched so the user knows the answer is grounded.

379. **Version archival is part of the version-bump definition-of-done, not a follow-up task** — Existing lesson #218 ("Archive old version folder in the SAME step as creating the new one") was written for Enhanced Standalone `KhairGate HUB V13 → V14` folder moves, but the same discipline applies to per-file version chains: `Relief_Center_SLA_v10.html → v11.html`, `OKR_KPI_Dashboard_vN.html → v{N+1}.html`, `Weekly_Meeting_Agenda_vN.html`, `Outlook_Email_Analysis_Report_vN.html`, `Claude_Courses_Dashboard_vN.html`, etc. **Why**: Apr 13, 2026 — created `Relief_Center_SLA_v11.html` and stopped there, leaving v1–v10 cluttering the SLA root; user had to prompt me the next turn to archive them into `Relief Center SLA - Old Vesrion/`. This violated both lesson #218 (same-step) and #222 (keep only latest in root). **How to apply**: the version-bump workflow is a 4-step single turn — (1) copy v{N} → v{N+1}, (2) apply edits to v{N+1}, (3) move v{N} to the archive subfolder, (4) update the project memory MD. All four steps in the SAME turn, never spread across turns. For the first archival of a version chain, create the archive subfolder in that same turn and ask the user for the folder name once (then reuse it verbatim for all future bumps of that project — respect the user's spelling even if misspelled, e.g., "Old Vesrion"). For projects where an archive folder already exists, skip the naming question and just move.

## PM Governance Execution & External Handover (Apr 14, 2026)

380. **Content authorship vs mechanical metadata — governance override rule** — The "PM doesn't directly edit team-owned files" rule (lesson #370) exists to protect **content authorship** where owner expertise is required. A purely mechanical change — renaming a team member, fixing a typo, updating a date, replacing a stale email address — is **metadata maintenance**, not authorship, and the PM can execute it directly. **Why**: Apr 14, 2026 — CR-07 assigned an Amr Atya → Mohammed Chtioui rename across 10 team-owned files to 3 different owners (BA + SA + PO), which would have sat Pending for days waiting for 3 separate trivial PRs. PM executed the mechanical rename directly in one commit (`f343156`), marking CR-07 Resolved with rationale "mechanical roster rename, owner sign-off waived as non-content change." **How to apply**: before deciding whether to route a change through a CR or execute it directly, classify: (a) **content change** (scope, architecture, KPIs, approval chains, business logic, AC language, story points) → always CR; (b) **metadata change** (team member names, dates, email addresses, rate cards when values are dictated externally, typos, version labels) → PM may execute directly with explicit rationale in the commit body. Document the waiver explicitly in the commit message so the governance model stays legible in git log.

381. **Add new CR items to the open CR document — don't spawn a second one** — When a new cross-cutting issue surfaces mid-sprint and an open Change Request document already exists, add a **new row** to it rather than creating a separate CR file. Keeps all governance in one tracked artifact and preserves the stat-strip counters as a single source of truth. **Why**: Apr 14, 2026 — UI/UX rename was added as CR-07 to the existing `CHANGE_REQUEST_2026-04-13.html` alongside CR-01 through CR-06 (not as a separate `CHANGE_REQUEST_2026-04-14.html`). Avoided CR sprawl and kept the team looking at one file. **How to apply**: when adding a row, atomically update (a) the row itself with proper ID, owner, target files, acceptance criteria, and status, (b) the stat-strip counters (CR Items count, Target Files count, Owners count if new owners are introduced), (c) the section heading text if it enumerates IDs (e.g., "CR-01 through CR-06" → "CR-01 through CR-07"). Missing any of the three creates inconsistency the next reader will spot immediately.

382. **Standalone-ify pattern for external handover copies** — When sending a repo-internal HTML document to an external team (UI/UX agency, client, auditor), create a standalone copy in a separate folder outside the repo and **strip all repo-specific dependencies** before sending: `<script src="nav.js">`, `<link rel="stylesheet" href="comments.css">`, `<script src="comments.js">`, any `<script id="embedded-comments">` JSON blob, and the `<nav class="nav-bar"></nav>` element. Add a prominent handover note banner at the top explaining the recipient's expected action and any scope caveats. **Why**: Apr 14, 2026 — cloned `_Website_Concept_en.html` from the Alaqraboon repo into `D:\Alaqraboon\UIUX_Handover\Detailed_Website_Concept_Al_Aqraboon.html` for the UI/UX designer. Without stripping the nav.js and comments.js dependencies, the file would have shown broken navigation and console errors to the recipient. The bilingual (EN/AR) language toggle still worked because it was self-contained in the same file. **How to apply**: (1) copy the file to a dedicated handover folder outside the repo, (2) grep for script/link references pointing to repo-only assets and remove them, (3) test-open in a fresh browser with devtools open — verify zero 404s or JS errors in console, (4) add the handover banner (orange/red callout with "Note to [Team]" heading) at the top of the body. Never send a repo-internal HTML file directly to an external team.

383. **Multi-repo find-and-replace requires per-repo `git status` check** — When a project spans multiple git repos (a common pattern for Alaqraboon = portal repo at `D:\Alaqraboon\Alaqraboon repository\` + Readiness repo at `D:\Alaqraboon\Enhanced standalone version\`), a cross-cutting find-and-replace may modify files in both. Running `git status` in only one repo silently misses changes in the other. **Why**: Apr 14, 2026 — today's Amr Atya → Mohammed Chtioui rename touched `pm_summary.html` in the portal repo AND Readiness Report V.1/V.2 in the Readiness repo. Almost missed the Readiness changes because `git status` in the portal folder only showed 5 files. Needed 2 separate commits (`0597006` on portal master, `f76c4c9` on Readiness v1) and 2 separate pushes. **How to apply**: before committing after any cross-cutting edit in a multi-repo project, run `git status` in **every repo folder** that could have been touched. Maintain a memory note listing all repo roots for each project (e.g., `repo_separation.md` for Alaqraboon). After editing, walk each repo root in sequence and either commit changes or confirm clean. The `Email/` folder (which is often not a git repo at all) is a third class — files there get edited but don't get committed anywhere; track them mentally as "live on disk, not versioned."

384. **`ls` the parent before `mkdir` new handover folders — case/separator variants create silent duplicates** — Before creating a new subfolder for handover materials, plan notes, or any session scratch space, **list the parent directory first** to confirm no similarly-named folder already exists. Subtle variants — `UI_UX_Handover` vs `UIUX_Handover`, `Claude Courses` vs `Claude_Courses`, `v2` vs `V2` — are trivially easy to miss and create parallel folders that fragment the work. **Why**: Apr 14, 2026 — created `D:\Alaqraboon\UI_UX_Handover\` not knowing `D:\Alaqraboon\UIUX_Handover\` already existed from 12 minutes earlier in the same session (a prior Claude run). Had to consolidate files afterwards and delete the duplicate. Root cause: did not run `ls D:\Alaqraboon\` before the `mkdir`. **How to apply**: add an `ls` or `Glob` step as the first action in any "create a new folder" workflow. If any folder with a similar name exists (case-insensitive, whitespace-insensitive, underscore/hyphen-insensitive), reuse it instead of creating a new one. If uncertain whether two similar names are the "same" folder conceptually, ask the user which to consolidate into before doing any writes.

385. **Kickoff email deadlines belong in 3 places** — When a kickoff email sets a deadline, embed it in **three distinct places** so recipients cannot miss it while scrolling: (a) the meta block at the top alongside To/From/Date/Subject/Attachment, (b) a prominent color-coded callout box (red/orange bordered) right after the greeting paragraph, (c) a small inline tag under each deliverable heading ("Due within: [window] by [date]"). Single-placement leaves too much room for oversight. **Why**: Apr 14, 2026 — the UI/UX handover email initially had no deadline. After the user specified "all UX prototypes, 14 Apr → 14 May (one month)", I embedded it in all three places: meta row, red "⏰ Overall Deadline" callout, and per-delivery due tags. This survives both skimming and careful reading. **How to apply**: every time a kickoff or handover email includes a delivery commitment, build the three-place pattern from the start — don't wait for it to be retrofitted. In HTML emails, define two callout classes (`.note` for informational orange, `.note-deadline` for red) and a `.due-tag` for inline badges. Reuse across projects.

386. **Email drafts save as `.html` not `.md` — Outlook copy-paste requires preserved formatting** — When drafting an email that will be sent via Outlook (or any Microsoft 365 mail client), save the draft as `.html` rather than `.md`. Markdown is excellent for reading in a code editor, but it pastes into Outlook as plain text: headers lose their styling, bullets collapse, bold/italic disappear, and callout boxes become indistinguishable paragraphs. HTML pastes into Outlook's rich-text editor with formatting preserved — headers stay bold, bullet lists stay bulleted, colored callout backgrounds stay colored. **Why**: Apr 14, 2026 — drafted `Email_to_UIUX_Team.md` first for quick iteration, then converted to `Email_to_UIUX_Team.html` once structure was stable, so the user could do Ctrl+A / Ctrl+C in the browser and paste directly into Outlook with the red deadline callout and orange session-note boxes intact. **How to apply**: (1) use `.md` only during early drafting when structure is still changing and diffs matter; (2) the moment the content is stable enough to send, convert to `.html` with inline CSS (not external stylesheet — Outlook strips many external references); (3) define a minimal style block with just the classes you use (body, h1/h2, .meta, .note, .sig); (4) open in Chrome/Edge before sending to verify visual rendering, then copy-paste into Outlook. Keep both `.md` and `.html` in the same folder so future updates can edit the `.md` and regenerate the `.html`.
