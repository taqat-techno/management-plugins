# Global Lessons Learned — Cross-Project Best Practices

> Accumulated from real project sessions. Apply these across ALL projects.
> Last updated: April 29, 2026 (567 lessons, 106 categories)
>

## Report Writing

1. **Be specific, not vague** — "Udemy training completed" > "Core training completed". Name the platform, tool, or system explicitly.
2. **Reports must stand alone** — The reader (CEO, Operation Manager, Engineering Manager) shouldn't need the author in the room to explain what a cell means.
3. **Every cell should be self-explanatory** — If someone reads a table cell and asks "what does this mean?", rewrite it.
4. **Same state = same label** — Use identical wording across all rows/departments for the same status. Don't say "Phase completed" for one and "Training done" for another.
186. **Pearl Pixels Quarterly Report and Q1 Report are complementary, not duplicates** — The quarterly report contains unique PM observations (execution-vs-planning mode, untracked study hours, QA validation gaps, zero-activity team members) not in the main Q1 report. Always read both when building Q2 plans or executive summaries.
187. **"Split" means separate into individual entries, never remove** — When user says "split" two items combined in one row, create separate entries for each. "Split" preserves all items, just separates them. Never interpret it as removing the second item.
227. **Verify team roles from the authoritative team directory before assigning action item owners** — Never assume roles from email CC lists or job title signatures. RC Hosting Investigation assigned backend tasks to an Implementation team member (El-afify) and an unlisted person (Badawy) based on email CC assumptions instead of checking DevOps_Teams.md. Always cross-reference the team directory first.
253. **Deep-study all repos and wikis before creating a dashboard** — A shallow pull-and-list misses wiki content (SOPs, architecture decisions, project plans, test reports). Clone wikis, read every file, and explore all branches before claiming a "complete" report. The Taqat dashboard missed 35 wiki pages (5,461 lines) on first pass.

## Status Labels & Completion Markers

5. **"Ongoing" is a lazy status** — Replace with specific next steps: "Phase 2: To Be Defined", "Certification prep continues independently (lab practice & exam)". Show intentional planning, not vagueness.
6. **Completion markers must be consistent** — If one department shows "Training completed" in a phase, ALL completed departments must show it. Never leave blank or `--` for a completed item.
7. **Explain what "independently" means** — If something continues outside the plan scope, say what specifically continues (e.g., "lab practice & exam"), not just "continues independently".

## Bilingual Documents (EN/AR)

8. **Always update both languages for every text change** — Never edit English without updating the Arabic span. Easy to forget on small fixes.
9. **Verify RTL visually** — Arabic layout can break borders, alignment, and table padding. Always open the HTML and toggle to Arabic after changes.
10. **Check both languages after toggle** — A change that looks fine in English might overflow or misalign in Arabic RTL mode.
76. **Bilingual i18n must use data attributes, not duplicate HTML — plan from day one** — v2 uses `data-i18n` keys with a JS translation map (~223 keys). Duplicating entire pages for EN/AR doubles maintenance and guarantees drift. Plan for `data-i18n` from the start so every text element is toggle-ready — adding bilingual support after the fact requires touching every text element across the whole deliverable. Every PM dashboard needs an EN/AR toggle from day one. (Folded former #33: "Arabic toggle must be planned from day one". Relocated from "OKR Dashboard v2 Lessons" — content was about i18n, not OKR.)
259. **Hub V2 i18n uses textContent, not innerHTML** — The `applyLang()` function replaces `el.textContent`, so nested HTML tags (bold, links, code) get stripped when toggling to Arabic. For elements with mixed HTML, either put `data-i18n` on text-only spans to preserve formatting, or accept plain-text Arabic. Always check how the i18n system works before adding translations to a new page. (Relocated from "Translation & i18n (Apr 5, 2026)" — single-lesson category absorbed.)

## Stakeholder Communication & Emails

11. **Give email options, not just one draft** — Provide 3 versions: formal (detailed), concise (professional), action-oriented (direct). Different audiences need different tones.
12. **"Dear All" for multiple managers** — Avoids hierarchy issues. Let the To/CC fields handle the addressing.
13. **Always include what's attached and why** — Don't just say "please find attached". List what the attachment contains (bullet points).
282. **Record email directives into deliverables immediately** — Semir's Mar 16 email changed Flow 2 ownership (Mohammed Agha → Ashik Mohamed/TaqaTechno) but sat unrecorded for 3 weeks, leaving the V2 summary stale with wrong RACI. Always update execution documents when new email directives arrive.
283. **Always verify and use correct job titles in formal documents** — "IT Project Manager" vs "Project Manager & Scrum Master" is a small detail but a big difference in formal deliverables. Cross-check titles against the latest org chart or email signatures before publishing.

## HTML Deliverables

14. **Open HTML after every change** — Code edits can look correct but render wrong. Always verify in the browser.
15. **Version control in filenames** — Use `_v2`, `_v3` suffixes. Keep old versions in versioned folders (`v1/`, `v2/`, `V3/`).
277. **Assessment files for candidates must NOT contain evaluator content** — When cloning assessment tabs into standalone HTML for email, exclude scoring rubrics, reference solutions, interview questions, scenarios, and PM decisions. Candidates see only requirements + deliverables. Evaluator content stays internal. Cost: leaking the scoring rubric lets candidates game the assessment.
284. **Changelog sections are internal tracking, not for stakeholder deliverables** — V2→V3 diff tables help the PM track what changed, but management-facing HTML should not include version changelogs. Remove them from final outputs presented to the board or stakeholders.
285. **Always reference organization identity explicitly in deliverables** — Include the Azure DevOps org name (TaqaTechno) and URL (dev.azure.com/TaqaTechno) in HTML reports. Never leave organization references generic when the audience needs to know which tenant/org is affected.
308. **Hardcoded candidate names make assessment pages non-reusable** — team-assessment-phase0.html had "Shehab & Hala" in 30+ places. When new candidates join, every reference breaks. Use generic terms ("candidates", "current backend team") from the start. Names belong in runtime data (contenteditable fields), not static HTML.
309. **Every HTML deliverable must include a favicon** — Missing favicons make browser tabs show a generic icon, making it hard to identify dashboards when multiple tabs are open. Use inline SVG data URIs (`<link rel="icon" type="image/svg+xml">`) so favicons work without external files. Check all linked HTML files during dashboard reviews. (See also #466 for the creation-time enforcement of this rule.)
466. **Every HTML I generate must ship with a favicon on the FIRST save — never as a follow-up fix** — On 2026-04-21 I delivered 3 HR candidate-comparison HTMLs (QC, BA, Senior Backend) without favicons. The user had to ask for them after the fact. This is the third+ time this miss has occurred despite lesson #309 already stating "every HTML deliverable must include a favicon." **Why:** #309 is phrased as a *review* rule, so it doesn't fire during *creation*. Fresh HTML generation needs a creation-time default, not a post-hoc check. The cost of the miss is cheap per file but the user has flagged the repetition as frustrating — favicon omission now reads as "Claude isn't paying attention to the basics." **How to apply:** (1) Treat favicon as part of the base HTML template, alongside `<!DOCTYPE html>`, `<meta charset>`, `<meta viewport>`, `<title>`. If any of those four are present, the `<link rel="icon">` must also be present. (2) Default favicon = inline SVG data URI (no external file), placed immediately after `<meta viewport>` and before `<title>`. (3) Choose an icon appropriate for the document family: HR → "HR" monogram on teal `#0f766e`; OKR → "OKR" monogram; PMO → "PM"; etc. Reuse across sibling files so tab strips look coherent. (4) When editing an existing HTML without a favicon, add one proactively — don't wait to be asked. (5) When producing a batch of HTMLs in one session (e.g. 3 candidate comparisons), apply the same favicon to all of them in the same pass. (6) Supersedes #309's review framing. #309 still applies to legacy files not created by me; #466 applies to any HTML I author or materially rewrite. (Relocated from "HTML Deliverable Defaults (Apr 21, 2026)" — single-lesson category absorbed.)

## Data Validation

16. **Always validate email/source dates first** — Before merging versions, check if source files actually changed. File size comparison catches identical files.
17. **Cost model is the #1 change trigger** — Major version changes are usually driven by pricing/subscription model changes, not content changes.
18. **Use PowerShell COM for .msg extraction** — `Outlook.Application` works but files must not be open in Outlook.
286. **No assumptions on source data — always read the actual files** — Never assume PDF/MD content matches what you remember. The V3 Recommendation had exact user lists (14 users, 2 teams), cost breakdowns (3 scenarios), and 6 role definitions that all had to match the email summary precisely. Always re-read the source before generating derivative documents.
292. **Always verify live system data before finalizing deliverables** — The V3 recommendation (Feb 3) said 24 users. Live Azure DevOps API check (Apr 7) showed 33 users — a 37% growth in 2 months that no document reflected. Always query the live system before publishing user counts, costs, or scope.

## OKR & KPI Dashboards

19. **OKR scoring must be automatic** — Don't rely on manual status entry for KR progress. Use editable "Current" fields that auto-calculate progress percentage, bar color, and status badge. Manual scoring drifts and becomes inconsistent.
20. **Inverse metrics need special handling** — KPIs where lower is better (bug escape rate, carry-over tasks) need inverse progress calculation: if current <= target, it's 100%. Don't use the same formula as "higher is better" metrics.
21. **Separate OKR (strategic) from KPI (operational)** — OKRs are quarterly aspirational goals with key results. KPIs are ongoing team metrics. Mixing them in one table confuses the audience. Use separate tabs/sections.
183. **Dashboard team rosters must stay in sync with actual assignments** — When a team member is added or moved between teams in DevOps, the dashboard roster (HTML arrays, WIQL member lists, team card tables, member counts, and header totals) must ALL be updated together. A member missing from any one array silently drops their metrics from queries. Check all data arrays — dashboards often have 3-4 redundant member lists that must match.

## Search Index Maintenance

22. **Rebuild search index after every pull** — When new HTML pages are added to a repo with a client-side search engine, the search index (e.g., `search-index.js`) won't include them automatically. Always check if new pages need to be added to the file list AND rebuild the index.
23. **New pages must be registered in build script** — Adding a new HTML file to the repo doesn't make it searchable. The file must also be added to the FILES array in the build script (e.g., `build-index.js`). This is a two-step process: (1) add to list, (2) rebuild.
312. **New root-level HTML files are not auto-indexed in the Sessions Index** — Creating a new HTML file in a project folder does not make it searchable in the Claude Sessions Index. A session card must be manually added to both `Claude_Sessions_Index.html` and `.md` with the correct path, badges, and date for the file to appear in search results.

## Project Estimation & Story Points

24. **Always verify project duration across all files** — Different documents (backlog, proposal, basis of calculation) may show conflicting durations. Cross-check all sources after every pull — the latest versioned file (v2) is the source of truth.
25. **Story points need a documented scale** — If a backlog uses SP without defining what each value means, derive the scale from actual story examples. Group stories by SP value, analyze complexity patterns, and document the scale for team alignment.
26. **Person-months ≠ calendar months** — Always calculate effective person-months (allocation % × duration) not headcount × months. A team of 11 at mixed allocations (20%-100%) may only deliver 15.93 person-months in 4 calendar months.
27. **AI acceleration changes all estimates** — When a project uses AI-assisted development, traditional effort estimates don't apply. Document the multiplier per role (QA gets highest ~4x, PM gets lowest ~1.5x) so stakeholders understand the compressed timeline.

## DevOps API Integration

28. **Use real DevOps states, not made-up labels** — Dashboard metrics must match the exact states in Azure DevOps (To Do, In Progress, Resolved, Done, Closed). Using generic terms like "Active" or "Carry-over" creates confusion when comparing with the DevOps board. Always query actual states first with the States API before building queries.
29. **Query by Assigned To when Area Paths don't exist** — If a DevOps project doesn't have team-specific area paths (Backend, Frontend, etc.), query by `[System.AssignedTo] CONTAINS 'member name'` across all projects. This is more flexible than area paths and works cross-project.
30. **Always provide a Data Source tab** — When building dashboards that fetch from APIs, include a transparent "Source" tab showing the exact queries used, results returned, and verification links. The user should never have to trust the numbers blindly — they need to verify against the source system independently.
31. **Discover projects via API before hardcoding** — Don't assume which DevOps projects exist based on email analysis or conversation context. Use `/_apis/projects` to discover all projects, then let the user confirm which ones to include.
32. **Never expose PAT tokens in chat or files** — PAT tokens should only be entered in browser modals and stored in localStorage. Never save them in code, memory files, or conversation. If a user shares one, remind them not to. (See also #395 for the next layer: moving PAT tokens out of localStorage into server-side `.env`.)
34. **"Last updated" means fetch time, not current time** — Dashboard timestamps should show when data was last retrieved from the API, not a live clock. A live clock gives false confidence that data is current when it may be hours old.
35. **Show full project names, not abbreviations** — In dashboards for CEO/Board, use full project names (e.g., "KhairGate (BMS)") instead of abbreviations (e.g., "BMS"). Abbreviations are internal shorthand that external stakeholders won't recognize.
293. **Azure DevOps API does not store job titles** — The user list API (`az devops user list/show`) only returns display name, email, and license type. Job titles must come from the user, HR system, or Microsoft Entra ID. Don't assume you can pull titles from DevOps.
387. **`wit_add_child_work_items` fails silently on projects with required `System.AssignedTo` — fall back to per-item `wit_create_work_item` + single link batch** — The Azure DevOps MCP tool `wit_add_child_work_items` only accepts `title`, `description`, `areaPath`, `iterationPath` per child. It does NOT accept `System.AssignedTo`, `Microsoft.VSTS.Common.Priority`, `System.Tags`, or any arbitrary field. If the target project has a workflow rule requiring AssignedTo at creation time (common in Agile/Scrum templates with mandatory ownership), every single child in the batch fails with HTTP 400 `TF401320: Rule Error for field Assigned To. Error code: Required, HasValues, LimitedToValues, AllowsOldValue, InvalidEmpty` — and the error comes back as an array of 32 identical rejections, not a single top-level failure, which is easy to miss on a quick read of the response. **Why**: Apr 15, 2026 — attempted a 32-task batch creation under Relief Center PBI #4217 using `wit_add_child_work_items`. All 32 failed with the AssignedTo rule error because the Relief Center project requires AssignedTo at creation. Had to pivot to 32 individual `wit_create_work_item` calls (which accept arbitrary fields via the `fields` array), then one `wit_work_items_link` batch call to attach all 32 as children of 4217. Total recovery: ~40 seconds of parallel tool calls vs what would have been a clean single-batch if the tool supported it. **How to apply**: (1) Before any batch child creation, check the project's work-item type rules — grep past lessons or project memory for "AssignedTo required" flags; (2) If AssignedTo is required OR if you need to set Priority / Tags / custom fields at creation → skip `wit_add_child_work_items` entirely; (3) Use `wit_create_work_item` in parallel (send 10-15 tool calls in one message — confirmed no rate-limit issues on the TaqaTechno org), each with the full `fields` array including `System.AssignedTo`, `Microsoft.VSTS.Common.Priority`, `System.IterationPath`, `System.AreaPath`; (4) Collect all returned IDs, then make ONE `wit_work_items_link` call with `type: "child"` and `id: <parent>` + `linkToId: <new task>` for every new task — this attaches all children in a single API round-trip and is much cheaper than 32 sequential link calls; (5) Record the project's required-fields in the project memory file (e.g., `feedback_devops_required_fields.md`) so future sessions don't repeat the failed batch attempt. **Known projects with AssignedTo required**: Relief Center (confirmed Apr 15, 2026). Add others as encountered. (Relocated from "Azure DevOps Batch Work-Item Creation (Apr 15, 2026)" — single-lesson category absorbed.)

## Session Workflow & Git Hygiene (Mar 16, 2026)

36. **Always pull before analyzing** — Never analyze or report on data without pulling latest from remote first. Stale local data leads to wrong conclusions (e.g., reporting 9-month plan when v2 already shows 4 months).
37. **URL.txt is the session start ritual** — For repos with a URL.txt, always run it first: fetch → compare → pull if needed → open HTML. This prevents working on outdated files.
38. **Check git diff after pull, not just commit message** — Commit messages like "design modifications" are vague. Always check `git diff --stat` to know exactly which files changed and how much, so you can assess impact.
39. **Version files may have internal inconsistencies** — A v2 file may still contain v1 data in some sections (e.g., team member durations showing 9 months in a 4-month plan). Flag these to the user rather than silently assuming one is correct.
278. **Remove project-specific context from standalone assessment files unless PM explicitly wants it** — When creating assessment HTML for email, candidates need technical requirements — not PM governance context (project names, phase numbers, sprint references, hiring gaps). Only include project context if the PM explicitly confirms it belongs in the candidate-facing file.
306. **Always run git pull first, not just git log** — `git log` shows fetched commits but doesn't update the working tree. Today's session checked `git log` (saw 4 commits) but didn't pull, leaving local 2 commits behind. The drift fix was applied correctly but the repo was stale. Always `git fetch` + `git pull` as the first action.
465. **Produce a session-closure handoff BEFORE the user closes VSCode — never after** — When the user signals any kind of session pause or close ("brb", "back in X min", "closing session", "resume later", "will close VSCode", "takecare from this action"), proactively emit a compact bookmark covering: (1) current working folder + active file(s); (2) active mode (plan mode / auto mode / skill in progress); (3) pending decisions awaiting user input; (4) hard deadlines or dates on the calendar; (5) a copy-pasteable resume prompt the user can paste into a fresh Claude Code session to restore context. **Why:** The user opens Claude Code sessions from inside VSCode. Closing VSCode ends the session with no auto-persistence of conversation state — the next launch starts cold. Without an explicit handoff, the user must re-explain the entire context on restart, which is expensive mid-deliverable (PMO dashboards, Outlook reports, Alaqraboon readiness work often span 3-5 sessions). The cost of writing a 6-line handoff before pause is trivial (≈30 seconds); the cost of reconstructing mid-deliverable context without one is 10-30 minutes of re-briefing and repeated exploration. Confirmed as a standing rule by the user on 2026-04-21, after I produced a handoff unprompted when they said "I'll be back in 10 min and will close the session" — user then instructed "anytime when I close VSCode … you should take care of this action." **How to apply:** (1) Trigger on any of these phrases or their variants: "brb", "back in X", "be right back", "step away", "close session", "closing VSCode", "pause", "resume later", "continue tomorrow", "takecare from this action". (2) Emit the handoff BEFORE the user actually leaves — use the gap between their pause message and their next action, not after they return. (3) Keep the handoff compact enough to fit one screen, but include the resume prompt verbatim so paste-back works with zero editing. (4) If plan mode or auto mode is active when the pause is announced, call it out explicitly in the handoff so the user knows to re-enter the same mode in the next session. (5) If a skill is mid-execution (e.g. `/PM_drift-fix`, `/PM-lessons-add`), note the skill name + phase so the next session can pick up without re-invoking. (6) Applies globally across ALL projects, not just the current folder — PMO, Outlook, OKR, Alaqraboon, Udemy, Claude Courses, etc. (7) Pair with per-project memory: write `feedback_session_closure_handoff.md` into `C:\Users\DELL\.claude\projects\<project>\memory\` so the rule persists across sessions even if this global lesson is not loaded. (8) The resume prompt should name the file paths, the deliverable version number, and the specific pending decision — generic "pick up where we left off" prompts are useless across a fresh session with no context. (Relocated from "Session Continuity & Closure Handoffs (Apr 21, 2026)" — single-lesson category absorbed.)
742. **When `git status` shows unexpected state mid-session, run `git reflog` BEFORE assuming local error — a parallel session may have committed your work** — On 2026-05-07 during the global_lessons.md refinement pass, mid-edit verification showed lesson count 734 instead of expected 733 and a previously-non-existent lesson #741. `git status` was clean (no uncommitted changes). `git diff HEAD` showed only my latest in-progress edits. The actual cause was a parallel Claude Code session that committed at 21:35 — bundling my uncommitted Section B work AND adding their new lesson, which they had originally drafted as #718 but renumbered to #741 to avoid colliding with my just-published renumber (commit `3019c2c` had taken #718). **Why:** `git status` and `git diff` only compare the working tree to HEAD. They CANNOT detect that HEAD itself moved while you were working — for that you need `git reflog`, which logs every HEAD movement including parallel-session commits. Multi-session work on shared files (especially `global_lessons.md`, the file most prone to parallel edits since lessons are added across all sessions) creates a window where another session can commit between your file reads and writes. Without reflog awareness, the only signal is "the file looks different than I expected." **How to apply:** (1) Snapshot HEAD at the start of any long-lived edit pass: `git rev-parse HEAD` and remember the SHA. (2) When mid-edit verification shows an unexpected count or content, IMMEDIATELY run `git reflog -10` before assuming local error — look for commits not in your authored set. (3) On detection, run `git diff <my-start-sha> HEAD -- <file>` to see what the parallel session added; reconcile incrementally instead of reverting. (4) If your work was bundled into the parallel commit (clean working tree despite ongoing edits), the work is preserved — just continue from current HEAD. (5) For `global_lessons.md` specifically, bias toward smaller more frequent commits to shrink the parallel-collision window. Pair with #306 (always pull first) and #36-#39 (Session Workflow basics) — pull-then-commit narrows but does not close the window. Reference: May 7 refinement pass, commits `3019c2c` (mine) → `c884731` (parallel) → `05934ac` (mine).

## Data Analysis & Presentation (Mar 16, 2026)

40. **Derive what's not documented** — If a backlog has story points but no scale definition, reverse-engineer it from the data. Count distribution, group by SP value, read actual stories at each level, then present the derived scale for validation.
41. **Show the math, not just the result** — When presenting person-months, show: Allocation % × Duration = Person-Months for each member. Stakeholders trust calculations they can verify.
42. **Compare before and after** — When two versions of a plan exist (v1 vs v2), always present a variance table. Decision-makers need to see what changed and by how much (e.g., -56% duration, -66% effort).
43. **Define jargon when asked** — Terms like "person-month", "story point", "sprint velocity" are not obvious to all stakeholders. When the user asks "what does X mean?", give a concrete example from their own project data, not a textbook definition.

## Memory & Lessons Discipline (Mar 17, 2026)

44. **Save lessons at end of every session** — Don't wait for the user to ask. At the end of each working session, proactively capture lessons learned into `global_lessons.md` and project-specific memory files.
45. **Memory files are for future conversations, not current tasks** — Don't save ephemeral task details (current debug state, temp file paths). Save things that help future-you understand the project: team composition, estimation methodology, key decisions, file relationships.
46. **Update session log after every pull** — The session log in MEMORY.md is the project timeline. Record: date, commit range, new/updated files, key actions taken. This helps trace when changes were introduced.

## Consolidation & Multi-Source Architecture (Mar 24, 2026)

47. **Never modify source files in consolidation projects** — Keep all source analyses untouched (e.g., S1: 468 files, S2: 21 files). Build an independent consolidated output (HUB v3.0). This preserves original analysis integrity for audit trails.
48. **Track conflicts explicitly with IDs and side-by-side display** — Number conflicts (CF-01 to CF-12) and show both positions without auto-resolution. Let governance decide. Transparent conflict management builds stakeholder trust.
49. **Color-coded source attribution on every data point** — Use consistent color tags (S1=blue, S2=orange, Merged=purple, New=green) so readers can trace any claim to its origin. Critical for multi-source projects.
50. **3-tier gap classification prevents analysis paralysis** — Classify gaps as Blocking / Important / Deferrable. Simple 3-tier system distinguishes urgency without overcomplicating prioritization.

## Portal & Pipeline Architecture (Mar 24, 2026)

51. **Zero-dependency static HTML is the most durable deliverable** — No build system, no bundler, no server. Works offline, on any machine, forever. Essential for board presentations and long-term archival.
52. **Modular CSS from day one saves rework** — Separate CSS files (variables, base, layout, nav, components, RTL, print) allow targeted changes without breaking unrelated pages. Invest in CSS architecture early.
53. **Numbered scripts enforce execution order** — Name pipeline scripts 01→22 to make execution order self-documenting. Anyone can run them in sequence without reading a README.
54. **Large JSON intermediates enable independent downstream scripts** — Files like `db_objects.json` (20MB) are large but let any downstream script work without re-extracting from source. The storage cost is worth the decoupling.
55. **FOLDER_OBJECTIVE_MAP.html serves as the project atlas** — A visual map of all folders and their purposes is essential when projects exceed 10 subfolders. New team members can orient themselves immediately.
56. **Recommendations stay as recommendations until governance approves** — Never auto-apply analysis recommendations to source files. Present findings; let the board decide. Technical teams provide analysis; governance decides.

## Dashboard Maintenance & Adoption (Mar 24, 2026)

57. **Maintenance burden kills dashboards** — If a dashboard requires > 30 manual inputs per week for one person, it won't be used. v1 had ~113 manual inputs and was never populated. v2 cut to ~24 by auto-fetching everything possible from DevOps APIs. Design rule: if an API can provide it, never ask the PM to type it.
58. **KRs must be outcomes, not activities** — "Weekly agendas with DevOps metrics pre-populated" is a task, not a key result. "Decision turnaround time < 3 days" is a measurable outcome. Before finalizing OKRs, test each KR: "Could someone else observe and verify this was achieved?" If not, rewrite it.
59. **PMs need their own scorecard for CEO visibility** — Team KPIs (sprint velocity, bug rate) measure the team, not the PM. Add a dedicated PM Scorecard with metrics the CEO evaluates the PM on: decision turnaround time, scope change control, meeting compliance, stakeholder satisfaction pulse. Without this, the PM is invisible in their own dashboard.
60. **localStorage snapshots enable trend tracking without a backend** — Save a weekly snapshot object into a history array on every "Save" click. Use inline SVG sparklines (no Chart.js dependency) to show week-over-week progress. Cap at 13 weeks (~500KB). Add Export/Import JSON for backup. This gives trend visibility with zero infrastructure.

## Enhanced Standalone Auto-Updater Rules (Mar 24, 2026)

61. **Surface updates are NOT enough** — After every Hub V2 pull, audit internal content (Document Control tables, assumptions, risk descriptions, internal text references), not just headers/footers/KPIs. V5-V10 missed this completely.
62. **Each pull with drift = new standalone folder** — Never edit existing version in place. Copy folder, increment version, apply fixes. Each version is a frozen snapshot.
63. **Create own reports, never copy Hub V2's** — PM creates own variance/story reports based on previous template. Hub V2 reports are Mohamed's internal documents with different branding.
64. **Hub V2 pages don't belong in standalone** — Only PM-created artifacts go in the standalone folder. Hub V2 pages (health-dashboard, etc.) are linked via nav, not copied.
65. **Document Control tables must match folder version** — Version field, Updated date, Data Source (with commit hash) must be current in every file after every update.
66. **Footer must show specific date** — "Modified: DD Mon YYYY" not generic "March 2026". Every file, every version.
67. **Phase 0 vs full program must be distinguished** — Budget, Timeline, Hiring show full 22-month program but kickoff is Phase 0 only. Both must be shown.
68. **Old reports don't belong in kickoff package** — Only latest variance + story variance versions. Remove historical snapshots (v10, v11, v1, v2).
69. **BA Review is a loop, not one-shot** — Mahmoud reviews → Mohamed fixes → Mahmoud verifies. Loop until no critical issues. Track pass number.
70. **Read lessons BEFORE executing** — At start of every session, read feedback files before touching any code. Prevents repeating past mistakes.
71. **Never assume git author = current user** — Check git log. MahmoudEl-Afify ≠ Mahmoud Elshahed.
72. **Validate Hub V2 claims against actual data** — Don't trust page counts, story counts, or status claims. Count actual files. Read actual KPI strips.
73. **Interactive buttons must sync via same localStorage key** — All files using status buttons must share `kg-action-progress` key. Labels must match across files (3 states for ADRs).
74. **Resolved sections should be collapsed by default** — `<details>` without `open` for ALL RESOLVED / CLOSED sections. Focus on pending items.

## OKR Dashboard v2 Lessons (Mar 25, 2026)

75. **Tab-based navigation beats sidebar for dashboards under 10 views** — v1 used 12 sidebar items; v2 consolidated to 7 tabs. Fewer navigation choices = faster cognitive load for executives. Only use sidebar when you have 10+ sections or need persistent visibility of all options.
77. **Print CSS must show all tabs, not just the active one** — Dashboard print styles that hide inactive tabs produce blank pages. Use `display: block !important` on all `.tab-content`, add `page-break-before: always` between sections, and inject section titles via `content: attr(data-tab-title)` pseudo-elements. Always test Ctrl+P after adding print styles.
78. **Metric cards should be clickable drill-downs, not dead-end numbers** — Showing "To Do: 12" is useless without knowing who owns what. Every aggregate metric card should open a per-member or per-project breakdown modal on click (`showTeamDetailModal`). This was the key UX improvement in Team Pulse v2 — all 6 teams, 4 states each, 24 clickable drill-downs.
79. **Declare cached variables BEFORE loadData() runs** — JS `var` hoisting hoists the declaration but not the `= {}` initializer. If `var cache = {}` appears after `loadData()`, the initializer overwrites whatever `loadData()` restored from localStorage. Always declare cache variables before any load function that populates them.
80. **Never duplicate team members across teams** — Double-counting inflates metrics silently and makes drill-downs misleading. One person = one team. If they work across teams, pick their primary. OKR v2 had 4 members in both Mobile and Qatar — split them to eliminate overlap.
81. **WIQL returns work item IDs for free — don't discard them** — `data.workItems` from Azure DevOps contains `[{id, url}]`. The original `runWIQL` threw away IDs and returned only `.length`. `runWIQLWithIds` preserves them at zero extra API cost. Cache IDs in localStorage so drill-down links survive page reloads.
82. **Dashboards need a built-in user guide** — A PM won't use a dashboard they don't understand. Embed a step-by-step weekly workflow, a timeline with deadlines, and an auto-vs-manual field reference directly in the dashboard. Don't rely on external documentation — the guide must live inside the HTML.
83. **Always open HTML after edits — no exceptions** — Never leave the user to open files manually. Run `Start-Process` or equivalent as the final step after every HTML edit session. This was explicitly called out as a failure and saved as a feedback memory.

## Auto-Updater & UX Redesign Lessons (Mar 25, 2026)

84. **PowerShell em-dash crash** — Unicode characters (em-dash, curly quotes) in PowerShell string literals cause silent parse failures. The window closes instantly with no error. Always use ASCII alternatives (`--` not `—`, straight quotes not curly).
85. **Always provide .bat launcher for PS scripts** — "Right-click → Run with PowerShell" closes the window on any error. Create a `.bat` file with `powershell -ExecutionPolicy Bypass -NoExit -File script.ps1` so errors stay visible.
86. **Disable update buttons when no drift** — "Full Update" when already up-to-date is misleading and risky. Separate into 3 buttons: Check (always available), Pull+Update (only when drift), Audit Only (always available). Grey out unavailable actions.
87. **Automate recurring failures** — If a check is missed 3+ times across sessions, build a tool. The auto-updater script exists because manual drift fixes repeatedly missed Document Control tables, assumptions, and internal content. Human checklists fail; scripts don't forget.
88. **3 page types define nav UX** — Action pages (green, interactive buttons for decisions), Reference pages (blue, read-only for board review), Audit pages (purple, health history snapshots). Organize navigation by type, not by arbitrary categories. Directors need to know: "where do I click?" vs "where do I read?"
89. **Merge overlapping pages** — Two pages covering the same topic (KICKOFF_READINESS_GUIDE + KICKOFF_READINESS_VALIDATION) confuse the Director. One source of truth per topic. If you create a summary page, merge it into the original rather than adding another file.

## Cross-File Consistency & Audit Lessons (Mar 25, 2026)

90. **Deleting a file breaks all relative links to it** — When we deleted root-level HUB_PULL_HISTORY.html, 14 V11 sidebar nav links (`../HUB_PULL_HISTORY.html`) broke silently. Always grep for all references BEFORE deleting any file, and fix links in the same commit.
91. **Don't duplicate files across locations** — Having HUB_PULL_HISTORY at root AND inside V11 caused staleness (root was March 18, V11 was current). One copy, one location. If other files need it, link to it — don't copy it.
92. **Location-specific terms don't belong in hiring risks** — "Qatar market" implies on-site only. Hiring may be hybrid/remote. Use skill-based language instead: "Odoo 19 skill scarcity" not "Qatar market scarcity". Applies to all R-08 references across PM_RISK_REGISTER, ADR_APPROVAL_REQUEST, and any hiring-related text.
93. **Health dashboard verdict must reference cards by name** — A flat list of 7 pending items is unreadable. Group pending items under the card they belong to (e.g., "POC Readiness V.2 — ADR-1, Budget, Odoo hiring"). Board can then click the right card directly.
94. **Visual badges (ACTION NEEDED / CLEAR) save board reading time** — 7 cards with no visual priority = Board reads all 7. Amber badge + green badge = Board reads only the 3 that matter. Always add status badges when cards have mixed states.
95. **Track review loops explicitly** — The BA review between El-Afify and Mohamed had no visible status on the dashboard. Pass 2 was pending but nowhere documented. Add "Review Loop Status" tracking boxes to both the health dashboard card and the readiness page.
96. **Purpose tips in headers orient the reader** — Every page header should have a one-line description of what this page does. Board members open 15+ files — a tip below the title saves them from scrolling to understand context. Use `.summary` div pattern.
97. **Auto-updater covers KPIs but not consistency** — The PS1 server catches number drift (stories, SP) but misses text mismatches ("Qatar market" in 2 files), broken nav links, file count disagreements, and version tag staleness. Build a 6-check audit: KPIs + text + nav + file count + status sync + version tags.
98. **Sprint-plan-phase0 and phase0-poc-readiness are a pair, not duplicates** — Sprint plan = developer execution guide (Mohamed's, 2,196 lines). POC Readiness = PM status dashboard (ours, 227 lines). Different audiences, same data. Keep both but verify data consistency between them.
99. **Session handoff must include tracking file updates** — HUB_PULL_HISTORY, FOLDER_OBJECTIVE_MAP, and MEMORY.md must be updated at end of every session. Save a "FIRST TASKS Next Session" section in memory if context runs out.
203. **Supporting documents drift silently during version bumps** — Approval emails, cover letters, and non-artifact HTML files are not included in version-bump agent scope. KICKOFF_APPROVAL_EMAIL had V5 data (commit 2f321a3, March 9) while all 13 PM artifacts were at V13 (March 30). Stale numbers (18 vs 19 risks, 21 vs 28 actions, 200+ vs 937 stories) went unnoticed for 4 versions. Include ALL HTML files in audit-check scope. (See also #300 for the dashboard-card variant of the same drift mode.)
204. **KPI smart cards must match source file buttons and KPI strips** — ADR_APPROVAL_REQUEST showed "1 Approved / 2 Pending" in KPI cards while all 3 ADR buttons were already "Approved." Manual review missed this; only the audit-check agent caught it. Smart card values are high-visibility — board sees them first — so mismatches create credibility risk.
205. **Internal summary text drifts independently from KPI cards in the same file** — PROJECT_STARTUP_CHECKLIST summary said "21 actions / 3 phases" while its own KPI cards correctly showed "28 / 4." PM_RISK_REGISTER meta said "18 Risks" while its KPI card showed "19." Always audit both the KPI strip AND the subtitle/meta/section headers — they are updated by different processes and diverge silently.
206. **Never expose internal PM governance file names on Hub V2 pages** — V13 Standalone file names (PM_RACI_MATRIX.html, HIRING_TEAM_PLAN.html) were added to phase0-poc-readiness.html on Hub V2. The team sees Hub V2 only; V13 references leak authority structure and create confusion about which files are canonical. Always use Hub V2 paths on Hub V2 pages.
207. **Hub V2 technical files created by engineering are NOT PM governance files** — sprint-plan-phase0 Key Contacts table (4 people) was treated as the project team structure. It's only a Phase 0 quick-reference. Hub V2 raci-matrix.html and hiring-team-plan.html are NOT approved by IT PM. Always confirm approval status before treating any file as authoritative for roles and responsibilities.
208. **Discrepancy reports need owner assignments + clickable source file links** — Review summary of sprint-plan-phase0 found 9 issues but initially had no owner or file references. Adding "Responsible: Mohamed Basha" + hyperlinked source files made it actionable. Without these, findings sit unresolved because nobody knows who should fix what or where.
209. **AI-generated placeholder names in Hub V2 files must be caught and flagged** — pbi-validation-phase0.html listed "Dr. Sarah Pemberton (BA)" as reviewer — a fabricated name. The actual reviewer is Mahmoud Afifiy (El-Afify). Placeholder names erode credibility if they reach board review and can cause confusion in formal audit trails.
210. **Separate internal evaluator content into local-only files before pushing** — Reference solutions (code answers) and interview questions (with expected answers) must NOT be in files pushed to shared repos. Candidates can see anything in the repo. Extract to V13-only files (e.g., ASSESSMENT_REFERENCE_SOLUTION_INTERNAL.html) and replace with locked placeholder in Hub V2 version.
211. **When moving files between folders, grep for ALL relative path references** — Moving team-assessment-phase0.html from Hub V2 to Team Assessment folder broke 8 CSS paths, 4 JS paths, and 3 cross-file links silently. Page rendered unstyled. Always grep for the filename across ALL files and fix paths in the same step as the move.
219. **After a drift fix, re-fetch origin before pushing** — Hub V2 moves fast. The E19 fix took hours, during which E2 expanded (+1 story, +3 SP), making V14 stale before it was even pushed. Always re-check for new commits after completing a drift fix.
220. **version-bump agent misses body-text prose with version numbers** — It handles version-tag spans, headers, footers, but NOT inline text like "all 5 operational docs complete in v13.0". Always grep for the old version in body text after version-bump completes and fix manually.
221. **Review summary fixes must be on SOURCE pages, not the tracking dashboard** — Basha marked C1-C5/H1-H4/D1-D3 resolved on phase0-poc-readiness (our PM dashboard) but sprint-plan-phase0 and pbi-validation-phase0 (source files) were not updated. Dashboard fixes are cosmetic — source files remain broken.
307. **Check all cross-tab dependencies before adding content to one tab** — Adding .gitignore, screen recording, and AI policy to Tab 1 (Assessment) required updates in Tab 3 (Scorecard scoring rows), Tab 4 (Deep Review anti-patterns), Tab 5 (Interview AI policy note), and Overview (description). Editing one tab without mapping dependencies first means follow-up edits that could be missed.

## Git, Branching & Deployment Lessons (Mar 26, 2026)

100. **`git rm -rf .` deletes actual files, not just tracking** — When creating orphan branches inside the working folder, `git rm -rf .` permanently deleted V12 files. All session changes had to be re-applied from scratch. Always work from a DIFFERENT directory when creating orphan branches, or use `git worktree`.
101. **PowerShell arrays of hashtables don't serialize with ConvertTo-Json** — `@() += @{...}` pattern produces empty JSON arrays. Use `[System.Collections.ArrayList]::new()` with `.Add()` instead. Also increase `-Depth` to 10+ for nested structures.
102. **Wrong Hub V2 path = wrong KPIs** — `KhairGate-Hub-Analysis` (V1, stale) vs `KhairGate-Hub-Analysis-V2` (current). The auto-updater pointed to V1 for months without anyone noticing. Always verify the exact folder name when configuring paths to data sources.
103. **Greedy regex causes false positives in audits** — `Financial Readiness.*100%` matched "To reach 100%" text instead of the actual 80% value on the same line. Fix: use tight patterns that match specific HTML structure (e.g., `progress-label` div value), not greedy wildcards across the whole line.
104. **Multilingual regex needs wider context windows** — Arabic "العودة" (fallback) was 40+ chars before "Odoo 18" but the 20-char lookback missed it. Expand context windows to 80+ chars when checking multilingual content for contextual exclusions.
105. **Historical files must be excluded from audit checks** — HUB_PULL_HISTORY, variance reports, and ADR contain intentional references to old tech (Camunda, Odoo 18, USD). They document history, not current state. Audit rules that flag "stale content" must have an exclusion list for historical/audit files.
106. **Hub V2 i18n uses data-i18n attributes + inline JS dictionary** — Not JSON files. ~550+ terms in a JS object (`var T = {...}`) inside i18n.js. `toggleLang()` swaps `textContent` via `querySelectorAll('[data-i18n]')`. New pages need both the data-i18n attributes AND the dictionary entries to translate. (See also #120 for the atomic-commit rule that pairs with this architecture.)
107. **Push ALL repos after changes, not just the one you're focused on** — Hub V2 changes (Arabic translations) were committed but not pushed until the user asked. When a session touches multiple repos, push all of them before ending.
108. **Orphan branches preserve version snapshots** — Each Enhanced Standalone version (v1-v12) as its own orphan branch with descriptive commit messages preserves the full evolution history. Use `git checkout --orphan v{N}` pattern.
109. **Auto-update lessons from audit findings with dedup** — Build the pipeline to write back to global_lessons.md automatically. Check for existing keywords before appending to avoid duplicates. Stale lesson files are worse than no lesson files.
217. **Create git branch in the SAME step as pushing a new standalone version** — V14 was pushed to main but v13/v14 branches were not created until the next day. Branches are the team's way to access frozen versions. Always: push commit → create branch → push branch — one atomic step.
218. **Archive the old version folder in the SAME step as creating the new one** — V13 folder couldn't be moved to archive for 2 days because .git was locked by VS Code. If deferred, file locks accumulate. Always: create V14 → move V13 to archive → verify — before any other work.
254. **Never assume repo state — verify with actual commands** — When asked "do we need to pull?", don't rely on environment metadata saying "not a git repo". Read reference files (URL.txt), check actual folders, run git status. The Taqat folder had 10 cloned repos inside it despite the root not being a repo.

## Dashboard & KPI Design Lessons (Mar 26, 2026)

110. **Separate work item types for accurate metrics** — Grouping Bug + Enhancement under "Task" hides defect trends. Each type needs its own WIQL query, KPI card, and column in drill-down tables. Without separation, you can't answer "how many bugs are open?" from the dashboard.
111. **Auto-calculate derived KPIs — don't let users manually set what can be computed** — Overall health = worst-of(Schedule, Quality, Scope) + blocker penalty. Manual "Overall" toggles drift from reality. Formula: min(S,Q,Scope), downgrade if blockers > 0, force Red if blockers > 3.
112. **Merge tiered tables into one unified view** — Tier 1 (full G/A/R) + Tier 2 (active items only) confused readers. One table, same columns, all 11 projects. Tiers are for prioritization logic, not UI separation.
113. **Column tooltips explain scoring criteria to non-technical readers** — Board members don't know what "Quality = Amber" means. Add `title` attributes: "Consider bug count, rework rate, QA pass rate." Every subjective column needs a tooltip.
114. **localStorage migration between dashboard versions** — v2→v3 key change (`okr-kpi-q2-2026-v2` → `-v3`) requires auto-migration logic on load. Users lose data without it. Always check for old key, transform data shape, write to new key, delete old.
294. **Cost scenario cards must show the phased ramp, not just the final number** — "$1,704/year" means nothing without showing it ramps $70→$106→$142/month across 3 batches. Management needs to understand cash flow timing per batch, not just the annual total.

## Document Consolidation Lessons (Mar 26, 2026)

115. **Identify the primary source before merging** — When 3 files cover the same meeting, one is always the most comprehensive. Use it as the skeleton; others are supplements for gaps only. Don't merge line-by-line — that creates duplicates.
116. **Email-ready HTML beats DOCX for meeting distribution** — Inline CSS, no attachment issues, renders in every mail client. For MoM/agenda distribution, HTML is the final format. DOCX is the input, not the output.
295. **Implementation roadmaps must evolve with the execution plan** — A 6-phase single-batch roadmap (Feb) became stale when execution shifted to 3-batch phased rollout (Apr). Both the summary tab and the recommendation tab must stay in sync — if one changes structure, the other must follow.

## Modal, Audit & i18n Lessons (Mar 26, 2026)

117. **Modal popups beat inline results for audit output** — Inline `<div id="results">` pushes page content down and gets lost on scroll. Modal overlay with backdrop = focused attention, dismissible, no layout shift. Include summary bar (pass/fail counts) + individual check cards.
118. **Remove vendor names from architecture documents** — "Camunda" → "external BPM tool". Docs survive tool changes without rewrites. Historical files (ADR, variance reports) keep vendor names as they document decisions made at that time. Only current-state docs use generic terms.
119. **Title consistency is a cross-file audit check** — "Project Manager" vs "IT Project Manager" appeared in 6+ files across V12. Title mismatches erode credibility with board readers. Add title verification to the audit engine. Exclude external stakeholder entries (e.g., Semir's role stays as-is).
120. **Batch i18n: attributes + dictionary in one commit** — Adding `data-i18n` attributes to HTML without updating the JS dictionary (`var T = {...}`) means keys resolve to raw key names. Always add both in the same commit. For Hub V2, that's 9 HTML files + i18n.js = one atomic commit. (See also #106 for the dictionary architecture this rule pairs with.)

## OKR & KPI Dashboard v3 Lessons (Mar 26, 2026)

121. **Numbers must reconcile across dashboard views** — If Executive Summary shows 26 Bugs total, Team Pulse must also total 26. Add an "Unassigned" card to catch work items with no owner. Formula: Project total = Sum(all teams) + Unassigned. If they don't match, users lose trust in the dashboard.
122. **Consistent drill-down pattern across all metrics** — Every clickable number must follow the same flow: Card number → Breakdown table → Work Items popup (with DevOps links) → Back button. Don't skip levels for Bugs/Enhancements while keeping them for To Do/In Progress. Inconsistency confuses users.
123. **Modal max-height prevents off-screen Close button** — A project with 18+ work items pushes the Close button below the viewport. Always set `max-height: 85vh; overflow: auto` on modal content. The Close and Back buttons must always be reachable.
124. **Don't add redundant summary strips** — A live data strip in the header repeating KPI card values adds clutter, not clarity. Use header space for report purpose/benefits instead. Data lives in KPI cards; identity lives in the header.
125. **G/A/R toggles need deselect capability** — Click to select, click again to deselect back to "none". Without this, once you set a value you can never clear it. Check `el.classList.contains('selected')` before toggling.
126. **Separate work item types in WIQL queries** — Mixing Task+Bug in `IN ('Task', 'Bug')` hides bug counts. Use separate queries for Bug-only and Enhancement-only counts. This enables auto-populating KR2.1 (production bugs) and gives PM visibility into defect vs improvement work.
127. **Persist in-memory data to localStorage** — `lastProjectData` was only in JS memory — lost on page reload, breaking detail modals ("Click Fetch first"). Save runtime data structures to localStorage in `saveData()` and restore in `loadData()` if the UI depends on them.
128. **Use the Blocked field, not tags** — Azure DevOps has a dedicated `Microsoft.VSTS.CMMI.Blocked` field (Yes/No). Don't rely on `[System.Tags] CONTAINS 'Blocked'` which requires manual tagging. The field is built into the work item form.
129. **Move action buttons to sidebar footer** — Fixed bottom bars (Save/Export/Clear) waste 50px of vertical space on every page. Sidebar footer is always visible and doesn't consume main content area. Also move Fetch/PAT buttons there.
130. **Two-column header: purpose left, identity right** — Title + subtitle + benefits on the left; name + org + quarter + meta tags stacked right-aligned on the right. No wasted horizontal space. AR toggle goes top-right.
131. **Always add tooltips and legends for Board audience** — G/A/R buttons, column headers (Schedule/Quality/Scope), KPI cards — every element needs hover `title` explanation. Board members won't guess what "Quality = Amber" means. Add a visible legend box above the table.
132. **Spell out acronyms in the title** — "OKR & KPI" means nothing to a CEO. Show "Objectives & Key Results / Key Performance Indicators" beside the short title. The abbreviated form is for navigation (sidebar), the full form is for comprehension (header).
133. **Use correct job titles consistently** — "IT Project Manager" not "PM" or "Project Manager". Check every occurrence: header, print summary, PM Scorecard title, meta tags. Title mismatches erode credibility in Board-facing reports.

## Email Analysis & Weekly Reporting Lessons (Mar 26, 2026)

134. **Verify topic categorization by participants, not keywords** — "Terms and Conditions (English)" was placed under Relief Center based on keyword match. Checking participants (Dr. Bahaa, Hacene, Syed) immediately revealed it belongs to BMS/KhairGate. Always check the participant list in topic_summary.csv before assigning new topics to project categories.
135. **Email volume ≠ project status — never set badges from data alone** — A project with 12 new emails might be on track; a project with zero emails might be critically blocked. Status badges (URGENT, ACTIVE, MEDIUM, etc.) must come from the PM's manual input, not automated email counts. Email data provides trends and counts; human judgment provides status.
136. **Checkboxes without persistence are decoration** — HTML checkboxes that reset on page refresh provide no tracking value. Either add localStorage persistence or accept that status tracking is manual. For weekly snapshot reports, manual Thursday review is sufficient — don't over-engineer interactivity.
137. **Embed the update process in the deliverable itself** — If a report has a recurring update workflow, document the process steps inside the report (e.g., info card on the Dashboard tab). Anyone opening the report should understand how it gets updated without needing external documentation.

## File Management & Deletion Safety Lessons (Mar 27, 2026)

138. **Never use bash `rm` on Windows — always use Recycle Bin** — Bash `rm` permanently deletes files with no recovery. Always use PowerShell safe delete: `[Microsoft.VisualBasic.FileIO.FileSystem]::DeleteFile('path', 'OnlyErrorDialogs', 'SendToRecycleBin')`. This applies to all user files, no exceptions. Lost 3 Skilljar HTML source files (~4 MB total) because `rm` was used instead of Recycle Bin.
139. **Always confirm which specific files to delete before any deletion** — When asked to "delete files not needed," never assume which files the user means. List all files with numbers and ask the user to pick. Assumption led to deleting original source files instead of generated ones.
140. **Source/original files are sacred — never delete originals** — Downloaded files, user-created content, and original sources may be irreplaceable. Only delete generated/derived files (and only after explicit confirmation). Generated files can always be recreated; originals cannot.
141. **Separate destructive actions from other work** — When a request includes both "delete X" and "do Y," handle the deletion as a standalone step with its own confirmation. Chaining "delete + open" led to rushing through deletion without proper verification.
222. **Archive old versions, keep only the latest in the root folder — never delete, always MOVE** — When versioned deliverables accumulate (v1–v12+), MOVE older versions to a dedicated archive subfolder and keep only the current version in the working directory. **Never delete, always move:** even when a user says "remove" or "delete" a version, MOVE it to the archive folder (`Enhanced standalone version/`, `Outlook Email Analysis Report/`, `Claude_Courses_Dashboard/`, etc.) — never `rm -rf`. Cost: V14 was permanently deleted in one session and had to be recovered from GitHub — the local copy was gone even though git had it. Workflow: create new version in subfolder → copy to root → move previous root copy to archive (or recycle if duplicate of subfolder copy). This reduces clutter, prevents accidental edits to old versions, and makes the current deliverable immediately identifiable. Cost example: v12 was left in root alongside v13 once, breaking the v1-v11 archival pattern. **Exception (see #167):** during active learning sessions or rapid iteration where multiple small changes accumulate within a day, in-place updates to the same version are preferred over creating v32, v33, v34 for each tweak — bump the version only for structural changes. Applies to dashboards (Course Dashboard subfolder pattern), Outlook reports (v1–v13 archival), Standalone HUB versions (V1-V14+), HTML/DOCX/MD deliverables, and any iterative file. (Folded former #155, #264, #265.)

## Course Dashboard & Content Extraction Lessons (Mar 27, 2026)

142. **Verify stats from the actual source HTML, not assumptions** — Course landing pages have exact lecture/video/quiz counts in `<span class="clp__stat-value">` tags. Claude Code in Action was 15 lectures (not 91 as assumed). Always read the HTML stats before hardcoding numbers. Wrong numbers propagated across 14 dashboard versions before being caught.
143. **Benefit cards must contain actionable content, not marketing language** — "Master context management strategies" is useless. `/init`, `CLAUDE.md` (3 locations), `#`, `@filename`, `/compact`, `/clear` — these are what the user actually needs. Every bullet should be a command, technique, or decision rule they can apply immediately.
144. **Always auto-open HTML deliverables in browser after creation or update** — Users want to visually verify every change immediately. Don't wait for them to ask "open it." This applies to new versions AND edits to existing versions. Code edits can look correct but render wrong.
145. **Course URL pages are the source of truth for stats, downloaded files are for content extraction** — The Skilljar URL page shows correct lecture count, video hours, and quiz count. The downloaded lecture HTMLs in subfolders are for extracting "What You'll Actually Use" content and "Lecture Breakdown" detail cards. Don't conflate the two sources.
146. **Track completion status accurately — "In progress" if any lecture is downloaded** — If even one lecture file exists for a course, the status is "In progress," not "Not started." Status should reflect actual evidence from the file system, not assumptions about user activity.
147. **All lessons in a completed course must show "Saved" tag** — When a course is marked Completed with a certificate, every lesson in that course's detail card must have the "Saved" badge, even if the individual lecture HTML wasn't downloaded as a separate file. The user completed the course = they consumed all content. Never leave the last lesson without a "Saved" tag in a completed course.
148. **Never trust sidebar stats in saved HTML pages** — Skilljar pages render OTHER courses' stats in JS sidebars. Claude Code in Action showed "15 lectures" which was actually a different course's stats. The real count was 21. MCP showed 16 but was actually 14. Always verify from the actual Skilljar URL page or user-provided screenshots.
149. **YouTube playlists are the source for video durations, not Skilljar sidebar data** — Subagents course showed "0.4h" (~24 min) from Skilljar sidebar, but the actual YouTube playlist totaled 15 min (4:44 + 3:42 + 2:49 + 3:45). Always check the video source directly.
150. **"Estimated time" on Skilljar = reading time, not video duration** — Each lecture page shows two separate values: "Estimated time: 20 minutes" (total reading time) and "(4 minutes)" below the heading (video duration only). Don't confuse or merge them.
151. **Keep the Recommended Learning Path section in sync with course status** — When a course is marked Completed or In Progress in the Course Comparison table, the Learning Path cards must be updated too (green border + "COMPLETED" or blue border + "IN PROGRESS"). This was missed for Course 2 and Course 3 initially.
152. **Use full course names everywhere in the dashboard** — Tab names, Topic Coverage Matrix headers, Course Comparison headers must all show the full official course name. Abbreviated names like "Claude API" or "MCP" cause confusion. All course name headers should also be hyperlinks to the official Skilljar URL.
153. **Record certificate earned date/time in 3 places** — When user reports a certificate: (1) Learning Path card with timestamp, (2) Certificate row in Course Comparison, (3) Status row in Course Comparison. Use the time the user reported it as the timestamp.
154. **Every recurring scan must complete the full update checklist** — Scan → compare → if changes: create version → update folder_snapshot.md → update CLAUDE.md → open browser. Skipping any step (especially snapshot and CLAUDE.md) creates drift that compounds across versions.
156. **Add external resource links (YouTube playlists, docs) to benefit cards** — When a course has associated YouTube playlists or reference URLs, add them as clickable links at the bottom of the "What You'll Actually Use" benefit card. Users need quick access to these resources.
157. **Hide redundant tracking rows from comparison tables** — If download/progress status is already visible in each course tab (Saved tags + lecture breakdown count), don't duplicate it in the Course Comparison table. Redundant information clutters the overview.
158. **Use Playwright for dynamic web data, not WebFetch** — Platforms like Skilljar hide duration/quiz stats behind dynamic rendering. WebFetch returns empty static HTML. Playwright sees the actual rendered page with all stats (lectures, hours, quizzes). Go straight to Playwright for any learning platform data extraction.
159. **Keep team columns alongside role columns in recommendation matrices** — Teams (Qatar, Mobile, Flutter) represent real organizational units with specific people who need personalized course guidance. Don't remove them — each team member needs to see their column to know which courses to take. Map team recommendations based on the actual roles of team members.
160. **Transpose wide tables — items as rows, not columns** — Any table with 10+ columns causes horizontal scroll and is hard to read. Flip it: put items (courses, projects, etc.) as rows and attributes/roles as columns. A 15-row × 8-column table is far cleaner than an 8-row × 15-column table.
161. **Never leave placeholder dashes when real data exists** — If a course page shows "1.1 hours of video" and "2 quizzes," the catalog must show those numbers, not "—". Leaving placeholders makes deliverables look incomplete and undermines credibility. Always scrape ALL available metadata before publishing.
162. **Sort comparison tables by difficulty level, not enrollment status** — Foundation → Beginner → Intermediate → Advanced with section headers. This shows the natural progression path at a glance. Grouping by "enrolled vs not enrolled" is self-centered; grouping by level is useful for everyone reading the catalog.
163. **Scan on demand, not auto-timer** — User prefers manual "scan" command over 15-minute auto-loop (/loop). Auto-scanning creates noise when the user is actively downloading lectures and wants control over when updates happen. Cancel the cron and wait for explicit "scan" instructions.
164. **Screenshots contain knowledge for benefit cards, not file tracking** — When a user downloads screenshots from course videos, READ the image content and add useful information to the "What You'll Actually Use" section. Do NOT track screenshots as per-lecture download badges — the user doesn't care about the file, only the knowledge in it. Only track actual downloadable resources (notebooks, zips, PDFs) per lecture.
165. **Quiz questions and answers are actionable knowledge — add to benefit cards** — Every quiz question + correct answer is a tested, practical rule that belongs in the "What You'll Actually Use" section. They're not just test results — they're the distilled, exam-worthy knowledge from the course. Include them as "Quiz Insights" in the benefit card.
166. **Scan before quiz, not after** — Always update the dashboard with new lecture downloads BEFORE the user takes a quiz. The user wants to see their progress current before starting the assessment. Don't wait for quiz completion to scan.
167. **In-place dashboard updates are OK during active learning sessions** — When rapidly iterating (downloading lectures, taking quizzes back-to-back), updating the same version (e.g., v31) throughout the day is fine and preferred over creating v32, v33, v34 for each small change. Create new version numbers only for structural changes.
168. **Track ALL downloadable file types, not just notebooks** — Scan for .ipynb, .zip, .pdf, .csv, .md, .py, .txt — any file the user downloaded separately as a course resource. Add them as blue badges (+1 notebook, +1 zip) on the relevant lecture with a "Download:" line showing the filename. Use code-tag styling for filenames.
169. **Course completion requires a full update checklist** — When a course is completed with certificate: (1) Course header → Completed + certificate timestamp, (2) Learning Path card → green border + COMPLETED, (3) Course Comparison table → status + certificate rows, (4) Page 2 catalog → status updated, (5) Stats bar → lectures saved count, (6) CLAUDE.md → version entry updated. Missing any step creates inconsistency.
170. **Deduplicate multi-language course listings** — When a course is offered in 10 languages, show it as ONE row with a "Languages" column listing all variants, not 10 separate rows. Keeps tables readable and prevents false inflation of course counts. Note the primary/English version link.
171. **Complement technical courses with PM-perspective courses** — Technical courses (Anthropic: API, tools, MCP) + PM courses (PMI: governance, ethics, adoption) together create a complete skillset. When building course catalogs, include both perspectives and explain how they complement each other.

### March 29, 2026 — Audit Engine Fix Session (KhairGate V13)

172. **Verify both sides of a PS1↔HTML API contract** — When PowerShell returns `items` but HTML reads `check.details`, the data silently disappears. Always grep both the server response field names AND the client rendering field names before declaring an API "working." Cost: 183 false warnings displayed to user.
173. **Never pre-escape AND use [regex]::Escape()** — PowerShell's `[regex]::Escape()` already handles special chars. If the whitelist array contains `v13\.0` (pre-escaped) and then `Escape()` runs again, it produces `v13\\.0` which matches a literal backslash, not a dot. Use plain strings in the array: `@('v13.0')`.
174. **Case sensitivity across language boundaries** — PowerShell returns `"PASS"` (uppercase) but JavaScript checks `==='pass'` (lowercase). Cross-language APIs must normalize case. Add `.toLowerCase()` on the JS side or return lowercase from PS1.
175. **Health dashboard push requires FULL content sync, not just badges** — Workflow G rule: when pushing to Hub V2 after a drift fix, update ALL health-dashboard content (stories, SP, prerequisites, dates, cards) — not just the verdict line. Partial updates create inconsistency that erodes board trust. Cost: 3 follow-up commits to fix what should have been 1.
176. **Add automated date consistency to audit engine** — Document Control "Updated" dates drifted 4 days behind footer "Modified" dates across 7 files without anyone noticing. Automate: extract footer date, compare with DocControl date, flag if DocControl is older. Added to Check 6 (Cross-File Data Consistency).
177. **SP/Story regex must use global matching with "any match" logic** — `-match` returns only the FIRST occurrence. Files with Phase 0 subset values (133 SP) before the total (3,882 SP) trigger false positives. Use `[regex]::Matches()` to find ALL occurrences, then flag only if NONE match the expected value. Also exclude files that legitimately contain only subset numbers.
178. **Status buttons must persist to HTML file, not just localStorage** — Board members opening the standalone file see DEFAULT states, not the PM's reviewed states. Solution: updater server `/api/save-status` endpoint writes `st-X` class + button text directly to the HTML file. Graceful degradation: if server not running, localStorage still works. Arabic text must be extracted from the file's own `data-labels-ar` attribute (not the JSON payload) to avoid encoding corruption.
179. **Bilingual span count must exclude CSS style blocks** — Audit Check 3 (bilingual balance) was falsely flagging 7 files with 10-27% AR > EN gap. Root cause: CSS selectors like `[lang-ar]{display:none}` and `body.ar div[lang-ar]` contain ~14 extra `lang-ar` references per file for the bilingual toggle. Fix: strip `<style>` blocks before counting. After stripping, all files are under 2.5% gap.
180. **Readiness progress cards must match status button states** — KICKOFF_READINESS_GUIDE showed "Technical Readiness 0%, 2 ADRs pending" while ADR buttons were all `st-1` (Approved). Stale readiness cards contradict the button states and mislead the Board. Added to audit-check agent Check 7: verify readiness card text matches button states.
181. **Hub V2 sync needs a dedicated subagent** — Manually syncing health-dashboard + phase0-poc-readiness after every drift fix caused 7 follow-up commits in one session (ADR-2 missing, prerequisites wrong, reports count wrong, R-08 not updated, variance reports stale). Created `hub-sync` agent to automate: reads V13 state, updates both Hub V2 pages + copies variance reports, returns diff summary.
182. **Browser caches PS1 server responses** — After updating the PS1 audit engine (6→11 checks), the Audit Only button still showed 6 checks even after server restart. Root cause: browser cached the old HTML page. Fix: Ctrl+Shift+R (hard refresh) after every server restart. Consider adding cache-busting headers to the PS1 HTTP response.

## Team Roster & Data Deduplication Lessons (Mar 30, 2026)

184. **One row per person in team rosters — combine groups into a single column** — When a team member belongs to multiple groups (e.g., Qatar Team + Mobile Team + Flutter), list them once with all groups in a comma-separated "Groups" column. Repeated rows inflate counts, cause confusion, and make updates error-prone. If you change a name, you'd have to find every duplicate row. Cost: DevOps_Teams.md had 15 rows reduced to 9 after deduplication.
260. **Always cross-reference team roles across ALL sources before answering** — The same person can have different titles in V15 RACI, Hub V2 pages, and DevOps Teams file. Example: El-Afify = "Odoo Consultant" (V15) vs "Business Analyst" (Hub V2) vs "Implementation" (DevOps Teams). Always read project_team_roster.md first — never answer from a single source. Cost: Multiple incorrect role attributions across sessions before full cross-reference was built.
261. **Maintain a team alias map for name fragments** — Team members are referenced by different name fragments across project files. Key aliases: "Basha"/"Salah"/"Mohamed Basha" = Mohamed Salah Basha (Tech Lead/EM), "El-Afify"/"Afifiy"/"Mahmoud Afify" = Mahmoud El-Afify, "Hatem"/"xhatem" = Hatem Abdelaziz, "Jassem" = Jassim Mohamed. Without the map, commit attribution and role questions produce wrong answers.
276. **Map every point in a team member's commit against PM artifacts before responding** — When reviewing a commit that proposes changes (like Hatem's Proposed Plan with 21 points), map each point against the original PM documents. Missing even one conflict (like Scenario C: BLOCKED vs Foundation proceeds) weakens the PM response and lets scope overreach go unchallenged.

## Localhost Server Management (Mar 30, 2026)

185. **Assign unique ports per project server and maintain a port map** — Multiple localhost PowerShell servers across projects (Outlook, KhairGate, Source-Root) can silently collide if they share the same port. Since many servers expose `/api/status`, an HTML dashboard will "connect" to whichever server grabbed the port first, then fail on project-specific endpoints. Maintain a port map (5050=Outlook, 5051=KhairGate, 5052=Scan, 5053+ next). Before creating a new server, grep all `.ps1` files for `$Port =` to verify no conflicts. Cost: Scan button returned "Unknown endpoint" because KhairGate server was on the same port.

## Executive Reports & One-Pagers (Mar 30, 2026)

188. **Pull from ALL data sources upfront when building executive reports** — Check OKR Dashboard, Outlook analysis, HR folder, DevOps audit, Source-Root index, Monthly Reports, and project-specific folders (e.g., Enhanced Standalone for HUB). Missing a single source means missing context. Cost: 4 additional sources (Source-Root, DevOps, HR, Pearl Pixels Quarterly Report) were missed in the first draft and had to be added iteratively.
189. **Two questions = two visually separate answer blocks** — When a director asks two questions in one request, structure the document with distinct labeled sections (Q1, Q2) and a visual divider between them. Don't blend answers into a single narrative — each question deserves its own clearly labeled block so the reader finds the answer immediately.
190. **Never duplicate data across sections in the same document** — Before finalizing, audit every section — each fact should appear in exactly one place. OKR targets stay in OKR section only. Portfolio items stay in portfolio table. Team development items covered by OKRs don't need their own section. Duplicates waste space and look unprofessional on a one-pager.
191. **Director-facing documents show only pending and planned, not completed** — Filter every item: if it's done, exclude it. The director wants a forward-looking view to understand what's coming, not a retrospective of what happened. Completed work belongs in quarterly reports, not in Q2 plans.
192. **"Hiring and assignment" — not just "hiring"** — When discussing resource needs, always consider both new hires AND reassignment of existing team members. The user corrected "Pending hiring" to "Pending hiring and assignment" for HUB project because some roles may be filled by moving existing staff.
193. **Verify position titles and candidate names with the user** — Don't assume role titles from earlier data. User corrected "Sr. Odoo Developer" to "Sr. Backend — Django & Python" and added Moqbel Elseaedy alongside Yousef Ashraf. Always confirm exact titles and full candidate lists.
194. **Handover completion doesn't mean process compliance** — When told "handover is done", the real issue may shift to process discipline. Hacene's handover was completed, but the actual problem was pushing to production and creating features without team alignment. Ask: "What's the current issue?" not just "Is it done?"
195. **OKR sections for directors need full KR numbers and measurable targets** — Compressed OKR summaries ("RC launch, BMS Sprint 5") lose the measurable specifics directors need. Show all 12 KRs with KR numbers (KR1.1, KR2.3) and exact targets (< 5 bugs, 80%, By Jun 30). The dashboard already has this data — don't compress it away.
196. **Check who's NOT on a platform — the gap matters more than who IS** — When user said "DevOps is mandatory", the key insight was naming the 5 specific people NOT on DevOps (Hossam, Muram, Walied, Ajay, Hacene). Listing the gap by name creates accountability. Don't just say "some members need onboarding."
197. **Risk levels must be confirmed with the user, not assumed** — I set Taqat Property to "Low" risk because docs were ready, but user corrected to "Medium." Risk perception depends on business context the PM understands better. Always default to user's assessment.
198. **8 iterations is normal for director-facing documents — plan for it** — The Q2 PM plan went through v1→v8 with corrections at each step. Each version fixed real issues (wrong status, missing projects, duplicated data, missing OKR details). Don't try to get it perfect in one shot — iterate, but track what changed in each version.
199. **Always save lessons to global_lessons.md, not just memory files** — Memory files are project-scoped and conversation-scoped. Global lessons persist across all projects and all future sessions. If a lesson applies broadly (report building, stakeholder communication, process discipline), it belongs in global_lessons.md.
737. **Trust DevOps closure dates — never second-guess or flag as "carried over"** — The `Custom.ClosureDate` field is the single source of truth for which month a work item belongs to. If the closure date says March, it's March — even if the item appeared in an earlier report. Do NOT cross-reference against previous report contents or create special "Carried Over / Re-dated" categories. Categorize items by their work type only. Cost: Flagged 4 items as "re-dated" and proposed a "Carried Over" category, delaying the March 2026 report and requiring user correction. (Relocated from "Executive Summary Reports (Apr 1, 2026)" — single-lesson category absorbed.)

## Cross-Project Dashboard & Query Copy (Mar 30, 2026)

200. **Use az CLI Bearer auth instead of PAT tokens for DevOps automation scripts** — PAT tokens expire, require manual rotation, and are easy to leak. `az account get-access-token --resource 499b84ac-1321-427f-aa17-267ca6975798` gives a short-lived Bearer token using existing login. Add a `-UseAzCli` switch to scripts so both auth methods are supported. Cost: Every script run previously required pasting a PAT; now it's zero-friction with az CLI.
201. **Copy queries BEFORE dashboard widgets — widgets depend on query IDs** — Dashboard chart widgets reference query GUIDs in their settings. If you copy widgets first, they'll point to non-existent queries and show errors. Always: (1) copy/create queries, (2) capture new query IDs, (3) replace source query IDs in widget settings, (4) then create widgets. Cost: Widgets with wrong query IDs show "Last time you checked, there were no results" permanently.
202. **For targeted widget additions, use direct az REST calls instead of the full copy script** — The full `Query-and-Dashboard-Copy.ps1` copies ALL queries and ALL widgets. When you only need 3 specific charts (e.g., adding frontend team to an existing dashboard), direct `az rest` POST calls are faster, more precise, and don't risk duplicating existing content. Use the script for new project setup; use direct API for incremental additions.
736. **Dashboard API URL requires project-level path for project-scoped dashboards** — Team-scoped dashboards use `/{project}/{team}/_apis/dashboard/dashboards/{id}` but project-scoped dashboards only need `/{project}/_apis/dashboard/dashboards/{id}` (no team segment). If a dashboard returns 404, try removing the team context from the URL before assuming it was deleted. When replicating widgets across dashboards in the same project, create team-specific queries first (update the group filter in WIQL), then update all embedded query IDs in widget settings (`groupKey`, `filter`, `queryId`).

## HR & Recruitment (Mar 31, 2026)

212. **Always check the email thread for ALL active JDs before evaluating any candidate** — A single recruitment folder ("Senior Odoo Developer") contained TWO separate active positions: (1) Senior Odoo Developer and (2) Sr. Backend Engineer — Django & Python. First evaluation scored Amr Deep against the Odoo JD (15% match); the correct Django JD yielded 35-40%. Cost: entire interview form had to be rewritten. Always read the .msg files to identify ALL open positions before building any evaluation.
213. **One recruitment folder can contain multiple positions with separate criteria** — Don't assume one folder = one role. The Basha email added a second position (Django & Python) alongside the original Odoo role. Both are active simultaneously with different JDs, different criteria, and different candidate pipelines. Always ask which position a candidate is being evaluated for.
214. **Color-code interview forms by role type for visual distinction** — Odoo interviews use purple (#714B67), Django/Python interviews use blue (#1a5276). When multiple role types share a recruitment folder, visual distinction prevents confusion during panel reviews.
215. **"Revised" or "new JD" doesn't always mean replacing the old one** — When an email says "refine position JD not to be Odoo BE developer, instead it should be Sr. Backend", it may mean adding a second position, not killing the first. Both the Odoo and Django roles were active simultaneously. Always confirm: is this a replacement or an addition? Assuming replacement when it's an addition means you stop evaluating valid candidates for the original role.
216. **Match each candidate to their target position at intake, not during evaluation** — When a new CV arrives, the first question should be "which of our open positions does this candidate map to?" before any scoring begins. Amr Deep's CV was initially scored against the wrong JD because the position mapping wasn't established upfront. Establish position → candidate mapping before building any evaluation artifact.

## OKR & KPI Dashboards (Apr 1, 2026)

223. **Never fabricate project-specific data in OKR/KPI dashboards** — Sprint numbers, deadlines, milestone names, and scope definitions must come from the user or a verified source (Azure DevOps, project documents, emails). If the information wasn't explicitly provided, ask — don't invent it. Cost: BMS "Sprint 5" with a "June 15" deadline was assumed and placed as KR1.2; neither the sprint number nor the date was verified, requiring correction after delivery.
224. **Show all active projects by default, then recommend — not the other way around** — When building OKRs or dashboards, first list ALL projects with their real DevOps data (iterations, dates, status). Then recommend which ones are critical. Never cherry-pick projects based on assumptions about which are "biggest" or "most active." The user decides the scope, not Claude. Cost: O1 originally had only 3 KRs (RC, BMS, Aqraboon+Wallet) when 8 projects had active Q2 sprints — 5 projects were invisible.
225. **Never merge unrelated projects into a single KR** — Each project has its own sprints, teams, and timelines. Combining "Aqraboon + Wallet" into one KR because they seemed "secondary" hid the fact that they have completely different cadences (Aqraboon Sprint 1 ends Jun 2, Wallet Sprint 6 ends Apr 23). One KR = one project = one measurable milestone.
226. **Always fetch iteration dates from Azure DevOps `_settings/work` before setting KR deadlines** — The source of truth for sprint start/end dates is `https://dev.azure.com/TaqaTechno/{ProjectName}/_settings/work`. Never assume sprint numbers or dates. Fetch first, then build the dashboard.
310. **Adding a new team to OKR dashboard requires 7 atomic updates** — When adding a new team (not just a member), update all 7 locations together: (1) version tag, (2) stats bar team/member counts, (3) CSS header gradient class, (4) HTML team card with full metrics grid + expandable member table, (5) teamDefs array, (6) memberDefs array, (7) teamInfoMap. Missing any one silently breaks drill-downs or WIQL queries. Always create a new version file — never edit in place.

## HTML Dashboard Search (Apr 1, 2026)

228. **Search boxes must use multi-word AND matching, not exact phrase matching** — When a user types "Executive Summary Aya 2026", they expect results where ALL words appear anywhere in the content — not as one contiguous substring. Using `text.includes(fullQuery)` fails when words are scattered across different parts of the same card or section. Always split input into words and check each independently: `words.every(w => text.includes(w))`. Cost: Claude Sessions Index search returned zero results for "Executive Summary for Aya Allah Nady Shapan for 2026" even though the data was present in the same card — the words just weren't adjacent.
229. **HTML dashboards that summarize CSV data lose searchability — bridge the gap** — When an HTML report shows curated summaries but the raw data lives in CSV files, any detail not manually written into the HTML becomes invisible to search. Solution: add a "Deep Search" mode that queries the CSV files via a local server endpoint (e.g., localhost:5050/api/search). This keeps the HTML lightweight while making all raw data searchable on demand. Cost: 4 email records about Aya Allah Nady Shapan existed in cat_other_projects.csv but were completely unfindable from the Outlook v12 HTML search panel.
255. **Search results should replace content, not stack on top** — When a dashboard has many sections, showing search results while keeping all other content visible creates noise. Enter key should hide everything (stats, diagrams, tabs) and show only results. Escape or clicking a result restores the normal view. Implemented via a CSS class toggle on the container.

## OKR & KPI Trend Charts (Apr 1, 2026)

230. **Trend charts must render with 1+ data points, not 2+** — A single-week baseline is still valuable data. Use dots for 1 point, lines for 2+. Never gate chart rendering behind a minimum week count without asking the user. Cost: User clicked Save and saw empty charts with misleading placeholder text.
231. **Add data tables below every SVG chart** — SVG dots/lines are hard to read precisely. A small HTML table below each chart with exact values per week makes the data board-ready and accessible when chart labels overlap.
232. **Heatmap cells need text labels (G/A/R), not just colors** — Color-only heatmaps are ambiguous (yellow could mean "at risk" or "no data"). Always include text inside cells and use gray (#e8e8e8) for "no data" to distinguish from amber/yellow status.

## KhairGate Drift Fix & Autonomous Execution (Apr 1, 2026)

233. **When user says "yes do it" — execute autonomously** — Don't ask "should I do it before or after X?" Follow the workflow rules, decide the approach, execute, and present the result for approval. Asking at every fork wastes time and frustrates the user.
234. **Re-fetch origin AFTER completing a drift fix, BEFORE pushing** — Hub V2 moves faster than drift fixes. V14 was built for E19 (947/3,928) but E2 arrived during the fix (+1/+3), making V14 stale before it was even pushed. Required immediate V15.
235. **If version-bump subagent fails, run sed commands directly** — The version-bump agent for V15 failed because it couldn't execute Bash. Don't relaunch — just run the sed replacements manually. Wasted time on a failed agent is worse than 30 seconds of direct sed.
262. **"No drift" Hub V2 pulls still need a HUB_PULL_HISTORY entry** — Even when Hub V2 commits don't affect standalone PM artifacts, document every commit in HUB_PULL_HISTORY.html with a "NO DRIFT" badge. Every pull must be tracked regardless of impact level. This maintains audit completeness and prevents gaps in the commit trail.

## Portfolio Health Scoring Methodology (Apr 1, 2026)

236. **Always cite framework references before recommending a scoring approach** — When recommending a methodology (e.g., worst-case vs. weighted scoring for RAG aggregation), verify which PM standards actually endorse it before presenting it as a recommendation. PRINCE2 and SAFe endorse worst-case; PMI Portfolio Standard endorses weighted scoring; PMBOK prescribes nothing. Never recommend an approach and attribute it to a standard without verification. Cost: Initially recommended "Average + Floor Rule" as the best approach with no standard backing — had to retract and re-research when challenged for references.
237. **When changing a calculation formula, audit every consumer of its output** — Changing the Portfolio Health Overall formula from worst-case to PMI Weighted Scoring caused the badge text to change from `'On Track'` to `'On Track (2.65)'`. Every JavaScript comparison using exact-match (`=== 'On Track'`) broke silently — Projects On Track counter, weekly snapshot saves, and PM Scorecard roll-ups all failed. Always grep for every reference to the output value and update comparisons (e.g., use `indexOf` instead of `===`). Cost: 3 broken counters across Executive Summary, PM Scorecard, and Save/History that would have shown 0/11 projects on track despite all being green.
238. **Bilingual dashboards must translate tooltips (title attributes), not just text content** — The `data-i18n` translation system only handles `textContent` via `el.textContent = T[key]`. HTML `title` attributes (hover tooltips) are invisible to this system and remain English-only when switching to Arabic. Solution: add a parallel `data-tip-i18n` attribute system and extend `applyLang()` to swap `title` attributes, storing originals in `data-tip-original`. **Alternative attribute convention:** some dashboards use `data-en-title` + `data-ar-title` pairs with switching logic baked into `switchLang()` instead of the `data-tip-i18n` + dictionary pattern — pick one convention per project and stay consistent. Applies to every bilingual HTML report with tooltips and EN/AR toggle. Cost: 20 tooltips across Executive Summary, Work Item Flow, PM Highlights, and Portfolio Health headers stayed in English while all surrounding text was Arabic — inconsistent bilingual experience for CEO presentation. (Folded former #258: "Tooltips need bilingual support in every HTML deliverable".)

## PM Framework Coverage Gaps (Apr 1, 2026)

239. **PM²-Agile covers individual project reporting, not portfolio-level health aggregation** — The PM²-Agile course materials (7 sessions, 33 templates) provide Status Report templates (#19, #20) and Development Status Reports but do not prescribe how to aggregate multiple RAG dimensions (Schedule/Quality/Scope) into an Overall score, or how to roll up health across a portfolio. Don't assume a PM course covers every PM topic — verify before citing it as a reference.
240. **PMBOK does not prescribe a specific RAG aggregation formula** — PMBOK 6th Ed Section 4.5.3 (Work Performance Reports) describes traffic-light reporting but explicitly leaves the aggregation methodology to "organizational process assets." When a stakeholder asks "what standard backs this?", PMBOK is not a valid answer for the specific calculation — only for the concept of using RAG status. Cite PRINCE2 Ch.12 for worst-case or PMI Portfolio Standard for weighted scoring instead.

## Quarterly Report Build & Data Pipeline (Apr 2, 2026)

241. **Read the actual template file before replacing values** — The Q2 Performance Report template had v7 KPI values (3,934.51) not v11 values (4,341.61). The replacement script searched for wrong numbers and left stale data in the KPIs. Always read the target file's actual values before writing a replacement script.
242. **Replace entire HTML blocks, never individual numbers** — Replacing "932" globally hit 9 unrelated locations (bar charts, commit counts, table cells). Use regex to match the full containing element (e.g., the entire KPI grid div) and replace it as a block. Bare number replacement is never safe in HTML.
243. **Regenerate every data section in one pass, not incrementally** — Replacing tables but missing trend bars, chart.js arrays, employee cards, and insight cards produces a broken hybrid of Q1 and Q2 data. Build a single script that regenerates all data-dependent sections from the JSON source in one execution.
244. **Check the data before asking the user** — Questions like "Is Water for Mosques paused?" and "Is IndoQat real?" are answerable from the query data. Don't ask the user to validate what the data already shows. Only ask about decisions (team structure, merge rules) not facts.
245. **Verify team structure from the latest authoritative source before building** — Q2 has 3 teams (Egypt/Qatar/Mobile per OKR/KPI v5), not the Q1 structure of 2 teams. Building reports with wrong team grouping required a full rebuild. Always check the current team definitions from the OKR/KPI dashboard or DevOps_Teams.md before starting.
246. **Get answers before generating — not after** — Generating reports with DRAFT placeholders, wrong team structure, and stale content, then asking for review wastes time on both sides. Collect all inputs (team roster, project status, achievements) before running the build script.
247. **Use MCP tools as PAT fallback, not just PowerShell** — When the PAT expired mid-session, MCP tools (`wit_get_query_results_by_id` + `wit_get_work_items_batch_by_ids`) successfully fetched all 366 items. Always have two data paths: PowerShell script (fast, bulk) and MCP tools (auth-independent, always available).
248. **Never show a report to the user without self-validation first** — If the KPI says 3,934 but the JSON sums to 1,386, that's the builder's bug to catch. Run a validation check (KPI values vs JSON aggregates, no Q1 remnants, correct employee count) before opening the report.
249. **Pre-build checklist is mandatory before any quarterly report** — Before generating: (1) read OKR/KPI for current team roster, (2) check who left/joined from task data, (3) confirm new projects from query, (4) read actual template values, (5) confirm team structure with user. Skipping this caused 1.5 hours of rework in Q2 build.
250. **Patching HTML is always slower than full generation** — Incremental find-replace across a 500K HTML file caused 5 rebuild cycles (wrong KPIs, orphan elements, stale bars, stale cards). A single full-generation script that builds all sections from JSON in one pass eliminates this class of errors entirely.
251. **When user says "okay" or "sure", execute immediately** — "Okay build it" means build it now, not "I designed the concept, want me to build it?" Asking for re-confirmation after the user already approved wastes time. This happened twice in the Q2 session (skill creation and refresh explanation).
252. **Read all 3 PM reports before working on any of them** — Monthly Performance, OKR/KPI Dashboard, and Quarter Plan share interconnected PM insights (risks ↔ KRs ↔ data). Working on one without reading the others produces disconnected insights. Always glob for latest version from each folder — never hardcode version numbers.
718. **ClosureDate is PM action, not developer completion** — PM closes items every Wednesday/Thursday. Developer completion = state changed to "Done" (from revision history). All time metrics (lead time, cycle time) must use the Done date from revisions, NOT ClosureDate/ClosedDate.
719. **Fetch form fields, not query columns** — Query columns can be hidden or changed by the user at any time. Always request fields directly in the API call. This makes the data pipeline independent of query display configuration.
720. **Exclude training/meeting items from time metrics** — Items with predetermined durations (training, courses, meetings, onboarding, KT sessions) have perfect estimation ratios by definition. Including them inflates accuracy scores and skews lead time averages. Filter by title keywords.
256. **Sprint length and iteration settings are per-project in Azure DevOps** — Don't assume 2-week sprints globally. Check `https://dev.azure.com/TaqaTechno/{PROJECT_NAME}/_settings/work` per project (or use MCP `work_get_team_settings`/`work_list_iterations`). Sprint definitions affect velocity calculations, carry-over detection, and completion rate thresholds.
257. **Don't duplicate content across sections in the same report** — If Challenges & Lessons and Key Insights both exist, merge into one section. Creating both caused confusion about where metrics-driven insights belong. One section per purpose, no overlap. In the CEO report, Key Insights is the single home for all PM observations.

## Tool & Environment (Apr 2, 2026)

721. **Paths with special characters break Windows shell open commands** — The `&` character in folder names (e.g., "Taqat Trading & Business Solutions") gets escaped as `^&` causing "file not found" errors in cmd/PowerShell/bash. Always copy files to a clean path (e.g., D:\tmp) before opening programmatically. This failed 3 times before the workaround was established.

## Claude Automation (Apr 5, 2026)

263. **Subagents for large-scale HTML edits plan instead of executing** — When delegating bulk translation or multi-section HTML modifications to background agents, they consistently produce plans rather than making edits. After 4 failed agent launches in one session, the work had to be done directly. For large HTML modification tasks (100+ elements), do the work directly in the main conversation instead of delegating.
279. **Always verify i18n.js syntax with `node -c` after adding translation entries** — A single missing comma breaks the entire translation dictionary and no Arabic toggle works on any page. Run `node -c path/to/i18n.js` after every edit. Cost: missing comma on line 918 broke all Arabic translations across both assessment and POC readiness pages until caught by syntax check.

## File Organization & Versioning Hygiene (Apr 6, 2026)

287. **Each document version gets its own Created date — no history chain needed** — Each versioned file (v1, v2, v3, v4) is a standalone snapshot with its own "Created" timestamp. Don't add version history tables tracking all previous versions' dates — the file system and CLAUDE.md already track that. Keep the document clean.
298. **Remove departing employees before provisioning, not after** — Eslam Hafez was leaving in 1 week. If all 17 users had been provisioned at once, a $6/month license would have been wasted. Phased batching naturally catches this — always verify the roster against HR status before each batch.
300. **Dashboard cards with version numbers go stale silently** — The Access Upgrade Summary card pointed to v2 while v5 existed. No one notices until they click and see outdated content. Every time a new version is created, update all referencing dashboards in the same session. (See also #203 for the supporting-document variant of the same drift mode.)

## Phased Rollout & License Management (Apr 7, 2026)

299. **Phased rollout splitting factors are dependency-based, not cost-based** — Cost is the same $142/month regardless of batching. The real factors for splitting: who blocks others (dependency), who trains others (role), who is senior vs junior (seniority), does each team have coverage (team balance), and risk reduction (validate licensing channel with small group first).
302. **Users on different platforms don't need the same tool license** — Walid uses GitHub for task tracking, not Azure DevOps. Don't assume every team member needs the same license. Ask about their actual workflow before including them in an upgrade plan.
303. **Running totals cascade on every single user change** — Removing 1 user from Batch 2 changed: user count, Basic count, monthly cost, annual cost, all 3 scenario cards, all batch running totals, and all 4 modals. One person = 20+ edits across the file. Always use a systematic checklist.

## Dashboard Data Integrity (Apr 6, 2026)

266. **Never assume project status — always pull from the actual source** — Assumptions about Relief Center being "Behind" and BMS being "At Risk" were wrong. Real OKR v5 Portfolio Health data showed both ON TRACK (3.00). Always read the source dashboard before assigning status labels. Cost: had to redo all 12 project badges after user caught the mismatch.
267. **Verify sprint counts from DevOps API, don't estimate** — Initial count said "6 active sprints" but querying all 27 projects with `work_list_team_iterations` revealed **13** had current sprints (timeFrame=1). The first query only checked 6 projects. Cost: presented wrong number to PM before meeting.
268. **One project, one name — check for duplicates before displaying** — "Taqat Property" and "Property Management" are the SAME project (DevOps name vs display name). Dashboard showed both as separate entries with different badges. Always verify project name mapping against DevOps project list before creating cards.
722. **Data gaps are more damaging than bad numbers** — Tasks without Estimated Time or Actual Time trigger direct management questions. Management doesn't look for perfect numbers — they look for understandable, complete numbers.

## Leadership Presentation Design (Apr 6, 2026)

269. **Keep leadership dashboards to one page with expandable cards** — Removing 15+ project tabs and using expandable cards on a single dashboard page was the right call. During a meeting, clicking through tabs is slow and loses the audience. Click-to-expand keeps context visible while allowing drill-down.
270. **Align status badges across all dashboards shown in the same meeting** — If OKR Dashboard v5 says "G — On Track (3.00)", the presentation dashboard must say exactly the same. Using different labels (e.g., "HIGH RISK" vs "On Track") for the same project creates confusion and undermines credibility with management.
271. **Understand WHY you clone a UI template before building** — v12 Outlook Email Analysis Report was chosen for its sidebar+cards+search pattern. Understanding the pattern's strengths (navigation, search, badges, expandable sections) led to better adaptation for the meeting dashboard than blindly copying HTML.
723. **Management evaluates the presenter, not just the presentation** — Semir wasn't just reviewing dashboards — he was assessing whether Mahmoud controls the numbers or just displays them. Every management meeting is a dual evaluation (content + competence).
724. **Use measurement language, never justification language in management presentations** — "Deviation" / "time gap" / "corrective plan" vs. "people" / "circumstances" / "pressure." The words you choose signal whether you're in control or making excuses.
725. **The golden rule for management impression: numbers + understanding + plan** — A beautiful dashboard means nothing. Management wants a manager who controls the numbers, understands the deviations, and has a corrective plan.
482. **Executive portfolio reviews are 15-minute high-level only — drill-downs stay as backup tabs, never lead** — On 2026-04-20 Semir set the style rule for every weekly Project Management Review: ~15 minutes maximum, focus on projects status / cost / schedule / teams / high-level indicators (SPI / CPI, red/amber/green), pattern "Where we are / What's next / Key risks." Deep technical drill-downs are banned during the live walkthrough — they reduce audience engagement and signal operator-mode instead of owner-mode. Paired governance rule: every red/amber indicator must trigger a documented action, not just a display. **Why:** Senior management needs concise, action-oriented signal. Comprehensive reporting shifts the frame from "manager who controls the numbers" (Lesson #294) to "operator who displays the dashboard" (Lesson #292). An exhaustive 45-slide walkthrough actively destroys the impression you are trying to build, regardless of content quality. **How to apply:** (1) When refreshing exec-review decks and talking points, structure for a 15-minute read — cut, don't add. (2) Default the live dashboard walkthrough to Executive Summary + Portfolio Status only; keep SBI / Team Pulse / Data Gate / Insights as expand-on-ask backup. (3) Open with the literal pattern "Where we are / What's next / Key risks (cost, schedule, resources)." (4) Every amber/red indicator must link to an action with owner + reason + follow-up plan. (5) If a future meeting invitation asks for a longer format, treat that as an exception and confirm explicitly rather than assuming. (6) Applies to all executive audiences (board, CEO, sponsor steering) — not PMO-specific. Pair with Lessons #269-271 (one-page dashboards) and #292-294 (management evaluation + golden rule).

## Chrome & Local File Access (Apr 6, 2026)

272. **Chrome blocks file:/// links even from other file:/// pages** — `window.open('file:///')`, `<a href="file:///" target="_blank">`, and `--allow-file-access-from-files` flag all fail in Chrome. Workarounds: (1) programmatic `<a>` element click, (2) `window.location.href` for same-tab navigation, (3) localhost HTTP server (`python -m http.server`). Edge handles file:/// links natively.
273. **Delete large commented-out HTML blocks — don't rely on HTML comments** — Old project tabs inside `<!-- -->` leaked visible text because the content contained `-->` in DevOps URL attributes, breaking the comment boundary. HTML comments cannot be nested. Always delete unused code entirely rather than commenting it out.

## Azure DevOps Dashboard Architecture (Apr 6, 2026)

274. **Azure DevOps dashboards exist at two levels — project and team** — Each of the 12 projects has an "Overview" dashboard (project-level, visible at `/{Project}/_dashboards/`). Additionally, Pearl Pixels has 8 team-level dashboards (Backend, Frontend, Implementation, QA-QC, Dashboard, Details, Monthly Report, Project Changed Date) visible at the dashboards directory. Present both levels separately in governance reviews.
275. **Always cross-reference new dashboards with existing deliverables** — The meeting dashboard must be consistent with OKR v5 (Portfolio Health scores), Q2 PM Plan (milestones/risks), and Performance Report (metrics). If badges, numbers, or project names conflict across dashboards shown in the same meeting, management loses trust. Cost: 3 correction cycles during dashboard build.

## Meeting Documentation & Extraction (Apr 7, 2026)

726. **Meeting chat extraction alone captures zero value when all discussion is verbal** — Teams meeting chat was completely empty; 100% of decisions and actions came from the transcript. Always request transcript extraction alongside chat highlights, or you risk missing all meeting outcomes.
727. **AI-generated meeting notes need a "decisions vs. discussion" split** — The Meeting Result doc mixed 17 discussion points + 4 follow-up tasks in one stream. The Transcript extraction properly separated decisions only. Always extract decisions, actions, and discussion notes as separate outputs to avoid burying actionable items in narrative.
728. **Multiple AI extractions from the same meeting produce overlapping but non-identical outputs** — The Next Actions doc and the Follow-Up Tasks from the Meeting Result doc overlap (e.g., SPI/CPI tracking, bi-weekly reviews) but use different wording and granularity. Consolidate into one dashboard but preserve both sources without merging/deduplicating to avoid losing nuance.
729. **Post-meeting action items must distinguish "what to build" from "what to present"** — Actions like "prepare executive presentation" and "prepare for next presentation in 2 weeks" are presentation tasks; actions like "activate SPI/CPI indicators" and "prepare employee performance reports" are system/report-building tasks. Mixing them without categorization makes prioritization unclear.
290. **Always extract management signals separately from meeting decisions** — Decisions tell you "what was agreed"; signals tell you "what management really expects but didn't formally state." The impression analysis revealed the shift expectation from "Dashboard Operator → Owner of Control & Metrics" which wasn't in any formal decision list.
291. **A Before/During/After checklist should follow every management review meeting** — Preparing for the next presentation starts immediately after the current one ends. The 3-phase structure (data readiness → delivery → control) ensures no gap between meetings.
480. **MOM and Action Items rarely match 1:1 — always reconcile both sources into a single Action Tracker** — On 2026-04-21, parsing the Apr 20 weekly review yielded 5 action items in the MOM document and 3 in the Action Items document (transcript-based extraction). Items 1-3 overlapped; items 4-5 existed only in the MOM. Using only one source would have silently dropped 2 of 5 actions. **Why:** AI-generated MOM and Action Items come from different source pipelines (MOM = formal summary, Action Items = transcript scan) and apply different filters. Their union is the true action set. Trusting one source alone is a coverage-loss bug disguised as a completeness claim. **How to apply:** (1) Extract both documents to MD. (2) Enumerate action items from each independently. (3) Build a reconciliation table with columns MOM# / Action_Items# / unified# / overlap (both, MOM-only, AI-only). (4) Every unique row becomes one entry in the consolidated `Action_Tracker.md`. (5) Source column ("MOM Sec X + Action_Items Sec Y") documents the reconciliation for audit. (6) If counts disagree between sources, do not treat it as an error — treat it as expected and reconcile. Pair with Lessons #277 and #278 — they flag the multi-source problem; this one supplies the resolution pattern.

## Data Extraction & Automation (Apr 7, 2026)

280. **Python DOCX with Arabic content fails on cp1252 Windows console encoding** — Python's python-docx library reads UTF-8 fine but Windows console defaults to cp1252, crashing on Arabic characters. Always wrap sys.stdout with io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8') before printing Arabic text from DOCX extraction scripts.
479. **A `.docx` is a ZIP — extract `word/document.xml` directly, no Word COM needed** — On 2026-04-21, parsing the Apr 20 MOM + Action Items `.docx` files used PowerShell `System.IO.Compression.ZipFile::OpenRead` + `[xml]` parse of `word/document.xml`, iterating `//w:p` elements and joining `//w:t` runs. Runs in <1 s per file, preserves Arabic UTF-8, zero Word dependency. **Why:** Word COM extraction requires Office installed, running, and licensed; it is slow (seconds per file), fragile (COM timeouts), and adds a binary-install prerequisite that kills portability. The underlying `.docx` ZIP structure is stable and documented — the direct-parse path has none of these failure modes. **How to apply:** For any `.docx` → text pipeline, use `System.IO.Compression.ZipFile` (PowerShell) or `zipfile` + `xml.etree` (Python). Wrap in a reusable script (`_extract_docx.ps1` pattern) and reuse it every meeting cycle. Handle `<w:tab/>` and `<w:br/>` as whitespace; join `<w:t>` runs per paragraph. Applies to `.xlsx` (`xl/sharedStrings.xml` + `xl/worksheets/sheetN.xml`), `.pptx` (`ppt/slides/slideN.xml`), and any Office Open XML format. Pair with Lesson #280 (Python + Arabic + Windows console cp1252 — same UTF-8 care needed).

## Dashboard & HTML Architecture (Apr 7, 2026)

281. **Meeting results HTML should be a separate file from the meeting preparation dashboard** — The pre-meeting dashboard (project status, team data, KPIs) serves a different purpose than post-meeting results (decisions, actions, follow-ups). Don't merge outcomes into the preparation file — create a dedicated results file to keep both artifacts clean and independently shareable.
288. **Self-contained deliverables over external references** — Embed the full recommendation as a second tab in the same HTML rather than referencing a separate PDF. One file = complete picture. The audience shouldn't need to open multiple files to understand the full context.
289. **Use clickable popups for drill-down data in dashboards** — Counts like "14 users" or "$130/month for 13 Basic + 1 Test Plans" should be clickable modals showing the full user list with names, emails, and roles. Keeps the main view clean while providing detail on demand without scrolling to another section.
296. **Collapsible sections (details/summary) beat side-by-side cards for detailed data** — 3 batch cards in columns were too cramped for tables with emails and roles. Collapsible rows give full-width tables that expand on demand. Use `<details open>` for the first item, collapsed for the rest.
297. **Popups must match the document's grouping logic** — When the document splits users by batch (not by team), all popups must also split by batch. Showing team-based grouping in popups while the main view uses batch-based grouping confuses the reader.
301. **CLAUDE.md version headers drift from the actual entries below them** — The header said "v3 is the latest" but v4 and v5 entries were already documented below it. Always update the header line when adding new version entries — don't just append.
313. **Scan buttons should auto-copy server CMD and auto-retry connection** — Requiring users to manually start a server, then click scan separately, creates unnecessary friction. Combine "Copy CMD" + "Wait for server" + "Auto-scan" into a single button click so the user only needs to paste in terminal.

## QC Task Management (Apr 8, 2026)

730. **KhairGate Wallet uses 2 QC templates per PBI — always create both** — When a PBI moves to "Ready For QC", create both [QC Test Execution] and [QC Bug Fixing] sub-tasks assigned to Mostafa Ahmed (mahmed@pearlpixels.com). Never create just one; the pair ensures testing and bug-fix tracking are separate work items from the start.
731. **Create QC Bug Fixing tasks proactively, not reactively** — The [QC Bug Fixing] task is created upfront alongside [QC Test Execution] when the PBI enters "Ready For QC", not after bugs are found. This gives the QC team a ready container for tracking fixes without waiting for task creation mid-cycle.
304. **Batch QC sub-task creation for multiple PBIs in the same state** — When several PBIs reach "Ready For QC" simultaneously, create all sub-tasks in parallel and link them to parents in a single batch call. For 4 PBIs × 2 templates = 8 tasks, this saves time vs. creating one-by-one.
305. **Always fetch project task templates before creating tasks — never assume template count** — Each project may have a different number of templates per role (e.g., Relief Center has 2 QC templates: [QC Test Execution] + [QC Bug Fixing], not one generic "QC" task). Always call the templates API first and confirm with the user which templates to use, rather than guessing the structure from role names alone.

## Team Onboarding (Apr 8, 2026)

732. **New team member onboarding requires three updates — memory, teams file, and Azure DevOps** — When adding a new member, update all three: (1) team roster memory for future conversation context, (2) DevOps_Teams.md as the authoritative team directory, and (3) Azure DevOps project team membership via security group API. Skipping any one creates drift between systems.
733. **Use security group membership API to add Azure DevOps team members — not `az devops team member add`** — The CLI does not have a direct `team member add` command. Instead, retrieve the team's security group descriptor via `az devops security group list`, then use `az devops security group membership add` with the group descriptor and the user's identity ID.
734. **Always confirm which Azure DevOps project when names are ambiguous** — "BMS Wallet" could mean KhairGate (BMS) or KhairGate Wallet — two separate projects. Never assume; ask the user to pick the correct one before making changes.

## Access Upgrade & Multi-Reference Document Updates (Apr 8, 2026)

735. **Adding one user to an access upgrade plan requires 20+ cross-reference updates** — A single user addition (Ayush Sakalley to Batch 1) required updating: batch counts, batch costs, running totals, 3 cost scenarios, next steps, implementation timeline, success criteria, 4 modals, user numbering across batches, Tab 2 team counts, appendix summary, action items, footer, and version header. Always enumerate every reference before starting edits to avoid partial updates.
311. **Always collect email address before creating a versioned document — never guess from naming patterns** — Email domains vary across the same organization (e.g., @Taqat.qa vs @taqatechno.com vs @pearlpixels.com). Guessing based on role or team leads to wrong entries that propagate across all modals and tables. Confirm the exact email first, then create the version.

## SLA & Service Agreement Creation (Apr 9, 2026)

314. **Extract all governance facts from source documents before drafting an SLA — never assume metrics, roles, or workflows** — The Relief Center SLA required reading 3 source documents (email + 2 MOMs) to extract 4 provider roles, 4 client roles, authorized requesters, and approval flows. 13 gaps were identified that needed explicit user input before any content could be written. Assumptions in contract documents create disputes later.
315. **When multiple meetings discuss the same topic, cross-reference all sources for contradictions before committing** — The Semir meeting focused on scope tracking and Shafik's bridge role; the Ola meeting defined authorized requesters and reporting lines. Both covered governance but from different angles. Merging without cross-checking would have missed that Ahmad Al-Jamal is the SPOC while Dr. Ola is the approver — two distinct roles confirmed in separate meetings.
316. **Always present multiple SLA metric options when no numbers were explicitly agreed — never commit to a single set** — The Relief Center meetings discussed service categories but zero response/resolution times. Presenting Option A (Aggressive) and Option B (Maximum Commitment) with a selection note lets both parties negotiate rather than the PM unilaterally committing to targets the team cannot meet.
317. **Logo positioning in bilingual HTML must use absolute positioning with RTL override, not centered layout** — A centered logo works for monolingual documents but looks unprofessional in bilingual SLAs. Use `position: absolute; left: 30px` for LTR and `html[dir="rtl"] { left: auto; right: 30px }` to flip the logo to the correct corner per language direction.
318. **UI elements with the same background color as their container become invisible — always use contrasting colors for fixed-position controls** — The EN/AR language toggle had `background: var(--primary)` matching the dark blue header, making it disappear visually. Fixed by switching to white background with dark text. Any fixed-position button overlaying a colored section needs explicit contrast.
319. **When a column has the same value in every row, lift it out into a banner above the table** — Relief Center SLA v6 had "Initial Response: 1 hour" repeated across all 5 priority rows. Moving it to a single info banner above the table reduced visual noise, freed column width, and made the unified commitment more prominent. Rule: a column constant across all rows is a header note, not a column. Applies to SLA tables, KPI dashboards, and status reports.
320. **"Logo" is ambiguous — confirm header logo vs browser favicon vs footer mark before building** — Spent a full round building an inline-SVG header logo when the user actually meant the browser-tab favicon. Three different artifacts (header brand mark, footer mark, favicon), three different sizes, three different file formats. Always ask which one before designing any graphic asset.
321. **Title prefixes (Eng./Dr./م.) must be applied in BOTH languages AND every occurrence — grep both Latin and Arabic name forms before declaring done** — "Eng. Semir Worku" needed updating in 4 places: Section 1 EN, Section 1 AR, Section 12 EN, Section 12 AR. The Arabic equivalent "م." is easy to miss because it's a different character set. When adding/changing any title prefix, search both name spellings before claiming the change is complete.
322. **Client-facing cover emails should avoid the word "support" / "الدعم" — frame the relationship as engagement or collaboration, not a help-desk transaction** — The Relief Center cover email originally read "post-sales support… during the support period". The client preferred a partnership tone, so all four occurrences (2 EN + 2 AR) were rewritten to "engagement period" / "فترة التعاون". Also avoid the substitute "post-sales services" — it still echoes the support framing. Always grep both languages (`support`, `الدعم`) and any euphemisms before delivering. Versioned as `Email_to_Client_SLA_Bilingual_v2.html`.

## Team Assessment & Hiring (Apr 12, 2026)

323. **Reduce follow-up assessment duration when prerequisites are passed and AI tools are permitted** — When candidates have already passed a prior level (e.g., Junior → Mid-Level), they've proven setup familiarity (project structure, Django CLI, models). Combined with AI-assisted coding, boilerplate (forms, views, URLs, auth) is fast. Cut Mid-Level from 2 working days to 5 hours. The real evaluation is the interview where candidates explain every line — duration should reflect coding complexity minus known skills, not a generous buffer.

## Source Attribution & PM Deliverable Rigor (Apr 12, 2026)

324. **Never invent a person's name — grep the repo or ask before putting it in a deliverable** — In the Al-Aqraboon PM Summary popup, I wrote "Ahmed El-Afify" twice (header + validation gate). The correct name is "Mahmoud El-Afify". I had conflated him with "Ahmed Lakosha" (an unrelated commenter in the source file). One `grep "El-Afify"` across the repo would have caught the guess. Rule: before any name ships in a deliverable, verify against a source file or ask the user — don't pull from conversational memory alone. Names in PM artifacts get quoted in meetings and cascade into other documents; the cost of correction is never zero.
325. **Display before push on shared repos — preview is mandatory for client-visible deliverables** — Mid-session on Al-Aqraboon, the user said "display before you push". By then two commits had already landed (121f128, aea60c2). For any edit to a file under version control that's visible to stakeholders (dashboards, proposals, email drafts, HTML reports), the flow must be: draft → show content in chat → wait for explicit approval → commit → push. Even small corrections. The cost of previewing is 30 seconds; the cost of a wrong name or wrong number landing in front of a client is far higher.
326. **Flag unverifiable numbers instead of passing them through as if sourced** — The Al-Aqraboon PM Summary showed "10 Days Stalled" in a KPI card. Arithmetic from the review date (2026-03-26) to today (2026-04-12) was 17 days, not 10. The number was passed through in the first answer without flagging the mismatch. Rule: if a headline number can't be traced to a computation you can show, say so explicitly in the answer ("this number is from the dashboard; I can't verify the start date"). Don't launder unverified counts into authoritative answers — stakeholders will cite them.
327. **Quoted verdicts beat paraphrases when the owner will cite them upward** — The Al-Aqraboon remediation popup embeds an exact quote from El-Afify's Section 11: *"This backlog CANNOT enter any sprint. Estimated remediation: 3–4 weeks of dedicated BA/PO work before Sprint 0."* Originally it was a paraphrase. The verbatim quote is more useful because the owner (Mohammed Salah) can cite it in meetings without re-verifying. Rule: for accountability items where an owner must defend a timeline or scope decision, embed a verbatim quote + source attribution, not a cleaned-up summary.
345. **Never fabricate itemized lists from cited counts — quote the count, name the source artifact, flag the gap** — When a status report cites a count ("10 remediation actions", "31 dependencies", "+190 missing stories") but does NOT enumerate the items, never invent the list to satisfy a stakeholder request. Quote only what the source literally says (count + buckets/categories), name the missing artifact by filename + commit hash (e.g., `backlog_validation_v2.html` @ `7f6eb9d`), and offer to fetch the real source before the user acts. **Why:** Stakeholders act on whatever you hand them. A guessed list of "10 remediation actions" becomes a fake commitment tracker — worse than saying "I don't have it." Triggered by Apr 12, 2026 Al-Aqraboon Readiness Report review where Salah asked for the 10 backlog remediation items; the report named only the count (10) and four buckets (acceptance criteria, SOP mapping, dependencies, DoR), and the itemized list lived in a file outside the working directory. **How to apply:** (1) Quote the count + buckets verbatim. (2) Name the source artifact precisely (file + commit). (3) Offer to fetch before answering definitively. (4) Never paraphrase a count into an enumeration. (Relocated from "Counts vs Itemized Lists (Apr 12, 2026)" — single-lesson category absorbed.)

## HTML Asset Loading & Image Fallbacks (Apr 12, 2026)

328. **Always null-guard `onerror` on `<img>` fallback handlers — `onerror="this.src='fallback.jpg'"` is an infinite-loop bug** — Relief Center SLA v8 had `<img src="../Logo.jpg" onerror="this.src='../Logo -.jpeg'">`. When the primary failed, `onerror` reassigned `src`; if the fallback also failed (or the reassignment re-triggered the error), the handler kept firing because `onerror` was never cleared. The browser entered a load→fail→reassign loop, producing rapid flicker (logo appearing and disappearing multiple times per second). **Rule**: every `onerror` fallback MUST clear itself first — `onerror="this.onerror=null;this.src='fallback.jpg'"`. Apply to all `<img>`, `<script>`, `<link>` fallback patterns.
329. **Inline critical visual assets as base64 data URIs in client-facing HTML deliverables** — Relative paths like `../Logo.jpg` break when (a) the parent folder name contains spaces (`Relief Center` → URL-encoding inconsistency across browsers/file:// contexts), (b) the file is moved or emailed, or (c) the client opens it from a different location. Embedding the logo as `data:image/jpeg;base64,...` directly in the `<img src>` makes the HTML self-contained and portable. Cost: +44 KB on a single-file deliverable — negligible. Benefit: zero broken images regardless of where the file lives. Apply to: logos, signature marks, small icons in any standalone HTML deliverable sent to clients.
330. **Spaces in parent folder names silently break relative asset paths in file:// HTML** — Even when the file exists at the resolved path, some browser/OS combinations fail to load `../Logo.jpg` from a folder named `Relief Center` because of inconsistent space handling (`%20` vs raw space vs `+`). The failure is intermittent and hard to reproduce, which is the worst kind. **Two fixes**: (1) URL-encode spaces explicitly in `src` attributes (`../Logo%20-.jpeg`), or (2) inline the asset as base64 to eliminate the path entirely. Prefer #2 for client deliverables.
331. **When patching a versioned deliverable, also patch every co-existing sibling copy carrying the same bug — `glob` the folder before declaring done** — The Relief Center SLA logo flicker was fixed in `Relief_Center_SLA_v9.html`, but a parallel client-named copy `Relief Center & Pearl Pixels Services level Agreement (SLA).html` lived in the same folder with the identical buggy `<img>` tag. The user opened the client-named copy (not v9), the loop ran for hours in their browser tab, and 111,138 requests accumulated before they reported it again. **Rule**: after fixing a bug in a versioned file, immediately `grep` the entire deliverable folder for the buggy pattern (`onerror`, the broken path, the bad function call) and patch every match in the same batch. Versioning the canonical file does NOT propagate fixes to client-named or email-attachment copies. Applies to: SLA documents, dashboards, proposals, any folder where multiple naming conventions co-exist.
332. **Browser tabs with infinite-loop bugs keep running until the tab is killed — fixing the file on disk does not stop the loop** — After patching the buggy HTML on disk, the user's browser tab was still racking up requests because the page in memory was the pre-fix version. Reload alone may not be enough if the tab is hung; the fix must include "close the tab completely, then reopen". When reporting any infinite-loop fix to the user, the verification instructions MUST start with "close the existing tab" — never assume a refresh is sufficient.

## PM Authority & Tone (Apr 12, 2026)

333. **PM does not "request" review windows from EM — PM defines and informs** — A Project Manager and an Engineering Manager are peers in the project chain, not subordinate/superior. Replace "I am formally requesting a 3–5 day review window" with "I will align with the PM Director on the brief in parallel." The PM defines the validation cycle and informs other stakeholders; they do not ask permission to do their job. In the Al-Aqraboon kickoff reply, the v3 draft asked Basha for a review window — the user rejected it because the tone made the PM look subordinate to the EM. The v4 draft removed the request and replaced it with a definitional statement.

334. **Frame quality gates as validation cycles, not approval requests** — Instead of "until the brief is approved by me and the PM Director", say "once the scope is stabilized through BA, FL, and QA validation". This frames the gate as a quality engineering practice (BA → FL → QA pipeline) that protects delivery, not as a top-down approval bottleneck that blocks the project. Validation language is constructive; approval language is obstructive. Same gate, different perception.

335. **Engage the QA gate AFTER BA finalizes scope, never in parallel or after sprint start** — When a Business Analyst is rebuilding scope from scratch, the QA/QC engineer must be brought into the cycle once the BA completes the rebuild — to identify business gaps and edge cases before the brief is locked. Skipping QA at this gate (or running QA in parallel with BA) lets gaps surface in later sprints, where they cost 10x more to fix. In Al-Aqraboon, Mostafa Ahmed was named in the brief's team table but had zero engagement when Basha sent the kickoff — the PM's reply explicitly called this out as a delivery quality risk and committed to engaging Mostafa after the BA's 3 May milestone.

336. **Don't acknowledge binding commitments before formally reviewing them** — Lines like "the 4-month commitment is noted" or "scope is acknowledged" implicitly accept a timeline, scope, or risk profile that has not been validated. Remove acknowledgment phrases for any commitment that is part of an unreviewed brief. Use neutral openers ("Thank you for the formal kickoff") that thank the sender without binding the PM to anything in the attachment. Implicit acceptance of unreviewed commitments is the #1 way PMs get trapped into impossible deliveries.

## Stakeholder Communication & Emails (continued — Apr 12, 2026)

337. **Never Reply-All on a sensitive governance message — manually narrow recipients** — When replying to a kickoff or commitment email that contains scope concerns, pushback, or naming of underperforming team members, manually set TO=sender + CC=chain-of-authority only. Reply-All exposes the sender publicly to peers/reports and names internal team gaps to people who don't need to know. In the Al-Aqraboon kickoff reply, Reply-All would have aired "scope not clear to the team" and "Mostafa not yet engaged" to 5 unrelated recipients (Hatem, Amr, Jassim, Agha, Hamza), embarrassing Basha publicly and exposing Mostafa by name. Send privately, with PM Director on CC for authority closure.

338. **Conclusion-first for executive emails — under 200 words, fits one mobile screen** — Lead with the PM observation (what's wrong + evidence), not formalities or section breaks. The Al-Aqraboon v3 draft (700+ words, 7 numbered sections, governance precedent paragraph, items-needed list) was rejected as "more huge for management to read on email." The v4 draft (under 200 words, conclusion-first, two-bullet evidence) was accepted. Management reads on mobile in 30 seconds — design for that. Long analytical sections belong in the supporting HTML file, not in the email body.

339. **First-time receipt disclosure is a governance baseline statement** — When a binding document (brief, contract, scope, commitment) is shared with the PM for the first time, explicitly state "this email is the first time X has been shared with me." It establishes the legal/operational baseline that the PM had no input during drafting and protects against implicit acceptance. Distinguish carefully from "the project is new to me" — the project may be 6 weeks old in the repo, but the formal binding document is new. State the receipt fact precisely.

344. **Complex governance reply emails require 4+ iterations with strategic feedback — plan for refinement, not one-shot drafts** — A complex Al-Aqraboon kickoff reply went through: v1 (generic acknowledgment, no context) → v2 (Apr 9 BA-rebuild context integrated) → v3 (brief approval gate framing, governance precedent paragraph) → v4 (executive short, PM-tone shift, BA/FL progress evidence). Each iteration was driven by user feedback I had not had context for at the previous step. Save each version's design intent as an inline callout so the user can compare. Don't try to nail it in v1; plan for iterative refinement. The user reveals the most important context (e.g., "I'm seeing this for the first time", "the team is rebuilding scope from scratch") incrementally as they review your draft. (Relocated from "Iterative Email Drafting (Apr 12, 2026)" — single-lesson category absorbed.)

## Memory & Cross-Project Verification (continued — Apr 12, 2026)

340. **Always grep ALL project memories for team roles before answering — not just the current project's memory** — When asked about a person's role or responsibility, run `Grep pattern="<name1>|<name2>" path="C:\Users\DELL\.claude\projects" glob="**/memory/*.md"` BEFORE answering. The master roster lives at `d--KhairGate--HUB--xhatem-Github\memory\project_team_roster.md` and covers all KhairGate + Taqat + Pearl Pixels people across projects. Single-project memory grep is not sufficient — many people appear only in other projects' memories. The Al-Aqraboon Email session required two corrections ("deep search your memory") because the first answer was guessed from email domains instead of cross-project memory grep. Reinforces lesson 227.

341. **Read project-specific status files before drafting any reply email** — When drafting a response to a project email, search the project repository for current status files (`pm_summary.html`, `Project_Readiness_Report.html`, `orientation_gaps_v2.html`, `backlog_validation.html`). These contain the team's most recent verified work and completely change the response framing. In Al-Aqraboon, the BA's 3-wave plan (Mar 30) and FL's 20 findings (Apr 12, same day as kickoff) transformed the reply from "acknowledge and stall" to "acknowledge and cite active progress." Drafting without reading these files produces a generic, weak response that misses the strategic angle.

## Document Sensitivity & Internal Numbers (continued — Apr 12, 2026)

342. **Never quote internal harsh numbers (readiness scores, gap counts, effort variance) in public replies** — Internal readiness scores (e.g., 1.5/10 backlog), gap counts (+190 missing stories), and effort variance estimates (40-50% underestimate) belong in private 1:1s with the PM Director, not in Reply-All emails to the engineering manager. Save those numbers for a private attachment to the PM Director, alongside the full readiness report. Public replies should reference team work (BA plan, FL findings) as constructive evidence instead. Quoting harsh internal numbers publicly humiliates the document owner and poisons the working relationship.

343. **First time you see an attachment ≠ first time the project exists — phrase the disclosure precisely** — Distinguish between "I'm seeing this brief for the first time" (governance fact about the document) and "the project is new to me" (which would be wrong if the project has prior history). The Al-Aqraboon project had 6 weeks of repository history (20 commits, 48 pages) when the formal brief was first shared with the PM. State the receipt fact precisely without implying ignorance of the project, otherwise the disclosure becomes self-undermining.

## Source-Root Index & Project Context Discipline (Apr 12, 2026)

346. **Treat the user-visible index as the canonical drift signal, not the JSON baseline** — Index drift can hide when `scan_baseline.json` is fresher than the user-facing `Claude_Sessions_Index.md/.html`. On Apr 12, 2026 the baseline already contained Afriqat, IndoQat, Herbal-Gardens, and AI Call Center, but the index never picked them up — the gap was invisible if you only diffed baseline vs disk. **Why:** The baseline is a staging file, not a deliverable. Stakeholders, future sessions, and the dashboard read the .md/.html index. **How to apply:** After every D:\ scan, diff index counts against the baseline (not just baseline against disk). Any folder in the baseline missing from the index is drift, even if disk and baseline agree.

347. **Every project root needs a tailored CLAUDE.md — no exceptions, no boilerplate** — On Apr 12, 2026 only 1 of 18 indexed project folders (`Water for Mosques`) had a root CLAUDE.md. The other 17 forced every new session to rediscover purpose, key subfolders, and standing rules from scratch. **Why:** Without root CLAUDE.md, Claude burns turns re-exploring things the index already knows, and standing rules (e.g., "Department Workflows = HTML only, never DOCX") get violated by accident. **How to apply:** Every top-level project folder gets its own CLAUDE.md with purpose, key subfolders, standing rules, current file counts, and links to sibling/sub CLAUDE.md files. Tailored from the index entry — never a generic template.

348. **Latest version subfolder gets a CLAUDE.md; historical versions stay frozen** — When a project has versioned subfolders (Udemy v1/v2/V3, Enhanced Standalone v2–v15, khairworld V.1/V.4, Claude_Courses_Dashboard v1–v31), only the *current* version needs its own CLAUDE.md. Adding context files to v1/v2/old snapshots invites editing frozen artifacts and breaks the "version = immutable snapshot" contract. **Why:** Historical versions are reference material, not active workspaces. **How to apply:** When creating per-version context files, scan for the highest-numbered/latest folder and create CLAUDE.md only there. Mention older versions by path with a "frozen, do not edit" note inside the latest version's CLAUDE.md.

349. **Two folders differing only in punctuation need explicit cross-reference CLAUDE.md files** — `D:\KhairGate (Wallet)\` and `D:\KhairGate Wallet\` are different projects with different owners (Sponsorship Module vs. Team Aya), but the index treated them as one for months. **Why:** Filesystem-similar names create governance hazards: edits land in the wrong folder, MEMORY entries collide, drift compounds silently. **How to apply:** Whenever two folders differ only in punctuation, parens, casing, or spacing, both must have CLAUDE.md files that explicitly cross-reference each other ("Not to be confused with X, which is owned by Y"). Update the Source-Root index to disambiguate them as separate sessions.

350. **When you find drift mid-task, write the flag into the artifact, not just the chat** — While creating the v15 Enhanced Standalone CLAUDE.md, I discovered the Source-Root index only tracked v9–v12. The flag was added as an "⚠️ Index Drift" section *inside* the v15 CLAUDE.md, not just verbally to the user. **Why:** Chat messages disappear; future sessions reading the artifact get the warning automatically without needing to re-discover the gap. **How to apply:** When drift is discovered as a side-effect of another task, embed a short "⚠️ Drift" callout in the file you're already creating/editing. Reference the canonical index location and what needs reconciling.

351. **Plan mode can re-engage mid-session — verify state before each non-trivial edit** — On Apr 12, 2026 a `Claude_Sessions_Index.md` edit slipped through after plan mode had silently re-activated. The edit was correct content but violated plan mode's read-only contract. **Why:** Plan mode toggles are easy to miss inside long tool-use chains, especially after a system reminder. **How to apply:** After any system reminder mentioning "Re-entering Plan Mode" or "Plan mode is active", explicitly confirm state in your next thought before any write. Treat plan mode like a transactional flag: re-check, don't assume.

## PM Dashboard Layout & Role Semantics (Apr 12, 2026)

352. **Arrow notation `A → B` in owner/responsibility labels needs semantic agreement before shipping** — I interpreted `A → B` as *"A reports/flows output to B"*. The PM's actual semantic was *"work flows FROM source A TO the person who must act B"*. Same glyph, opposite meaning. Shipping Action #3 with `Mohammed Salah → PM + Ahmed Lakosha` was wrong — the correct form was `Ahmed Lakosha → Mohammed Salah` (Lakosha issued the findings; Salah must review them). The PM called it out directly: "you don't understand the status". Rule: before any arrow goes in a PM label, state the semantic explicitly to the user (flow-of-work vs reporting direction) and confirm in one word. The glyph is free; the ambiguity is expensive.
353. **Nested reply threading inside a table cell beats adding columns for 3-party feedback** — When Basha posted 11 counter-comments to El-Afify's 10 remediation items and the PM then replied to each, the cleanest UI was a light-blue indented block with a `↳ PM Reply — [Name]` label sitting directly under the reviewer's quoted comment inside the same `<td>`. Reads as a conversation thread, no horizontal real estate lost, bilingual-safe, degrades gracefully on mobile. Rule: for 3-party feedback (author → reviewer → PM) inside a data table, use nested reply blocks in the same cell before reaching for extra columns.
354. **Restructure by role-owner beats grouped sub-blocks when a section contains inputs from multiple roles** — When the "Team Inputs Requiring Integration" section had Block A (Lakosha/FL), Block B (El-Afify/BA), and a Readiness Summary all stacked inside one `<div class="sec">`, the cleanest fix was splitting them into two parallel sections — one per role-owner — with distinct color coding (BA=red, FL=purple). Each section then has one owner, one source, one status, one action. Rule: when a section contains inputs from 2+ distinct role-owners, promote each to its own top-level section with its own color identity before the audience has to ask "who owns what".
355. **Replace an unverifiable KPI card instead of defending it** — The "10 Days Stalled" KPI was the counter that triggered the whole Basha review chain, but the arithmetic (2026-03-26 → 2026-04-12 = 17 days, not 10) meant the number couldn't be traced to any source. Rather than hiding it or keeping it and flagging, the right move was replacing it with a factual count: "30 Open Findings" (20 Lakosha + 10 El-Afify). Same card slot, same urgency color, sourced from two real files. Rule: when a dashboard KPI can't be traced to a computation you can show, replace the card with a factual count from a known source rather than defending the unverifiable number or deleting the card entirely.
356. **Disambiguate same-first-name collisions explicitly in client-visible UI** — Al-Aqraboon has two people named "Mahmoud" — *Mahmoud Elshahed* (PM) and *Mahmoud El-Afify* (Senior BA) — plus *Mohamed Basha = Mohammed Salah* (same person, two names in use). Every label that references them must be fully surname-qualified, and where a person has two names in daily use, both must appear (e.g. `Mohamed Basha (Mohammed Salah) — Engineering Manager`). Ambiguity in a single name cost one push cycle today when a validator was labeled "Ahmed El-Afify" instead of "Mahmoud El-Afify". Rule: in any PM-owned artifact that goes to stakeholders, never refer to a team member by first name alone. Surname + role at first mention, consistent through the document, dual identity shown if applicable.

## External Deliverables vs Repository Canonicalization (Apr 13, 2026)

357. **External deliverables drift silently from the repo — variance-check before distribution, never after** — On 2026-04-13, Al-Aqraboon's external brief (`D:\Alaqraboon\Email\AQ_project_brief.html`) was authored by the same solution architect who maintains the repo, but from an earlier snapshot. Headline totals matched (244 stories, 1,111 SP, 6 epics, 12-member team, 4 months) — enough to pass a cursory glance. Six epic-level SP distributions had drifted (E0 −10, E1 −35, E2 −10 stories/−151 SP, E3 −34, E4 −10 stories/−15 SP, E5 +14), and the brief carried business-case data the repo never had (60→10 min processing KPI, 1,200/840 capacity gap, QAR approval tiers, Sandi integration). None of this was visible without a line-by-line diff. **Rule:** any external deliverable authored outside the version-controlled repo must be variance-checked against repo v2 files before distribution. Compare at minimum: story totals, SP totals, per-epic breakdown, team roster, integration list, workflow count, approval tiers, tech stack, headline KPIs. Declare the repo as canonical source of truth. Document the variance in a dedicated bilingual HTML report committed to the repo alongside the deliverable. **Why:** headline-total matches create false confidence; the damage only surfaces when a stakeholder quotes a number the team can't defend.

358. **Git log is free, defensible evidence — use it for provenance tables in trust-critical reports** — When building a variance or audit report, every "source" claim gains defensibility if it carries `git log --follow --format="%ai %h %s"` dates (created + last-updated + commit hash). On 2026-04-13 the Alaqraboon variance report added a Section 12 "Evidence Pages — Source Dates" table with 8 rows, showing that the oldest evidence file (Al_Aqraboon_Workflows.html, 2026-02-25) predated the brief by nearly 2 months, and the newest repo updates (2026-04-12) were 1 day before the report. This single table shifts the report from "Mahmoud's opinion" to "git's verdict" — the reader cannot dismiss the timeline. **Rule:** any report that argues "source A is more current/authoritative than source B" must include a git-sourced provenance table. Cost: one `git log --follow` per file. Benefit: the argument becomes machine-verifiable. Applies to variance reports, audit reports, readiness reports, brief-vs-repo comparisons, scope reconciliation docs.

359. **Bilingual footer stamps use direction-neutral styling — border-top, not border-left/right — to avoid RTL override hell** — On 2026-04-13 we added `Created / Last Updated / Commit` footer stamps to 8 Alaqraboon pages. The stamps use inline-styled `<div>` with `border-top:2px solid #1565C0` instead of `border-left/right`, so no RTL override is needed. Text uses `data-en` / `data-ar` attributes which every page's existing `toggleLang()` function picks up automatically (confirmed working with both `textContent`-style toggles and `innerHTML`-style toggles). Inline styles avoid CSS variable dependencies, so the stamp works regardless of the host page's theme. **Rule:** for any cross-cutting UI element added to pages with different CSS themes and bilingual requirements, prefer (a) direction-neutral CSS properties (border-top/bottom, text-align:center, no margin-left/right), (b) inline styles (no class dependencies), (c) `data-en`/`data-ar` attributes (picked up by existing toggle handlers). Test on at least one page using `innerHTML`-style toggling AND one using `textContent`-style toggling before declaring the pattern safe.

360. **When adding a 3rd dropdown to a 2-way toggle handler, upgrade to N-way arrays — don't hardcode pairs** — Alaqraboon `nav.js` originally had `['v1-dd', 'v2-dd'].forEach(...)` with `otherId = ddId === 'v1-dd' ? 'v2-dd' : 'v1-dd'` hardcoded. Adding a 3rd dropdown (`it-pm-dd`) with the same pattern would have required 3 pairwise branches. The right fix was to promote the list to a single `DD_IDS = ['v1-dd', 'v2-dd', 'it-pm-dd']` array and loop: on click, toggle current, then close all others in the array. Adding a 4th dropdown now requires one array entry — zero logic change. **Rule:** when any binary pair is about to become a triple, stop and generalize to an array. Pairwise special-casing is a maintenance debt that compounds with each addition.

361. **Plan mode re-enters silently after a browser review break — stage files survive but commit is blocked until ExitPlanMode** — On 2026-04-13, after I opened the variance report in the user's browser for visual review (via `start ""`), the harness re-entered plan mode. The user then approved the commit message, and I ran `git add` (which succeeded — the hook fired and staged 12 files). But `git commit` was still blocked by plan mode's read-only contract; I had to call `ExitPlanMode` mid-flow to complete the commit. The plan file was outdated (described the pre-review state), so I also had to edit it before exiting. **Rule:** after any user interaction that might trigger a plan-mode re-entry (opening files in external apps, long pauses, approval breaks, tool-use chains spanning hours), explicitly check for plan-mode state before the next write operation. If plan mode is active, edit the plan file to reflect current state FIRST, then call `ExitPlanMode` to request re-approval. Never try to commit or push while plan mode is active — the harness will not stop you on `git add` but will stop you on `git commit`.

362. **Commit messages: drop narrative framing, keep structural sections — shorter beats rhetorical** — On 2026-04-13 I drafted a commit message with an opening justification paragraph ("Reports that the external brief ... diverges ... Flags repo v2 as canonical source of truth"), followed by 5 structural sections (New deliverable / Navigation / Search / Git provenance stamps / Housekeeping). The user refined it to drop the justification paragraph, drop Git provenance stamps, drop Housekeeping — keeping only subject line + 3 structural sections (New deliverable / Navigation / Search) + co-author trailer. The final message was shorter, purely mechanical, and readable as a changelog entry. **Rule:** commit message bodies should describe WHAT changed (files, categories, counts) in structural bullet sections, not WHY it changed (the "why" belongs in the PR description, the variance report, or the linked issue). Narrative framing ages poorly in `git log`. Keep the subject line for the headline and the body for a changelog-style breakdown. If you find yourself writing a paragraph, move it to the deliverable itself.

## Source-Root RAG Sync & Live-Scan Discipline (Apr 13, 2026)

363. **"No new folders" is one check out of three, not the whole coverage audit** — On 2026-04-13 I answered "no new folders on D:\" when the user asked if RAG needed updating. That was technically correct but missed two other classes of drift: (a) existing indexed folders missing their root `<folder-name>.md` because the last batch script skipped edge cases (see lesson 365), and (b) new *sub-folders* created inside already-indexed parents since the last baseline scan — which is what held up `D:\Project Management\Project management Quarter Plan\Projects Handling Review Meeting 6-4-2026` that day. **Rule:** every RAG coverage audit must run all three checks: (1) new top-level folders vs `scan_baseline.json`, (2) every HTML-bearing folder's root `<folder-name>.md` via Glob, (3) diff the live RAG list (`curl /ui/projects/list`) against the current index sessions — not just the baseline file. Skipping any of the three yields false-negative answers that look thorough but aren't.

364. **RAG entries are per sub-session, not per top-level folder — map accordingly, never add root entries on top of sub-entries** — On 2026-04-13, checking the RAG at `http://127.0.0.1:21420/ui/projects/list` revealed 42 entries organized per sub-session (KhairGate HUB = 11 entries, Project Management = 9 entries, DevOps = 4, HR = 2, Personal = 3), not one-per-D:\-folder. After creating a new root-level `D:\KhairGate (HUB)\KhairGate (HUB).md` to close a gap from the Apr 12 batch, I initially planned to register it as a new RAG project — but that would have duplicated chunks already covered by the 11 existing sub-entries, bloating the index and degrading search relevance. **Rule:** before adding any new RAG entry, check whether its path is already covered recursively by an existing entry. If yes, the folder MD becomes documentation-only (for humans and future Claude sessions), not a RAG entry. Map RAG granularity to `Claude_Sessions_Index.md` sub-section level, never to top-level folder.

365. **WebFetch auto-upgrades HTTP→HTTPS — it WILL fail on any localhost dev server** — On 2026-04-13, `WebFetch http://127.0.0.1:21420/projects` returned `ECONNREFUSED` despite the RAG server being up and the URL loading fine in Chrome. The tool silently upgrades HTTP to HTTPS (documented in its description), and local dev servers don't bind HTTPS. Switching to `curl -s -m 5 http://127.0.0.1:21420/...` via Bash worked immediately. **Rule:** for any HTTP-only localhost service (RAG UI 21420, Outlook Update Server 5050, Standalone Update 5051, Taqat 5052, D Drive Scan 5053, or any PowerShell HttpListener), always use `curl` via Bash, never WebFetch. Document the port-to-service map in MEMORY.md so future sessions don't waste turns rediscovering this.

366. **Baselines go stale the day they're written — always cross-check against the live source** — `scan_baseline.json` last updated Apr 12, 2026 missed a folder created Apr 13 at 06:50 (20 hours later). Any "compare against baseline" audit on Apr 13+ gives false negatives for same-day additions. **Rule:** treat `scan_baseline.json` as a fixed point-in-time snapshot, not a live truth source. Every coverage check must also query the live state: `ls D:/<folder>` for disk truth, `curl /ui/projects/list` for RAG truth, `grep Claude_Sessions_Index.md` for index truth. Reconcile disagreements by trusting the live source and updating the baseline last. Never answer "nothing new" based on a baseline older than ~24 hours without live verification.

367. **Batch scripts that operate on folder names silently skip names with parentheses, spaces, `&`, or Arabic characters** — On 2026-04-12 a batch created 37 `<folder-name>.md` files across every indexed session folder. On 2026-04-13 a coverage check revealed `D:\KhairGate (HUB)\KhairGate (HUB).md` was missing — almost certainly because the script's glob or regex didn't escape the parentheses, and the folder was silently skipped without raising an error. **Rule:** any batch operation over the filesystem must be tested against edge-case folder names *before* the batch runs (parens, spaces, `&`, single quotes, Arabic characters like `D:\Global Lessons\مشاريع`). Add an explicit post-run verification step that counts output files against expected-input count and reports any gap — silent skips are the worst failure mode because nothing surfaces until a coverage audit weeks later.

368. **Remove-then-re-add for RAG updates; direct-add for new entries** — On 2026-04-13 the user clarified the rule: when a RAG entry already exists and its source MD has changed, remove the existing entry first then re-add, because RAG caches metadata and in-place updates can serve stale chunks. This rule applies only to *existing* entries — brand-new entries (like the `project-management-quarter-plan` added the same day) go straight through `/ui/projects/add` with no removal needed. **Rule:** (a) before any RAG add operation, check via `curl /ui/projects/list` whether the target path or ID already exists; (b) if it exists, DELETE first via `/ui/projects/<id>/remove`, confirm deletion, then POST the add; (c) if it doesn't exist, POST directly. Never update in-place. Document both the remove and re-add in the session changelog so future sessions can trace what was re-indexed when.

## Publish vs Memory Classification (Apr 13, 2026)

369. **Every piece of user information has one of two destinations — classify before acting, default to memory, ask when unclear** — On 2026-04-13 while building the Al-Aqraboon Change Request HTML (CR-2026-04-13), I embedded an internal PM governance rule (about which files are "PM governance artifacts" vs "scope sources") directly into Section 2 of the team-facing CR document. The rule was true and useful as internal discipline, but it was NOT supposed to be visible to team owners reading the CR — it was background context for me, not content for the deliverable. The user had to explicitly correct me: *"remove this word ... it's for your memory only, not to publish."* Then the user taught me the meta-rule: *"Let's agree that there are things that can be published and that there are other things to teach you and provide you with information to keep in your memory. You should keep that in mind when I talk to you."* **Rule:** Every piece of information the user shares during a conversation has one of two destinations — (a) **Publish:** goes into a deliverable (HTML report, email, commit message, public document) where stakeholders will see it; or (b) **Memory only:** stays in internal memory files, plan notes, or mental context, never appears in any user-visible artifact. **How to apply:** (1) When user provides context or rationale → default to **memory only** unless they explicitly say "add this to the file" or the context is obviously part of the deliverable being built. (2) When user requests a specific content change → default to **publish**, but confirm whether associated reasoning is meant to be published alongside. (3) When I discover a justification or exclusion rule during exploration → treat it as memory by default; surface it in chat for user review but do NOT auto-embed it in the deliverable. (4) When user says "remove X from the file" → interpret it as "keep X in memory, don't show stakeholders" unless they explicitly say "forget X entirely". (5) When in doubt, **ASK** — better to confirm "is this for the file or for your memory?" than to over-publish internal context or under-publish intentional content. **Publish signals:** add / include / show / mention / note in the document. **Memory signals:** remember / keep in mind / for context / understand that / to teach you / so you know. **Ambiguous — always ask:** "note that" / "by the way" / "also" / "additionally". Applies across ALL projects and sessions. Saved to `feedback_publish_vs_memory.md` in Alaqraboon project memory and as a standing rule in both `d:\Alaqraboon\CLAUDE.md` and `C:\Users\DELL\.claude\CLAUDE.md`.

## PM Governance Boundaries & Assumption Discipline (Apr 13, 2026)

370. **PM orchestrates, never authors team-owned artifacts** — On 2026-04-13 I proposed that the PM directly edit `backlog_v2.html`, `architecture.html`, `technical_proposal_v2.html`, `proposal_v2.html`, `Al_Aqraboon_Workflows.html`, and `basis_of_calculation_v2.html` to backport business-case data and fix title references. The user correctly pushed back: *"you need me as IT project manager to make these changes on the pages that team created and updated on repo, right?"* The answer is no. Each of those files has a specific authoring owner (BA, FL, SA, EM) who maintains them through their own PR workflow. When the PM edits a team-owned artifact directly, it (a) overrides the owner's authority on their own file, (b) violates the canonical-source rule ("repo v2 is team-authored"), and (c) creates future drift when the real owner next touches the file and overwrites or disagrees with the PM's changes. **Rule:** PM's role on team-owned files is **flag → request → review**, never author. Use a formal Change Request document (HTML or Markdown) to assign items to owners with acceptance criteria; owners push their own PRs; PM reviews each. PM may directly author only: (a) files the PM created (PM Summary, variance reports, change requests, dashboards), (b) governance / memory / CLAUDE.md / plan files, (c) files where the author explicitly delegates edits back to PM. The Change Request pattern from 2026-04-13 (`CHANGE_REQUEST_2026-04-13.html`) is the canonical example — it tells each owner WHAT to edit, WHERE to edit it, WITH what acceptance criteria, and nothing more. PM does not embed owner's content inside the Change Request.

371. **Never propagate PM assumptions across multiple documents — caught early or corrected with discipline** — On 2026-04-13 I invented deadlines (5 / 10 / 15 working days) for the 6 CR items when drafting `CHANGE_REQUEST_2026-04-13.html`. Those invented numbers then propagated into `pm_summary.html` (3 locations), the variance report Section 7, `change_request_status.md` memory, and the plan file. The user eventually caught them: *"who is add the Deadline for every topic? me or someone of the team or your assumption?"* Full admission + removal across every document took a dedicated commit (`001489a`) to undo. **Rule:** Any numeric commitment — deadlines, effort estimates, story points, dollar amounts, capacity figures, dates — MUST come from a real decision-maker (PM, BA, owner, customer) OR be explicitly labeled `TBD — owner to confirm` at first mention. Never invent AND never propagate invented numbers across documents where they become load-bearing. **Detection signal:** if you can't answer "where did this number come from?" with a specific person + date, it's an invented number and needs either (a) removal, (b) TBD marker, or (c) a rationale + disclaimer. **Prevention:** when drafting governance documents (CRs, RFCs, scope summaries), separate "facts known from source files" from "values that need decisions." Mark the second category clearly BEFORE any other file references them.

372. **Verify the team's communication model before designing handoffs** — On 2026-04-13 I offered to draft Slack / Teams / email handoff messages for the CR items, assuming push-style communication. The user clarified: *"the team have access on the report and they get pull and do his works"* — the team pulls the repo and reads `pm_summary.html` + `CHANGE_REQUEST_2026-04-13.html` directly. The repo IS the communication channel. This required a new commit to fix pm_summary.html Next Actions item 9 and add a "Pull-based Workflow" info-box. **Rule:** before designing any handoff / notification / coordination mechanism, explicitly ask or verify HOW the team receives information. Three models exist: (a) **Push** — PM sends messages via Slack/Teams/email and team reacts, (b) **Pull** — team checks the repo/dashboard on their schedule and reads updates there, (c) **Sync** — updates happen only at standups/meetings and the team brings back notes. Each model dictates different content placement: push needs standalone messages with context, pull needs centralized hub pages that the team bookmarks, sync needs agenda items and meeting minutes. Wrong model = wrong content structure = extra commits to fix later. **How to verify:** ask "does the team see repo changes automatically, or do I need to tell them?" The answer determines whether content belongs in a message OR in the repo.

373. **Silent fix patterns extend by category — maintain consistency once established** — On 2026-04-13 the user established "silent fix" as the norm for Mohammed Salah title corrections (pm_summary.html lines 270 + 354). Over the next several turns, the silent pattern extended organically to `basis_of_calculation_v2.html` (3 locations), `AQ_project_brief.html` (Section 3 table + Action #11), variance report Section 7 (the Action cell for Salah's row), variance report line 169 author label, and `pm_summary.html` line 440 (role enumeration). Each extension was a user decision, but the user expected consistency — if I had marked one of them explicit while others stayed silent, the git log would become inconsistent and confusing. **Rule:** once a content category is marked "silent" (commit message does not name the change, even though the diff shows it), all future work in that same category stays silent unless the user explicitly reverses the policy. Track the silent-fix category in memory (e.g., `silent_fix_category_title_corrections = [pm_summary.html, basis_of_calculation_v2.html, AQ_project_brief.html, Brief_vs_Repo_Variance_Report_v1.html]`) and check against it before naming any future fix. **Why consistency matters:** git log readers infer intent from patterns. If 5 silent fixes are followed by 1 explicit one, the explicit one looks anomalous and raises questions the PM may not want asked.

374. **"What about X?" questions need antecedent clarification, not interpretation** — On 2026-04-13 the user asked *"what about if highlighted this in the file pm_summary.html?"* with no clear antecedent for "this" (could have meant: the pending task list, the handoff instructions, a specific CR item, a summary status, or something I couldn't guess). I jumped to interpretation based on the most recent topic in the conversation. Multiple clarification rounds followed before the real intent surfaced: integrate CR items into the existing Section 1 BA Stream, Section 2 FL Stream, and Next Actions. **Rule:** when a user question contains ambiguous pronouns ("this", "that", "it") without a clear antecedent, ASK — don't interpret. Offer 3–4 candidate interpretations via `AskUserQuestion` tool or plain text. Cost of asking: 1 turn. Cost of guessing wrong: 2–4 turns of backtracking + potentially wasted file edits. **Signal phrases:** "what about this", "should we add that", "maybe do X" — all antecedent-light, all should trigger a clarification before action.

## Commit Discipline & Content Governance (Apr 13, 2026)

375. **Git diff ≠ commit message — "silent" means unnamed, not invisible** — On 2026-04-13 when silently fixing Mohammed Salah's title, I framed the action as "silent" because the commit message did not name the change. But the git diff still showed every modified character. When the user asked "will owners see this?", the honest answer was YES in the diff (anyone running `git show <hash>` sees it) but NO in the one-line summary. "Silent" is a property of the commit MESSAGE, not the commit CONTENT. **Rule:** when discussing silent edits with a user, always clarify: silence applies only to the commit subject line and body text. The diff remains fully visible to anyone viewing the commit. Silence is for de-emphasis, not for concealment. If the user wants true concealment (changes that don't appear anywhere), that requires NOT committing at all — which defeats the purpose of version control. Be explicit about this distinction the first time silence is proposed in a session.

376. **Squash related small commits BEFORE the first push, not after** — On 2026-04-13 three small commits (`db3f3d2` silent line 440 fix, `001489a` deadline removal + CR highlights, `3fefee3` pull-workflow reframe) stacked locally before any push. The user requested squash-then-push, which was trivial because nothing had reached `origin/master` yet (`git reset --soft` + recommit, zero force-push risk). If those three commits had each been pushed individually first, the squash would require `git push --force` — a dangerous operation on `master`. **Rule:** when related work in a single session produces multiple small commits touching overlapping files, offer to squash them into one clean commit BEFORE the first push. Check with the user: "these 3 commits are all on the same topic — squash into one before push, or keep separate?" The answer determines whether the final git log tells a clean story (1 commit = 1 feature) or a messy one (3 tiny commits that the team has to read through). **Gate:** squash is safe only before push. After push, never squash without explicit force-push authorization.

377. **Commit messages with explicit "Rationale:" paragraphs are self-documenting — adopt as default for non-trivial explicit commits** — On 2026-04-13 three commits (`001489a`, `3fefee3`, the final squashed `92bcd78`) each ended with a `Rationale:` paragraph explaining WHY the change was made, not just WHAT changed. Example: *"Rationale: Team has direct repo access and pulls regularly. Previous phrasing incorrectly assumed push-style communication. Original CR deadlines (5/10/15 working days) were PM placeholder estimates never validated with owners and have been removed per no-assumptions rule; owners negotiate delivery dates at kickoff."* This style makes the commit self-documenting — a future session reading `git log` doesn't need external context to understand why the change happened. It's especially valuable when the change reverses or corrects an earlier commit (like removing deadlines after introducing them). **Rule:** for any non-trivial explicit commit where the WHY matters (policy reversal, governance change, assumption correction, scope refinement), add a `Rationale:` paragraph at the end of the commit message body. Keep the rationale under 4 sentences. Skip the rationale for (a) silent commits — they should stay silent in the message, (b) trivial mechanical changes (typo fixes, formatting), (c) commits where the subject line alone is self-explanatory. When in doubt, add it — the cost is negligible and the future value is high.

## Process Discipline — Rule Lookup & Version Archival (Apr 13, 2026)

378. **"No rule exists" requires a 3-location search, not 1** — Before telling the user a rule or convention doesn't exist, grep ALL three locations: (a) project-scoped memory folder `C:\Users\DELL\.claude\projects\<sanitized-cwd>\memory\`, (b) `D:\Global Lessons\global_lessons.md`, (c) global CLAUDE.md at `C:\Users\DELL\.claude\CLAUDE.md`. Use broad keywords — the rule may use different wording than the user's question (e.g., rule says "version" when user asks about "archive", rule says "standalone folder" when user asks about "per-file versioning"). **Why**: Apr 13, 2026 — told user "no rule about archiving old versions into a subfolder" after grepping only the project memory folder; lesson #222 ("Archive old versions, keep only the latest in the root folder") was sitting in global_lessons.md line 272 the whole time, plus #218 for same-step discipline, #264 for "move not delete", and #155 for the Claude Courses precedent. A confident negative answer based on one search location is worse than saying "let me check" because the user trusts it and acts on it. **How to apply**: whenever the user says "as we have in memory, right?" or "isn't there a rule for this?" or "should we do X as usual?", run parallel greps across all three locations before answering. Cite the lesson number when one is found. If no match in any location, say "I checked memory, global_lessons, and CLAUDE.md — no rule found" — mention what you searched so the user knows the answer is grounded.

379. **Version archival is part of the version-bump definition-of-done, not a follow-up task** — Existing lesson #218 ("Archive old version folder in the SAME step as creating the new one") was written for Enhanced Standalone `KhairGate HUB V13 → V14` folder moves, but the same discipline applies to per-file version chains: `Relief_Center_SLA_v10.html → v11.html`, `OKR_KPI_Dashboard_vN.html → v{N+1}.html`, `Weekly_Meeting_Agenda_vN.html`, `Outlook_Email_Analysis_Report_vN.html`, `Claude_Courses_Dashboard_vN.html`, etc. **Why**: Apr 13, 2026 — created `Relief_Center_SLA_v11.html` and stopped there, leaving v1–v10 cluttering the SLA root; user had to prompt me the next turn to archive them into `Relief Center SLA - Old Vesrion/`. This violated both lesson #218 (same-step) and #222 (keep only latest in root). **How to apply**: the version-bump workflow is a 4-step single turn — (1) copy v{N} → v{N+1}, (2) apply edits to v{N+1}, (3) move v{N} to the archive subfolder, (4) update the project memory MD. All four steps in the SAME turn, never spread across turns. For the first archival of a version chain, create the archive subfolder in that same turn and ask the user for the folder name once (then reuse it verbatim for all future bumps of that project — respect the user's spelling even if misspelled, e.g., "Old Vesrion"). For projects where an archive folder already exists, skip the naming question and just move.

## PM Governance Execution & External Handover (Apr 14, 2026)

380. **Content authorship vs mechanical metadata — governance override rule** — The "PM doesn't directly edit team-owned files" rule (lesson #370) exists to protect **content authorship** where owner expertise is required. A purely mechanical change — renaming a team member, fixing a typo, updating a date, replacing a stale email address — is **metadata maintenance**, not authorship, and the PM can execute it directly. **Why**: Apr 14, 2026 — CR-07 assigned an Amr Atya → Mohammed Chtioui rename across 10 team-owned files to 3 different owners (BA + SA + PO), which would have sat Pending for days waiting for 3 separate trivial PRs. PM executed the mechanical rename directly in one commit (`f343156`), marking CR-07 Resolved with rationale "mechanical roster rename, owner sign-off waived as non-content change." **How to apply**: before deciding whether to route a change through a CR or execute it directly, classify: (a) **content change** (scope, architecture, KPIs, approval chains, business logic, AC language, story points) → always CR; (b) **metadata change** (team member names, dates, email addresses, rate cards when values are dictated externally, typos, version labels) → PM may execute directly with explicit rationale in the commit body. Document the waiver explicitly in the commit message so the governance model stays legible in git log.

381. **Add new CR items to the open CR document — don't spawn a second one** — When a new cross-cutting issue surfaces mid-sprint and an open Change Request document already exists, add a **new row** to it rather than creating a separate CR file. Keeps all governance in one tracked artifact and preserves the stat-strip counters as a single source of truth. **Why**: Apr 14, 2026 — UI/UX rename was added as CR-07 to the existing `CHANGE_REQUEST_2026-04-13.html` alongside CR-01 through CR-06 (not as a separate `CHANGE_REQUEST_2026-04-14.html`). Avoided CR sprawl and kept the team looking at one file. **How to apply**: when adding a row, atomically update (a) the row itself with proper ID, owner, target files, acceptance criteria, and status, (b) the stat-strip counters (CR Items count, Target Files count, Owners count if new owners are introduced), (c) the section heading text if it enumerates IDs (e.g., "CR-01 through CR-06" → "CR-01 through CR-07"). Missing any of the three creates inconsistency the next reader will spot immediately.

382. **Standalone-ify pattern for external handover copies** — When sending a repo-internal HTML document to an external team (UI/UX agency, client, auditor), create a standalone copy in a separate folder outside the repo and **strip all repo-specific dependencies** before sending: `<script src="nav.js">`, `<link rel="stylesheet" href="comments.css">`, `<script src="comments.js">`, any `<script id="embedded-comments">` JSON blob, and the `<nav class="nav-bar"></nav>` element. Add a prominent handover note banner at the top explaining the recipient's expected action and any scope caveats. **Why**: Apr 14, 2026 — cloned `_Website_Concept_en.html` from the Alaqraboon repo into `D:\Alaqraboon\UIUX_Handover\Detailed_Website_Concept_Al_Aqraboon.html` for the UI/UX designer. Without stripping the nav.js and comments.js dependencies, the file would have shown broken navigation and console errors to the recipient. The bilingual (EN/AR) language toggle still worked because it was self-contained in the same file. **How to apply**: (1) copy the file to a dedicated handover folder outside the repo, (2) grep for script/link references pointing to repo-only assets and remove them, (3) test-open in a fresh browser with devtools open — verify zero 404s or JS errors in console, (4) add the handover banner (orange/red callout with "Note to [Team]" heading) at the top of the body. Never send a repo-internal HTML file directly to an external team.

383. **Multi-repo find-and-replace requires per-repo `git status` check** — When a project spans multiple git repos (a common pattern for Alaqraboon = portal repo at `D:\Alaqraboon\Alaqraboon repository\` + Readiness repo at `D:\Alaqraboon\Enhanced standalone version\`), a cross-cutting find-and-replace may modify files in both. Running `git status` in only one repo silently misses changes in the other. **Why**: Apr 14, 2026 — today's Amr Atya → Mohammed Chtioui rename touched `pm_summary.html` in the portal repo AND Readiness Report V.1/V.2 in the Readiness repo. Almost missed the Readiness changes because `git status` in the portal folder only showed 5 files. Needed 2 separate commits (`0597006` on portal master, `f76c4c9` on Readiness v1) and 2 separate pushes. **How to apply**: before committing after any cross-cutting edit in a multi-repo project, run `git status` in **every repo folder** that could have been touched. Maintain a memory note listing all repo roots for each project (e.g., `repo_separation.md` for Alaqraboon). After editing, walk each repo root in sequence and either commit changes or confirm clean. The `Email/` folder (which is often not a git repo at all) is a third class — files there get edited but don't get committed anywhere; track them mentally as "live on disk, not versioned."

384. **`ls` the parent before `mkdir` new handover folders — case/separator variants create silent duplicates** — Before creating a new subfolder for handover materials, plan notes, or any session scratch space, **list the parent directory first** to confirm no similarly-named folder already exists. Subtle variants — `UI_UX_Handover` vs `UIUX_Handover`, `Claude Courses` vs `Claude_Courses`, `v2` vs `V2` — are trivially easy to miss and create parallel folders that fragment the work. **Why**: Apr 14, 2026 — created `D:\Alaqraboon\UI_UX_Handover\` not knowing `D:\Alaqraboon\UIUX_Handover\` already existed from 12 minutes earlier in the same session (a prior Claude run). Had to consolidate files afterwards and delete the duplicate. Root cause: did not run `ls D:\Alaqraboon\` before the `mkdir`. **How to apply**: add an `ls` or `Glob` step as the first action in any "create a new folder" workflow. If any folder with a similar name exists (case-insensitive, whitespace-insensitive, underscore/hyphen-insensitive), reuse it instead of creating a new one. If uncertain whether two similar names are the "same" folder conceptually, ask the user which to consolidate into before doing any writes.

385. **Kickoff email deadlines belong in 3 places** — When a kickoff email sets a deadline, embed it in **three distinct places** so recipients cannot miss it while scrolling: (a) the meta block at the top alongside To/From/Date/Subject/Attachment, (b) a prominent color-coded callout box (red/orange bordered) right after the greeting paragraph, (c) a small inline tag under each deliverable heading ("Due within: [window] by [date]"). Single-placement leaves too much room for oversight. **Why**: Apr 14, 2026 — the UI/UX handover email initially had no deadline. After the user specified "all UX prototypes, 14 Apr → 14 May (one month)", I embedded it in all three places: meta row, red "⏰ Overall Deadline" callout, and per-delivery due tags. This survives both skimming and careful reading. **How to apply**: every time a kickoff or handover email includes a delivery commitment, build the three-place pattern from the start — don't wait for it to be retrofitted. In HTML emails, define two callout classes (`.note` for informational orange, `.note-deadline` for red) and a `.due-tag` for inline badges. Reuse across projects.

386. **Email drafts save as `.html` not `.md` — Outlook copy-paste requires preserved formatting** — When drafting an email that will be sent via Outlook (or any Microsoft 365 mail client), save the draft as `.html` rather than `.md`. Markdown is excellent for reading in a code editor, but it pastes into Outlook as plain text: headers lose their styling, bullets collapse, bold/italic disappear, and callout boxes become indistinguishable paragraphs. HTML pastes into Outlook's rich-text editor with formatting preserved — headers stay bold, bullet lists stay bulleted, colored callout backgrounds stay colored. **Why**: Apr 14, 2026 — drafted `Email_to_UIUX_Team.md` first for quick iteration, then converted to `Email_to_UIUX_Team.html` once structure was stable, so the user could do Ctrl+A / Ctrl+C in the browser and paste directly into Outlook with the red deadline callout and orange session-note boxes intact. **How to apply**: (1) use `.md` only during early drafting when structure is still changing and diffs matter; (2) the moment the content is stable enough to send, convert to `.html` with inline CSS (not external stylesheet — Outlook strips many external references); (3) define a minimal style block with just the classes you use (body, h1/h2, .meta, .note, .sig); (4) open in Chrome/Edge before sending to verify visual rendering, then copy-paste into Outlook. Keep both `.md` and `.html` in the same folder so future updates can edit the `.md` and regenerate the `.html`.

## Workspace Reorganization & Session Hooks (Apr 15, 2026)

388. **Check for SessionStart auto-pull hooks before reorganizing workspace folders** — Moving repo folders under a new parent silently breaks any `cd "<old-path>" && git pull` hook in the project-local `.claude/settings.json`. **Why**: Apr 15, 2026 — consolidating 10 Taqat repos under `D:\Taqat Trading & Business Solutions\Taqat Refactor - Repo\`. The SessionStart hook at `.claude/settings.json` had a hardcoded `cd "D:/Taqat Trading & Business Solutions"` that would have silently re-cloned every repo into the old root on the next session, undoing the move. Luckily noticed before executing. **How to apply**: before any folder move that touches pulled repos, grep the project-local `.claude/settings.json` for `SessionStart` / `git pull` / hardcoded paths to the affected folders, and update the hook's `cd` target FIRST (step 1 of the move sequence), then do the file-system move (step 2), then update doc references (step 3). Never leave an auto-pull hook pointing at a deleted or renamed folder — the next session will fight your change. Also check `Run-Dashboard-Server.ps1` or any other long-running script that hardcodes repo paths.

389. **PowerShell `Move-Item` "process in use" errors on directories don't always mean the move failed** — The error message "The process cannot access the file because it is being used by another process" can fire when the source directory's contents were successfully moved but PowerShell can't delete the now-empty source folder. In that case the data is already at the destination and a retry will fail with "destination already exists". **Why**: Apr 15, 2026 — during a batch `Move-Item` loop over 10 Taqat repos, `Protected`, `Reserved`, and `odoo-orch` each reported the "in use" error. A naive retry would have failed. Actual state check revealed `Protected` had fully moved (source was empty) while `Reserved` and `odoo-orch` had NOT moved at all (source still held the full tree). Different recovery for each: `Protected` needed `rmdir` of the empty source, while `Reserved`/`odoo-orch` needed a re-run of `Move-Item`. **How to apply**: after any batch `Move-Item` that reports errors, verify actual state with `ls source/` AND `ls target/` for each failing item before deciding on recovery. If source is empty and target has the content → `rmdir` source. If source has the content and target is empty or missing → retry `Move-Item`. If both have content → manual reconciliation needed (likely a partial copy). Do not blindly retry the entire batch — you'll fail on the items that actually succeeded.

## UI/UX Design Brief Generation (Apr 16, 2026)

390. **Generate a structured UI/UX Design Brief alongside every concept handover document** — When handing over a website/app concept to a UI/UX designer, don't send the raw concept document alone. Generate a companion **Design Brief** using a standardized 12-section template. The brief distills the concept into actionable designer instructions while the concept remains the full reference. **Why**: Apr 16, 2026 — the `Detailed_Website_Concept_Al_Aqraboon.html` (14 pages, 11 components, 6 objectives) was comprehensive but lacked designer-actionable structure: no MVP prioritization, no user flows, no deliverable checklist, no color direction, no responsive breakpoints. The design brief extracted these into a scannable format with assumption tags, MVP badges, and visual color swatches — giving the designer a clear starting point without having to parse the entire concept. **How to apply**: (1) read the full concept document and extract objectives, pages, features, technical constraints; (2) narrow to MVP scope — label each feature/page as MVP or post-MVP; (3) define 2-4 user flows as step-by-step journeys; (4) include color swatches inline (not just hex codes — render them visually); (5) clearly label assumptions with tags so the designer knows what's confirmed vs estimated; (6) match the handover note (Session 1/Session 2 scope) from the concept doc; (7) add the brief as a second attachment in the kickoff email. Both files live in the same handover folder. The brief is NOT a replacement for the concept — it's a structured companion that answers "what should I design first and how?" **Reusable Prompt (12-section template)**: "You are a senior UI/UX designer and product strategist. Convert the provided concept into a professional UI/UX Design Brief with these 12 sections: (1) Project Overview — Project Name, Project Type (Website/Mobile App/Dashboard/System), Brief Description; (2) Business Goals — Main objective, Expected KPIs; (3) Target Audience — User segments, Technical level, Devices used; (4) Core Features (MVP Scope) — List 3 to 6 key features only; (5) User Flow (High-Level) — Step-by-step user journeys; (6) Pages / Screens — List main screens required; (7) Content & Assets — Content availability, Branding status; (8) Design Preferences — Suggested style, Color direction with visual swatches, UX recommendations; (9) Technical Considerations — Platform, Constraints; (10) Deliverables — What the designer should provide; (11) Timeline Suggestion — Logical based on project size; (12) Additional UX Recommendations — Smart improvements based on the idea. Rules: keep it concise but professional, use bullet points, label assumptions clearly, focus on MVP, write in clear English, output as bilingual standalone HTML (EN/AR) matching handover folder style."

## Browser API Call Throttling & Dashboard Stability (Apr 16, 2026)

391. **Never fire more than 6 concurrent Azure DevOps API calls from a browser dashboard — use a throttle pool** — When a browser-based HTML dashboard queries Azure DevOps via `fetch()`, never use `Promise.all()` with eagerly-created promises. Instead, wrap each call in a lazy task factory (`function() { return fetch(...) }`) and run them through a concurrency-limited pool (max 6, matching Chrome's per-domain connection limit). **Why**: Apr 16, 2026 — `Project Management Handling.html` fired ~290 simultaneous API requests on a single "Fetch from DevOps" click (11 projects x 7 query types + 7 teams x 6 queries + 20 members x 6 queries + batch fetches). This exhausted browser memory (2-4 GB spike), froze the network stack, and crashed the entire laptop — requiring 2 forced restarts. Root pattern: 5 nested `Promise.all()` arrays all created eagerly, meaning all HTTP connections were initiated at construction time with no backpressure. **How to apply**: (1) define a `throttledRun(taskFactories, limit)` function that takes an array of zero-argument functions returning promises, starts `limit` concurrently, and launches the next task only when a slot frees up; (2) convert every `someArray.push(fetch(...))` to `someArray.push(function() { return fetch(...) })`; (3) replace `Promise.all(someArray)` with `throttledRun(someArray, 6)`; (4) add an abort flag checked between task launches so users can cancel mid-fetch; (5) show a progress counter ("X/N queries") so users know the fetch is alive. The throttle adds ~30-40s to total fetch time (sequential vs parallel) but prevents system instability. For dashboards with 100+ queries, consider a server-side cache (e.g., `DevOps_Cache_Server.ps1` on localhost:5051) that fetches once and serves cached JSON to all HTML consumers.

392. **Always add an Abort button to long-running browser fetch operations** — Any browser fetch operation that fires more than ~10 API calls should include a visible "Abort" button that sets a cancellation flag, stops new requests from launching, and cleanly restores the UI (re-enable the fetch button, show "Aborted" status). Without an abort mechanism, the user's only escape from a stuck or slow fetch is to kill the browser process — which on a resource-constrained machine can require a full system restart. **Why**: Apr 16, 2026 — `Project Management Handling.html` had no abort mechanism. Once "Fetch from DevOps" was clicked, ~290 requests were queued with no way to cancel. The user's laptop hung and required 2 forced restarts. After adding an `_abortFetch` flag checked by `throttledRun()` between task launches, plus a red "Abort" button injected next to the fetch button, the user can cancel at any time and the UI recovers cleanly. **How to apply**: (1) declare a module-level `_abortFetch = false` flag; (2) set it to `false` at the start of each fetch; (3) check it inside the throttle pool's `next()` function — if true, reject the promise chain; (4) create/show an "Abort" button (red, next to the fetch trigger) when fetch starts; (5) hide the abort button and re-enable the fetch button in both the success and error handlers; (6) in the error handler, check if the error was an abort and show a distinct "Fetch aborted by user" message instead of a generic error.

## PM-Dashboard Platform Build (Apr 16, 2026)

393. **When multiple PowerShell HTTP listeners serve the same user, consolidate into one lightweight server** — On Apr 16, 2026 the PM workspace had 4 separate PowerShell `HttpListener` servers on ports 5050-5053 (Outlook search, DevOps cache, drive scanner, reserved), each requiring manual startup via right-click `.bat` files. All shared the same `Send-Json` helper function copy-pasted across files. Consolidating into a single Bun + Hono server on port 3000 replaced all 4 with one `start.bat`, eliminated code duplication, and enabled shared SQLite state across all endpoints. **Why:** Each additional listener is a manual startup step the user forgets before meetings, a port conflict risk, and a maintenance burden when the shared pattern (CORS headers, JSON serialization) changes. **How to apply:** When you find 2+ local HTTP servers serving the same user/browser, unify them behind one server with route prefixes (`/api/cache`, `/api/search`, `/api/scan`). Use a framework like Hono that adds ~15KB overhead. Keep the old scripts as fallback until the new server is proven.

394. **Bun + Hono + bun:sqlite is the optimal stack for single-PM local dashboards on Windows** — After evaluating Grafana (200MB, no RTL), Metabase (requires Java), Evidence.dev (requires rewriting HTML to Markdown), n8n (Docker, overkill), and Astro/Next.js (build step complexity), the best fit for a Windows PM who works in Claude Code is Bun (10ms startup, built-in SQLite, TypeScript) + Hono (15KB server) + bun:sqlite (zero-dependency DB). **Why:** The PM is not a full-time developer — the stack must be maintainable by someone who codes through Claude Code, not by hand. Bun's built-in SQLite eliminates native module compilation issues on Windows. Hono's API is simple enough that Claude can scaffold and maintain the entire server. **How to apply:** For any local-first dashboard project with <100K data rows, Azure DevOps as data source, and a single PM user — default to this stack. Skip React/Vue/Angular entirely; vanilla JS + ECharts serves the same purpose without build tooling.

395. **Move PAT tokens from browser localStorage to server-side .env files immediately** — On Apr 16, 2026 the PM workspace had `localStorage.getItem('devops-pat')` returning the Azure DevOps PAT in cleartext across 3+ HTML dashboards. Any browser extension or same-origin script could read it. Moving the PAT to a `.env` file read by the Bun server eliminated this exposure — the browser never sees the token, only calls `localhost:3000/api/*` which adds auth headers server-side. **Why:** localStorage has no access control, no encryption, and persists across browser sessions. A leaked PAT grants full Azure DevOps access. **How to apply:** (1) Create `.env` with `AZURE_DEVOPS_PAT=...`; (2) Add `.env` to `.gitignore`; (3) Server reads PAT via `process.env` and adds `Authorization: Basic` header on outbound requests; (4) Browser fetches from `localhost/api/*` with no auth header; (5) Remove PAT modal from HTML dashboards (keep as fallback for direct DevOps access). (See also #32 for the chat/file exposure rule that this lesson extends to browser storage.)

396. **localStorage is not persistence — SQLite is the minimum viable data store for dashboards** — On Apr 16, 2026 all PM dashboard state (OKR scores, health statuses, Data Gate snapshots, SPI/CPI calculations, portfolio narratives) lived exclusively in browser localStorage under 15+ keys. Clearing browser data, switching browsers, or a browser crash wiped everything with no recovery path. The only backup was a manual "Export JSON" button. Replacing localStorage with SQLite (`pm-dashboard.db`) gave file-system persistence, historical snapshots, cross-dashboard shared state via API, and a single backup target. **Why:** localStorage is per-origin, per-browser, volatile, and has a 5-10MB limit. SQLite is a single file that survives browser changes, can be backed up with a file copy, and supports SQL queries for trend analysis. **How to apply:** For any dashboard that stores user-entered data beyond session scope, use SQLite (via `bun:sqlite` or `better-sqlite3`). Expose state via `GET/PUT /api/state/:key` endpoints. Keep localStorage as a read cache for offline scenarios, but treat SQLite as the source of truth. (See also #764 for the JSON-file implementation that actually shipped — month-sharded files won over SQLite for the v13 PMO Dashboard.)

397. **When migrating HTML dashboards to a new API, keep the old fetch path as fallback** — On Apr 16, 2026 OKR v7 was migrated to fetch from `localhost:3000/api/cache` instead of direct Azure DevOps calls. Instead of replacing the existing "Fetch from DevOps" button, a second "Fetch via Cache" button was added alongside it. This meant if the Bun server was down, the user could still fetch directly from Azure DevOps. **Why:** The new server is a single point of failure. If it crashes, all dashboards that depend on it become read-only. Keeping the direct-fetch fallback ensures the PM can always get fresh data for a meeting, even without the server. **How to apply:** When adding a cache/proxy layer in front of a direct API, add a new button/path rather than replacing the old one. Label clearly: "Fetch via Cache (fast)" vs "Fetch from DevOps (direct)". Auto-detect server availability on page load and show a badge.

398. **Auto-start local servers via Windows Startup folder shortcut, not VBS or Task Scheduler** — On Apr 16, 2026 three approaches were tried for auto-starting the PM-Dashboard server on Windows login: (1) `schtasks` — failed with "Access denied" (needs admin); (2) VBS script in Startup folder — failed with "Not enough memory resources"; (3) `.lnk` shortcut pointing to a `.bat` file with `WindowStyle=7` (minimized) — worked reliably. **Why:** Task Scheduler requires admin elevation. VBS `WScript.Shell.Run` is fragile on Windows 11 with memory pressure. A plain shortcut to a `.bat` is the simplest, most reliable approach — Windows executes it natively with no interpreter overhead. **How to apply:** (1) Create `start-silent.bat` with hardcoded paths (no `%USERPROFILE%` expansion issues); (2) Use PowerShell to create a `.lnk` shortcut with `WindowStyle = 7` (minimized); (3) Place the `.lnk` in `%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\`; (4) Log stdout to a file (`>> pm-dashboard.log 2>&1`) for debugging.

399. **Index CSV email data into SQLite at server startup for instant search** — On Apr 16, 2026 the Outlook email search on port 5050 scanned 12 CSV files (3,342 rows) on every search request using PowerShell `Import-Csv` + in-memory string matching. Moving the index to SQLite at server startup (136ms one-time cost) made every subsequent search instant (<10ms) and eliminated the need for the PowerShell server. **Why:** CSV scanning is O(n) per query and requires the PowerShell server to be running. SQLite indexing is O(n) once at startup, then O(log n) per query, and the data persists across restarts. **How to apply:** At server startup, read all CSV files, parse with a simple CSV parser, and bulk-insert into an `email_index` table using a transaction. Re-index on a schedule (every 6 hours) or on-demand via `POST /api/index-emails`.

400. **Historical snapshots in SQLite enable trend sparklines with zero additional infrastructure** — On Apr 16, 2026 every `POST /api/refresh` stored SPI/CPI and Data Gate values as timestamped rows in `spi_cpi_snapshots` and `data_gate_snapshots` tables. After just 2 refreshes, the `GET /api/trends/spi-cpi?days=30` endpoint returned enough data to render sparkline mini-charts (ECharts, 80x30px) in the SPI/CPI table. Over days/weeks, these accumulate into meaningful trend lines with no additional tooling. **Why:** Most trend visualization requires a time-series database (InfluxDB, Prometheus). But for a single PM with <1000 data points per month, SQLite `INSERT + SELECT WHERE snapshot_at >= ?` is sufficient. The key insight: store snapshots on every refresh, not just the latest value. **How to apply:** For any metric that changes over time (SPI, CPI, gap%, task counts), always INSERT a new row with a timestamp rather than UPDATE the latest row. Add a `GET /api/trends/:metric?days=N` endpoint that returns the time series. Render with ECharts `type: 'line'` at 80x30px for inline sparklines.

## Projects Handling Review Meeting — Dashboard Consolidation & Data Integration (Apr 14-16, 2026)

401. **Consolidation over fragmentation** — When building multiple related dashboards, consolidate into ONE file with tabs from the start. Never create separate HTML files for related features — it creates duplicate JS, duplicate fetch calls, and confuses stakeholders. **Why:** Apr 14-16, 2026 — 4 separate HTML files were built (Projects_Handling_Review_Meeting.html, Executive_Performance_Review_v1.html, Multi_Model_Estimation_Process_v1.html, Projects_Handling_Review_Meeting_Results.html) before the PM pushed back: "why we make with all fix another html, why, !!!!". Consolidated into one `Project Management Handling.html` with 10 tabs. 3 files archived. **How to apply:** Before creating a new HTML file, ask: "Does this belong as a tab in an existing dashboard?" If the data source is the same (DevOps, localStorage), it's a tab, not a file.

402. **No assumption defaults — if there is no data, show "no data"** — Never apply optimistic defaults when data is missing. If a project has no active DevOps iteration covering today, render it as "NO SPRINT" — not "ACTIVE". If a field has no data, show "No data" — don't invent a default value. **Why:** Apr 15, 2026 — `buildPortfolioProjects()` defaulted every project to `status: 'active'` when not in the hardcoded PORTFOLIO_KNOWN map. Water for Mosques (no sprint since Feb 8) and 12 other projects showed as "ACTIVE" in a director meeting — misleading. PM's rule: "no assumption please." Every silent default is a lie waiting to be caught. **How to apply:** Replace `|| 'active'` fallbacks with computed status from live data (iteration dates). Add explicit "NO SPRINT", "PLANNED", "NO DATA" states. Never use a positive status as a default.

403. **Metrics alone are not narratives — combine DevOps data + email context + PM knowledge** — Auto-generated text from SPI/CPI/task counts gives numbers but not context. Real portfolio status narratives (Where are we / What's next / Risk) require combining DevOps metrics + email communications + PM domain knowledge. **Why:** Apr 16, 2026 — `generateAutoText()` produced sentences like "SPI 0.92 — on track. 70 open tasks." The PM said: "i not talk about color, i talk for insights and the data." Real narrative required reading Outlook CSVs (1,436 emails) + DevOps cache (27 projects) + PM corrections ("GitHub access for Youssef belongs to BMS not HUB"). **How to apply:** Build auto-text as a starting point from metrics. Then audit email data for real context. Always present drafts to the PM for validation before hardcoding.

404. **Validate data sources before building features** — Before coding a feature that displays data, audit the data source first. Map coverage gaps so you know which cards will be empty. **Why:** Apr 16, 2026 — when building Portfolio Status cards for 27 projects, an audit revealed: 10 projects had zero email coverage, 13 had no active sprint, 3 had expired iterations. Knowing this BEFORE building prevented empty cards and false confidence. The PM's process: verify data → organize → then build. **How to apply:** Before any dashboard feature, run a data coverage audit: (1) list all entities to display, (2) check which have data in each source, (3) report gaps to the PM, (4) agree on fallback content for gaps, (5) then build.

405. **The PM is the integration layer — always validate automated extraction with the human** — No system automatically connects DevOps work items, Outlook emails, meeting decisions, and business context. Only the PM knows cross-system relationships that no API can detect. **Why:** Apr 16, 2026 — automated draft said "KhairGate (HUB): GitHub access requested for Youssef." PM corrected: "this topic related to BMS, not HUB." Also: "Afriqat: remove pending government approval — quality is done, waiting for content and Figma issues." And: "Water for Mosques: Project on Hold" (not "SPI 0.99 on track" which was technically accurate but contextually misleading). **How to apply:** Always present auto-generated content as a draft. Show it to the PM project-by-project. Expect 30-50% of projects to need corrections. The PM's corrections ARE the value — the automation just saves them from starting with a blank page.

406. **Match proven patterns for infrastructure — copy working code before innovating** — When building a new local server (PowerShell HttpListener, Node.js, Bun), copy the exact syntax patterns from an existing working server in the same workspace before adding new features. **Why:** Apr 16, 2026 — `DevOps_Cache_Server.ps1` crashed 3 times with PowerShell parser errors: (1) `<>` in WIQL strings interpreted as redirection operators, (2) `OR` keyword breaking string parsing, (3) here-strings with indentation issues. The Outlook `Run-Update-Server.ps1` on port 5050 — working on the same machine — used single-quoted strings + concatenation and had zero parser issues. Should have copied that pattern from the start. **How to apply:** Before writing infrastructure code, find the nearest working example in the workspace. Copy its string quoting style, error handling pattern, and request loop structure. Then add new features on top of the proven base.

407. **Confirm the audience before building the presentation** — Always ask "who will be in the room?" at the start of any presentation-preparation session. Different audience = different story emphasis. **Why:** Apr 14-16, 2026 — for 2 days the dashboard was framed as "presenting to Semir" (Director). At the end of day 2, the PM said "by the way, Semir and Dr. Amr." A second senior stakeholder changes the narrative structure, the level of technical detail, and which metrics to emphasize. This should have been established on day 1. **How to apply:** First question in any presentation prep session: "Who exactly will be in the room? Names and roles." Document in the plan. Revisit if the answer changes.

408. **Cache server as a force multiplier — one fetch, serve many dashboards** — A local HTTP listener that fetches all DevOps data once and caches it to a JSON file replaces 50+ individual API calls across multiple HTML dashboards. All dashboards read from the same cached JSON instead of each making their own API calls. **Why:** Apr 16, 2026 — `DevOps_Cache_Server.ps1` on port 5051 fetched all 27 projects (iterations + open Tasks + closed Tasks + SPI/CPI) in 42 seconds and cached to `devops_cache.json`. `Project Management Handling.html` reads this cache via `fetchFromCacheServer()` — one click replaces 3 separate refresh buttons (Data Gate + SPI/CPI + Iterations). Same pattern as the Outlook server on port 5050. **How to apply:** When 2+ HTML dashboards query the same API (Azure DevOps, Outlook, etc.), build a cache server that fetches once and serves cached JSON to all consumers. Use PowerShell HttpListener or Bun+Hono. Always include a fallback path so dashboards still work if the server is offline.

## Ragtools Evaluation & Operational Hardening (Apr 16, 2026)

409. **`rag serve` vs `rag service start` — know which command starts what** — On 2026-04-16, spent multiple restart attempts using `rag serve` to fix a crashed HTTP service. `rag serve` starts the MCP stdio server (for Claude Code integration), NOT the HTTP server on port 21420. The correct command is `rag service start`. The two commands serve completely different purposes and the naming is confusingly similar. **Why:** The ragtools CLI has two server modes: `serve` (MCP stdio transport, waits for Claude Code to pipe commands) and `service start` (HTTP server on 127.0.0.1:21420, serves the web UI and API). Using the wrong one produces a process that appears healthy (`rag.exe` in tasklist, 500+ MB RAM) but never binds the HTTP port. **How to apply:** Before running any CLI tool's subcommand, check `--help` first. For ragtools: `rag serve` = MCP stdio, `rag service start` = HTTP on 21420, `rag service run` = HTTP foreground. Document the distinction in the project's operational notes so future sessions don't repeat the confusion.

410. **Stale PID files mask service crashes — always verify process existence by PID AND endpoint** — On 2026-04-16, `service.pid` contained PID 16236 but that process had died hours earlier. Three other `rag.exe` processes were running (MCP direct mode instances), creating the illusion that the service was alive. Only `curl http://127.0.0.1:21420/health` revealed the HTTP service was actually down. **Why:** PID files are written at startup and never updated if the process crashes. Meanwhile, other instances of the same binary can be running for unrelated reasons (MCP direct mode spawns per-session). Checking `tasklist | grep rag` shows "rag is running" — which is true but misleading when the specific HTTP server instance is dead. **How to apply:** Never trust a PID file alone. Always verify with (1) `tasklist | grep <exact-PID>` to confirm the specific PID exists, AND (2) a health endpoint check (`curl /health`). A running binary name doesn't mean the right instance is running. For any service with a PID file, build a health check that confirms the actual endpoint responds — not just that a process with the right name exists.

411. **Versioned folder copies inside a RAG-indexed project multiply chunks silently — use ignore_patterns** — On 2026-04-16, project #7 (KhairGateWorld-Repository) consumed 11,854 chunks (41% of the entire index). Investigation revealed 152 of 381 MD files were in `versions/` subfolders (v1.0-base, v1.1-language-nav, v1.2-improvements) — historical snapshots of the same BRD documents, tripling the chunk count. Adding `ignore_patterns = ["**/versions/**"]` to config.toml and restarting the service dropped the total index from 28,799 to 20,957 chunks (-27.2%). **Why:** RAG indexers process every file they find. Versioned copies are intentional duplicates — same content, different paths — that bloat the index and can degrade search relevance (duplicate results). The Qdrant local-mode warning threshold is 20,000 points; this single change brought the index back within range. **How to apply:** When a RAG-indexed project contains versioned copies (versions/, archive/, old/, backup/, snapshots/), add them to `ignore_patterns` immediately. Check the top 5 projects by chunk count monthly — concentration risk means one bloated project can push the entire index past performance thresholds. Current top consumers: #7 KhairGateWorld-Repository, #40 Migration_Analysis (xhatem), #6 Migration_Analysis (Basha), #34 Taqat T&BS, #26 Alaqraboon.

412. **System tray icons replace invisible watchdogs — users need visual confirmation of background services** — On 2026-04-16, the PowerShell watchdog (`Watch-RAGService.ps1`) was running invisibly at login, checking ragtools health every 5 minutes. The user had no way to confirm ragtools was healthy without opening a terminal and running curl. Replacing it with a Python `pystray` system tray icon (green circle = healthy, red = down, yellow = checking) gave instant visual feedback plus a right-click menu for test search, benchmark, restart, and diagnostics. **Why:** Invisible background scripts provide zero user confidence. A green dot in the system tray answers "is ragtools running?" at a glance — no terminal needed. The tray icon also subsumes the watchdog's auto-restart capability (3 consecutive health-check failures trigger `rag service start`), eliminating one scheduled task. **How to apply:** Any background service the user depends on daily should have a visible system tray indicator, not just an invisible watchdog. Requirements: (1) color-coded status icon, (2) auto-restart on repeated failure, (3) right-click menu for common actions (open UI, restart, test, exit), (4) log to a file for post-mortem. Technology: Python `pystray` + `Pillow` for icon generation — zero external icon files needed. Launch via `pythonw.exe` (windowless) registered in Task Scheduler at login.

413. **Hook decision analytics reveal retrieval system blind spots — enable observability from day one** — On 2026-04-16, analyzing 143 hook decisions from `hook-decisions.log` revealed that 14% of domain questions fell through as `silent-pass:service-down` — the HTTP service was crashed and the hook couldn't probe the knowledge base. Without the analytics, this gap would have been invisible (the hook silently passes when the service is down, so the user never sees an error). Additional findings: 52% of prompts correctly bypassed the shape gate (short commands like "ok", "go", "6"), only 28% of domain questions had matching KB content (expected for a broad rule), and 0% of probes scored HIGH (>0.7) through the hook's quick single-query probe. **Why:** The retrieval-reminder hook makes real-time decisions (inject reminder vs silent pass) but those decisions are invisible unless logged. The `service-down` gap was the most actionable finding — it directly validated the need for the auto-restart watchdog/tray icon. **How to apply:** Enable `hook-observability` from day one on any retrieval-augmented system. Review the decision log weekly. Key metrics: service-down rate (target <2% after watchdog), reminder injection rate (13% is healthy — not too noisy), probe hit rate (28% is expected for broad coverage rules). Run `/rag-config hook-observability analyze` for aggregate stats.

414. **Coverage audits catch what file watchers miss — schedule them alongside live sync** — On 2026-04-16, the ragtools file watcher (debounce=3000ms) correctly caught real-time file changes in projects 1 (Global Lessons) and 30 (Relief Center). But it could NOT catch: (a) 5 missing root `<folder-name>.md` files that were never created (the watcher only tracks changes to existing files, not the absence of files that should exist), (b) the `versions/**` chunk bloat that predated the watcher's startup, (c) potential new sub-folders from the Apr 15 workspace reorganization that weren't registered in config.toml. The 3-check coverage audit (lesson #363) found all three structural gaps that live sync missed. **Why:** File watchers handle incremental changes (file modified/added/deleted). Coverage audits handle structural drift (missing files, unregistered folders, bloated projects, stale baselines). Both are needed — they catch different classes of problems. **How to apply:** Schedule the 3-check audit monthly (or after any workspace reorganization) alongside the weekly re-index. The audit checks: (1) new top-level folders vs config, (2) root MD existence for every indexed path, (3) live project list diff against config.toml. The weekly re-index catches content drift; the monthly audit catches structural drift.

415. **`pythonw.exe` shows as "Python" in Windows taskbar — compile to a named .exe with PyInstaller** — On 2026-04-16, the ragtools system tray icon launched via `pythonw.exe` appeared as "Python" in Windows Personalization > Taskbar settings, making it unrecognizable among other tray icons. Copying `pythonw.exe` as `RAGTools.exe` did NOT fix it — Windows reads the executable's embedded FileDescription metadata, not the filename. The fix was compiling `tray_icon.py` into a standalone `RAGTools.exe` using `pyinstaller --onefile --noconsole --name RAGTools`. This embeds the correct name in the executable's version info. **Why:** Users identify tray icons by name in taskbar settings. "Python" tells the user nothing — "RAGTools" tells them exactly which service it monitors. This matters when multiple Python-based tray apps run simultaneously. **How to apply:** For any Python system tray application intended for daily use: (1) develop with `pythonw.exe` for fast iteration, (2) once stable, compile with `pyinstaller --onefile --noconsole --name <AppName>`, (3) update the Task Scheduler task to point to the compiled exe, (4) clean up build artifacts (`build/`, `*.spec`). The compiled exe is self-contained — no Python installation needed on the target machine.

416. **Build a "Full System Test" into every tray icon — CMD diagnostic proves the system works end-to-end** — On 2026-04-16, added a "Full System Test" menu item to the RAGTools tray icon that opens a CMD window and runs 7 sequential checks (health, index stats, project registry, search test, benchmark, file watcher, config validation) with colored PASS/FAIL output and a summary box. This gives the user a one-click way to verify the entire ragtools stack — not just "is the service up?" but "can it search, how fast, how many projects, are paths valid?" **Why:** Individual health checks answer one question each. A full system test answers "is everything working?" in 10 seconds. When something breaks, the colored output immediately shows which layer failed — service, index, search, watcher, or config — without the user needing to know which diagnostic to run. **How to apply:** For any background service with a tray icon, add a "Full System Test" that exercises every major subsystem in sequence. Requirements: (1) open a visible console window (CMD/terminal) so users see real-time progress, (2) use colored output with checkmarks/crosses for quick scanning, (3) include a benchmark with latency bars for performance regression detection, (4) end with a summary box showing pass/fail/warn counts, (5) keep the window open (`input()` or `cmd /k`) so users can read results. Separate the test script from the tray icon script so it can also be run standalone from the command line.

## Team Development — Cross-skilling & Certification Ladders (Apr 17, 2026)

417. **Match headcount investment to pipeline mix — single employee, double hat is the right move when the minority skill doesn't justify full-time** — On 2026-04-17, confirmed the BA Courses (IIBA study) deliverable was driven by a real team-development need: the company is a software house where Odoo is a minority line of business. There was one employee new to his position currently acting as Odoo Functional Consultant + Business Analyst. The PM pushed him into the BA role because BA skills transfer across ALL projects (KhairGate, Wallet, BMS, Aqraboon, Relief Center, Afriqat, Herbal Gardens, Property Management) whereas Odoo skills only serve Odoo clients. **Why:** Dedicating full-time headcount to a minority skill (Odoo in a software-house pipeline) wastes capacity that could cover a horizontal gap (BA function missing across the entire portfolio). One double-hatted employee, with the dominant hat pointing toward the skill that serves every project, is more efficient than two half-utilized specialists. **How to apply:** Before hiring or assigning dedicated roles, audit the pipeline mix: what percentage of active projects actually need this specialty full-time? If <30%, double-hat a capable employee with the horizontal/transferable skill dominant. Re-evaluate quarterly — if the minority skill grows past 50% of pipeline, split the roles. Track both hats in the employee's development log separately so career conversations stay honest.

418. **Udemy cert → IIBA ECBA is the correct ladder step for a self-taught BA, not a substitute for formal credentialing** — On 2026-04-17, the Taqat BA employee completed the Udemy "Business Analysis Study Guide" course (https://www.udemy.com/course/business-analysis-study-guide/) after one month of self-research. Udemy certificates are informal — they prove study effort, not vendor-recognized competence. The correct next step is a formal IIBA credential (ECBA as the entry rung — no prereqs, ~$395 all-in with 1-yr membership), NOT a second Udemy course. **Why:** Employers and clients recognize IIBA ECBA/CCBA/CBAP on a résumé; Udemy certificates are invisible outside self-learning circles. A Udemy course is good preparation for an ECBA exam but doesn't replace the credential. For an employee acting as BA on real client projects, the credential chain (Udemy foundation → ECBA → AAC or CCBA → CBAP long-term) gives both the skills AND the external validation clients look for. **How to apply:** When an employee completes an informal training course in a new specialty, always map the next formal credential step on the same day — don't let self-study momentum dissipate. For BA: ECBA is the entry. For agile-heavy teams: follow ECBA with IIBA-AAC (85Q/2hr, $250 member, $30/3yr recert). For product-owner track: IIBA-CPOA (60Q/90min, no recert). Long-term senior track: CBAP (7,500 BA hours — PM requirements/stakeholder work counts). Document the ladder in the project's reference MD so future career conversations reuse it.

419. **Deep-study deliverables exist to enable decisions, not to showcase the researcher — always ask "what decision will this inform?"** — On 2026-04-17, the BA Courses (IIBA deep study) project looked on the surface like a general-interest research exercise. The real purpose, revealed only after delivery, was to build decision-support material for guiding a specific employee's certification path. The dashboard + 7 per-cert MDs + index now serve as the PM's reference when having career-development conversations with the employee. **Why:** Research without a target decision becomes a hobby. When a stakeholder asks for a "deep study" of anything (a website, a competitor, a certification program), the real question is "what will you DO differently after reading this?" Surfacing the intended decision reshapes the deliverable — role-recommendation matrices, cert-comparison tables, and learning-path diagrams matter more than exhaustive encyclopedic coverage. **How to apply:** On any research/study request, ask up-front: "Who will use this, and what decision will it inform?" If the answer is vague, propose a decision the deliverable COULD inform and confirm. Then weight the deliverable's structure toward that decision — the role-recommendation matrix in the BA Courses dashboard is the single most useful section because it answers "which cert for which employee?" directly. Everything else supports that answer.

## MCP & Retrieval Infrastructure — Connection-State Diagnosis (Apr 17, 2026)

420. **Retrieval hook "MCP is loaded" ≠ MCP tool actually callable from the agent** — On 2026-04-17, the rag-plugin retrieval-reminder hook fired with "MCP is loaded (probe score 0.60)" multiple times during a session where the actual `mcp__plugin_rag_ragtools__search_knowledge_base` tool was NOT in the agent's deferred-tool registry (ToolSearch returned no match). The hook tracks its own connection state to the ragtools HTTP service, but has no visibility into whether Claude Code successfully registered the MCP tool schemas for agent use. **Why:** the hook and the agent tool-registry are two independent systems — the hook proves the HTTP backend is reachable, not that the MCP stdio bridge delivered tool schemas to the agent. Trusting the hook alone produces silent retrieval failures: the agent "knows" RAG is available but can't actually call it. **How to apply:** on any session start, confirm RAG tool availability by running `ToolSearch(query="search_knowledge_base")` before relying on it. If the hook fires but ToolSearch returns nothing, assume broken bridge and either fall back to `curl http://127.0.0.1:21420/api/search` or trigger a full Claude Code restart.

421. **MCP stdio bridges are spawned at Claude Code startup — service restart alone does not re-bridge** — On 2026-04-17, after `rag service start` brought the HTTP service back up on port 21420 (confirmed `{"status":"ready"}`), the MCP tool `search_knowledge_base` was still absent from the agent tool registry. A simple "reload" of Claude Code also failed to restore it. Only a full close-and-reopen of Claude Code re-spawned the MCP stdio subprocess and re-registered the three ragtools MCP tools (`search_knowledge_base`, `index_status`, `list_projects`). **Why:** MCP servers in stdio transport mode are spawned as child processes of Claude Code at startup, reading `claude_desktop_config.json` / plugin config once. If that stdio child crashes or the target HTTP backend dies, Claude Code doesn't automatically re-spawn the stdio bridge on the next turn — it treats the tool set as permanently unavailable for the rest of the session. Also confirmed the tool name in the hook's reminder message (`mcp__plugin_rag-plugin_ragtools__...`) differs from the actual registered name (`mcp__plugin_rag_ragtools__...`) — don't rely on hook-quoted names for ToolSearch. **How to apply:** when the rag (or any MCP) backend is restarted mid-session, treat the MCP tool as lost and instruct the user to do a full Claude Code restart (not just window reload). Offer HTTP fallback (`curl http://127.0.0.1:21420/api/search?query=...&top_k=N`, which is GET, not POST) as an immediate unblocker while they restart.

422. **RAG-first rule surfaces cross-project connections the PM forgot existed** — On 2026-04-17, verifying the newly-repaired MCP with a test query "BA IIBA certification" returned as the 3rd-ranked hit (score 0.62) the Odoo Functional Consultant Course Plan, which contained the same Udemy BA course the employee had just completed — listed as Course 15 (v1) / Course 9 (v2) worth 21 IIBA PDUs. Neither the user nor Claude mentioned this connection during the earlier BA Courses deep-study session — it only surfaced because the RAG-first rule was enforced during routine verification. This proves that even when the immediate task has no obvious RAG relevance, a search can reveal related planning assets across projects. **Why:** users plan things in one project context and forget the links when working in another. RAG is the only layer that naturally finds those cross-project echoes — neither memory files nor project CLAUDE.md can surface them reliably because they're scoped to one project. **How to apply:** when a user mentions a specific artifact (a course, a vendor, a certification, an employee name, a project code), run `search_knowledge_base` against that exact noun even if the current task doesn't seem to need it. Surface cross-project hits to the user: "I noticed this same Udemy course was already in your Odoo Training Plan — worth 21 PDUs. Want me to log it in his BA Development Log?"

423. **Identify project audience before cloning a pattern — single-audience ≠ team-wide** — On 2026-04-17, when extending the BA Courses dashboard (v1 → v2) with the new Udemy Foundation course, Claude cloned the pattern from the Claude Courses dashboard — including the 8-row "Role → Recommended Certification" matrix, PM personal footnotes ("For an IT Project Manager (me)…"), and generic site-map pages for external IIBA resources. The user corrected: "this one is specifically for BA upgrade role, cause the folder that I was told to make like that, its course will be all team engage on it like claude AI, but this one it's specifically for BA." v3 removed the entire multi-role table and replaced it with a 4-step BA Employee Development Plan, stripped all PM first-person language, and trimmed Page 2 to BA-relevant resources only. **Why:** Claude Courses is a **team-wide training plan** (8 roles × multiple courses), while BA Courses is **single-audience** (one employee's role-upgrade ladder). Cloning the team-wide pattern into a single-audience project dilutes the focus — the BA employee doesn't care which cert a Frontend Developer should take. Structure is a communication signal: a role matrix tells the reader "this applies to many people"; a single-path ladder tells them "this applies to you." **How to apply:** **Before replicating a dashboard/deliverable pattern across projects, ask: "Who is the intended audience — one person, one role, or many?"** If one person → single-ladder, first-person-addressable content ("your next step"), no multi-role matrices, no generic footnotes for unrelated roles. If many people → role matrices and recommendation tables earn their place. The folder purpose (documented in project CLAUDE.md or memory) is the authoritative signal, not the folder's structural similarity to another project.

424. **Search RAG for employee names + dept roles BEFORE writing "the BA employee" or "the new hire" in any deliverable** — On 2026-04-17, Claude authored 3 dashboard versions + 8 MD files + a project memory file, all referring to the BA employee as "the Taqat Implementation BA employee" / "the new hire" / "the BA employee." The user then said: "BA employee's name is Mahmoud Afify, you should know this info." A RAG search for "Mahmoud Afify BA employee" returned `Department_Workflows_and_Roles_V2.md` at score 0.67 confirming **Mahmoud Abdelrahman Ameen El-Afify** is in Implementation Department with formal responsibilities (PBI Creation, BRD Management, Odoo Functional Consulting, Requirement Analysis). That data was already indexed and searchable — Claude should have searched BEFORE introducing any placeholder. **Why:** Placeholders like "the BA employee" feel neutral but are actually privacy-muting the person the file is about. The reader (the employee's manager, or the employee themselves) notices immediately. Every deliverable addressing a specific person should name them unless there's a privacy reason not to — and the RAG-first rule gives us the name at zero cost. Team role descriptions, department assignments, and reporting lines are all in the indexed `Department_Workflows_and_Roles` / `Roles_and_Responsibilities` MDs across multiple project versions. **How to apply:** Before writing the first draft of any deliverable that references "an employee," "a team member," "the new hire," "the BA," "the developer," etc., immediately run `mcp__plugin_rag_ragtools__search_knowledge_base(query="<role> <department> Taqat team member")`. If RAG returns a match ≥0.6, use the actual name in the deliverable. If no match, then use a placeholder and ask the user "who specifically is this deliverable for?" Do NOT proceed with a placeholder unless the user confirms they prefer one. This is a stricter application of the RAG-first rule, targeting the specific failure mode of addressing-by-role when addressing-by-name is available.

425. **Visible assumption transparency belongs IN the deliverable, not hidden in audit chat output** — On 2026-04-17, after producing a HIGH/MEDIUM/LOW assumptions audit in chat for BA_Courses_Dashboard_v3.html, the user said: "MEDIUM — content assumptions should be noted also on the file." The audit had existed only in conversation transcript and in a memory file — invisible to anyone opening the HTML weeks later (Mahmoud Afify, his manager, reviewers). Response was to add a standalone amber-themed "Assumptions & Verification Notes" card to the Overview tab and mirror the same structure in the Index MD: (a) item, (b) where used in the file, (c) basis, (d) status (⏳ verify / ✅ confirmed). A separate "Confirmed (no assumption)" section lists what IS solid. **Why:** Audit visibility in chat is for the PM who sees both chat + file; file visibility is for the file's actual audience, who only has the file. "Transparency" means transparent to the downstream reader, not just to the person who commissioned the work. A deliverable that silently mixes verified facts with reconstructed estimates erodes trust the moment a reader spots a wrong guess. **How to apply:** Any deliverable whose research mixes confirmed facts + reconstructed content + strategic choices needs an in-file transparency card. Template: amber/yellow border, ⚠ emoji, two tables — "Assumptions (MEDIUM risk)" with item/location/basis/status columns, and "Confirmed (no assumption)" as a bulleted list with ✅ markers. Include a "Never present in this file (by rule)" line for categories the user explicitly rejected (e.g., calendar dates). Mirror the card as a section in the accompanying Index MD so MD readers see the same transparency.

426. **HIGH-risk invented content: remove first, reconfirm if needed — never leave as silent default** — On 2026-04-17, the v2 → v3 BA Courses refinement surfaced 5 HIGH-risk assumptions I had invented (ECBA target date "end of Jun 2026", IIBA-AAC "after 6 months of BA work", CBAP "~3–4 yrs", skip-CCBA strategic decision, AAC-as-Step-2 pick). My audit listed them but left them IN the file, hoping the user would confirm. User's reply was "i don't need assumption for the high table" — confirming the correct default would have been to strip HIGH items up-front and ask "want me to restore specific dates?" rather than leaving invented commitments in a deliverable that might reach a stakeholder. This is a refinement of lesson #402 ("no assumption defaults"): the fix is not just "don't apply optimistic defaults silently" but also "when you realize you DID apply one, remove it by default — don't wait for the user to notice." **Why:** A deliverable carrying invented dates / prescribed paths is a lie-in-waiting. If the user skims the audit and doesn't push back, the invented content ships. If the user catches it later, they've already invested trust that needs rebuilding. The asymmetry is severe: removing and offering to restore costs one extra exchange; leaving and being caught costs credibility. **How to apply:** After an assumptions audit, triage: (a) MEDIUM/LOW items with reasonable industry defaults → keep + flag visibly in the transparency card (lesson 425); (b) HIGH items with fabricated specifics (dates the user never stated, named choices the user never authorized, prescribed paths) → **strip to a neutral form by default**. For dates: replace with "duration only" or "IIBA prereq only" framing. For prescribed choices: neutralize to "option · decision for user + stakeholder." Then tell the user in chat what was removed and offer to restore with their actual values. Never ship invented commitments waiting for silent confirmation.

427. **Same-session defects fix in place; cross-session evolution earns a new version** — On 2026-04-17, three successive corrections in one afternoon clarified when to version and when to edit. Correction 1: v2 had multi-role framing defect from cloning Claude Courses pattern — user caught it → I created v3 (v2 had already been opened + reviewed, so it earned version history). Correction 2: v3 had unauthorized calendar dates introduced in the same session as v3 — user caught it minutes later → I fixed v3 in place (no v4). Correction 3: v3 was missing Mahmoud Afify's name + visible assumptions card — user caught it minutes later → I fixed v3 in place again. Creating v4 for corrections 2 and 3 would have meant 3 versions in one afternoon with 2 of them never really "delivered" — noise, not history. **Why:** The versioning rule "never overwrite" exists to preserve the audit trail of what stakeholders have seen. In-session corrections that the user catches BEFORE the file leaves the conversation are authorship iterations, not deliverable revisions. Bumping versions for them inflates the version tree with drafts pretending to be milestones. The tie-breaker is whether the prior version has been reviewed externally — once it has, the next change earns a version bump; until it has, edits are authorship. **How to apply:** When correcting something you wrote in the *same session* that the user is actively reviewing → edit the current version in place. When correcting something you wrote *in an earlier session* or that was opened/shared externally → bump the version. Tie-breaker question: "has this version been seen by anyone other than me and the user in this session?" If no → edit in place. If yes → bump version. Document the in-place correction in the file's version-history note so the audit trail is preserved textually even when the filename doesn't change.

428. **Check the working directory for locally-saved source files BEFORE falling back to reconstruction from generic templates** — On 2026-04-17, when Udemy WebFetch returned 403 on the BA course URL, I immediately fell back to "reconstruct from typical 15h BA curriculum aligned to BABOK v3." The actual course HTML (`Full Business Analysis (21 IIBA® PDUs).html`, 1.66 MB) had been saved in the BA Courses folder at 11:17 that morning — *hours before I started the Udemy deep-study work*. The file was discovered only when the user later asked "have missing or pending?" and I finally ran `ls` on the folder. Three hours of reconstructed content shipped into the dashboard while the authoritative source sat 10 feet away. **Why:** Users often save source artifacts into the project folder BEFORE asking for work on the topic. That behavior is easy to miss because the conversation turns focus on the deliverable, not the folder's contents. But a reconstructed section breakdown is visibly worse than verified metadata — once the real JSON-LD was extracted, we found 10 sections (not my reconstructed 7), specific durations, real instructor name, rating, and IIBA Endorsement — all things reconstruction couldn't give. **How to apply:** The moment a remote fetch fails (403/404/rate limit/auth required) or before it even fires: **`ls` the working folder** for files whose names match the topic (`*Udemy*`, `*<course-name>*`, `*<product-name>*`, relevant file extensions like `.html`/`.pdf`/`.docx`). Grep for URL slugs or identifiers from the user's conversation. Only after confirming no local artifacts exist should reconstruction be acceptable — and even then, mark every reconstructed claim with a "verify from source" flag (per lesson #425).

429. **Verification-from-source ≠ verification-by-the-human-subject — never conflate "the document says X" with "the person did X"** — On 2026-04-17, after extracting the 10-section Udemy course TOC from the saved HTML's JSON-LD Course schema, I classified it as "✅ Confirmed / no assumption" and moved it out of the MEDIUM assumptions list. The user pushed back: "keep them in the HTML as assumption." JSON-LD proves what Udemy published on the course landing page — it does NOT prove Mahmoud Afify personally completed all 10 sections, or that the course hadn't been updated after his enrollment. Two different kinds of verification got conflated. We moved the item back to MEDIUM with the basis updated to: "Extracted from JSON-LD · best available evidence · awaiting Mahmoud's TOC confirmation." **Why:** When a deliverable is about a specific person's activity (training completed, hours logged, skills acquired), source fidelity (the document matches what was published) is necessary but not sufficient. The second required verification is whether the human subject confirms the document describes their actual experience. Skipping the second check means the deliverable silently claims as fact what is still a reasonable inference. **How to apply:** When marking any item as "Confirmed / no assumption" in a transparency card (lesson #425), distinguish two statuses: (a) **Confirmed from source** — document matches canonical publication, safe to cite factually; (b) **Confirmed by subject** — the human the deliverable is about has reviewed and agreed. For claims about a person's activity (coursework, certifications, hours, completions), require (b) not just (a). For claims about published facts (exam fees, IIBA requirements, course page metadata), (a) is enough. Label the status explicitly: "✅ verified from source · ⏳ pending subject confirmation" when both are relevant but only (a) has been done.

## Ragtools Operational Hardening — Watchdog Registration & Boot Readiness (Apr 17, 2026)

430. **Writing a watchdog script ≠ registering it — verify with Get-ScheduledTask, not by reading the script header** — On 2026-04-17, investigation of "RAG not working after reboot" revealed `C:\Users\DELL\AppData\Local\ragtools\Watch-RAGService.ps1` (written Apr 16) had a comment at the top reading "Registered via Task Scheduler to run at login", but `schtasks /query` showed no such task existed. `watchdog.log` contained exactly one line from Apr 16 16:10:30 when it was launched manually, then nothing — it died with that shell and was never resurrected. For 24+ hours the user believed "the watchdog protects me" while it was doing nothing. Fix was a single `Register-ScheduledTask` call. **Why:** Shell scripts, hooks, and watchdog scripts are only active when *something else* — Task Scheduler, systemd, a Run registry key, a Startup shortcut, a service wrapper — is configured to run them. Writing the file is maybe 20% of the work; wiring it into the runtime is the other 80%. Comments inside the script describing its intended deployment are intent, not proof. **How to apply:** For every "run at login / on a timer / at service start" script, the commit that adds the script must also add the scheduler entry that actually runs it (and a verification command that confirms it). Acceptance test on Windows: `Get-ScheduledTask -TaskName '<name>'` returns `Ready` or `Running`. On Linux: `systemctl is-enabled <name>` returns `enabled`. On macOS: `launchctl list | grep <label>`. If the verification command returns nothing, the script is not deployed — regardless of what the header says. Pair every new watchdog/autorun script with its register-and-verify commands in the same checklist.

431. **"Service started" ≠ "service ready" — autostart scripts must poll the readiness endpoint before exiting** — On 2026-04-17, `RAGTools.vbs` in the Startup folder called `rag.exe service start` and exited immediately. The CLI returns in milliseconds, but the HTTP service on port 21420 took 3–4 minutes to become responsive (encoder load + 20,968 Qdrant points — past the 20k local-mode recommended limit documented in `config.toml`). Every login had an invisible 1–2 minute dead window where MCP queries failed silently before RAG became available. Fix was to add a polling loop after `service start`: `For i = 1 To 12 / WScript.Sleep 5000 / MSXML2.XMLHTTP GET /health / Exit For on 200`, capped at 60s. **Why:** Service start commands typically return as soon as the child process is launched, not when the service is actually listening. Any downstream caller that assumes "start returned → service ready" hits a race. For heavyweight services (ML model load, large vector-store init, multi-GB cache warmup), readiness can lag process launch by minutes. The only reliable signal that a service is ready is a 200 response from its own health endpoint. **How to apply:** In every autostart / service-launch script, after issuing `start`, loop: sleep N seconds → probe `/health` (or the service's readiness endpoint) → exit success on 200 → timeout with a loud error after M minutes. Never exit success based on "start command returned". Log the wall-clock gap between start-issued and health-ready — if it grows, your cache/model/index has crossed a size threshold and the load time is now a user-visible delay worth mitigating (docker, remote backend, on-demand init, etc.). For ragtools specifically: the config.toml warning at 20k Qdrant points matched the observed 3–4 min load.

432. **"Not recognized" errors mean wrong shell, not wrong command — and full-buffer pastes corrupt the next shell** — On 2026-04-17, the user ran `Stop-Process -Name rag -Force` in `cmd.exe` (Command Prompt) and got `'Stop-Process' is not recognized as an internal or external command`. `Stop-Process` is a PowerShell cmdlet; CMD has no access to it. The user's next attempt was to paste the *entire* CMD window contents — banner `Microsoft Windows [Version 10.0...]`, copyright line, previous prompt, error text, everything — into PowerShell. PowerShell tried to parse the banner as code and emitted `Unexpected token 'Microsoft' in expression or statement`. Both errors looked like the command was wrong; neither was. The first was a wrong-shell problem; the second was a paste-hygiene problem. **Why:** Command names work differently across shells — PowerShell cmdlets aren't in CMD, bash utilities aren't in PowerShell, Unix-style flags aren't in Windows shells. When a user copies from one terminal and pastes into another, they often grab the full buffer including prompt text, which the target shell tries to execute. The resulting parser errors look like syntax problems but are really shell-identity and paste-hygiene problems. Blaming the command wastes time. **How to apply:** (1) When a shell says "not recognized" or emits `Unexpected token '<English-word>'`, first diagnose **which shell** the command is meant for: PowerShell cmdlets (`Stop-Process`, `Get-ChildItem`, `Invoke-WebRequest`) need a `PS C:\>` prompt; CMD commands (`dir`, `taskkill`, `where`) need `C:\>`; bash commands (`ls`, `grep`, `curl`) need a Unix shell. The prompt prefix is the tell. (2) For every shell-specific instruction given to a user, include both (a) how to confirm they're in the right shell (prompt marker), and (b) a one-line fallback for their likely other shell — e.g., CMD `taskkill /F /IM rag.exe` as a fallback for PowerShell `Stop-Process -Name rag -Force`. (3) When showing a command in docs/chat, present only the command line, never the `PS>` / `>` prefix — users will copy prefix and all.

## Report Transparency, Cross-Tab Navigation & MOM ↔ Follow-up Reconciliation (Apr 20, 2026)

433. **Add an explicit Assumptions Register tab whenever a deliverable layers interpretation on top of source documents** — On 2026-04-20, the v1 of the BMS ↔ Aqraboon 15-Apr MOM HTML was delivered with five tabs (Executive Summary · MOM verbatim · Salah's follow-up email verbatim · Deep Study · Consolidated Actions) and immediately got the pushback question "did we have assumption in this file?" The analysis tabs contained a 5/2/1/2 reclassification of the 10 sync gaps, three "divergence" framings, nine email-derived action rows with inferred owners and statuses, and six "missing from both documents" items I had added — none of which were literal quotes. v2 added a sixth **Assumptions Register** tab with a 5-column table (# · Location in HTML · What I asserted · What the source actually says · Risk pill: High/Medium/Low), separating 10 hard assumptions (interpretations, classifications, status inferences, invented actions) from 4 soft ones (colour choices, inferred owners, editorial phrasing), and closing with a "What is NOT an assumption — fully source-faithful" card certifying the verbatim tabs. **Why:** For any deliverable that reclassifies or interprets source documents, the credibility risk isn't that interpretations are wrong — it's that stakeholders can't tell which claims are facts and which are PM judgements. Answering that inside the deliverable itself turns a weakness into a governance feature: business sign-off becomes row-by-row ("confirm / soften / remove") rather than a whole-deliverable debate, and the closing-loop email writes itself from the "confirm" rows. **How to apply:** For every MOM, closing-loop report, gap-analysis, quarterly review, or multi-document synthesis that layers analysis on sources — add the Assumptions Register tab *before* sending. Target ~10–15 hard assumptions and 3–5 soft ones as a healthy range; fewer usually signals shallow analysis, more signals scope creep. Use severity pills (High for classifications and invented actions, Medium for framing choices, Low for time derivations and colour conventions). Cross-link from every analytical claim in the body back to its assumption row via an inline `→ Assumption #N` chip (lesson #434).

434. **Use prefix-based anchor IDs (#tab-item) + a JS hash resolver so cross-tab deep-links Just Work in multi-tab HTMLs** — On 2026-04-20, v3 of the BMS MOM HTML added cross-tab navigation after v2 had grown to six tabs and reviewers couldn't follow the claim-chain MOM Decision #1 → Divergence #1 → Email gap #9 → Consolidated row 27 → Assumption #14. Each pivotal element got an ID of the form `<tab-prefix>-<item>` (e.g. `study-div-2`, `email-gap-9`, `mom-dec-1`, `act-27`, `ass-hard-1`). A tiny JS map `{exec:'exec', mom:'mom', email:'email', study:'study', act:'actions', ass:'assumptions'}` plus a `resolveHash()` function parses `location.hash`, extracts the prefix, activates the matching tab via the existing tab-switcher, then `scrollIntoView` with a 2-second yellow-flash animation on the target. A delegated `document.addEventListener('click', ...)` intercepts all internal `<a href="#...">` clicks so the flow is one click from anywhere. Each tab also got a "Top-Picked Cross-References" callout at the top — 5–6 **curated** jumps (labelled by intent: "Drill into finding 1 →", "Divergence #2 ↔ source →", "Challenge my analysis →"), not auto-generated from every anchor. **Why:** In a long multi-tab report, the value isn't tab independence — it's the web of relationships between tabs. Without explicit cross-links, readers have to search the other tabs by hand, lose context, and give up on verifying individual claims. Editorial curation matters more than completeness: 5–6 hand-picked jumps per tab are more useful than 50 auto-generated ones because they signal "this is what a reviewer should trace first." **How to apply:** Apply to any HTML with ≥ 4 tabs or where the same logical entity appears in multiple places (OKR dashboard KRs, Outlook report categories, Weekly Meeting agenda items, gap-analyses, multi-doc syntheses). Prefix conventions: use the tab's short name (max 5 chars) as prefix so hashes stay readable (`#mom-act-3` not `#meeting-minutes-action-item-3`). Add `scroll-margin-top: 180px` to the `.xref-target` CSS class so the sticky header doesn't cover the flash animation. Skip for single-tab pages or purely linear reports — the cost isn't worth it.

435. **Always cross-check a MOM's "accepted limitations" against the EM's follow-up email — technical dispositions often close more gaps than the MOM records** — On 2026-04-20, the Apr-15 Aqraboon ↔ BMS integration MOM recorded 10 sync gaps between the two systems as "accepted current state — not defects" (Decision #1, "sync boundaries are acknowledged and accepted"). The MOM's out-of-scope list then reinforced the passive framing by listing "real-time reverse synchronization from BMS to Aqraboon" as out of scope. But Mohamed Salah Basha's follow-up email at 16:24 Doha (≈3 hours after the meeting) contained an "Initial Investigation" column against each of the 10 gaps with dispositions like "Current QC APIs should cover this" (gap 1), "Not a valid business case — prevent in AQ" (gap 2), "New QC API once Wallet finished return implementation" (gap 3), "Balance-update flag fix" (gap 7), "Lock AQ beneficiaries in BMS" (gap 9). Bucketing the 10 dispositions yielded: 5 **Closeable now** (gaps 1, 6, 7, 8, 10), 2 **Scope out** (gaps 2, 5), 1 **Pending dependency** (gap 3 blocked by Wallet team's return API), 2 **Genuinely open** (gaps 4, 9). The MOM's passive language had under-reported the team's actual technical position by 7 gaps — copying it into the closing-loop email to Semir/Bahaa would have weakened the governance artifact. **Why:** An MOM is a meeting-time record; it captures what was agreed by consensus, which tends toward conservative "accept current state" language when the technical implications aren't fully unpacked in the room. The EM's written follow-up arrives with 2–4 hours of additional reflection and technical analysis. Treating both documents as equal-status inputs to the closing-loop artefact — not treating the MOM as the only authoritative source — is what produces a closing-loop that actually reflects the team's position. **How to apply:** For every multi-system integration MOM (BMS / Wallet / Aqraboon / HUB / and similar), wait for the post-meeting email (up to 24 hours) before drafting the closing-loop. Render the email verbatim in a dedicated tab of the deliverable (next to the MOM verbatim). In the Deep Study tab add a "Reclassification" table using the five-bucket taxonomy (**Closeable now / Close with policy / Scope out / Pending dependency / Genuinely open**) and a headline count (e.g. "5 / 2 / 1 / 2"). Flag the reclassification as a PM interpretation in the Assumptions Register (per lesson #433) so stakeholders can challenge individual bucket assignments without rejecting the framework. The closing-loop email then goes out *from the reclassified list*, not from the MOM's passive language.

## Bilingual Dashboard Completeness & Roster Modeling (Apr 20, 2026)

436. **Search local project data files BEFORE saying "not in the knowledge base"** — On 2026-04-20, when asked about team member "Mostaf Labib" for the Team Velocity tab's roster, I ran ragtools `search_knowledge_base` against every Department_Workflows_and_Roles, OKR_KPI_Dashboard, and RELIEF_CENTER_PROJECT_STATUS doc — came back empty — and told the user "not found anywhere". The user pushed back with "search deep in your memory". A plain Grep for `Labib` across `D:\Project Management\` turned up `devops_cache.json` in the exact same folder as the dashboard, with `"assignee": "Mostaf Labib"` on 2 tasks in KhairGate (BMS) Sprint 3 — the authoritative source the dashboard itself reads from. Same pass surfaced 2 more unmapped assignees (Afthab P A, Badr Mohamed Sayed Mohamed Badr) the KB didn't have either. **Why:** Ragtools indexes *documentation* (what PMs write about the team). But the authoritative source of who exists in the system is the application's own cache/data files (`devops_cache.json`, exported CSV rosters, Azure DevOps user cache). Documentation lags reality by weeks-to-months; cache files refresh daily. Saying "not in KB" when the user asked "is this person real" is a retrieval failure dressed up as an answer. **How to apply:** When the question is "does X exist" (person, project name, field value, feature flag) rather than "what do we know about X" (narrative, context, history), search in this order: (1) the current project's own data files (JSON/CSV/MD in the same folder as the artefact you're building or editing); (2) ragtools for context about what was found in step 1; (3) conversation memory. Never skip step 1. Local data is authoritative; the indexed KB is a derivative and always stale by definition.

437. **Never embed HTML tags inside `data-i18n` translation values — split into separate spans instead** — On 2026-04-20, the Team Velocity tab's "How this tab works" footer had three notes shaped `<div data-i18n="tv-note-1"><strong>Velocity = ...</strong> ClosedDate is stamped ...</div>`, with matching Arabic values in the T map containing literal `<strong>...</strong>` tags. When the user toggled AR, the `<strong>` rendered as visible text "<strong>" on-screen because `applyLang()` uses `el.textContent = T[key]`, which stringifies tags literally. Worse, subsequent toggle back to EN also lost the bold because `data-i18n-original` had captured the plain-text form during the first AR pass. Fix was to restructure each note into two adjacent spans — `<span data-i18n="tv-note-1-lead" style="font-weight:700;">…</span> <span data-i18n="tv-note-1-tail">…</span>` — with plain-text values in the T map. **Why:** Dashboard i18n systems built around `textContent` are incompatible with HTML markup inside translation strings. The contract is: one `data-i18n` element = one contiguous piece of text. Styling (bold, italic, colour) must live on the element itself via CSS or inline style, not inside the translatable string. If you need mid-string emphasis, split the string into multiple sibling elements each with its own `data-i18n` key. **How to apply:** (1) When writing new bilingual HTML, never put `<strong>`, `<em>`, `<b>`, `<i>`, `<code>`, `<a>`, or any other tag inside a `data-i18n` element's direct text content if the i18n system uses `textContent`. (2) When auditing an existing bilingual page, Grep for `data-i18n="[^"]*"><[a-z]` to find offending cells. (3) If you *must* preserve inline tags (legacy content), switch that one element to `innerHTML` replacement in the applyLang walker — but document the exception clearly because it opens an XSS vector if any T value is untrusted.

438. **Store timestamps as ISO in `localStorage`; format with the current locale at render time** — On 2026-04-20, the Project Management Handling dashboard stored the last-fetch timestamp in `FETCH_TS_KEY` as a pre-formatted string `"4/19/2026, 3:19:52 PM"` (output of `toLocaleDateString() + ' ' + toLocaleTimeString()`). Toggling AR after a fetch could not flip the `"PM"` suffix to Arabic `م` because the stored value had already baked in the English formatting — the language toggle handler had no ISO to work from. Fix was three-part: (a) introduce `i18nLocale()` returning `'ar-EG'` or `'en-US'` based on `<html lang>`; (b) introduce `fmtTs(isoOrString)` that parses via `new Date(…)` and calls `toLocaleString(i18nLocale())`, falling back to the raw string if parsing fails (backward compatible); (c) change the save site to `new Date().toISOString()` and make `updateFetchTimestamp()` call `fmtTs(ts)` before display. `applyLang()` then re-invokes `updateFetchTimestamp(savedTs)` on every toggle, so `Fetched: 4/19/2026, 3:19:52 PM` instantly becomes `تم الجلب: ١٩‏/٤‏/٢٠٢٦، ٣:١٩:٥٢ م`. Also updated 10 other `toLocale*()` callsites (insight timestamps, cache time, fetch completion, print header, iteration formatter) to pass `i18nLocale()` instead of defaulting to browser locale or hardcoded `'en-US'`. **Why:** Persistent representation and display representation are two different concerns. `localStorage`, database columns, JSON payloads, and URL parameters should hold raw data (ISO strings, numeric cents, raw codes). Locale / number / currency formatting is a render-time concern that depends on the user's *current* language preference, which can change after the data was stored. Coupling them means every locale toggle requires re-fetching or re-computing the underlying data — a slow, brittle path. **How to apply:** For every bilingual or multi-locale application: (1) never call `toLocaleString()`, `toLocaleDateString()`, `toLocaleTimeString()`, `toLocaleString()`, `Intl.NumberFormat`, `Intl.DateTimeFormat` without explicitly passing the current locale; (2) for data that persists beyond one request (localStorage, API payload, DB row), store ISO 8601 dates / raw numbers / locale-independent codes; (3) add a single `i18nLocale()` helper that reads the current UI language and returns the appropriate BCP-47 tag — every formatting call goes through it; (4) wire the language toggle handler to re-run any function that renders persisted data so formatting updates immediately on toggle; (5) Grep periodically for `toLocale[A-Z]\w*\(\)` without arguments to catch regressions.

439. **When modeling team rosters, plan for "people who left" from day one — not as an after-the-fact patch** — On 2026-04-20, the Team Velocity tab initially assumed every name in `ASSIGNEE_TO_TEAM` represented a current employee. When user said "Badr became no longer in our company, like Eslam", the existing model had no way to represent it. Had to retrofit: (a) add a `FORMER_MEMBERS` set of display names; (b) add a `decorateName(name)` helper that appends `<span style="…">(former)</span>` when matched; (c) apply `decorateName()` to every render site where a name appears (Per-Member table, Members popup, Top contributors in the sprint tree, Team Roster cards); (d) add `'former' → 'سابق'` translation key so the marker flips bilingually. Their past closed tasks *still count* toward team velocity (historical accuracy is preserved) — the only change is the visual marker. A separate "Team Roster" reference section was also needed because former members with zero *current* closed tasks didn't appear in velocity tables at all. **Why:** Team rosters are almost always modelled as a flat {name → team} map with no lifecycle dimension. But real organisations have joiners, leavers, role changes, and dual-role/cross-team people. The first time a former employee appears in historical data (tasks, bugs, commits, timesheets), the naive model breaks down: they show up with a team label as if they're still current, or they disappear entirely because they have no current activity. Either outcome misrepresents the organisation to stakeholders. Retrofitting this after velocity / capacity / performance displays are built is painful because every render site needs updating. Planning for it from day one is a 5-minute addition: one `FORMER_MEMBERS` set + one `decorateName()` helper + one translation key + systematic application. **How to apply:** For any dashboard that lists team members (velocity, capacity, roster, org chart, performance review, timesheet): (1) model `TEAM_ROSTER` with the full canonical name list per team *and* a separate `FORMER_MEMBERS` set listing departures; (2) provide a `decorateName()` helper that attaches the "(former)" marker when matched; (3) use it at every render site — don't spread raw names directly into HTML; (4) bilingual marker text goes in the T map; (5) in velocity/throughput calculations, keep former members' historical contributions counted toward team totals (truthful history) but clearly visually marked so stakeholders don't plan future capacity around them; (6) if former members drop out of current-activity views entirely, add a separate "Team Roster" reference section that always shows the full org regardless of activity. Consider extending later to richer lifecycle states (joiner, on-leave, part-time, contractor) using the same pattern.

## Personal PM Prep Doc & Role-Verification Discipline (Apr 20, 2026)

440. **Verify team roles against the department-workflow source-of-truth BEFORE writing ownership or handoff statements into any deliverable** — On 2026-04-20, while building v3/v4 of the BMS Aqraboon-integration PM prep HTML, I routed PBI creation to Ahmed Lakosha ("Implementation / QA" per the 15-Apr MOM DOCX attendee list) and QA-QC approval to "Shehab / Hala". Both were wrong. Weekly Meeting Agenda roster + Department Workflows V4 Roles section confirmed: **Mahmoud Afify** is Implementation Team Rep (owns PBI creation per Sec.6.1 Request-to-Delivery), **Ahmed Lakosha** is Frontend Team Rep, **Mostafa Ahmed** is the sole QA-QC team member, **Shehab Gamal** is Backend Team Rep. The MOM DOCX's "Implementation / QA" label for Lakosha was stale; I inferred current roles from stale source text instead of the authoritative workflow doc. Same class of error in the same artefact: v4 said "confirm the Aqraboon project exists on dev.azure.com/TaqaTechno — if missing, ping Hatem before the call to create it" even though the project `KhairGate (BMS)` had existed under TaqaTechno for a year as a High-priority portfolio item. User caught both mistakes, not me. **Why:** A PM prep doc is an instrument for assigning commitments in a meeting — if the owner is wrong, the ask lands on the wrong person, and in the room it reads as a credibility hit (the PM doesn't know their own team). Role-verification is not a nice-to-have; it is a prerequisite for every sentence that names a person. **How to apply:** Before finalising any deliverable that contains a handoff statement, run a verification pass: for every named role or ownership phrase, (a) find the person in the authoritative source-of-truth for the project (Department Workflows V3/V4 + Weekly Meeting Agenda attendee list + Active Projects Portfolio), (b) confirm their current role, (c) confirm the system they work in is in the expected state (project already exists? access level correct?). When RAG returns a role statement, trust it over the MOM or email. If inconsistent between a MOM and the workflow doc, the **workflow doc wins** and the MOM gets flagged as stale. Applies to: PBI ownership assignments, QA approval handoffs, DevOps project provisioning steps, access-upgrade recommendations, closing-loop email recipient lists.

441. **Personal PM prep HTML is a separate artefact class from stakeholder-facing reports — different voice, different structure, different audience discipline** — On 2026-04-20, after delivering the stakeholder-facing v3 BMS-Aqraboon MOM HTML (6 tabs, neutral voice, source-faithful), the PM asked for a separate meeting-day playbook "for me only". What worked: a red 🔒 "For Mahmoud Only — do not circulate" badge in the header; first-person voice ("I", "me"); 5-tab structure mapped to meeting phases (Today's Plan / Agenda & Script / DevOps Setup / Risks & Questions I Must Ask / After the Meeting); speaking scripts as italic quote-cards for politically sensitive moments ("Presenting the reclassification without stepping on Salah", "Asking Aya for the Wallet ETA"); an explicit **Things I Should NOT Say** card paired with a **Say Instead** card; a **Political Sensitivities** card naming people and what might go wrong ("Bahaa carries business sign-off — don't ask him for technical commitments"; "Mostafa is a 1-person QA team — confirm capacity, don't just announce the SLA"); explicit **Green-Exit vs Red-Exit** outcomes at the top so success and failure are defined before the meeting starts. Filename prefix `PM_Personal_Meeting_Prep_vN.html` saved alongside the meeting-source artefacts (MOM DOCX, follow-up .msg) — distinct enough that it is visually obvious which files travel to stakeholders and which stay private. **Why:** Stakeholder HTMLs are credibility artefacts — neutral language, no political commentary, source-faithful rendering. Prep docs are the opposite — they NEED the political commentary, the "credit Salah publicly", the "watch out for Mostafa's capacity gap", the "never say the MOM got it wrong". Mixing these two genres into one file means either the stakeholder view is inappropriately candid, or the prep view is uselessly sanitised. Splitting them lets each be maximally useful for its audience. **How to apply:** Every time a PM has a meeting where they are running the room (kickoff, review, closing-loop, sprint retro, handover), produce a separate PM Personal Prep HTML alongside the stakeholder MOM/agenda. Use the Tab 1–5 pattern (Today / Agenda+Script / DevOps / Risks+Questions / After). Include speaking scripts for at minimum: the opening, presenting any analysis you did, asking the critical single-blocker question, closing the call. Include a Green-Exit / Red-Exit pair at the top so success is defined before the meeting starts. Always name it with the `PM_Personal_*` prefix and carry the 🔒 badge — these conventions make "do not share" visually obvious. Pair with lesson #442 (live-capture bridge) for the meeting-time capture dimension.

442. **Add form-field capture + localStorage auto-save + export-to-text button to turn a static prep doc into an in-meeting capture tool that feeds the minutes email directly** — On 2026-04-20, v6 of the BMS PM prep HTML contained a "Questions I must get answers to" table (8 rows) and an "18-PBI draft" table (18 rows) but they were read-only — the PM had no way to capture answers / owners / dates / decisions as the meeting progressed. User asked: "add the field for every action when I move through the meeting today, to make sure I can close all gaps". v7 added three ingredients: (1) **form fields on every actionable row** — for the 8 questions, columns Answer (textarea) / Committed-by (text) / Date (date picker) / Status (select with Answered·Date-to-answer·Parked·Blocked); for the 18 PBIs, columns Owner (text) / Afify Decision (select with Keep·Merge·Split·Rename·Drop) / Notes (text). Each input carries a unique `data-field` id and human-readable `data-label`. (2) **Auto-save to `localStorage` on every input/change event**, keyed under a versioned single key `pm-prep-v7-capture` holding a `{fieldId: value}` object. Visual feedback via toggling a `.filled` CSS class (amber background → green with green border). A **floating bar fixed bottom-right** shows "📝 Captured: X / Y" updating on every keystroke, with Export and Clear buttons. (3) An **Export button** that scans all `.capture` elements, groups them by their parent `<tbody>`'s `data-capture-section` attribute, emits a plaintext block with ISO timestamp and "=" separator, shown in a modal with a **📋 Copy to clipboard** button. The exported text is deliberately formatted to paste straight into the minutes email — grouped headings, bullet rows, one line per field. A Clear button with `confirm()` prompt lets the PM reset between meetings. Total: 86 capture fields across 2 tables, all auto-persisted, all exportable in one click. **Why:** A static prep doc stops being useful the moment the meeting starts. During the call, a PM can't track 8 questions × 4 fields + 18 PBIs × 3 fields in their head, and typing into a separate notes app loses the spatial context (which field goes with which row). Fields in the prep doc itself are spatial — they carry meaning-by-placement. Auto-save-to-localStorage means the PM can close the tab, switch to the DevOps tab in another window, come back, and the captured data is still there. Export-to-text is the real payoff: the prep doc IS the draft of the minutes email after the meeting ends — a one-click bridge from prep → execution → minutes that was previously three manual steps. **How to apply:** For any meeting-day prep doc where the PM will capture answers / decisions / owners / dates during the call, add: (a) form fields on every actionable row with unique `data-field` ids and human-readable `data-label` for export; (b) a `data-capture-section` attribute on each `<tbody>` to group the export by section; (c) JS that on `input`/`change` saves to `localStorage` under a versioned key, toggles a `.filled` class for visual feedback, and updates a floating `.capture-bar` counter; (d) an Export modal that reads localStorage, groups by section, and renders plaintext with `📋 Copy to clipboard`; (e) a Clear button with `confirm()`. Field types: `<textarea rows="2">` for free-text answers; `<input type="text">` for names; `<input type="date">` for deadlines; `<select>` for status/decision enums. Pair with lesson #441's private-playbook pattern — this bridge technique applies specifically to personal PM prep docs, not stakeholder-facing HTMLs (where filling fields in place is inappropriate because stakeholders receive the artefact read-only). This is distinct from lessons #60 (localStorage snapshots for trend tracking) and #127 (persist in-memory data) — the novel contribution is the **export-to-text bridge** that makes the prep doc feed the next deliverable automatically.

## V3 Dashboard Build & Bilingual Display Polish (Apr 20, 2026 — continued)

443. **Never nest `<script>` tags inside another `<script>`; HTML5 terminates the outer block at the first inner `</script>`** — On 2026-04-20, while extending `Project Management Handling_v3.html` with an embedded snapshots feature, a helper Node script injected `<script type="application/json" id="portfolio-apr16-diff">{...}</script>` plus a follow-up `<script>...snapshot JS...</script>` *between* `var PORTFOLIO_NARRATIVES = {...};` and `function generateAutoText(...)`, both sitting inside an existing IIFE-wrapped outer `<script>`. The page rendered blank Portfolio cards. Playwright console surfaced `ReferenceError: PORTFOLIO_NARRATIVES is not defined`. Root cause: HTML5's "script data" parser is raw-text except it scans for the literal string `</script>` to close the block. My injected `</script>` terminated the OUTER script prematurely, leaving a stray `<script type="application/json">` opening tag as a JS syntax error inside the outer block — the whole outer script fails to parse, so `PORTFOLIO_NARRATIVES` never evaluates and every downstream consumer breaks. Fix was a follow-up Node script `_fix_snapshot_injection.js` that cut the injection from its bad position and reinserted it AFTER the outer script's closing `</script>` tag. **Why:** The `<script>` element's raw-text parsing is unintuitive — the HTML parser doesn't understand JS syntax, only the literal ASCII `</script>` string. This means any tool that mechanically inserts content into a large self-contained HTML must know where the outer script boundaries are, because nested `<script>` tags *look* fine visually but destroy the outer block. **How to apply:** (1) When writing a tool that injects scripts or embedded JSON into an existing HTML, first identify the enclosing outer `<script>` boundaries (find the preceding `<script>` tag and its matching `</script>`). Insert strictly outside those boundaries — typically just before `</body>`. (2) If you *must* embed within a string, escape every `</script>` as `<\/script>` (the backslash is ignored by JS but not by the HTML parser). (3) Verify with Playwright: navigate to the page, evaluate `typeof PORTFOLIO_NARRATIVES`. If the answer isn't `'object'`, there is a parse error you can't see without Chrome DevTools. See also KB entry `pm-html-infrastructure/SKILL.md` which flags this as a standing rule.

444. **When injecting code that consumes IIFE-scoped variables, add explicit `window.X = X;` exports at the end of the IIFE** — On 2026-04-20, after fixing the nested-script bug (lesson #443) by moving the snapshot injection outside the outer `<script>`, the page STILL threw `ReferenceError: PORTFOLIO_NARRATIVES is not defined` from the new snapshot code. The outer dashboard script was wrapped in `(function() { ... })();` (IIFE) starting at line 2427 and closing at line 7869, and `var PORTFOLIO_NARRATIVES = {...};` declared inside the IIFE was function-scoped — invisible to any `<script>` block sitting after line 7869. Resolution: inserted five `window.X = X;` lines just before the closing `})();` — `window.PORTFOLIO_NARRATIVES = PORTFOLIO_NARRATIVES;`, `window.loadPortfolioText = ...`, `window.loadPortfolioStatus = ...`, `window.buildPortfolioProjects = ...`, `window.dgT = ...`. After the page reload, Playwright confirmed `typeof window.PORTFOLIO_NARRATIVES === 'object'` and the snapshot feature rendered 27 project narratives correctly. **Why:** `var` at the top level of a `<script>` tag (no IIFE) creates a window global. Inside an IIFE, `var` is function-scoped — it's encapsulation working as intended, but also a scope barrier that new code added outside the IIFE cannot cross without an explicit bridge. The fix is trivial (5 lines), but the diagnosis takes a few minutes if you don't know to look for an IIFE boundary. **How to apply:** Before injecting any code into a single-file HTML dashboard that references an existing variable: (1) grep for `(function\s*\(\s*\)\s*\{` or `\}\)\s*\(\s*\)\s*;` to detect IIFE wrapping; (2) if found, add `window.X = X;` exports for every consumed var at the end of the IIFE; (3) note that functions already assigned as `window.X = function() {...}` ARE global — only bare `var`/`const`/`let`/`function` declarations need bridging; (4) if the codebase already exposes many helpers (like this dashboard did with `showToast`, `renderPortfolioStatus`, etc.), follow the same convention — don't rely on the scope closure alone.

445. **Watch for dual encoding in hand-written JS source: some strings use literal `—` while others use `\u2014` (6-char) escape** — On 2026-04-20, while refreshing `PORTFOLIO_NARRATIVES` narratives in v3, direct `Edit` tool replacements on the English fields kept failing with "String to replace not found" even though the string was visually present. `od -c` on the file revealed English strings were written as 6-char escapes `\`, `u`, `2`, `0`, `1`, `4` while Arabic strings in the same block used literal UTF-8 `—` em-dash. The Edit tool's `old_string` parameter passes through a JSON-style decoder that interprets `\u2014` as the single em-dash character — so my `old_string` contained a 1-char em-dash while the file had 6 ASCII chars. They don't match. Workaround: wrote a Node script (`_apply_apr20_refresh.js`) that composed replacement strings using template literals with `EM = '\\u2014'` (escaped backslash), used JS `indexOf` / `slice` to locate and replace each narrative block byte-exact. Node doesn't decode anything; template literals preserve bytes as typed. **Why:** File tools commonly have a JSON-layer escape interpretation between "what you typed in the tool" and "what lands in `old_string`". Files hand-written over years often mix literal Unicode chars with `\uXXXX` JS escapes depending on the keyboard used at the time — there is no guarantee of consistency even within one file. Mechanical find-and-replace silently fails when encoding assumptions mismatch. **How to apply:** (1) Before bulk-editing strings containing non-ASCII in an unfamiliar file, run `grep -c '\\u' FILE` and `grep -c '—' FILE` to detect whether the file uses escapes or literals — if BOTH have non-zero counts, you have mixed encoding. (2) For mixed-encoding files, prefer a small Node script over direct `Edit` — Node's template literals `\`...\${EM}...\`` with `EM = '\\u2014'` preserves bytes exactly. (3) After a replace, verify with `od -c FILE | grep 'u2014'` or similar so you know which form survived. (4) Prefer writing new narratives with only ONE form (usually literal UTF-8) to avoid perpetuating the mix.

446. **For fresh emails not yet in the CSV export, decode `.msg` files via PowerShell Outlook COM** — On 2026-04-20, the user referenced an Alaqraboon "on hold" letter sent Apr 19 at 17:07 from Mohamed Salah Basha to Mohammed Abdulla Al-Yafei + Dr. Amar Bendou. The standard Outlook CSV category files (`cat_khairgate.csv`, etc.) were last exported at Apr 16 13:57 and didn't contain anything after that timestamp. A Grep of `D:\` surfaced the exact `.msg` file at `D:\Alaqraboon\Email\الأقربون - تأجيل التنفيذ - مقترح كتاب موجه لسعادة الرئيس التنفيذي لقطر الخيرية.msg`. Decoded via: `powershell.exe -NoProfile -Command "[Console]::OutputEncoding = [System.Text.Encoding]::UTF8; $ol = New-Object -ComObject Outlook.Application; $msg = $ol.Session.OpenSharedItem('D:\\Alaqraboon\\Email\\...msg'); @('SUBJECT: ' + $msg.Subject, 'FROM: ' + $msg.SenderName, 'TO: ' + $msg.To, 'DATE: ' + $msg.SentOn, '---BODY---', $msg.Body) | Out-File -FilePath 'D:\\Alaqraboon\\Email\\_extracted_body.txt' -Encoding UTF8"`. Then `Read` on the UTF-8 text file gave full English metadata + full Arabic body text — a critical portfolio input (project ON HOLD) that reshaped the Alaqraboon Portfolio Status card. **Why:** Outlook CSV exports are point-in-time snapshots; any email arriving after the export is invisible to text-only tooling. Binary `.msg` files (OLE compound documents) are not plain-text greppable without a parser. PowerShell + Outlook COM gives native access to `Subject`, `SenderName`, `To`, `SentOn`, `Body`, `Attachments`, etc. with full Unicode support as long as you write to a UTF-8 file (the console codepage mangles Arabic to `?`). **How to apply:** For any PM analysis where the CSV cutoff is older than the user's question: (1) `Glob` / `Grep` the local filesystem for `.msg` files matching keywords; (2) extract with the PowerShell one-liner above, saving to a scratch `_extracted_body.txt`; (3) `Read` the text file; (4) clean up the scratch file when done. Requires Outlook installed on the local machine (normally true for PMs). Pair with lesson #436 (search local data before saying "not in KB") — a `.msg` file on disk is authoritative local data the KB doesn't see.

447. **Threshold-color the *number* itself, not just the card's border** — On 2026-04-20, user pointed out that the v3 SPI/CPI summary cards showed 0.94 and 0.74 in dark navy text even though the underlying `indicatorColor()` function existed and was being called — but only to set `el.style.borderLeftColor`. The big 32px value stayed the default dark color. Fix: after the two `borderLeftColor` assignments, added two more lines: `document.getElementById('exec-overall-spi').style.color = indicatorColor(tot.spi, 'spi');` and same for CPI. After reload, SPI 0.94 rendered green (≥0.80) and CPI 0.74 rendered red (<0.80) — instantly readable without reading the label. **Why:** Visual hierarchy on a dashboard follows a simple rule — the reader's eye lands on the largest, boldest, most-colored element first. If the color coding lives on the frame (border) instead of the focal content (number), stakeholders have to second-guess the color's meaning. A red left-border around a black "0.74" reads as ambiguous; a red "0.74" with a red left-border reads unambiguously as a red indicator. The fix is typically one or two lines per tile. **How to apply:** For every "traffic-light" indicator in a dashboard (SPI, CPI, % complete, budget ratio, risk score, health rollup): the visible number MUST take the threshold color, not the surrounding chrome. Border / background colors are secondary reinforcement. Periodically Grep the codebase for `style.borderLeftColor` or `.style.borderColor` and audit whether each also has a matching `.style.color` on the text inside the same element. Exception: if the number is part of a multi-column table row, color the cell background OR the number; keep it consistent across the column.

448. **The PM Snapshot audit-trail pattern — Save Snapshot button on the live tab + separate History tab with dated locked entries** — On 2026-04-20, user clarified a board-reporting need: "every bi-weekly review cycle needs its own frozen snapshot so I can reproduce what I showed the board on any past date". Solution shipped in v3: (a) a **📌 Save Snapshot** button in the Portfolio Status tab header that prompts for a label (default "Bi-Weekly Review — <today>") and appends a locked JSON entry to `localStorage.exec-portfolio-snapshots-v1`; (b) a new **📜 Project Status (History)** sidebar tab with a table of all snapshots (date · label · author · project count · locked/draft badge · View / Export / Delete actions); (c) two snapshots embedded in the HTML at build time via `<script type="application/json" id="portfolio-apr16-diff">{...}</script>` — one for the Apr 16 seed (preserved as a diff of the 5 refreshed projects' old text) + one for Apr 20 refresh (full PORTFOLIO_NARRATIVES) — so the HTML ships board-ready with zero configuration. View opens a read-only modal rendering all 27 project cards as they were at that snapshot date. **Why:** Without dated snapshots, every narrative refresh silently overwrites history — the PM cannot answer "what did we tell the board two weeks ago" without rummaging through old email threads. The board-review cycle IS the calendar of record; each cycle deserves a frozen baseline like PMI Earned Value uses. Embedded snapshots (in the HTML itself) beat external JSON files for this use case because emailing the HTML = emailing the full audit trail in one file. localStorage is the right target for in-session saves because the dashboard is file://, so it survives page reloads but not device switches — a separate **Export All Snapshots** button covers the cross-device case. **How to apply:** For any stakeholder-facing narrative dashboard that will be reviewed on a recurring cadence (bi-weekly, monthly, quarterly): (1) add a prominent Save button on the editing tab — tied to the review cycle, not to every DevOps fetch; (2) add a separate History tab showing a dated table of saved snapshots with View / Export actions; (3) embed existing snapshots as `<script type="application/json">` blocks in the HTML so new deployments inherit the history; (4) treat published snapshots as immutable (read-only, no edit) — corrections go in the NEXT snapshot with a "Correction from <prev date>" note; (5) the snapshot object shape is `{date, label, publishedBy, publishedAt, locked:true, narratives: {...}, statuses: {...}, metricsAtPublish?}` — `metricsAtPublish` captures live numbers at publish time so the compare view can show deltas even after DevOps data moves on.

449. **Use real Unicode characters in translation values, never HTML entities (`&Sigma;`, `&mdash;`, `&times;`)** — On 2026-04-20, v3's new EVM glossary in AR mode rendered `&Sigma; OriginalEstimate` on-screen literally. The i18n engine's `applyLang()` walker does `el.textContent = T[key]`, and `textContent` sets raw text — it does NOT decode HTML entities. Fix: replaced `&Sigma;` in the three `evm-ev-def` / `evm-pv-def` / `evm-ac-def` translation values with the actual Unicode character `Σ` (U+03A3, `\u03a3` in escaped form). After reload, AR rendered `(Σ OriginalEstimate حيث الحالة Done/Closed)` correctly. **Why:** HTML entities are an HTML-parser-layer concern. When an i18n framework bypasses the HTML parser (by setting `textContent` or `innerText`), entities stay as literal ampersand-sequences. The same issue bites `innerHTML`-based frameworks too if the entity appears in an attribute or data field that later gets `textContent`'d. Translation dictionary values are *data*; HTML entities are a *presentation* layer. Mixing them creates a presentation assumption that breaks the moment the framework changes its rendering path. **How to apply:** (1) When writing translation values (either EN or AR or any language), always use real Unicode characters: `Σ` not `&Sigma;`, `—` not `&mdash;`, `×` not `&times;`, `→` not `&rarr;`, `≥` not `&ge;`, etc. (2) When auditing existing translation dictionaries, Grep for `&[a-zA-Z]+;` inside string values and replace each with its Unicode equivalent. (3) HTML entities in the HTML *structure* (outside translation values) are fine — they get parsed by the HTML parser normally. The rule is scoped to values that will pass through `textContent`. (4) This is an extension of lesson #437 (no HTML tags in `data-i18n` values) — same root cause, different symptom.

450. **Always provide the full name of an acronym in the glossary, with the acronym in parentheses** — On 2026-04-20, user reviewed v3's "How to read" legend and flagged: "Earned Value Management - EVM, full name, not shortcut, for all". Original glossary led with acronyms: `EV (Earned Value) — ...`, `PV (Planned Value) — ...`. Revised to lead with full names: `Earned Value (EV) — ...`, `Planned Value (PV) — ...`, `Actual Cost (AC) — ...`. Section title changed from "EVM Glossary" to "Earned Value Management (EVM) Glossary". The in-legend SPI/CPI formulas changed from `SPI = EV / PV` to `Schedule Performance Index (SPI) = Earned Value (EV) / Planned Value (PV)`. Same pattern applied in AR: `مؤشر أداء الجدول الزمني (SPI) = القيمة المُكتسَبة (EV) / القيمة المخطّطة (PV)`. The top-of-page summary cards stayed compact (`SPI = EV / PV`) because space is tight — only the glossary block where the user reads for context gets the full expansion. **Why:** Board members and cross-functional stakeholders (finance, legal, HR) rarely memorise PM / EVM acronyms. When they see "SPI 0.94" on a dashboard, they glance at the legend to confirm what SPI measures. A legend that says "SPI = EV / PV" recursively offloads the interpretation cost — now the reader has to resolve three acronyms instead of one. Leading with the full name cuts the chain to one step. The compact cards preserve fast scanning; the glossary preserves correctness. **How to apply:** (1) Every glossary section leads with **Full Name (Acronym)** per row — never just the acronym alone. (2) The section *title* follows the same pattern (`Earned Value Management (EVM) Glossary`, not `EVM Glossary`). (3) In-context formulas in the glossary expand the acronyms: `Schedule Performance Index (SPI) = Earned Value (EV) / Planned Value (PV)`. (4) Summary tiles, table column headers, and sparkline tooltips stay compact (acronym only) — the glossary carries the expansion. (5) In bilingual dashboards, the Arabic glossary row includes both Arabic full name AND the English acronym — e.g., `القيمة المُكتسَبة — Earned Value (EV)` — so the reader can cross-reference literature in either language.

451. **In RTL dashboards, do NOT override the flex header with `flex-direction: row-reverse`; it cancels the natural RTL flip** — On 2026-04-20, user showed a screenshot of v3 in Arabic mode with the title/subtitle at the left and the user-info card (Mahmoud's name, company, last-updated) at the right — opposite of what the Arabic reading direction expects (title at right = reading start). The culprit was a single CSS rule: `html[dir="rtl"] .header { flex-direction: row-reverse; }`. The intent had been "flip the header for RTL", but the browser's natural RTL rendering already flips flex children — adding `row-reverse` flips them back to LTR order. Removing the rule, adding `text-align: right` on the title block and flipping the accent bar from `border-left` to `border-right` produced the correct layout: title on right (Arabic reading start), user-info on left (visual end). Playwright confirmed `titleBlockLeft=337, userInfoBlockLeft=28` in AR mode. **Why:** Flexbox in RTL mode already mirrors children visually when `dir="rtl"` is set on an ancestor — first child goes to the right, last child goes to the left. Applying `flex-direction: row-reverse` on top of this cancels the flip (two negatives = positive), which is almost never what you want. This is a common reflex error: developers who think "RTL ⇒ I need to reverse" reach for `row-reverse` when the browser has already done the work. **How to apply:** (1) For every flex container styled inside `html[dir="rtl"]`, remove `flex-direction: row-reverse` unless you have explicitly tested that the default RTL flip goes the WRONG way for your use case (rare). (2) Handle text alignment and border accents separately: inside `html[dir="rtl"]`, add `text-align: right` (or `text-align: start` for direction-agnostic styling) on text-heavy children, and flip `border-left` to `border-right` + swap `padding-left`/`padding-right`. (3) For vertical alignment in a flex column, `align-items: flex-end` flips automatically in RTL (flex-end becomes left), so usually no override is needed. (4) Verify with Playwright: navigate, switch to AR, call `document.querySelector('.header').children[0].getBoundingClientRect()` — the first child's `left` should be > the last child's `left` in RTL. (5) Related to lesson #437 (no HTML tags inside translation values) and lesson #438 (ISO timestamps, render-time locale) — all three are part of the bilingual-dashboard discipline that must be audited together.

452. **Audit `localStorage` keys before answering "will this button erase my data?"** — On 2026-04-20, early in the session, the user asked: "if I click Fetch from DevOps, will the data in Portfolio Status be removed?" Rather than guessing, Grep'd the code for every `localStorage.setItem` / `removeItem` call and produced a clean answer: Fetch from DevOps writes to `okr-kpi-*` and `okr-data-gate-snapshot` keys; Portfolio Status uses an independent `exec-portfolio-text-v1` + `exec-portfolio-status-v1` pair; there is zero overlap, so the user's Where/Next/Risk edits survive any Fetch. Even the Clear button's reach was traced — it only wipes `STORAGE_KEY` + `FETCH_TS_KEY`, leaving Portfolio Status edits intact. **Why:** "Will this action destroy my work?" is one of the most common and highest-anxiety questions a PM asks about their own dashboard. A wrong answer ("yes, it'll overwrite") scares the user into avoiding features they need; a wrong answer ("no, it's safe") can cost them hours of typed-in narrative if it turns out to be false. The answer is always discoverable by tracing storage keys — feature boundaries typically align with storage-key boundaries because each feature was built by someone who namespaced their own state. Give a precise, trace-based answer, not a reassurance. **How to apply:** When asked whether action A will mutate data B, (1) Grep for `localStorage.setItem` / `sessionStorage.setItem` / `indexedDB` writes inside A's handler; (2) compare the key names to the storage keys that hold B; (3) if no overlap, answer "no" with the key names as proof; (4) if overlap exists, explain exactly which fields get overwritten and suggest an export-first workflow. Use this same pattern for any "does X affect Y" question about shared state — cache clears, bulk operations, auth resets.

453. **Keep historical KR / metric queries pinned to the historical roster — don't let team reassignments rewrite past performance** — On 2026-04-20, moving Houssem Ben Mbarek + Muram Makawi Abuzaid from Qatar team → Backend team (v2 edit) touched six code sites: rendered rows, `TEAM_ROSTER`, `ASSIGNEE_TO_TEAM`, `teamDefs` in `fetchMetrics`, `memberDefs`, and `teamInfoMap`. All six followed the new roster. BUT the `qatarAllTimeDone` array (used for KR3.1 "Qatar members with 10+ done items") was deliberately LEFT pointing at `['Houssem', 'Muram', 'Hacene']` — the three members who were on Qatar during the historical window the KR measures. If `qatarAllTimeDone` had also been rewritten to just `['Hacene']`, the KR would drop from a potential 3/3 to a permanent 1/3 — making the team look like it underperformed when in reality it was just restructured. This insight was noted in the CLAUDE.md v2 entry as a deliberate choice. **Why:** KRs and retrospective metrics measure performance within a time window. Changing team membership today should not retroactively rewrite who "counted" yesterday. Coupling the metric's member list to the current roster introduces survivorship / restructuring bias into historical comparisons — stakeholders lose the ability to compare Q1 velocity to Q2 velocity if Q1's team members are no longer associated with their historical team in the metric code. **How to apply:** (1) When refactoring team / assignment maps, separate "current roster" (used for live velocity, capacity, current-sprint rollups) from "historical rosters" (used for KRs, trend lines, performance reports). (2) Give the historical lists explicit names like `qatarAllTimeDone` (suffix `AllTime` = pinned) to make the intent obvious to the next maintainer. (3) When a team restructure happens mid-quarter, consider whether any KR thresholds need adjustment (e.g., Qatar KR3.1 target was 3/3 — with 1 person left, target may need to move to 1/1 or the KR may need to be retired). Flag this as a PM decision, not an automatic code sync. (4) Document the split in the file's top comment or README so future refactorers don't "helpfully" unify the two lists.

454. **`localStorage` is per-file-origin on `file://` URLs — user edits in v2 don't automatically transfer to v3** — On 2026-04-20, cutting `Project Management Handling_v3.html` from v2 raised the question "how do I preserve the user's typed-in narrative edits from v2?" Answer: you can't, automatically. Browsers treat each `file://` path as its own security origin — `file:///d:/.../v2.html` and `file:///d:/.../v3.html` have separate `localStorage` sandboxes that cannot see each other. The user's typed-in Where/Next/Risk overrides in v2 stay locked to v2's origin. Workaround options evaluated: (a) pre-flight DevTools snippet the user runs in v2 to export to clipboard, pasted back into chat; (b) temporary "Export" button added to v2 that dumps state to a file; (c) build v3 with an "Import Legacy v2 State" input panel accepting the JSON; (d) skip migration entirely if the user hasn't edited anything. Chose (d) for this session because user confirmed the visible text was still the Apr 16 seed. **Why:** Many PMs assume "it's the same HTML folder, so the data flows". That assumption holds only if both files share the SAME origin — which they don't, by browser design. This is a hidden cost of the "new version every update" rule: each version starts with an empty localStorage for that specific file path. For small changes (display-only polish), in-place edits to the existing file preserve user data with zero migration. For cross-day / major-scope changes, accept that user data is orphaned in the old file and build a migration step. **How to apply:** (1) Before cutting a new versioned file, run a pre-flight check: does the user have localStorage data in the old version that they care about? (2) If yes, provide at least one of: a DevTools snippet to copy to clipboard, an "Export" button added to the old file, or an "Import" panel in the new file. (3) If the change is display-only (no schema change), prefer in-place edits — see lesson #458. (4) Document the origin-per-file behavior in the dashboard's own README/help tooltip so users aren't surprised. (5) Consider serving files from `localhost:PORT` (via a persistent Node server per lesson #455) so all versions share the same origin — a bigger architectural commitment but eliminates the migration problem entirely.

455. **Playwright blocks `file://` by default — spin up a Node one-liner HTTP server on a throwaway port to verify local HTMLs** — On 2026-04-20, attempting to navigate Playwright to `file:///.../Project Management Handling_v3.html` returned `Error: Access to "file:" protocol is blocked`. Chrome's security default disables `file://` for automated browsers. Workaround: `node -e "const http=require('http'),fs=require('fs'),path=require('path');const root=__dirname;http.createServer((req,res)=>{let f=path.join(root,decodeURIComponent(req.url.split('?')[0]));if(f.endsWith(path.sep))f=path.join(f,'index.html');fs.readFile(f,(e,d)=>{if(e){res.statusCode=404;res.end('404');return;}const ext=path.extname(f).toLowerCase();const ct={'html':'text/html;charset=utf-8','js':'application/javascript','json':'application/json','css':'text/css'}[ext.slice(1)]||'application/octet-stream';res.setHeader('Content-Type',ct);res.end(d);});}).listen(5099,()=>console.log('on :5099'));"` — run as a background process, then Playwright navigates to `http://localhost:5099/v3.html`. **Why:** Playwright and other headless-browser tools disable `file://` to reduce local-file-exfiltration attacks. The fix isn't a Playwright flag (most environments don't expose `--allow-file-access-from-files`) — it's serving the file over HTTP. A one-liner Node server is zero-install (Node is standard on PM workstations) and throwaway (different port for each session, killed with Ctrl+C). **How to apply:** (1) For any Playwright verification of a local HTML file, start the Node one-liner above in the background; pick a port unlikely to collide (5099 is free on most PM workstations per CLAUDE.md's enumeration of 5050-5053). (2) Navigate Playwright to `http://localhost:PORT/filename.html`. (3) Note that the localhost-HTTP origin will have EMPTY `localStorage` vs the user's `file://` localStorage — the two are different origins (see lesson #454). Use this for render verification and JS behavior testing; don't use it to poke at the user's actual localStorage state. (4) Kill the server when done (`kill %1` in the same shell, or let the session close). (5) If the file has external asset references (CSS, JS imports), make sure the Node server serves those too — the one-liner above does via the root-dir static serve.

456. **When a `<span data-i18n="key">text</span>` has trailing text AFTER the span in the same parent, that trailing text is invisible to the i18n engine** — On 2026-04-20, user spotted a legend line that read (in Arabic mode) `SPI = نسبة اكتمال النطاق (...). 1.0 = half complete = all scope closed.` — half translated, half English. The HTML source was `<div><strong>SPI</strong> = <span data-i18n="spi-formula">...</span>. 1.0 = all scope closed; 0.5 = half complete.</div>`. The `spi-formula` key translated correctly; but the period + rest (" 1.0 = all scope closed; 0.5 = half complete.") sits as a RAW text node AFTER the span in the same parent `<div>`. The i18n walker only touches elements with `data-i18n` attributes — bare text nodes between/after tagged spans are invisible. Fix: wrap the trailing text in a new `<span data-i18n="spi-values-legend">...</span>` and add the AR translation to the T dictionary. **Why:** Any i18n framework built on `data-i18n` attribute selectors has this blind spot. The fix is trivial per occurrence, but the drift accumulates across long-lived bilingual pages because contributors add "one more clause" inline to an existing translated sentence without wrapping it. **How to apply:** (1) Periodic audit pattern: Grep for `</span>[.]` or `</span>\s[A-Za-z0-9]` or `</span>\s*\([A-Z]` to catch trailing English after a `data-i18n` span. (2) Stricter rule: inside any element that contains at least one `data-i18n` child span, every text fragment must also be wrapped in a `data-i18n`-bearing element — no bare text nodes mixed with translated spans. (3) When adding a clause to an existing translated sentence, wrap it in a new span rather than appending raw text. (4) Consider adding a lint script that walks the DOM and flags any element containing both a `data-i18n` descendant AND a non-whitespace text node at the same level. (5) Related to lesson #437 (no HTML tags inside translation values) and lesson #449 (no HTML entities in translation values) — together these three lessons form the "i18n-clean data contract" for bilingual dashboards.

457. **Translate full noun phrases precisely: "Schedule" (الجدول) ≠ "Schedule Performance Index" (مؤشر أداء الجدول الزمني) — maintain a bilingual PM vocabulary** — On 2026-04-20, user caught that the AR translation for SPI said "مؤشر أداء الجدول" when the correct PM-literature form is "مؤشر أداء الجدول الزمني" — the "الزمني" (temporal / time-based) qualifier distinguishes "schedule" (as in time schedule) from "الجدول" (which alone could mean a data table). Without "الزمني", Arabic-speaking board members parse the term ambiguously. Two translation keys needed fixing: `'Schedule Performance Index'` → "مؤشر أداء الجدول الزمني" and `'spi-formula-title'` → "مؤشر أداء الجدول الزمني (SPI) = ...". CPI's "مؤشر أداء التكلفة" was already correct because "التكلفة" (cost) is unambiguous. **Why:** Literal first-word translations of English compound terms often drop context-essential qualifiers that the full phrase carries. PM / EVM / Agile vocabulary is especially prone to this because terms like "Schedule", "Burndown", "Velocity", "Story Point" carry discipline-specific meaning that single-word Arabic translations don't preserve. The cost of a wrong translation isn't just linguistic — it's stakeholder confusion during board review. **How to apply:** (1) Maintain a bilingual PM vocabulary reference — a markdown file mapping each PM/EVM/Agile term to its Arabic industry-standard translation (not a machine-translated word-for-word). Seed entries: SPI = مؤشر أداء الجدول الزمني; CPI = مؤشر أداء التكلفة; EV = القيمة المُكتسَبة; PV = القيمة المخطّطة; AC = التكلفة الفعلية; Sprint = سبرنت (transliterated) or تكرار (iteration); Velocity = سرعة الفريق; Burndown = الاحتراق التدريجي; KR = نتيجة رئيسية; Objective = هدف. (2) When adding a new bilingual string, look up the term in the vocabulary first — don't invent a translation on the fly. (3) For acronyms, keep the English acronym in parentheses alongside the Arabic full name (see lesson #450): `مؤشر أداء الجدول الزمني (SPI)`. (4) Periodically run a "PM jargon audit" — grep for known terms and verify every translation uses the agreed form. (5) Crosscheck with RAG results from PM²-Agile / PMP / PMBOK Arabic editions when available.

458. **Pragmatic in-place edits are allowed for same-day polish; version bumps are for cross-day or major-scope changes — set a "polish window" exception to the versioning rule** — On 2026-04-20, v3 shipped with the Portfolio refresh + snapshot feature. Over the next few hours, v3 absorbed ~10 follow-up fixes in-place without becoming v4/v5/v6/...: (a) untranslated suffix wrapping; (b) Σ entity fix; (c) number-color on SPI/CPI cards; (d) EVM terminology in cards + legend; (e) full acronym names per user feedback; (f) "الزمني" Arabic correction; (g) RTL header flex fix; (h) border-left → border-right accent bar; (i) alignment corrections. If each had triggered a new version file, the folder would now hold v3 through v12 within a single calendar day — unreadable sprawl. Decision: within a "polish window" (same calendar day, display-only changes, no schema/data/contract changes), edit in place; the standing "new version every update" rule kicks in for the NEXT cross-day change. **Why:** Versioning is a governance tool for traceability, not a mechanical rule for every keystroke. The real signal a version bump carries is "this is a distinct state worth referring back to". Ten incremental typo/style fixes in the same afternoon do NOT produce ten distinct states worth referring back to — they produce one polished end-of-day state. Mechanical versioning degrades the signal-to-noise ratio of the version log. **How to apply:** Define a polish-window exception to the "new version on every update" rule: **in-place edits are permitted when ALL of these hold** — (1) same calendar day as the version's original creation; (2) display-only changes (text, colors, translations, CSS) — no new data fields, no schema changes, no new localStorage keys, no new API calls; (3) no content changes that stakeholders would want to reference separately ("show me v3 before the EVM fix" is a sign you should've bumped to v4). Outside this window — cross-day changes, any data/schema/contract change, any content stakeholders might want to pin — bump the version per the standing rule. Document the polish-window edits as a dated CHANGELOG paragraph inside the HTML's top comment or the companion CLAUDE.md entry so traceability is preserved without file sprawl.

## Cadence Migration, i18n Edge Cases & CWD-Lock Workarounds (Apr 20, 2026 — continued)

459. **Audit BOTH literal-Unicode AND `\uXXXX` escape forms when bulk-replacing non-ASCII text in a single file** — On 2026-04-20, migrating the PM Handling dashboard from bi-weekly to weekly cadence required replacing Arabic phrases like "نصف الشهرية" → "الأسبوعية" and "3 مايو" → "26 أبريل". A single JS file mixed both forms: `PORTFOLIO_NARRATIVES` entries used literal Unicode chars (the user typed them directly), while the T i18n dictionary used `\u0645\u0627\u064a\u0648`-style escape sequences (copy-pasted from older code generators). My first Node pass with literal-Arabic replacements caught ~70% of occurrences; the remaining ~30% needed a second pass targeting the escape form explicitly. Took 3 passes + 1 manual edit to reach 0 residual. **Why:** File tools see bytes, not characters. The literal-char form `\u064a\u0648` as Unicode scalar U+064A U+0648 (a 4-byte UTF-8 sequence) is COMPLETELY different from the ASCII escape sequence `\` + `u` + `0` + `6` + `4` + `A` + `u` + `0` + `6` + `4` + `8` (11 ASCII bytes). A string-replace targeting one form silently skips the other. Long-lived codebases accumulate both forms because different contributors/tools wrote them differently. **How to apply:** (1) Pre-flight grep: `grep -c "LITERAL_ARABIC_WORD" FILE` AND `grep -cE "\\\\u[0-9a-f]{4}.*\\\\u[0-9a-f]{4}" FILE` — if both non-zero, the file has mixed encoding. (2) For bulk rename, write TWO replacement tables: one for literal chars, one for `\u`-escape form. (3) Run both passes. (4) Final grep for residuals using the escape form of each target word (e.g. `\u0645\u0627\u064a\u0648` for مايو). (5) If you're generating new content, prefer ONE form (literal UTF-8 is more human-readable) and don't perpetuate the mix.

460. **Arabic (and other Semitic languages) gender/state inflections demand their own replacement rules — don't assume one form covers all** — On 2026-04-20, my cadence migration replacement table handled the feminine definite form `نصف الشهرية` but missed two other forms that appeared in the same file: the masculine definite `نصف الشهري` (no ة on the adjective — because "اجتماع" is masculine and agreement requires a masculine adjective) and the indefinite feminine `نصف شهرية` (without the definite article ال). A roadmap-footer sentence reading "يتم مراجعة التقدم في اجتماع 3 مايو نصف الشهري" (progress is reviewed in the May 3 bi-weekly meeting) survived both automated passes because the adjective was masculine (`الشهري` without ة) — my pattern was `نصف الشهرية` with ة. Required a targeted manual edit. **Why:** Semitic-language adjectives inflect for gender (masc/fem), state (definite/indefinite), number (singular/dual/plural), and case (nominative/accusative/genitive — in classical Arabic). A single English word like "bi-weekly" can correspond to 4+ Arabic surface forms depending on what noun it modifies: `نصف شهري / نصف شهرية / نصف الشهري / نصف الشهرية / نصف شهريان / نصف الشهريان / ...` Naive string-replace hits one form and leaves the rest. The same class of issue exists for Hebrew, French, German, Spanish (adjective agreement), Russian (case endings), etc. **How to apply:** (1) Before writing a bulk-rename script, grep the file for the ROOT noun (e.g., `شهري`, `أسبوعي`) not the full phrase — list every inflected form you find. (2) Build the replacement table with one pair per inflection: `(نصف الشهرية, الأسبوعية)`, `(نصف الشهري, الأسبوعي)`, `(نصف شهرية, أسبوعية)`, `(نصف شهري, أسبوعي)`. (3) After the automated pass, grep for the root noun one more time — any survivors are forms you missed. (4) For content PMs write themselves, keep an internal glossary of the forms each acronym takes (see lesson #457 on the bilingual PM vocabulary reference).

461. **Phrase-level replacement tables must include disambiguating context — a bare date or number match across hundreds of files corrupts calendar-week markers** — On 2026-04-20, the bi-weekly→weekly migration needed to change the next-review-target date "May 3" → "Apr 26". The v3 HTML contains "May 3" in 13 places, but only 11 were deadline references to update. The remaining 2 were calendar-week timeline rows: `<tr>W18 Apr 27 – May 3 Weekly update</tr>` and `<tr>W21–W22 May 18 – May 31 Weekly updates</tr>`. A bare `'May 3' → 'Apr 26'` find-and-replace would corrupt the W18 week into a nonsense `Apr 27 – Apr 26` span. Fix: every entry in the replacement table included disambiguating context — `'by May 3 review'`, `'by May 3.'`, `'Target ≥ 60% by May 3'`, `'Sunday May 3'`, `'May 3 bi-weekly review'` — never bare `'May 3'`. **Why:** A date string like "May 3" is semantically overloaded: it can be a deadline, a calendar week endpoint, a historical event marker, a meeting name, a sprint close, a milestone. Each semantic needs a different fate during bulk migration. The context before/after the date disambiguates. Naive global find-and-replace is the second-easiest way to silently corrupt data (after accidentally deleting something). **How to apply:** (1) Before writing any bulk-rename rule for a date, number, acronym, or name, grep the file for ALL occurrences of the bare string and categorize them by surrounding context. (2) Write one replacement pair per context ("by X review" vs "W## X – Y" vs "X Sprint start"), never a bare pair. (3) After the pass, grep for bare occurrences of the original — residual hits should all be false positives (leave-alone cases) or clear misses worth a manual review. (4) For calendar-week vs deadline disambiguation specifically: week markers follow the pattern `W## <date> – <date>` or `Week N (<date>)`; deadlines follow `by <date>`, `<date> review`, `<date> sprint close`. Grep for both patterns to pre-classify. (5) This is a stronger form of lesson #445 (dual-encoding audit) applied to semantic ambiguity instead of byte ambiguity.

462. **When renaming an i18n key, update THREE locations in one atomic pass: the HTML `data-i18n` attribute, the dictionary lookup key, and the translation value — drift between any two breaks the render** — On 2026-04-20, changing the next-review date broke multiple i18n lookups because I updated the visible English text (between `<span data-i18n="...">...</span>` tags) but forgot to update the `data-i18n` attribute VALUE (the lookup key) OR the dictionary entry KEY string. Result when toggling AR: the browser looked up `T['Estimation Accuracy from SBI tab ≥ 60% by Apr 26 review']`, found no match (the dictionary still had `T['Estimation Accuracy from SBI tab ≥ 60% by May 3 review']`), and fell back to showing the EN text verbatim — looking "not translated" instead of translated. Also saw the opposite symptom: dictionary KEY was updated but the AR VALUE still contained the old date — AR mode then showed an Arabic string with the WRONG date. Took 3 passes to align all three. **Why:** The i18n contract is a three-way identity: `<span data-i18n="KEY">EN_TEXT</span>` PLUS `T[KEY] = AR_TEXT`. If any one of (EN_TEXT, KEY, AR_TEXT) changes without the other two, the contract breaks. Most UI i18n frameworks have a "key-first" pattern where translations are looked up by key, so dictionary key and HTML attribute MUST match byte-for-byte; the EN visible text is somewhat decoupled but visually should match the key for consistency. **How to apply:** (1) Treat the three locations as one atomic unit in any find-and-replace script. For each rename, produce three rules: one targeting the HTML attribute value (`data-i18n="old"` → `data-i18n="new"`), one targeting the inner EN text, one targeting the dictionary key (`'old':` → `'new':`). (2) After the pass, grep for the OLD form across all three contexts — if any survive, the rename is incomplete. (3) Verify via Playwright: toggle to AR and confirm the AR text appears (not the EN fallback). If the EN text shows in AR mode, the key/attribute/dictionary-key don't match. (4) Also update the AR VALUE in the dictionary if it contained the old date/term — the AR text has its own domain-specific strings that need updating in parallel.

463. **Cadence changes ripple across 8+ artifact classes simultaneously — audit the full list before starting, not after** — On 2026-04-20, switching the PM Handling review cadence from bi-weekly to weekly touched 8 files plus 1 folder with a total of 124 text replacements, across 4 automated passes + manual fixes. Artifact classes involved: (1) filesystem folder names ("Bi-Weekly Project Reviews" → "Weekly Project Reviews"); (2) dashboard UI labels (`Weekly Review Charter`, button tooltips); (3) default dynamic values (`saveSnapshot()` prompt default); (4) embedded historical snapshot labels (Apr 20 embedded snapshot); (5) narrative commitment dates inside portfolio cards ("First calibration check at May 3 bi-weekly review" → "Apr 26 weekly review"); (6) companion HTML documents (Sunday Talking Points throughout); (7) markdown source docs (`_portfolio_narratives.md` section headers + index); (8) CLAUDE.md governance/context. Had I missed any one class, the dashboard would ship inconsistent — "Weekly Review Charter" with a "next review May 3" inside it. **Why:** A cadence change is really a policy/governance change. Policy changes ripple through every policy-derived artifact. In a PM/dashboard ecosystem, that's filesystem layout + UI text + default values + historical snapshots + narratives + companion docs + governance files. Humans undercount this reliably — they think "change the Charter card" and miss the other 7 classes. **How to apply:** (1) Before starting a cadence (or any policy) change, enumerate every artifact class that carries the policy: folder names, dashboard UI labels, default values, historical labels, narrative text, companion docs, governance files, schedule automation (Task Scheduler, cron). (2) For each class, list specific files and run an initial grep for the old term. (3) Build one replacement script that handles ALL classes in one pass rather than walking class-by-class — this avoids the "I forgot that file" trap. (4) Verify with a comprehensive grep for the OLD term at the end — zero residual hits across all classes means the migration is complete. (5) Apply to: cadence changes (bi-weekly↔weekly), version bumps (Q2→Q3), role restructures (team member moves), terminology changes (acronym renames), scope changes (adding/removing a project from the portfolio).

464. **Windows refuses to rename or move a folder if any process's current-working-directory (CWD) is set to that folder OR any subfolder within it — work around by moving CONTENTS rather than the folder itself** — On 2026-04-20, attempting to archive `D:\Project Management\Project management Quarter Plan\Projects Handling Review Meeting 6-4-2026\` with `mv` returned `Device or resource busy` even after killing all `node.exe` HTTP server processes holding the folder open. Root cause: the current Claude Code session had this folder set as its CWD (from the original VSCode workspace-open), and Windows NTFS holds a handle on the CWD of every running process. Even an in-place rename was blocked. Workaround that worked: create the archive target, then MOVE each TOP-LEVEL ITEM out of the folder one-by-one, leaving only the tool-state subfolders (`.claude/`, `.playwright-mcp/`) that the session was actively using. Result: 35 items relocated, folder shell remains with 2 hidden tool-state dirs, clean separation between "archived content" and "active session state". **Why:** Windows NTFS differs from POSIX filesystems in how it handles folder locks. On Linux/macOS, an inode's path is rename-safe even when a process has CWD there — the process just continues holding the OLD path via its inode reference. On Windows, NTFS locks folders that are any process's CWD, and the rename fails. This affects `mv`, `ren`, `robocopy /MOVE`, `PowerShell Rename-Item`, and even `rmdir`. Subprocess trees inherit the parent's CWD, so spawning `bash` or `powershell` from a shell rooted in the folder compounds the issue — every child process also has a lock. **How to apply:** (1) Before attempting to move/rename a folder that's currently open in any editor/IDE/terminal/debugger, identify which processes have it as their CWD. `handle.exe -p <pid>` (Sysinternals) or `PowerShell Get-Process | Where-Object { $_.Path -like "*folder*" }`. (2) If you CAN close those processes (plain terminals, build watchers), close them and retry. (3) If you CAN'T (your own editor session's state folder), move CONTENTS instead of the folder. Use a loop: for each top-level item, `mv item TARGET/`. Skip hidden tool-state subfolders like `.git`, `.claude`, `.playwright-mcp`, `.vscode`, `node_modules` only if they belong to the active session. (4) After the content move, the empty shell can be renamed/deleted from a DIFFERENT session (one whose CWD is elsewhere). (5) For automation scripts that MUST rename folders, set `cd /` or `cd ~` at the start so the script's own CWD isn't inside the target. (6) For VSCode + Claude Code specifically: the session's workspace root is the CWD — move content only, delete the shell from a fresh session opened elsewhere.

## Team Directory Drift & Retroactive Propagation (Apr 21, 2026)

467. **When a user confirms an unmapped team member's role, propagate the fact across ALL registry files in one batch — and close any matching 'Current Gap Identified' flag** — On 2026-04-21, user confirmed Afthab P A (afthab@taqatechno.com) is a QC engineer dedicated to the KhairGate Wallet project. Before confirmation, his name appeared ONLY in Azure DevOps work items (PBI #3986 + Tasks #3987/#3988/#3989 KT Sessions + Bug #4293) — none of the downstream registries knew about him: Department Workflows V1-V4 role tables, DevOps_Teams.md, OKR & KPI Dashboard v7 team roster, Project Management Handling_v3 Team Velocity tab, and Azure DevOps Access Upgrade v6 roster. Worse, Department Workflows V4 (Feb 2, 2026) still carried a "Current Gap Identified: QA-QC team consists of only 1 member handling all testing across multiple projects. A Senior QC Engineer hiring has been approved (target: by end of March 2026)." This gap flag was stale for ≥19 days because Afthab's Apr 2 onboarding had already closed it — but the stakeholder reports kept showing the gap. The one-shot propagation bundle: (a) memory file + MEMORY.md index entry; (b) DevOps_Teams.md row 11; (c) Department Workflows V5 folder with updated QA-QC section + gap-closure note replacing the stale gap callout; (d) OKR Dashboard v8 with member count 18→19 + QA-QC team card 1 member→2 members + Team Pulse table row + 3 JS arrays (fetchMetrics prefix, member object array, teamInfoMap); (e) Project Management Handling_v4 with same QA-QC structural updates; (f) Access Upgrade v7 adding Afthab to Batch 1 with Basic+Test Plans license (same tier as Mostafa, because QC engineers require Test Plans management — NOT the default Basic tier) with cost update $142→$194/mo ($1,704→$2,328/yr); (g) this lesson. Key sub-lesson: when adding a second member to a role that already exists on the team, match the existing member's license tier and batch placement by default — don't slot the new hire into the cheapest tier just because they are a "new" line item. Otherwise the stakeholder report produces a misleading cost figure (would have been $148/mo, actual $194/mo — a 31% undercount). **Why:** Team rosters are the skeleton that every stakeholder-facing dashboard and report hangs off. Stale gap callouts in the rosters cause downstream reports to tell the wrong story (e.g. "QA-QC is still understaffed, needs urgent hire" when the hire is already onboarded and 3 weeks into KT). The cost of fragmented propagation is cumulative: every report generated between the gap-closure-in-reality and gap-closure-in-documentation carries the wrong message. Propagating all six files in a single batch — triggered by the single moment when a role is confirmed — prevents downstream drift for the rest of the quarter. **How to apply:** When a user confirms a team member's role for the first time (phrases like "his role is X", "she's a Y", "add him to the Z team"), run the 6-file propagation bundle in one batch: (1) save a canonical memory file and update MEMORY.md index; (2) update DevOps_Teams.md (the team directory); (3) create the next numbered version of Department Workflows and close any matching "Current Gap Identified" note in the relevant department section; (4) create the next numbered version of OKR & KPI Dashboard with roster count and team card updates (member count in header badge, team-card-header "N members" label, Team Pulse table row, 3 JS arrays: fetchMetrics `members`, memberDefs `name/key` objects, teamInfoMap `members/keys`); (5) create the next numbered version of Project Management Handling (same 5 update points as OKR); (6) create the next numbered version of Access Upgrade with user added to the appropriate batch + cost totals updated + V{N} changelog row added. ALSO: (7) ensure an empty role (former member, QA-QC single-member, backend 1-person team) gap flag that is now resolved is explicitly rewritten from "Current Gap Identified" to "Gap Closure ({Month} {Year})" — don't just delete the flag; stakeholders need the audit trail showing the gap was closed, not swept away. Surfaced by the Afthab P A (QC/Wallet) case on Apr 21, 2026.


## Heuristic Text Parsing & Data Pipeline Hygiene (Apr 21, 2026)

468. **Factual placeholder beats empty or invented when the source data is literally unrecoverable** — When a source artifact exists but cannot be read (e.g., image-only PDFs that fail text extraction with zero characters recovered, locked Outlook .msg files, corrupted attachments), the row/entry must still carry a label. The honest choice is a descriptive placeholder like `Unreadable CV (1776112376090.pdf)` — never a blank field, never a fabricated guess. **Why:** Two user-stated rules often collide — "don't leave any row without a name" AND "don't assume any name". A factual placeholder satisfies both: it's a real, verifiable string that tells the reader exactly what state the data is in. Invented values (blindly guessing a name from a filename stem) cross into fabrication; blank values violate the completeness guarantee and usually cascade into downstream joins and filters silently dropping rows. **How to apply:** (1) Detect unrecoverable cases at the extraction layer, not the rendering layer — mark them with a distinct confidence tag (e.g. `name_conf = "unparsed"`) so downstream consumers can treat them specially. (2) The placeholder string must include the source identifier (filename, URL, message ID) so the human reviewer can trace back. (3) Surface the unparsed count in the run summary — hiding them behind the placeholder is as bad as hiding them behind a blank. (4) Applies beyond CVs: any bulk ingest (emails, receipts, scanned invoices, OCR pipelines, satellite image metadata) with partial failure modes. Surfaced by the D:\HR candidate-name audit on Apr 21, 2026 — 3 of 51 fixed candidates had image-only PDFs with zero extracted text.

469. **Heuristic text extractors fail in three predictable classes — blocklist all three with complementary data structures** — On 2026-04-21, the candidate-name parser in `D:\HR\_pipeline\step2_parse_and_score.py` misread 51 names as (a) section headers like `Work History` / `Core Skills`, (b) education/credential phrases like `Bachelor of Computer Science` / `Future University in Egypt`, and (c) 2-word capitalized phrases that coincidentally look like names such as `Nasr City` / `Saudi Arabia`. The original heuristic had only ~25 section-header entries and missed all three classes. **Why:** Each class needs a different data structure. Exact-match sets catch single phrases ("work history"), substring-match sets catch variants ("bachelor of", "bachelor degree", "bachelor's degree" all reduce to a single "bachelor of" prefix), and per-word disqualifier sets catch any line containing a poisoned token. One list catches one class — trying to use a single exact-match set for all three was the root cause. **How to apply:** (1) Build three blocklist structures up front in any heuristic extractor: `SECTION_HEADERS` (exact match), `NON_NAME_SUBSTRINGS` / `NON_MATCH_SUBSTRINGS` (substring match for multi-word variants), and `DISQUALIFIERS` (per-word match). (2) Seed them from a real 10-20 case audit of actual production data — do not rely on imagination. (3) When a new false positive is reported, classify it first: is this an exact phrase, a multi-word variant, or a poisoned token? Add to the matching structure that fits — not to whichever list is most convenient. (4) Applies beyond names — the same pattern fits email subject-line classifiers, product-title parsers, and any regex-heuristic layer upstream of an ML model.

470. **Scan order matters in multi-pass parsers — specific/tight rules must run BEFORE broad/noisy rules** — In the extraction function `extract_name()`, running single-line separator-splitting on lines 1-50 BEFORE the "combine two consecutive short lines" pass caused regressions: a CV with "Ola" on line 1 and "Mohammed" on line 2 (each a single word needing consecutive-line combine) was instead parsed as "Nasr City" from line 9's address `Nasr City, Cairo, Egypt` — the broad rule matched first and short-circuited the specific one. Reordering — specific rule first, broad rule second — fixed this. **Why:** In heuristic parsers, the first passing rule wins. If a noisy/broad rule runs early, it will occasionally claim a false positive before the correct tight rule has a chance to fire. The intuitive ordering ("scan top-down, try every rule on every line") is wrong for rule pipelines — you want confidence-ordered, not location-ordered. **How to apply:** (1) List all heuristic passes with their precision/recall tradeoff. Tight rules (specific patterns, narrow scope) go first. Broad rules (fuzzy separators, wider scope) go last. (2) For each rule, enumerate what it *will* match that it shouldn't. If that list is non-empty and the next-tighter rule would catch those cases, reorder. (3) Run a diff harness after every rule-order change — the surface area for unintended matches is wide. (4) Applies to any ordered rule engine: spam filters, fraud-detection thresholds, CSS selector specificity, redirect rule lists, firewall rules.

471. **Scope fuzzy rules to the narrowest window where they pay off** — Separator-splitting on double-space `  `, `|`, `·`, `•`, `, ` is valuable on the CV header line ("Ahmed Abaza  Junior Quality Manager" → splits to `["Ahmed Abaza", "Junior Quality Manager"]`, first chunk is the name) but toxic on line 9 of the same CV ("Nasr City, Cairo, Egypt" → splits to `["Nasr City", "Cairo", "Egypt"]`, first chunk is a location). Same rule, different damage by scope. Limiting the separator-split pass to the first 5 lines (the header zone) eliminated all location false positives without losing the header wins. **Why:** Fuzzy rules carry both signal and noise; the signal-to-noise ratio depends on WHERE in the document they fire. Document structure gives you implicit scope information — headers are name-dense, body is fact-dense, footers are reference-dense. Applying a fuzzy rule uniformly across all regions ignores this and collects noise that cancels the signal. **How to apply:** (1) For every fuzzy/noisy rule, ask: "what is the narrowest span of the document where this rule pays off?" Default to lines 1-5 for CV headers, the first paragraph for news articles, the subject line for emails, the first screen for web pages. (2) Log where the rule fires during development — if >30% of fires are outside the intended zone, narrow the scope. (3) Never apply separator-splitting, letter-spaced recovery, or email-inference fallbacks globally — always scope them to the region they were designed for. (4) Applies to OCR post-processing, address extraction, author/byline detection, table-header detection, CSV-guessing heuristics.

472. **Arab/Egyptian compound names need hyphen-aware parsing — reject only lowercase-hyphenated phrases, never "any hyphen in a multi-word line"** — Names like `El-Absawy`, `Abd-Elhamid`, `Al-Zanaty`, `Abu-Bakr`, `Al-Hussein` must pass the name-plausibility check. The original rule "if line contains `-` AND has multiple words, reject" silently dropped every candidate with a hyphenated surname segment — forcing them into the email-fallback path and often producing incomplete or wrong names. The correct rule: "each hyphen-separated segment of a word must start with a capital letter". This accepts `El-Absawy` (both caps) and `Abu-Bakr` (both caps), but still rejects English compound phrases like `web-development`, `end-to-end`, `results-driven`, `object-oriented` (all lowercase). **Why:** In the MENA hiring pool, a material fraction of surnames contain an Arabic definite-article hyphen (`El-`, `Al-`, `Abd-`, `Abu-`, `Ibn-`). A blanket hyphen-reject rule discriminates against them by silently dropping their CVs into a fallback path with worse accuracy. The English lowercase-hyphen-phrase pattern is structurally different (lowercase-first, kebab-case) and can be distinguished with a per-segment capital check. **How to apply:** (1) In any name-validation routine, replace the "reject multi-word lines with hyphens" rule with a per-word check: for each word containing `-`, split on `-` and verify each segment's first character is uppercase. (2) Add explicit test cases for Arab compound names alongside the English-kebab rejection cases. (3) Applies to any global HR system, authorship metadata, directory listings, or ID verification that needs to handle multi-cultural name formats (also relevant for Spanish `de la`, Dutch `van der`, etc. — add those as supplementary rules).

473. **Regression-test the WHOLE corpus, not just the known-bad cases you set out to fix** — The 51 targeted name fixes in step2 silently introduced 4 regressions on previously-correct rows (Ola Mohammed, Amir El-Absawy, Software Quality Control, Saudi Arabia) because my before/after test harness only compared outcomes against the 51 known-bad fix targets. The 342 other candidates — whose names had already been parsing correctly — were outside the test loop, so a heuristic change that now mis-parses them went undetected until I scanned the final HTML output. Adding a "scan all output for suspicious patterns NOT in your fix list" gate caught the regressions and prompted the hyphen-rule and separator-scope fixes. **Why:** Targeted heuristic changes rarely stay targeted — any adjustment to a shared parser, filter, or scorer has blast radius across every row that touches the changed code path. Testing only the rows you *meant* to fix confirms you addressed the intended bug but gives zero signal on the rows you didn't mean to break. **How to apply:** (1) Before any heuristic change, snapshot the full current output (all rows, not just the bad ones). (2) After the change, diff the full new output against the snapshot. (3) Any row whose value changed AND that wasn't on your intended-fix list is a regression candidate — triage each one explicitly. (4) For production pipelines, automate this as a gate: fail the build if the diff touches rows outside the allowed changeset. (5) Applies to any rule/scorer/filter change — spam classifiers, fraud thresholds, tax-category mappers, pricing rules, permission matrices.

474. **Empty return beats wrong-but-plausible, when a fallback chain exists** — When a heuristic extractor returns `""` (empty), the next stage in the fallback chain (email-local-part inference, filename stem, structured-metadata lookup) can recover. When it returns "Nasr City" (a wrong-but-plausible 2-word phrase), the chain short-circuits because `""` is falsy but `"Nasr City"` is truthy — so the wrong answer ships. Tuning the primary heuristic toward restraint ("return empty if I'm not confident") is strictly better than tuning it toward coverage ("return something") for any pipeline that has downstream fallbacks. **Why:** Short-circuit chains (`result or fallback1 or fallback2 or default`) are common in Python, JavaScript, Ruby — they stop at the first truthy value. A falsy-but-honest empty is transparent to the chain; a truthy-but-wrong value is opaque and blocks recovery. The asymmetry is: a false negative (rejecting a real name) costs one fallback step; a false positive (returning a fake name) costs the entire correctness of that row. **How to apply:** (1) In any extractor feeding a fallback chain, bias toward `return None` / `return ""` over "best guess" when confidence is below a threshold. (2) Use an explicit confidence tag (`name_conf = "ok" | "email-derived" | "filename-fallback" | "unparsed"`) so downstream rendering can flag the provenance. (3) Log empty-return cases — they are not errors, they are correct honest outputs. (4) Do NOT apply this pattern when there is no fallback chain — a single-shot extractor must return its best guess or the row is lost. (5) Applies to OCR confidence thresholds, ML-classifier calibration (abstain-option), entity-resolution joins, and any parser/normalizer with a "don't know" option.

475. **Always keep a post-processor override layer for the long tail the heuristic can't reach** — Even a well-tuned heuristic name parser won't hit 100% on messy real-world CVs — graphic layouts that bury the name past contact/skills/certificates blocks, all-image PDFs, letter-spaced headers spanning 3 lines, multilingual headers with mixed scripts. After the Apr 21 step2 audit, the heuristic direct-hit rate for the 51 known-bad candidates went from 0/51 to 29/51 (57%) — a big win, but still leaves 22 cases needing help. The persistent solution is a small manual-override file (`D:\HR\_pipeline\fix_names.py`) mapping `filename → (corrected_name, name_conf)`, applied as the final pipeline step. It's idempotent, survives step2 re-runs, and lets the PM append new overrides in one line without touching the heuristic. **Why:** Heuristics have fundamental limits on messy natural-language data. Chasing 100% parser accuracy via more blocklists produces a parser that is overly conservative, breaks on legitimate new inputs, and still misses the truly hard cases. An override layer cleanly separates "rules that generalize" (in the parser) from "exceptions that don't" (in the override file), keeping each code path simple. **How to apply:** (1) Every extract/parse/transform pipeline with human-review output should have an override step as its last stage. (2) The override format should be plain data (dict, JSON, YAML, CSV) that a non-coder can edit — `{filename: name}`, `{id: status}`, `{key: value}` — not a script. (3) The override layer applies unconditionally; it does NOT check whether the upstream stage disagreed — that conflict is the whole point of the override. (4) Document the override file in the pipeline's README as "the tail-end corrector" — otherwise future maintainers will delete it thinking it's dead code. (5) Applies to: ETL pipelines, ML-inference post-processing, address geocoders, product-catalog enrichment, PII redaction. Pair with Lesson 44 (save lessons) and Lesson 286 (don't assume source data) — the override file becomes part of the source data for the next pipeline run.


## Cross-File Hyperlink Audit & MD/HTML Parity (Apr 21, 2026 — continued)

738. **When creating a new numbered version of an HTML file, audit it for hard-coded references to the OLD version number across ALL attribute types — not just `<a href>`** — On 2026-04-21, after copying `Project Management Handling_v3.html` → `_v4.html` and updating the QA-QC team data, I declared the file ready. User asked "did you consider the relation between them if have hyperlink or something like that?" — prompting a re-audit that surfaced **5 stale references to `OKR_KPI_Dashboard_v7.html`** buried inside `data-hidden-reason` attributes on hidden tab buttons (lines 387, 398, 404, 413, 416). These informational strings telling users "OKR Progress tab belongs to OKR_KPI_Dashboard_v7.html — the canonical OKR tracker" needed to point to v8 now that v8 existed. Similar stale pattern found in V5/00_Index.html (title "V4 Index", metadata version 4.0, date Feb 15). The visible `<a href>` audit had passed cleanly — all 30+ `href` values were Azure DevOps portal URLs (version-independent). The miss was in non-href attributes: `data-*`, `<title>`, header metadata, changelog rows, inline comments referencing sibling-file names. **Why:** When versioning a file, the mental model "update the links" only covers `<a href>` and `<link rel>`. But real cross-references in modern HTMLs live in 6+ places: `<title>` tags, `<meta>` descriptions, `data-hidden-reason` / `data-tooltip` / `data-*` attributes, JS string constants, version-history tables, footer text, localStorage key names. A file that passes the href-audit can still ship with 5 stale attribute references that tell the user "see v7" when v7 is no longer canonical — degrading the document's authority. **How to apply:** After copying file-vN → file-v(N+1), run a single regex grep for the OLD version number across the whole file — `grep -n "_v{N}\|V{N}\.html\|Version.*{N}\.0\|{N} Index"` — BEFORE declaring the new version ready. Every match is a potential stale reference. Additionally audit: (a) `<title>` tag content; (b) `<meta>` description/keywords; (c) all `data-*` attribute values that look like prose; (d) JS string literals that name sibling files; (e) version-history table rows (keep old rows AS-IS, add new row at top/bottom); (f) footer/header text announcing "vN"; (g) any `localStorage` key names that embed the version. The grep takes 2 seconds; the miss costs credibility when the user spots it mid-review. Pair with lesson #467 — cross-file audits are part of the retroactive propagation discipline.

739. **MD/HTML parity check is not optional — grep sibling MDs before declaring an HTML-only update complete** — On 2026-04-21, after delivering 7 HTML files with Afthab's role change (OKR v8, PM Handling v4, Access Upgrade v7, V5 Dept Workflows suite), I announced completion. User asked "did md file for this html files is updated with what we do here?" — prompting an MD audit that found **2 authoritative MD files with stale team rosters**: `DevOps_Complete_Setup_Guide_V1.md` (line 80: Mostafa as only QC; line 310: Phase 3 "Upgrade Mostafa Ahmed to Basic + Test Plans") and `DevOps_Access_Level_Recommendation_Full_V3.md` (line 83: Mostafa as row 9, sole QA-QC entry). Both MDs are indexed by the local ragtools knowledge base — so any future search for "QA-QC team members" or "Azure DevOps access plan" would return stale answers long after the HTML had been fixed. Plus, the user's standing convention "HTML-only after vN" for OKR and Access Upgrade email-summary series had been interpreted by me as "no MD parity needed" — but the convention applied only to those specific versioned series, NOT to the authoritative complete-setup-guide + full-recommendation reference docs in the same folder. **Why:** MDs are the authoritative reference layer for the RAG index. HTMLs are the presentation layer for stakeholders. When team/cost/role data changes, BOTH layers need the update — the HTML so stakeholders see it, the MD so the RAG index returns the right answer. "HTML-only after vN" is a rule for *versioned releases of the same document*, not a license to skip MDs that carry overlapping team data. The deeper failure mode: Search the knowledge base BEFORE declaring a propagation task done — if RAG still returns stale answers after you've updated the HTML, the work isn't complete. **How to apply:** For every HTML update that touches team rosters, cost tables, org structure, or access lists: (1) `ls *.md` in the same folder and grep each for the entity being changed (person name, role, cost line); (2) audit sibling folders that commonly carry overlapping data — `DevOps Access Level` has both email-summary MDs AND complete-setup-guide MDs AND recommendation-full MDs; (3) if the MD is a versioned-series that follows "HTML-only after vN" (e.g. OKR v3+ and Access Upgrade v4+ per user's convention), restore parity by creating a fresh MD mirroring the latest HTML content — it takes 10 minutes and protects the RAG index; (4) if the MD is an authoritative legacy reference (Complete_Setup_Guide_V1, Access_Level_Recommendation_Full_V3), patch it minimally with a prominent "⚠️ Legacy reference — minimally patched, not fully restated" banner plus the specific roster/Phase/checklist updates AND a pointer to the current canonical HTML; (5) DO NOT recompute cost accounting in legacy MDs without a full rewrite — just flag that cost figures are stale. Pair with lessons #436 (search local project data files before saying "not found") and #467 (retroactive propagation) — #436 says to search files, #467 says to propagate across registries, #469 says specifically: check BOTH MD and HTML layers of the same data, because the RAG index reads MDs.

481. **Before editing on a "file has stale content" report, hash the file on disk and grep the actual references** — On 2026-04-21 a user report said `Project Management Handling_v4.html` linked to `OKR_KPI_Dashboard_v7.html`. Direct verification via `sha256sum` + `grep -n -o 'href="file:///.../OKR_KPI_Dashboard_v[0-9]+\.html"'` on the live file took 10 seconds and proved the on-disk file was correct — all 6 references pointed to v8. The real cause was a browser-cache hit rendering a stale prior version. Had I taken the report at face value, I would have re-edited an already-correct file and introduced inconsistency. **Why:** User reports of "file X says Y" are observations of a rendered view (browser tab, editor preview, HTML export) — not of what the file on disk actually contains. Browser caches, CDN caches, editor snapshot buffers, and stale file-watchers routinely lie. Trusting the report and editing the file can flip a correct file to wrong. **How to apply:** (1) For any "the file says X, should be Y" report, first verify on-disk state: mtime + size + SHA-256 + targeted `grep -n -o` for the specific pattern. (2) If the on-disk state already matches what the user expects, the issue is in the view layer — direct them to hard-refresh (Ctrl+F5), clear cache, or re-open the file. (3) Only edit when on-disk grep confirms the stale value. (4) In the reply, show the evidence (hash, mtime, line-numbered grep output) — proves the point and teaches the diagnostic pattern. Pair with Lesson #273 (HTML-comment rot) and the general principle: the file on disk is authoritative; the render is not.


## MD Merge & Consolidation Workflow (Apr 21, 2026)

476. **Appendix heading prefixes prevent anchor collisions in multi-source MD merges** — When merging multiple MD files verbatim into one document (e.g., `v1.md` + `v2.md` → consolidated `.md`), identical section names across sources ("Objective 1", "Team Roster") collide in the Markdown TOC because both resolve to the same auto-generated anchor slug. Fix: shift original headings down one level AND add a unique per-source prefix such as `A1 Objective 1` / `A2 Objective 1`. Surfaced on 2026-04-21 merging `OKR_KPI_Dashboard_v1.md` + `_v2.md` + `_v8.md` into `OKR & KPI.md` — both v1 and v2 had `## Objective 1` and `## Team Roster`, which without prefixes would produce two `#objective-1` anchors silently clobbering each other. **Why:** Markdown renderers generate anchors from heading slug (`## Team Roster` → `#team-roster`). Two identical H2s in the same document both resolve to the same anchor; the TOC link and any in-document hyperlink jumps only to the first match. Prefixes keep each appendix's navigation independent and audit-ready. **How to apply:** (1) Before merging, pre-scan all source files for duplicate H2/H3 text — `grep -h "^## " *.md | sort | uniq -d`. (2) For every duplicate, demote all copies down one level inside the appendix (so the main doc's `##` section headers still own the top-level TOC) AND add a short per-source prefix (`A1 …` / `A2 …` / `Legacy …`) to each appendix's sub-headings. (3) Apply prefixes only in appendices/supplement content — the primary skeleton (per Lesson #115) keeps its natural headings. (4) Applies to any multi-source consolidation: merged MOM documents, combined quarterly reports, audit-trail archives, multi-version technical specs.

477. **For "merge into one file" tasks, the PM default is full preservation + delete originals** — When asked to consolidate multiple MD/HTML versions into a single target file, two choices compound: **history depth** (full/condensed/current-only) and **source-file fate** (keep/delete/archive). The PM's default workflow is **full preservation** (keep all source tables verbatim in appendices) paired with **delete originals after merge** — the merged file becomes the single audit trail. Confirmed on 2026-04-21 by the OKR & KPI merge (v1 + v2 + v8 → `OKR & KPI.md`). **Why:** Full preservation protects against future "what did v1 actually say" questions; deleting originals prevents fragmented file sprawl and the risk of stakeholders reading outdated v1/v2 as if they were current. The two choices only work together — deleting without preservation loses data, and preservation without deletion defeats the "one file" goal. **How to apply:** (1) Before merging, if the consolidation scope is ambiguous, surface both questions via AskUserQuestion rather than guessing. (2) If the defaults apply, still announce both choices before execution ("I'll preserve v1+v2 as appendices and delete the originals after verification"). (3) Verify the merged file BEFORE any deletion runs: file size ≥ sum of source bodies minus shared boilerplate; grep for unique strings from each source ("Objective 5" → Appendix 1, "Tier 1 — Tracked in Detail" → Appendix 2); confirm all top-level section anchors exist. (4) Only delete after ALL verification checks pass — if any fail, keep the originals and ask the user to inspect. Pair with Lesson #138 (never bash `rm` on Windows user files — for low-risk derivative files after a merge, verified deletion is fine; for originals with irreplaceable content, use Recycle Bin) and Lesson #140 (originals are sacred — once merged, source MDs become derivatives eligible for deletion because the merged file carries 100% of their content).

478. **Plan-mode clarifiers with AskUserQuestion beat guessing on ambiguous consolidation tasks** — Two targeted questions (history depth, source-file fate) turned an ambiguous "merge all this md in one only" request into a deterministic spec. Without them, the output would likely have defaulted to condensed history or left source files in place — either of which would have required a rework cycle. Proven on 2026-04-21 during the OKR & KPI 4-file merge. **Why:** Merge/consolidation requests are structurally ambiguous because the user's mental model rarely distinguishes "consolidate content" from "consolidate files" from "preserve all history" from "keep just current state." Silent guessing produces outputs the user will reject; asking costs 10 seconds and locks the spec before writing a single line. **How to apply:** (1) In plan mode, on any task touching multiple sources/versions, use AskUserQuestion with 2-3 options per question BEFORE finalizing the plan. (2) Put the recommended option first and label it "(Recommended)". (3) Default question set for any merge: (a) history depth — full preservation / condensed / current-only; (b) source-file fate — keep / delete / archive. (4) Skip the clarifiers only if the user pre-specified the choice, or the task is truly trivial (single-line typo fix, one-file rename). (5) Applies beyond merges to any structurally ambiguous request: renaming a folder (move/copy), removing content from a deliverable (delete-from-file/remove-from-publish-only), updating a shared template (in-place/new-version), handing off a project (read-only archive/live-editable).


## Plugin Design — Skills-First vs Hooks-First (Apr 21, 2026)

483. **For Claude Code plugins, authoring-time skills beat post-write hooks for most coverage gaps** — When extending `pm-guidelines-plugin` from v1.4 → v1.7 to close 12 coverage gaps from global_lessons.md, the initial plan leaned heavily on hook extensions (regex checks in `post_write_dispatcher.py`). User course-corrected: "decrease the creation of hooks and use the skills instead as much as possible." The revised plan delivered 3 new skills + 2 new agents + 6 skill extensions + 0 new hooks — closing all 12 gaps with visible authoring-time guidance instead of silent post-write enforcement. **Why:** Hooks (Python scripts on PostToolUse) fire AFTER the mistake lands in the file. They're silent until a regex matches; when they fail they fail quietly; they're Python which carries a cross-platform maintenance tax. Skills (Markdown with prompt-signal activation) fire BEFORE the mistake — at the moment Claude is about to write the wrong thing. They're visible when they activate; they're iterable without restarting the harness; they're domain-portable. The right division of labor: hooks for deterministic security blocks (PAT tokens, protected files), dispatcher for regex-detectable quality checks, skills for everything else (workflows, checklists, judgment-heavy rules, cross-file audits delegated to read-only agents). **How to apply:** (1) When adding new coverage to a plugin, default to a new skill or skill-extension, not a new hook. (2) New hooks are only justified when: the check is regex-detectable AND must fire unprompted (e.g., security gate) AND fits the existing dispatcher's scope. (3) For judgment-heavy or cross-file work, pair an authoring-time skill (prevents) with a read-only audit agent (verifies) — see Lesson #485. (4) Measure coverage via the plugin's own health check after every change; the description-string carries a "N guidelines" pattern that the lesson-count validator expects — keep it current. (5) Applies to any Claude Code plugin, not just pm-guidelines: see `d:\Global Lessons\plans\soft-tinkering-taco.md` for the worked routing table across 12 gaps.

484. **Plan-mode agent forks with different architectural theses produce better plans than a single agent** — When designing the v1.5+ upgrade, I launched TWO Plan agents in parallel with opposing instructions: Agent A was told to maximize new skills/agents; Agent B was told to minimize new files and extend existing surfaces. The divergence surfaced real trade-offs: Agent B correctly identified that `pm-cross-tab-reconciler` naturally lives in `pm-report-reviewer`'s rubric; Agent A correctly identified that forcing everything into one agent overloads it. Agent B also caught a path error (Agent A used `d:\Global Lessons\pm-guidelines-plugin\`; Agent B verified on disk and found the correct `d:\Global Lessons\plugins\pm-guidelines-plugin\`). The final synthesis absorbed the cheap extensions from B's thesis AND the specialist agents from A's thesis — better than either alone. **Why:** A single Plan agent optimizes for its given direction. Two agents with opposing directions surface the actual design space — where one agent's optimum is clearly wrong, the other's is clearly right; where they agree, you have high-confidence structure; where they disagree, you have a genuine architectural fork to present to the user via AskUserQuestion. Cheap (two agents running in parallel is ~same wall time as one) and higher-quality. **How to apply:** (1) For any non-trivial plan involving architectural choices, launch 2-3 Plan agents with EXPLICITLY DIFFERENT theses — "minimize X", "maximize Y", "prefer Z". (2) Brief each agent on the OTHER agent's thesis so they don't accidentally converge. (3) Synthesize by cross-checking divergent recommendations against ground truth (file-system verification, memory entries, CLAUDE.md rules). (4) When both agents arrive at the same recommendation, trust it. When they diverge, offer the user the fork via AskUserQuestion rather than picking silently. (5) Applies to: plugin design, database migration strategy, API redesign, codebase refactoring plans, any task with real architectural trade-offs.

485. **The skill-plus-agent dual-surface pattern is the right shape for judgment-heavy coverage** — Gaps like "dashboard cross-tab number drift" and "MD↔HTML parity" aren't solvable by a single surface. At authoring time, you need a skill that teaches Claude the single-source-of-truth rule BEFORE writing code. At audit time, you need a read-only agent that semantically aligns near-synonym labels ("Open Items" ≈ "Outstanding Tasks") using Opus judgment across the finished file. Using the SAME NAME for the skill and the agent (e.g., `pm-cross-tab-reconciler` skill + `pm-cross-tab-reconciler` agent) reinforces that they solve the same problem at different times — skill prevents, agent verifies. Proven in v1.5+v1.6 of pm-guidelines plugin: cross-tab-reconciler shipped as both skill (Phase 1) and agent (Phase 2); md-html-parity-checker agent (Phase 2) pairs with the `pm-session-discipline` rule "MD is source of truth" (Phase 1). **Why:** Skills activate on prompt signals — they fire while Claude is authoring. Agents are invoked explicitly or via delegation — they fire after authoring is done. Neither alone covers the full lifecycle: a skill with no agent means mistakes that slip past authoring are never caught; an agent with no skill means Claude repeats the mistake every session. The same-name convention makes the pairing visible in the plugin structure (`pm-X/SKILL.md` + `agents/pm-X.md`). **How to apply:** (1) For judgment-heavy gaps, design BOTH surfaces from day one: skill for authoring, agent for audit. (2) Use the same name for both when they solve the same problem; Claude disambiguates via context (prompt signal → skill; explicit launch or reviewer delegation → agent). (3) Cross-reference in both files: skill's body says "if unsure, invoke the agent for post-authoring semantic review"; agent's body says "this is the audit side of the skill — when the skill's discipline is followed, this agent should report PARITY CLEAR". (4) Wire the main reviewer agent to auto-delegate to specialist agents when the target type matches — avoids overloading one agent with every possible check. (5) Applies to any domain with both authoring and audit phases: CI pipeline rules, document quality standards, security review, accessibility compliance.


## Folder Absorption & Multi-Source Consolidation (Apr 22, 2026)

486. **Additive-over-destructive is the data-loss safety net for folder absorptions** — When absorbing a sibling folder into a parent (e.g., `QA-QC & Devlopment Activity - RACI Matrix\` and `Taqat Alignment Framework - Hatem\` into `Department Workflows\` on 2026-04-22), never delete or overwrite the originals during the merge. Move the folder as-is, copy the content into the aggregator MD/HTML, and keep every source file at its new path with its original mtime. The aggregator becomes the primary reading surface; the originals stay as the rollback. **Why:** A merge that deletes originals has no rollback if the aggregator turns out to be missing content, mis-demoting headings, or flattening tables. "Don't lose data" isn't just about the merge pass — it's about the hours, weeks, and future sessions after the merge when someone needs to cross-check a specific claim against the raw source. Additive consolidation costs a few MB of disk; destructive consolidation can cost a weekend of forensic reconstruction. **How to apply:** (1) Default rule: absorb-by-move, never absorb-by-delete — the moved folder keeps every file intact at its new path. (2) Aggregator file (typically a consolidated `Department Workflows.md` or similar) adds content, never replaces. (3) A consolidation is only "complete" when: originals exist at their new path, aggregator has provenance markers (Lesson #487), subfolder stubs carry relocation breadcrumbs (Lesson #489), and the index registry is updated. (4) Only consider deleting originals after the user explicitly asks for cleanup — and even then, prefer Recycle Bin over `rm`. (5) Pair with Lesson #477 (full preservation is the PM default) — but recognize that #477 allows delete-after-verification for versioned MD series with a single consolidated target, whereas folder absorption keeps the originals because they represent independent inbound work.

487. **Every merged block needs a `<!-- SOURCE: path, mtime, size-metric -->` provenance marker** — When concatenating many source files into one aggregator, prefix every included block with a single HTML-comment marker: `<!-- SOURCE: V5\md\01_Organization_and_Governance.md, 2026-04-21 15:51:12, 172 lines -->`. This makes it trivial to (a) prove which block came from which source, (b) detect duplicate inclusions, (c) audit completeness via `grep -c "^<!-- SOURCE:"`, (d) spot-check drift if a source is later edited (compare current mtime to the marker). Applied on 2026-04-22 during the Department Workflows consolidation — 24 markers for 2 DOCX + 2 HTML + 20 MD sources, each verifiable in one grep. **Why:** Without markers, a merged file is a black box — you can't tell which section came from which source, whether anything was silently dropped, or whether a future re-run would produce the same output. With markers, the aggregator is self-auditing: the marker count should equal the source count, and `grep "^<!-- SOURCE:" | sort | uniq -c` catches duplicates. **How to apply:** (1) In any merge script that concatenates files, emit a SOURCE marker before each block — path relative to the aggregator's folder, source mtime as ISO timestamp, one size-metric (lines for MD, paragraphs/tables for DOCX, headings/paragraphs/lists/tables for HTML). (2) After the merge, run `grep -c "^<!-- SOURCE:"` and assert it equals the expected source count — build this as an automated check, not a mental exercise. (3) When citing a block in a later discussion or quality check, quote the marker alongside the content so reviewers can trace back. (4) Don't count `<!-- SOURCE:` inside regular prose — only match on `^<!-- SOURCE:` (line-anchored) to avoid false positives when the aggregator mentions the marker pattern in documentation. (5) Applies to any multi-source concatenation: merged MD reports, combined HTML dashboards, stitched transcript files, audit-log bundles.

488. **DOCX tables require structured XML walking, not regex-over-paragraphs** — First pass of the 2026-04-22 QA-QC RACI DOCX extraction flattened every table cell to a one-cell-per-paragraph list (each `<w:tc>` cell wraps a `<w:p>` paragraph internally, so splitting on `</w:p>` and stripping tags yields "Activity / BO / PM / DEV / QA / QC / Define Business Requirements / R / C / C / C / I / …" — a vertical column of orphaned cells with the grid structure destroyed). Fixed by walking the body XML linearly, branching into `<w:tbl>` → `<w:tr>` → `<w:tc>` → `<w:p>` and emitting real Markdown tables. The 6-phase RACI (26 activities × 5 roles) rendered correctly on the second pass. **Why:** Word's OOXML format stores table cells as paragraphs-inside-cells-inside-rows — a regex-over-paragraphs approach sees only leaf content, losing the hierarchical structure. A linear XML walk preserves the nesting. Markdown tables can express everything Word RACI tables express, but only if you first parse the tree. **How to apply:** (1) Any DOCX-to-Markdown transcription that touches tables needs an explicit table branch — not just paragraph extraction. (2) Walk the `<w:body>` linearly with `re.finditer(r'<w:(p|tbl)\b[^>]*>', ..., re.DOTALL)` tracking element boundaries; emit `('para', text)` for paragraphs and `('table', rows)` for tables in document order so paragraph-before-table and paragraph-after-table ordering is preserved. (3) For each table, walk `<w:tr>` then `<w:tc>` then join the `<w:t>` runs inside each cell's `<w:p>` elements — this gets the cell text cleanly. (4) Normalize row length (pad short rows with empty cells), escape pipes in cells (`|` → `\|`), emit the header row, separator, and body rows as standard Markdown. (5) Same principle applies to HTML extraction — walk `<h1-h6>, <p>, <li>, <table>` in document order with a single block-level regex, don't concatenate all `<td>` text as flat paragraphs. (6) If tables aren't critical for the use case, state that upfront and fall back to paragraph-only extraction — but never silently flatten a table and pretend it was prose.

489. **Folder relocation needs a three-layer breadcrumb: subfolder stub → aggregator front matter → Source-Root index** — When moving a folder under a new parent AND consolidating its content into an aggregator MD, three separate pointers must be planted so the relocation is findable from any entry point: (a) **Subfolder stub** — the stub MD inside the moved folder (e.g., `QA-QC & Devlopment Activity - RACI Matrix.md`) gets a "⚠ RELOCATED" banner with old path, new path, date, reason, and the aggregator Part number where content lives. (b) **Aggregator front matter** — the parent's consolidated MD (e.g., `Department Workflows.md`) gets a "Consolidation / Move History" table listing every absorbed source by date, from/to paths, and landing Part. (c) **Source-Root index** — the old session entry (e.g., section 2.6 Taqat Alignment) is marked `*(ABSORBED YYYY-MM-DD into 2.N)*` with old+new paths preserved, not deleted. Proven on 2026-04-22 across three absorptions: governance DOCX + QA-QC RACI folder + Taqat Alignment folder, all consolidated into `Department Workflows\`. **Why:** A user (or future-you) discovering a file may enter through any of three doors: opening the folder directly (sees the stub), reading the aggregator (sees the front matter table), or searching the session index (sees the absorbed marker). Plant the breadcrumb in only one layer and the other two entry points will return stale answers — the stub will look like a current session when it's really a relocated artifact, the aggregator will look like it was built from scratch when it really absorbed existing work, and the index will either still point to the old (non-existent) path or silently delete the session and lose the audit trail. **How to apply:** (1) For every folder move + consolidation, plan the three breadcrumbs as a single atomic unit — if you can only do one, delay the move until you can do all three. (2) Subfolder stub template: `> ⚠ RELOCATED <date> — Old path: X | New path: Y | Consolidated into: Z Part N | Originals preserved`. Keep the old H1 title so grep and RAG still find it. (3) Aggregator front matter: a table with columns Date / Action (Moved file | Absorbed folder) / From / To / Lands in (Part N). (4) Source-Root index: change the section heading to `### 2.N Original Name *(ABSORBED <date> into 2.M)*`, keep the original path labelled "Old Path (moved)" and add the new path underneath — never delete the entry. (5) Cross-check after planting: from each entry point, can you navigate to the other two? If no, a breadcrumb is missing. (6) Applies to any relocation that combines move + merge: folder consolidation, session absorption, project re-org, MD series collapse, vault restructuring.


## Legacy Reverse Engineering & Migration Planning (Apr 22, 2026)

490. **Private GitHub repo access — verify BEFORE cloning, not during** — When the user hands you a GitHub URL under a different owner's org (e.g. `mbashaPPQ/TTElectronicServices` while you're authenticated as `melshahedpearl`), run `gh auth status` **and** `gh repo view OWNER/REPO --json visibility,defaultBranchRef,pushedAt,description,diskUsage` BEFORE the clone. That one API call proves the token has read access, returns visibility (private/public), last-push date, and disk size — everything you need to decide whether the clone will succeed and how long it will take. **Why:** `gh repo view` returns JSON in ~1 second. A failed `git clone` with wrong credentials takes 30–300 seconds of hanging prompt + credential-manager popups + eventual rage-quit. On 2026-04-22 the `gh view` step caught that the repo was private, had one commit, and was ~19 MB — all answers relevant to the plan. **How to apply:** (1) When a GitHub URL comes from a different org/user than your authenticated identity, always `gh repo view` first. (2) If the view call 404s unauthenticated, you must either have the repo owner add your account OR clone via a token-authenticated URL. (3) Capture `diskUsage`, `pushedAt`, `defaultBranchRef.name` into the plan up front — saves a round-trip later. (4) Same check applies to Azure DevOps repos (`az repos show`), GitLab (`glab repo view`), Bitbucket — never skip the metadata probe. Reference: LEGACY_RE_BRIEF_ES.md Step 1 verification.

491. **Parallel Explore agents for wide codebase surveys — partition scope explicitly to prevent overlap** — On a 2,577-file C#/ASP.NET legacy solution (2026-04-22 TTElectronicServices reverse-engineering), launching 3 Explore agents in a single message (project-meta extraction / Tier-1 deep walks of 4 most-used pages / Tier-2 categorized index of remaining 52 pages) completed in the time the single-agent sequential version would have cost for just one tier. **Why:** Agents don't coordinate with each other — you coordinate them via mutually-exclusive prompt boundaries. The Tier-2 agent prompt explicitly said "exclude these 4 pages" and listed them; without that, it would have re-walked the Tier-1 files and duplicated work. **How to apply:** (1) For any codebase survey spanning ≥3 independent slices (per-module, per-layer, per-concern), launch agents in parallel with NON-OVERLAPPING scopes — enumerate the files/folders each agent owns vs excludes. (2) Max 3 agents per message (more becomes hard to audit). (3) Each prompt must be self-contained: agents can't read your earlier tool results — paste counts, file lists, and prior-agent findings into the prompt. (4) After parallel return, YOU synthesize — don't chain another agent on top to merge; do it yourself so you can spot cross-agent contradictions. (5) Save token+time budget by specifying thoroughness per agent ("very thorough" only where it matters; "medium" or "quick" for enumeration tasks). Reference: LEGACY_RE_BRIEF_ES.md Step 1 three-agent parallel scan.

492. **Always deep-read Excel and .msg reference files BEFORE writing the plan** — On 2026-04-22 the user's prompt "read and take action" was ambiguous. Reading the `.msg` via Outlook COM (`$outlook.CreateItemFromTemplate(path); $msg.Subject; $msg.Body`) revealed the strategic request was about TWO systems (Archiving + Electronic Services), not just the ES folder the session opened in, and that the central question was "Odoo vs Django/Next rebuild" — which anchored the entire plan's Section 10 decision matrix. Reading the `.xlsx` via Excel COM (`$excel.Workbooks.Open($path, $false, $true); $sheet.UsedRange`) surfaced the `AppearInMenu=True` column flagging the 4 most-used pages, which became the Tier-1 scope anchor. **Why:** An ambiguous prompt answered from only the folder structure would have produced the wrong scope (both repos), the wrong emphasis (inventory everything vs focus on 4 menu pages), and the wrong recommendation framing (generic vs Odoo-vs-Django). Cost of reading the 2 reference files: ~90 seconds. Cost of skipping: a wasted brief. **How to apply:** (1) When the working directory contains .xlsx, .msg, .pdf, .docx files alongside code/data, ALWAYS read them before writing the plan — never after. (2) Outlook COM pattern: `New-Object -ComObject Outlook.Application; $outlook.CreateItemFromTemplate(<path>); print Subject/From/To/CC/SentOn/Body`. (3) Excel COM pattern: `New-Object -ComObject Excel.Application; $excel.Visible = $false; Workbooks.Open(path, $false, $true); iterate Sheets and UsedRange cells`. (4) If output exceeds 30KB, persist to a tool-results file and Read it back. (5) Build the plan's Context section by quoting the .msg sender, date, and strategic ask verbatim — that anchors stakeholder alignment. Reference: LEGACY_RE_BRIEF_ES.md Context section.

493. **Legacy-RE deliverable: Decision Matrix > Prose, every time** — When the stakeholder's strategic question is "Option A vs Option B for N modules" (2026-04-22 Semir's Odoo-vs-Django ask spanning 9 ES modules), end the brief with a per-module scorecard table: columns `Module | Pages | Out-of-box fit | Stack A effort (S/M/L/XL) | Stack B effort | Recommendation`. Follow it with a recommended path (Hybrid/A-heavy/B-heavy) and exactly 3 critical caveats. **Why:** Decision-makers read the matrix first and everything else second. Prose-only briefs force them to build the matrix mentally — error-prone and time-consuming. The matrix+path+caveats trio is what stakeholders quote back in meetings and paste into follow-up emails. On 2026-04-22, Section 10's matrix (9 module rows × 6 columns) produced a Hybrid recommendation in ~25 weeks with 3 caveats (WCF coupling, Odoo bench strength, monolith decomposition first) — clean one-glance answer. **How to apply:** (1) Effort column uses consistent buckets: S ≤1 wk / M 2-4 wk / L 1-2 mo / XL >2 mo. (2) Out-of-box fit uses Full / Partial / None badges with color. (3) Every row ends with a Recommendation cell — don't let reviewer infer. (4) Below the table: named path (Hybrid/A-heavy/B-heavy) + 2-sentence paragraph on why it beats each pure alternative + exactly 3 caveats with specific remediation per caveat. (5) Mirror the matrix in both the MD and the HTML (with the same column order and badges) — don't strip it down for HTML. Reference: LEGACY_RE_BRIEF_ES.md Section 10.

494. **Monolith decomposition is always Phase 0, never Phase 1 or later** — When a legacy codebase has a page/module >2,000 LoC (2026-04-22 `RequestService.aspx.cs` = 2,936 LoC, `ServicesInbox.aspx.cs` = 1,084, `BlueprintDesigner.aspx.cs` = 843), the migration effort estimate depends heavily on whether the monolith is decomposed on the legacy platform FIRST. Going direct to rewrite bakes in worst-case assumptions and blows up effort on BOTH target stacks from L to XL. **Why:** Migrating a 2,936-LoC monolith whole-cloth forces the target stack to absorb the tangled coupling (dynamic form rendering + workflow orchestration + blueprint binding all in one class). Decomposing first (3 weeks on the legacy platform: extract pure logic classes, thin the UI controller, add characterization tests) reveals the real surface area — often 4-5 cohesive units, each L or less to migrate. Skipping decomposition makes Odoo XL and Django XL instead of L each. **How to apply:** (1) Add a "Phase 0 — Pre-work (3 wk)" before any rewrite phases: decompose monoliths + SP inventory + characterization tests + decision confirmation. (2) The "monolith" threshold is 2,000 LoC per class or 500 LoC per page or 200 LoC per React component — adjust per stack but flag aggressively. (3) Characterization tests around the decomposition prevent regression during the refactor AND become the golden-path regression suite for the migrated stack. (4) Applies to every stack: C# WebForms, Django fat views, Rails god-models, React god-components, Angular mega-services. (5) If the user pushes back on Phase 0 duration, show them the L→XL cost delta on the decision matrix — it's usually 4-6 weeks per target stack, dwarfing the 3-week Phase 0. Reference: LEGACY_RE_BRIEF_ES.md Section 12 Phase 0.

495. **One-commit GitHub repo named "Initial commit: X solution" = code dump, not the live repo** — On 2026-04-22 `mbashaPPQ/TTElectronicServices` showed exactly 1 commit dated the morning of the handoff email. That is a snapshot for the reverse-engineering exercise, NOT the production repo. **Why:** Contributor history, blame-traceable bug origins, feature-add timelines, PR patterns — none of those signals exist in a one-shot dump. Reviewers who don't read this signal may mistake "one contributor, one commit" for "immature project" or ask why there's no PR history. The real dev lifecycle lives in whatever TFS/internal-GitLab/Azure-DevOps the owner actually uses. **How to apply:** (1) In the repo-metadata inventory MD, always include a "What this repo does NOT contain" section listing: real commit history, SP source, tests, CI, DB schema, ENV template, README. (2) Explicitly say: "one-commit code dump, not the production repo" — don't let readers infer. (3) In the brief's migration-risk table, add a Medium-severity row for "Stored proc source not in-repo" + "Real repo history not available" with mitigation = request from owner. (4) Count files via `git ls-files | awk -F. '{print tolower($NF)}' | sort | uniq -c | sort -rn` to compare language-breakdown numbers with the GitHub-API language report — divergence tells you what binaries/generated files skew the web display. (5) If the repo is clean-room, also check for leftover developer scaffolding (the 2026-04-22 ES dump had `WebApplication1/` — a stray ASP.NET template project). Flag and recommend removal. Reference: `_inventory/github-metadata.md`.

496. **Stored-procedure source is almost never in the application repo — flag as "request from owner" upfront** — When a .NET codebase calls `Microsoft.ApplicationBlocks.Data.SqlHelper.ExecuteDataset` / `sp_*` patterns / EF `Database.SqlQuery`, assume the DDL and SP scripts are NOT in-repo until proven otherwise. They typically live in a sibling DB project (SSDT), a Redgate SQL Source Control repo, a SQL Server Management Studio backup, or DBA-owned scripts outside source control entirely. **Why:** On 2026-04-22 the 365 `.cs` files under `Library/DataAccess/` included SP calls via `ApplicationBlocks.Data` but the corresponding CREATE PROCEDURE source was absent — because it was in a separate DBA-owned SQL project. A migration estimate that ignores the SP layer misses 20-40% of the real effort (SP translation to ORM queries + testing + data-migration-dependent procs). **How to apply:** (1) During tech-stack metadata scan, grep for `sp_`, `usp_`, `SqlHelper.Execute`, `Database.SqlQuery`, `CommandType.StoredProcedure` — any hit means check for SP source in a sibling folder/project. (2) If not found, add a High-severity migration-risk row: "SP source NOT in-repo — request catalog from owner." (3) Ask for: (a) SP script files, (b) a SP-usage audit (which procs are live vs dead), (c) DDL for referenced tables. (4) Don't finalize migration-effort estimates until the SP catalog is on the table — add a "pending SP inventory" caveat to Section 10 of the brief. (5) Applies to any stack with legacy DB coupling: PHP + stored procs, Java + procs, Python + procs, old Perl CGI + procs. Reference: LEGACY_RE_BRIEF_ES.md Section 9 + Section 11 risk table.

497. **When a repo handover includes URL.txt (or REPO.txt) pointer files, clone INTO the folder next to each pointer — not into a custom scaffold** — On 2026-04-22 the handover dropped `Archiving System/URL.txt` and `Electronic Services/URL.txt`, each pointing to a GitHub repo. The user's intended layout was `{subfolder}/TT{System}/` as a sibling of URL.txt. I cloned into `Archiving System/_legacy_audit/TT*` instead, which forced a mid-session move + sed rewrites across MD + HTML + JSON + `_refresh.sh` (cost ~40 min of rework). **Why:** pointer files are an implicit contract — they say "the repo belongs here." Ignoring that creates two divergent folder trees, confuses future refresh scripts, and forces hot-swap moves once the user notices. **How to apply:** (1) Before any `git clone`, scan the working directory and its sibling directories for `URL.txt`, `REPO.txt`, `repo-url.txt`, `.repo`, or any empty folder whose name matches the repo name in the URL — that's the target. (2) If an empty placeholder folder exists at the target, clone into it (`git clone <url> <empty-dir>` works on existing empty dirs). (3) If a sibling already contains a populated clone from a parallel Claude session, use it as canonical — do not duplicate. (4) Save the `_refresh.sh`-style refresh paths as CONFIG in a JSON file (not hardcoded into the script), so a move costs one JSON edit not three files. (5) If the user's URL.txt says repo A goes at location X and you want to clone elsewhere, ASK — don't substitute.

498. **Commercial NuGet / pip / npm packages in dependency manifests are hidden rebuild cost drivers — scan dependency files early, surface in the scoping document** — On 2026-04-22, `TTArchivingSystem/UIWeb/packages.config` declared `Aspose.Words 19.4` (commercial annual license) + `LPTPrinting` / `LPTPrinting32` VB-authored DLLs (legacy dot-matrix/line printer support — hardware-binding). Both change 5-year TCO materially and are easy to miss in a first-pass "what framework is this?" scan. **Why:** a migration estimate built from LOC + framework + DB engine alone will miss the annual Aspose renewal cost AND the hardware-printer preserve-mode engineering that the rewrite must replicate. Both items belong in the Decision Matrix TCO row and the Risk Register. **How to apply:** (1) For any .NET-legacy assessment, grep every `packages.config` + `*.csproj` `<PackageReference>` for: `Aspose`, `DevExpress`, `Telerik`, `Syncfusion`, `ComponentOne`, `Infragistics`, `EPPlus` (commercial since v5), `GemBox`, `ActiveReports`, `Stimulsoft`, `Iron*` (IronPdf, IronOcr), `Spire.*`. (2) For Python: check `requirements.txt` / `pyproject.toml` for `reportlab` (commercial for advanced features), `PyPDF2`-vs-`pypdf`, any `*-pro` or `*-enterprise` suffix. (3) For Node: `package.json` for `@devexpress/*`, `@telerik/*`, any `-pro` suffix. (4) Also check for legacy printing/scanning DLLs (LPT*, TWAIN*, WIA*) — these imply hardware coupling and preserve-mode work. (5) Flag every hit in a "Hidden Commercial / Hardware Dependencies" section of the rebuild-scoping document with: current license cost, replacement open-source option, replacement engineering effort in weeks.

499. **Runtime dynamic form builders (Blueprint + BlueprintField pattern) do NOT map to Odoo Studio — this is the #1 Odoo gap for DMS / ESM / eGov migrations** — Odoo Studio is **design-time**: an admin drags fields in a UI and the schema persists. The Blueprint/BlueprintField pattern in the 2026-04-22 TTElectronicServices code is **runtime**: field definitions live as data rows (`Blueprint`, `BlueprintField`, `BlueprintFieldValidation`, `BlueprintSubmitValidation`), rendered on-the-fly per request, with validation rules evaluated from DB. Odoo coverage for this domain in the v1 analysis: **35 %**. **Why:** many government and enterprise legacy systems (Qatar Charity, ministries, banks) use this runtime pattern because non-IT staff author forms without deploying code. If the rebuild recommendation is Odoo, Studio cannot cover it without a heavy custom module that reinvents Form.io — which collapses the "Odoo covers X %" argument. **How to apply:** (1) When auditing any DMS / eGov / ESM / service-request legacy system, search entity names for `Blueprint`, `FormDefinition`, `DynamicField`, `FieldSchema`, `FormTemplate` — match on runtime-form pattern. (2) If found, add a "Dynamic Form Builder — Runtime vs Design-time" block to the Odoo Coverage Map. (3) Recommend **Form.io**, **django-forms-builder**, or **formio-builder** on the Django/Next side — they map 1:1 to the runtime pattern. (4) Flag this explicitly in the Decision Matrix under "Customizability — hard domains" — it's usually the tie-breaker. (5) Do not describe Studio as "equivalent" — call it "design-time only" and quote the gap %.

500. **Outlook .msg attachments can show 0 bytes while the actual file exists as a sibling in the parent folder — always check before declaring an attachment missing** — On 2026-04-22, Basha's `.msg` (extracted via Outlook COM) reported `Archive-ES-SolutionPages.xlsx (0 bytes)` as the embedded attachment, but a full 26 KB copy existed at `D:\Archiving & Electronic Services System\Archive-ES-SolutionPages.xlsx` as a sibling of the `.msg`. This is an Outlook rendering / COM-projection artifact when an attachment was saved out-of-band, not file corruption. **Why:** a plan built on the assumption "the attachment is missing, we cannot read it" would have been delayed or scoped wrong. The sibling copy was the authoritative source and parsing it unblocked the entire Excel-deep-study phase. **How to apply:** (1) When `.msg.Attachments[i].Size == 0`, do NOT declare the file missing. (2) Search the `.msg`'s parent folder (and one level up) for files matching the attachment's `FileName`. (3) If found, document the quirk in the plan's Assumptions section: "treating the N-byte folder copy as authoritative; .msg embedded copy shows 0 bytes (Outlook artifact)." (4) Save both the `.msg` body-hash AND the folder-copy-hash into your change-detection metadata (`msg_metadata.json`) so future refresh can detect new emails from the same thread. (5) This also applies to embedded OLE objects and linked files in Word/Excel — always probe the parent folder for real siblings before giving up. (6) Specific COM invocation: `New-Object -ComObject Outlook.Application; $ns.OpenSharedItem($path); foreach ($a in $msg.Attachments) { Write-Output "$($a.FileName) ($($a.Size) bytes)" }` — the Size field is the unreliable one.

501. **Windows empty-but-locked folder: use `git clone <url> <empty-dir>` instead of fighting the lock** — On 2026-04-22 an empty placeholder folder (`Archiving System/TTArchivingSystem/`, 0 files, no `.git`) refused both `rmdir` ("Device or resource busy") and PowerShell `Remove-Item -Recurse -Force` ("Cannot remove the item ... because it is in use"). Root cause was a terminal `cd`'d into the folder OR Windows Explorer selecting it — leaving a handle that `handle.exe` wasn't in PATH to identify. **Why:** the standard fix ("find the process and close it") is time-costly and needs user cooperation. `git clone <url> <dir>` allows cloning into an existing empty directory (Git 2.14+), so you can populate the folder WITHOUT first deleting it. Saved ~15 min of "close your VS Code / Explorer / terminal" ping-pong. **How to apply:** (1) When a Windows folder operation blocks with "Device or resource busy" / "in use" and the folder is empty or mostly-empty, try an IN-PLACE operation instead of delete-then-create. (2) For a locked empty target: `git clone <url> "<locked-empty-path>"` — works. (3) For a locked partially-populated target: move content OUT file-by-file (usually a few files aren't locked even if the folder root handle is), then clone in. (4) For a locked folder that's neither empty nor clone-able: rename the PARENT folder (Rename-Item sometimes bypasses handle locks that Remove-Item cannot), then work with the renamed copy. (5) General principle: on Windows, prefer in-place transforms (write-over, clone-into, rename-parent) over delete-then-create. (6) Cross-reference: OS-level `handle.exe` from Sysinternals identifies the locking process, but is rarely worth installing mid-session.

502. **Parallel Claude sessions in the same working tree — detect by mtime, never assume unknown files are mine** — On 2026-04-22 during a 2-hour session, new files appeared in the project tree that I did not create: `LEGACY_RE_BRIEF_ES.md` + `.html` at `Electronic Services/` (34 KB + 60 KB), a `_inventory/` folder with 5 analysis `.md` files inside `TTElectronicServices/`, and a fresh `CLAUDE.md` at the parent root. These were from a parallel Claude session the user was running concurrently. Twice I almost touched them before checking mtime. **Why:** auto mode + multiple Claude windows open on the same project is a normal user pattern. If I assume every new file is mine and edit or rename it, I destroy the parallel session's work AND confuse the user about which session owns what. **How to apply:** (1) On entering a long session, record a mtime baseline: `find <workdir> -newer /tmp/claude-session-start.marker` (or equivalent). (2) Every 30 min or after each major phase, re-run `ls -la` and compare — any file with mtime AFTER my last known-edit-to-that-file needs classification. (3) Classify unknown-origin files into three buckets: (a) user's manual work → leave alone, (b) parallel Claude session output → read for context but do not edit, (c) tool artifact (linter, IDE) → usually safe to leave. Ask the user when (a) vs (b) is unclear. (4) Cross-reference `_inventory/`-style analysis folders as the user's parallel-session output that COMPLEMENTS mine — they are peers to my deliverable, not inputs I own. (5) Before any `rm -rf` or bulk `Edit`, re-check the folder manifest. (6) On the summary deliverable, note: "parallel artefact detected at X — NOT modified by this session" so the user has a clean handoff trail. (7) If the parallel session has produced a competing deliverable (same topic, different angle), propose a reconcile step before either ships to stakeholders — do not silently assume mine supersedes.

## Single-File HTML Apps & Browser-Direct Azure DevOps Pattern (Apr 27, 2026)

503. **Default to browser-direct PAT pattern (single HTML, no PS server) for any new local Azure DevOps tool — server-backed is legacy** — On 2026-04-27 the PBI Subtask App was first built as 3 files (Run-Subtask-Server.ps1 + Start-App.bat + HTML on localhost:5060) following the Outlook v9-v13 listener pattern. User immediately asked to collapse it to ONE HTML file calling Azure DevOps REST directly from the browser with PAT auth. The collapse worked end-to-end: GET (fetch work item) AND PATCH (create child task with JSON-Patch body) both pass through CORS from `file://` with `Authorization: Basic btoa(':' + PAT)`. **Why:** server-backed adds 3 moving parts (PS window must stay open, port management, BAT launcher), zero benefit for read+write Azure DevOps operations once you accept PAT-in-localStorage auth. Server pattern is appropriate ONLY when the tool needs file-system access beyond File API, an unauthenticated origin proxy, or executing CLI tools (e.g., `az` for `az rest`). **How to apply:** (1) For any new local PM/DevOps tool against `dev.azure.com/TaqaTechno`, default to single-HTML browser-direct. (2) Reuse the localStorage key `devops-pat` workspace-wide — same key the OKR v7, Weekly Meeting v7, and PM Handling v2/v3 dashboards use, so user pastes the PAT once and it works in every dashboard. (3) Auth helpers to copy verbatim (from `D:\Project Management\OKR & KPI\OKR_KPI_Dashboard_v7.html` lines 2586-2587): `function getPAT() { return localStorage.getItem('devops-pat') || ''; }` and `function getAuthHeader() { return 'Basic ' + btoa(':' + getPAT()); }`. (4) PAT modal pattern at lines 1640-1650 of OKR v7 — paste-to-localStorage, show last-4-chars confirmation. (5) Read pattern: `GET https://dev.azure.com/{org}/{project}/_apis/wit/workitems/{id}?$expand=all&api-version=7.1`. (6) Write pattern: `POST https://dev.azure.com/{org}/{project}/_apis/wit/workitems/$Task?api-version=7.1` with `Content-Type: application/json-patch+json` and a 5-op JSON-Patch body for Title / AssignedTo / IterationPath / AreaPath / Hierarchy-Reverse parent link. (7) Required PAT scope for write: **Work Items: Read & write**. (8) CORS fallback if `file://` is blocked by a hardened browser policy: `python -m http.server 8000` from the folder, open via `http://localhost:8000/...html` — same HTML, different origin, no code change. (9) Server-backed pattern (Outlook v9-v13, OKR Data Gate `_run_data_gate.sh`) stays valid for tools that genuinely need shell execution; do not migrate those to browser-direct without need.

504. **"Our PBI subtask approach" lives in TWO files, not one — sprint-plan.md + create-subtasks.ps1 must be read together** — On 2026-04-27, building the PBI Subtask App, RAG returned `~/.claude/plugins-main/devops-plugin/devops/sprint-plan.md` as the canonical "approach" doc. It defines the 6 templates (`[Dev]`, `[Front]`, `[Front Bug Fixing]`, `[QC Test Execution]`, `[QC Bug Fixing]`, `[IMP]`) and a generic role-default mapping (e.g., `[Dev]` → Shehab Gamal). But `D:\DevOps\create-subtasks.ps1` overrides this for BMS (uses Hala Ibrahim, not Shehab) and Wallet's QC routes to Afthab P A (not Mostafa Ahmed). **Why:** if a future tool extracts "the approach" from MD alone, it would assign BMS Dev tasks to the wrong developer (Shehab, who doesn't work BMS) and Wallet QC to Mostafa (who doesn't dedicate to Wallet). MD has the framework; PS1 has the project-specific routing rules that MD never absorbed. **How to apply:** (1) When user references "our PBI subtask approach" or "sprint plan approach", read BOTH `~/.claude/plugins-main/devops-plugin/devops/sprint-plan.md` AND any PS1 scripts under `D:\DevOps\` matching `create-subtasks*.ps1` / `*subtask*.ps1` / `Preview-Subtask*.ps1`. (2) Treat the PS1 as authoritative for projects it explicitly covers; treat MD defaults as authoritative for projects not in any PS1. (3) Currently encoded overrides (as of Apr 2026): BMS Dev = Hala (not Shehab); Wallet QC Test + QC Bug = Afthab P A (not Mostafa); all other projects = MD defaults. (4) Capture this in any new PBI-touching tool's project catalog as JS/PS1 constants, NOT inline strings — so adding/changing a project rule is one place. (5) When MD and PS1 disagree, the PS1 wins because it's the actually-running script; flag the divergence to the user once, then update sprint-plan.md to absorb the override (long-term path: MD = single source of truth, PS1 = thin wrapper).

505. **Inline SVG data-URI favicon preserves the "single-file HTML" guarantee** — On 2026-04-27 user asked for a browser-tab favicon on the PBI Subtask App without breaking the single-file deliverable. Solution: `<link rel="icon" type="image/svg+xml" href="data:image/svg+xml;utf8,<svg ...>...</svg>">` with the SVG URL-encoded inline (escape `<` → `%3C`, `>` → `%3E`, `#` → `%23`; use single-quotes inside the SVG for attributes to avoid `"` escaping). **Why:** any sibling `.ico` / `.png` favicon would break the "double-click one HTML and go" promise, force the user to track an extra file, and silently fail when the HTML is moved without its sibling. Inline SVG scales infinitely (no rasterization), works in every modern browser, and survives email/Slack attachment-share intact. **How to apply:** (1) For any single-file HTML deliverable that needs a tab icon, use inline SVG data URI in the `<head>`. (2) Design at viewBox `0 0 64 64` for sharp 16-32-64 px rendering. (3) URL-encode `<`, `>`, `#`. Single-quote SVG attributes to dodge `"` escaping. Spaces, slashes, equals signs need no encoding inside `;utf8,` data URIs. (4) Color palette tip: match the brand color used elsewhere in the HTML (e.g., the primary button) for visual cohesion. (5) Hard-cap SVG size at ~600 chars URL-encoded — anything bigger and the page-source diff noise outweighs the favicon value; switch to base64 PNG instead. (6) PNG fallback for browsers older than IE-edge-2017 is a separate `<link rel="icon" type="image/png">` line — almost never needed in 2026. (7) Cross-reference: same pattern works for app-icon emoji approximation (`<text>` SVG with a single Unicode emoji at large font-size) when a vector design is overkill.



## MD-to-HTML Parser Pitfalls (Apr 27, 2026)

506. **Greedy `\s*$` at end of a heading regex consumes line breaks past the heading when the input has mixed CRLF/LF endings — use `[ \t]*$` instead** — On 2026-04-27 the Phase 3 HTML-builder script `_apply_snapshot_refresh.js` parsed `_portfolio_narratives.md` to extract `## ProjectName` and `### Where/Next/Risk` sections. The regex `^##\s+(.+?)\s*$/gm` was used for project-heading detection. With mixed line endings (Promote-Digests writes CRLF; Inject-ArTranslations.ps1 wrote LF only via PowerShell `` `n ``), `\s*$` greedy-matched `\r\n\r` (CR + LF + next-line CR) instead of just `\r`. This added 2 chars to `pm[0].length`, which shifted `starts[i+1].idx` forward by 2. Boundary subtraction (`starts[i+1].idx - "## NAME\n".length`) was off by 2, leaking `##` from the next project's heading into the EN narrative content. **Why:** `\s` matches all whitespace INCLUDING `\r` and `\n`. With `m` flag, `$` anchors to before any `\n`, so a greedy `\s*` will happily consume across multiple lines until it finds an endpoint where `$` is satisfied — typically the next `\n`. The bug is silent on pure-LF or pure-CRLF input but blows up on mixed input. **How to apply:** (1) For any line-anchored heading regex, use `[ \t]*$` (horizontal-only whitespace) instead of `\s*$`. (2) When extracting "everything after this heading until before the next heading," prefer storing `pm.index` of the next heading and using THAT directly as the end boundary — never subtract a string-template length, that's brittle to line-ending differences. (3) Detect mixed line endings on input early: `(crlfCount, lfOnlyCount)` — if both > 0, normalize first via `.replace(/\r\n/g, '\n')` before parsing. (4) PowerShell scripts that emit MD must explicitly use `"\r`n"` in string literals if the surrounding file is CRLF, OR explicitly normalize via `Set-Content -NoNewline` + manual line endings. (5) In Node-side parser tests, run on three fixtures: pure-LF, pure-CRLF, mixed (LF inside CRLF). (6) Verbose-debug helper that catches this fast: `console.log('pm0Last:', JSON.stringify(pm[0].slice(-3)))` — a value like `"\r\n\r"` immediately reveals the over-consume. Reference: `_apply_snapshot_refresh.js` parseNarrativesMd + parseProjectSections.

507. **Detect parenthesized header variants (e.g., `(AR)`, `(EN)`, `(draft)`, `(new)`) BEFORE stripping parens — strip-then-match destroys disambiguation** — On 2026-04-27 `normaliseHeader('Where (AR)')` was implemented as: lowercase → `s.replace(/\([^)]*\)/g, '')` → switch on cleaned string. This stripped `(AR)` before the AR check ran, so `Where (AR)` reduced to `where` and was treated as a duplicate of EN `Where`. Result: AR fields stayed empty across all 24 cards because the duplicate-merge logic kept the first occurrence (the EN one). **Why:** generic strip-parens handles "decorative" annotations like `(new)`, `(previous)`, `(draft)` that should NOT change the field's identity. But `(AR)` IS the identity discriminator — stripping it collapses two distinct fields into one. **How to apply:** (1) In any header normalizer, run identity-bearing-suffix tests FIRST: `if (/^where\s*\(ar\)$/.test(s)) return 'whereAr'`, similarly for `(en)`, language tags, kind tags. (2) Only AFTER those tests, strip generic decorative parens. (3) Same rule applies to filename normalizers (`report-v2.docx` vs `report (final).docx` — version vs annotation), URL slug builders, and any parser that uses suffix-stripping. (4) Test fixture: feed the parser pairs of inputs differing only in the identity suffix and assert they produce DIFFERENT keys. (5) The bug surfaces silently as "data missing" — the cleaner symptom — so include "every field expected to be populated IS populated" assertions in any post-parse validator. Reference: `_apply_snapshot_refresh.js` normaliseHeader, fixed Apr 27.

508. **Heading-content boundary calc: prefer `nextHeading.pmIndex` directly over `nextHeading.startAfter - "### NAME\n".length` subtraction** — On 2026-04-27 the parser computed each section's content end as `endIdx = hits[i+1].start - ('### ' + hits[i+1].title + '\n').length` where `hits[i+1].start = pm.index + pm[0].length`. The subtraction was supposed to walk back from "after the next heading" to "right before the next heading". But the walk-back used a hand-built template string (`### NAME\n`) whose length didn't match what `pm[0]` actually consumed — the regex's greedy `\s*$` consumed `\r` too, making `pm[0].length` larger than the template length. Net effect: section content boundary off by N chars where N = `pm[0].length - template.length` (typically 1-3). **Why:** any time you compute "position before X" by subtracting a hand-coded length from "position after X", you're assuming what the regex consumed equals what you're subtracting. That assumption breaks the moment the regex matches `\r` or trailing whitespace beyond the heading text. **How to apply:** (1) Always store `pm.index` (the position WHERE the next heading STARTS) alongside `pm.index + pm[0].length` in the hits array. (2) Use `nextHit.pmIndex` directly as the previous section's `endIdx` — no subtraction. (3) This idiom is robust across all encodings, line endings, and trailing-whitespace policies. (4) Same bug class affects: chunk-extraction in chunkers, comment-strippers, MD-section walkers, code-fence parsers. Anywhere you write `start_of_next - "delimiter".length` to find "end of previous", refactor to "use start_of_next directly." (5) Memoize: "boundary by next-start-position, never by next-content-length-subtraction." Reference: `_apply_snapshot_refresh.js` fixed Apr 27 — both outer (project) and inner (subsection) boundary calcs migrated.

509. **When the same key (like a project name) appears in BOTH a runtime helper map AND the main data object, scope all `findBlock(html, key)` lookups to the main object's bounds — never search the whole HTML** — On 2026-04-27 the Phase 3 HTML-builder injected an `ALAQRABOON_CARD_MAP` helper near the top of the `<script>` block: `window.ALAQRABOON_CARD_MAP = { 'KhairGate - Alaqraboon Center': { devops: '...', areaPath: '...' }, ... }`. Later, the same key `'KhairGate - Alaqraboon Center': {` appears inside `var PORTFOLIO_NARRATIVES = {` further down. When Phase 3 was re-run on the previous version of the HTML to apply a new snapshot, `findProjectBlock(html, 'KhairGate - Alaqraboon Center')` did `html.indexOf('\'KhairGate - Alaqraboon Center\': {')` and found the HELPER occurrence FIRST (lower file offset). It then balance-matched the closing brace and replaced the helper's `{ devops, areaPath }` config with the new EN+AR narrative block, corrupting the helper AND leaving the actual `PORTFOLIO_NARRATIVES` entry untouched. Symptom: dashboard's Portfolio Status renders Center/Wallet cards with empty `whereAr/nextAr/riskAr`. **Why:** any HTML that mixes "config maps" and "data maps" using JS object literals with the same key name will hit this collision the moment a generic key-search runs. The first run is fine because no helpers exist yet; the SECOND run (after helpers were injected) is when the bug manifests — making this a "self-corrupting" subtle bug that passes naive testing. **How to apply:** (1) Before any `findBlock(html, key)` lookup, locate the bounds of the target object first: `findBoundsOf('var DATA_MAP = {')` returns `{startIdx, endIdx}`. (2) Pass those bounds to `findBlock`, which uses `html.indexOf(marker, bounds.startIdx)` and rejects matches `> bounds.endIdx`. (3) Generalize the rule: ANY HTML or JS file that gets injected with auxiliary data structures (helper maps, manifest arrays, config objects) needs scoped lookups for ALL key-based mutations. (4) Detection: if your build script injects a helper that uses object keys matching your data keys, wrap a regression test that runs the script TWICE on the same input and asserts the helper's structure is unchanged after the second run. (5) Alternative architecture: prefix helper keys to make them disjoint from data keys (e.g., `'card:KhairGate - Alaqraboon Center'` in the helper, plain key in the data). Less safe than scoped lookups (relies on convention). (6) For the JS object case specifically, balance-match the OUTER object's `{...}` once on first encounter, cache the bounds, then ALL subsequent inner-key lookups go through the bounds-restricted helper. Reference: `_apply_snapshot_refresh.js` `findPortfolioNarrativesBounds` + `findProjectBlock`, fixed Apr 27.

## Git Merge Strategy & Pre-Merge Inspection (Apr 27, 2026)

510. **Always run `git log A..B --stat` on BOTH diverging sides before picking merge strategy — file-level overlap is what determines conflict risk, not directory or commit-message overlap** — On 2026-04-27 in the Alaqraboon repo, local had 5 unpushed RAG-restructure commits and remote had 3 V3-rollout commits. Both sides' commit messages mentioned `backlog_v2` (local: "restructure backlog_v2.md"; remote: "Embed 496 open questions inline under user stories in backlog v2"). Looked like a guaranteed conflict on the same file. Pre-merge inspection via `git log origin/master..HEAD --stat` and `git log HEAD..origin/master --stat` revealed **zero file overlap**: local touched only `.md` files, remote touched only `.html` files. The commit messages mentioned the same logical concept ("backlog v2") but the actual file extensions differed. Merge ran clean with the `ort` strategy, no conflicts, no manual resolution. **Why:** commit messages describe intent; file paths describe what actually changed. A fix-up phase that updates "the backlog" might touch the markdown source on one side and the rendered HTML on the other — appearing identical at the concept level but disjoint at the file level. **How to apply:** (1) When `git pull` reports diverging branches, run `git log A..B --stat` BOTH directions before deciding merge/rebase/cherry-pick/branch-and-PR. (2) If the file-stat output shows no overlapping paths, default to plain `git merge --no-ff` — it will succeed without conflicts. (3) If there IS overlap, drill into the specific overlapping files with `git log A..B -p -- <path>` to see if the line-ranges overlap (e.g., one side adds at top, other at bottom — still safe). (4) Stash uncommitted in-progress edits BEFORE the pull/merge so they don't conflate with merge resolution. (5) After merge, verify no unexpected changes via `git diff --stat HEAD` — should be empty. (6) Save 30+ minutes of avoided manual rebase, branch creation, or stash juggling per occurrence.

511. **Markdown pseudo-heading-to-real-heading promotion is NOT safe-mode for inline labels/badges/meta lines — automated bold-to-H2 must check document-outline impact before applying** — On 2026-04-27 a previous rag-plugin md-rag-enhance run promoted multiple inline labels to `## H2` headings across `response_to_aq_analysis.md` and 4 `website-demo/*.md` files: case category badges (`**Urgent -- Medical**` → `## Urgent -- Medical`), price labels (`**500 QAR/month**` → `## 500 QAR/month`), news date meta (`**Mar 28, 2026 -- Partnership**` → `## Mar 28, 2026 -- Partnership`), and answer-list intro labels (`**Why 4 months is achievable:**` → `## Why 4 months is achievable:`). All were broken: (a) **heading without a section** — H2 outranks the H3 it sits under (`### Case #AQ-2026-0471` → followed by `## Urgent -- Medical` — the case description re-parents under the badge instead of the case); (b) **duplicate headings across files** — `## Mar 28, 2026 -- Partnership` appeared in BOTH `index.md` and `news.md`, breaking chunker identity (the chunker uses heading paths as identifiers); (c) **fragmented numbered structure** — promoting a sub-list intro inside `## 6. Timeline & AI Compression` made it a peer top-level section, breaking the 1-13 numbered outline. **Why:** the rag-plugin pseudo-heading rule has a precondition the safe-mode check doesn't enforce — the bold line must act as a section divider followed by content that BELONGS under it. Inline badges, meta lines, and intro labels do NOT meet this precondition. The chunker sees the wrong outline and the document renders with broken card layouts. **How to apply:** (1) Before promoting `**X**` to `## X`, check three signals: (a) is X followed by 1+ paragraphs of content that would not fit under the surrounding heading? (b) does X uniquely identify content (no other file/section has this exact heading)? (c) is X NOT a numbered/ordered list intro (`Why X:`, `How to apply:`, etc.)? (2) If ANY answer is "no", leave as `**X**` — it's a label, not a heading. (3) Add MD036 (no-emphasis-as-heading) and MD026 (no-trailing-punctuation in heading) to the linter — but treat them as informational, not enforcement (the symptom of correctly-restored badges IS MD036). (4) Auto-promotion runs should output a "structural findings" report listing every promotion candidate with its surrounding context — the human PM accepts/rejects each one. Never apply silently. (5) Reverse trigger to detect post-hoc: `grep -rn "^## .*:$" docs/` — colon-terminated H2s are usually wrong promotions. (6) For website demo / public-facing MD that gets converted to HTML, the rendered side is the source of truth — verify HTML output matches markdown intent before accepting any "chunker hygiene" change. Reference: Alaqraboon repo Apr 27 — 16 wrong promotions reverted across 5 files.

512. **PowerShell `-replace` regex on Windows .md files has three failure modes the docs don't warn about** — On 2026-04-27 batch-reverting 16 pseudo-heading promotions in the Alaqraboon repo via PowerShell hit all three: (a) **`\s*$` consumes `\n`** — pattern `^## (X)\s*$` with replacement `**$1**` ate the blank line below each label because `\s` includes `\n` and `$` doesn't anchor before-only. The original pre-promotion content had a blank line between badge and following paragraph; after my regex it was gone. Fix: use `[ \t]*$` for line-end whitespace, OR plain `$`. (b) **`\r?\n` doesn't match standalone `\r`** — when files have mixed terminators (CRLF + CR + LF, common after intermediate edits by mixed tools), regex `\r?\n` skips standalone-`\r` lines, leaving regex-pattern fragments orphaned across the wrong line boundaries. After my regex pass on `index.md`/`news.md`, every label had its closing `**` orphaned on a separate line. Fix: normalize to a single line ending FIRST (`$c -replace "\r\n", "\n" -replace "\r", "\n" -replace "\n", "\r\n"`) then run pattern. (c) **`[System.IO.File]::WriteAllText` fails on memory-mapped files** with `"The requested operation cannot be performed on a file with a user-mapped section open"` — VS Code's markdown preview, Sublime's file-watcher, or any editor with the file open can hold an exclusive map. Fix: write to `$f.tmp`, then `Move-Item -Force -Path $tmp -Destination $f` (atomic replace bypasses the lock because it's a directory operation, not a file open). **How to apply:** (1) For any PowerShell regex-on-MD batch script, START with line-ending normalization, FINISH with atomic temp-file replace, NEVER use `\s*$` for end-of-heading anchors. (2) Pre-flight check: `file <path>` reveals mixed terminators (`ASCII text, with CRLF, CR, LF line terminators` is the smoking gun — react before running regex). (3) Test fixture for any new MD-mutating script: 3 inputs (pure-LF, pure-CRLF, mixed) × 1 expected output → assert all three converge to the same diff. (4) The "fixed" / "no change" log line per file is necessary but insufficient — also `git diff --stat` after the script runs to catch line-ending churn (372-line diff for a "small" change is the symptom). (5) For unattended scripts (cron/CI), additionally `git diff --check` to catch trailing whitespace and merge markers introduced by regex slop.

740. **Windows PowerShell 5.1 reads `.ps1` files as cp1252 by default — non-ASCII characters (em-dash, Arabic, accents) inside the script body must be saved with UTF-8 BOM** — On 2026-04-28 the second weekly run of `Build-NarrativeDigest.ps1` failed with parser errors like `Unexpected token 'no' in expression or statement` and `The hash literal was incomplete`. Root cause: the file contained literal em-dash `—` (U+2014) inside string literals like `"Newly split card — no previous narrative."`. UTF-8 encodes `—` as 3 bytes `0xE2 0x80 0x94`. Windows PowerShell 5.1 (the default on most Windows machines, invoked by `powershell.exe`) reads `.ps1` files as cp1252 unless the file has a UTF-8 BOM (`0xEF 0xBB 0xBF`). The 3 bytes get re-interpreted as the cp1252 sequence `â€"`, which the PS parser tokenizes as garbage and bombs the syntax check. The previous (working) run a week earlier may have been launched from PS Core (`pwsh.exe`) or a session that happened to default to UTF-8 — the failure is intermittent depending on launch context. **Why:** any PS1 written by a UTF-8-default tool (VS Code, Sublime, modern editors, AI coding agents like Claude) without explicit BOM will load fine in PS Core but break under PS 5.1. The break is silent on pure-ASCII files, surfaces only when the script body contains non-ASCII glyphs. **How to apply:** (1) For any `.ps1` that targets Windows machines and contains non-ASCII characters in string literals or comments, save it with UTF-8 BOM. PowerShell snippet that fixes a file in-place: `$c = Get-Content $path -Raw -Encoding UTF8; [System.IO.File]::WriteAllText($path, $c, [System.Text.UTF8Encoding]::new($true))`. (2) Pure-ASCII PS1 files don't need BOM — but if you ever add a non-ASCII char later, the re-save is required. (3) Defensive alternative: replace non-ASCII glyphs with ASCII fallbacks (em-dash → `--`, smart quotes → `"`, ellipsis → `...`). Less elegant in output but encoding-immune. (4) For non-ASCII glyphs you genuinely need (Arabic strings, foreign names), use the BOM approach — string-replace would lose semantic meaning. (5) Cross-check via `file <path.ps1>` — if it says "UTF-8 text" without "(with BOM)", PS 5.1 will struggle. (6) When invoking from a wrapper, prefer `& powershell -ExecutionPolicy Bypass -File $script` over dot-sourcing — the parser context is consistent across the chain. (7) Same rule applies to `.psm1`, `.psd1`, and any file PS-parses as a script. Reference: `Build-NarrativeDigest.ps1` failure 2026-04-28; fixed by mass BOM-application across all 5 pipeline scripts.

## V16 Drift Fix Session — Lessons (Apr 28, 2026)

774. **HTML rendering bugs that pass content review — two rules: (a) nested `<a>` inside `<a class="card">` silently breaks grid layouts (HTML5 parser auto-closes outer `<a>` when it encounters inner one; remaining card content becomes orphan siblings of grid container, collapsing 2-column to single-column); (b) post-push browser visual check is mandatory for ANY card/grid/table change — file-content review can't catch parser-level fix-ups (auto-closing nested anchors, malformed table closures, illegal nesting) that are invisible in source view but transform DOM at render time** — Two patterns from Apr 28 V16 Drift Fix session. **(a) Nested anchor in card grids:** Content models in HTML spec disallow descendant interactive content inside `<a>`; browsers enforce by parser-level fix-up, not by rendering error. The outer `<a class="card">` gets auto-closed when the inner `<a>` is encountered, then remaining card content becomes orphan siblings of the grid container — collapsing a 2-column `cards-2` (or any `repeat(2,1fr)` grid) to single-column with empty placeholder cells visually broken next to populated cards. **Rules:** Never put `<a>` inside `<a class="card">` even if the inner one is to a different URL. Replace inner link with `<code>` text (or move link OUTSIDE the card to a sibling element). After editing any card description, run `grep -nE "<a[^>]*>[^<]*<a " <file>` — zero results expected. Visual symptom in cards-2 grids: 1 card per row with empty placeholder next to it. **(b) Post-push browser visual check:** Even after content review, push, and audit, some HTML bugs only surface visually in browser. Today's nested-anchor bug passed file-content review (HTML "looked fine" in source) but broke cards-2 grid only when rendered. **Why:** HTML5 parser-level fix-ups (auto-closing nested anchors, malformed table closures, illegal nesting) are invisible in source view but transform DOM at render time. **Rules:** After ANY push that includes new HTML cards, links, tables, or grid layouts, do an actual browser refresh test — not just file-content review. Specifically verify grid-based layouts (`grid-template-columns`, `display:flex` with wrap, `cards-2`, etc.) where one broken card silently breaks neighbors. Click each new link/card to confirm targets are valid. Toggle EN ↔ AR (parser fix-ups can differ by content). Mobile breakpoint check (different grid behaviors at <768px). For dashboard-style pages with multiple cards in rows, count visible cards-per-row before/after the change — regression to 1-card-per-row is the smoking gun. The user's screenshot is the gold standard; before they have to send one, do the visual check yourself. **Why these two cluster:** Both are HTML rendering-layer bugs that the source-code review pipeline cannot catch. (a) is the specific failure mode (nested anchors); (b) is the general pre-emptive practice (browser visual check) that catches (a) and other parser-level fix-ups before the user does. They share the same root cause (HTML5 parser invisibly transforms DOM) and the same mitigation (don't trust source-only review for grid/card/table changes). **How to apply:** (1) Never nest `<a>` inside `<a class="card">`; replace inner with `<code>` or move outside; grep for `<a[^>]*>[^<]*<a ` post-edit. (2) After ANY push touching cards/grids/tables, browser visual check + count cards-per-row + click-test new links + EN↔AR toggle + mobile breakpoint. References: 2026-04-28 — `health-dashboard.html` Variance v13 card had GitHub commit link nested inside `<a class="card">`, broke USV3 + V13 row 3 layout — pushed bug, user spotted via screenshot, fixed by replacing nested `<a>` with `<code>5562f81</code>` (commit ac42040). V16 xhatem commit 7276542 had the bug; passed file-content review and audit; only surfaced when user opened file and screenshot-flagged empty right-column placeholders. (Folded former #513, #532.)

776. **Cross-repo push-order + git-stash hygiene — two rules for managing dependent commits across two repos and mixed staged/unstaged working trees: (a) when two repos have linked commits (one references artifacts in the other), the source-of-truth/Hub repo must push FIRST; the standalone/dependent repo pushes second — wrong order causes 404s if anyone pulls between the two pushes; (b) `git stash push -u` sweeps in BOTH WIP and legitimate today-edits — use `--keep-index` to preserve staged files in working tree** — Two patterns from Apr 28 V16 Drift Fix push session. **(a) Cross-repo push order:** When pushing related commits across two repos, decide push order based on dependency direction: which repo's content does the OTHER repo link to? Push that one first. **Why:** if the standalone references `https://github.com/xhatem/.../variance-v13.html` but xhatem hasn't been pushed yet, the link 404s for any reader between push-1 and push-2. **Rules:** Today's order: xhatem/Khairgate_analysis FIRST (sister copy of Variance v13 + health-dashboard with v13 card linking to xhatem-resident URL); then melshahedpearl/KhairGate-HUB--Kickoff (V16 standalone with primary copy of Variance v13). For pushes within minutes, the gap is usually invisible; for longer gaps (re-running audit, taking lunch), readers ARE exposed to 404s. Cross-check by grep'ing the pre-push working tree of the dependent repo for `https://github.com/<source-repo>/...` URLs — confirm those targets exist on origin BEFORE pushing dependent. **(b) `git stash push -u --keep-index`:** When mixing today's intentional changes with old WIP in the same working tree, naive `git stash push -u -m "WIP"` captures everything including the today-relevant edits you wanted to keep. **Why:** stash without `--keep-index` doesn't distinguish staged from unstaged; `-u` extends to untracked too. **Rules:** `git add <today-relevant-files>` to stage what you want to KEEP in working tree. Run `git stash push -u --keep-index -m "WIP <date> — <scope>"` — stashes only unstaged + untracked, leaves staged files in working tree. Verify with `git status` — should show only the staged files. Stash message convention: include date + scope keywords (e.g., "WIP Apr 28 2026 — RAG-plugin auto-instrumentation + BRD-15 notes (set aside; not for V16 push)") so future `git stash list | grep` can find it. **Why these two cluster:** Both are git workflow discipline rules around the same multi-repo push session. The push-order rule (a) determines WHICH commits go to WHICH origin in WHAT sequence; the stash hygiene rule (b) ensures that the working-tree state going into each push is the right subset (today's relevant edits, not yesterday's WIP). They share the same trigger (preparing a multi-file multi-repo cross-push) and the same risk class (linked-content 404s, accidental stashing of today's work). **How to apply:** (1) For two-repo cross-pushes, identify the dependency direction (who links to whom) and push source-of-truth/Hub repo FIRST; grep dependent repo's pre-push tree for `https://github.com/<source-repo>/...` URLs to verify targets exist on origin. (2) For mixed staged/unstaged working trees, `git add <today-relevant>` then `git stash push -u --keep-index -m "WIP <date> — <scope>"`; verify with `git status` shows only staged files. References: 2026-04-28 — V16 push — xhatem commit 7276542 first (3 files: health-dashboard + phase0-poc-readiness + variance-analysis-report-v13.html), then melshahedpearl commit 6891bbe (24 files V16 standalone); order matched the dependency: V16 standalone references the Hub V2 sister copy. First stash captured legitimate health-dashboard + phase0-poc-readiness edits along with 38 .md WIP; had to pop + re-stash with `--keep-index` trick to rescue them. (Folded former #514, #526.)

771. **Multi-version HTML clone hygiene — three rules for keeping v[N-1] → v[N] clones from leaking past-version state into the new release: (a) clone-and-edit workflow inherits all internal section content (past-version narratives, "V10 SSOT", "9/9 RESOLVED", "FULLY READY", "March 23-24 Findings", "v11 Value / v12 Value" comparison columns) — version-bump subagent only handles tags+dates+PS1 config, NOT section prose; (b) NEW HTML files in multi-file portals must wire into cross-ref nav before "ready for review" — orphan files are unreachable from any sibling; (c) when adding a "(LATEST)" successor card, the existing predecessor card retains stale badges that reflect its state when it WAS the latest — reframe to "HISTORICAL" / "Superseded by v[N+1]" with neutral styling** — Three patterns from Apr 28 V16 Drift Fix session. **(a) Past-version chatter on v[N-1] → v[N] clones:** version-bump is mechanical regex on tags+dates; section prose is content-domain logic outside its scope. **Rules:** After cloning v[N-1] → v[N], grep for past-version strings: `grep -nE "(V10|v10|v11|v12 historical|FULLY READY|9/9 RESOLVED|March 23-24)"`. Section-by-section rewrite of any matches with V[N]-current narrative. Update Workflow chips (chronology), drift table column headers, "Files Updated" lists, "Authoritative Numbers" row labels (e.g., "v11 Value / v12 Value" → "Pre-V[N] / Post-V[N]"). Footer line: replace "Sections X-Y are v[N-1] historical" with single-version footer. Final-callout verdict: replace "9/9 RESOLVED — FULLY READY" with v[N] verdict (e.g., "4/6 RESOLVED — 2 FLAGGED — READY pending governance"). Pre-publish audit: re-grep for ALL prior version numbers; any hit means cleanup incomplete. **(b) Cross-ref nav integration:** Creating a new file without adding it to existing sidebar/nav makes it an orphan: reviewers cannot reach it from any other file. Nav-integration is part of "done", not optional polish. **Why:** users navigate via sidebar; if the new file isn't there, it doesn't exist from their perspective. They'll either miss it entirely or have to discover URL out-of-band. **Rules:** When creating a new HTML file in a portal, immediately edit cross-ref nav in ALL sibling files to include the new entry. Decide which category the new file belongs to (ACTION / REFERENCE / AUDIT / OVERVIEW / etc.) and place it consistently. Update link count if nav has count display ("18 Links" → "19 Links"). Verify by opening any sibling file in browser and clicking the new nav entry. For dual-repo setups (e.g., melshahedpearl/Hub V2), ensure both copies (if synced) get the nav update. **(c) Stale badges on superseded cards:** When adding a "(LATEST)" tag or successor card, audit ALL related cards (sibling cards in same row, parent verdict callouts, KPI strips) for stale badges. **Why:** card status text is content-coupled to "this is the latest health verdict" framing; once superseded, the badge meaning shifts but the text doesn't auto-update. **Rules:** Reframe the predecessor: change "✓ CLEAR" / "FULLY READY" green badge to "✓ HISTORICAL" or "Superseded by v[N+1]" with neutral grey styling. Update the predecessor's description text to past-tense: "9/9 items resolved AS OF March 23" not "9/9 items resolved". Cross-link from predecessor to successor: "Superseded by v[N+1] above". The historical card should still link to underlying file (it remains a valid artifact for traceability). **Why these three cluster:** All three are clone-and-edit hygiene rules — same "I made a new version, now what?" workflow. Without rule (a), past-version narratives confuse readers; without (b), the new file is unreachable; without (c), the predecessor's stale "FULLY READY" badge contradicts the new verdict. The trigger for all three is identical: a new versioned HTML file landing in a multi-file portal. **How to apply:** (1) After cloning v[N-1] → v[N], grep for past-version strings + section-by-section rewrite. (2) Wire new file into cross-ref nav across ALL sibling files in same step. (3) Reframe predecessor card badges to "HISTORICAL" / neutral styling + cross-link to successor. References: 2026-04-28 — Variance-Analysis-Report_v13.html cloned from v12 (4 section rewrites + 9 grep iterations); READINESS_ONE_PAGER + PROJECT_ONE_PAGER created without nav links (reviewers couldn't reach them — eventually deleted per #770 but lesson stands); Variance v13 (LATEST) card added to health-dashboard.html — v12 card still showed "FULLY READY" green badge, reframed to "HISTORICAL" + "Superseded by v13" + grey styling. (Folded former #515, #522, #525.)

516. **Hub V2 KPI strip + grand totals can drift internally when a "Planned R[N]" epic is added** — Hub V2 `epic-breakdown.html` declared 23 Business Epics in KPI strip (correctly bumped 22→23 after E22 added) but kept rolled-up totals (3,931+ SP / 948 stories / 192 features) at the 22-epic baseline because E22 is flagged "Planned R3" not "Built". **Why:** when an epic is added as Planned (not yet committed to delivery), Hub V2 maintainers correctly avoid rolling its SP/Stories/Features into Built totals, but the KPI count itself bumps to reflect the epic's existence. The result is a one-page internal inconsistency. **How to apply:** (1) After any Hub V2 pull, cross-check epic-count KPI against rolled-up SP/Stories/Features; if epic count bumped but totals didn't, look for "Planned R[N]" flag on the new epic. (2) Standalone reports follow Hub V2 published numbers as authoritative until promoted-to-Built — don't roll up unilaterally. (3) Use conditional notation in standalone: "+380 SP / +24 stories / +8 features if E22 promoted" wherever totals appear. (4) Flag the discrepancy as a Hub V2-side variance finding (e.g., F-05) and signal Mohamed to roll up totals when promotion occurs. Reference: Apr 20 Hub V2 commit 5562f81 added Epic 22 (380 SP / 24 stories / 8 features) as Planned R3; KPI bumped 22→23 but totals stayed at 22-epic baseline; V16 standalone followed published numbers + added conditional notation.

775. **Wording precision in stakeholder docs — three rules that prevent overclaiming completeness, hiding open blockers, or framing options in technical jargon: (a) "100% complete from a planning perspective" is misleading when planning gates remain — conflates artifact-completeness with gate-closure; use precise dual-statement language; (b) Consolidated Blockers needs explicit 3-tier structure (Resolved / Active Prereqs / In-Flight) rather than a flat chronological mixed-callout list; (c) AskUserQuestion options must use plain-language operational descriptions, not technical abstractions (repo URLs, Git remotes, system internals)** — Three patterns from Apr 28 V16 Drift Fix session. **(a) "100% complete" wording when gates remain:** Conflates "PM documentation set is complete and Board-ready" (artifact-level) with "planning is fully closed" (gate-level). When gates like Mid-Level team assessment, BRD review by EM, or strategy decisions remain unresolved, planning is NOT fully closed even if the 13 PM artifacts are board-ready. **Why:** stakeholders reading "100% planning complete" assume Sprint 0.1 can begin immediately; if 3 prerequisites (1 planning-level + 2 execution-level) actually block kickoff, the wording misrepresents project state and erodes trust on discovery. **Rules:** Use precise language: "PM documentation set is complete and Board-ready" + "planning is not yet fully closed — N prerequisites remain (M planning-level + K execution-level)". Distinguish artifact-completeness from gate-closure in every Bottom Line / Situation / Purpose paragraph. Pre-publish: grep for "100% complete", "planning is done", "fully ready", "kickoff package complete" — any hit needs softening if gates remain. Each unresolved prereq must be classified planning-level vs execution-level. The closing sentence should specify what unblocks Sprint 0.1, not what's "done". **(b) 3-tier Consolidated Blockers structure:** Chronological mixed-callout blocker lists let readers miscount active blockers. A reader looking at "1 prerequisite blocking" tag and "FULLY READY" badge can't tell if 1, 3, 5, or 7 things are actually open. Mixing Resolved + Active + Risk-level + Cross-referenced items in one callout list obscures the actual gating count. **Why:** Boards and Directors need at-a-glance "what's open / what's done" — a flat chronological list of mixed-status callouts forces them to read every line and mentally classify. **Rules:** Replace flat callout list with 3-tier structure: **Tier A — RESOLVED** (brief recap, what's cleared); **Tier B — ACTIVE Prerequisites** (numbered, must clear before kickoff, classify each as planning/execution-level); **Tier C — IN-FLIGHT / Cross-Referenced** (parallel items + Hub V2-side risks, with cross-links to detail sections). Close with verdict roll-up: "X Resolved (A) · Y Active prereqs (B) · Z In-flight items (C). Sprint 0.1 cannot kick off until all Tier B items clear." Cross-link items in Tier C to their detail home (e.g., "see Section 6 BA Review", "see Section 6b PBI Validation"). Bilingual EN+AR; AR mirror must agree with EN tier counts. **(c) AskUserQuestion plain-language options:** Framing options in terms of repo URLs, Git remotes, or system internals causes user confusion. Their mental model is "what files end up where", not "which Git remote receives the push". **Why:** the user is the IT PM, not a Git internals expert; even technically literate users default to file/folder mental models when reasoning about deliverables. **Rules:** Phrase each option in terms of concrete file/folder operations: "stash locally", "copy to new folder X in repo Y", "commit to repo Z and push", "discard". Avoid bare technical references: "push to xhatem repo" is opaque; "push to Mohamed's source repo (Hub V2)" is concrete. Use the user's vocabulary: in this project they say "your repo" / "Mohamed's repo", not the URLs. When the user replies "I don't understand", that's the signal to re-ask with simpler ops-level framing — not to add more technical detail. Pre-flight check: would a non-Git-expert understand each option? **Why these three cluster:** All three are stakeholder-communication discipline rules for how PM docs + tooling-prompts present project state. Each fixes a different precision-failure mode: (a) overclaims completeness; (b) hides blocker count in a chronological list; (c) hides operational consequences behind technical labels. The pattern is identical: stakeholder reads message, gets wrong mental model, makes wrong decision (or asks "I don't understand"). **How to apply:** (1) For ANY "complete" claim in a stakeholder doc, distinguish artifact-completeness from gate-closure; pre-publish grep "100%/done/fully ready" + soften if gates remain. (2) For ANY blocker section, structure as 3 tiers (Resolved/Active/In-Flight) with verdict roll-up + cross-links; bilingual mirrors must agree. (3) For ANY AskUserQuestion option, phrase in user's file-folder mental model, not Git internals; pre-flight "would a non-expert understand?" check. References: 2026-04-28 — V16 KICKOFF_PREREQUISITES brief Bottom Line + Situation + Purpose row + KICKOFF_APPROVAL_EMAIL all said "package 100% complete" while Mid-Level Django assessment was in flight + BRD review pending; user spotted; softened across 5 files; KICKOFF_READINESS_GUIDE Section 5 was a chronological 4-callout block mixing R-08-resolved + Budget-pending + R-14 + R-05; rewrote as 3-tier (2/3/4) with verdict roll-up; first AskUserQuestion about 38 .md files used "push to xhatem/Khairgate_analysis" vs "push to melshahedpearl/KhairGate-HUB--Kickoff" — user replied "I don't understand your request"; re-asked with "stash locally / copy to new folder / commit / discard" → answered immediately. (Folded former #517, #518, #523.)

773. **Date-bump + subagent re-invocation discipline — two rules for keeping Modified-date metadata accurate without corrupting event-date audit trail: (a) date-bump must distinguish event-dates (immutable historical facts: "V15 moved 27 Apr", "ADRs approved 26 Mar", "commit pushed Apr 20") from page-modification-dates (mutable metadata) — never use blanket bare-date regex; use prefix-keyed sed patterns; (b) version-bump subagent doesn't re-run automatically when content edits continue past the original V[N] build day — at session-end, grep modified-today files for stale Modified-date tags and re-bump targeted** — Two patterns from Apr 28 V16 Drift Fix session. **(a) Event-date vs Modified-date discrimination:** Blanket regex like `sed -i 's/27 Apr 2026/28 Apr/g'` is dangerous because event dates must NOT bump while Modified-dates must. **Why:** event dates are immutable historical facts about when something happened; modification dates are mutable metadata about when the document was last touched. Treating them identically corrupts audit trail. **Rules:** Use targeted Edit calls per file or a precise sed pattern keyed to metadata prefix: `sed -i 's/Modified: 27 Apr 2026/Modified: 28 Apr 2026/g'` or `s/Updated: 27 Apr 2026/Updated: 28 Apr 2026/g`. NEVER use bare-date pattern (no prefix) — it will hit event dates. For Arabic mirrors: `s/تعديل: 27 أبريل 2026/تعديل: 28 أبريل 2026/g`. Post-bump verification: grep the bare bumped-from date — every remaining hit should be a legitimate event date (verify each one). Common safe-to-bump prefixes: "Modified:", "Updated:", "تعديل:", "Last Updated:", "Generated:". Common DO-NOT-bump: "moved", "approved", "hired", "committed", "pushed", "added", "released". **(b) Subagent re-invocation gap:** V16 was built with version-bump on day 1; day 2 content edits re-touched files but didn't re-stamp Modified-date. Audit trail then misleads (file mtime says Apr 28 but Modified-tag says Apr 27). **Why:** version-bump is invoked once per drift-fix orchestrator pass; subsequent same-version content edits within the same V[N] don't trigger another invocation. **Rules:** At session-end (Workflow I/J), grep all files modified-today for stale Modified-date tags: `grep -lE "Modified: <yesterday>"` on today's edited files; if any hit, re-bump dates. Either re-run version-bump subagent with TODAY=current-date (handles dates + tags + PS1 in one mechanical pass), or do targeted `sed -i 's/Modified: <old>/Modified: <new>/g'` per file. Frozen historical files (e.g., Variance v12, User-Story-Variance v3, HUB_PULL_HISTORY entries) must NOT bump — date stays at their last actual edit. Distinguish which files were content-edited today vs which were only mtime-touched (e.g., by RAG-plugin auto-instrumentation) — only bump the former. **Why these two cluster:** Both are date-management discipline rules around the same workflow boundary (multi-day V[N] builds + session-end verification). The same prefix-keyed sed pattern that protects event dates from bare-date bumps (a) is the tool used to surgically re-bump after subagent re-invocation gap (b). They share the same trigger (Modified-date refresh needed mid-version) and the same risk (corrupting event-date audit trail). **How to apply:** (1) NEVER use bare-date regex; always use prefix-keyed `sed Modified:`/`Updated:`/`تعديل:` patterns; verify post-bump that remaining hits are legitimate event dates. (2) At session-end, grep modified-today files for stale Modified-tags; targeted re-bump via same prefix pattern; skip frozen historical files. References: 2026-04-28 — V16 + FOLDER_OBJECTIVE_MAP date bump used `sed Modified:` prefix to surgically bump 14 files (11 V16 + FOLDER_OBJECTIVE_MAP) while leaving "V15 moved 27 Apr" event date intact at line 211; V16 built Apr 27 with version-bump stamping "Modified: 27 Apr 2026"; 11 V16 files content-edited Apr 28 still had stale "27 Apr" until end-of-session sweep. (Folded former #519, #527.)

770. **Coverage-matrix discipline BEFORE creating new files OR offering AskUserQuestion options OR deleting orphans — three rules that prevent both content duplication and unintentional deletion of net-new content: (a) before creating a new consolidated one-pager / executive summary, run a coverage matrix against existing files (≥80% redundancy → enhance canonical files instead); (b) before AskUserQuestion offers "create new file" as an option, run the matrix check first — otherwise the user inherits the create-it assumption; (c) before deleting orphan/redundant files, identify and migrate genuinely-net-new content to the right canonical file FIRST** — Three patterns from Apr 28 V16 Drift Fix session (READINESS_ONE_PAGER + PROJECT_ONE_PAGER 1700+ lines combined → ~95% redundant → deleted after migrating 3 net-new risks). **(a) Coverage matrix before new file:** When tempted to create new "executive summary" or "consolidated readiness" page, first audit existing files. If ≥80% of planned content already exists in canonical files, enhance those instead of creating orphans. **Why:** new files create maintenance burden (every drift fix now updates more files), navigation gaps (orphan files unreachable from existing nav), and content redundancy (same numbers in multiple places drift over time). **Rules:** Build coverage matrix BEFORE creating new file: rows = sections you plan to include, columns = existing files, cells = "✓ fully covered / ~ partially / — missing". If ≥80% rows have at least one ✓ cell, new file duplicates existing content — enhance canonical file(s) instead. Unique value should drive the decision (single-screen consolidation, print handout, exec audience). If "single-screen" is the only justification, ask whether existing nav sidebar already serves that. If you DO create the new file, integrate it into cross-ref nav of all sibling files in same step (see #771 multi-version HTML clone hygiene). **(b) AskUserQuestion + matrix:** When I'm about to ask "where should this new file live?", I'm implicitly assuming the new file is needed. That assumption deserves the matrix check from rule (a) BEFORE the question goes to user. **Why:** otherwise AskUserQuestion misframes the choice — instead of "create-or-not?" it presents "where-to-create?" as the only branch. The user inherits the create-it assumption. **Rules:** Before AskUserQuestion offering "create new file" options, run coverage-matrix check. If matrix shows ≥80% redundancy, present "do not create — enhance existing file X instead" as Option 1 (Recommended). "Create new in V16 folder" / "Create new in Hub V2" / etc. become Options 2/3 only if matrix shows file is genuinely net-new. The user's role is to choose AMONG analyzed options, not to do the analysis themselves. When the user later questions ("why are we adding them in V16?"), that's the late signal that I should have done the matrix earlier. **(c) Migrate net-new BEFORE delete:** When deleting a file that has some unique content, identify and migrate the genuinely-net-new pieces FIRST. Don't blindly delete. **Why:** even mostly-redundant files often have a few net-new items (new risks, new findings, new actions) that would be lost if deleted without migration. **Rules:** Before deletion, run section-by-section uniqueness audit: for each section, does content exist verbatim or near-verbatim in a canonical file? For sections marked "fully covered" → safe to skip (delete confirms). For sections marked "partially covered" → decide if missing piece warrants enhancing canonical file or is redundant detail. For sections marked "net-new" → migrate to right canonical file with proper format/structure. Then delete. Post-deletion verify: zero broken cross-references via `grep -lE "(deleted_filename1|deleted_filename2)" *.html`. **Why these three cluster:** All three are pre-action discipline rules around the same operation (creating-or-deleting a file in a workspace with canonical files). The cost of skipping any one is the same: orphan files, content duplication, drift, lost net-new items. The matrix is the load-bearing primitive; rules (a), (b), (c) are three operational uses of it. **How to apply:** (1) For ANY new "consolidated" file, run coverage matrix first; if ≥80% redundant, enhance canonical instead. (2) For AskUserQuestion that offers "create new" options, run matrix BEFORE the question; first option becomes "don't create — enhance X instead" if matrix shows redundancy. (3) For ANY delete of orphan/redundant file, run section-by-section uniqueness audit FIRST; migrate net-new content; verify zero broken cross-refs after. References: 2026-04-28 — created READINESS_ONE_PAGER + PROJECT_ONE_PAGER (1700+ lines combined), discovered ~95% redundancy hours later via coverage matrix audit, deleted both. Net unique content was 3 risks (Senior Django hiring delay, BRD Index review pending, Mid-Level Django assessment outcome) — migrated to PM_RISK_REGISTER as R-20/R-21/R-22 with bilingual rows + KPI strip 19→22 + Trend paragraph update, THEN deleted. AskUserQuestion offering V16 / Hub V2 / both as 3 options should have included "Don't create them — content already covered by existing files" as Option 1 (Recommended). (Folded former #520, #524, #531.)

772. **Bilingual + audit-check drift catchers — two rules that catch silent V[N] inconsistencies before stakeholder review: (a) AR-mirror sync drift is silent until language is toggled — every EN content edit needs an immediate scan of the surrounding 5-line context for a matching `lang-ar` element + update; (b) audit-check subagent needs V[N]-specific accommodation rules in the prompt when vocabulary shifts (e.g., "Active" → "Built · Planned R3 · Cancelled"), or it flags correct content as drift** — Two patterns from Apr 28 V16 Drift Fix session. **(a) AR-mirror toggle drift:** In toggle-based bilingual HTML (`body.ar [lang-en]{display:none}`), edits to one language are invisible to reviewers using the other language. After EN content edits, the matching `lang-ar` mirror element is the most common forgotten update. Visual EN review doesn't catch it (AR span is hidden until toggle), so bug only surfaces during AR review or audit-check sweep. **Rules:** After every EN content edit, immediately scan surrounding 5-line context for matching `lang-ar` element and update. Run audit-check after every batch of EN edits — its bilingual-balance check catches imbalance. Pre-publish: toggle EN ↔ AR on every modified file in browser; visually compare counts/dates/words. For numerical-only edits (e.g., bumping "1" to "3"), add a grep pattern to your edit checklist: `grep -nE "<.*lang-ar.*>.*${old_value}.*</.*>"` per file. **(b) Audit-check V[N] accommodations:** When wording schema changes between versions (e.g., "Active" → "Built · Planned R3 · Cancelled"; "21/0 BRDs" → "21 Done + 1 Planned R3 (BRD-24)"), audit subagent flags correct content as drift unless new vocabulary is passed explicitly. **Why:** audit-check has hard-coded baseline vocabulary in its definition; major schema shifts in the standalone require runtime override via prompt. **Rules:** When invoking audit-check after a version with vocabulary shifts, write a "Special accommodations for V[N]" block in the prompt listing acceptable variants: "treat as VALID: 'X', 'Y', 'Z'". For each new metric/wording, list the EN+AR variants and any equivalents the audit might encounter. Specifically call out new conditional notation patterns ("+380 SP / +24 stories if E22 promoted"), new badge wordings ("Planned R3"), new owner attributions. Without these accommodations, expect false-positive WARN findings on correctly-written V[N] content. **Why these two cluster:** Both are post-edit verification patterns that fire DURING the same audit-check sweep. (a) catches content that was edited but mirror not updated; (b) prevents the same audit-check from raising false alarms on intentional V[N] vocabulary shifts. They share the same trigger (audit-check invocation after V[N] edits) and the same goal (zero false positives + zero misses). **How to apply:** (1) After every EN edit, scan ±5 lines for matching AR mirror; toggle EN↔AR in browser pre-publish; grep for old numerical values across language pairs. (2) Before invoking audit-check after a vocabulary-shifting version, write "Special accommodations for V[N]" block with EN+AR variants of each new wording. References: 2026-04-28 — audit-check caught 2 AR mismatches that visual EN review missed: USER_STORY_STRUCTURE line 661 epic hierarchy box still said "22 ملحمة (20 نشطة)" while EN line 633 said "23 Epics (20 Built · 1 Planned R3)"; KICKOFF_READINESS_GUIDE line 769 callout still said "1 remaining prerequisite — budget" while EN line 768 said "Remaining prerequisites (2)"; audit-check post-V16 needed half-page accommodation block for "20 Built / 1 Planned R3 / 2 Cancelled" framing + "21 Done + 1 Planned R3 (BRD-24)" + BRD-24 amber-tag intentional + Senior Django ×5 → ×2 corrected. (Folded former #521, #529.)

528. **Plan-mode discipline: each new task should overwrite the plan file** — When the user starts a new unrelated task within the same session, the plan file should be overwritten cleanly, not appended to. **Why:** appending creates a stale-content cocktail that mixes concerns from prior tasks; the user reading the plan file can't tell which sections are current. **How to apply:** (1) When entering plan mode and a plan file exists, evaluate: "is this a continuation of the same task, or a new task?". (2) Same task → modify-in-place, clean up outdated sections. (3) Different task → overwrite cleanly (Write tool, not Edit). (4) The plan file is ephemeral per-task scratch space, not a session-wide append-log. (5) The orchestrator/system explicitly tells you this on plan-mode re-entry — follow that instruction. Reference: Apr 28 `cheeky-orbiting-spindle.md` was rewritten 4 times for 4 unrelated tasks (3-change post-review pass → date-bump → Consolidated Blockers rewrite → one-pager deletion). Each overwrite was correct behavior, not a problem.

530. **My own "(Recommended)" tag in AskUserQuestion carries undue anchoring weight** — When I mark an option "(Recommended)", users tend to accept without independent analysis. If my reasoning was shallow, they inherit a poor decision. **Why:** anchoring bias is real; the user trusts my framing partially because they're delegating analysis to me. **How to apply:** (1) Before tagging an option "(Recommended)", do the deeper coverage/redundancy/dependency check that would have surfaced the counter-argument. (2) When recommending, ALSO surface the strongest counter-argument inline: "(Recommended) — but trade-off X" beats "(Recommended)" alone. (3) Where uncertain, omit the tag entirely and let the user weigh options without my thumb on the scale. (4) Post-recommendation: if the user accepts and we discover the recommendation was wrong, document the lesson and improve future recommendations. (5) The user's challenge "why are we doing X?" is the early signal that my recommendation was anchored without sufficient analysis. Reference: Apr 28 recommended Option 1 ("one-pagers in V16 folder") for AskUserQuestion → user accepted → discovered hours later via the user's "why?" challenge that the one-pagers were ~95% redundant duplicates, should have been deleted from day one. The recommendation reasoning was shallow ("V16 is where the package ships"); the counter-argument ("V16 is meant to be 13 frozen artifacts, not a living dashboard") would have been stronger if surfaced.

## PMO Dashboard System Framing & Doc Architecture (Apr 28, 2026)

533. **A dashboard's main-goal statement must enumerate ALL data sources, not just the dominant one — incomplete framing erodes stakeholder trust** — On 2026-04-28 the user asked "what is the main objective for our system in PMO Dashboard?". I summarized from `PMO_Objectives.md` Section 1: "Run the IT portfolio at TaqaTechno + Pearl Pixels as one system that turns Azure DevOps activity into Board-ready outcomes." User immediately challenged: "but you think we get the data also from email, right?" — correct catch. The dashboard is **dual-source**: Azure DevOps (live API → quantitative tabs: SPI/CPI, Data Gate, SBI, Velocity, Exec Summary) AND Outlook native Categories (auto-draft pipeline → qualitative narratives in Portfolio Status, Bi-Weekly Review prep, Sunday Talking Points, Action Tracker context). Section 1 had been written DevOps-first; Section 4 Layer 6 already listed Outlook Email Analysis Report v13 and the auto-draft pipeline, but the headline goal didn't reflect it. **Why:** stakeholders read Section 1 as the canonical statement of system purpose. If it names only one input source, they assume the others are tooling support, not co-equal data sources. When they later discover the system is dual-source from someone else, the trust loss is asymmetric — they wonder what else is mis-framed. **How to apply:** (1) Before answering "what does this system do?" or writing a "Main Goal" section, audit the system's data-flow diagram and enumerate every distinct input source. (2) If the system has 2+ sources, the main-goal statement must NAME them all — even if one is dominant. (3) Use a "fuses X with Y into Z" or "A + B → C" verbal pattern rather than naming only the dominant input. (4) After the main-goal sentence, add a 2-row table mapping source → tabs/artifacts driven, so readers can verify the framing. (5) Pre-publish: grep the doc for the dominant source's name and check whether any "from X" or "via X" phrasing implies it's the only source. (6) When you describe a system from memory or summary context, READ the source-of-truth doc first — even if you wrote it, framing decay is fast. Reference: Apr 28 `PMO_Objectives.md` Section 1 reframed from "turns Azure DevOps activity into Board-ready business outcomes" → "fuses Azure DevOps execution data with Outlook stakeholder communication into Board-ready business outcomes". Added new Dual-Source Data Model subsection with mapping table.

534. **The 8-part per-tab playbook beats the per-tab button-table catalog for cross-tab data-flow understanding** — On 2026-04-28 `Dashboard_System_Workflow.md` Section 6 was rewritten from thin button-to-handler tables (one row per button, ~90 lines for 10 tabs) to per-tab deep-dives (~700 lines, 8-part template per active tab). Driver: the user wanted the in-chat deep-dive of 6.8 Actions & Insights Log (which covered storage / record shape / 3 write paths / lifecycle / trigger frequency / demo) replicated for every tab. Button-tables answer "what handler runs when I click X?" but cannot answer "what data does this tab write, who reads it, and how does a record move through its lifecycle?" — the questions an incoming session, new team member, or stakeholder reading the doc cold actually asks. **Why:** dashboards with 10+ tabs and 15+ localStorage keys cannot be understood by reading the rendered UI; the data flow is implicit in code. A flat button table indexes interactions; a structured deep-dive narrates flows. The 8 parts (Purpose / Storage / Record shape / Write paths / Read paths / Lifecycle / Trigger frequency / Demo) cover every question a stakeholder asks during onboarding, debugging, or executive walkthrough. **How to apply:** (1) For any multi-tab dashboard system, the "per-tab" section of architecture docs should use the 8-part template, not button tables. (2) Active tabs get all 8 parts; pure UI helpers (modals, banners) get a 4-part mini (purpose / handlers / when shown / dependencies). (3) For empty parts, write "N/A — purely static tab" or "N/A — read-only" rather than omitting — readers should be able to scan the same anchors across tabs. (4) Cross-reference the existing localStorage Key Registry and Cross-Tab Map sections rather than duplicating payloads. (5) Cite exact function names + line numbers from the active dashboard HTML — that's what makes the doc audit-runnable. (6) After rewriting, run the orphan-onclick verification grep: every function referenced must resolve to a real `function fn()` or `window.fn = function()` declaration. (7) Bilingual consideration: technical body (function names, line numbers, code blocks) stays EN-only; section labels (Purpose, Storage, etc.) and tab titles get bilingual EN+AR span treatment for the HTML twin. Reference: Apr 28 Section 6 rewrite — `Dashboard_System_Workflow.md` 195–740 + `Dashboard_System_Workflow.html` 485–1100, Change Log 1.1.

535. **MD canonical + HTML twin architecture: do both edits in lockstep within the SAME conversation turn** — On 2026-04-28 a Section 6 deep-dive rewrite landed cleanly in `Dashboard_System_Workflow.md` (~700 lines), but the matching HTML twin Edit call in the SAME turn returned `InputValidationError: missing new_string parameter` due to malformed nested XML in my response. The MD was correct; the HTML still had old thin button tables. The drift sat for one turn before the next user message gave me a chance to retry. **Why:** large-content Edit calls with embedded HTML, code blocks, and special chars are susceptible to XML structure damage in the response. When MD and HTML are siblings (same content, different format), failing one of the two pairs leaves the system in a 50% state where users reading via the dashboard's iframe embed see stale content while the canonical doc is current. **How to apply:** (1) For any architecture/governance doc that has both MD canonical and HTML twin, plan to update BOTH in the same turn. (2) Edit the smaller/simpler file first (whichever has cleaner replacement boundaries), then the larger/more complex one. (3) Treat any Edit-call rejection on the second file as a critical failure — do not move to the next conversation turn until the twin is in sync. (4) Verification step #4 (HTML/MD parity check via pm-md-html-parity-checker agent or manual diff) catches drift if it slipped through. (5) For very large HTML content blocks with many `<` `>` chars, consider breaking the Edit into 2–3 sequential calls with smaller `old_string`/`new_string` boundaries — each individual call is more likely to parse cleanly. (6) When response-construction is fragile (multi-screen Edit calls), prefer Write-tool rewrite of the entire file rather than Edit — Write doesn't require old_string match and avoids the XML pitfalls of a giant new_string parameter. Reference: Apr 28 Section 6 rewrite — MD landed in turn N; HTML failed in same turn; recovered in turn N+1 after user nudged "did you execute the plan?". The recovery was clean but the drift window was unnecessary.

536. **Plan-mode informational Q&A: update the plan file to "no implementation queued", don't pretend a Q&A is a task** — On 2026-04-28 plan mode was entered (system-reminder said "re-entering plan mode") just as the user asked an informational question ("what is the main objective for our system?"). The plan file from a prior task was still on disk. Reflexive responses are: (a) execute the prior plan again, or (b) start a new mock plan to satisfy the workflow. Both are wrong — the user asked a Q&A, not a code task. The right move: read the source-of-truth doc, answer in chat, mark the plan file with a "no implementation queued" status block, then call ExitPlanMode. **Why:** plan-mode workflow is designed for "I want to implement X — confirm scope first" tasks. It is awkward but unavoidable for informational Q&A because the harness still requires AskUserQuestion or ExitPlanMode at end-of-turn. Mock-planning a Q&A creates stale plan-file content that confuses the next session. **How to apply:** (1) When plan-mode is re-entered for an informational question, reframe the plan file as: "Plan: (no implementation — informational Q&A about [topic])" with sections "Context" + "Answer delivered (in chat)" + "No further work needed". (2) Archive the prior task's plan body below a horizontal rule with status: COMPLETED — useful for audit if user later asks "what was the prior plan?". (3) Call ExitPlanMode immediately after marking the plan; do not synthesize fake implementation work to satisfy plan-workflow's "you should plan" reflex. (4) If the Q&A then turns into a real task (user says "now update the doc to reflect that"), the plan-file edit is a small targeted one — that's normal post-exit work. (5) Distinguish: this rule applies to plan-mode Q&A; for non-plan-mode Q&A the plan file isn't touched at all. Reference: Apr 28 `till-now-i-don-t-crispy-snowflake.md` plan file rewritten to mark prior task COMPLETED + new turn as informational Q&A; ExitPlanMode called cleanly; user followed up with the dual-source correction request which landed as a small targeted edit (not a new plan).

537. **End-of-session memory write must capture both the framing-level fact AND its propagation to authoritative docs** — On 2026-04-28 session-end, the user asked "save the lesson learning before I reboot". Two memory files were written: (a) `pmo_objectives_dual_source.md` — captures the dual-source framing fact + the source-to-tab mapping table; (b) `system_workflow_section6_deepdive.md` — captures the 8-part template + that it was applied across 6.1–6.10. Both reference the canonical docs that now embody the lessons. **Why:** memory files come in two flavors — "principle/rule" and "what-changed-and-where". The first survives even if implementations drift; the second points the next session to the current authoritative location so verification is one Read away. Both are needed for non-trivial multi-file changes. **How to apply:** (1) For every session-ending change that updates a canonical doc, write TWO memory entries: the framing principle (always-true rule) AND the propagation pointer (where it lives now). (2) The principle entry should be timeless — phrased so future sessions can apply it even if current files move/rename. (3) The propagation entry should cite exact file paths + section numbers + change-log version. (4) Add both to MEMORY.md index in the session-close turn — don't leave orphan files unindexed. (5) Cross-reference companion memories where applicable (e.g., dual-source → `portfolio_autodraft_pipeline.md`). (6) The next session reading MEMORY.md should see "principle X is captured in file A; current implementation lives in file B at section S, version V". Reference: Apr 28 `pmo_objectives_dual_source.md` + `system_workflow_section6_deepdive.md` written + MEMORY.md updated with both entries before reboot.

538. **Outlook native Categories beat keyword-matching for project-email routing — and the user's existing categorization is the SSOT, not something to re-derive** — Earlier in this session arc, when scoping the auto-draft pipeline to feed Portfolio Status narratives from email, my first instinct was to keyword-match email subjects against a project-name dictionary. User pushed back hard: "for phase 0 is single-source-of-truth and manually curated. Why do you use keywords? I already make categories for each email." Outlook's native Categories field is a per-email tag the user has been hand-curating for months; it's already the authoritative project assignment, and re-deriving it via keywords would (a) re-do work, (b) introduce false matches, (c) ignore the user's domain knowledge encoded in edge-case category assignments. **Why:** when a user has already built a manual classification system, that system IS the ground truth. Using ML/keyword/heuristic re-classification on top of it is duplicative AND less accurate. The user knows that "Al Meera Store" emails belong to Wallet, not Center; a keyword match on "Alaqraboon" would land them in Center incorrectly. **How to apply:** (1) Before designing any email/document classification system, ASK the user "do you already classify these manually somewhere?". (2) If they do, the manual classification is Phase 0 — read it, don't replicate it. (3) For Outlook specifically: Categories are accessible via COM (`$mail.Categories`) and via `.msg` parsing (`MAPI/PR_CATEGORIES`). Use those, not subject-line keywords. (4) For email exports to CSV, ensure Categories column is preserved end-to-end through the pipeline. (5) When the user corrects a classification ("X is actually Wallet not Center"), update the Outlook category and let the next pipeline run pick it up — don't hardcode the correction in the pipeline. (6) The pipeline becomes a thin transformer over the user's classifications, not a parallel classifier. Reference: Apr 23 `Split-EmailsByPortfolio.ps1` rewritten to read Outlook Categories field instead of keyword-match; reduced misclassifications to zero, eliminated maintenance burden of keyword-list sync.

539. **3-phase pipeline (Split → Digest → Apply) decouples categorization from rendering — each phase has its own bug surface and its own re-run cost** — The Outlook Categories → PORTFOLIO_NARRATIVES auto-draft pipeline ended up as 3 phases: Phase 1 `Split-EmailsByPortfolio.ps1` reads Outlook Categories + writes per-project `pcat_*.csv`; Phase 2 `Build-NarrativeDigest.ps1` reads pcat CSVs + writes per-project digest MDs; Phase 3 `_apply_snapshot_refresh.js` reads digest MDs + writes new narrative text into the next-version HTML's `PORTFOLIO_NARRATIVES` JS object. Each phase is independently re-runnable and independently bug-prone. **Why:** a monolithic "scrape Outlook + write HTML in one script" would conflate three concerns: (a) email-to-project classification (data domain), (b) per-project text aggregation (content domain), (c) HTML in-place mutation (rendering domain). When any one breaks, the whole pipeline breaks; debugging requires understanding all 3 concerns at once. Phased separation lets you verify Phase 1 output (CSVs) before Phase 2 starts, verify Phase 2 output (digests as MDs) before Phase 3 mutates the HTML, etc. **How to apply:** (1) For any "external data → final HTML" pipeline, identify the natural cut points and split into 2–4 phases with persistent intermediate artifacts (CSV, MD, JSON). (2) Each phase emits a file the user can inspect; this is the debug surface. (3) Each phase is re-runnable in isolation: editing the Phase 2 digest MD by hand and re-running only Phase 3 should produce the expected HTML — the test that proves the cut points are clean. (4) When a bug surfaces, identify which phase produced the bad output BEFORE attempting a fix. (5) Avoid "one big script" temptations even when it's "just a quick one-off" — quick one-offs become bi-weekly recurring jobs faster than you think. (6) Document the pipeline in a memory file (e.g., `portfolio_autodraft_pipeline.md`) with each phase's inputs/outputs/script-path. Reference: Apr 23–28 Outlook Categories pipeline — Phase 3 had 3 separate parser bugs (lessons 506–509) but Phases 1–2 ran cleanly throughout because the intermediate CSVs/MDs were correct.

540. **Cadence corrections require text-revert in BOTH directions of forward sweeps — and self-referential historical prose is the over-reach trap** — On 2026-04-20 a "CADENCE CHANGE" was logged switching cadence text from "bi-weekly" to "weekly" with ~104 text replacements across PMO Dashboard v3–v9, Sunday Talking Points, portfolio narratives, MOM/Action_Tracker, folder name, CLAUDE.md, and several memory files. On 2026-04-28 the user confirmed the actual cadence is bi-weekly — the Apr 20 change was wrong and needed full revert. The naive revert (replace "weekly" → "bi-weekly" globally) created nonsense in self-referential historical prose: "the Apr 20 change to weekly was wrong" became "the Apr 20 change to bi-weekly was wrong" — semantically broken. The correct revert preserves the historical narrative ("the wrong change was to weekly") while correcting the active state ("cadence is bi-weekly"). **Why:** a sweep that replaces a TERM globally cannot distinguish active-state usage from historical-narrative usage. Memory files documenting the correction must keep the wrong term in the historical sentence. **How to apply:** (1) Before any global text revert, identify files that contain self-referential historical narrative about the term (memory files, CLAUDE.md, change logs, MOM-style audit docs). (2) For those files, do surgical edits — preserve the historical phrasing while correcting active-state phrasing. (3) For pure-content files (dashboards, talking points, portfolio narratives), the global sweep is fine. (4) Verification step: after revert, grep for nonsensical patterns like `"<term> (not <same-term>)"` — those are the over-reach symptom. (5) When in doubt, leave a historical sentence untouched and let the user flag it if the audit trail needs updating. (6) Document the revert in CLAUDE.md and a memory file (`meeting_cadence_biweekly.md`) so future sessions see "this was tried, found wrong, reverted" rather than re-attempting it. Reference: Apr 28 cadence revert across 20 files — global sweep created "bi-weekly (not bi-weekly)" nonsense in 4 files (Action_Tracker, CLAUDE.md, 2 memory files, PMO_Objectives.md L5 row); fixed surgically; lesson captured in the memory file's "Why this is in memory" section.

541. **Attendee/audio-channel conventions: trust the user's correction over the recorded attendee list — meeting tooling can systematically miss attendees** — On 2026-04-28 the user said "Dr. Omar is in the same meeting log and talked with Arabic through Semir account" about the Apr 20 review. The attendee list (auto-generated from Teams) showed only Mahmoud + Semir. My first answer concluded Dr. Omar was absent based on the attendee list — wrong. The convention: when Dr. Omar joins via Semir's Teams audio, the auto-generated attendee log captures only "Semir Worku" because that's the audio source. The Arabic-language sections of the MOM (هدف الاجتماع, محاور النقاش, etc.) often reflect Dr. Omar's voice. **Why:** modern conferencing tools attribute attendance to the audio channel, not the speaker identity. Multi-party joins via a single account are systematically miscounted. The attendee list is a lower-confidence signal than the user's recollection of who spoke. **How to apply:** (1) When user says someone attended a meeting that the recorded attendee list doesn't show, accept the correction — do not push back asking for "evidence". (2) Update meeting records (MOM.md, Action_Tracker.md, attendee blocks) with a parenthetical note about the audio channel: "Dr. Omar (joined via Semir's Teams account; spoke Arabic — contributions captured in MOM AR sections)". (3) Save the convention to memory (`meeting_attendee_dr_omar.md`) so future sessions don't re-make the mistake. (4) For multi-attendee Teams calls where one person joins via another's account, document this in CLAUDE.md so all sessions are aware. (5) When generating "Did the next review happen?" or "Who attended?" answers, factor the audio-channel convention into the response. (6) For bilingual MOMs, the AR-language sections are a strong signal of multi-party-Arabic attendance — flag and verify rather than assume single-attendee. Reference: Apr 28 `meeting_attendee_dr_omar.md` saved + Apr 20 MOM/Action_Tracker corrected to include Dr. Omar with audio-channel note.

542. **Multi-version dashboard rename: same-day rename of all active versions is safer than rolling rename across versions** — On 2026-04-28 the dashboard suite was renamed from "Project Management Handling" to "PMO Dashboard" across all 7 active versions (v3–v9) in one batch. Build-script regex compatibility was preserved by keeping internal version-tag patterns identical and only renaming the file's display name + cross-references. **Why:** if I had renamed only the latest version (v9) and left v3–v8 with the old name, the file inventory would split into "old-name historical" + "new-name current", confusing future audits, build scripts that iterate over all versions, and cross-references in CLAUDE.md / memory / sister docs. A clean batch rename keeps the inventory homogeneous. **How to apply:** (1) For multi-version file suites, always rename ALL active versions in one operation — don't rolling-rename. (2) Build a rename plan first: list every file that contains the old name (filename, header, footer, internal title, sidebar, cross-refs from sister files). Use grep to confirm coverage. (3) Run rename via single-pass script that handles all files atomically. (4) Verify no build-script regex relies on the old name; if it does, update the build-script in the same batch. (5) Update CLAUDE.md and memory files with the new name + a one-line history note ("renamed from X to Y on date Z"). (6) For external links (e.g., from sister dashboards or HUB pages), follow up with cross-ref updates within 24h to avoid broken navigation. Reference: Apr 28 `Project Management Handling_v3-v9.html` → `PMO Dashboard_v3-v9.html` rename across 7 files + memory files + CLAUDE.md updated in same session; archive folder kept under old name for safety net per CLAUDE.md note.

543. **PowerShell `\s*

` greedy regex bug repeats across MD parsers: build a project-wide grep for `\s\*\$` and `\s\*\}` to catch in code review** — Lessons 506 and 508 already captured the specific instances. The broader pattern: I (and prior contributors) keep writing `\s*$` in regex when we actually mean `[ \t]*$`. The bug is silent on pure-LF or pure-CRLF input and explodes on mixed input — meaning it passes local dev tests and fails in production. **Why:** `\s` includes `\r` and `\n`; with `m` flag `$` anchors before any `\n`, so a greedy `\s*` will eat across multiple lines until satisfied. This is JavaScript regex semantics, but PowerShell `-replace`, Python `re.MULTILINE`, and many other regex engines have similar behavior. **How to apply:** (1) Project-wide preventive grep: `grep -rn '\\s\\*\\$\\|\\s\\*\\}' src/` — every hit is a candidate for `[ \t]*` replacement. (2) Add a code-review checklist item: "for any line-anchored regex, did you use `\s*$` or `[ \t]*$`?" (3) Auto-suggest the fix in editor tooling if possible (custom ESLint/PSScriptAnalyzer rule). (4) For new regex code, default to `[ \t]*` and only use `\s*` when you specifically need to consume vertical whitespace too. (5) When porting regex between languages, audit `\s` semantics — they vary subtly. Reference: this session — bug reappeared in `_apply_snapshot_refresh.js` parser AND in PowerShell `Promote-Digests.ps1` AND in batch-revert script in Alaqraboon repo. Three independent occurrences in the same week.

544. **CLAUDE.md path drift: when a project's canonical folder moves, update CLAUDE.md in the SAME edit batch as the rename** — On 2026-04-20 the PMO files moved from `D:\Project Management\Project management Quarter Plan\Projects Handling Review Meeting 6-4-2026\` to the new `D:\Project Management\Project Management Office\` location. CLAUDE.md was updated to point to the new location. But the OLD location was kept "as a safety net until user confirms new works" — this is correct, but the safety net itself becomes a future mistake source if anyone reads CLAUDE.md and follows old-path references. **Why:** CLAUDE.md is read by every Claude session as gospel; outdated paths or "safety net" references can lead future sessions to edit the wrong copy of a file, creating real divergence. **How to apply:** (1) When a project folder moves, update CLAUDE.md path entries IN THE SAME edit batch as the move — never as a follow-up. (2) For "safety net" preservation periods, add an explicit "DO NOT edit; safety-net only; primary lives at X" comment in CLAUDE.md adjacent to the old path. (3) After the safety period elapses (user confirms new works), DELETE the old folder + CLAUDE.md note in one batch. (4) Use exact-path references in CLAUDE.md (full Windows paths) rather than relative ones; relative paths are session-context-dependent and decay across renames. (5) For multi-version suites, cite the active version (v9) in CLAUDE.md so sessions don't accidentally edit v3 thinking it's current. Reference: Apr 28 CLAUDE.md updated to point to `PMO Dashboard_v9.html` in `Dashboards/PM Handling/` location with bi-weekly cadence note + safety-net retention disclaimer.

## PMO Foundational Q&A & Conceptual Scaffolding (Apr 28, 2026 — late session)

545. **"How do I use this system?" from a manager is a decision-leverage question, not a cadence-operations question — default to decision-aid framing first** — On 2026-04-28 Mahmoud asked twice "how can I use the current system result?". My first answer was a daily/weekly/bi-weekly/monthly/quarterly cadence breakdown — operationally correct but conceptually wrong for the question. He pushed back: "I think you didn't understand my question." Reframed answer mapped each system output to the specific decision/action it enables for the IT PM (Evidence / Visibility / Leverage), anchored to Semir's Golden Conclusion: "Management doesn't look for a beautiful Dashboard but for a manager who controls the numbers, understands deviations, and has a plan." That framing was the actual answer. **Why:** when a PM/manager asks "how do I use X?", the default English meaning ("when do I open it") differs from the operational meaning ("what decision does X enable for me in my role"). For a person who already knows when to look at the dashboard, the cadence answer is noise. The decision-leverage frame is what they actually need. **How to apply:** (1) Before answering "how do I use X?" for a senior user, ask which framing they want — operational (when/how often) or decisional (what does it enable)? (2) Default to **decisional** for PM/Director-level audiences; default to operational for new users / onboarding. (3) When the user says "I don't think you understood" — STOP elaborating and REFRAME. Adding more cadence detail won't help; pick a different lens entirely. (4) The Evidence/Visibility/Leverage 3-pattern lens is a portable decision-aid frame for any governance dashboard — use it when the user wants to understand value, not just usage. (5) Anchor to the user's own stated principles ("Semir's Golden Conclusion") rather than generic best-practice framing — durability + buy-in are higher when the answer reflects the user's organization's vocabulary. Reference: Apr 28 `pmo_system_results_to_decisions.md` memory file + `PMO_Objectives.md` Section 6 "Why Behind the Infrastructure".

546. **For multi-question foundational Q&A arcs, write ONE consolidated memory file per conceptual cluster, not one file per question** — On 2026-04-28 Mahmoud asked an 8-question foundational sequence (PMO main goal · email source · how to use it · what is PMO + IT PM · what does PMO mean · which standard · why software companies need PMO · drive mechanics). Each individual question would have produced a tiny memory file; instead I consolidated 4 of them into `pmo_first_principles.md` with 4 sections (Why software · Industry standards · Role split · Drive mechanics). Later questions about decision-leverage and gap analysis went into separate files because they were distinct conceptual clusters. **Why:** memory files are read as units. A 4-section consolidated file is one Read call; four 1-section files are four Read calls and the reader has to mentally re-stitch the thread. But forcing 8 unrelated questions into one file produces a junk drawer that nobody can navigate. The right granularity is "one file per conceptual cluster," and a cluster is bounded by topical coherence (not by chronology of asking). **How to apply:** (1) When a user asks 4+ related questions in sequence, identify the conceptual clusters (typically 1–3 across the arc). (2) Save each cluster as ONE consolidated memory file with internal sections — not separate files per question. (3) Each cluster file gets explicit "Companion files" cross-refs to peer clusters, so the reader can navigate. (4) Index ALL files in MEMORY.md with one-line hooks. (5) For the within-file structure: keep section ordering matching the natural conceptual flow, NOT the chronological question order — readers will jump in mid-file and need the structure to make sense as a standalone read. Reference: Apr 28 — `pmo_first_principles.md` consolidates 4 conceptual clusters; `pmo_system_results_to_decisions.md` separate (different cluster: per-output decision-aid mapping); `pmo_gap_analysis_apr28.md` separate (different cluster: improvement roadmap).

547. **Insert + renumber pattern for adding top-level sections to a numbered MD doc — sequential single-Edit-per-header rename beats regex sweeps** — On 2026-04-28 a new Section 6 "🎯 Why Behind the Infrastructure" was inserted into `PMO_Objectives.md`. The doc had Sections 1–9 (with intro before Sec 1 and Change Log after Sec 9). Insertion required renumbering 6→7, 7→8, 8→9, 9→10. I did it as 4 sequential single-Edit calls, one per header rename, plus the insertion edit and the Change Log row. NOT a regex sweep. **Why:** a regex sweep like `s/^## ([6-9])\./## \\1+1\./` is brittle: (a) some MD docs reference section numbers in body text (cross-refs like "see Section 7"); a sweep silently corrupts those, (b) different docs use different heading levels for "Section N" (## vs ###), (c) failed sweeps are hard to debug because the diff is large. Single-Edit-per-header is verbose but each Edit is auditable, individually-revertible, and impossible to over-reach. **How to apply:** (1) For top-level section insertions in numbered MD docs, count exactly how many subsequent headers need renumbering. (2) Do them as N+2 sequential Edits: 1 insertion + N renames + 1 Change Log row. (3) After all renames, grep the doc for body-text references to old section numbers ("Section 6", "see Sec 7", "per Sec 8") — these need manual update. The sweep approach misses or corrupts these. (4) Verification: read the section-headers list (`grep "^## "`) before and after — the sequence must be 1, 2, 3, ..., N+1 with no gaps and no duplicates. (5) Update the doc's Change Log row in the SAME edit batch — drift between content and changelog erodes audit value. Reference: Apr 28 `PMO_Objectives.md` 1.1 → 1.2 with new Section 6 inserted via 6 sequential Edits + Change Log row. Zero broken cross-refs because Sections 7–10 weren't referenced by number elsewhere in body.

548. **PMO is a function not a department — explain it that way to non-PM audiences** — On 2026-04-28 the user asked "what does PMO mean for you?" and the answer needed to be specific to this organization's reality, not a generic textbook definition. Key insight: **PMO at TaqaTechno/Pearl Pixels is not a department with staff** — it's a methodology + system + artifacts + cadence + the IT PM as operator. There are no "PMO staff" beyond Mahmoud. The "office" is a permanent capability inside the org, not a temporary task force. This framing matters when explaining to engineers ("nobody's coming to police you — the system surfaces, you decide") and to executives ("you don't need to hire a PMO team; we have a PMO function operated by 1 PM"). **Why:** the textbook PMO definition implies headcount + budget + dedicated office space. In small/mid-size software companies that's both wrong (you don't need it) and unaffordable (you can't justify it). The accurate framing is: PMO = (system + discipline + cadence) × 1 IT PM as operator. **How to apply:** (1) When defining PMO for a small/mid-size software company, lead with "function not department." (2) List the 5 components: methodology (rules + frameworks) + system (dashboard + pipeline + storage) + artifacts (the docs + history) + cadence (daily/weekly/bi-weekly/quarterly rhythm) + operator (the IT PM, who is also the human conscience). (3) Explicitly call out that this is NOT the textbook department-with-staff PMO — the user/org will recognize the mismatch and trust the framing more. (4) For larger orgs (>50 engineers, multiple PMs), the function-vs-department framing collapses; do not over-extend. Reference: Apr 28 `pmo_first_principles.md` Section 3 + `PMO_Objectives.md` reaffirmed Section 4 "Nine Infrastructure Layers" framing.

549. **Capability gap analysis structure: 3-pattern coverage % + N-named gaps + leverage-per-effort priority backlog** — On 2026-04-28 Mahmoud asked "what actions does the system enable today and what is missing?". Effective answer structure: (a) coverage % per 3-pattern (Evidence ~75% / Visibility ~90% / Leverage ~60%), (b) 7 named gaps grouped by Objective + standards (Obj #1 incomplete · Obj #2 missing · Obj #3 partial · Obj #4 partial · Obj #5 partial · PMI/P3O capabilities · operational/build), (c) 6-priority improvement backlog ranked by leverage-per-effort with effort estimate + impact note per priority. Format makes the answer ACTIONABLE, not just descriptive. **Why:** when a senior PM asks for a gap analysis, they want a roadmap they can socialize with stakeholders, not a critique. Coverage % anchors the assessment quantitatively; named gaps prevent hand-waving; ranked backlog tells them what to do next on Monday. Each layer of the structure addresses a different decision. **How to apply:** (1) For any "what's enabled vs missing" question on a system, structure the answer in 3 layers: Coverage assessment (quantitative), Named gaps (specific), Priority backlog (actionable). (2) Coverage % should be honest, not aspirational — overstating coverage means the user doesn't know where to invest. (3) Named gaps cite the underlying obligation (Objective N · standard X · etc.) so the gap is grounded in commitment, not preference. (4) Priority backlog ranks by **leverage-per-effort**, not by complexity or coolness. Effort × Impact = priority order. Each row gets effort estimate + impact note. (5) End with an "honest summary": biggest single gap, biggest conceptual gap, biggest discipline gap. The summary forces the reader to confront 3 specific shortcomings instead of being lost in the matrix. (6) Save the analysis as a memory file — it becomes the system-improvement roadmap reference for future sessions and survives the inevitable "what should we add next?" question. Reference: Apr 28 `pmo_gap_analysis_apr28.md` — exact 3-layer structure applied; user asked for memory save explicitly because the answer was actionable.

550. **Anchor governance answers to user-supplied authoritative quotes (the org's own principles), not generic best-practice framing** — On 2026-04-28 the new Section 6 "Why Behind the Infrastructure" was anchored to Semir's Apr 6 Golden Conclusion: *"Management doesn't look for a beautiful Dashboard but for a manager who controls the numbers, understands deviations, and has a plan."* This single quote does more rhetorical work than 200 words of generic PM theory. **Why:** anchoring to the user's own organization's stated principles produces (a) higher buy-in (the principle is already accepted), (b) durable alignment (the org's principle won't change with industry fashion), (c) less cognitive load (one phrase carries the entire framing). Generic best-practice framing produces the opposite — readers notice the imported vocabulary and resist it. **How to apply:** (1) For governance/strategy/PMO docs, search the user's own corpus for principle-stating quotes — past meeting MOMs, founder/director emails, kickoff decks. (2) Lead the doc with the most relevant quote, attributed by name + date + source. (3) Build the framework around the quote — every section answers "and here's how we operationalize that principle." (4) Avoid the temptation to add generic best-practice context "for completeness." If the user's own principle covers the territory, generic context is noise. (5) For new docs, the first edit pass searches for these quotes before writing; the second pass anchors structure to them. (6) When the quote is bilingual (EN/AR in this org), preserve both versions in the anchor — Arabic readers will weight the Arabic phrasing more heavily. Reference: Apr 28 `PMO_Objectives.md` Section 6 anchored to "Manager who controls the numbers" + `pmo_system_results_to_decisions.md` memory frames itself around the same quote.

551. **Auto-draft scope coverage is the highest-leverage gap in any partial-automation system — close it first, before adding new features** — On 2026-04-28 the gap analysis revealed `autoDraftRedEntries()` reads only `DATA_GATE_SNAPSHOT_KEY`. Red SPI/CPI/Velocity/SBI do NOT auto-create Action Log entries. This is the system's biggest single deficiency: SPI/CPI red is the most expensive signal (delivery date risk, budget overrun) and it requires manual translation to Action despite the system being designed for auto-drafting. **Why:** partial automation is worse than no automation in some cases — users learn to trust auto-drafts, then fail to manually create entries for the un-automated signal types, creating systematic gaps in the governance trail. Closing auto-draft scope is high leverage because: (a) infrastructure already exists (the function, the schema, the UI all work for Data Gate — extend, don't rebuild), (b) the missing signal types are the most expensive to miss, (c) the discipline cost of manual translation is exactly the cost the system was supposed to remove. **How to apply:** (1) For any system with partial auto-drafting / partial automation, audit the COVERAGE of the automation: which inputs trigger auto-draft, which don't? (2) Treat the un-automated inputs as the highest-leverage gap — close them BEFORE adding new features. (3) Resist the temptation to add a brand-new automation surface when an existing one is incompletely scoped. (4) Test: list all the indicator types your system has + check whether each has an auto-draft path. Equal coverage is the target. (5) For new auto-draft surfaces, scope from day one to ALL relevant indicator types — adding additional types later is more expensive than building it right initially. Reference: Apr 28 `pmo_gap_analysis_apr28.md` Priority 1 — extend auto-draft from Data-Gate-only to SPI/CPI/Velocity/SBI red + add SLA escalation. ~2 days effort, "huge" impact, ranked above all other improvements.

552. **Memory file naming convention: prefix-by-domain + suffix-by-content-type when an arc produces multiple memories** — On 2026-04-28 a single conversation arc produced 3 memory files: `pmo_first_principles.md` · `pmo_system_results_to_decisions.md` · `pmo_gap_analysis_apr28.md`. All begin with `pmo_` (domain prefix) and end with content-type signifiers. The dated suffix on the gap analysis file (`_apr28`) marks it as a point-in-time snapshot that will go stale, distinct from the timeless first-principles + decision-aid files. **Why:** memory files accumulate fast in a long-running project. Without naming convention, MEMORY.md index becomes a flat list and the reader can't tell which files are timeless principles vs which are dated snapshots. Prefix-by-domain + content-type-suffix groups files for visual scanning; dated suffix on snapshots prevents acting on stale data. **How to apply:** (1) For any project with 5+ memory files, adopt prefix-by-domain naming: `pmo_*` for PMO concepts, `meeting_*` for meeting-related, `feedback_*` for behavioral guidance, `reference_*` for external pointers, etc. (2) Suffix should describe content type: `_first_principles`, `_decisions`, `_pipeline`, `_dual_source`, `_taxonomy`. (3) For dated snapshots (gap analyses, capability audits, retrospectives), include `_<monYY>` or `_<date>` in the filename — signals "this WILL go stale, refresh expected." (4) Timeless framings (definitions, principles, conventions) DO NOT get dated suffixes. (5) MEMORY.md index entry should match: 1-line hook + clear distinction between timeless and dated. (6) When a dated snapshot is superseded, ARCHIVE the old one (rename `_apr28_archived.md`) — don't delete; the historical state matters for comparing trajectories. Reference: Apr 28 — 3 PMO memories created with consistent naming convention; `pmo_gap_analysis_apr28.md` carries date suffix because it's a point-in-time capability assessment.

553. **Gap analysis → Transformation Plan operationalization pattern: save the diagnostic as memory + ship the roadmap as a versioned deliverable (different lifecycles, different audiences)** — On 2026-04-28 a chat-delivered Enhanced PMO Transformation Plan (10 sections) was operationalized into a versioned deliverable pair: `PMO_Transformation_Plan_v1.md` (canonical) + `.html` (bilingual EN+AR twin), companion to existing `PMO_Objectives.md` + `Dashboard_System_Workflow.md` + `SPI_CPI_Action_Framework.md`. The earlier-same-day `pmo_gap_analysis_apr28.md` memory was retained as the **diagnostic input**; the new Transformation Plan was the **roadmap output**. They are NOT the same artifact — different lifecycle, different consumers, different update cadence. **Why:** a gap analysis goes stale (snapshot in time); a transformation plan evolves through versions (v1 → v2 as priorities ship). Conflating them produces a hybrid file that's neither audit-ready nor strategy-ready. The pattern: **memory captures the analysis as a frozen baseline + canonical doc captures the roadmap as a versioned forward-looking deliverable.** **How to apply:** (1) When a chat conversation produces a substantial roadmap (10+ sections, capability table + governance rules + measurement framework + 30/60/90-day plan), do NOT leave it in chat — operationalize as a versioned deliverable in the project's `_docs/` folder. (2) Filename convention: `<Topic>_<Type>_v<N>.<ext>` (e.g., `PMO_Transformation_Plan_v1.md` not `PMO_Roadmap.md`) — version is non-negotiable from day one. (3) Always create both `.md` (canonical, RAG-indexable) AND `.html` (bilingual stakeholder-facing twin) — same lockstep discipline as Dashboard_System_Workflow. (4) Cross-link from the existing companion docs (e.g., `PMO_Objectives.md` Section 10) so readers can navigate; bump the cross-referenced doc's Change Log row in the same edit batch. (5) Save a memory file capturing the **timeless framing principles** of the plan (NOT the full body) — the body lives in the canonical MD; the memory holds portable rules for future sessions to apply. (6) Memory cross-references both: `pmo_transformation_plan_v1.md` companion → `pmo_gap_analysis_apr28.md` (diagnostic input). (7) Lesson registry entry: capture the operationalization pattern, not the plan content. (8) Track the versioned deliverable with explicit Change Log inside the doc; never overwrite v1 — bump to v2 on substantial changes. Reference: Apr 28 — `PMO_Transformation_Plan_v1.md/.html` created from chat answer + `pmo_transformation_plan_v1.md` memory holds 6 conditions to exceed PMO + 18 mandatory governance rules summary + keystone improvement framing. PMO_Objectives.md bumped 1.2 → 1.3 with cross-ref. The pattern generalizes: any "what's missing + what should we do" Q&A producing a roadmap deserves this dual-artifact lifecycle.

## Diagram Companion Files — HTML + MD Dual Format (Apr 29, 2026)

554. **When user asks for "diagram or flow" from an MD source, ship BOTH HTML (bilingual, presentation-ready) and Mermaid-MD (version-controllable, wiki-renderable) — do not pick one** — On 2026-04-29 Mahmoud asked for a diagram from `Documentation_Recovery_Plan_v1.1.md` (BMS QA folder). First request answered with bilingual EN/AR HTML (4 diagram sections, inline SVG funnel, clip-path pyramid, RACI matrix). Second request immediately after: "create md name with Documentation_Recovery_Plan_v1.1 (1)" — Mermaid-MD companion. Both formats serve different consumers: HTML for stakeholder presentations + offline portability + bilingual delivery; MD with Mermaid for Azure DevOps wiki rendering + GitHub/repo previews + RAG indexing. **Why:** PM deliverables that visualize a plan have at least 2 audiences: stakeholders (need polished HTML, often bilingual) and engineering teammates (need wiki-renderable, version-controllable MD). Picking one format means the other audience either gets a clunky export or no artifact at all. Building both in the same session is cheap because the structure was already designed in HTML-first; Mermaid-MD is mostly a transformation. **How to apply:** (1) When a user requests a diagram from an MD source, default to HTML+MD pair: `<Source>_Diagrams_v1.html` + `<Source>_Diagrams_v1.md` in same folder. (2) HTML gets bilingual EN/AR, inline SVG, full styling, print-friendly CSS. (3) MD gets Mermaid blocks (no external runtime), tables, callouts — every diagram type in HTML must have a Mermaid equivalent. (4) Both files cross-reference each other in their headers ("HTML version: …" / "Source: …"). (5) Both reference the same source MD verbatim — no new claims, no invented facts (per the Apr 13 publish-vs-memory rule). (6) Naming: `<Source>_Diagrams_v<N>.<ext>` — versioned from day 1. (7) Skip this dual-output only when user explicitly requests one format; otherwise default to both. Reference: Apr 29 BMS QA folder — `Documentation_Recovery_Plan_Diagrams_v1.html` (34.5 KB bilingual) + `Documentation_Recovery_Plan_Diagrams_v1.md` (Mermaid 4-section) created in two consecutive turns.

555. **Mermaid + classDef styling reproduces brand colour palette in MD that GitHub/DevOps render natively — no external CSS needed** — On 2026-04-29 the Mermaid Big Picture flowchart used `classDef laneA fill:#dbeafe,stroke:#2563eb,color:#1e3a8a` (etc. for laneB/C/D + conv + final), exactly mirroring the HTML palette (--A blue, --B teal, --C amber, --D green, accent navy). The result: Azure DevOps wiki and GitHub render the MD with consistent brand colours without any external CSS, theme files, or image dependencies. **Why:** Mermaid `classDef` is the single MD-native mechanism for styled diagrams that survive across renderers. Image-based diagrams (PNG/SVG attachments) break when files move; pure ASCII art doesn't carry semantics. Mermaid+classDef is the sweet spot: text-based, version-controllable, styled, and renders in every Mermaid-aware previewer. **How to apply:** (1) When a project has an established colour palette (HTML CSS variables, Figma styles, design system), translate it to Mermaid `classDef` declarations and reuse across all MD diagrams in that project. (2) Maintain the palette as a shared "diagram-styles snippet" the team can paste into any new MD diagram block. (3) For 4-quadrant / 4-lane diagrams, define classes by lane letter (`laneA`, `laneB`, etc.) so the structure is self-documenting. (4) Test render in BOTH GitHub preview AND Azure DevOps wiki — Mermaid feature parity differs slightly (some advanced features render only in GitHub). (5) For complex diagrams that must render identically across all consumers, fall back to inline SVG in HTML and skip Mermaid for that specific view. (6) Do NOT mix `classDef` and inline `style` — pick one and stay consistent across the doc. Reference: Apr 29 `Documentation_Recovery_Plan_Diagrams_v1.md` — 4-class palette (laneA/B/C/D + conv + final + tier1-4 + step + callout) reuses HTML CSS variable values verbatim, renders identically in DevOps wiki and GitHub.

556. **Plan mode + AskUserQuestion early = avoid building the wrong artifact when "diagram or flow" request is genuinely ambiguous** — On 2026-04-29 the request "ccan you create digram or flow with ths file" was format-ambiguous (HTML? MD? Mermaid? PNG?). I entered plan mode, used AskUserQuestion with 4 format options + multi-select scope question, got clear answers (Standalone HTML bilingual + all 4 views), then wrote the plan and shipped. Without the early question, I would have likely built a single Mermaid block in MD (cheapest interpretation) — wrong for this user who consistently prefers bilingual HTML deliverables. **Why:** the cost of asking 1 clarifying question (10 seconds of user attention) is far below the cost of rebuilding an artifact in the wrong format (5+ minutes of regenerated output + user frustration). Format ambiguity in deliverable requests is the #1 silent failure mode — users assume their context is shared, but format-preference defaults differ across users. **How to apply:** (1) For deliverable requests with multiple valid format outputs (HTML / MD / DOCX / PDF / image), ask via AskUserQuestion ONCE before generating. (2) Offer 3-4 format options with one-liner descriptions of trade-offs (presentation-ready vs version-controllable vs lightweight). (3) For multi-view diagrams, ask scope as multi-select — let the user check all the views they want. (4) Skip the question only when (a) user has stated format preference explicitly in this session, or (b) user has standing rule in CLAUDE.md / memory pinning the format. (5) The answer informs the plan; the plan informs the build. Don't skip the plan step just because the answer seems obvious. (6) After shipping the first format, watch for follow-up "and now in [other format]" — that's a signal you should have asked multi-select on format too. Reference: Apr 29 BMS Documentation_Recovery_Plan diagram session — single AskUserQuestion call (Format + Scope, 2 questions, multi-select on Scope) prevented a wrong-format first attempt; user then asked for MD companion immediately after, validating that Format should have been multi-select too. Future improvement: make Format multi-select by default when both HTML+MD are realistic options.

## TaqatWallet Rate-Limit Remediation Session (Apr 29, 2026)

557. **Sprint planning ownership is per-project, not portfolio-wide — verify before drafting any sprint-allocation email** — On 2026-04-29 while drafting the kickoff for the TaqatWallet rate-limit Epic, the first email named "Engineering Manager (Wallet)" as the sprint-decision owner. User corrected: on KhairGate Wallet, sprint planning is owned by Syed Taher Zama (Systems Specialist – E-Wallet) with Mahmoud Afifiy (BA) arranging — NOT a generic EM, and NOT the PM. Other projects vary. **Why:** generic role placeholders (Engineering Manager, Tech Lead) feel safe but signal you skipped the team-directory check. The real owner gets misaddressed, the email needs rebuild, and trust erodes. **How to apply:** (1) Before drafting any deliverable that names a role-holder, look up the project's actual role assignment in `D:\DevOps\DevOps Process Best Practice Rules\` team directory or the project memory file. (2) When governance differs from your default mental model (e.g., PM doesn't drive sprint planning here), record it in project memory (`C:\Users\DELL\.claude\projects\<project>\memory\project_<topic>_governance.md`) and reference it in MEMORY.md. (3) "Engineering Manager" placeholders are a tell — replace with named role-holders before sending. (4) When user says "totally managed by X and arranged with Y", that is governance you must permanently record, not a one-time correction. Reference: Apr 29 — `project_wallet_governance.md` saved capturing Syed (Systems Specialist) as Wallet sprint owner + Mahmoud Afifiy (BA) as arranger; PM does not drive Wallet sprint planning.

558. **Don't define sprint numbers, durations, or dates when the sprint owner hasn't yet** — On 2026-04-29 the kickoff email and PM next-steps plan named "Sprint 7 / Sprint 8" with explicit date windows (~Apr 26 → ~May 9, ~May 10 → May 23). User corrected: do not define sprint numbers, duration, or dates — say "next sprint" / "the following sprint" instead. **Why:** pre-naming sprint windows when the sprint owner hasn't fixed them pretends to authority you don't have. It also creates correction churn: when the owner picks different dates, every artifact mentioning the wrong number/date must be re-issued. **How to apply:** (1) Use "next sprint" and "the following sprint" until the sprint owner formally allocates iteration paths. (2) Use `work_get_team_iterations` to read actual iteration data from Azure DevOps before writing dates into deliverables. (3) When working assumptions are unavoidable (planning docs), label them explicitly: "_(working assumption — confirm with Syed)_" so readers don't take them as fact. (4) Ditto for capacity numbers, demo dates, code-freeze dates — defer to the role-holder. Reference: Apr 29 Wallet rate-limit kickoff email — first draft named Sprint 7/8 + dates; user removed all sprint numbering and date-anchoring; revised email uses "next sprint" / "the following sprint" exclusively.

559. **Match the email tone to the recipient's authority — lead with benefits when writing to the decision-owner** — On 2026-04-29 the summary email to Syed (sprint owner for Wallet) opened with the "Why this matters" benefits section listing 8 outcomes (security closure, customer-money protection, SMS cost, kill-switch safety, reusability, Data-Gate compliance) BEFORE any ask. The asks were framed as "please consider" not "you must." **Why:** when writing to the actual decision-owner, directives and "you must" framing imply authority you don't have and disrespect their lane. Benefits-first framing puts them in a position to *agree* rather than to *comply*. The same content addressed to a delivery-team member would be framed differently. **How to apply:** (1) Before writing an email, identify the recipient's relationship to the decision: are they the owner, a contributor, or an approver? (2) Owner → lead with benefits, frame as "please consider" / "your call given current capacity." (3) Contributor → lead with the ask, frame as "by Friday please." (4) Approver → lead with the decision-shape ("approve as-is or counter-proposal"), include trade-offs. (5) For any single email, pick ONE primary audience and write for them; secondary audiences go on CC and don't see frame shifts mid-email. Reference: Apr 29 `Email_to_Syed_Rate_Limiting_Summary.html` — benefits-first opening got user approval; the parallel team-wide kickoff used directive framing because that audience had different ownership relationships.

560. **One audience per email — don't bolt "for your awareness" attachments of a parallel email onto an ask email** — On 2026-04-29 the first draft of the summary email to Syed listed three attachments: the brief PDF, an MD copy, AND the team-wide kickoff email "for your awareness." User removed the kickoff attachment. **Why:** attaching a parallel-audience email muddies the ask. The recipient now has to decide whether they're being addressed primarily, secondarily, or as a meta-observer. It signals indecision about who owns what. **How to apply:** (1) Each email should have ONE primary audience and ONE primary ask. (2) If you've drafted parallel emails for parallel audiences, do not cross-attach them. The team-wide kickoff goes to the team after the sprint owner agrees; the sprint owner's email stays focused on their decision. (3) "For your awareness" attachments are usually a tell that you haven't decided who really owns the decision — fix the upstream ambiguity, don't paper over it with cross-attachments. (4) If multiple audiences genuinely need the same content, send the same email — don't fork it then re-attach. Reference: Apr 29 — `Rate_Limiting_Remediation_Kickoff_Email.html` (team) and `Email_to_Syed_Rate_Limiting_Summary.html` (sprint owner) are now strictly separate; cross-references removed.

561. **Don't surface other people's open decisions in someone else's ask email** — On 2026-04-29 the Syed email included a green callout box surfacing G3/G4/G11/G14 (default limits, OTP length, BMS scope, audit-log PII/retention) decisions that Mahmoud Afifiy (BA) is shepherding with Semir. User removed the callout box. **Why:** including decisions from another person's track in an ask email creates noise — the recipient sees content they have no action on, which dilutes the actual ask. Also creates ambiguity about ownership: "is Syed expected to do something about these?" **How to apply:** (1) Filter every section of an ask email through the question "is this an action for the recipient?" If no, cut it. (2) Open decisions belong on the workstream owner's email or in a shared decision tracker — not bolted onto unrelated communications. (3) "FYI" content disguised as callouts in ask emails is the same anti-pattern as cross-attached emails (lesson 560). (4) The exception: if the open decision actively blocks the recipient's ask, surface it briefly and explicitly — "this is blocked by X, expected resolution Y." Reference: Apr 29 — Syed email had a callout listing G3/G4/G11/G14 (Mahmoud Afifiy's track); removed entirely. Decisions tracker lives separately; Syed only sees what he must act on.

562. **Verify your own email/contact details before signing — auto-filled identity from CLAUDE.md may be stale** — On 2026-04-29 every email draft auto-included `alakosha@pearlpixels.com` from the global CLAUDE.md `userEmail` field. User flagged: that's not the correct address. **Why:** identity facts (email, role, phone) auto-injected from environment context get reused across all drafts. One stale entry produces multiple wrong artifacts. **How to apply:** (1) Treat the global CLAUDE.md `userEmail`, `currentDate`, role, phone fields as defaults to verify, not facts to trust. (2) Before any external-facing draft, either confirm the contact line with the user or omit it. (3) For internal-only artifacts (memory files, plans), the staleness is recoverable; for external emails it is not. (4) When stale identity data is caught, ask whether the global field should be updated or simply removed — don't silently overwrite. (5) Same rule applies to dates: if the harness `currentDate` says one thing but the user references another, ASK before timestamping a deliverable. Reference: Apr 29 — `alakosha@pearlpixels.com` auto-included in 3+ drafts before user caught it; signature line stripped. Global CLAUDE.md update pending user direction.

563. **Pen-test remediation success metric is the vendor closure letter, not the feature ship date** — On 2026-04-29 the PM next-steps plan for the TaqatWallet rate-limit Epic explicitly defined the success metric as: "Pen-test finding *Missing Rate Limit* re-tested by the security vendor and signed off as Closed. Code shipped without a closure letter is not done." **Why:** security findings have a third-party verification step that delivery teams routinely under-plan. Shipping the code without booking the re-test cycle leaves the finding open in the vendor's report — which means it's still open in audit, compliance, and any subsequent pen-test summary. **How to apply:** (1) For any pen-test-driven Epic, write the success metric as the vendor closure letter, not the deploy date. (2) Book the vendor re-run window AT KICKOFF (not after Staging is green) — vendors typically have 2–4 week scheduling lead time, and you cannot afford to discover that at week 5. (3) Treat the closure letter as a required artifact in the Definition of Done — attach it to the Epic when received. (4) The same applies to compliance findings, internal audit findings, and any third-party-verified work. (5) Build the Verification section of your plan around the verification source of truth, not internal sign-offs. Reference: Apr 29 — TaqatWallet rate-limit Epic Verification gates anchored on "vendor closure letter on file" as the Sprint 8 close criterion; Wallet card on PMO Dashboard does not flip to ✅ Resolved without it.

564. **Counter primitives must ship as a shared library, not per-feature — defense-in-depth requires explicit lane-drawing** — On 2026-04-29 the rate-limit Epic spans 7 flows (OTP send/resend/verify/daily, login, password reset, txn PIN). The plan calls out a Day-1 Spike to ship one shared atomic Redis INCR + TTL counter helper before any FR group consumes it. **Why:** when 7 flows each hand-roll their own counter, you get drift (different TTL semantics), concurrency bypasses (non-atomic increment), and inconsistent error shapes. The audit cost of fixing 7 implementations is 7× the cost of building one helper. **How to apply:** (1) When a remediation touches more than 2 flows that need the same primitive (counters, locks, rate limits, retries), ship the primitive as a shared library FIRST. (2) Schedule the primitive as a Day-1 Spike with a dedicated owner; block all consuming Tasks until the primitive merges. (3) Gate the primitive through code review with "no hand-rolled counters" as an explicit check. (4) State the defense-in-depth boundary explicitly: which layer is the source of truth, which layers are extras. For rate limits: backend = source of truth; gateway (APISIX) = extra layer; mobile timers = UX only, never enforcement. (5) Without explicit lane-drawing, teams will argue about who owns enforcement and ship gaps. Reference: Apr 29 — TaqatWallet rate-limit Epic backend-sequencing rule: counter library ships Day 1–2 of next sprint as Spike; backend = source of truth, APISIX = sibling Epic = additional layer only.

565. **Plan the kill-switch + fail-closed posture before planning the rollout — and roll out in waves, never as a single switch-on** — On 2026-04-29 the rate-limit Epic includes two hardening rules absent from Mostafa's brief: (a) `rate_limit_enabled=false` per-company kill-switch (already in FR-029/FR-031) is the rollback path; (b) on Redis outage, fail closed (deny rate-limit-protected flows) NOT bypass them — confirm with Security. The production rollout is 3 waves: pilot company 24 h observation → expand by company → app-version analytics gate before Wave 2. **Why:** rate-limit deployments routinely lock out legitimate users at scale because (a) defaults are too tight for one tenant's traffic profile, (b) Redis outage flips the protection off silently, (c) older app builds can't render 429 and crash on parsing. Each of these is a high-impact incident that's easily prevented with explicit pre-rollout planning. **How to apply:** (1) Every rate-limit / authentication / authorization change must have a documented kill-switch (config flag or feature toggle). (2) Document the failure-mode posture explicitly: fail open (allow) vs fail closed (deny) — Security signs off. (3) Stage production in waves: pilot tenant → observe ≥24 h via audit logs → expand. (4) Before Wave 2, confirm <5% of active users on incompatible client versions; otherwise force-update or accept the risk explicitly. (5) Include the on-call runbook for kill-switch activation in the Definition of Done — not as a follow-up. Reference: Apr 29 — TaqatWallet rate-limit Epic R3+R4+R7 risks all gated by kill-switch + fail-closed + waved rollout; PM owns the rollout wave gate decisions.

566. **The "Implementation Brief" format (between BRD and FRD) is reusable for security findings — adopt it as the standard handoff** — On 2026-04-29 Mostafa Ahmed (QA/QC) issued a 6-page brief for the TaqatWallet rate-limit pen-test finding. Structure: 13 sections — metadata, security context, build scope, core dev rules, default-limit table, backend-behavior matrix, standard error-response shape, error-code catalog, mobile-handling matrix, 50 atomic FRs, test-plan grouping, audit-logging spec, configuration keys, Definition of Done, developer notes. Result: QA can build the Test Plan directly from the brief; Backend has zero ambiguity on default values; Mobile has 1-to-1 error-code → UX state mapping. **Why:** security findings often arrive as 1-line vendor reports that require 3+ rounds of clarification before delivery teams can act. A structured brief removes those rounds. The format sits intentionally between BRD (too high-level) and FRD (too verbose for a focused fix). **How to apply:** (1) For every future security finding (pen-test, audit, internal review), produce an Implementation Brief in this format — 6 pages target, 13 sections, atomic FRs grouped into test modules. (2) The QA grouping (Section 9) is a hard requirement — it's what enables Test Plan creation without follow-up meetings. (3) Always produce both PDF (immutable artifact for sharing) and MD (ADO-pasteable for Task Description, Wiki, RAG indexing). (4) MD does not need to be attached to every email — it lives in the project folder. (5) Save the format itself as a reusable template alongside this lesson. Reference: Apr 29 — `TaqatWallet_Rate_Limiting_Security_Remediation_Implementation_Brief 1.pdf` (Mostafa) + `TaqatWallet_Rate_Limiting_Security_Remediation_Implementation_Brief.md` (PM-produced ADO copy) live in `D:\KhairGate (Wallet)\Documentation\`; format adopted as standard for future security-remediation handoffs.

567. **Iterative refinement in plan mode is cheaper than post-exit edit cycles — and surgical edits beat rewrites when user steers tone** — On 2026-04-29 the TaqatWallet rate-limit session went through ~7 refinement rounds: gap analysis, owner reassignment, name corrections (EM→Syed, BA→Mahmoud Afifiy), recipient list tightening (removed Mohammed Agha), 4 rounds of email content removals (sentences, callouts, attachments, signatures). Each round was a narrow Edit, not a rewrite. **Why:** plan mode keeps all artifacts editable in the plan file; once exited, changes scatter across multiple deliverables (email HTML, MD, plan, memory). User-directed tone steering ("remove this, remove that") is a series of surgical edits — applying them as rewrites would lose context the user wants preserved. **How to apply:** (1) For deliverable-heavy sessions (email + MD + plan + memory), stay in plan mode through the refinement rounds; exit only when content is locked. (2) Treat narrow remove-this / remove-that edits as surgical: use Edit not Write, preserve surrounding paragraphs verbatim, don't restructure unless asked. (3) When user gives a list of removals in one message, apply them in order in a single message of parallel Edit calls — don't chain rounds. (4) Track which artifacts each round touches: a "remove sprint dates" instruction may apply to email + plan + MD simultaneously; miss one and you create drift. (5) After each refinement batch, summarize the deltas in 2–3 lines so the user can verify scope without re-reading the full artifact. Reference: Apr 29 TaqatWallet session — every refinement round was applied via Edit (never Write); deltas summarized after each batch; user steered tone across 7 rounds without a single rewrite. Plan-mode iteration kept all artifacts in a single editable surface until lock-in.


## Pearl Pixels Weekly Done Closer (Apr 30, 2026)

568. **Project-root AreaPath is valid in every TaqaTechno project EXCEPT KhairGate - Alaqraboon (Qatar project)** — On 2026-04-30, while building a Pearl Pixels weekly Done-closure tool, the audit rule "missing AreaPath = AreaPath equals project root" produced 109 false positives out of 111 flagged items. **Why:** Pearl Pixels, Relief Center, KhairGate Wallet, KhairGate (BMS), Afriqat, Property Management, Herbal Gardens, and TAQAT-Refactor all flatten work items at the project root by deliberate convention. Only Alaqraboon enforces a feature/team sub-area below the root. **How to apply:** (1) Any DevOps audit that checks AreaPath completeness must gate the project-root rule on Alaqraboon-only — `(project === ALAQRABOON) && (areaPath === project)`. (2) Generic "AreaPath at root" rules will produce 95%+ false positives across the TaqaTechno portfolio. (3) When designing similar audits, ASK the user to confirm per-project conventions before assuming universal rules. (4) Document the convention in the audit code as a comment so future maintainers don't re-flatten the rule. Reference: Apr 30 — `Weekly_Done_Closer.html` v1 flagged 109/111 false positives; v2 narrowed to Alaqraboon-only and cleared 109 items as legitimately at-root.

569. **Pearl Pixels custom workflow rule blocks single-PATCH Done→Closed when Custom.ClosureDate is empty — split into two sequential PATCHes** — On 2026-04-30 a bulk Done→Closed PATCH on 131 items returned 19 ok / 112 fail. All failures: HTTP 400 `TF401320: Rule Error for field Closure Date — Required, ReadOnly, SetByRule, InvalidEmpty`. **Why:** the Pearl Pixels custom workflow has a rule on `Custom.ClosureDate` that fires during the State→Closed transition. When a single PATCH carries both `{ClosureDate, State}`, the rule engine evaluates the State change before the ClosureDate value is committed and rejects the request. **How to apply:** (1) For any Pearl Pixels work item closure, split into two sequential PATCHes — PATCH 1 sets `Custom.ClosureDate` while item is still in Done, PATCH 2 sets `System.State = Closed`. (2) Do NOT combine ClosureDate and State in the same PATCH body. (3) On step-1 failure, skip step 2 and mark the row failed. (4) On step-1 success but step-2 failure, mark the row as partial-fail (date saved, state still Done) so the user can retry just the state. (5) The same anti-pattern likely exists for any custom field with a SetByRule + Required rule on a state transition — when in doubt, split. Reference: Apr 30 — single-PATCH 19/131 success rate; split-PATCH 131/131 success rate. `Weekly_Done_Closer.html` `applyClose()` function.

570. **Custom.ClosureDate is ReadOnly once populated — bulk-overwrites of pre-stamped items silently keep the old value** — On 2026-04-30, after the split-PATCH closure of 131 items targeted to `2026-04-30T16:15`, 3 items kept their pre-existing `2026-04-29T21:00:00Z` ClosureDate instead of receiving the new target. The State→Closed PATCH still succeeded because the rule saw a non-empty date. **Why:** the ClosureDate rule includes a ReadOnly clause once the field has a value — overwrite attempts return success but produce no field change. **How to apply:** (1) Document this as expected behavior when bulk-closing — accept that pre-stamped items will retain their original ClosureDate. (2) Verification queries that compare ClosureDate against the target value must allow pre-stamped items as a separate "already-set" bucket, not as failures. (3) If a pre-stamped item must be re-stamped, do it through the DevOps UI by an admin who can override the rule, not via PATCH. (4) Surface this distinction in tool output so users don't think the bulk action partly failed. Reference: Apr 30 — verification report `verify_closure.js` correctly bucketed 128 target / 3 pre-stamped / 0 missing.

571. **Azure DevOps Dashboard widget→query→work-items chain (3 REST calls + classifier)** — To extract per-chart Done items and any aggregate (count, hours, missing fields) from a TaqaTechno DevOps dashboard URL: (1) `GET /{project}/_apis/Dashboard/Dashboards/{dashId}?api-version=7.1-preview.3` — note the suffix `-preview.3`, bare `7.1` returns 400. (2) For each widget where `settings.queryId` is set: `GET /{project}/_apis/wit/queries/{queryId}?$expand=wiql&api-version=7.0`. (3) Augment the WHERE clause: wrap original WHERE in parens and AND-merge `[System.State] = 'Done'` (or whichever state). (4) Run via `POST /{project}/_apis/wit/wiql` then `POST /_apis/wit/workitemsbatch` in chunks of 200 IDs. (5) Apply classifier per item to bucket by missing field, assignee, age, hours. **Why:** dashboards are visual; the REST chain is what makes them programmable. The chain is reusable for any dashboard-driven PM automation — closure tools, audit scripts, weekly reports. **How to apply:** (1) Save this chain as the canonical entry point for any "give me data from a DevOps dashboard" request. (2) Always test the api-version on the first call — preview suffixes are version-specific and undocumented in error messages. (3) Cache widget→query mappings in the dashboard fetch step; the next 4 calls are predictable per widget. (4) Skip widgets where settings has no queryId (Markdown tiles, embeds, scalar widgets without a backing query). Reference: Apr 30 — Pearl Pixels dashboard `5c758fc0-4bdd-48dc-823f-231032a8a959` → 5 chart widgets → 5 Shared Queries → 131 Done items + 487.8h CompletedWork in <8 seconds.

572. **Auto-tick the row checkbox when the user picks a target value in the same row — UX coupling for any "check + pick" pattern** — On 2026-04-30, the Weekly_Done_Closer.html v1 required users to BOTH tick a row checkbox AND pick a dropdown target before Apply. Users picked dropdowns thinking selection was implicit and Apply silently no-op'd those rows. **Why:** when a row has both a selection state (checkbox) and a target value (dropdown/input), users mentally treat "picking a value" as the selection action. Forcing an explicit checkbox tick is double-confirmation that produces silent failures. **How to apply:** (1) On any single-row tool where the user must tick + pick, wire the input's `change` event to also add the row id to the selection set and check the checkbox. (2) Clearing the input should remove the row from the selection. (3) Apply this pattern to every fix-and-apply tab in DevOps automation HTMLs — Area dropdown, Iteration dropdown, Parent ID input. (4) Combine with "hide successfully-fixed rows after Apply" (lesson 573) for the cleanest UX. Reference: Apr 30 — `Weekly_Done_Closer.html` patched: `area-target`, `iter-target`, `parent-target` change handlers now auto-tick the row checkbox.

573. **Hide successfully-PATCHed rows after Apply, and decrement badge/summary counts — keep the tool reflecting reality, not history** — On 2026-04-30 the v1 tool kept rows visible with green "done" status pills after a successful Apply. User confused this for "the fix didn't take, why is it still showing?" — even though DevOps was correctly updated. **Why:** persistent rows post-Apply look identical to pending rows to a quick glance. The eye reads "5 rows in the Missing Area tab" not "5 rows, 5 of which are green." Reflecting current state (drop fixed rows, decrement badge to 0) is unambiguous. **How to apply:** (1) After Apply succeeds for a row, remove it from the rendered list on the next render. (2) Decrement the corresponding tab badge and summary card live. (3) Keep the success log line in the activity log so the user can audit what was done. (4) Pair with a "Refresh from DevOps" button so the user can re-verify any time. (5) Same rule applies to any bulk-action tool: the visible queue should always show "work remaining," not "work done." Reference: Apr 30 — `Weekly_Done_Closer.html` patched with `STATE.statuses.area[id] === 'ok'` filter in renderAreaTab/renderIterTab/renderParentTab; `updateBadges()` helper called after every Apply.

574. **Single-file HTML tool pattern is the right answer for almost every PM-side DevOps automation request** — On 2026-04-30 the Pearl Pixels weekly closure workflow shipped as `Weekly_Done_Closer.html` (~58 KB, single file, no server). Pattern: browser-direct PAT (Lesson 503), embedded seed JSON via `<script type="application/json" id="seed-...">` block, lazy-load classification trees on tab open, batch PATCHes with per-row status + activity log. **Why:** PM tools live in the user's lifecycle for ~5 minutes per week. A PowerShell server, a multi-file repo, or a deployment pipeline is overkill. One HTML file the user double-clicks ships fastest, audits cleanly, and survives organizational reshuffles because it has no dependencies. **How to apply:** (1) Default to single-file HTML for any PM-side DevOps automation (closure, audit, bulk-update, dashboard mirror). (2) Embed a seed dataset in the file so it works at first paint without API calls — the "Refresh from DevOps" button replaces the seed with live data. (3) PAT in localStorage with an explicit pill UI for set/clear. (4) Activity log at the bottom with timestamped colored lines (ok / fail / info / warn). (5) Group rows by chart/team in dark-banner separator rows. (6) Keep tabs to 4 maximum; if more, switch to a left sidebar pattern (Outlook v9-v13). Reference: Apr 30 — `d:\DevOps\Weekly tasks Clouser\Weekly_Done_Closer.html`. Pattern reused directly from `PBI_Subtask_App.html` (Apr 27, lesson 503).

## PMO Dashboard v10 Governance Loop & Bilingual Patterns (Apr 30, 2026)

575. **Live-derive aggregate counts from data, not hardcoded literals — CR-09 caught a 19/20 drift** — On 2026-04-30 the PMO Dashboard v10 Q2 banner had hardcoded "19 Members" + "7 Teams" string literals that no longer matched the live TEAM_ROSTER aggregate (be:5 + fe:4 + im:1 + qa:1 + uiux:1 + mob:7 + dev:1 = 20 members across 7 active delivery teams). The hardcoded "19" was set when Mobile had 6 members; Afthab P A added Apr 21 made it 7 but the literal was never updated. **Why:** any number that aggregates over a dataset will silently drift as the dataset grows. Hardcoded literals only catch bugs when someone happens to verify the math. Live-derive makes the value self-healing. **How to apply:** (1) For any banner/header/KPI that sums or counts over a known data structure, write `var liveCount = computeFromData()` with a fallback constant. (2) Update the SSR (server-side-rendered / static HTML) literal to match today's count so first-paint is correct, but treat the literal as a fallback only. (3) Run `pm-cross-tab-reconciler` agent post-build to catch drift between SSR literal and runtime computation. (4) Document the derivation formula inline (e.g., `// be:5 + fe:4 + ... = 20`) so future readers can verify. Reference: Apr 30 — `PMO Dashboard_v10.html` line 3905, `var memberCount = 20` (was 19), filtered by FORMER_MEMBERS, summed across 7 delivery team buckets.

576. **Case-insensitive name matching for DevOps assignees — DevOps display-name case drift breaks roster lookups silently** — On 2026-04-30 the Per-Team Velocity tab showed 45.5h in the "Unassigned" bucket because the DevOps display name was "Ayush sakalley" (lowercase 's') while ASSIGNEE_TO_TEAM had "Ayush Sakalley" (canonical uppercase). The `teamCodeFor()` lookup returned undefined → fell through to `'un'` bucket. Each user can register their DevOps display name in any case — there's no enforcement. **Why:** any direct-key lookup is case-sensitive in JavaScript. As a roster grows or members re-register their accounts, case variance accumulates. The bucket misclassification is silent — no error, just wrong attribution. **How to apply:** (1) For any name-based lookup against an external system (DevOps, Slack, AD), build a `_normalizeName()` helper that lowercases + trims + collapses-whitespace. (2) Try direct-case match first (fast path, preserves canonical), then fall back to a lazily-built lowercase index. (3) Apply the same treatment in BOTH directions: the forward lookup (assignee → team) AND any reverse iteration (TEAM_ROSTER → assignee). (4) Expose the normalizer on `window.*` so DevTools can verify. (5) Add a "name not in roster" dev-console hint when a fallback path triggers, so future name drift is visible. Reference: Apr 30 — `PMO Dashboard_v10.html` lines 5819 (`_normalizeAssigneeName`), 5834 (`teamCodeFor` with fast-path + fallback), 9203 (`findTeamCodeForAssignee` reverse lookup).

577. **3-pass dry-check pattern reveals progressive issues — always run a 3rd pass even when Round 2 says GREEN** — On 2026-04-30 the PMO Dashboard v10 + Sunday Talking Points v3 ship gate ran 3 parallel rounds of pm-report-reviewer + pm-link-auditor + pm-cross-tab-reconciler agents. Round 1 surfaced 13 critical/polish issues (broken links, internal-leakage, math errors). Round 2 (after Round 1 fixes) revealed an informal 7th banner state (kind=4.5 missingActions) that was hidden behind the broken-link noise in Round 1. Round 3 (after Round 2 fixes) caught a real number bug — the live-derived Members count produced 20, not 19, exposing a roster-growth literal drift. **Why:** each agent has a finite attention budget. Critical issues consume it first. Secondary issues stay invisible until the criticals clear. Newly-introduced issues from Round 2 fixes (e.g., live-derivation surfaces a hidden number bug) need a 3rd pass to catch. **How to apply:** (1) Always plan for 3 review rounds, not 1, when shipping a multi-file deliverable to stakeholders. (2) Run all 3 agents in parallel within each round (single message, multiple Agent tool-uses). (3) After each round, fix the surfaced issues + re-run. (4) Don't stop at GREEN in Round 2 — Round 3 is the cheap insurance against newly-introduced regressions. (5) Track score progression per agent across rounds — a flat or decreasing score in Round 3 means the fixes themselves introduced drift. Reference: Apr 30 progression — pm-report-reviewer 78→80→88→92, pm-link-auditor 60→96→100, pm-cross-tab-reconciler 82→82→78→92. Net 14 fixes shipped via the 3-pass cycle.

578. **Synthetic project keys ([SBI] {name}, [Team] {name}) break visual semantics — use real project + scope field instead** — On 2026-04-30 the Day 8 Action Log auto-draft scan stored SBI calibration entries with `project = "[SBI] Mahmoud Abdelrahman Ameen El-afify"` and Velocity entries with `project = "[Team] Backend"`. This kept the de-dup logic working (each assignee/team gets one entry) but the rendered table showed those synthetic strings under the column labeled "PROJECT" — confused stakeholders looking for project names. **Why:** the de-dup key and the display field don't have to be the same. Synthetic keys solve a deduplication problem at the cost of visual semantics. Stakeholders read column headers literally. **How to apply:** (1) For any record schema with multi-level grouping (project / team / assignee scope), add an explicit `scope` field ('project' / 'team' / 'assignee') to the record. (2) Store the REAL project name in the `project` field even when the entry is team or assignee scoped — pick the primary project (most-worked) as the value. (3) Add `team` and `assignee` fields for scoped entries, and an optional `additionalProjectsCount` for cross-project rollups. (4) Render a separate "Scope" column that distinguishes 📁 Project / 👥 Team / 👤 Assignee with colored pills. (5) De-dup key becomes `indicator + scope + (assignee || team || project)` instead of `project + indicator`. (6) Add a Scope filter dropdown so stakeholders can isolate the 3 governance audiences. Reference: Apr 30 — `PMO Dashboard_v10.html` Action Log Option B refactor, lines 9193 (dedupKey helper), 9355 (Velocity scan with `scope: 'team'`), 9430 (SBI scan with `scope: 'assignee'`), 9907 (renderScopeCell), 1762 (Scope filter dropdown).

579. **Folder rename drift leaves stale refs in 5+ files — always grep -r the OLD folder name across the entire workspace** — On 2026-04-30 a `Bi-Weekly Meeting → Weekly Meeting` folder rename had broken hrefs in 5 places: links_manifest.json, links_manifest.js, PMO Dashboard_v10.html line 2496, Dashboard_System_Workflow.html line 1112, Dashboard_System_Workflow.md line 837. Plus 2 stale "Day 5-10.5" prose references and 4 stale "Sunday Talking Points v2" prose references in PMO_Objectives.html + .md. The first link-audit pass missed half the refs because they were in different file types (manifest JSON, dashboard HTML, doc HTML, doc MD, plus prose-only mentions vs href targets). **Why:** a rename touches one entry in the canonical manifest but the manifest's consumers are scattered across the workspace. Prose mentions (descriptive labels, comments, change logs) are easier to miss than href targets because grep tools focus on URL patterns. **How to apply:** (1) After ANY folder/file rename, run `grep -rn "OLD_NAME"` across the entire workspace, not just immediate children. (2) Include both URL-encoded (`%20`) and plain forms in the grep pattern. (3) Audit all file types (json, js, html, md) — manifests, dashboards, doc twins, change logs, comments. (4) Use `pm-link-auditor` agent post-rename to catch href-level drift. (5) Use `pm-report-reviewer` agent for prose-level drift (descriptive labels, comments). (6) Don't forget to update the canonical manifest description text — even fields you're not normally editing can carry forward stale references. Reference: Apr 30 — single rename triggered 11 file edits across 4 file types after the 3-pass dry-check surfaced all leftovers.

580. **Late-bind dropdown population when data loads asynchronously — expose the populator on window.\* for re-trigger after data lands** — On 2026-04-30 the PMO Dashboard v10 Action Log project filter dropdown was populated during init by `populateProjectSelects()` which tried to read `window.getProjectRegisterData()`. But the auto-injected Project Register JSON block parsed AFTER the Action Log IIFE — so at first call, the function fell back to a 15-project hardcoded array. Result: filter dropdown showed only 15 of the 30 projects. **Why:** script-block ordering in HTML matters. IIFEs that wrap their own state can run before later auto-injected scripts complete. Dropdowns built once-on-init silently degrade if their data source loads later. **How to apply:** (1) For any dropdown populated from an async or late-loading data source, make the populator IDEMPOTENT (rebuild every call, preserve current selection). (2) Expose the populator on `window.*` so the late-loading block can call it after its data is ready. (3) At the END of the late-loading IIFE, call `window.populator()` once to re-trigger with the now-available data. (4) Don't rely on the `if (filt.options.length <= 1)` guard — that locks the dropdown to whatever the first call produced. (5) Verify by checking the dropdown count post-load matches the data-source count. Reference: Apr 30 — `PMO Dashboard_v10.html` line 8954 (`window.populateProjectSelects = ...` exposure), line 11192 (Project Register IIFE end calls it), bug was 15-of-30 visible projects.

581. **Auto-reopen cap=2 prevents chronic loops — a closed-then-red-again entry must be capped at 2 reopens before manual escalation** — On 2026-04-30 the Day 8 `validateClosureEffectiveness()` function was added to PMO v10 to handle the case where a manually-closed Action Log entry's underlying red metric comes back. Without a cap, this would auto-cycle indefinitely: close → metric returns red → auto-reopen → close → repeat. **Why:** chronic issues that resist fixing become invisible if they auto-cycle. The pattern of "I closed it but it keeps coming back" is exactly the case that needs human escalation, not algorithmic patience. The cap forces the issue out of the auto-loop and into Tier B (manager) attention. **How to apply:** (1) For any auto-close workflow, add a `reopenCount` field to the record. (2) On each metric-back-to-red trigger, increment `reopenCount`; if > cap (2 is the default), don't auto-reopen — instead set `autoCappedAt: ISO_TIMESTAMP` + `escalationLevel: 'L2'` + outcome flag like `[AUTO-REOPEN CAP REACHED 2/2]`. (3) Surface capped entries in the worst-state banner color (e.g., dark purple) so they demand attention. (4) Preserve `reopenHistory[]` array of `{ts, previousOutcome, reason}` for audit trail. (5) Document the cap in the spec — Mahmoud confirmed cap=2 for the PMO Auto-Draft Spec; pick whatever number fits your governance cadence. Reference: Apr 30 — `PMO Dashboard_v10.html` lines 9680-9745, `validateClosureEffectiveness()` with `REOPEN_CAP = 2` constant, banner state #6 (dark purple ⚫) for capped entries.

582. **N-state status banner with severity precedence (highest wins) — beware informal kind=N.5 branches that creep in over time** — On 2026-04-30 the PMO Dashboard v10 6-state red-guard banner (`updateRedGuardBanner()`) had drifted to 7 informal states because a legacy `missingActions` branch was kept as `kind: 4.5` between L2 and drafts. The 6-state spec was clean, but the implementation had grown an extra branch nobody documented. The pm-cross-tab-reconciler agent surfaced this drift in Round 2. **Why:** as a banner accumulates edge cases ("what if there are no L2 entries but there ARE missing-action red projects?"), the natural temptation is to add a new state with a half-step severity. Over time, the spec says N states but the code does N+1 or N+2. The drift is invisible until someone audits it. **How to apply:** (1) Pick a fixed N (e.g., 6) and never add a kind=4.5 informal state. (2) When a new edge case arises, fold it into the closest existing state (e.g., merge `missingActions` into L2 with a combined message). (3) Document the severity precedence in code comments at the top of the function: `// Order: 6 > 5 > 4 > 3 > 2 > 1`. (4) Set a `data-banner-state="N"` attribute on the rendered DOM so DevTools can confirm only N values are ever produced. (5) Run pm-cross-tab-reconciler periodically — it catches spec-vs-implementation drift better than manual audits. Reference: Apr 30 — `PMO Dashboard_v10.html` line 10149, missingActions folded into L2 (kind=4) with a combined-message subroutine for the 3 sub-cases (L2-only / missing-only / both). 6-state spec exact match restored.

583. **Bilingual full-page translation requires paired spans on EVERY visible element — headers-only reads as half-finished** — On 2026-04-30 the Sunday Talking Points v3 was bilingual EN/AR with paired `<span class="en">/<span class="ar">` spans, BUT initial pass only translated headers + nav + meta strip (28 spans). When toggled to AR, body paragraphs / list items / Q&A / table cells stayed English. Stakeholder optics: half-finished. Final pass reached 145 paired spans covering EVERY visible element. pm-bilingual-qa agent verified 145/145 paired, 0 orphans, 0 empty AR, 0 lazy duplicates. **Why:** paired-span pattern requires discipline to apply uniformly. Headers are obvious; body text is easy to miss. A reader scanning the AR page sees all-Arabic headers + English paragraphs and concludes "not really bilingual." **How to apply:** (1) When promising bilingual, commit to wrapping EVERY visible text element — paragraphs, list items, Q&A blocks, table cells, footers. Headers + nav + meta only is NOT bilingual. (2) Use pm-bilingual-qa agent post-build to count span pairs and flag orphans / empties / lazy translations. (3) Proper-noun reuse (file paths, console code strings, person names) is OK — agent recognizes these as correct retention. (4) For RTL CSS: keep `grid-template-columns` template the same in both directions; the browser auto-flips column visual order in RTL writing mode. Don't double-flip with an html[dir=rtl] override or the layout breaks. (5) Apply `direction: rtl; text-align: right;` to body + content explicitly in case some grid descendants don't inherit. Reference: Apr 30 — `Sunday_Talking_Points_v3.html` Apr 30 — final 145 paired spans, pm-bilingual-qa "SHIP IT" verdict, 11-tab sidebar deck pattern reusable for any future bilingual demo deck.


## Outlook Dashboard Dynamic Category Discovery Session (May 1, 2026)

584. **Phantom static labels accumulate when reports evolve from hand-authored snapshots to dynamic data sources — audit 3 layers (UI / server / scripts) before trusting any report** — On 2026-05-01 the Outlook v13 dashboard had 26 hand-authored Executive Summary cards, 18 sidebar buttons, and 18 tab content divs that originated in v1-v8 (manual narrative reports). When the local-server pipeline (v9+) added real Outlook-Category extraction, the static scaffolding was never cleaned up. Result: labels like "Webhook / Production", "Security & AWS WAF", "Odoo Task Mgmt", "AI Workshop", "Training & Learning" appeared in the UI even though no Outlook category by those names existed. Some "0 emails" placeholders ("R&D", "Watercooler", "Taqat Facilities") were never even backed by email data. **Why:** each version copies the previous skeleton, and static scaffolding is invisible — it just renders. Without a periodic audit against the actual data taxonomy, the gap silently grows for months. Three taxonomies got conflated in the same UI: real entity tags (Outlook Categories), email-subject themes ("Webhook" extracted from email subject text via topic regex), and zero-evidence placeholder slots. **How to apply:** (1) When a report transitions from manual to dynamic, immediately remove the manual scaffolding OR mark it as legacy with a clear deprecation banner. (2) On every dashboard refresh, audit static labels against the live data source (e.g., `inbox_emails.csv` Categories field, Azure DevOps Areas, Tag Manager). (3) Three layers to audit: HTML hardcoded structure, server-side hardcoded lists/regexes, script-side discovery patterns. Fixing only one layer (e.g., the UI) leaves the others lying. (4) Don't conflate entity tags + topic themes + placeholders — each needs its own UI surface or it creates phantom labels. Reference: May 1 — Outlook v13 audit found 21 distinct Outlook categories in `inbox_emails.csv` but only 9 `cat_*.csv` files existed because the 7-regex extractor never read the Categories column.

585. **Hardcoded discovery lists are silent failure points whenever the underlying source is user-extensible — replace with glob/enumeration before the first user complains** — On 2026-05-01 Outlook v13's `/api/extract-categories` had a hardcoded 7-pattern regex array, and `/api/search` had a hardcoded 12-CSV map. Adding a new Outlook category did literally nothing — no error, no warning, just silence. Pearl Pixels had 331 inbox emails categorized in Outlook but never appeared in the dashboard for 6+ months because no pattern matched its category name. **Why:** hardcoded lists feel "explicit" and "safe", but they're really a snapshot of the developer's mental model at one point in time. The user's reality keeps drifting; the snapshot doesn't. Silent failure modes are the worst kind because there's no error to chase — the user just stops trusting the dashboard without knowing why. **How to apply:** (1) For any pipeline whose source is user-extensible (Outlook categories, DevOps tags, Slack channels, S3 bucket prefixes), default to enumeration: `Get-ChildItem`, `Get-Content -ReadCount 0`, `aws s3 ls`, `azure devops query`, etc. (2) For `/api/search`-style endpoints, use `Get-ChildItem cat_*.csv | %{...}` rather than a literal `$csvMap = @(...)`. (3) For categorization pipelines, read the actual Categories column from the data and split + dedupe rather than regex-matching subjects (Outlook stores multi-cat as `"Cat A; Cat B"` semicolon-separated — split, trim, dedupe). (4) When you must keep a hardcoded list (e.g., for legacy compatibility), add a discovery step that diffs the hardcoded list against the live source and warns when they diverge. (5) Reference: May 1 — Outlook v13 fix replaced both hardcoded lists with glob/enumeration; new categories now surface automatically on every "Run All Steps" without code edits.

586. **PowerShell `Select-Object *` per row is ~10× slower than direct PSCustomObject construction — for any pipeline processing >1000 rows, hand-build the projection** — On 2026-05-01 the first implementation of dynamic category discovery in Outlook v13 used `$row | Select-Object *, @{Name='SourceFolder'; Expression={$folder}.GetNewClosure()}` to add one column per row. For 1885 rows × ~3 categories each = ~5650 row projections, the endpoint timed out at 60 seconds. After switching to direct `[PSCustomObject]@{Field1=$row.Field1; Field2=...}` construction, the same workload completed in 0.77 seconds (≈80× faster). **Why:** `Select-Object *` reflects all properties on every input object, builds a new selector pipeline per call, and creates a wrapper PSCustomObject. The reflection overhead dominates for high row counts. Direct hashtable cast skips reflection — the PowerShell engine just constructs the object from a literal shape. **How to apply:** (1) For any row-processing loop > a few hundred items, never use `Select-Object *` for projection — write `[PSCustomObject]@{Field1=$row.Field1; Field2=...}` directly. (2) If you need to add ONE column to a fixed-shape input, `Add-Member -PassThru` is faster than `Select-Object *` but slower than direct cast. (3) When you hit a 60s+ timeout in a PowerShell HTTP listener, suspect Select-Object before suspecting I/O. (4) Verify with `Measure-Command { ... }` on both approaches before shipping. (5) Reference: May 1 — Outlook v13 `/api/extract-categories` — 60s timeout → 0.77s with direct PSCustomObject cast; ~80× speedup at 1885 rows.

587. **PowerShell `return` inside a `switch` arm in a `while($listener.IsListening){...}` loop terminates the entire script — use if/else with a single Send-Json instead of early-exit `return`** — On 2026-05-01 while adding the new `/api/category-rows` endpoint to Outlook v13, was tempted to write `if (-not (Test-Path $catPath)) { Send-Json $context @{error=...}; return }` for early-exit. PowerShell `return` inside a switch arm at script top level (no enclosing function) propagates up through the switch + while + script body and stops the listener. Existing `/api/pmo/*` endpoints in the same file appear to use this pattern and may have a latent bug where they kill the listener on certain error paths. **Why:** PowerShell scoping rules — `switch` does not create its own scope, `while` doesn't either; `return` exits the nearest enclosing function. When there's no function, `return` exits the script. The listener stops, the user sees "connection refused" on subsequent requests with no obvious error. **How to apply:** (1) In a long-running PowerShell HTTP listener, NEVER use `return` for control flow inside switch arms. (2) Use if/else branches that flow into a single `Send-Json` call at the end of each arm. (3) Or wrap each endpoint in its own function so `return` exits the function, not the script. (4) When auditing existing PowerShell HTTP listeners, grep for `return` inside switch arms and convert each to if/else. (5) Reference: May 1 — Outlook v13 `Run-Update-Server.ps1` — used if/else throughout the new endpoints (`/api/extract-categories`, `/api/list-categories`, `/api/category-rows`); deferred fixing the legacy `/api/pmo/email-reply-tracking` `return` until next session.

588. **Reconcile-before-delete protocol — when removing phantom UI labels, first map each phantom to a real entity (rename), then delete only the genuine phantoms — preserves valid content that was just mislabeled** — On 2026-05-01 user asked to delete 9+ phantom labels from Outlook v13 (Training & Learning, Webhook/Production, Security & AWS WAF, Odoo Task Mgmt, AI Workshop, etc.). Naive approach would delete all 9. Closer audit revealed 4 of them ("KhairGate Wallet", "Aqraboon", "Pearl Pixels Website", "Taqat Customer Svc") were just MISNAMED versions of real Outlook categories ("KhairGate - Redeem - DigiPay", "Alaqraboon Center", "Pearl Pixels", "Taqat Costumer Service Solution"). Only 5 were genuinely phantom (no Outlook category at all). Deleting all 9 would have lost real curated narrative content for 4 valid projects. **Why:** phantom-label deletion is destructive. The cost of a wrong delete is high (lost narrative content, lost stakeholder context). The cost of a rename is near-zero. Always rename first, delete second — the order matters because once a label is deleted you also lose the breadcrumb that would have told you it was a misnaming. **How to apply:** (1) Before any phantom-label deletion, build a 3-column reconciliation table: phantom label → action (rename / delete) → real entity name (if rename). (2) Confirm the table with the user before executing any deletion. (3) For each rename, update all 3 layers (sidebar button, content div heading, slug→tab map). (4) For each delete, remove sidebar button + content div + slug→tab entry + any reference in dashboard cards. (5) Run the discovery endpoint and verify the deleted slugs are not produced (confirms the data source agrees they don't exist). (6) Reference: May 1 — Outlook v13 reconciled 7 entries to real Outlook category names, deleted 5 genuine phantoms (Training, R&D, Taqat Facilities, Watercooler, plus 4 dashboard-only items: Webhook/Production, Security & AWS WAF, Odoo Task Mgmt, AI Workshop).

589. **Idempotency in a discovery pipeline requires stale-output cleanup AND a sibling display-name index — write-on-top alone leaks orphan files and loses friendly names** — On 2026-05-01 first discovery implementation for Outlook v13 wrote new `cat_<slug>.csv` files but never deleted old ones. After a category rename in Outlook, two files would coexist (old slug + new slug), and the orphan would still be searchable returning stale data. Also: filename slug ("khairgate_bms") loses the original display name ("KhairGate - BMS"); rendering the slug back to the UI by un-slugging gives wrong-cased / wrong-punctuation versions ("Khairgate Bms"). **Why:** idempotent discovery means "after running, the output state matches the input state exactly" — not just "the new files are written". Stale files violate this contract. And slug-only filenames are lossy — un-slugging is a one-way function that drops capitalization, hyphens, and special characters. **How to apply:** (1) At the start of every discovery write, build a set of expected output filenames; after writing, delete any matching-glob file not in the expected set. (2) Persist a sibling index file (e.g., `cat_index.json` mapping slug → original display name) on every discovery run; downstream endpoints load it to recover friendly names. (3) Test idempotency formally: run discovery twice in a row, diff the output dir — should be identical. Then delete a category in source, run discovery, confirm the orphan file is gone. (4) For multi-consumer pipelines (search + list + row-viewer), all consumers must load the same display-name index — don't let each one re-derive names independently. (5) Reference: May 1 — Outlook v13 `/api/extract-categories` deletes stale `cat_*.csv` before writing new ones, writes `cat_index.json` with slug→display map; `/api/search`, `/api/list-categories`, `/api/category-rows` all load the index via `Get-CategoryIndex()` to render real category names.


## PMO Dashboard v13 Phase 4 Finish (May 1, 2026 — afternoon session)

590. **PowerShell `ConvertTo-Json` unwraps 1-element arrays into single objects — JS callers crash with `data.field.map is not a function` — fix with dual layer: PS `[object[]]@(...)` cast + JS `Array.isArray` defensive normalizer** — On 2026-05-01 v13 PMO Dashboard Email tab "Emails Awaiting Reply" broke with `TypeError: data.unreplied.map is not a function` after dedup collapsed many emails to 1 sender. Root cause: PowerShell `ConvertTo-Json` treats a 1-element array as a single object (e.g., `@($x) | ConvertTo-Json` on a 1-element array emits `{...}` not `[{...}]`). JS callers using `.map()` then crash. The Mentions tab worked only because ≥2 unique threads remained after dedup. **Why:** silent contract violation between server (which thinks it's returning an array) and client (which sees a non-iterable object). Asymmetric — works at ≥2 elements, breaks at 1, hides at 0. **How to apply:** (1) Server-side: wrap array-returning fields with `[object[]]@(...)` cast at the response object construction. (2) Client-side: defensive normalizer at the top of every fetch handler — `var arr = Array.isArray(data.field) ? data.field : (data.field && typeof data.field === 'object') ? [data.field] : [];`. (3) Use BOTH layers — server fix + client defense. (4) Audit other endpoints in the same file for the same pattern. (5) Reference: `D:\Project Management\Outlook\Run-Update-Server.ps1` lines 689 + 810 + `PMO Dashboard_v13.html` `fetchUnreplied` + `fetchMissedMentions`.

591. **PowerShell HTTP listener does NOT auto-reload code changes — fix is invisible until process restart, leading to "fix didn't work" reports** — On 2026-05-01 fixed `$i.Category` (singular, returns null) → `$i.Categories` (plural, correct CSV column) at line 735 of Run-Update-Server.ps1. User refreshed dashboard, drafts STILL showed UNCATEGORIZED. Investigation: PowerShell listener was started before the edit, the running process still had the buggy code in memory. Restart picked up the new code. **Why:** PowerShell scripts are not hot-reloaded by `System.Net.HttpListener`. The script file is read once at process start; subsequent edits to the .ps1 file have zero effect on a running listener. Same issue affects any long-running PS-based daemon. **How to apply:** (1) When a PS listener fix "doesn't work", FIRST verify the user restarted the process before debugging code. (2) Add a version banner that prints at startup so user can confirm which version is running. (3) Document restart-required in the fix instructions explicitly: Ctrl+C the running window, re-launch the .ps1, verify version banner. (4) Same pattern applies to Python sidecar, Node service, etc. — only restart-on-save dev tools (vite, nodemon) auto-reload. (5) Reference: May 1 — Run-Update-Server.ps1 `$i.Category` → `$i.Categories` fix required user to manually Ctrl+C + re-launch.

592. **PowerShell field-name plurality mismatches fail SILENTLY — `$i.Category` vs `$i.Categories` returns null without error — always grep CSV header before naming fields** — On 2026-05-01 unified `Run-Update-Server.ps1` had two endpoints reading the same `inbox_emails.csv`. Line 658 used `$i.Categories` (correct, plural). Line 735 used `$i.Category` (incorrect, singular). Both load through `Import-Csv` so the field is the EXACT CSV header string. Wrong-name access returns `$null` silently — no exception, no warning. Result: line 735's endpoint silently returned empty category for every email, breaking all downstream category-dependent logic. **Why:** PowerShell PSObject property access on Import-Csv objects is dynamic — `$obj.NonExistentProperty` returns `$null`, doesn't throw. CSV column names are case-sensitive in property access. Plurality is the most common silent-failure source because it's a 1-character typo. **How to apply:** (1) Before referencing any CSV field in PS code, run `Get-Content file.csv -First 1` and copy-paste the EXACT column name. (2) Define field names as constants at top of script (`$CategoriesField = "Categories"`) so all references are in one place. (3) Add a startup self-check: load 1 row, assert all expected fields are non-null on at least one row, fail fast if not. (4) When debugging "data is empty" bugs, ALWAYS grep for field-name typos across the script before assuming the data source is broken. (5) Reference: May 1 — Run-Update-Server.ps1 line 735 typo blocked email category extraction for the entire missed-mentions endpoint.

593. **Dynamic-DOM updates: pre-build HTML once with stable-ID spans, then update via textContent only — never concatenate dynamic data into DOM-set strings** — On 2026-05-01 building Phase 4 daily briefing banner, hit a security_reminder_hook that flagged a DOM-set string with user-influenced data. Refactored to use predefined HTML structure with `<strong id="ebb-unreplied-count">0</strong>` placeholders + JS sets `.textContent` per ID + toggles `.style.display`. **Why:** even when content "looks safe" (e.g., counts are numbers), future code changes might add user-controlled data to the same template. Hooks flag the pattern itself defensively. textContent + DOM walk is XSS-impossible by construction. **How to apply:** (1) For dynamic UI elements that update with backend data, write the static HTML structure ONCE in the markup (with placeholder IDs), then update via JS reading data into those elements via `.textContent` only. (2) Conditional visibility via `element.style.display = 'none'/'block'/'inline'` instead of conditionally building HTML strings. (3) Pre-make all required spans even if some are conditionally hidden — easier to toggle than to insert/remove. (4) The same applies to React/Vue (use `{value}` not raw HTML injection props). (5) Reference: May 1 — Phase 4 daily briefing banner uses ID-tagged spans + `pillUnrepliedCount.textContent = String(u)` instead of DOM-set template strings.

594. **Closure-scoped variables in IIFE-A are invisible to IIFE-B — bridge via `window.X = X` exposure OR direct localStorage access from IIFE-B** — On 2026-05-01 v13 PMO Dashboard's Phase 4 IIFE could not see `loadLog`/`saveLog` declared inside the main IIFE. Result: `autoDraftEmailEntries()` failed silently because `if (typeof loadLog !== 'function')` short-circuited. Fix: bypass the closure entirely in Phase 4 IIFE by accessing localStorage directly with the same key the main IIFE uses (`var EMAIL_LOG_KEY = 'pmo-action-log-v1'; function _emailLoadLog() { return JSON.parse(localStorage.getItem(EMAIL_LOG_KEY) || '[]'); }`). **Why:** JS closures isolate scope by IIFE. Two `(function(){...})()` blocks in the same `<script>` cannot see each other's variables unless one explicitly exposes via `window.X`. **How to apply:** (1) When extending an existing IIFE-organized codebase from a new IIFE, do NOT assume helpers from the original IIFE are accessible. (2) Two clean approaches: (a) modify the original IIFE to expose helpers via `window.helperName = helperName`, OR (b) re-implement the read/write directly in the new IIFE with same storage key. Approach (b) is safer when the original IIFE is brittle/locked. (3) Document the storage key as a "shared protocol" so both IIFEs know they're reading the same data. (4) Same pattern applies to TEAM_ROSTER closure bug fixed in v12 earlier in the project. (5) Reference: May 1 — v13 Phase 4 IIFE uses `_emailLoadLog`/`_emailSaveLog` to bypass main-IIFE closure-scoped `loadLog`/`saveLog`.

595. **One-shot data-shape migration in client code — drop entries matching legacy patterns so they auto-recreate with new schema — must catch ALL legacy patterns including alarm placeholders** — On 2026-05-01 v13 email auto-draft logic changed twice in one session: first version saved `project: '(Email)'` placeholder, second version saved `project: '⚠️ UNCATEGORIZED — assign...'` alarm. Migration v1 only dropped `project === '(Email)'`. After server restart the UNCATEGORIZED drafts persisted because migration didn't catch them. Fix: extend predicate to `p === '(Email)' || p.indexOf('UNCATEGORIZED') !== -1`. **Why:** schema evolution often creates 2+ legacy patterns. Migration must catch ALL of them or stale data persists indefinitely. Single-pattern migration creates "ghost" entries that survive every refresh. **How to apply:** (1) When changing default values for new entries, list ALL prior placeholder values (original fallback, intermediate iterations, alarm strings) and include each in the migration predicate. (2) Use a fingerprint approach — match if `project` is in any known legacy set, OR matches alarm-prefix like `⚠️`. (3) Test by running 2+ generations of legacy data through migration and confirming a clean refresh. (4) Idempotency check: run migration twice — second run should be no-op. (5) Reference: May 1 — autoDraftEmailEntries migration extended from single-predicate to multi-predicate.

596. **Trust user-curated source-of-truth labels over canonical lookup tables — when user labels emails by category, use the category label DIRECTLY as project name; surface mismatches as alarms, not fallbacks** — On 2026-05-01 v13 had a 30+ branch `mapEmailCategoryToProject()` that mapped Outlook categories to canonical Project Register names. User pushback: "every time I allocate category in email — if you found no category, alarm me." Refactored to use first non-empty category text directly as project name + `null` return when missing → renderer shows "⚠️ UNCATEGORIZED". **Why:** canonical mapping is brittle (every new project requires code change), opaque (user doesn't know why "Aqraboon" became "KhairGate - Alaqraboon Wallet"), and silent (mapping miss → fallback to placeholder). Direct-label trust + alarm-on-missing matches the user's mental model and surfaces real data-quality gaps. **How to apply:** (1) When the user is the data source AND maintains discipline (every email categorized), use their labels as-is — case preserved. (2) Replace canonical-lookup-with-fallback with "use first non-empty input value or alarm-if-missing". (3) Mismatches between user discipline and reality become alarms (forces user to fix the source data, not the code). (4) Document the discipline in the alarm message itself so the alarm is self-explanatory. (5) Same pattern applies to: tag-based filing systems, custom field values in DevOps, manual classifications. (6) Reference: May 1 — v13 `mapEmailCategoryToProject` refactored from 30-branch lookup to 5-line direct-label extraction.

597. **Bi-weekly compressed reveal vs snapshot reveal — choose based on stakeholder bandwidth + reveal-of-acceleration tradeoff** — On 2026-05-01 user faced choice for Sun May 17 demo: reveal v11 only (Phase 2), v12 only (Phase 3a), or v13 (Phases 2+3a+4 all at once). Three strategies emerged: (α) Snapshot — one phase per demo, sustainable but hides ready-to-ship features; (β) Compressed — reveal all completed phases at next demo, demonstrates acceleration but big-bang risks overwhelm; (γ) Hybrid — partial reveal balancing both. User picked β for May 17. **Why:** when work accelerates faster than the demo cadence, snapshot reveal feels artificially restrained; compressed reveal demonstrates real velocity but risks raising "did you skip estimation?" trust questions. Hybrid is the safe middle. The right choice depends on (a) Tier B/Sponsor bandwidth, (b) cadence-trust history, (c) phase coupling. **How to apply:** (1) Before any multi-phase demo, evaluate which reveal strategy fits the stakeholder context. (2) For a high-trust audience that already saw Phase 1 land cleanly, β (compressed) is the right play. (3) For new/uncertain audiences, α (snapshot) lowers risk. (4) Always show the road forward at the demo to anchor cadence regardless of reveal strategy. (5) Document the reveal decision in the talking-points deck. (6) Reference: May 1 — STP v4 chose β-compressed reveal for May 17.

598. **"Create new version every update" rule has explicit user-override cases — single growing file is correct when phases form one coherent system that will eventually port wholesale to a new stack** — On 2026-05-01 standing user rule says "create new version with every update" → would have spawned v14/v15/v16/v17 for Phases 5-8. User explicitly overrode: "create phase 5,6,7,8 till the v2 Electron Version in the same file v.13". Reasoning: v13 grows progressively across bi-weekly demos until all 8 phases are in it; THEN entire system ports to Electron as a unit. Multi-version files would just create maintenance overhead with no audit-trail benefit. **Why:** the version-per-update rule serves audit trails. When the audit trail will be replaced by a complete platform migration (v2 Electron), preserving every intermediate version adds noise. Single growing file is cleaner because: (a) consistent demo file path, (b) no cross-version drift, (c) port-to-new-stack is one file, not five. **How to apply:** (1) When a user invokes "do X in the same file" against a standing rule, treat it as an explicit override and document it in the plan/memory. (2) Never reflexively follow "every-update-new-version" rules without checking if the user wants the override. (3) Document override intent in plan artifacts. (4) When the migration target lands, the single growing file becomes the comprehensive port source. (5) Reference: May 1 — `PMO Dashboard_v13.html` grows for Phases 5-8 (~13K → ~23K lines); v2 Electron migration ports the final v13 wholesale starting Sun Jul 26+.

599. **Folder-rename + file-archive operations need careful "what's still referenced?" audit — classify each file as ACTIVE / FUTURE-DEMO / HISTORICAL before moving** — On 2026-05-01 archived v3-v12 PMO Dashboards then user asked "we still work on v10/v11/v12 right?" — confusion because v10 (May 3 demo), v11 (frozen baseline), v12 (frozen Phase 3a) are technically "future demo artifacts" still in active rotation, not historical. Restored v10/v11/v12 to main folder; archived v3-v9 only. **Why:** "archived" is a semantically loaded folder — implies "no longer used". When a multi-version system has versions in 3 categories (active dev, future demo artifact, truly historical), "archived" is correct only for the third. Future-demo artifacts are NOT historical. Mistakenly archiving them creates UX friction (user has to navigate to archive to find the file they need next Sunday). **How to apply:** (1) Before archiving, classify each file: ACTIVE / FUTURE-DEMO / HISTORICAL. (2) Only HISTORICAL belongs in archive; FUTURE-DEMO stays in main folder. (3) When user asks "do we still need X?", answer based on the date of next demo, not on file age. (4) For the demo cadence pattern: every locked version that has a Sunday demo date in the future is FUTURE-DEMO, not historical. (5) Reference: May 1 — v3-v9 archived; v10/v11/v12/v13 stayed in main folder.

## PMO Dashboard v13 Phases 5-8 Build (May 2, 2026 — single-session sprint)

763. **Phased single-file build patterns — three rules that keep multi-week growing-HTML-file builds shippable: (a) per-day pause-for-verification creates artificial friction when scope is additive-only; ship all days continuously and verify ONCE at the end; (b) phased builds in single file grow IIFE-by-IIFE, each phase = its own IIFE = self-contained closure + console.log signature for diagnostic; (c) entry schema scales by adding OPTIONAL fields per phase — `scope` discriminator routes UI/filter logic, never remove old values, only add new** — Three patterns from May 2 PMO Dashboard v13 Phases 5-8 (~13K lines → ~16K lines via 4 new IIFEs). **(a) Continuous shipping for additive single-file builds:** May 2 was building Phase 5 in 8-day sequence. After Days 1-2, paused for verification. User pushback: "why pause? auto mode is on, all phases land in same v13 file, additive only." Continued straight through Days 3-8 + Phases 6-8 in single session. **Why:** verification-per-day made sense in older "create new version every update" pattern where each day produces a separate artifact and a regression at Day N requires reverting to Day N-1. In single-growing-file pattern with additive-only changes, regression risk is zero because: (i) all new code is in new IIFEs that don't touch existing logic; (ii) new endpoints don't break old endpoints; (iii) new tabs don't affect old tabs. Per-day pause adds no safety, only friction. Phased builds in a single growing file should run continuously when design is additive-only. Reserve per-phase pause for builds where each phase modifies prior phases. Verify ONCE at end. Auto mode + clear design doc = signal to ship continuously. **(b) IIFE-per-phase + console.log signatures:** Each new IIFE has its own startup `console.log('[v13 Phase N] ... ready. Helpers: window.X, window.Y. Endpoint: /api/pmo/...')`. **Why:** when debugging "is feature X working?", DevTools console immediately shows which phase IIFEs initialized successfully (signatures present) vs which failed (signature missing → IIFE didn't load → check for syntax error). Self-contained closures prevent cross-phase variable pollution. Each phase has its own `'use strict'` + own helpers + own endpoints. **Rules:** For multi-phase additive builds in a single HTML file, use one IIFE per phase. Start every IIFE with `'use strict';`. End every IIFE with `console.log('[file vN Phase M] ready. Helpers: window.X, window.Y, ...')`. Use phase-specific local helpers inside IIFE — don't pollute global scope. Expose only the public-API helpers via `window.helperName = helperName`. When debugging, check console for ALL phase signatures present — missing one = silent loading failure. v13 has 5 IIFEs total: main (Phase 1-3a), Phase 4 finish, Phase 5, Phase 6, Phase 7+8. **(c) Schema scales via optional fields + `scope` discriminator:** Action Log entries grew from Phase 1 base shape to Phase 5+ enriched shape with optional fields: `scope` ('project' / 'team' / 'assignee' / 'email' / 'meeting'), `meetingId`, `meetingSubject`, `meetingDate`, `emailKey`, `emailCategory`, `emailUncategorized`, `boardAsk`, `autoCappedAt`, `closedAt`. **Why:** every phase added new entry-type (email-reply, meeting-action, etc.) without breaking existing entries. `scope` field is the discriminator: filter logic checks `e.scope === 'meeting'`, render logic checks `e.scope === 'email'`, etc. Backwards compat: entries from before Phase N don't have new fields, so checks must use `=== value` not `!== value` (avoid false positives from undefined). **Rules:** Add a `scope` (or equivalent discriminator) field early in schema design — never remove old values, only add new. New phase fields = optional, with sensible defaults when missing. Filter logic: `if (e.scope === 'meeting') ...` — explicit equality check, never negate-check on missing values. Migration logic: when changing default values, migrate ALL legacy patterns (see lesson #595). UI filters: scope dropdown grows with each phase but defaults to "All Scopes" so old entries still appear. **Why these three cluster:** All are single-file phased-build discipline rules — together they let a 13K-line HTML file grow to 16K lines across 4 phases without breaking earlier phases or requiring per-phase verification. Without these rules, additive-but-undisciplined builds fragment into "is this working?" checks that bottleneck velocity. **How to apply:** (1) For additive single-file phased builds, ship continuously; verify once at end. (2) One IIFE per phase + console.log signature; never pollute global scope; expose only public-API helpers via `window.X`. (3) Schema grows via optional fields + scope discriminator; never remove old values; explicit equality checks in filter/route logic. References: 2026-05-02 — Phase 5 Days 1-8 + Phases 6+7+8 shipped in single session; v13 file structure (5 IIFEs); v13 Action Log entries (5 scope values + ~10 phase-specific optional fields). (Folded former #600, #608, #609.)

761. **Office COM (Outlook + Word + Calendar) lifecycle and activation — three rules that prevent silent failures: (a) Outlook Calendar COM (folder index 9 = olFolderCalendar) requires `Items.IncludeRecurrences = $true` BEFORE iterating, or recurring meetings show only first instance; (b) Word COM for .docx extraction needs explicit `Visible=false` + readonly open + `Close($false)` + `Quit()` + `ReleaseComObject` in a try/finally — leaks Word.exe process handles otherwise; (c) COM activation is integrity-level-locked — admin-elevated PowerShell cannot connect to user-level Outlook (HRESULT 0x80080005 / CO_E_SERVER_EXEC_FAILURE) — match server integrity to client integrity** — Three patterns from May 2 PMO Dashboard v13 Phase 5 (Meeting Intelligence: Calendar COM + Word MOM extraction + dashboard wiring). **(a) Calendar COM IncludeRecurrences:** Mirror of `Extract-Inbox.ps1` (folder 6 = olFolderInbox), with 3 extra steps for calendar (folder 9): (i) `$items.IncludeRecurrences = $true` — without this, recurring weekly/daily meetings show only first instance, not future occurrences; (ii) `$items.Sort("[Start]")` before `Restrict()` — sort must happen BEFORE restrict for the date filter to work properly; (iii) `Restrict("[Start] >= '$startStr' AND [Start] <= '$endStr'")` — server-side filter, much faster than iterating all events. Outlook's Calendar is a recurring-event store, not a flat event list. Iterating `$Calendar.Items` directly without IncludeRecurrences gives each recurring event ONE row (the master), not future instances. Use `g` format for date strings for COM filter compatibility. Standard calendar fields: EntryID, Subject, Organizer, Start, End, Duration, Location, RequiredAttendees, OptionalAttendees, Categories, MeetingStatus, IsRecurring, ResponseStatus. **(b) Word/Outlook/Excel COM lifecycle:** Pattern: open in try, close+quit+release in finally. Without the close+quit+release sequence, every script run leaves a Word.exe / Excel.exe / Outlook.exe process running invisibly. COM objects are reference-counted by .NET on the runtime side, but the underlying Office process doesn't auto-quit when references are released — it stays running. Hundreds of orphan processes can accumulate over time. Rules: (i) Always `$word.Visible = $false` (or `$excel.Visible = $false`) BEFORE any Documents.Open / Workbooks.Open. (ii) `Documents.Open(path, $false, $true)` — third param `$true` is ReadOnly mode (matters for permissions + file locks). (iii) `try/finally` block: open in try, `$doc.Close($false)` + `$word.Quit()` + `[System.Runtime.InteropServices.Marshal]::ReleaseComObject($word)` in finally. (iv) Same pattern for Outlook COM, Excel COM, PowerPoint COM. (v) Test by running script 100× and checking Task Manager for orphan processes. **(c) COM integrity-level match (extends to lesson #757 Task Scheduler):** May 2 evening: after fixing every visible code path, calendar extraction still failed silently. Dashboard auto-recovery surfaced real script error: `FATAL ERROR: Retrieving the COM class factory for component with CLSID {0006F03A-0000-0000-C000-000000000046} failed due to the following error: 80080005 Server execution failed (Exception from HRESULT: 0x80080005 (CO_E_SERVER_EXEC_FAILURE))`. Diagnostic chain: csv missing → JS auto-recovery → script output surfaced → real error visible → root cause identified. Root cause: PowerShell HTTP listener was launched as **Administrator** (window title prefix "Administrator: PMO Server (port 5050)"), while Outlook runs as the **regular user**. Windows COM cannot bridge integrity levels — an elevated process is forbidden from instantiating a COM class factory hosted by a non-elevated process. Fix: close the elevated PowerShell window, re-launch the same script WITHOUT "Run as administrator" (just plain double-click the .bat or the .ps1). After plain-user re-launch, calendar extraction immediately returned 22 events, populating Meetings tab successfully. Windows User Account Control (UAC) enforces process integrity levels (Low / Medium / High). COM activation cross-IL is blocked by design as a security boundary — otherwise any unprivileged process could puppet an elevated process. The error message is opaque (CLSID GUID) and HRESULT 0x80080005 ("Server execution failed") sounds like a server bug, not a permissions issue. The diagnostic clue is buried in the window title bar. **Rules:** (1) For ANY COM-based local integration (Outlook / Excel / Word / IE / Access / PowerPoint), the calling process AND the COM server must be at the **same integrity level**. (2) When automating user-level apps (Outlook etc.), launch the calling script as the **regular user** — never as Administrator. (3) When debugging "ClassFactory failed 0x80080005" on Windows, FIRST check: does the PowerShell window title start with "Administrator:"? If yes, re-launch without elevation. (4) When the COM server requires elevation (rare — e.g., system-level WMI), then caller must also elevate. (5) Add a startup self-check to the PS script: detect elevation via `[Security.Principal.WindowsPrincipal][Security.Principal.WindowsIdentity]::GetCurrent()).IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)` and warn if elevated when it shouldn't be. (6) Document the integrity-match rule in the script header and in any setup .bat — make it impossible to miss. (7) The diagnostic that surfaced this took 4 layers of indirection (CSV missing → JS auto-recovery → endpoint stdout capture → dashboard surfaced output) — invest in surfacing real script output to UI for any background pipeline, otherwise root causes stay opaque. **Why these three cluster:** All are silent-failure modes specific to Office COM automation — recurring meetings invisibly missing, processes leaking until Task Manager fills with orphans, COM activation failing with cryptic HRESULT codes. Diagnostic chain is the same in all three: end-to-end test reveals data wrong/missing, root cause is in COM activation/iteration discipline. **How to apply:** (1) For Outlook Calendar COM, ALWAYS `IncludeRecurrences = $true` + sort-before-restrict. (2) For Word/Excel/Outlook COM, ALWAYS try/finally with `Visible=false` + readonly + Close+Quit+ReleaseComObject. (3) For ANY local COM integration, match integrity level — never run automation script as Administrator when target Office app runs as user. (4) Surface real script output in UI for background pipelines so cryptic HRESULT codes are visible, not buried. References: 2026-05-02 — `Get-CalendarEvents.ps1` lines 18-25 (IncludeRecurrences pattern), `Extract-MOM.ps1` lines 30-43 (Word COM lifecycle), Phase 5 verified end-to-end (Generate Agenda + Extract MOM + Route to Action Log + JSON persistence + meeting auto-drafts all confirmed working) after re-launching `Run-Update-Server.ps1` as regular user. (Folded former #601, #604, #612.)

762. **PowerShell + npm preflight building blocks for shipping Windows-desktop integration scripts — three patterns: (a) `Read-PostBody` helper for HTTP listener POST endpoints (`$context.Request.HasEntityBody` + `StreamReader` on `InputStream`, with UTF-8 enforced — see lesson #656); (b) npm `pre*` script + tiny `.cjs` helper is the cleanest pattern for ENV-VAR / TOOL-PRESENCE preflight guards (1-line script + 5-10 line .cjs); (c) multi-resolution Windows ICO files can be built from scratch in PowerShell using `System.Drawing` + manual ICO header assembly — no ImageMagick / Inkscape / sharp dependency needed when scaffolding** — Three patterns from May 2-3 PMO Dashboard v13 + PMO_OS Phase A scaffolding. **(a) `Read-PostBody` helper:** PowerShell's `System.Net.HttpListener` doesn't expose request body as string by default — must read input stream manually. `$context.Request.InputStream` + `StreamReader` with the request's `ContentEncoding` is canonical. Without helper, every POST endpoint duplicates 4 lines of stream-reading code. Define once at top: `function Read-PostBody($context) { if (-not $context.Request.HasEntityBody) { return "" }; $reader = New-Object System.IO.StreamReader($context.Request.InputStream, [System.Text.Encoding]::UTF8); $body = $reader.ReadToEnd(); $reader.Close(); return $body }` — note UTF-8 hardcoded per lesson #656 (system ANSI fallback corrupts Arabic/em-dash/smart-quote bytes silently). Use in every POST endpoint. Always check `$context.Request.HttpMethod -ne "POST"` and return 400 if wrong method. Reference: May 2 — Run-Update-Server.ps1 helper + 5 POST endpoints (save-agenda, extract-mom, save-action-log, save-board-pack, save-briefing). **(b) npm `pre*` + `.cjs` preflight guard:** May 3 evening, after lesson #616 amend recommended a `predev:all` pre-script for `ELECTRON_RUN_AS_NODE` defense, shipped `D:\PMO_OS\scripts\check-electron-env.cjs` (12 lines: check env, log clear error with both bash + PowerShell unset commands, exit 1) + added `"predev:all": "node scripts/check-electron-env.cjs"` to package.json. Verified both gates: `ELECTRON_RUN_AS_NODE=1 npm run dev:all` exits 1 with formatted error; clean env exits 0 and falls through to `dev:all`. **Why:** npm's `pre*` hook runs the same-named script before any `npm run X` invocation, in a child Node process — perfect for cheap preflight checks. Compared to alternatives: (i) `electron/main.ts` guard fires only AFTER electron starts (reactive not proactive); (ii) shell wrapper script requires user education; (iii) ESLint rule requires running lint separately; (iv) git pre-commit hook only fires on commit, not on dev launches. `pre*` is invisible to users (no extra command), platform-agnostic (Node runs everywhere), cheap (sub-50ms when passing). For any precondition that must hold before `npm run X` (env vars, tool presence, file existence, OS feature like Developer Mode), write a `pre<X>` script. Body = tiny `.cjs` helper at `scripts/check-<thing>.cjs` so package.json stays one-line and logic is testable in isolation. When precondition fails, exit 1 + emit clear error with: (a) what's wrong, (b) why it matters, (c) exact remediation commands for both shells, (d) reference to underlying lesson/doc. Pair with runtime guard (lesson #616 item 2) for defense in depth — pre-script catches at launch, runtime guard catches if env was set after pre-check. **(c) Multi-res ICO via PowerShell + System.Drawing:** May 3 evening Phase A Day 6, needed `D:\PMO_OS\build\icon.ico` for electron-builder NSIS. Generated 6-size ICO (256/128/64/48/32/16) matching v13's chart-bars favicon (navy #1a1a2e bg + 4 ascending bars in green/amber/blue/red) directly via PowerShell: `Add-Type -AssemblyName System.Drawing` → loop sizes → for each, create `Bitmap`, `Graphics.Clear` to navy, `Graphics.FillRectangle` for each bar with proportional positioning → save each as PNG to MemoryStream → manually assemble ICO with `BinaryWriter`: 6-byte ICONDIR header (reserved=0, type=1, count=N) + 16-byte ICONDIRENTRY per size (width/height as bytes; 0 means 256, otherwise size; 32-bpp PNG payload pointer + length) + concatenated PNG bytes. Result: 4 KB valid Windows icon resource accepted by electron-builder. `file build/icon.ico` confirms "MS Windows icon resource - 6 icons, 256x256 with PNG image data". **Why:** ICO format is well-documented and trivial to assemble. Windows accepts PNG-payload ICOs since Vista. Avoids dependency on ImageMagick (heavy install), online converters (privacy concern, unreliable), or sharp (Node-only, build-step). Pure PowerShell + .NET = always available on Windows. Multi-res ensures icon looks crisp at every Windows touchpoint (taskbar, alt-tab, file explorer, About dialog, installer wizard). For any scaffold needing placeholder/permanent icon, generate via PS one-liner inline rather than committing a binary from elsewhere. Always emit multi-res (256/128/64/48/32/16) — Windows picks the right size per context. Use 32-bit RGBA for transparent backgrounds (set Bitmap pixel format to `Format32bppArgb`). Save each size as PNG inside the ICO (vs uncompressed BMP) — shrinks file 5-10× without quality loss. Verify with `file path\icon.ico` — should report "MS Windows icon resource - N icons". Match visual language to source app's design tokens (color palette, shape grammar) for visual continuity. **Why these three cluster:** All are "Windows-desktop integration scaffolding building blocks" that ship together when bringing up a new PowerShell-server-backed dashboard or Electron app. The pattern is: write tiny self-contained PowerShell or .cjs helper + reuse it widely; avoid heavy external dependencies; document the discipline so future scripts inherit the same care. **How to apply:** (1) For ANY PS HTTP listener accepting POST, define `Read-PostBody` once with UTF-8 forced. (2) For ANY env-var / tool-presence precondition before `npm run X`, ship a `pre<X>` script + `scripts/check-<thing>.cjs`. (3) For Windows desktop icon scaffolding, use PS + System.Drawing manual ICO assembly; avoid ImageMagick/sharp deps. References: 2026-05-02 — `Run-Update-Server.ps1` helper lines 33-39 + 5 POST endpoints; 2026-05-03 evening — `D:\PMO_OS\scripts\check-electron-env.cjs` + `package.json predev:all` paired with `electron/main.ts:19` fail-fast guard; `D:\PMO_OS\build\icon.ico` (4 KB, 6 sizes) + companion `icon-256.png` (1.8 KB) generated from PS in ~5 seconds, visual = direct port of v13 favicon SVG at `PMO Dashboard_v13.html` line 93. (Folded former #602, #618, #619.)

764. **Persistent storage patterns for browser-served dashboards that need to outgrow localStorage — two rules: (a) JSON file persistence under `_data/{purpose}/YYYY-MM/{date}.{ext}` (month-sharded, append-on-save) replaces 5MB localStorage cap; localStorage stays as fast read cache, file write is the durable record; (b) `Storage.prototype.setItem` can be patched globally to add side-effects (auto-save to JSON file) without modifying every caller — but ONLY for one specific key (filtered), never globally** — Two patterns from May 2 PMO Dashboard v13 Phase 5 Day 7 (Action Log durable persistence migration). **(a) Month-sharded JSON files:** Schema: `_data/{purpose}/YYYY-MM/{date}.{ext}` where purpose = action-log / agendas / mom / board-pack / briefings. Server endpoint creates monthDir on demand. localStorage stays as fast read cache; file write is debounced via Storage.setItem hook (1500ms after last write). **Why:** localStorage 5MB cap blocks Phase 5+ scope. Month-sharded JSON files: (i) no size cap; (ii) git-friendly; (iii) OneDrive-syncable for backup; (iv) human-readable for audit; (v) Date-based filename = chronological browse. **Rules:** Use `_data/{purpose}/YYYY-MM/{date}.{ext}` schema for any "growing data" the dashboard produces. Wrap saves in `Storage.setItem` hook with 1500ms debounce. Always include schema version + savedAt + entryCount + entries fields in the JSON wrapper. Server creates monthDir on every write — idempotent. (See also #396 for the principle this implements — localStorage is not persistence.) **(b) `Storage.setItem` patch with key filter:** Every time `localStorage.setItem('pmo-action-log-v1', ...)` is called, fire `/api/pmo/save-action-log` 1500ms later (debounced). Implementation: patch `Storage.prototype.setItem` to call original then trigger debounced side-effect IF key matches the target. **Why:** existing Action Log code uses `localStorage.setItem` in many places. Patching `setItem` once intercepts all writes without touching each caller. Filtering by specific key ensures we don't accidentally trigger save on UNRELATED localStorage keys. **Rules:** Patch `Storage.prototype.setItem` ONCE per IIFE — preserve original via `var origSetItem = Storage.prototype.setItem`. ALWAYS filter by specific key — never apply side-effect to all setItem calls. Debounce side-effect (1500ms) so rapid sequential writes coalesce into one server call. Make side-effect non-blocking — call original setItem first, then trigger side-effect via setTimeout. Document the patch in a comment so future maintainers don't get confused by the mysterious server calls. **Why these two cluster:** Together they form the "durable persistence" pattern — JSON files are the source of truth, localStorage is the cache, the `setItem` patch is the bridge that keeps them in sync without invasive changes to every caller. Without the patch, every existing `setItem` call site would need to be edited to add server calls; with the patch, one IIFE intercepts all writes filtered by key. **How to apply:** (1) For any dashboard data growing past 5MB, use `_data/{purpose}/YYYY-MM/{date}.{ext}` JSON files; localStorage as cache only. (2) Patch `Storage.prototype.setItem` ONCE per IIFE with key filter; debounce side-effects 1500ms; document in comment. References: 2026-05-02 — `/api/pmo/save-action-log` writes `_data/action-log/2026-05/2026-05-02.json`; v13 `_wireAutoSaveJSON` IIFE. (Folded former #603, #607.)

605. **Bilingual EN/AR Board Pack reuses STP v3 paired-span pattern + built as ONE HTML string then saved as standalone file via Blob URL — opens in new tab without server roundtrip** — On 2026-05-02 Phase 6 Board Pack generator builds the entire HTML doc as a string (with embedded CSS + 1 toggleLang script + paired EN/AR spans), then offers two actions: "Save to disk" (POSTs to `/api/pmo/save-board-pack` which writes `_data/board-pack/Q2-2026/{date}.html`) AND "Open in new tab" (creates Blob URL via `var blob = new Blob([html], { type: 'text/html' }); var url = URL.createObjectURL(blob); window.open(url, '_blank');`). **Why:** standalone HTML output is portable — Mahmoud can email the saved file to Tier A as attachment, or print-to-PDF via browser. Embedding CSS inline (not external stylesheet) makes it work offline + email-attachable. Blob URL avoids the (XSS-flagged) inline-write pattern. The bilingual toggle button + `html[dir="rtl"]` CSS exactly mirrors STP v3 — proven pattern. **How to apply:** (1) For any "Board-quality" output, generate complete standalone HTML with inline CSS + paired EN/AR spans + lang toggle script. (2) Use Blob URL for in-browser preview. (3) NEVER use inline-write methods — XSS hooks flag them. (4) Always escape user-controlled data before HTML interpolation: `function _esc(s) { return String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;'); }`. (5) Reference: May 2 — `window.generateBoardPack` in v13 emits ~3 KB bilingual HTML.

765. **Windows scheduling + build primitives — two rules for shipping desktop apps that need to run on a developer or user laptop: (a) Task Scheduler "Wake the computer to run" + "Run task as soon as possible after a scheduled start is missed" gives 8 AM first-wake semantics without keeping laptop awake overnight; (b) electron-builder NSIS pipeline on Windows 11 requires Developer Mode (or Administrator) to extract `winCodeSign` cache — contains macOS dylib symlinks Windows refuses to create without `SeCreateSymbolicLinkPrivilege`** — Two patterns from May 2-3 PMO Dashboard v13 Phase 8 Daily Briefing + PMO_OS Phase A Day 5 build. **(a) Task Scheduler 'Wake to run' + 'Run if missed':** May 2 Phase 8 Daily Briefing needs 8 AM email push but Mahmoud doesn't want laptop awake all night. Solution: configure Task Scheduler with: (i) Trigger Daily at 7:55 AM; (ii) Conditions → Wake the computer to run this task = ON; (iii) Settings → Run task as soon as possible after a scheduled start is missed = ON. Task fires at 7:55 if laptop awake; if not, fires within 30 min of next wake. **Why:** matches user's actual usage pattern — laptop sleeps overnight, wakes when Mahmoud opens it in the morning. Task Scheduler handles "fire if missed" logic natively. No need for service/daemon. **Rules:** For 8 AM daily push tasks on user laptops, use Task Scheduler with both wake-to-run + run-on-missed-start enabled. Trigger 5 min before target time (7:55 for 8 AM goal) — gives buffer for COM startup. Action: `powershell.exe -ExecutionPolicy Bypass -File "C:\path\to\script.ps1"`. Document the exact Task Scheduler GUI clicks in setup notes. Companion to lesson #757 (`RunLevel Limited` for user-COM tasks). **(b) Developer Mode for electron-builder symlink extraction:** May 3 evening Phase A Day 5 build, `npm run build` failed with `ERROR: Cannot create symbolic link : A required privilege is not held by the client` during winCodeSign extraction. Setting `CSC_IDENTITY_AUTO_DISCOVERY=false` did NOT help — electron-builder still downloads + extracts winCodeSign even when not signing (used for resource embedding). Each retry redownloaded into a fresh hash subdir (272593323, 209868651, 399210431, 531272203 — different per run). `electron-builder --dir` mode also fails with same symlink trap. Only fix: enable Windows Developer Mode (`HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\AppModelUnlock\AllowDevelopmentWithoutDevLicense = 1`) which grants `SeCreateSymbolicLinkPrivilege` to non-admin processes. After enabling + clearing broken cache (`Remove-Item -Recurse -Force "$env:LOCALAPPDATA\electron-builder\Cache\winCodeSign"`), same `npm run build` succeeded end-to-end producing `release/PMO_OS_Setup_v2.0.0.exe` (74 MB NSIS installer) + `release/win-unpacked/PMO OS.exe` (177 MB). **Why:** electron-builder unconditionally extracts winCodeSign because the bundle contains binaries used for both code-signing AND resource manipulation (icon embedding, version-info patching). The macOS dylib symlinks inside are needed for cross-platform builds — Windows-only builds don't actually use them but still need them to extract. Windows symlink creation is a privileged operation by default; Developer Mode lifts that requirement specifically for app development. **Rules:** Before any `npm run build` involving electron-builder on a fresh Windows machine, FIRST verify Developer Mode is enabled: `(Get-ItemProperty "HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\AppModelUnlock").AllowDevelopmentWithoutDevLicense` should return `1`. If not, enable via Settings → Privacy & Security → For developers → Developer Mode = On (UAC prompt + ~30s install). One-time, persists across reboots. `CSC_IDENTITY_AUTO_DISCOVERY=false` does NOT help — don't waste time trying. `electron-builder --dir` does NOT help either — same symlink path. When error appears mid-build, clear broken cache before retrying. Document prerequisite in project's setup README so future contributors don't hit it cold. **Why these two cluster:** Both are Windows-specific scheduling/build primitives that are off-the-shelf with one-time configuration knobs but invisible until you hit them — Task Scheduler "fire if missed" is a check-box buried in the Conditions tab; Developer Mode requires a Settings → Privacy registry-flip. Both have CRYPTIC failure modes when not configured (laptop wakes at 8 AM but task didn't fire / `npm run build` fails with privilege error mid-extraction). **How to apply:** (1) For any laptop-resident scheduled task, use Task Scheduler with wake-to-run + run-if-missed; trigger 5 min early; document setup steps. (2) For any Windows machine running `electron-builder`, verify Developer Mode is on BEFORE first build; clear winCodeSign cache if you hit the symlink error. References: 2026-05-02 — `Send-DailyBriefing.ps1` + setup instructions in v13 Daily Briefing tab card; 2026-05-03 evening — `D:\PMO_OS\` Phase A Day 5 build (failed twice with symlink error before Developer Mode enable; succeeded immediately after with cache cleared). (Folded former #606, #617.)

767. **STP demo deck clone-and-edit + bulk pre-build patterns — two rules for keeping bi-weekly demo decks shipped on time without burning weekend prep slots: (a) clone-and-edit pattern `cp STP_v(N) → STP_v(N+1)` saves ~80% of work via 9-item edit checklist; watch for stale pacing math + Phase Roadmap "today" markers; (b) when N future demo dates with locked phase reveals are known, BULK PRE-BUILD all N decks in one session via `cp` + 7-edit minimum set per deck — Q&A blocks + tab content can stay as v(N-1) stubs since live demo uses the dashboard, not the deck** — Two patterns from May 2 PMO Dashboard v13 STP deck production (built v5 from v4 + bulk pre-built v6/v7/v8 in single session). **(a) Clone-and-edit per deck (9-item checklist):** May 2 built STP v5 from STP v4 via `cp` + targeted MD edits + HTML edits. Saved ~80% of work because v4's 16-tab structure + 8 anticipated Q&As + bilingual EN/AR paired spans + Phase Roadmap table all carried over. **9-item edit checklist:** (i) page title + sidebar subtitle + meta strip date/version; (ii) opening narrative replaced (was "compressed reveal" → "Phase 5 reveal"); (iii) NEW dedicated tab section for the keystone reveal (Tab 8 Meetings became today's keystone, replacing Email's keystone marker); (iv) 4 NEW Phase-N-specific Q&A blocks added; (v) Phase Roadmap table — mark TODAY's row with green-highlight + ✅ marker + "(today)" label + remove "(same file)" tag; (vi) Closing commits to N+1 phase by next demo date; (vii) Change Log adds new row + de-emphasizes prior row (remove green-highlight from v(N-1) row, transfer to v(N) row); (viii) pacing math recomputed if tab count changed (formula: `total = opening_seconds + (tab_count × seconds_per_tab) + keystone_demos + Q&A + closing`); (ix) links_manifest.json + .js bumped current. **Why:** stakeholders see the same audit-trail of decks across cadence (proven during v3→v4 transition); each deck stands alone if emailed externally; pacing math drift is the most common bug — adding 1 new tab without recomputing throws deck-walkthrough timing off. Always `cp v(N) → v(N+1)` instead of writing from scratch. Always update both MD + HTML in same session (drift = chaos). Always bump links_manifest.json + .js together. **(b) Bulk pre-build N future decks (7-edit minimum set):** May 2 built STP v6/v7/v8 (3 future decks for Jun 14 / Jun 28 / Jul 12 demos) in single ~30-min batch via `cp v5 → v6,v7,v8` + 7-edit minimum set per deck. **Why:** when calendar is locked (4 known demo dates with known phase reveals), pre-building all decks at once eliminates "Sunday morning crunch" pattern where each deck competes with weekend prep time. Future-Claude sessions opening these decks see the locked deck for that Sunday — no need to remember context. **7-edit minimum set per deck** (vs 9 for full clone-and-edit): (i) `<title>`; (ii) sidebar subtitle line; (iii) meta strip 5 fields (Date/Cadence/Reveal labels EN+AR); (iv) opening narrative paragraph (~3 sentences); (v) Phase Roadmap row highlight (✅ today + de-highlight prior); (vi) Closing commitment (commits to N+1 phase by next demo date); (vii) Change Log row (highlighted + prior de-highlighted). **Skip per-deck:** Q&A blocks (carry over fine), tab-content sections (carry over fine — live demo IS the content), pacing math text (recompute is optional — 1-min drift across 6 weeks is acceptable). Spend 5-10 min per deck max — anything more means scope creep on the bulk build. **Why these two cluster:** Both are demo-deck production discipline patterns that turn what feels like creative content work into a checklist-driven 5-30 min mechanical operation. Without them, every bi-weekly cycle re-litigates "what changed in this deck vs the last one" and burns Sunday morning. The 9-item full-clone is for the IMMEDIATE-next-demo deck (where keystone reveal demands fresh content); the 7-edit bulk pre-build is for FUTURE decks where the live demo is the actual content. **How to apply:** (1) For each immediate next-demo deck, `cp v(N) → v(N+1)` + run 9-item edit checklist; recompute pacing math; bump links_manifest.json + .js together with MD + HTML in same session. (2) For N known-future demos, bulk-build all decks in one session via `cp` + 7-edit minimum per deck; skip Q&A and tab-content (carry over fine). References: 2026-05-02 — STP v5 built from STP v4 in ~30 min (all 9 checklist items completed); STP v6+v7+v8 built in ~25 min total via 7-edit min-set per deck, 4 demos pre-locked end-to-end through Jul 12. (Folded former #610, #611.)

766. **Cadence direction-changes + roadmap pull-forwards touch ~15-30 files across 3 tiers even when only ~7 calendar dates change — file naming, talking-points content, dashboard headers, design doc demo dates, manifest descriptions, Action Log JSON entries, memory files, and global lessons all get touched. Two patterns: (a) cadence direction-change (Sun→Mon, weekly→bi-weekly) — surgical date swaps preserving historical artifacts as point-in-time records; (b) roadmap pull-forward of N weeks — same surface area PLUS forced re-purpose of pre-built decks (not just date swaps) PLUS dual-track demo pattern that's worth treating as its own deliverable** — Two patterns from May 3 PMO Dashboard v13 cadence sweep + PMO_OS Phase A pull-forward (commits L058-L059, the same Sunday). **(a) Cadence direction-change:** May 3 user corrected bi-weekly meeting day with Semir + Dr. Omar from "Sundays" to "Mondays" — every locked demo date shifted +1 day across 18-week roadmap (May 4 → Nov 17). Naive expectation: "7 dates change → 7 edits". Reality: 15+ files, ~60+ surgical edits, plus scheduling collision (Mon Jul 13 last v13 demo + Mon Jul 13 Phase A Electron build start) requiring 1-day shift on Electron roadmap (Tue Jul 14). **Why:** every prior day-of-week assumption is baked deep into artifact text — talking-points decks (titles, meta strips, opening narratives, Phase Roadmap tables, closing commitments, Change Log rows, AR translations), dashboard headers (BUILD LOG, Charter card "next review" pills, AR translation maps), design docs (build-window dates, demo dates, demo scripts), memory files (anchor day, confirmed dates), manifest descriptions, hardcoded auto-draft entries, lessons referencing "Sun {date}" demos. Filename naming may be locked by inbound-link integrity (e.g., Sunday_Talking_Points_vN had ~10 cross-references) — keep filenames, update content. **Rules:** Before declaring cadence change "done", grep entire workspace for OLD day-of-week token + every OLD locked date — expect 60+ matches if cadence is multi-month. Ask 3 disambiguating questions BEFORE editing: (i) when is the immediate next demo; (ii) does each future date shift +1 day or to nearest new-anchor; (iii) any same-day collisions with locked downstream events. Tier the work: Tier 1 = artifacts needed before next demo (1-2 days). Tier 2 = artifacts needed before next-next demo. Tier 3 = rolling artifacts touched per phase. Don't do all in one sitting. **Preserve historical artifacts as point-in-time records** — DO NOT rewrite lesson #N to say "Mon X" when it actually said "Sun Y" at write-time. Lessons are records of what was true at write-time. Add a NEW lesson documenting the cadence-change instead. Same rule for Action Log JSON files written before the change. Update memory file `meeting_cadence_*` first — that's the canonical anchor downstream sessions reference. Use a 2-column "old/new date table" at top of planning doc so every artifact edit can verify against single source of truth. **(b) Roadmap pull-forward + dual-track demo:** May 3 evening, immediately after Sun→Mon cadence sweep, user pulled v2 Electron Phase A start forward 10 weeks (Tue Jul 14 → Sun May 3 same day). Total roadmap compression: 18 weeks → ~16 weeks. v13 retirement pulled forward from Mon Nov 17 to Mon Aug 24 (3 months earlier). **Why this is more work than cadence sweep:** date-swap scripts catch ~70% of references mechanically, but remaining 30% require **content re-purpose** — STP decks pre-built for "Phase 5 reveal in v13" become "Phase B SQLite demo in v2 + Phase 5 as parity check" (whole narrative arc shifts). Phase A doc's Day 2-9 internal sequence (Wed Jul 15 / Thu Jul 16 / Fri-Sat Jul 17-18 weekend / Sun Jul 19 etc.) was hand-tuned for original calendar week and needed manual remapping to new May 3-14 calendar. **Why dual-track is worth its own pattern:** parallel-tracking (v13 production + v2 Electron skeleton both running simultaneously, dual-track demos) is a strategic pattern not a date sweep — pre-warn stakeholders ("you'll see two tracks Mon May 18"), reframe v13 phase reveals as feature-parity checks rather than separate demos, document new schedule in fresh memory file rather than overwriting old. **Rules:** Date-swap scripts should run THREE phases: (i) bulk regex pull-forward; (ii) follow-up pass for internal day-by-day sequences; (iii) manual review of pre-built decks for content re-purpose (not just date swaps). Pre-warn stakeholders at the demo immediately preceding dual-track demo: "Sunday I started parallel build; in 2 weeks you'll see both side-by-side." Reframe original-stack reveals (v13 phases) as feature-parity checks against new-stack (v2) — same content but accompanies v2 progress rather than running standalone. Capture schedule shift in a NEW memory file (don't overwrite the old roadmap memory) so original schedule remains accessible if parallel build slips and reverts to original timing. Solo capacity check: parallel-tracking 8-day Phase A build + ongoing demo prep + production daily-use of original system is heavy. Enforce Fri-Sat off + buffer days. If Phase A 8 days slips to 12, push first dual-track demo by 2 weeks — don't compress further. **Why these two cluster:** Both require sweeping ~15-30 files across content + naming + scheduling + memory layers; both have a "naive expectation" (7 dates → 7 edits) that misses the real surface area; both demand preservation of historical artifacts as point-in-time records rather than retrospective rewriting. The pull-forward is a strict superset of the cadence change in terms of surface area. **How to apply:** (1) For any cadence direction-change or roadmap pull-forward, expect 15-30 files touched across 3 tiers; tier the work; preserve historical artifacts. (2) Update memory file first as canonical anchor; use 2-column old/new date table at top of planning doc. (3) For pull-forwards, run THREE date-swap phases (bulk regex + day-by-day internal + content re-purpose for pre-built decks); pre-warn stakeholders for dual-track demos. References: 2026-05-03 — Sun→Mon pivot affected v13 BUILD LOG header, STP v3-v8 (12 files), CLAUDE.md PMO RESTRUCTURE, `meeting_cadence_biweekly.md` memory, `pmo_v13_phase4_finish_may1.md` memory, 6 Phase A-F design docs, Roadmap v2, links_manifest, Transformation Plan, plus 1-day shift to Electron Phase A start (Mon Jul 13 → Tue Jul 14) to avoid same-day collision; 2026-05-03 evening — pulled v2 Electron Phase A start Tue Jul 14 → Sun May 3 (10 weeks); affected ~30 files across Roadmap v2.1→v2.2, 6 Phase design docs, STP v4-v9 decks, CLAUDE.md PMO RESTRUCTURE, 2 memory files, links_manifest. STP v4 reworked as DUAL TRACK deck (v13 compressed reveal + v2 Phase A FIRST DEMO). v13 retirement: Mon Nov 17 → Mon Aug 24. (Folded former #613, #614.)



768. **Phase-A scaffold pull-forward + multi-repo workspace pattern — two rules for bringing up a new product alongside an existing governance workspace: (a) Day 1 + Day 4 same evening is viable when sample data is already structured (v13 PORTFOLIO_KNOWN map + Project_Register sidecar JSON) — pure-React component renders independently of Electron wiring; (b) separate local-only git repos per "project shape" — shared workspace (governance/docs/decks) + product workspace (product code) — keeps audit-trail discrete per concern, sidesteps cross-tracking ambiguity, allows different `.gitignore` semantics** — Two patterns from May 3 PMO_OS Phase A scaffold-and-product-render evening. **(a) Day 1 + Day 4 in same evening:** May 3 evening, after scaffolding `D:\PMO_OS\` Day 1 (folders + 14 config + entry-point files + npm install clean), jumped to Day 4 (PortfolioStatus 28-card render) same session. Total effort ~3 hours. **Why this works:** Day 4's "extract 28-project sample from v13 PORTFOLIO_KNOWN → src/data/projects-sample.json" can run independently of Day 2 (Electron BrowserWindow wiring) and Day 3 (Vite HMR confirmation) — React component renders identically regardless of whether running inside Electron or in plain browser via `vite dev`. Skipping Day 2-3 in parallel session is fine because: (i) `vite build` clean = renderer is shippable; (ii) `tsc --noEmit -p electron/tsconfig.json` clean = Electron main is shippable; (iii) Day 2's actual work is just `npm run dev:all` wiring (~15 min) which can land later. **Why the data part was easy:** v13's PORTFOLIO_KNOWN map (line 5815) only has 11 entries, but Project_Register_v2.json sidecar already has 30 entries with name/tier/stage/owner/eng-lead/sponsor/notes structure — a 50-line Python script extracted 28 active (excluded 2 containers) into a flat JSON. React component groups by tier + applies confidence pills (🟢/🟡/🔴) + visual language matching v13 navy + accent colors. **Rules:** When scaffolding a new Electron+React app, check whether sample data is already structured in the source app — if yes, Day 1+Day 4 can land same session. Defer Day 2-3 (Electron BrowserWindow + Vite HMR) to next focused window — `npx vite build` + `npx tsc --noEmit -p electron/tsconfig.json` clean is sufficient evidence toolchain works. Day-4 sample data should match production data shape closely (same field names, same enum values) so Phase B's SQLite migration is mechanical. Carry visual language tokens (colors, fonts) from source app into new Tailwind config so dual-track demo doesn't look like two unrelated apps. **(b) Multi-repo workspace pattern:** May 3 evening, after committing STP v3-v11 + v13 BUILD LOG to `D:\Project Management\` master (`a01bd341`), realized `D:\PMO_OS\` was on different drive path AND had no `.git/` yet. Initialized fresh local repo: `git init` → `.gitignore` already pre-configured (excluded `node_modules/`, `dist/`, `dist-electron/`, `release/`) → added two more excludes (`vite.config.{js,d.ts}` tsc derivatives + `.vite/` dev cache) → `git add -A` (24 files staged) → root commit `a99f5e5` with full Phase A baseline + Days 5-6 narrative. Two parallel local repos now: governance/PM at `D:\Project Management\` (master with 4 prior commits + today's `a01bd341`) and product code at `D:\PMO_OS\` (master with single root commit `a99f5e5`). **Why:** trying to track product code inside a PM workspace repo creates two problems — (i) `.gitignore` semantics differ (PM workspace has typed-narrative MD files that should never be ignored; product workspace needs aggressive `node_modules/` ignoring); (ii) commit history gets noisy (governance edits interleaved with code edits make `git log` hard to read for either audience). Separate repos = clean per-domain history + correct ignore rules + each can grow at its own cadence. **Rules:** When a new "project shape" appears in your workspace (e.g., a buildable product alongside a docs folder), `git init` it as its own repo from day one rather than tracking it within a parent workspace repo. Pre-stage `.gitignore` BEFORE first `git add` — saves a follow-up "remove tracked file from index" edit. Local-only is fine per CLAUDE.md DEFAULT-BRANCH PUSH RULE for solo workspaces (no remote setup needed until/unless multi-machine or multi-author). When committing across repos in same session, use distinct commit message prefixes (e.g. `pmo:` for PM workspace, `phase-a:` for product) so cross-repo `git log` greps are unambiguous. Document multi-repo layout in CLAUDE.md or workspace README so future-you doesn't ask "where does this live?". **Why these two cluster:** Both are "bring-up a new product evening" patterns — same calendar slot when a new product shape forks off an existing governance workspace. The Day 1+Day 4 pull-forward proves the toolchain works end-to-end visually; the multi-repo split keeps the audit trail clean from day one. Both rules need each other: without separate repos, the same-evening commit batch interleaves governance + code; without same-evening Day 4, the new repo has no rendered evidence the toolchain works. **How to apply:** (1) When scaffolding new Electron+React app alongside existing governance workspace, target Day 1 + Day 4 same evening if sample data is structured; carry visual language tokens for dual-track demo coherence. (2) Initialize new product as its own repo from day one; pre-stage `.gitignore`; use distinct commit message prefixes for cross-repo log greps; document layout in CLAUDE.md. References: 2026-05-03 evening — `D:\PMO_OS\src\data\projects-sample.json` (28 entries from Project_Register_v2.json) + `src/components/PortfolioStatus.tsx` (78-line React component, tier-grouped grid, confidence pills) + Tailwind config carrying v13's navy `#1a1a2e` + accent `#0f3460` (all shipped same evening as Day 1 scaffold); `D:\Project Management\` repo at `a01bd341` (PMO governance) + `D:\PMO_OS\` repo at `a99f5e5` (Electron product) initialized in same session; both master, both local-only, both auto-ignored derivatives. (Folded former #615, #620.)

616. **`ELECTRON_RUN_AS_NODE=1` in any shell forces the Electron binary to behave as plain Node — `process.type` becomes `undefined`, `require('electron')` returns the binary path string instead of the API object, and `app.isPackaged` throws `TypeError: Cannot read properties of undefined (reading 'isPackaged')` — the bug masquerades as a "broken main.ts" but the scaffold is correct, the env is poisoned** — On 2026-05-03 evening Phase A Day 2 smoke-test, ran `npm run dev:all` from a Claude Code bash shell — Electron crashed at `const isDev = !electron_1.app.isPackaged;` (compiled from `import { app } from 'electron'`). Direct `npx electron .` reproduced the same error. Probe script `console.log(typeof require('electron'), process.type)` from the Electron binary returned `'string'` and `undefined` — confirming Electron was NOT initializing the main-process context. Root cause discovered via `env | grep -i electron` → `ELECTRON_RUN_AS_NODE=1` was set in the bash sandbox env. After PowerShell-level `Remove-Item Env:ELECTRON_RUN_AS_NODE` and re-launch, probe returned `'object'` + `'browser'` + 30 main-process API modules, and the full `npm run dev:all` chain worked: Vite ready in 484ms / Electron 5 processes spawned / DevTools auto-opened / app console clean. **Why:** `ELECTRON_RUN_AS_NODE=1` is an official Electron env var that downgrades the Electron binary to a Node.js-compatible runtime. It strips out the main-process API layer entirely, so `require('electron')` falls through to the npm package's Node-resolution path — which exports the binary path as a string for tooling like `electron-builder` to locate the executable. The error message blames `main.ts`, but the scaffold is fine. The trap is that the env var can be set anywhere — Claude Code's bash sandbox (sets it for safety so agents don't spawn invisible GUIs), CI runners, IDE-integrated terminals, parent processes that launched Electron-as-Node for a separate purpose. **How to apply:** (1) When `app.isPackaged` (or any Electron API) throws "undefined" in a fresh scaffold that was working yesterday, FIRST check: `echo $ELECTRON_RUN_AS_NODE` (bash) or `$env:ELECTRON_RUN_AS_NODE` (PowerShell). If non-empty, that's the culprit. (2) Add an explicit env-var check at the top of `electron/main.ts`: `if (process.env.ELECTRON_RUN_AS_NODE) { console.error('FATAL: ELECTRON_RUN_AS_NODE is set — Electron is running as plain Node and cannot create BrowserWindows. Unset this env var and retry.'); process.exit(1); }` — fails fast with a clear message instead of an opaque TypeError. (3) Document the gotcha in the project's troubleshooting README + Phase F design doc — every Electron developer hits this once. (4) For Claude Code agent sessions specifically, **BOTH bash AND PowerShell sandboxes can inherit `ELECTRON_RUN_AS_NODE=1`** — the original "prefer PowerShell" workaround was wrong. Verified 2026-05-03 evening Phase A Day 2 re-test: `$env:ELECTRON_RUN_AS_NODE` was non-empty in PowerShell sandbox (`Remove-Item Env:ELECTRON_RUN_AS_NODE` returned "Cleared ELECTRON_RUN_AS_NODE"). The only reliable defenses are (a) the `electron/main.ts` guard from item (2) above (fail-fast at process startup with a clear message), and (b) explicit unset BEFORE every launch regardless of shell — `unset ELECTRON_RUN_AS_NODE` (bash) or `Remove-Item Env:ELECTRON_RUN_AS_NODE -ErrorAction SilentlyContinue` (PowerShell). A `pre*` npm script (e.g. `predev:all`) that runs this unset is a clean way to make the protection automatic so every `npm run dev:all` invocation is safe regardless of shell. (5) The diagnostic that surfaced this was the probe script (`require('electron')` shape inspection) — adopt this as a generic "is Electron really initializing?" health-check for any future bring-up. (6) Reference: May 3 evening — `D:\PMO_OS\` Phase A Day 2 smoke-test; bash launch failed with `app.isPackaged` TypeError; PowerShell launch ALSO had the env var set (verified 2026-05-03 evening re-test) — `Remove-Item Env:ELECTRON_RUN_AS_NODE` was required before launch succeeded end-to-end (window 1280×800, DevTools open, 28-card render, console clean). Scaffold (`electron/main.ts`, `package.json`, `dist-electron/main.js`) was 100% correct in both attempts — both Claude Code sandbox shells were poisoned.

769. **Cross-dashboard integration drift — three silent-failure classes that only surface when a user clicks the affected control: (a) jump-list dashboards with hardcoded `_v{N}.html` cross-references go stale silently when a sibling deliverable bumps version; (b) hardcoded `localhost:<PORT>` integration constants drift silently when planned services were never built or migrated to a different port; (c) `new Date(value + 'Z')` is a JavaScript NaN trap when the source already includes timezone (PowerShell `o` format produces `+00:00` not `Z`)** — Three silent-failure patterns from May 3 evening cross-dashboard audit (PMO Dashboard v13 + OKR v10 + Outlook v13). **(a) Stale `_v{N}.html` jump-list hrefs:** May 3 evening, user spotted PMO Dashboard v13's "Local PM Dashboards" tab opened `OKR_KPI_Dashboard_v8.html` when latest on disk was `_v10.html`. Audit revealed: only 1 of 11 cards was stale (OKR fell behind by 2 versions). Other 10 cards (Q2 PM Plan v8, Quarterly Performance v1, Cairo Quarterly v1, Bi-Weekly Agenda v7, Outlook Email v13, Department Workflows Manual V5, Claude Courses v31, DevOps Best Practice PDF V.1, DevOps Access Upgrade v7, Department Workflows & Roles V2) were current. **Why:** versioned filenames are an audit-trail pattern (every meaningful change → new file), but cross-dashboard links are point-in-time pointers that don't auto-track. Each jump-list href is just a static string; nothing on disk verifies target is "latest". When sibling deliverable bumps version, parent dashboard's link doesn't notice. The pattern silently drifts until a user clicks an old card and realizes they're seeing month-old data. Audit is cheap (Glob per card folder + compare filename) but has to be triggered. **Rules:** After ANY sibling deliverable version-bump, audit every dashboard that contains a jump-list of cross-references. Cheapest way: maintain `links_manifest.json` that lists every cross-reference + current target version, run `check-link-drift.ps1` script that Globs each card folder and reports stale entries. When NEW cards are added to jump-list, embed version number in description text (e.g. "v10 — live DevOps→label sync") so visual drift is obvious before clicks. Consider `latest` symlink convention OR `?v=current` query-string pattern if staleness becomes recurring. When cadence shift happens (lesson #766) OR sibling-version sweep happens, ADD "audit cross-dashboard jump-lists" as checklist item. **(b) Hardcoded `localhost:<PORT>` drift:** OKR v10 PM-Dashboard badge stayed "offline" because line 1677 hardcoded `var PM_DASHBOARD = 'http://localhost:3000/api'` while actual Azure DevOps cache server (`DevOps_Cache_Server.ps1`) listens on port **5051**. CLAUDE.md's reserved-ports list (5050 Outlook / 5051 Standalone PM / 5052 Taqat / 5053 D Drive Scanner) had NO 3000 entry — confirming 3000 reference was either scaffolded for unbuilt Bun server OR stale port from before migration. Sweeping workspace for `localhost:3000` returned 51 files; classifying them caught a SECOND silent bug: Outlook v13 line 1155 had `var PM_API = 'http://localhost:3000/api'` for Deep Search, but actual `/api/search` endpoint lives on port 5050 (`Run-Update-Server.ps1`). Both bugs silent because: (i) badge probe `.catch(...)` just sets badge text to "offline" without surfacing actual error; (ii) Deep Search would only fail when a user typed in search box AND toggled deep mode AND server happened to be down. Fix: repointed both files to correct ports + verified endpoint shapes match. **Why:** when workspace has multiple local services, port-to-service mapping must live in ONE source of truth (CLAUDE.md ports section) and every integration constant must be cross-checked. Hardcoded URL strings are pre-mortem candidates for drift — they survive code reviews because they look correct in isolation, but port-registry audit catches mismatch immediately. **Rules:** Maintain "Localhost ports" section in CLAUDE.md with PORT → SERVICE → SCRIPT triples (current: 5050 Outlook+Search/Run-Update-Server.ps1, 5051 DevOps cache/DevOps_Cache_Server.ps1, 5052 Taqat, 5053 D Drive). Periodically grep workspace for `localhost:` and verify every hit corresponds to a registered port. For dashboards with multiple integrations, use distinct named constants (`API`, `PM_API`, `CACHE_SERVER`) and document each comment-side which script serves it. When introducing new service, add to port registry FIRST, then write dashboard integration. When deprecating service or migrating ports, grep + update ALL references same session. Add runtime visible-error pattern to badge probes: when probe fails, badge shows red "Server X@5051: not responding" instead of grey "offline" — port number exposes integration intent. **(c) `new Date(value + 'Z')` NaN trap:** After fixing OKR v10's port, badge displayed `Cache: NaNm old • 27 projects` — server reachable but staleness math broken. Root cause: line 1778 had `var age = (Date.now() - new Date(data.lastFetched + 'Z').getTime()) / 60000;`. PS server emitted `lastFetched` as ISO-8601 with timezone-offset suffix already present (PowerShell's `o` format produces `+00:00` for ToUniversalTime, NOT `Z`). Adding `+ 'Z'` corrupted the string. Fix: removed the `+ 'Z'` append. JavaScript `Date` parses both `Z` and `+00:00` correctly when given clean input. **Why:** the `+ 'Z'` defensive concatenation was likely added when original API returned tz-naive timestamp (e.g. `new Date().toISOString().slice(0, -1)` without Z). When producer changes to a timestamp library that emits full timezone info, defensive concatenation becomes corruption. Type-coercion silence: `NaN` flows through arithmetic without throwing, so bug surfaces only as visible "NaNm" in UI. **Rules:** NEVER blindly append `'Z'` to a timestamp string you don't control. When parsing timestamps from server response, FIRST log `data.lastFetched` raw to console — confirm whether it ends with `Z`, `+HH:MM`, or no offset. If tz-naive, use `+ 'Z'` to assert UTC; if tz-aware, parse as-is. Add defensive guard: `var ms = new Date(value).getTime(); if (isNaN(ms)) { /* fallback or log */ }` — surfaces bug instead of visible NaN strings. When swapping backend servers, ALWAYS test timestamp-handling code paths since timestamp formatting is per-language conventional. PowerShell's `ToString('o')` is the standard "round-trip" format and emits ISO-8601 with timezone. **Why these three cluster:** All are silent-failure modes that only surface when a user clicks the affected control. The audit is cheap (Glob filenames / grep ports / log timestamps) but has to be TRIGGERED — there's no error, no warning, no console log. They cluster because they share the same origin: cross-dashboard integration constants drift over time as siblings version-bump, services migrate ports, and backends change timestamp formatters. **How to apply:** (1) After any sibling version-bump, audit jump-list hrefs across parent dashboards via Glob/`links_manifest.json`. (2) Maintain port registry in CLAUDE.md; grep workspace for `localhost:`; visible-error pattern in badge probes. (3) Never blindly append `Z` to server timestamps; defensive `isNaN` guard surfaces bugs. References: 2026-05-03 — PMO Dashboard v13 line 2935 stale `OKR_KPI_Dashboard_v8.html` → fixed to `_v10.html` (10 cards verified current via 9-Glob batch); OKR v10 line 1677 (3000→5051) + Outlook v13 line 1155 (3000→5050) port repoints; OKR v10 line 1778 `new Date(data.lastFetched + 'Z')` → `new Date(data.lastFetched)` after PS server's `o`-format already supplied `+00:00` offset. (Folded former #621, #622, #623.)

## Bilingual EN/AR translation completion via Playwright self-audit (May 4, 2026)

624. **Static analysis under-counts bilingual translation gaps by 5-10× — the only reliable audit of "is this fully translated?" is to spawn the file in a headless browser, toggle to AR, walk every tab, and scan for English text NOT inside `[data-i18n]` ancestors via `TreeWalker(SHOW_TEXT)`** — On 2026-05-04, after the user asked "is v13 fully translated in Arabic?", a static `pm-bilingual-qa` agent reported "PARTIALLY TRANSLATED — 14 gap locations". Built v14 with those 14 fixes, but when the user asked Playwright to verify, the headless browser surfaced **440+ additional unresolved data-i18n keys + 200+ JS-rendered English strings across 18 tabs** that the static agent never saw. The 14-gap initial report was 5% of reality. The true scope only emerges from runtime inspection. **Why:** static grep on `[A-Za-z]{3,}` matches PROPER NOUNS, code paths, English keys-as-text, and template literals identically — too noisy to filter manually. Static parsing of `data-i18n=...` finds attribute presence but not whether the key EXISTS in the T dictionary. Static analysis can't see the difference between `el.textContent = 'Loading...'` (translatable) and `el.textContent = data.someField` (data passthrough). Only at runtime, with a real T dictionary loaded, can you classify `dgT(key) === key` ⇒ unresolved vs resolved, AND walk visible text nodes filtered by `data-i18n` ancestor. **How to apply:** (1) For any bilingual deliverable claim ("is this translated?"), respond with "I'll verify in a browser" rather than running a static reviewer agent — the static agent is calibrated for structural issues (paired-span integrity, dir attribute presence) not coverage. (2) Standard verification harness: spawn HTTP server (Python `python -m http.server`) at the dashboard's folder; Playwright `browser_navigate` to the file; `browser_evaluate` `window.toggleLang()` to AR; iterate tabs via `window.showTab(id)`; run two scans per tab — (a) `[data-i18n]` keys where `dgT(k) === k` for missing dictionary entries, (b) `TreeWalker(SHOW_TEXT)` filtered by `!ancestor.hasAttribute('data-i18n')` AND `/[A-Za-z]{3,}/` AND `!/[؀-ۿ]/` for JS-rendered English leaks. (3) Patch in batched rounds (~50-100 keys per round), reload, re-scan — convergence usually takes 4-7 rounds. (4) Each round narrows what's left: Round 1 finds chrome/section labels, Round 2 finds modals + toasts, Round 3 finds table cells, Round 4 finds auto-draft/template strings, Round 5+ finds long-tail (status messages, error text, EVM glossary, threshold legends). (5) Acceptable remaining English at convergence: proper nouns (people, project names, brand names), code/file paths (`powershell.exe`, `_data/...`, `window.HOURLY_RATE`), date strings (need locale-aware conversion which is a separate concern), pre-existing localStorage entries with empty `*Ar` fields (legacy migration), mathematical formulas (`SPI = EV / PV`). (6) Reference: May 4 — v14 final state 798 data-i18n attributes, 0 unresolved, ~250 new T entries vs v13. The static-analysis report's "14 gaps" was 5% of the true 250+ scope; only Playwright surfaced the rest.

625. **The `data-i18n + applyLang(textContent swap)` pattern silently corrupts inline HTML on EN→AR→EN toggle cycle — `el.setAttribute('data-i18n-original', el.textContent)` captures plain text only, so any `<code>`, `<strong>`, or other inline elements inside the translatable span are LOST after the first toggle back to EN; security hooks block the obvious fix (innerHTML); the safe pattern is to break HTML elements OUT of the i18n span** — On 2026-05-04 while wrapping setup notes that contained `<code>D:\Project Management\...</code>` paths, the obvious fix was to upgrade `applyLang` to use `innerHTML` for both stash and restore. Security hook intercepted with "Setting innerHTML with untrusted content can lead to XSS". Even though T is hardcoded by the developer (not user input), the hook is a hard guard. The clean alternative: restructure HTML so `<code>` / `<strong>` / `<a>` live OUTSIDE the data-i18n span. **Why:** `el.textContent` is a getter that flattens DOM to plain text. Inline elements become invisible after the first toggle. AR string with `<code>...</code>` literal renders the tags as visible text (readers see `<code>D:\\...</code>` not styled code). Going back to EN restores ONLY the captured plain-text — original `<code>` styling lost. This is silent corruption. Security hook blocks the innerHTML solution because the hook can't distinguish "developer-hardcoded T" from "user input concatenated into T". Restructuring is the only path. **How to apply:** (1) When wrapping a paragraph that contains inline elements: split the text into 3+ adjacent spans, putting inline elements (`<code>`, `<strong>`, `<a>`) BETWEEN spans not INSIDE: `<span data-i18n="setup-1">Email tab needs the shared</span> <code>Run-Update-Server.ps1</code> <span data-i18n="setup-2">running on port 5050...</span> <code>D:\\...</code> <span data-i18n="setup-3">once.</span>` (2) Each span's textContent is pure plain text; `applyLang` swaps cleanly; the `<code>` outside survives EN↔AR toggle. (3) Naming convention: `<key>-1`, `<key>-2`, `<key>-3` for sequential pieces of one logical paragraph. Add to T as separate entries. (4) Cost: 3 keys per paragraph instead of 1, plus careful AR translations that grammatically make sense when concatenated with the inline-HTML breakpoints. (5) Trade-off accepted: AR translation is slightly less natural (sentences split at HTML boundaries) but the toggle is stable. (6) Reference: May 4 — `email-tab-setup-1`/`-2`/`-3`, `meetings-tab-setup-1`/`-2`/`-3`, `board-pack-setup-1`/`-2`, `briefing-tab-setup-1`/`-2`. Each replaces a single `data-i18n="x-tab-setup"` that had `<code>` tags inside. Pattern applied across 4 setup-notes paragraphs.

626. **Auto-draft entries written to localStorage BEFORE bilingual schema extension carry empty `actionAr/reasonAr` fields forever — there is no automatic backfill; render path must fall back to EN gracefully + accept "legacy entries stay English in AR mode" as a permanent migration debt** — On 2026-05-04 during v14 build, extended `buildDraft()` + 5 auto-draft scan loops (Data Gate / SPI / CPI / Velocity / SBI / Email-Reply / Email-Mention / Agenda / MOM-Missing) to populate parallel `actionAr` and `reasonAr` strings via T-dictionary template substitution. New entries from May 4 onward have full bilingual coverage. But the user's existing `localStorage['pmo-action-log-v1']` already contained 44+ auto-draft entries from v13 builds — those have `action: '[DRAFT — ...]'` populated and `actionAr: undefined`. In AR mode, the render line `(isAr && e.actionAr) ? e.actionAr : (e.action||'')` correctly falls back to English for legacy entries. Visible result: legacy entries display English action/reason text in AR mode, new entries display Arabic. **Why:** localStorage is a write-once snapshot per session. Schema additions don't auto-trigger a re-draft of historical entries. Templates that populate `actionAr` only fire when the auto-draft scans run AND the entry's de-dup key isn't in `blockingTuples`. Existing entries remain blocked from re-creation indefinitely. The graceful-fallback pattern (`e.actionAr || e.action`) is the standard solution: avoid rendering empty strings, accept temporary visual inconsistency until natural turnover (closures, migrations) ages out legacy data. **How to apply:** (1) When extending an entry schema with bilingual parallel fields, ALWAYS implement render-side OR-fallback: `(isAr && e.fieldAr) ? e.fieldAr : (e.field || '')`. (2) Don't write a "migration backfill" pass — it would have to re-run the entire auto-draft logic against historical state, which the snapshot data may no longer contain. (3) Document the migration debt: explicitly note in commit/memory that "legacy entries stay English; new entries get AR; turnover ages this out within ~30-60 days". (4) Optionally: write a one-shot console helper `window.backfillActionLogAr()` that loops `pmo-action-log-v1`, runs each entry's `indicator` through the corresponding template, and patches in `actionAr/reasonAr` if missing — but only if the data needed for substitution (project name, value, deviation) is still on the entry. Most cases it is (those fields are persisted), so backfill is doable but tedious. (5) Reference: May 4 — v14 Action Log render line ~10473 implements the fallback. Memory file `pmo_v14_arabic_completion.md` documents migration debt under "Out of scope". User accepted "legacy entries stay English" as known debt.

627. **The `option` element's `data-i18n` works for plain-text option labels but breaks if the option contains inline `<span>` children — `<option value="x"><span data-i18n="key">Label</span></option>` does NOT translate because option text is rendered atomically by the browser; only `<option data-i18n="key">Label</option>` (attribute on option itself) flips correctly via `el.textContent = T[key]`** — On 2026-05-04 wrapping Action Log filter dropdowns, initial fix tried `<option value="project">📁 <span data-i18n="al-scope-project">Project</span></option>` to keep the emoji + add translation. The span never rendered (option rendering is text-only at the OS level), and applyLang's `[data-i18n]` selector found the span but textContent swap on a child of a `<select>` option had no visible effect. **Why:** HTML `<option>` is rendered by the browser's native widget system using only the textContent of the option, not its child DOM. Spans inside options are stripped to text. Applying data-i18n to a child of `<option>` wraps a phantom: applyLang finds the span and updates it, but the dropdown widget continues showing the original option.textContent (cached at form-render time on most browsers). Only the option's OWN `data-i18n` attribute works, and only by setting `option.textContent = T[key]` on the option element directly. **How to apply:** (1) Put `data-i18n` directly on `<option>` element, not on a child `<span>`. (2) Sacrifice inline emoji prefixes — either bake the emoji INTO the AR translation string (`'al-scope-project': '📁 مشروع'`), OR drop the emoji from the option text entirely. (3) Working pattern: `<option value="project" data-i18n="al-scope-project">📁 Project</option>` with T entry `'al-scope-project': '📁 مشروع'`. (4) Verify in browser: open the dropdown, toggle EN↔AR, confirm the option text actually changes (don't trust DevTools-element-inspector, which shows the underlying span value not the rendered option text). (5) Reference: May 4 — Action Log filters at line ~2202-2205 use this pattern; T entries `al-scope-project`/`al-scope-team`/`al-scope-assignee`/`al-scope-email` include the emoji directly so the option flips visually as a single unit.

628. **Tracking unresolved-keys WITHOUT keeping a stable AR-mode reference returns false-positive "440 missing" results because `dgT(key)` returns the English key when `lang === 'en'` — the audit script must explicitly assert `applyLang('ar')` before scanning, OR check both directions (`document.documentElement.getAttribute('lang') === 'ar' && dgT(k) === k`)** — On 2026-05-04 first audit run after Round 2 patches reported 440 unresolved keys; second run after Round 3 reported 0. Nothing changed in the dictionary between runs. The explanation: the toggle had flipped the page back to EN between Round 2 and Round 3 (because of `localStorage` persistence + a manual `window.toggleLang()` call). When `lang === 'en'`, `dgT(key)` returns the input key as-is (it's the EN fallback), so EVERY data-i18n attribute appears "unresolved" if the script equality-checks `dgT(k) === k`. The audit was telling the truth about what dgT does in EN mode, but interpreting that as "missing translation" was false. **Why:** `dgT` is a contextual translator — it inspects the current `lang` attribute. The unresolved-detection pattern only makes sense when `lang === 'ar'`. Forgetting to assert AR mode produces a confidently-wrong report. **How to apply:** (1) Audit script MUST assert AR mode at the top: `if (document.documentElement.getAttribute('lang') !== 'ar') window.toggleLang();` before any `dgT(k) === k` check. (2) Better: write the audit to be lang-independent — directly inspect `window.T` if exposed, otherwise wrap `dgT` calls inside a manual `applyLang('ar')` → check → `applyLang('en')` if needed pattern. (3) Add a sanity print at the top of the report: `lang: 'ar', dir: 'rtl'` — confirms the audit ran in the right state. (4) When the report shows suspiciously many missing keys (>50% of total), treat as "audit ran in wrong mode" first hypothesis, dictionary gap second hypothesis. (5) Reference: May 4 — initial run reported 440 missing → re-asserted AR mode → real count was 6 (5 nav-section keys + 1 typo). Pattern formalized into the standard audit harness in lesson #624.

629. **Memory files describing "Day 1 scaffold" or "current phase" go stale within hours when delivery is moving fast — verify file system reality with Read/Glob BEFORE recommending from memory, especially when memory says "skeleton with 1 tab" but the actual `App.tsx` has 19 tab imports** — On 2026-05-04 user asked "v14 vs PMO_OS Data Gate parity?" My memory file (`pmo_v2_electron_parallel_build_may3.md` written ~16 hours earlier) said PMO_OS was a Day-1 skeleton with only PortfolioStatus tab. I almost answered "PMO_OS has no Data Gate yet" based on memory alone. Catching myself, I read `D:\PMO_OS\src\App.tsx` and found 19 tab imports including `<DataGate />`, plus `DataGate.tsx` (499 lines) + `DataGateProjectDetail.tsx` (230 lines) + `lib/devops-fetch.ts` all live. The Phase A→B→C→D→E phased roadmap had been overtaken by vertical-slice delivery in <24 hours. **Why:** Memory entries are point-in-time snapshots. When a project is in compressed-delivery mode (e.g., the 16-week roadmap collapsing to "ship every tab vertically in days"), the gap between memory write-time and read-time is enough to invalidate confident claims about file presence, line counts, tab inventories. Recommending against the memory snapshot leads to false-negative answers ("X doesn't exist yet") that confuse the user and damage trust. **How to apply:** (1) For ANY question about "does X exist in Y?" or "what's the current state of Z?", treat memory as a hint not as truth — verify with Read on the actual file before answering. (2) After verifying, immediately update the stale memory file with the new reality (don't leave it for "later"). (3) Add an explicit "Reality check (DATE)" section to memory files that describe phase/status snapshots so future sessions see the override clearly. (4) When memory contradicts file reality, trust the file and update the memory in the same turn. (5) Standing standard: any memory line beginning with "Phase X is currently…" or "Day N…" or "scaffold has 1 tab…" should trigger a verification pass before quotes. Reference: May 4 — PMO_OS memory said "Day 1 scaffold + 1 tab"; reality was 19 tabs wired with tRPC + SQLite + DevOps fetch.

630. **Adopting `useTranslation` in a React component that has `array.map((t) => …)` patterns silently breaks the component because the loop variable `t` shadows the i18n `t` — TypeScript catches the type mismatch ONLY when a `t.someStringField` happens to also be a valid translation key path, otherwise it compiles silently and renders junk** — On 2026-05-04 porting `DataGateProjectDetail.tsx` to bilingual EN/AR via `const { t } = useTranslation()`. Existing code had `filtered.map((t) => { … <td>{t.id}</td> <td title={t.title}> … })`. After adding `const { t } = useTranslation()` at the component top, the inner `t` variables shadow the i18n `t`. The first usage I added — `title={t('dataGate.detail.openInDevOps')}` — was inside the same `.map` callback, so it called `t('…')` on the TaskDetail object, which is `undefined`. **Why:** JavaScript scoping has the loop variable shadowing the outer const. TypeScript catches the call-as-function-on-non-function type error, BUT only at compile time and ONLY for direct calls like `t('…')`. References like `<td>{t.id}</td>` still work (they refer to the loop var). The dangerous case is when half the references resolve to the loop var (correctly) and half resolve via shadowing chaos — the file looks correct on read-through but renders broken at runtime. **How to apply:** (1) BEFORE adding `useTranslation`, grep the target file for `\\((t)\\b` and `\\b\\bt\\.` patterns to find all loop-var conflicts. (2) Rename loop variables to non-`t` names (`task`, `tk`, `item`, etc.) BEFORE adding the `t` import. Do this as a separate edit to make it review-able. (3) Re-grep after the `useTranslation` call is added to confirm zero `t.id`-style references remain that should have been the i18n function. (4) `npx tsc --noEmit` catches the most obvious failures (`t is not callable`) but won't catch every misuse — run a Vite build AND visually inspect the rendered tab if possible. (5) Reference: May 4 — caught after fixing line 207's `#{task.id}` (renamed) but missing `t.type`/`t.state`/`t.title`/`t.assignee`/`t.originalEstimate`/`t.completedWork` on lines 210-228; secondary occurrence in `tasks.filter((t) => t.state === s)` on line 146. Always do the rename pass first.

631. **Per-cell click-through with preset filter + in-modal switchable chip is a richer interaction than row-onClick — every numeric value becomes its own "drill into THIS subset" hyperlink, and the preset filter is just a pre-selected chip that the user can change without closing the modal** — On 2026-05-04 user asked: "when I click Project show all tasks; click Open show only open; click Miss Est show missing-estimate only; click Miss Act show missing-actual only; all values should be hyperlinks". Originally the row had a single `<tr onClick>` opening the modal with no preset filter. Refactor: replaced row-onClick with per-cell `<button>` links — Project → `gap=all`, Open → `gap=open`, Miss Est → `gap=missing-est`, Miss Act → `gap=missing-act`, Miss Either → `gap=missing-either`. The drill-down modal accepts `initialGapFilter` prop, applies it via `passesGapFilter()`, and renders a 5-chip filter row above the existing state-filter row. Chips show live counts. The preset is just the chip that's selected on first render; the user clicks any other chip to switch without reopening. **Why this design wins:** (1) Each cell answers a different question — clicking "5" on Miss Est means "show me those 5"; clicking "12" on Open means "show me those 12 open tasks"; row-click is a less precise gesture. (2) Visual hyperlink styling (`text-accent hover:underline`) telegraphs that the value is actionable. (3) Greying out zero-count cells (`disabled` button + `text-fg-muted/40`) prevents pointless modal opens. (4) In-modal switchable chips let the user reframe the same data without modal-flicker. **How to apply:** (1) Convert row-onClick patterns to per-cell buttons whenever each cell carries semantic information that maps to a filterable subset. (2) Style cell buttons as hyperlinks (`text-accent hover:underline cursor-pointer`) — users recognize the color affordance. (3) Disable cells whose value is 0 or N/A — saves wasted clicks. (4) The modal's filter UI should display the current preset as a selected chip, not as static text — communicates "this is changeable". (5) Show live counts per chip so users see filter outcomes before clicking. (6) Reference: May 4 — `DataGate.tsx` row → 5 buttons + `DataGateProjectDetail.tsx` 5-chip gap-filter row above existing state-filter row; ~50 lines of TSX changes total.

632. **Electron `setWindowOpenHandler` + plain `<a target="_blank">` is the simplest path to "open external URL in user's default browser" — no need to expose `shell.openExternal` through the preload bridge** — On 2026-05-04 adding DevOps deep-links to Data Gate drill-down rows. First attempt: tried `window.api.shell.openExternal(url)` via preload — but the preload didn't expose `shell` (correctly so, per least-privilege CSP-locked preload pattern). Adding it would mean updating preload + type declaration + invoking-via-IPC plumbing. Better approach: since `electron/main.ts:124-127` already has `mainWindow.webContents.setWindowOpenHandler(({ url }) => { shell.openExternal(url); return { action: 'deny' }; })`, plain HTML `<a href="…" target="_blank" rel="noopener noreferrer">` automatically routes through that handler — no JS, no preload changes, no IPC. The renderer's `window.open()` (which target=_blank uses internally) gets intercepted, denied for in-Electron rendering, and the URL is forwarded to the OS's default browser. **Why:** `setWindowOpenHandler` is Electron's intended hook for this exact use case. Adding `shell.openExternal` to preload is unnecessary plumbing when the framework already has a route. **How to apply:** (1) When you need "open external URL from renderer", check `electron/main.ts` for an existing `setWindowOpenHandler` first — most secure-Electron scaffolds have one. (2) If yes, just use `<a href target="_blank" rel="noopener noreferrer">` — no JS, no preload exposure, no IPC. (3) If no, add the handler in main.ts (4 lines) before adding shell.openExternal to preload. The handler approach is inherently safer because the URL passes through main-process inspection. (4) Always add `rel="noopener noreferrer"` for security (window.opener exfiltration prevention). (5) Reference: May 4 — `DataGateProjectDetail.tsx` line ~199-208 — single `<a>` element handles deep-links to `https://dev.azure.com/TaqaTechno/{project}/_workitems/edit/{id}` with zero added preload surface.

633. **Cross-artifact parity audits MUST grep imports + call sites BEFORE flagging "missing" features — the v14↔PMO_OS Data Gate audit flagged "auto-draft 5-indicator governance hook missing in PMO_OS" but a 1-line grep of `ActionLog.tsx` revealed it was already wired at line 189 (`trpc.dataGate.latestByProject.useQuery()`)** — On 2026-05-04 building the v14↔PMO_OS Data Gate parity comparison, my initial 4-item gap list claimed "Auto-draft 5-indicator hook not yet ported to PMO_OS". User accepted the audit and asked to "fix all 4". Before editing, ran `grep auto.draft "D:\PMO_OS\src\components\ActionLog.tsx"` and found the hook fully implemented — the tagline `"Day 14: Auto-draft full 5-indicator pipeline (Data Gate · SPI · CPI · SBI · Velocity)"` plus a `trpc.dataGate.latestByProject` query. Item #4 was a false positive and got marked "no fix needed". If I'd implemented the "fix" without verifying, I'd have created a duplicate auto-draft path with conflicting state. **Why:** Parity audits comparing two artifacts (especially when one is a monolithic HTML and the other is split across many TS files) suffer from name-asymmetry — v14's `saveDataGateInsight()` lives in a single IIFE in `PMO Dashboard_v14.html`, while PMO_OS's equivalent is `auto-draft.ts` + `ActionLog.tsx` + `lib/insights-helpers.ts`. A surface-level "I don't see it in DataGate.tsx" reading misses cross-component wiring. **How to apply:** (1) Before flagging ANY feature as "missing in artifact B", grep the entire codebase for the function/concept name, not just the obvious component. (2) Look at imports at the top of related files — Action Log imports from `lib/auto-draft.ts` proves auto-draft exists somewhere. (3) Use `grep -r` with multiple synonyms (`autoDraft`, `auto-draft`, `auto_draft`, `dataGate.latestByProject`, etc.) — naming conventions vary. (4) State each gap claim with its evidence: "auto-draft missing — searched for X, Y, Z, found nothing" not just "auto-draft missing". (5) When the user asks to fix a list of gaps, re-verify each gap immediately before editing — confirming saves you from creating duplicate paths. (6) Reference: May 4 — initial audit listed 4 items; verification reduced to 3 actual fixes (% overlay + bilingual port + deep-links); the 4th was a false positive that would have cost ~30 minutes of duplicate wiring.

634. **When porting bilingual support to a TSX component file, ADD the new i18n keys to BOTH locale files in the SAME edit pair — never push to git or run dev with only EN populated, because i18next falls back to the key STRING when AR is missing, which causes the UI to display literal strings like `"dataGate.detail.colId"` in AR mode and looks like the worst kind of bug** — On 2026-05-04 adding `dataGate` namespace + 50 keys to en.json + ar.json. Standard pattern: write en.json keys → write parallel ar.json keys → wire `t('dataGate.title')` calls in TSX. If you stop after en.json (e.g., "I'll do AR after lunch"), `t('dataGate.title')` still resolves in EN mode (correctly) but in AR mode i18next fails the lookup and renders the key path itself — `dataGate.title` literal string in the UI. Looks broken to anyone testing the bilingual toggle. **Why:** i18next's missingKeyHandler defaults to "return key" — by design, so devs see a visible signal in development. But in production-track code the visible literal looks like a rendered template error. **How to apply:** (1) Edit en.json + ar.json in the same edit pair (use the Edit tool sequentially for both files before moving to TSX). (2) When adding keys, draft the AR translation alongside the EN string, not after — even rough draft is better than missing key (the key falls back to EN-equivalent rendering vs. the broken literal display). (3) For RTL languages, add a final pass that toggles to AR in dev mode and visually inspects the tab; literal `dataGate.detail.colId`-style strings instantly stand out. (4) Add a CI lint or dev-time console warning for missing AR keys — `i18next.on('missingKey', (lngs, ns, key) => console.warn(...))` is a 3-line install in i18n config. (5) Reference: May 4 — DataGate i18n port: 50 keys × 2 files (en.json + ar.json) added in same session; first build went straight to AR-mode visual check, no literal-key bugs.

635. **The 7-gap parity matrix (Identical / Improvement / Regression columns) is the right shape for a v14 HTML → v2 Electron migration audit because it makes the "regression" cell the only blocking item, while improvements get celebrated and identical rows get a one-line ✓** — On 2026-05-04 producing the v14↔PMO_OS SPI/CPI tab parity report, the audit produced a 7-row table: Storage, Bilingual EN/AR, Drill-down, Live fetch UX, Sorting, Alaqraboon split, Governance hooks. 4 rows were "PMO_OS already better" (storage = SQLite history vs localStorage, drill-down has variance modal v14 lacks, fetch UX has progress bar, Alaqraboon split via area path). 1 row was a true regression (Bilingual: v14 had data-i18n + AR translations, PMO_OS shipped hardcoded English). 2 rows were cosmetic differences (sort order, sample-size criterion). The regression was the only item that BLOCKED the May 18 dual-track demo where users would toggle to Arabic. By making the regression visually distinct (red badge in the matrix), the "what to fix first" decision became automatic. **Why:** Migration audits typically dump everything as a flat "diff list" which buries the regression in a sea of nice-to-haves. The 3-column structure (Identical / Improvement / Regression) forces classification; the act of classifying surfaces the blocking item. Improvements double as marketing for the migration ("v2 also gains X, Y, Z") which buys goodwill from stakeholders questioning the migration cost. **How to apply:** (1) For any artifact-vs-artifact migration audit, structure findings as a 3-column matrix: green ✓ for parity, blue ↑ for v2-improvements, red ⚠ for regressions. (2) Block the migration on regressions only — improvements and cosmetic differences are NOT blockers. (3) Each cell has line numbers + evidence (e.g., `v14: line 5558 spi formula identical to v2 lib/devops-fetch.ts:325`). (4) Save the audit as a permanent .md plan file before editing — it becomes the prioritization doc when the user says "fix all gaps". (5) Reference: May 4 — `C:\Users\DELL\.claude\plans\did-we-have-a-glowing-nygaard.md` produced this matrix for SPI/CPI tab; 1 regression (i18n) closed first, then 2 cosmetic gaps + 1 improvement (sparkline) added beyond v14.

636. **A 64×16 inline SVG polyline is enough trend visualization for at-a-glance review — no chart library, no dependency, ~40 LOC including y-axis clamp + threshold reference line + zone-colored end-cap** — On 2026-05-04 closing the storage-history gap (PMO_OS SQLite keeps multi-snapshot history per project, v14 localStorage couldn't), added a `<SpiSparkline values={number[]} />` component inline in `SpiCpi.tsx`. Renders the project's last N SPI values as an SVG polyline with: (a) y-axis clamped [0, 1.2] for stable cross-row comparison, (b) horizontal dashed line at y=0.80 marking the green threshold, (c) end-cap circle colored by latest-value zone (green ≥0.80 / amber ≥0.50 / red <0.50), (d) graceful degradation: 0 values → em-dash, 1 value → centered dot, 2+ values → full polyline. **Why:** Tiny per-row sparklines need the smallest possible footprint — chart libraries (Recharts, Visx, even d3-shape) add 50KB+ JS and force ResponsiveContainer/measurement plumbing that doesn't make sense for 64×16 cells. Inline SVG with a y-scaling helper is faster, smaller, and easier to style with the existing Tailwind palette. (`stroke="#22c55e"` matches `bg-zone-green`.) **How to apply:** (1) For per-row trend cells ≤ 80px wide, write the SVG by hand — `polyline points="x,y x,y …"` is dead simple. (2) Always include a threshold reference line (`<line strokeDasharray="2 2">`) so the user reads the sparkline against a fixed benchmark, not just relative to itself. (3) End-cap dot colored by zone makes the latest reading immediately readable without inspecting numbers. (4) y-axis must be clamped to a fixed range across all rows — auto-scaling per row makes rows incomparable. For SPI use [0, 1.2]; for CPI use [0, 1.5]; for percentages use [0, 100]. (5) Bake fall-back rendering for 0/1 data points (em-dash / centered dot) — most projects in fresh DBs have only 1 snapshot. (6) Reference: May 4 — `D:\PMO_OS\src\components\SpiCpi.tsx:65-98` `SpiSparkline` component; 0 added deps; pulls history via `trpc.execSnapshots.list({limit:500}).useQuery()` indexed by project on the client.

637. **Adding a sort-key field (`taskCount`) to a SQLite/Drizzle table for v14 parity is a 5-touch phase-additive migration: schema → drizzle-kit generate → fetcher return → zod input → consumer sort callback. Default 0 NOT NULL keeps pre-migration rows usable without backfill** — On 2026-05-04 closing parity gap #5 (v14 sorts SPI/CPI table by task count desc; PMO_OS sorted by planned-hours desc). Touched 5 files in one commit: (a) `src/main/db/schema.ts` — added `taskCount: integer('task_count').notNull().default(0)`; (b) `npx drizzle-kit generate --name task_count` produced `0003_task_count.sql` with a single `ALTER TABLE … ADD COLUMN`; (c) `src/lib/devops-fetch.ts` — added `taskCount: number` to `ProjectSpiCpiSnapshot` interface + return `items.length` from `fetchSpiCpiForProject`; (d) `src/main/trpc/router.ts` — added `taskCount: z.number().int().nonnegative().optional().default(0)` to `execSnapshotInsertInput`; (e) `src/components/SpiCpi.tsx` — sort callback `(b.taskCount ?? 0) - (a.taskCount ?? 0)` with `b.planned - a.planned` tiebreaker for pre-migration rows that have taskCount=0. **Why:** Phase-additive (lesson #763) means existing rows must continue to deserialize and render. `NOT NULL DEFAULT 0` lets SQLite back-populate the column atomically during ALTER. The optional+default zod modifier prevents older insert callers from breaking. The tiebreaker on planned-hours keeps pre-migration rows ordered sensibly until they get re-fetched and gain real taskCount values. **How to apply:** (1) For any new sort-key field, the 5 touches are: schema → drizzle generate → DTO interface → zod input → consumer sort. (2) Always `NOT NULL DEFAULT <sane>` for additive columns so old rows survive. (3) Always make the zod input field optional with a default — protects older callers. (4) Always include a tiebreaker on a pre-existing field for rows where the new field is at default. (5) Test: run the existing test suite once before edit + once after; both must pass at 0 errors with no migration-related test additions needed (additive change). (6) Reference: May 4 — exec_snapshots gained `task_count` column via migration 0003; SpiCpi.tsx sort `(a, b) => (b.taskCount ?? 0) - (a.taskCount ?? 0) || (b.planned - a.planned)` matches v14's behavior for new rows + degrades gracefully for legacy.

638. **Same-tab governance affordance pattern: indicator tabs (SpiCpi, DataGate, etc.) get an "⚡ Auto-draft red rows" header button + per-row "covered/uncovered" pill that mirror the Action Log state — reuse `buildXDrafts` + `buildBlockingTuples` rather than duplicating the pipeline** — On 2026-05-04 closing parity gap #7 (governance hooks: in v14 the Action Log auto-draft + SLA + L1/L2/L3 escalation logic lived in OTHER tabs, not the SPI/CPI tab; same gap existed in PMO_OS). Closed it with two visible affordances: (a) header button "⚡ Auto-draft red rows" calls `buildSpiDrafts(rows, blockingTuples) + buildCpiDrafts(rows, blockingTuples)` then `await createActionMutation.mutateAsync(d)` for each; (b) per-row "Governance" column shows one of three pill states: 🟡 covered (`SPI L1 · 3d open`), ⚠ uncovered (red row, no entry yet), or em-dash (green row, no action needed). The pills index `openActions` by `(indicator|project)` key for O(1) lookup. **Why:** Forcing the user to context-switch to the Action Log tab to draft an entry from a red metric breaks flow. The governance loop should be initiated where the metric is read. AND duplicating the auto-draft logic creates two pipelines that drift over time. The clean pattern is: indicator tab REUSES the existing `buildSpiDrafts/buildCpiDrafts/buildBlockingTuples` helpers (same ones the Action Log auto-draft button uses), with `buildBlockingTuples(openActions)` ensuring no double-drafting. The per-row pill makes the binding visible — user sees "this red SPI is already at L1, 3 days old" and doesn't redraft. **How to apply:** (1) Any indicator tab that surfaces zone-classified rows MUST have a same-tab "auto-draft red rows" button that reuses the canonical draft builders. (2) Build a `Record<string, ActionLogState>` keyed by `${indicator}|${project}` (or equivalent dedup key) and render a per-row pill that shows the open entry's level + days. (3) Pill color: amber for "covered" (an open entry exists), red for "uncovered" (red row needs draft), muted for "no action" (green row). (4) The header button MUST report skipped count: `created N, skipped M (already covered)` — confirms the dedup actually worked. (5) NEVER write a parallel auto-draft pipeline — the 5-indicator pipeline lives in `auto-draft.ts` + Action Log; the indicator tabs are CALLERS, not implementers. (6) Reference: May 4 — `D:\PMO_OS\src\components\SpiCpi.tsx:114-189` `autoDraftRedRows()` + `openActionByKey` index + `Governance` column with `govPillCovered`/`govPillUncovered`/`govPillNone` i18n keys.

639. **Empty-state for filtered-bucket sections must be affirmatively visible (✓ green panel with "what populates this") not muted italic — users mistake muted-italic empty-state for "section is missing" and report bugs** — On 2026-05-04 user feedback after first SpiCpi tab demo: "Projects with Tasks but Data Gap is not appear". The section header WAS rendering with count=0, followed by italic muted text "No projects in this state." User read the muted italic as "the section is broken / not loading". Fix: changed empty-state styling from `bg-bg/40 border-border/50 italic text-fg-muted` (muted, easy-to-miss) to `bg-zone-green/5 border-zone-green/30 text-zone-green` with a leading ✓ checkmark and an explanation of what triggers population (e.g., "every active project has more than 5 closed actual hours"). Result: empty section now reads as "this section IS here AND the data is healthy" rather than "looks broken". **Why:** Visual hierarchy communicates loading/empty/error states. Muted italic reads as "placeholder, nothing to see" — same affordance as a disabled button. Affirmative green reads as "this is the working state, and it's good." Users who report "X not appearing" are usually saying "X is rendering but the styling told me it's not active." **How to apply:** (1) NEVER style an empty-bucket section with muted italic + low-opacity border. (2) Use ✓ green panel ("affirmatively healthy") for filter buckets that are intentionally empty — e.g., "no projects with gaps", "no failing tests", "no SLA breaches". (3) Always include a one-line explanation of what would populate the section, so the user understands the criteria. (4) Reserve the italic-muted style for genuinely irrelevant or "future content" zones, NOT for "filter matched zero rows". (5) Apply the same rule across all bucket-style sections (DataGate's clean projects group, Risk Register's no-active-risks state, etc.). (6) Reference: May 4 — `SpiCpi.tsx` Data Gap empty state: `<div className="bg-zone-green/5 border-zone-green/30 text-zone-green">✓ {t('groupGapEmpty')}</div>`.

640. **v14's strict `closedActual === 0` filter for "Data Gap" hides thin-sample projects whose CPI is already noise — broaden to `closedActual ≤ 5h` so small/new projects surface and don't pollute the Metrics group with unreliable indicators** — On 2026-05-04 user feedback flagged Data Gap section as "not populating" even though the section was rendering. Investigation: every active project in the dataset had ≥1h of closed actuals, so v14's strict filter (`closedActual === 0` = "CPI uncomputable") matched ZERO rows. But projects like AI Call Center (4h closed), IndoQat (3h closed), Taqat (1h closed), Taqat Facilities (1h closed) had CPI numbers based on so few hours that the indicator was meaningless — yet they were grouped with Wallet (1593h closed) and Pearl Pixels (1333h closed) in the Metrics table as if their CPIs were equally trustworthy. Lesson #225 already said "below 5h closed, CPI is noise." Fix: broadened filter to `r.planned > 0 && r.closedActual <= 5` (using ≤5 so the 5h threshold itself counts as thin-sample). 4 projects moved from Metrics → Data Gap; section now visibly populated. **Why:** v14's strict filter optimizes for "uncomputable" (literal /0) but ignores the broader category of "computed but statistically meaningless." A CPI of 1.0 derived from 1 closed task is not a healthy CPI; it's the only sample we have. Users reading the Metrics table can't distinguish "CPI 1.0 from 200 closed tasks" (real signal) from "CPI 1.0 from 1 closed task" (random outcome). Surfacing thin-samples in Data Gap forces the conversation. **How to apply:** (1) When porting filters from a legacy artifact, check whether the strict version captures the meaningful population. Sometimes the spec was right but the threshold needs rebasing (e.g., from "0" to "≤ N where N is the noise floor"). (2) Document the deviation from the source artifact in a code comment AND in the i18n strings so users see "PMO_OS broader than v14" not "PMO_OS broken vs v14". (3) When broadening, deduplicate downstream — projects in Data Gap MUST NOT also appear in Metrics. Build a `Set<string>` of data-gap project names and filter Metrics with `!dataGapKeys.has(r.project)`. (4) Pick the threshold from existing project lessons (here #225's "5h"), not arbitrary. (5) Reference: May 4 — `SpiCpi.tsx` `DATA_GAP_THIN_SAMPLE_THRESHOLD = 5` constant + dedup via `dataGapKeys` Set; i18n hint updated to "planned hours but ≤5h closed actuals — CPI not computable or sample too thin to trust."

641. **Page placement of "How to read this tab" expandable info panel: BOTTOM-anchored beats TOP-anchored — at the bottom it answers "wait, what does this column mean?" the moment the user encounters something unfamiliar; at the top it interrupts the read-the-data flow with a wall of explanation that gets dismissed and never re-opened** — On 2026-05-04 after first SpiCpi tab demo, user explicitly asked: "move the How to read this tab in below in the page". Initial placement was TOP (right under the header), reasoning was "explain before showing". User's reasoning for BOTTOM: by the time the user has scrolled through the metrics, sparkline, governance pills, group sections, they've already encountered every column and concept — the panel becomes a "click here if anything was unclear" reference, not a tutorial gate. TOP placement creates noise on the read-the-data path; BOTTOM placement makes the panel discoverable when needed without forcing a context interruption. **Why:** Information panels positioned BEFORE the data force every user (including expert users who already know the conventions) to skim/dismiss the panel before reaching the data. Information panels positioned AFTER the data are encountered only by users who scrolled to the bottom looking for help — i.e., users who need it. The opt-in flow is better than the opt-out flow. (`<details>` collapsed by default minimizes the visual weight either way, but bottom placement is still cleaner.) **How to apply:** (1) Default placement for "How to read this tab" / "Glossary" / "About these metrics" panels: BOTTOM of the section, after all data renders. (2) Exception: TOP placement is correct ONLY when the data itself cannot be read without the explanation (e.g., abbreviations that have no obvious meaning). For tabs with clear column headers + zone colors, BOTTOM is right. (3) Use `<details>` (collapsed by default) regardless of placement — the panel should never be open at first paint. (4) The summary line should be `ⓘ How to read this tab` with an "click to expand"/"click to collapse" hint, so users know it's interactive. (5) Reference: May 4 — `SpiCpi.tsx` panel was relocated from line ~335 (under header) to line ~722 (just before `</section>`); user confirmed "this is the right place" without further iteration.

## v14 → PMO_OS Outlook Categories Governance Port (May 5, 2026)

642. **Porting localStorage-keyed governance to SQLite: keep the same idempotency key (here, `emailKey`) but elevate it from a JSON-property dedup check to a database-level filter — the rule looks identical at the call site, but execution moves from "scan-the-array" to "WHERE clause" and now scales beyond the 5MB localStorage cap** — On 2026-05-05 porting v14 PMO Dashboard's `autoDraftEmailEntries` + `autoCloseResolvedEmailDrafts` (both keyed by `emailKey`) into PMO_OS Electron. v14 used `pmo-action-log-v1` localStorage with a JS array iterated to find duplicates; PMO_OS uses Drizzle ORM + SQLite via tRPC. The naive port replaced the array with a SQL `select * from action_log` and then iterated in JS, which technically works but throws away the durability win of moving to SQLite. The right port: change `buildBlockingTuples` to accept the same dedup field (`emailKey`) on its lite-row type, and add a tRPC `closeResolvedEmail` procedure that does the closure check server-side via `WHERE scope='email' AND indicator=$ind`. The dedup logic at the call site looks unchanged (`tuples.has(key)`), but the data lives in a transactional store and the closure runs as a single SQL pass. **Why:** Porting a feature without re-thinking its storage layer leaves you with v13-shape code on top of v2 infrastructure — same bugs, same scaling cliff, plus an extra layer to debug. The localStorage version of `autoCloseResolvedEmailDrafts` had to load the full array, mutate in place, JSON.stringify, write back. The SQLite version closes via UPDATE `WHERE id=...` per resolved row — atomic, durable, bounded. Same key, different execution model. Same correctness guarantees, plus durability. **How to apply:** (1) When porting governance/dedup logic across storage backends, identify the **idempotency key** first and lift it INTO the schema (here: `emailKey TEXT` on `action_log`). (2) Keep the BUILDER function pure: it returns drafts based on a `Set<string>` of blocking tuples. The CALLER is responsible for sourcing those tuples from the new backend. (3) Add a closure procedure server-side instead of replicating the v13 client-side mutation loop — `closeResolvedEmail({indicator, currentEmailKeys})` reads open rows once, computes the diff, issues UPDATEs. (4) Wire it into the existing sync flow: `Promise.all([...drafts.map(create), close('email-reply', keys), close('email-mention', keys)])`. (5) Update the i18n success string to surface `closed: N` so the user sees both halves of the loop fire (`Synced X reply + Y mention · auto-closed Z resolved`). (6) Reference: May 5 — PMO_OS `auto-draft.ts` + `router.ts` `closeResolvedEmail` procedure + `ActionLog.tsx` `syncEmailIndicators`; the port took ~3h end-to-end including 22 new Vitest tests with zero baseline regressions (203 → 248 tests).

643. **`emailKey` strategy is asymmetric per indicator and that asymmetry IS the rule — sender-based dedup for reply tracking (one row per pending sender), thread-based dedup for missed mentions (one row per thread). Don't unify them or you'll either spam or under-count** — On 2026-05-05 porting v14:13452 logic into PMO_OS `buildEmailReplyDrafts` and `buildEmailMentionDrafts`. v14 chooses the key with this branch: `(kind === 'mentions') ? normalizeThreadKey(subject) : (fromEmail || from).toLowerCase().trim()`. The asymmetry isn't an oversight — it reflects what the user is being asked to do. For email-reply: the user owes a sender N replies; one outstanding row per sender (collapsing multiple emails from the same person into one entry) keeps the Action Log uncluttered. For email-mention: the user owes the thread a response; one row per thread (so Re:/Fw: collapse together) is right. If you flip them — sender-key on mentions or thread-key on replies — you either get one row for every email-thread mention from a single person (spam) or you collapse multiple replies-needed senders into one row when they happen to share a Re: prefix (under-count). **Why:** The dedup key reflects the unit-of-action. Reply-owing is per-sender (you reply to a person about anything they sent); mention-owing is per-thread (you respond to one specific conversation). Different units = different keys. (`normalizeThreadKey` strips Re:/Fw:/Fwd:/Res: + Arabic رد:/إعادة توجيه: prefixes so threads collapse correctly even across mixed-language replies.) **How to apply:** (1) When implementing dedup for any "obligation tracker", identify the unit-of-action first. (2) Mirror v14's helper structure: `mapEmailCategoryToProject(category) -> string|null` (first non-empty token from `;`/`,`-split list) + `normalizeThreadKey(subject) -> string` (lowercase, strip prefixes). Both pure functions, both easy to unit-test. (3) When category is missing, surface as alarm (`'⚠️ UNCATEGORIZED — assign category in Outlook'` + `emailUncategorized=true` flag) instead of inventing a fallback project name — this enforces user-side discipline and avoids polluting the project register with auto-coined names. (4) Test both helpers exhaustively: empty, null, whitespace, semicolon-delimited, comma-delimited, stacked English prefixes, Arabic prefixes, mixed EN+AR. (5) Reference: May 5 — PMO_OS `auto-draft.ts:467+517` builders + `auto-draft.test.ts:262+` tests; lesson #596 (May 1) is the parent — "trust user-curated source-of-truth labels over canonical lookup tables".

644. **SLA threshold drift between v13 prose, v14 implementation, and PMO_OS port: pick ONE threshold, anchor it to the source-of-truth artifact, and document the cascade — if v14 says `hoursOld > 96` and your port still says `>= 120`, you're 24 hours late on every red signal** — On 2026-05-05 implementing the email-reply zone classifier. PMO_OS had `function emailZone(h) { return h >= 120 ? 'red' : 'amber'; }` (5 workdays past SLA). v14 has `pastSla = hoursOld > 96` (3 workdays past SLA). The 24h drift would have meant the Action Log ringing the alarm a full day later than the source dashboard. Fixed `emailZone` to `h > 96` and updated tests at 96h/97h boundary. **Why:** When two artifacts implement the "same" governance rule with different thresholds, the artifact in daily use IS the source of truth — even if the documentation said 120h. v14 has been live for weeks; the user's mental model is calibrated to v14's red-fires-at-96h timing. PMO_OS exists to inherit that exact calibration, not to re-derive it from first principles. **How to apply:** (1) When porting numeric thresholds, grep the SOURCE artifact (`hoursOld\s*[><=]+\s*\d+` etc.) for the actual constant in use; don't trust prose like "5 workdays". (2) Add the source-line reference in a code comment: `// L049 (v14 parity, ...) — v14:13459 uses hoursOld > 96`. (3) Test the boundary: 96 = amber, 97 = red — boundary tests catch off-by-one drift instantly. (4) When the user adjusts a threshold in the source artifact, search for `[constant]` in the port and update both — never let the port lag. (5) Reference: May 5 — PMO_OS `auto-draft.ts:489` zone threshold + boundary tests at hoursOld 96/97 and escalation L2 above 168h.

## Outlook Follow-up Flag end-to-end + sidecar hot-reload (May 5, 2026 — afternoon session)

645. **User-marked priority signals (Outlook Flag-for-Followup, P1 ticket badge, "starred" indicators) MUST override SLA-driven zone math — a flag is the user's explicit "act now" gesture and should NOT be diluted by the time-based curve, even when hoursOld is well below the SLA threshold** — On 2026-05-05 wiring `IsFlagged` from `Extract-Inbox.ps1` → PowerShell server → `buildEmailReplyDrafts` / `buildEmailMentionDrafts` (PMO_OS) AND v14 `autoDraftEmailEntries`. First impulse: just append a 🚩 badge to the action text but keep zone driven by `emailZone(hoursOld)`. Wrong. The flag means "I want to see this NOW, regardless of how old it is" — a user can flag a 2-hour-old email and expect it red. So the rule became: `flagged ? 'red' : emailZone(hoursOld)` for the reply builder, AND `flagged ? 'red' : 'amber'` for the mention builder (which has no SLA curve at all and was always amber). Plus `escalationLevel = flagged ? 2 : (hoursOld > 168 ? 2 : hoursOld > 96 ? 1 : 0)`. **Why:** SLA logic is a fallback for unmonitored streams — the system trying to surface what the user hasn't manually triaged. Once the user HAS triaged (flagged it), the SLA's job is done — the flag IS the alarm. Diluting the flag through the SLA curve means flagged emails get the same color as unflagged-but-old emails, which is the worst of both worlds. Hierarchy: explicit user input > automated curve, every time. **How to apply:** (1) Any zone classifier that takes both an automatic metric (hoursOld, ratio, count) and a user-marked flag should apply the flag FIRST and short-circuit: `if (flagged) return 'red';` before the curve runs. (2) Same rule for escalation level — flagged starts at L2 (the highest), not L0. (3) Surface the flag visually with TWO redundant signals: row background tint + a 🚩 badge with tooltip showing the user's flag-request text (Outlook's `FlagRequest` is a free-text field — don't drop it). (4) When grouping/deduping flagged items (e.g., one row per sender, collapsing multiple emails), OR-merge the flag: `mergedFlag = a.isFlagged || b.isFlagged` so a flag on ANY email survives the collapse. Otherwise the dedup throws away the user's explicit signal. (5) Reference: May 5 — `Extract-Inbox.ps1` (FlagStatus=2 OR IsMarkedAsTask → IsFlagged column), `Run-Update-Server.ps1` (OR-merge in sender-dedup pass), `auto-draft.ts` builders (forced red+L2), `OutlookCategories.tsx` (🚩 column + summary card), v14 `autoDraftEmailEntries` (mirror logic).

646. **Test "skip empty input" cases by normalizing the RAW field, not the display fallback — `normalizeThreadKey('(no subject)')` returns a non-empty string so an empty-subject mention sneaks through and creates a row with the placeholder as its dedup key** — On 2026-05-05 first test run after L049 surfaced this exact bug: `buildEmailMentionDrafts` had `const subject = it.subject?.trim() || '(no subject)'; const emailKey = normalizeThreadKey(subject);`. Test `skips items with empty subject (no thread key derivable)` expected length 0; got 1. Root cause: the display-fallback `(no subject)` was applied BEFORE the normalize-and-skip check, so `normalizeThreadKey('(no subject)')` returned `(no subject)` (non-empty), passed the `if (!emailKey) continue;` gate, and emitted a draft with `emailKey: '(no subject)'` — a single shared dedup bucket that would silently collapse every empty-subject mention into one row across all senders. v14 line 13452 normalizes the RAW subject and skips on empty. Fix: split the variables — `emailKey = normalizeThreadKey(it.subject || '')` for the gate check, and `subject = it.subject?.trim() || '(no subject)'` ONLY for display. **Why:** Display fallbacks and dedup keys serve opposite purposes. Display fallback wants "always show something readable" (so the row has a subject column). Dedup key wants "skip when there's no real signal" (so empty-subject items don't all collapse together). When you let the display fallback feed the dedup key, you're treating "no input" as a valid input — silently dangerous. **How to apply:** (1) Always derive dedup keys from the RAW field, before any user-facing fallback. (2) When you have a dedup-vs-display split, name them differently in code (`emailKey` vs `subject`) so they can't be accidentally swapped. (3) Add a test for the empty/null/whitespace input — it's the cheapest test to write and catches this class of bug at compile time. (4) Reference: May 5 — `auto-draft.ts:540` mention builder split into `emailKey = normalizeThreadKey(it.subject || '')` then `if (!emailKey) continue;` then `subject = it.subject?.trim() || '(no subject)'`.

647. **In-app "Restart Sidecar" button beats teaching the user to close-and-reopen the whole Electron app — sidecar lifecycle is already managed by the Electron parent, expose it via a tRPC mutation so the renderer can hot-reload the Python child without bouncing the host** — On 2026-05-05 after editing `sidecar/outlook.py` to emit `is_flagged`, the user asked "how do I restart the sidecar?" The first answer was a multi-step close+reopen procedure (Ctrl+C the dev:all terminal, re-run, etc.). User pushback: "do these steps now". The right move was: stop documenting the workaround, build the button. Added `sidecar.status` (returns {pid, healthy}) + `sidecar.restart` mutation (calls `killSidecar()` → SIGTERM → 2s → SIGKILL → `launchSidecar()` → polls /health with exponential backoff → returns {ok, status}) on the tRPC root, then a 🔄 Restart link on the Settings → Python Sidecar card. Total: ~25 lines of router code + ~15 lines of UI. After this, every `sidecar/*.py` edit is a 1-click hot-reload — no terminal bounce, no full Electron restart, no losing the renderer's tRPC query cache. **Why:** Documentation is a workaround when the host's lifecycle owns a child process the user wants to manage. The tools to control the child (kill + spawn) already exist in `electron/sidecar/launcher.ts` exports — the only thing missing is renderer access. tRPC bridges that gap with no extra IPC plumbing. Documentation says "I gave up; here's a procedure"; the button says "I solved it." When the user asks "how do I X" three times in a session and X is something the system already does internally, the answer is to expose it, not document it. **How to apply:** (1) For any iterative workflow (edit → test → edit → test) where the user has to manually kill+respawn a managed process, build a one-click control instead of teaching the procedure. (2) The tRPC pattern: `<feature>.status` (query) + `<feature>.restart` / `<feature>.runNow` (mutation) — same shape as `backupRouter`, `briefingRouter`, `autoFetchRouter`. (3) Place the button near where the symptom appears: Restart Sidecar lives next to the sidecar health probe on Settings. (4) UX details: show "Restarting…" while pending, then `✓ pid=<pid>` (green) on success or `✗ /health never went green` (red) on failure. Re-probe after success so the card auto-flips to 🟢 Healthy. (5) Reference: May 5 — `src/main/trpc/router.ts` `sidecarRouter` + `src/components/Settings.tsx` Python Sidecar card.

## PMO_OS Velocity / Outlook / Insights v14 Parity Patterns (May 5, 2026 — afternoon-evening session)

648. **When porting a curated lookup table from a working artifact, hardcode the SOURCE table verbatim in the port and use the local DB as a SECONDARY fallback — making the port DB-driven from day one creates a drift class where stale seeds silently produce wrong-team-bucketing on every render** — On 2026-05-05 porting v14 Team Velocity to PMO_OS. First implementation read `team_roster` SQLite table to map assignee→teamCode. Symptom: 7 of 17 contributors landed in ⚠ Unassigned because (a) the seed had only 19 of 30 v14 members, AND (b) `seedTeamRosterIfEmpty()` short-circuited because the DB was non-empty from a prior launch with the OLD seed, so the expanded 30-canonical+19-alias seed never wrote. Fix: created `src/lib/v14-team-mapping.ts` with `V14_ASSIGNEE_TO_TEAM = new Map(...)` mirroring v14:6231 verbatim (47 entries, normalized lowercase keys), and rewrote `teamCodeFor(name)` to a 3-tier lookup: hardcoded v14 map → DB roster → `'un'`. Same pattern applied to `V14_FORMER_MEMBERS` for the `(former)` decoration. **Why:** v14 has been hand-curated for months. The DB seed is downstream of v14, not upstream — when the user moves Hacene from Qatar→Backend in v14, that's the source of truth, and any port that doesn't mirror v14 is wrong by definition. DB-driven approaches sound flexible but in practice introduce a synchronization burden the user shouldn't carry. Hardcoded primary + DB fallback gives you "deterministic by default, extensible when needed" — new hires can land in the DB before v14 catches up, but the 30 known cases are bulletproof. **How to apply:** (1) When porting any concept from a working artifact (team mapping, team labels, project tier, "former members", category→project routing), find the curated table in the source HTML/JS, copy it byte-for-byte into a `src/lib/<artifact>-<concept>.ts` file as a const Map or Set. (2) Normalize keys at load time (`name.trim().toLowerCase().replace(/\s+/g, ' ')`) so DevOps display-name drift (case, extra spaces) resolves correctly. (3) Wrap the lookup in a function that consults hardcoded FIRST, DB SECOND, fallback THIRD: `mapped = HARDCODED.get(norm) ?? db.get(norm) ?? FALLBACK`. (4) Add a code comment with the source line number so future updates can sync: `// v14:6231 ASSIGNEE_TO_TEAM — verbatim mirror`. (5) Apply same pattern across SBI, Action Log, Insights — any tab that resolves names. (6) Reference: May 5 — `src/lib/v14-team-mapping.ts:V14_ASSIGNEE_TO_TEAM` + `V14_FORMER_MEMBERS` + `src/components/TeamVelocity.tsx:teamCodeFor`.

649. **Electron `dev:all` does NOT recompile `electron/main.ts` between launches — Vite hot-reloads only the renderer, the main process loads from `dist-electron/electron/main.js` which is produced by a SEPARATE `tsc -p electron/tsconfig.json` step that no `npm run dev` invokes** — On 2026-05-05 swapping `seedTeamRosterIfEmpty` → `seedTeamRoster` in `electron/main.ts`. Restart cycle showed the OLD log line `[main] team_roster already populated; skipping seed` — proving the OLD compiled bundle was still loading. `package.json` `main: "dist-electron/electron/main.js"`. The `dev:all` script is `concurrently npm:dev npm:dev:electron` — neither runs `tsc`. Fix: ran `npm run build:electron` (which executes `tsc -p electron/tsconfig.json`) then restarted `dev:all`. Permanent fix: prepend `build:electron` to `dev:all` so the chain becomes `npm run build:electron && concurrently …`. **Why:** Electron's two-process architecture (main + renderer) has two separate build paths. Renderer = Vite + TS = HMR works. Main = standalone tsc compile to dist-electron, no watch in dev mode. Vite plugins like `vite-plugin-electron` paper over this but PMO_OS doesn't use one — pure separation. Documentation usually omits this because most Electron tutorials use a Vite plugin; when you don't, the two halves drift silently. **How to apply:** (1) When editing `electron/**/*.ts`, run `npm run build:electron` immediately after, BEFORE testing. (2) When the user reports "I restarted but the change didn't take", ALWAYS check whether the file lives in `electron/` (needs build:electron) vs `src/` (just hot-reloads). (3) Add `build:electron` to the front of the `dev:all` chain so this trap is impossible: `"dev:all": "npm run build:electron && concurrently …"`. (4) When the project supports cleanup (`Remove-Item dist-electron`), inform the user this DELETES the compiled bundle — they must run `build:electron` before `dev:all` will launch at all. (5) Reference: May 5 — `package.json:9` main field + `:12` dev:electron script; `dist-electron/electron/main.js:369` post-fix `seedTeamRoster()` call.

650. **`Microsoft.VSTS.Common.ClosedDate` is NOT a required Azure DevOps field — when filtering "delivered tasks", fall back to `Microsoft.VSTS.Common.StateChangeDate` for tasks in Closed/Done/Resolved state, OR you'll silently reject the majority of completed work and show 6× lower velocity than the working dashboard** — On 2026-05-05 PMO_OS Team Velocity showed 288 closed tasks vs v14's 1652 — a 5.7× gap that page-size and silent-error fixes didn't close (only +1 task gained from `$top=500` + retry). Inspecting v14:5534–5537 revealed: `var closedDate = f['Microsoft.VSTS.Common.ClosedDate'] || null; if (!closedDate && isClosed) { closedDate = f['Microsoft.VSTS.Common.StateChangeDate'] || null; }`. Many closed tasks in the user's DevOps org never had `ClosedDate` stamped (it's not auto-populated when state moves to Done — it's only set if a workflow rule or manual action writes it). PMO_OS rejected those tasks via `if (!task.closedDate) continue;` — the fix was extending `VELOCITY_DETAIL_FIELDS` to include `Microsoft.VSTS.Common.StateChangeDate` and applying the same fallback in `fetchVelocityTasksForProject`. **Why:** Azure DevOps work-item fields fall into "always present" (System.Id, System.State) vs "optional" (the Microsoft.VSTS.Common.* family). `ClosedDate` is in the optional family. v14 has been live for months and the fallback was discovered organically; without it PMO_OS is technically "more strict" but in practice "wrong." **How to apply:** (1) When porting any DevOps WIQL+batch fetch, treat every `Microsoft.VSTS.*` field as optional and design fallbacks BEFORE shipping. (2) For closedDate specifically, the chain is: `ClosedDate || (state ∈ closedSet ? StateChangeDate : null)`. (3) Always add a code comment with the v14 source line: `// L034 (v14 parity, ...) — v14:5534 fallback`. (4) When task counts diverge between two dashboards on the same DevOps org, ALWAYS check field-fallback completeness BEFORE assuming page-size or scope issues — the 6× gap was 100% explained by this one field. (5) Reference: May 5 — `src/lib/devops-fetch.ts` `VELOCITY_DETAIL_FIELDS` + `fetchVelocityTasksForProject` closedDate resolution.

651. **Azure DevOps `_apis/projects` defaults to `$top=100` and that limit is silently applied server-side — explicitly request `$top=500&stateFilter=wellFormed` matching the source artifact, otherwise orgs with > 100 projects lose work to the page boundary in a way that's invisible from the response** — On 2026-05-05 diagnosing PMO_OS Team Velocity 6× count gap. v14:4321 explicitly URL-encodes both params: `&$top=500&stateFilter=wellFormed`. PMO_OS `listProjects()` was hitting `_apis/projects?api-version=7.1` with no overrides — Azure default applies, returns first 100 projects in some unspecified order, response gives no signal that there were more. Fix: pass `$top=500&stateFilter=wellFormed` exactly as v14 does. Belt-and-braces: keep the client-side `state === 'wellFormed'` filter even though the server now applies it, in case the API version changes. **Why:** Default page sizes in REST APIs are silent traps because the response shape doesn't tell you "this is page 1 of 3" unless you look for a continuation token. Azure DevOps follows this pattern — clients that ignore $top look like they're getting "all" projects when they're really getting the first N. The user's test bed has > 100 projects, so the truncation matters; smaller orgs would never notice. **How to apply:** (1) Any time you call a DevOps `_apis/<collection>` endpoint, explicitly pass `$top=<bound>` matching the largest realistic count for the org. 500 is a safe ceiling for projects/teams/iterations. (2) For paginated results that might exceed the bound, implement continuation-token loops; for collections that are bounded by org structure (projects, teams), one-shot $top is fine. (3) Mirror the source artifact's exact query string when porting — easy to grep for `_apis/projects` in the v14 HTML and copy the params. (4) Add a code comment naming the source line: `// L033 (v14 parity, ...) — v14:4321 verbatim`. (5) Reference: May 5 — `src/lib/devops-fetch.ts:listProjects` URL with explicit `$top=500&stateFilter=wellFormed`.

652. **Per-project fetch errors in a multi-project loop must surface as a UI banner, not a silent `console.warn` — when the snapshot is missing tasks, the user has no way to know whether the data is incomplete or just smaller than expected unless the failures are visible** — On 2026-05-05 PMO_OS `runFetchWithPat()` had `try { await fetchVelocityTasksForProject(...); } catch (err) { console.warn(...); }` — every project that timed out, 401'd, or 404'd was silently dropped from the snapshot. User saw a smaller-than-expected count but no signal pointing at the cause. Fix: (a) added retry-once-with-500ms-backoff so transient errors recover, (b) accumulated final failures into `fetchErrors: { project, error }[]` array stored on the snapshot, (c) rendered a yellow `⚠ N projects failed to fetch (click to inspect)` `<details>` element on the tab with each project + error message + "retry the full fetch" hint. **Why:** Silent error handling optimizes for "the loop completes" — the wrong optimization. The user's mental model is "I clicked Refresh, I see the result." If 5 of 18 projects errored silently and the result looks small, the user wonders whether the data is wrong or whether they're misreading the dashboard. Visible errors collapse that ambiguity into "yes data is incomplete and here's why." **How to apply:** (1) Any loop iterating over external resources should accumulate errors in a structured array, never just log them. (2) Surface the error count + per-resource detail in the UI with a `<details>` block (cheap to ignore when empty, scannable when expanded). (3) Always retry once with backoff before recording a failure — most network errors resolve. (4) The error message must be the actual `err.message`, not a generic "fetch failed" — users need the underlying cause (timeout vs 401 vs 404 vs CORS) to triage. (5) Reference: May 5 — `src/components/TeamVelocity.tsx:runFetchWithPat` `fetchWithRetry` + `fetchErrors[]` + the ⚠ details UI block.

653. **Markdown-stored insights + a runtime markdown→HTML renderer (react-markdown + remark-gfm) gets you v14-style rich card visuals without any schema migration — the storage layer (text body) and the render layer (component overrides) are independent concerns and should be evolved independently** — On 2026-05-05 the Insights tab in PMO_OS rendered every saved insight body as `<pre>{section.body}</pre>` — markdown headings showed as `### Header` literal text, tables showed as pipe-character lines, bold/italic disappeared. v14 stores insights as structured JSON with kind-specific render functions (rich HTML tables, colored pills). Switching PMO_OS to v14's structured-JSON storage would have meant: schema change, data migration, per-kind render functions matching v14 exactly — ~2 days of work breaking all existing insights. Alternative chosen: keep markdown storage, install `react-markdown@^10.1.0` + `remark-gfm@^4.0.1`, replace `<pre>` with `<ReactMarkdown remarkPlugins={[remarkGfm]} components={MD_COMPONENTS}>`. `MD_COMPONENTS` is a 14-element record mapping `h1-h4`, `p`, `strong`, `em`, `ul/ol/li`, `code`, `pre`, `table/thead/th/td`, `a`, `blockquote`, `hr` → Tailwind-styled equivalents matching v14's small uppercase headings + bordered tables + monospace numerics. ~30 lines of override code, zero migration, every existing insight instantly renders rich. **Why:** Storage and render are different concerns. Markdown is a great storage format (human-readable, diffable, future-proof, easy to write programmatically via helper functions); the only thing it lacks is a renderer. Adding the renderer at the leaf component is cheaper than restructuring storage AND keeps storage format stable for next year's needs. The `remark-gfm` plugin specifically unlocks GitHub-flavored markdown tables — without it, react-markdown silently passes pipe-tables through as plain text. **How to apply:** (1) When stored content is markdown but rendering shows raw markdown source, the fix is a renderer at the component, not a storage migration. (2) Install `react-markdown` + `remark-gfm` together — gfm is required for tables, autolinks, strikethrough; just react-markdown alone is too minimal. (3) Define a single `MD_COMPONENTS` const at module scope so every insight card renders consistently. (4) Map each markdown element to a Tailwind-styled equivalent matching the source artifact's visual language: small uppercase `<h3>`, bordered `<table>` with `<thead>` accent bg, monospace `<td>`, inline code chips, etc. (5) Test by saving an insight that includes a pipe-table — confirm it renders as a real HTML table with borders, not pipe-character lines. (6) Reference: May 5 — `src/components/Insights.tsx:MD_COMPONENTS` + `package.json` deps `react-markdown@10.1.0` + `remark-gfm@4.0.1`.

654. **Auto-draft logic that lives in `lib/` is INVISIBLE to the user until a UI tab renders it — when the user dogfoods a tab and asks "where are the SLA signals?", the answer is to surface the existing helpers in the UI, not to build new logic** — On 2026-05-05 PMO_OS Outlook Categories tab showed only category inventory (counts, recent messages, flagged summary). v14's equivalent surface had Unreplied Emails (SLA > 48h) + Missed Mentions tables. PMO_OS already had `fetchEmailReplyTracking()` + `fetchMissedMentions()` helpers in `src/lib/outlook-server-fetch.ts` (port 5050 sidecar) AND `buildEmailReplyDrafts()` + `buildEmailMentionDrafts()` in `src/lib/auto-draft.ts` — all the SLA logic was wired, just not RENDERED. The user could see the auto-drafts in Action Log AFTER they ran but had no preview. Fix: added 2 sections to OutlookCategories.tsx with parallel `Promise.all` fetch + per-section loading/error/empty states + `emailZone()` color-coding mirroring auto-draft.ts thresholds. ~150 lines of UI, zero new helpers. **Why:** A library function that no UI calls is dead code from the user's perspective. PMO architecture treats `lib/` as the "engine" and components as the "dashboard"; both must exist for the engine to be usable. The trap is shipping the engine first (because it has tests, it's clean) and forgetting the dashboard never got the corresponding section. **How to apply:** (1) When dogfooding reveals a "where is X?" gap, GREP `src/lib/` for the concept — likely the helper exists already and the component just doesn't call it. (2) Map every `lib/*.ts` exported function to the component(s) that surface it; treat unmounted helpers as a documentation debt. (3) Surface auto-draft signals BEFORE drafts are committed — show the user "here's what would be drafted" so they can preempt false positives. (4) For multi-source tabs, fire all fetches in parallel via `Promise.all` so a slow sidecar doesn't block the fast one. (5) Each section gets its own loading/error/empty state so failures isolate. (6) Reference: May 5 — `src/components/OutlookCategories.tsx` Unreplied + Missed Mentions sections calling pre-existing `fetchEmailReplyTracking` + `fetchMissedMentions`.

655. **Outlook `Items.Restrict("[Start] >= X")` silently drops recurring meetings whose master appointment Start is before X — `IncludeRecurrences = True` only expands occurrences during ITERATION, not during `Restrict()` evaluation, so a 28-day-forward filter on a calendar with mostly-weekly recurrences returns zero events** — On 2026-05-05 PMO_OS v2 Calendar tab clicked Refresh and showed "No events in the next 28 days" despite 8+ active weekly recurring meetings (BMS Weekly, DevOps Weekly, Aqraboon Weekly, etc.). Live diagnostic via pywin32 confirmed: `Items.Restrict("[Start] >= 05/05/2026 AND [Start] <= 06/02/2026")` → count=0; `for item in sorted+IncludeRecurrences items: if start in window: count` → count=10 (all 8 recurring expansions + 2 one-off events). The recurring meetings had master Start dates in March/April (when the series began); the `Restrict()` `[Start]` predicate evaluates against the master Start, not the upcoming occurrence Start. Fix in `D:\PMO_OS\sidecar\calendar_com.py`: removed `Restrict()` block + `_format_for_restrict()` helper, replaced with sorted-iteration loop (`for item in items: if start < now: continue; if start > end: break`). Also changed `datetime.now()` → `datetime.now().astimezone()` because Outlook returns tz-aware datetimes and naive comparison raises `TypeError`. After fix: Calendar tab renders 10 events / 29.2h total / 8 recurring as expected. **Why:** Outlook's COM model treats recurrence expansion as an iteration-time concern, not a query-time concern. `IncludeRecurrences = True` instructs the iterator (when you `for item in items`) to yield individual occurrence objects with their per-occurrence `Start` property. But `Restrict()` runs as a server-side query against the underlying MAPI table, which only knows about master appointments — so the predicate `[Start] >= X` filters master Start, missing the in-window occurrences. v13's `Get-CalendarEvents.ps1` uses iterate-then-filter (lesson #761) and correctly returns recurrences; PMO_OS v2 sidecar tried to be "more efficient" with `Restrict()` and silently lost data. The bug is invisible from outside (response body is `{count: 0, events: []}` — looks like a calendar-empty success, not a query bug). **How to apply:** (1) When porting any Outlook calendar fetch from PowerShell to Python, NEVER use `Items.Restrict()` for the date filter — always iterate the sorted+IncludeRecurrences collection and filter in code. The performance cost (full enumeration) is bounded by the calendar size, not the window size, and is irrelevant for typical PM calendars (< 500 items). (2) Always make `datetime.now()` tz-aware (`datetime.now().astimezone()`) when comparing against Outlook `.Start` properties — Python's strict tz-aware-vs-naive comparison rule means a naive `now` will start raising `TypeError` the moment any iteration happens. (3) Document the lesson IN THE FILE'S DOCSTRING — calendar_com.py now has the failure mode in its module docstring so the next reader doesn't re-introduce `Restrict()` "for performance." (4) When a user reports "Refresh shows 0 events" for any data fetch, the FIRST diagnostic is to compare two implementations of the same query (Restrict-vs-iterate, server-vs-client filter, paginated-vs-full) — the count divergence localizes the bug instantly. (5) Reference: May 5 — `D:\PMO_OS\sidecar\calendar_com.py:67-75` removed Restrict, added iterate+skip+break. Verification: 10 events visible in PMO OS v2.0 Calendar tab after sidecar respawn.

656. **Windows `HttpListener` `Request.ContentEncoding` defaults to the system ANSI codepage (Windows-1252 on en-US machines), NOT UTF-8 — when the client omits `charset=utf-8` in the Content-Type header, JSON request bodies get silently decoded as Latin-1, corrupting every Arabic / em-dash / smart-quote / warning-glyph byte while LEAVING THE FILE'S UTF-8 BOM INTACT (looks valid, payload is garbage)** — On 2026-05-05 testing PMO Dashboard v14 Board Pack tab end-to-end via Playwright revealed every Arabic character in the saved `_data\board-pack\Q2-2026\*.html` file was double-encoded mojibake (`Ø¥Ù†Ø´Ø§Ø¡` instead of `إنشاء`). Byte inspection: file had correct UTF-8 BOM (EF BB BF) but `[Encoding]::UTF8.GetString(bytes)` returned ZERO Arabic codepoints. Decoding the same bytes as Windows-1252 produced the visible mojibake string. Root cause traced to `Read-PostBody` in `D:\Project Management\Outlook\Run-Update-Server.ps1:36`: `New-Object System.IO.StreamReader($context.Request.InputStream, $context.Request.ContentEncoding)` — `Request.ContentEncoding` falls back to `[Encoding]::Default` (system ANSI codepage = CP1252) when the client doesn't set `charset=` in Content-Type. The browser sent `Content-Type: application/json` (no charset), so UTF-8 bytes for `إنشاء` were decoded as 5 separate Windows-1252 chars `Ø¥Ù†Ø´Ø§Ø¡`, then `Set-Content -Encoding UTF8` re-encoded those mojibake chars as UTF-8 → file has BOM + double-encoded body. Same defect affected `/save-action-log` and `/save-briefing` endpoints (single Read-PostBody helper used by all three). Fix (1 line): force UTF-8 unconditionally — `New-Object System.IO.StreamReader($context.Request.InputStream, [System.Text.Encoding]::UTF8)`. Verified end-to-end: Arabic round-trip from browser → server → disk → re-read produces exact codepoint match (275 Arabic chars in test pack, byte-for-byte). **Why:** JSON spec mandates UTF-8 (RFC 8259). Browsers correctly send UTF-8 bytes regardless of whether they label it. PowerShell's HttpListener inherits .NET's "respect declared encoding, fall back to system ANSI" behavior which is wrong for any modern HTTP API. The mojibake is invisible at the API level (HTTP 200, file written, BOM present) — only end-to-end byte-level inspection catches it. Latin-1 mis-decode is the silent-failure-mode equivalent of "the test passes but the data is corrupt." **How to apply:** (1) ANY PowerShell HttpListener that accepts a JSON body MUST hardcode `[System.Text.Encoding]::UTF8` in the StreamReader constructor — never trust `Request.ContentEncoding`. Same rule for any other server framework whose body-decode falls back to system codepage. (2) When debugging "looks like Arabic but isn't Arabic" mojibake: read file as bytes, check for BOM presence, decode as UTF-8 and grep for `[؀-ۿ]`, decode same bytes as CP1252 — if Arabic appears under CP1252 but not UTF-8, you have a double-encoded UTF-8 file. Recovery: `[Encoding]::UTF8.GetString([Encoding]::GetEncoding(1252).GetBytes(<utf8_decoded_text>))` reverses the corruption mathematically. (3) Always test save endpoints with a non-ASCII payload (Arabic, em-dash `—`, smart quote `"`, warning glyph `⚠`) before declaring them complete — ASCII-only smoke tests miss this entire class of bug. (4) Even after fixing the server, historical files saved through the broken endpoint stay corrupt unless reverse-decoded — choose recovery script vs natural-rebuild based on whether closed/frozen entries exist (closed entries in Action Log don't get re-saved). (5) Reference: May 5 — `D:\Project Management\Outlook\Run-Update-Server.ps1:34-42` `Read-PostBody` UTF-8 forced; verified via PMO Dashboard v14 Board Pack save → 275 Arabic chars round-trip clean. Test report: `D:\tmp\board-pack-test-report-2026-05-05.md`.

657. **Renderer tabs that fetch live data MUST go through tRPC, not `window.api.net.fetch` — a tab using local `useState` for fetch results loses everything when the user switches tabs because Electron tab routing unmounts inactive components, but `trpc.X.useQuery` results are cached at the React Query client level and survive any number of unmount/remount cycles** — On 2026-05-05 PMO_OS v2 Calendar tab clicked Refresh, fetched 10 events, rendered them; user switched to Action Log; came back to Calendar; saw the empty "Click Refresh to scan your Outlook calendar" idle state again. The component had been unmounted and remounted — `useState({state:'idle', events:[]})` reinitialized to empty. Calendar.tsx was the only tab in 19 that fetched directly via `fetchCalendarEvents()` → `proxiedJson` → `window.api.net.fetch` → port-5070 sidecar. Every other tab (ActionLog, DataGate, SpiCpi, Insights, ProjectRegister, etc.) used `trpc.X.list.useQuery({...})` which is React-Query-backed and caches results across mounts indefinitely (or until staleTime). Fix: added `calendarRouter` to `src/main/trpc/router.ts` with a `list` query that calls the sidecar from the main process via `getSidecarBaseUrl()` + Node 18+ global `fetch`, validates the response with a Zod schema, returns typed `CalendarListResult`. Switched Calendar.tsx from `useState`/`load()` to `trpc.calendar.list.useQuery({ days }, { staleTime: 5*60*1000, refetchOnWindowFocus: false, retry: false })`. Refresh button now calls `query.refetch()`. Added a `cached Ns ago` indicator next to the cost summary so the user can see the cache layer is working. **Why:** tRPC isn't just a typing convenience — it's the bridge that enables React Query's cache. Without it, every fetch in a renderer component dies with the component. Electron tab-routing apps (where only the active tab is in the DOM) interact catastrophically with local-fetch-state because every navigation = full reload. tRPC's `useQuery` registers the result in a query client that lives at App.tsx scope (above the tab boundary), so the cache survives. The "single source of truth in main process" benefit is real but secondary — the cache benefit is what makes the architecture viable for tab-routed apps. **How to apply:** (1) Audit every tab in any new Electron app: any component calling `window.api.*` or direct `fetch` in `useEffect`/event handlers needs a tRPC migration; same applies to direct sidecar URL calls in helpers like `lib/sidecar-fetch.ts`. (2) The migration recipe: define Zod input + output schemas in the router, write a query that calls the sidecar from main process, replace component's `useState` + manual fetch with `trpc.X.useQuery(input, options)`. Set `staleTime` based on how often the upstream changes (calendar 5min, devops 2min, register 30min). Default `refetchOnWindowFocus: false` and `retry: false` for sidecar/COM calls — focus-refetch causes COM dialogs, retry-storms hide real errors. (3) For COM-dependent endpoints (Outlook, Excel, Word) the main-process call inherits the user's logged-in COM session — same integrity-level rule (lesson #761) applies but only one place to enforce it. (4) The renderer-side `proxiedJson` + `window.api.net.fetch` pattern was a workaround for renderer-CORS and is now obsolete for any data that should survive tab switches; keep it only for one-shot operations (download, file dialog). (5) Always show a cache-age indicator (`cached Ns ago` or "fetched at HH:MM") so users distinguish "stale data still loading fresh" from "live just-fetched data" — invisible cache layers feel buggy. (6) Reference: May 5 — `D:\PMO_OS\src\main\trpc\router.ts` `calendarRouter.list` (Zod-validated `CalendarListResult`) + `D:\PMO_OS\src\components\Calendar.tsx` `trpc.calendar.list.useQuery` migration; same pattern queued for Meetings tab next.

## PMO_OS Meetings Tab v14 Parity (May 5, 2026)

756. **MENA workweek + bilingual document patterns — three rules for Egypt/Saudi/UAE/Qatar/Kuwait projects: (a) workweek is Sunday-Thursday (Fri+Sat weekends) — build `addWorkdaysSunThu()` helper, never `addWorkdays(Mon-Fri)`; (b) bilingual `EN | AR:` pipe-separated section headers in Word MOMs require an optional middle group in classification regex (shared `_PIPE_TAIL` pattern) — without it, every header is unclassified and parser dumps document into `_unclassified[]` while reporting `ok: true`; (c) Calendar APIs need explicit `days_back` parameter alongside forward `days` — forward-only windows hide Past Meetings governance (MOM Status, Extract MOM, post-meeting actions)** — Three patterns from May 5-6 PMO_OS Meetings tab + sidecar work. **(a) Sun-Thu SLA math:** Naive `addWorkdays(start, n)` using Mon-Fri (US convention) gives a 2-day-shift bug — a 3-workday SLA assigned Tuesday computes due-Friday under US rules, but Friday is a weekend day in MENA so real due date is Sunday. Build `addWorkdaysSunThu(start: Date, workdays: number): string` — increments one day at a time, skips `dow === 5 || dow === 6` (5=Friday, 6=Saturday in JS Date getDay()), counts only Sun(0)/Mon(1)/Tue(2)/Wed(3)/Thu(4) as workdays. Returns YYYY-MM-DD string (date only, no time — SLAs are calendar-day boundaries; storing time creates timezone confusion). Used by `buildDataGateDrafts`, `buildSpiDrafts`, `buildCpiDrafts`. Test with at least 7 cases: (i) no weekend within window, (ii) start on Friday rolls to Sunday, (iii) start on Saturday rolls to Sunday, (iv) span across two Fri-Sat weekends, (v) zero workdays = same day (must be workday), (vi) start on Sunday + 5 workdays = next Thursday, (vii) workday count ignores 0-extra-day skipping of starting weekend. Why the shift matters: governance rules typed as "3 workdays to respond" mean exactly 3 working business days; if helper counts Friday as a workday in MENA, user gets due-Friday alert that's already a weekend day — SLA appears expired before they can act, dashboard's "overdue" count flags every weekend-due item as red, polluting urgency signal. For ANY MENA-resident project, audit all date-shift code (`addDays`, `setDate(d+n)`, sprint date math, escalation timers, briefing scheduling) for the implicit-Mon-Fri-weekend assumption. Build a SINGLE helper per project — DON'T inline dow-skip logic in 6 places. For multi-region projects (US team partnering with Egyptian team on same SLA clock), pick ONE workweek convention per ticket and document it; mixing conventions silently is worse than picking the wrong one explicitly. **(b) `_PIPE_TAIL` for bilingual headers:** May 5: PMO_OS sidecar `/extract-mom` against `MOM 20-42026.docx` returned `paragraph_count: 99, attendees: 0, decisions: 0, actions: 0, _unclassified: 99` — 100% classification miss that LOOKED like successful extraction. User's MOM template uses house pattern: every section header is `English | Arabic:` (e.g. `Date | التاريخ:`, `Attendees | الحضور:`, `Meeting Type | نوع الاجتماع:`). Pre-existing regex `^\s*(date|التاريخ|تاريخ)\s*[:：]` matched neither `Date:` standalone nor `Date | التاريخ:` because pipe-Arabic-tail wasn't expressible. Fix in `D:\PMO_OS\sidecar\word_com.py:38-54`: shared `_PIPE_TAIL = r"(\s*\|\s*[^\n:：]+?)?"` optional group inserted between keyword alternation and trailing colon — so `^\s*(date|التاريخ|تاريخ)` + `_PIPE_TAIL` + `\s*[:：]` matches `Date:`, `التاريخ:`, `Date | التاريخ:`, AND `التاريخ | Date:`. Verification: same docx now returns `attendees: 86, _unclassified: 4` (96% classification rate; remaining 4 are non-section paragraphs). When porting any text classifier from English-only to bilingual EN/AR documents, audit EVERY regex anchor for the assumption that headers are single-language. Use shared `_PIPE_TAIL` optional-middle pattern instead of duplicating regex variants — keeps keyword list DRY. Test with representative real document containing user's house template BEFORE declaring shipped (synthetic test data passes by construction). When `_unclassified[]` count is high but `ok: true`, treat as parser bug, not empty document. **(c) Calendar `days_back` parameter:** May 5: PMO_OS Meetings tab refactored to v14 layout (Upcoming + Past split). Initial fetch via `fetchCalendarEvents(14)` → sidecar `/calendar-events?days=14` returned 6 events, all upcoming, Past Meetings table rendered "No past meetings in window" despite user having 14+ past events visible in v14 HTML for same week. Root cause: sidecar `events(days: int = 28)` computed `now → now + days`, no backward direction. v14 HTML's `Get-CalendarEvents.ps1` defaults to 14 back + 14 forward (28-day centered window). Fix: extended Python signature to `events(days: int = 28, days_back: int = 0)`, computed `start = now - timedelta(days=days_back)` instead of `now`. Updated main.py route to read `days_back` query param. Updated sidecar-fetch.ts `fetchCalendarEvents(days, daysBack)` signature. Updated `Meetings.tsx` to call `fetchCalendarEvents(14, 14)`. Backward-compat preserved — `days_back=0` is default, existing callers don't change behavior. After fix: window expanded Apr 21 → May 19, 20 events split 6 upcoming + 14 past, governance counters (`9 MOM missing > 48h`) fired correctly. Why: forward-only windows model "what's coming" but not "what just happened." For Meetings tab specifically, post-meeting governance flow (write MOM within 48h, extract decisions, route to Action Log) needs backward window to surface "you owe an MOM for that meeting 3 days ago." Without `days_back`, the entire Past Meetings governance surface is invisible to UI even when data exists in Outlook. Calendar fetch helpers should default to CENTERED window (back + forward), not forward-only — consumers can pass `daysBack=0` to opt out, but default should match human use cases. Default `days_back: int = 0` keeps backward compatibility (no breaking change). Pass both as query params, not single combined `days`, so future windowing logic (relative offsets, asymmetric windows) doesn't require breaking the API. Mirror the source artifact's window — v14's 14+14 is grounded in 2-week sprint cadence; copy that, don't reinvent. **How to apply:** (1) For any MENA-resident project, audit all date-shift code for Mon-Fri assumption; build single `addWorkdaysSunThu` helper, test 7+ cases. (2) For any bilingual EN/AR text classifier, use shared `_PIPE_TAIL` pattern; test against real user-template docs. (3) For any calendar fetch helper, default to centered window via `days_back: int = 0`; mirror source-artifact cadence. References: `D:\PMO_OS\src\lib\auto-draft.ts:90-110` `addWorkdaysSunThu()` + `auto-draft.test.ts` (7 tests, v2.0.3); `D:\PMO_OS\sidecar\word_com.py:38-54` SECTION_PATTERNS + `_PIPE_TAIL` regex; `D:\PMO_OS\sidecar\calendar_com.py:48` `events()` signature + `D:\PMO_OS\sidecar\main.py:127-141` route + `D:\PMO_OS\src\lib\sidecar-fetch.ts:90-99` + `D:\PMO_OS\src\components\Meetings.tsx loadEvents`. (Folded former #658, #659, #691.)

746. **Electron renderer-vs-main process pitfalls — three classes of silent failure manifesting as "button hangs / service offline / no error in console"** — The renderer is a sandboxed Chromium context with stubbed browser-globals; the main process is Node booted once per `electron .` invocation with no auto-reload. Three traps from the May 5 PMO_OS Meetings tab build, each invisible at the API/console level. **(1) `window.prompt`/`alert`/`confirm` are no-op in renderers** — they return `null` immediately without rendering a dialog. Code depending on them hangs the UI in whatever state was set just before (e.g., a `busy: true` row stuck on `⏳ Working…` forever because the `null` return path took a setRow that left busy=true). Fix: render React state-driven `<input>`/modal in the component DOM tree; for inline fallbacks (when an IPC handler isn't loaded yet), use per-row state to switch between "button" and "input + submit + cancel" (autoFocus + onKeyDown Enter, keyboard-accessible). For yes/no confirms, use a modal component or `dialog.showMessageBox` via main-process IPC. **(2) Renderer-side `fetch()` to localhost is silently blocked by Chromium's loopback CORS even with `Access-Control-Allow-Origin: *`** — Chromium evaluates the origin pair (e.g. `file://` vs `http://127.0.0.1:5070`) before consulting CORS headers; the fetch rejects with "Failed to fetch" and no console error. Symptom: status badge "always says offline" while curl from a separate shell confirms the service is up. Fix: route ALL renderer-side localhost fetches through `window.api.net.fetch` (main-process proxy via `ipcRenderer.invoke('devops:fetch', ...)`) — Node's fetch in main doesn't enforce browser origin policy. When fixing, audit related labels in the same component for staleness — port numbers, service names, version strings often drift (PMO_OS LiveBackendBadge had stale `'sidecar :5060'` label that had been wrong since the probe URL changed weeks earlier). **(3) New `ipcMain.handle('channel-name', ...)` requires a full `npm run dev:all` restart — Vite HMR does NOT propagate to the main process** — Vite watches `src/**` for HMR; nothing watches `electron/**`. The renderer-side code that invokes the new channel reloads via HMR (preload re-runs on page reload, so `typeof window.api.x === 'function'` returns true), but the main-process side is still running OLD compiled code from `dist-electron/electron/main.js` and returns `Error: No handler registered for 'channel-name'`. `npm run build:electron` updates the on-disk bundle but the running Electron process holds the old bundle in memory. Fix: when adding any new ipcMain.handle, IMMEDIATELY warn the user that `dev:all` needs a restart; pair the new handler with a graceful fallback in the renderer (typeof check + try/catch on invoke + error-message regex `No handler registered`); the fallback should be a real working alternative (inline input, manual path entry), not just an error message. Long-term, consider `vite-plugin-electron` or `electronmon` to eliminate this trap. **Why:** Electron has a TWO-PROCESS architecture and three independent silent-failure modes that all SOUND like the same generic "Electron quirk" but stem from different layers (browser globals stubbed for security, Chromium private-network-access policy, and main-process boot lifecycle). Each trap is invisible at the API level — HTTP 200, no console error, `typeof X === 'function'` — only end-to-end testing surfaces them. **How to apply:** (a) Never use `window.prompt`/`alert`/`confirm` in renderer code — render React UI elements instead. (b) Never write `fetch()` to localhost from a renderer — copy from existing helpers (sidecar-fetch.ts, devops-fetch.ts), don't write `fetch()` from scratch. (c) After ANY new ipcMain.handle, warn the user about the required `dev:all` restart + add a graceful fallback in the renderer. (d) When debugging "button hangs busy" / "service shows offline despite curl OK" / "Error: No handler registered", check these three pitfalls first. References: `D:\PMO_OS\src\components\Meetings.tsx:extractMOMForRow` (inline-input fallback) + `LiveBackendBadge.tsx:34-46` (probeSidecar via window.api.net.fetch, mirrored from sidecar-fetch.ts:24 proxiedJson pattern) + `electron\main.ts:148-160` (ipcMain.handle('dialog:open-file', ...)). (Folded former #660, #661, #662.)

750. **UI migration + version-bump discipline in Electron apps — four rules that prevent silent regressions: (a) frozen-artifact discipline (never edit FROZEN HTML files; new feature work goes to the successor system); (b) window/menu version strings ALWAYS via `app.getVersion()`, never hardcoded; (c) when removing/replacing a UI surface, audit every component's CSS color assumptions and re-home into the same color context; (d) build a side-by-side affordance diff before merging any UI migration so icons/badges/ordering/accelerators carry over** — Patterns from May 5-6 PMO_OS v2.0.5-v2.0.12 work. **(a) Frozen-artifact discipline:** When an HTML file is declared FROZEN (v10/v13/v14 PMO Dashboard, locked Sunday Talking Points decks, "do not touch" board reports), all NEW feature work belongs in the successor system (PMO_OS v2 Electron) — the frozen file becomes a parity reference, not an editing target. May 5: user asked "make QUARTER dropdown like v.14" while v14's Board Pack tab was under test; I edited `PMO Dashboard_v14.html` directly (Fiscal Year section + 8 helpers + i18n). User pushback: "i need that in PMO system, not in the Html, We mentioned that we will freeze v.10 and v13 and v14, right?!!!!" Recovery: revert all v14 edits across 6 sites (~20 min) + re-implement in PMO_OS (~35 min). The "ACTIVE / canonical" framing in CLAUDE.md was ambiguous — testing is ALWAYS allowed (read-only); edits are NOT. Rules: BEFORE editing any HTML in deliverable folders, grep CLAUDE.md/memory for `frozen`/`FROZEN`/`archive`/`do not touch`/`canonical until` near the file's name; default-interpret ambiguous "make X like v.14" as "match v.14's behavior in active system" not "edit v.14 directly"; if you DO accidentally edit, build per-site revert checklist + multi-pattern grep for ALL identifier names introduced; treat git status `??` (untracked) on frozen files as a yellow flag; the successor should reuse the EXACT same localStorage key namespace OR a clearly-prefixed new one (`pmo-os-fiscal-year-v1` vs v14's `pmo-fiscal-year`) — never silently overload the same key, users running both apps in parallel get cross-talk. **(b) Version-string hardcoding:** ANY user-visible string that includes a version number MUST come from `app.getVersion()` (or `pkg.version` for plain Node, `import.meta.env.VITE_APP_VERSION` for Vite renderer). Same rule for: window title, About dialog, footer text, log file headers, telemetry envelopes, error report subject lines, "User Agent" strings. May 6: PMO_OS v2.0.8 shipped with `title: 'PMO OS v2.0'` hardcoded in `electron/main.ts createMainWindow()` — title bar showed v2.0 while Help → About correctly showed v2.0.8 (8-version drift since Phase A scaffold). Fix in v2.0.9: `title: \`PMO OS v${app.getVersion()}\`` — line never needs changing again. Audit any string literal containing `v\d+\.\d+\.\d+` shape via `grep -rE "['\"\\`]v[0-9]+\.[0-9]+\." src/ electron/`. Don't try to keep dual-source-of-truth synced via release checklist; humans miss those. Test by `npm version patch` + smoke-launch — title should reflect new patch with zero code changes outside package.json. For Vite renderer: `define: { 'import.meta.env.VITE_APP_VERSION': JSON.stringify(pkg.version) }` in `vite.config.ts`. **(c) UI surface removal — audit color contexts:** Removing a sidebar is a 3-line CSS swap (`grid-cols-[240px_1fr]` → `flex flex-col`) PLUS a careful re-home of three things: app title, language toggle, live-backend badge. The naive "delete the aside, leave a centered header" version breaks the LanguageToggle because its `text-white/60 hover:text-white border-white/20` classes only render correctly on a dark background. Correct: slim `<header className="bg-primary text-white px-5 py-3 flex items-center justify-between gap-4 sticky top-0 z-30">` — same navy as the old sidebar, so LanguageToggle styling stays valid. Add a "current tab" pill (icon + label, `bg-white/10`) so the user keeps a "where am I" anchor that the sidebar's `bg-accent` highlight previously gave for free. Make header `sticky top-0 z-30` so it doesn't scroll out (sidebars are sticky-by-default; new header should match). Pair sidebar removal with menu split: replace single Navigate with three top-level entries (Daily Use / Bi-Weekly / Reference) via `buildBandSubmenu(labels, band, onTabClick)` helper that filters `TAB_TABLE` by band; accelerators stay unchanged. **(d) Affordance diff before merging UI migration:** Before merging ANY UI surface migration, write a markdown table: `| Affordance | Old Surface | New Surface | Status |` with rows for icons, labels, ordering, accelerators, badges, tooltips, hover state, focus ring, group separators, disabled state. Mark each Status as ✅ carried / ⚠ dropped (justified) / ✗ regression. Anything ✗ blocks merge. Anything ⚠ requires one-line justification. May 6 v2.0.8 sidebar→3-menus shipped without per-tab emoji icons (typecheck passed, tests passed, click handlers worked — only signal was visual when comparing old vs new). Mahmoud noticed within hours: "need to appear same icon which that was before beside the screen in the three tabs". Fix in v2.0.12: `TAB_ICON_MAP: Record<TabId, string>` mirroring `NAV[].icon` + concat `TAB_ICON_MAP[t.id] + '  ' + labels.tabs[t.id]` in both `buildBandSubmenu` and `buildNavigateSubmenu`. Icons are language-agnostic Unicode → same map serves EN+AR. For per-item icons that ARE pixel art (.png), use Electron's `NativeImage`; for emoji, prefer Unicode-string approach (zero file deps, free localization, survives package-asset-path changes). When icon source-of-truth lives in two places (renderer's `App.tsx` + main's `navigate-menu.ts`), comment the secondary location: `// MIRRORS NAV[].icon in src/App.tsx:47-66. Edit both.` **Why these four rules cluster:** All are silent-failure modes during UI work — typecheck passes, tests pass, click handlers work; only signal is visual or version-string drift. Frozen-artifact violations cost 45 minutes of re-work per incident; hardcoded version strings show stale strings to every user on every launch; removed UI surfaces with bad re-homes break component CSS expectations; missing affordances after migration feel like the new surface "is less" than the old. **How to apply:** (1) Treat any HTML in frozen folders as read-only — successor system gets the change. (2) NEVER hardcode version strings; always use `app.getVersion()` or equivalent. (3) Before deleting any sidebar/panel, list every component inside it + audit each one's CSS color assumptions + re-home into matching color context. (4) Before merging any UI migration, run the affordance diff. References: v14 reverts at `Dashboards/PM Handling/PMO Dashboard_v14.html` lines 814-817/1973/9072-9075/15012/15030+/15250 + PMO_OS port at `src/lib/quarter-helpers.ts` + `BoardPack.tsx` + `Settings.tsx`; `electron/main.ts:114` (v2.0.9 title fix); `src/App.tsx` (sidebar→header migration) + `electron/lib/navigate-menu.ts` (`buildBandSubmenu` + `TAB_ICON_MAP`, v2.0.8/v2.0.12). (Folded former #663, #687, #695, #697.)

747. **Reach for native Electron primitives before adding heavyweight deps — Electron already bundles Chromium, so `webContents.printToPDF()` from an offscreen `BrowserWindow` replaces Puppeteer (~140 MB savings on installer), and the unidirectional `webContents.send('channel', payload)` + preload `on{Event}(cb)` listener pattern replaces ad-hoc IPC RPC for main → renderer signaling (menu→tab switch, system event, scheduler tick)** — Two patterns from the May 6 PMO_OS Roadmap residual #16 (Quarterly Board Pack PDF) and Navigate-menu (19-tab keyboard-shortcut surface). **(a) PDF generation:** Original spec said Puppeteer; stacking it onto Electron 30 + Vite + better-sqlite3 + react-i18next would have pushed the NSIS installer past 280 MB. Use `BrowserWindow({ show: false, webPreferences: { offscreen: true, sandbox: true, javascript: false } })`, load HTML via `data:text/html;charset=utf-8,${encodeURIComponent(html)}` URL (file:// requires a temp file + cleanup; `data:` is in-memory and disappears with the window), await `loadURL()` (resolves at `did-finish-load`) + 100-300ms paint-settle timeout, call `webContents.printToPDF({ pageSize: 'A4', printBackground: true, margins: {top:0.6,bottom:0.6,left:0.5,right:0.5}, landscape: false })`, write the buffer to `%APPDATA%/<app>/_data/...`, destroy the window in `finally`. Sandbox + `javascript: false` are CRITICAL — PDF source is static; running JS opens XSS surface for nothing. The 200ms paint-settle is non-obvious but necessary — `did-finish-load` fires when DOM is ready, not when layout has flushed; without it `printToPDF()` sometimes returns a buffer with page background but missing table content. `show: false` alone still creates an OS-level window briefly; pair with `offscreen: true` to keep it fully out of compositing. Sanitize path-component strings with `/[^A-Za-z0-9_-]/g → _` before joining — never trust client strings as path components. **(b) Menu→renderer state:** Main owns `Menu.buildFromTemplate()` + `Menu.setApplicationMenu()`. Each item's click handler resolves the focused window and calls `win.webContents.send('nav:set-tab', tabId)`. Preload exposes `window.api.navigate.onSetTab(cb)` which subscribes via `ipcRenderer.on` and returns an unsubscribe fn for React useEffect cleanup. Renderer hooks once on mount: subscribe → `setActiveTab(tabId)` on each event. Bilingual menus rebuild on language change via a one-way `'menu:rebuild'` IPC (renderer → main, no result needed); a second useEffect on i18n.language fires the rebuild. Co-locate menu labels + tab IDs in a single `electron/lib/navigate-menu.ts` module exporting EN+AR label constants + `buildApplicationMenu(opts)` — never inline 90+ lines of nested template objects in main.ts. Accelerator strings: `CmdOrCtrl+...` for cross-platform (even Windows-only apps for forward compat). Group shortcuts by band: Shift = primary, Alt = secondary, Alt+Shift = reference, Ctrl+Comma = Settings (matches VS Code muscle memory). Section separators + disabled section-header items give visual grouping without nesting submenus. **Why:** Electron's value prop is "Chromium + Node in one process tree." Puppeteer's value prop is "drive headless Chromium" — adding it to an app that ALREADY has headless Chromium via `BrowserWindow({ show: false })` doubles the installer for zero new capability (only legitimate Puppeteer case is driving a SEPARATE Chromium with a different version/profile/extension set, which is rare). Similarly, ad-hoc IPC RPC schemes for menu→tab signaling reinvent what `webContents.send` already does cleanly — using the canonical primitive means listener lifecycle (subscribe / unsubscribe) lines up naturally with React useEffect cleanup, preventing leaks across HMR cycles in dev. The cost of the wrong choice is enormous: 140 MB of installer bloat means slower CI, slower customer downloads, more disk pressure, more attack surface to patch. **How to apply:** (1) Before adding any "headless browser automation" dep, check if `BrowserWindow({ show: false, offscreen: true, sandbox: true, javascript: false }) + webContents.printToPDF()` solves it — only consider Puppeteer/Playwright if you can name a specific feature BrowserWindow lacks (multi-tab automation, network interception). (2) For menu → renderer signals, ALWAYS use `webContents.send` + preload `on{Event}(cb)` returning an unsubscribe — DO NOT pass React setters across IPC, DO NOT use globals. (3) Extract input validation + path resolution into pure helpers in `src/lib/` so they're vitest-testable without the Electron runtime; the IPC handler stays thin (validate → spawn → render → write → destroy). References: `D:\PMO_OS\electron\ipc\board-pack-handlers.ts` + `src/lib/board-pack-pdf-helpers.ts` (15 tests) + `src/components/BoardPack.tsx` 📄 Export PDF button (commit `03cff63` L071, replaces Roadmap v2.2 #16's Puppeteer assumption); `electron/lib/navigate-menu.ts` (~190 LOC) + `electron/main.ts installMenu` helper + `electron/preload.ts navigate.onSetTab` + `src/App.tsx` (two useEffects: listener subscribe + i18n.language rebuild). v2.0.5 ships Navigate menu. (Folded former #664, #681.)

749. **Keep Electron-bound IPC logic thin, push everything else into pure helpers in `src/lib/` — anything that can run in plain Node (input validation, regex patterns, path joining, sanitization, default lookups, env resolution, backup-filename parsing) goes into a helper that vitest can import without spawning Electron, and that plain-Node CLIs (drizzle-kit, tsx scripts, db-restore) can also use. The IPC handler becomes a 5-line orchestrator (validate → call Electron API → return result); the helper carries 100% of unit-test coverage** — Two patterns from May 6 PMO_OS that surface the same architectural rule. **(a) Per-handler purity:** When writing any new `electron/ipc/*-handlers.ts` file, FIRST identify every piece of logic that can run in pure Node — those go in `src/lib/`. The handler should be a thin orchestrator: `(args) => { const v = validate(args); if (!v.ok) return v; return doElectronWork(resolved(args)); }`. Pass Electron-derived state (paths, window handles, app metadata) as explicit arguments to helpers — never have a helper call `app.getPath()` internally. Cover path-traversal edge cases (`../`, `;`, spaces, slashes, dots) in sanitizer tests — those are the ones exploited if input is ever client-controlled. Same pattern across PMO_OS: backup-db, auto-fetch, briefing, migrate-v13, board-pack-pdf — each ships ~10-15 helper tests + a 5-line handler. Test count is a useful proxy: if you wrote 0 tests for a new handler, you probably leaked logic into it. **(b) Shared helpers for dual-runtime apps:** When SOME code runs in Electron (with `app.getPath('userData')`) and OTHER code runs as plain Node (drizzle-kit, tsx scripts, CLIs), userData path resolution MUST live in a SHARED helper that BOTH runtimes import. PMO_OS `src/lib/db-path.ts` exports `resolveAppEnv(opts)` + `resolveUserDataDir(env)` + `resolveBackupDir(env)` + `formatBackupName(env, date, kind)` + `parseBackupName(filename)`. Helper has zero Electron imports so `drizzle.config.ts` + `scripts/db-restore.ts` use it without bundling Electron. Resolution order: (1) explicit `opts.override` → (2) `process.env.APP_ENV` → (3) `opts.isPackaged` (Electron-only) → (4) safe default `'dev'`. Folder mapping: `prod → %APPDATA%\<App>\`, `dev → %APPDATA%\<App>_DEV\`. Backup filenames embed the env (`pmo_prod_20260506_153022_pre-migrate.db`) so a backup-folder listing shows env at a glance. Critical wiring in `electron/main.ts`: `const env = resolveAppEnv({ isPackaged: app.isPackaged }); app.setPath('userData', resolveUserDataDir(env));` runs SYNCHRONOUSLY before `app.whenReady()` resolves — so `initLogger()` / `runMigrations()` / backup / pat-store all see the env-aware path via `app.getPath('userData')` without each knowing about env. setPath AFTER whenReady is broken: `initLogger()` joins userData + LOG_FOLDER_NAME at boot, every subsequent log line ends up in the wrong place. **Why:** Electron's main-process modules can only be imported in a running Electron context — `import { app } from 'electron'` from a vitest worker throws because `app` is initialized by the Electron runtime, not Node's import system. The naive workaround is heavy mocking (`vi.mock('electron', () => ({ app: { getPath: vi.fn(...) }, BrowserWindow: vi.fn() }))`) — brittle, slow, tests almost nothing meaningful. The clean solution is to NEVER let validation/sanitization/path-resolution touch Electron imports; if the helper takes (userDataRoot, quarter, packDate) and returns a string, vitest tests it directly. Same logic extends to dual-runtime tooling: drizzle.config.ts can't import Electron — putting `app.isPackaged` checks there would crash; the shared helper sidesteps the problem entirely. The default for plain Node (no Electron context, no APP_ENV var) MUST be `'dev'` — never `'prod'` — so running drizzle-kit on a fresh checkout never trashes prod data. **How to apply:** (1) For new `electron/ipc/*-handlers.ts`, identify pure-Node logic FIRST and extract to `src/lib/` BEFORE writing the handler body. (2) Pass Electron-derived state as explicit arguments — helpers never call `app.*` internally. (3) Vitest file goes next to the helper (`board-pack-pdf-helpers.test.ts` next to `board-pack-pdf-helpers.ts`), not next to the handler. (4) For dual-runtime apps with Drizzle/Prisma/CLI tooling, put userData path + env resolution in a shared helper imported by both runtimes; helper MUST have zero Electron imports. (5) Always call `app.setPath('userData', resolved)` SYNCHRONOUSLY at main-module top, before `app.whenReady()` resolves — verify by logging `app.getPath('userData')` inside initLogger on first boot. (6) Backup filenames MUST embed env (`pmo_prod_...db` / `pmo_dev_...db`); provide a `parseBackupName()` companion to `formatBackupName()` so future tooling extracts env + date without regex-by-hand. References: `D:\PMO_OS\electron\ipc\board-pack-handlers.ts` (thin handler) + `src/lib/board-pack-pdf-helpers.ts` (15 tests, pure helpers — same pattern as backup-db, auto-fetch, briefing, migrate-v13); `src/lib/db-path.ts` (~130 LOC, 28 tests) + `electron/main.ts:155-164` synchronous setPath + `drizzle.config.ts` + `scripts/db-restore.ts` (v2.0.9). (Folded former #665, #696.)

755. **Bilingual hardcoded-string scanner evolution — four rules: (a) for tight ship-gate timelines (≤2 weeks), DO NOT add ESLint to a project that doesn't have it — a 150-line custom Node script gives 80% of `eslint-plugin-i18next`'s coverage at 5% of the integration cost; (b) split emoji from translatable label in badge/pill constants (`{emoji, labelKey}` not `{label}`) so emoji stays language-neutral and locale strings stay clean; (c) iteratively tune scanner heuristics — each rule that drops a false-positive class (ALL-CAPS identifiers, template-literal contents) makes the scanner more trustworthy and maintainers more likely to fix true positives; (d) for known false-positive shapes like TypeScript `Promise<X>` generics in JSX-heavy files, hoist the offending types into a non-`.tsx` sibling — don't try to "escape" the regex or rewrite the scanner** — Four patterns from May 6 PMO_OS bilingual sweep arc (commits L069, L070, L076, L077, gh-pat work). **(a) Custom scanner over ESLint mid-sprint:** May 6 closing Roadmap v2.2 #25 ("Hardcoded-string lint rule") with 9 days to May 13 ship gate. The "right" solution is `eslint-plugin-i18next` with `@typescript-eslint/parser` + `eslint-plugin-react` — 3-4 hour task involving `eslint.config.mjs`, peer-dep alignment, ignore-path tuning, inevitable warning storms across 24-component codebase. Wrong tool for the deadline. Built `scripts/check-hardcoded-strings.cjs` instead: walks `src/components/**/*.tsx`, runs two `String.matchAll()` passes (`/>([^<>{}\n]{4,})</g` for JSX text + `/\s([a-zA-Z][\w-]*)=(["'])((?:(?!\2).)+?)\2/g` for JSX attributes), filters with three heuristics (length ≥4, starts with `[A-Z]`, doesn't look like Tailwind class list), skips comment-only lines + attribute whitelist (`className`, `key`, `href`, `src`), prints `file:line [kind] text`, exits 1 if total > 0. Wired as `npm run check:strings`. First run: 54 findings across 6 components — all known v14-track tech debt (BoardPack 11, Calendar 4, Meetings 19, OwnerAudit 3, Settings 17, TeamVelocity 2). Captured as known baseline, NOT fixed; future commits run script — if findings count rises, that's a regression in a feature branch. Custom regex scanners catch ~80% of obvious cases at ~5% of integration cost. They miss subtle cases (template literals with embedded text, dynamic JSX, conditional rendering with literals) — but those are rarer than "forgot to wrap a string in t()", and ESLint also struggles. The trap is treating the scanner as a long-term substitute — it's a stopgap that lets you ship the deadline without skipping discipline. After ship, the proper plugin can be added against a known-clean baseline. Stopgap MUST exit non-zero on findings — passive scanner that always exits 0 doesn't get adopted. Capture existing tech debt as baseline count in commit message so future contributors know what's tolerated vs new. **(b) Emoji + labelKey split for badge/pill constants:** Pre-existing `STATUS_BADGE: { active: { bg, text, label: '🟢 active' }, paused: { ..., label: '◯ paused' } }` is a translation anti-pattern. Naive translation creates keys like `'🟢 نشط'` (translators copy emoji along with text) — duplicating emoji across every locale file. Refactor to `{ active: { bg, text, emoji: '🟢', labelKey: 'portfolioStatus.statusActive' } }` with JSX `{cfg.emoji} {t(cfg.labelKey)}`. Locale files now have `"statusActive": "active"` (en) and `"statusActive": "نشط"` (ar) — clean strings, no emoji. Same pattern for inline labels (`◎ Where`, `▶ Next`, `⚠ Risk`, `➜ Next Step`) — render icon in JSX, translate just the word. Emojis-in-i18n-values are anti-pattern for three reasons: (a) translators are tempted to "translate" them (replacing 🟢 with culturally-specific symbols); (b) changing emoji means editing every locale file; (c) RTL bidi reordering can put emoji on wrong side of text — JSX-rendered emoji + translated label keeps source order, neutral bidi class doesn't flip. Keep emoji-that-IS-the-label (🔴 AT-RISK as brand badge, 📸 Save Snapshot as button affordance) as single translation string when emoji semantically belongs — but per-case judgment, default to splitting. When translators touch the file, emoji-in-string cases are red flags. **(c) Heuristic tuning compounds:** Scanner output trends matter — if false-positive rate exceeds 20%, add a heuristic rule. If true-positive rate is high but maintainers ignore output, you have trust collapse — fix before adding next feature. Two specific extensions from L076+L077: (i) skip all-uppercase identifier-style tokens (`/^[A-Z][A-Z0-9_]+$/`) — `ALIASES`, `RESET`, `RUN_AS_NODE`, `UTF-8`, `SQLite` are code identifiers/proper nouns meant to render literally; flagging them creates noise that desensitizes maintainers. (ii) skip strings inside template literals via unescaped-backtick counting — `BoardPack.buildHtml()` emits server-rendered HTML inside template literals; scanner was matching `>Project<` inside template strings as JSX text. Count unescaped backticks before match offset; odd count = inside template literal → skip. Eliminated 8 false-positives in one component. Static scanners exist on a precision-recall tradeoff curve — aggressive scanners catch more true positives but bury them in noise; conservative scanners are clean but miss real bugs. Right operating point: ship aggressive heuristics, prune false positives as discovered. End-state is a scanner that NEVER lies — when it flags something, maintainer trusts it's real and fixes it. Lying scanners get `# noqa` annotations everywhere and stop catching anything. Document each heuristic inline with the false-positive example that triggered it (`// e.g. ALIASES literal in TeamVelocity.tsx:980`). Test scanner against its own output: known-clean component reports findings → rule too aggressive; known-broken component reports zero → too lenient. The compound rule (a + (c)i + (c)ii) achieved 0 false positives on 24-component codebase — target steady-state. **(d) TypeScript generic false-positive (`Promise<X>`):** Adding GitHub PAT flow to `Settings.tsx` UpdatesCard introduced inline type annotations like `{ api?: { ghPat?: { status: () => Promise<GhPatStatus> } } }`. Scanner immediately reported 5 findings, all on lines with `Promise<...>`. The JSX-text rule `/>([^<>{}\n]{4,})</g` matches the `> Promise<` sequence (closing `>` of arrow + space + `Promise` + opening `<` of generic). " Promise" is 8 chars, starts capital, no banned chars — flagged. Tried two fixes: (i) extract via `type Foo = Promise<X>` then refer to `Foo` — didn't help, type definition still has same shape; (ii) move type into sibling `src/lib/gh-pat-types.ts` file — scanner only walks `src/components/*.tsx`, this works immediately. Final fix: type hoist + `getGhPatApi()` helper in `gh-pat-types.ts`; `Settings.tsx` imports + calls; scanner runs clean. Why not "fix the scanner": intentionally simple (~140 lines, regex-only, no AST per design comment) — adding TS-generic awareness would require AST-based rewrite (multi-hour effort + new dependency) or heuristic to skip lines that LOOK like type annotations (regex tax + brittle). Hoist-to-types-file is one line per occurrence + zero scanner changes + matches existing project structure (`*-types.ts` files already exist). False positive cost is one type hoist; AST scanner rewrite is 4+ hours; lean toward "live with false positives + apply workaround." Group renderer-side API surface types into `src/lib/<feature>-types.ts` modules so component files stay JSX-focused and scanner stays clean. Pattern `getXxxApi(): XxxApi | undefined` lives well in the types module. DO NOT comment-out or `@ts-ignore` — the false positives are real lines doing real work; just relocate. DO NOT try to make scanner smarter unless investing in AST-based replacement. Document the false-positive class in scanner's header comment. **How to apply:** (1) Before adding ESLint mid-sprint, ask: "critical path to next ship gate, or post-ship discipline?" Mid-sprint = stopgap; post-ship = full setup. (2) Audit every `t('namespace.key')` call where English value contains non-letter visual prefix (emoji, glyph, arrow, symbol) — candidates for split. Type signature: `{ bg, text, emoji, labelKey }`, not `{ bg, text, label }`. (3) Track scanner output trends; add heuristics as false-positive classes emerge; document each rule with the example that triggered it. (4) For TypeScript-generic false positives, hoist types to non-`.tsx` files; group into `src/lib/<feature>-types.ts`. References: `D:\PMO_OS\scripts\check-hardcoded-strings.cjs` (~150 LOC) + `package.json check:strings` (commit `8919452` L070); `src/components/PortfolioStatus.tsx:55-67 STATUS_BADGE` refactor + `narrativeWhereShort/narrativeNextShort/narrativeRiskShort/narrativeNextStepShort` short-label keys + `en.json` + `ar.json` `portfolioStatus.*` namespace ~58 keys (commit `d3cd932` L069); `isLikelyEnglishUiString + isInsideTemplateLiteral` heuristics (commits `5c3a02d` L076 + `767d30d` L077); `src/lib/gh-pat-types.ts` (~30 LOC, NEW) `GhPatStatusShape, GhPatResult, GhPatMsg, GhPatApi, getGhPatApi()` (v2.0.8). (Folded former #666, #667, #672, #689.)

668. **A 5x cold-launch determinism test on a Vite-backed Electron app shows iteration 1 ~1000ms slower than iterations 2-5 (Vite first-compile populates `node_modules/.vite` cache); iterations 2-5 run within ±15ms of each other (steady state). When establishing a launch-time regression baseline, USE THE STEADY-STATE MEAN (iters 2-5), NOT the run mean (iters 1-5) — including iteration 1 inflates the baseline and lets cold-cache regressions hide. The variance signal across iters 2-5 (max - min, in ms) is the cleanest "build-system stability" metric — sub-50ms variance = deterministic; sustained >100ms variance = something nondeterministic in the boot path** — On 2026-05-06 running `scripts/test-cold-start.ps1` (5 iterations, kill-strays-then-spawn-dev:all-then-validate-all-19-tabs per iter) as the Day 9 cold-launch smoke gate. Result: iter 1 = 3594ms launch, iters 2-5 = 2543, 2554, 2544, 2546ms (range = 11ms, stddev ~5ms). Run mean = 2756ms; steady-state mean (iters 2-5) = 2547ms. Total per-iter time stable at ~35900ms (validator runs + tab inspection). All 5 iterations passed 19/19 tabs. The 1047ms gap between iter 1 and iters 2-5 is Vite's dependency pre-bundling — the first dev server spawn populates `node_modules/.vite/deps/` with esbuild-pre-bundled chunks; subsequent spawns reuse the cache. Reporting "mean launch time: 2756ms" understates how stable the system actually is and would let a future regression that adds ~250ms hide inside the iter-1 noise. The right baseline to lock in is "iter 2-5 mean: 2547ms ± 15ms" — any new iter-2 above 2700ms would be a regression worth investigating. **Why:** Cold-launch testing exists to detect regressions in startup time + module loading + IPC handler registration + tRPC bridge wiring. Vite's first-compile cost is real but it's a development-time artifact, not a production startup cost — production builds use pre-bundled `dist/` assets and don't have the Vite warmup gap. Including iter 1 in the baseline conflates two different things (dev-server warmup + actual app boot), and regressions in the second usually get masked by noise in the first. Steady-state-only baselines isolate the signal. The variance metric (max-min of iters 2-5) is the build-system health check: deterministic = good; jittery = something is racing or non-pure during boot (clock-dependent code, RNG, file-system cache effects). **How to apply:** (1) When establishing or reviewing a cold-launch baseline, ALWAYS report two numbers: run mean (iters 1-5) AND steady-state mean (iters 2-5) — and lock the regression alarm on the steady-state value. (2) Run at minimum 5 iterations — fewer than 4 makes the steady-state mean too sample-light to trust. (3) Track variance (max - min of iters 2-5) over time as a build-stability metric — sub-50ms is healthy; >100ms means investigate. (4) Iter 1 is the "dev environment warmup" number — useful as a developer-experience metric ("first launch of your day takes 3.6s") but not a production-stability metric. (5) The same pattern applies to ANY benchmark with cache layers: the first iteration measures cold-cache; iterations 2+ measure steady-state. Quote both, gate on the latter. (6) Reference: 2026-05-06 — `D:\PMO_OS\scripts\test-cold-start.ps1` 5-iteration loop; `D:\PMO_OS\cold-start-summary.log` results: iter 1 = 3594ms, iters 2-5 = 2543/2554/2544/2546ms, all 19/19 tabs pass each iter. Steady-state baseline: 2547ms ± 15ms.

760. **Pinned-version dependency traps in Electron main process — two rules: (a) electron-store v9+ is ESM-only, pin to `^8.2.0` for any CommonJS-compiled Electron main (also affects chalk 5+, node-fetch 3+, ora 7+, p-limit 4+, p-queue 7+); (b) NEVER use `path.join(__dirname, '..', 'dist', 'index.html')` to reach renderer assets in production — `__dirname` after compilation is at `dist-electron/electron/` not the project root, single-level `..` lands inside `dist-electron/` not at the asar root; use `path.join(app.getAppPath(), 'dist', 'index.html')` instead** — Two patterns from May 6 PMO_OS v2.0.0-v2.0.4 build/install scripting. **(a) electron-store ESM-only after v9:** v8.2.0 is the last CJS release and has every feature most apps need (typed schema, atomic writes, defaults, `clearInvalidConfig`); v9+ requires top-level await + ESM main, which most production Electron apps haven't migrated to. May 6 implementing Roadmap v2.2 #23 (L072): initial `npm install electron-store --save` pulled v11. Electron main is CommonJS-compiled (`module: "CommonJS"` in `electron/tsconfig.json`); `require('electron-store')` would fail at runtime with `ERR_REQUIRE_ESM`. Fix: `npm install electron-store@8`. v8 has same surface API (typed Schema, defaults, clearInvalidConfig, get/set/clear) — no code changes beyond version pin. Used at `electron/lib/user-config.ts` for theme/language/updateChannel/lastSeenVersion. **Why:** Many JS libs went ESM-only in 2023-2025 (chalk, node-fetch, ora, electron-store, p-limit, p-queue). Electron's main-process build pipeline still defaults to CommonJS because better-sqlite3, ffi-napi, and many native modules don't ship ESM bindings. The mismatch surfaces only when renderer is fine but `dist-electron/electron/main.js` throws at boot. **Rules:** Before `npm install`-ing any new dep into Electron main, check lib's CHANGELOG for "BREAKING: ESM-only". Common cutoff versions: chalk 5+, node-fetch 3+, electron-store 9+, ora 7+, p-limit 4+, p-queue 7+. Pin to the last CJS release with a caret range (`^8.2.0`) — newer 8.x patches stay safe, 9.x bumps are blocked. Detection trick: if `require()` works in dev (Vite handles ESM transparently) but `dist-electron/electron/main.js` fails post-build with `ERR_REQUIRE_ESM`, it's almost always this. Long-term path forward: migrate Electron main to ESM only when ALL native deps + electron-builder support it cleanly (probably Electron 32+). **(b) `app.getAppPath()` for in-asar files:** May 6 evening user installed `PMO_OS_Setup_v2.0.0.exe` and reported blank navy window with PMO OS title bar. Task Manager showed 4 processes alive (~200 MB RAM combined) — renderer running, just not painting. Root cause: `electron/main.ts:120` had `mainWindow.loadFile(path.join(__dirname, '..', 'dist', 'index.html'))`. After `tsc -p electron/tsconfig.json` compiles main.ts → `dist-electron/electron/main.js`, `__dirname` at runtime equals `dist-electron/electron/`. Single `..` resolves to `dist-electron/`, then `+ /dist/index.html` = `dist-electron/dist/index.html` which DOES NOT EXIST. The actual layout inside `app.asar` is `app.asar/dist/index.html` (Vite output) and `app.asar/dist-electron/electron/main.js` (TS output) — separate top-level folders. To reach `dist/` from main.js, you'd need TWO levels up — or cleaner: use Electron's API: `path.join(app.getAppPath(), 'dist', 'index.html')` which always returns the asar root regardless of compilation layout. **Why dev mode hides the bug:** in dev, `loadURL('http://localhost:5173')` fires instead of `loadFile`, so path bug never executes. **Why production silently fails:** `loadFile` with non-existent path causes Electron to load `chrome-error://` page. The `backgroundColor: '#1a1a2e'` (navy) configured on BrowserWindow paints first → chrome error page would normally overlay it, but in unsigned production builds with `webContents.openDevTools` gated by `!isDev`, the error page is invisible. Result: navy void with window title. Looks like hung renderer. **Rules:** NEVER use relative `..` paths to reach `dist/` from `main.js` in Electron production. The compile output structure (`dist-electron/electron/`) makes the relative offset non-obvious. ALWAYS use `app.getAppPath()` for renderer assets — single API that points at asar root regardless of compilation layout. For other resources (extraResources, Drizzle migrations, fixtures), use `process.resourcesPath/...` — that points OUTSIDE asar to unpacked resources folder. When debugging "blank window" in Electron production, FIRST check URL the renderer tried to load: enable production DevTools temporarily via `mainWindow.webContents.openDevTools()` unconditionally + rebuild + check console for actual error. Test production build locally with `electron-builder --dir` (creates `release/win-unpacked/`) BEFORE committing .exe build flow — catches this class of bug before NSIS packaging adds 5 minutes per iteration. Generalizes: `app.getAppPath()` is preferred over relative paths for ANY in-asar file access. **How to apply:** (1) Before adding any dep to Electron main, check CHANGELOG for ESM-only breaking change; pin to last CJS major. (2) NEVER use `__dirname/..` for in-asar paths; use `app.getAppPath()`. (3) For unpacked resources, use `process.resourcesPath`. (4) Test production build with `--dir` BEFORE NSIS packaging. References: `D:\PMO_OS\package.json` electron-store@^8.2.0 + `electron/lib/user-config.ts` Store<UserConfigSchema> wrapper (commit `eb4293d` L072); `D:\PMO_OS\electron\main.ts:120` fix from `path.join(__dirname, '..', 'dist', 'index.html')` to `path.join(app.getAppPath(), 'dist', 'index.html')` (commit L084 in L084-L086 bundle). (Folded former #669, #676.)

753. **electron-updater hardening end-to-end — four rules covering provider choice (generic vs github), private-repo authentication, and unsigned-binary verification: (a) `generic` provider with `file:///` OneDrive URL for private/single-user (no GitHub, no CDN, no signing infrastructure); (b) `github` provider with CI workflow on `v*` tags is strictly better when repo policy allows public Releases — three coupled config sites must change in lockstep; (c) when the GitHub repo is private, ship a per-user safeStorage-encrypted `gh-pat` token store (clone of the DPAPI PAT-store pattern), wire updater.ts to read at boot; (d) for unsigned distributions, SHA-512 verify the local-built .exe against the CI-published `latest.yml` before install — replaces the missing Authenticode signature ceremony** — Patterns from May 6 PMO_OS v2.0.0-v2.0.8 release-engineering work (commits L073, L086, L090). **(a) Generic provider for private/single-user (L073):** Configure electron-builder with `publish: { provider: generic, url: file:///C:/Users/DELL/OneDrive%20-%20Taqat/PMO_OS_releases/stable/, channel: stable }`. Renderer-side updater wraps autoUpdater with the same URL via `electron/lib/updater.ts resolveFeedUrl()` so build + runtime stay in sync. `electron-builder --publish always` writes `.exe` + `latest.yml` to URL; clients poll hourly via electron-updater's generic provider; OneDrive sync handles cross-device distribution. Wrap URL in `${env.PMO_OS_UPDATE_FEED:-default}` so tenant URL isn't baked into source. Use `file:///` for OneDrive/shared-drive; `https://` for self-hosted nginx/S3/R2. For unsigned builds: `forceCodeSigning: false` in electron-builder config — auto-update still works, Windows shows SmartScreen warning on first install. Why: GitHub Releases is the obvious default for public repos but creates friction for private ones (token management, repo-scope, public-vs-private release timing). OneDrive is already mounted, already syncs cross-device, already has access controls aligned with the user's identity, costs $0 extra. **(b) GitHub provider when repo can be public (L086):** Three coupled config sites: (i) `electron-builder.yml publish:` block (controls `--publish always` at build time); (ii) `electron/lib/updater.ts setFeedURL()` (controls what installed clients poll); (iii) new `.github/workflows/release.yml` triggered on `v*` tag pushes (Windows runner: setup Node + Python, run setup-python-runtime, build, publish). Workflow `permissions: { contents: write }` is mandatory; without it, electron-builder fails 403 on Releases API. `GITHUB_TOKEN` provided to workflow runs has `contents: write` — sufficient for public repos, no PAT secrets needed. Pin action versions (`actions/checkout@v4`, `actions/setup-node@v4`) but track upstream deprecations. `windows-latest` runners come with Developer Mode ENABLED by default (sidesteps lesson #765's winCodeSign symlink-extraction privilege requirement). Don't try to support BOTH providers simultaneously via env-var switching — pick one per channel and document. First tag push (`git tag v2.0.1 && git push origin v2.0.1`) triggered the workflow which completed in 5m19s. **Why GitHub provider strictly better when allowed:** zero manual publish step; tags are immutable/auditable source of truth; UI shows release notes/sizes/download counts; first-class case in electron-updater (no auth complications for public); clients trust GitHub TLS chain (no self-signed cert workarounds); blockmap delta updates work transparently. **Why generic NOT obsolete:** still right when repo is private + no CI access, air-gapped + file:// URL, experimental branch builds, one-machine personal projects with no CI budget. **(c) Private-repo authentication via DPAPI gh-pat (L090):** electron-updater's GitHub provider accepts an optional `token` field in `setFeedURL`. When repo is private, ship a per-user safeStorage-encrypted token store and read on app start. NEVER bake token into .exe (rotation pain + secret in binary); NEVER make user re-paste on every check (they give up). Right pattern: clone existing PAT-store (DPAPI-backed) for second slot called `gh-pat`. Implementation: (i) `electron/lib/gh-pat-store.ts` — clone of pat-store.ts but writes to `gh-pat.bin` next to userData, same DPAPI/safeStorage path, plain-text fallback when encryption unavailable. (ii) `electron/ipc/devops-handlers.ts` — three new IPC handlers `gh-pat:set/get/clear` mirroring `pat:*` shape. The `get` handler returns `{hasPat, encryptionAvailable, storageMode}` — never the value itself, even over IPC, to avoid leaking via DevTools network panel. (iii) `electron/preload.ts` — `window.api.ghPat.{set,get,clear}` bridge. (iv) `electron/lib/updater.ts` — at top of `setupAutoUpdater()`, lazy-`require('./gh-pat-store').readGhPat()` (must be after `app.whenReady` so safeStorage is initialized). If token found, pass `token: ghToken` into `setFeedURL`; if absent, fall through to anonymous polls. (v) `src/components/Settings.tsx UpdatesCard` — when `status.state === 'error'` AND `/(404|authentication|auth)/i.test(status.error)`, render a yellow explainer block with `<input type="password">` for pasting PAT + Save/Clear/Hint copy. On save, call `window.api.ghPat.set(value)` + advise restart (electron-updater caches feed config — restart hint is critical). 5 new EN+AR i18n keys under `settings.updater.{authNeededTitle,authNeededBody,patField,savePatBtn,clearPatBtn,patHint,patSaved,patCleared,patAlreadySaved}`. The 404-on-anonymous-poll-of-private-repo error rendered raw with no recovery path is what surfaced this — Mahmoud installed v2.0.7, saw `Error: 404 method: GET url: https://github.com/melshahedpearl/PMO_OS/releases.atom`; root cause was prior `pmo_os_repo_private_no_push.md` memory was stale (gh repo view --json visibility confirmed isPrivate: true). **(d) SHA-512 verify before install (L091):** For unsigned .exe distribution (no $300/yr code-signing cert), verify byte-for-byte that local-built binary matches CI-published artifact BEFORE running on your own machine. Hash command: `Get-FileHash -Algorithm SHA512 'path\to.exe' | Select -ExpandProperty Hash` (PowerShell). Compare against CI's `latest.yml sha512:` field — but note that field is base64-encoded, decode with `echo "<base64>" | base64 -d | xxd -p -c 256 | tr 'a-f' 'A-F'` (bash; the `tr` lower→upper matches PowerShell's uppercase output). Match = same source, no tampering. Mismatch = rebuild or download fresh from CI (`gh release download v<X> --pattern "*.exe"`). **Why hash-verify matters even on your own machine:** (a) supply-chain attacks against npm packages can inject malicious code at build time — local build differing from CI's clean-room build is signal; (b) AV / endpoint-protection software occasionally modifies binaries silently (signing-cert injection, DLL hooking) — hash mismatch reveals; (c) interrupted builds can produce corrupt .exe that "looks fine" but crashes at install; hash mismatch catches before installation; (d) cost is 5 seconds of typing vs hours of "why won't it install" debugging. Signed binaries have an Authenticode chain-of-trust ceremony that catches tampering automatically; unsigned binaries have NO automatic check — SHA verify replaces the missing signature ceremony. For continuous trust: write `verify-build.ps1` that downloads CI's latest.yml, hashes local `release/*.exe`, compares. Run before every install. **How to apply:** (1) When repo private + small audience (≤ team size 50), default to `generic` provider; when public, default to `github` provider. (2) Three coupled config sites must change in lockstep — keep them in sync via comment cross-reference at each. (3) NEVER bake credentials into source or shipped binaries; reuse existing credential-store patterns (DPAPI PAT-store) rather than inventing a second one. (4) Keep tokens out of IPC GET responses — only return existence flags + storage mode. (5) When the network error UX is on a Settings card, replace cryptic provider-level errors with friendly explainer + recovery action gated on regex-detection of failure mode. (6) Ship "Restart required" hint in success message — electron-updater's `setFeedURL` is called once per app boot; runtime token changes don't propagate. (7) Whenever you build a binary locally that you intend to install or distribute, hash it + the CI artifact, compare. DO NOT skip this for "trivial" releases — supply-chain attacks specifically target moments developers feel safe. References: `D:\PMO_OS\electron-builder.yml` publish block + `D:\PMO_OS\electron\lib\updater.ts setupAutoUpdater + setFeedURL` + `D:\PMO_OS\src\lib\updater-helpers.ts resolveFeedUrl` (commits `f672187` L073 + `ad2e07c` L082); `.github/workflows/release.yml` (~75 lines, first run 25429228362 5m19s); `electron/lib/gh-pat-store.ts` (~120 LOC) + 3 IPC handlers + preload bridge + Settings.tsx friendly UX (v2.0.8); SHA-512 verification of v2.0.7 .exe (107 MB) byte-identical to CI artifact (`Get-FileHash` returned `35A445D2B71DCC74FE464E211096AB73108D50F610F151BDD88B14991FBA5D52DF13A3CD3E02AFD1FA732FD666C5D98606989C05E09A909A5535F188AC11F8C0`). (Folded former #670, #678, #686, #693.)

671. **When a JS-only npm lib lacks bundled .d.ts files AND no `@types/*` package exists, prefer a typed factory shim via `require()` + cast over writing an ambient `declare module` file. The cast localizes the type loss to the import line; the rest of the file keeps full type safety. Pino's `Level` type also illustrates a related pattern: when a lib's union type excludes a value you legitimately want to use (e.g. `'silent'`), narrow at the boundary rather than widening internally** — On 2026-05-06 implementing Roadmap v2.2 #22 (L074). `pino-roll@4` ships JS-only with no bundled .d.ts. Naive `import pinoRoll from 'pino-roll'` failed `tsc` with `TS7016: Could not find a declaration file for module 'pino-roll'`. Two reasonable fixes: (a) create `pino-roll.d.ts` with `declare module 'pino-roll'; export default function ...` — drags type debt into the project's typings folder + creates a maintenance burden when pino-roll ships v5. (b) Inline factory shim: `const pinoRoll = require('pino-roll') as (opts: Record<string, unknown>) => Promise<NodeJS.WritableStream>;`. Picked (b). Cast says exactly what we use the function for; the rest of `logger.ts` is fully typed. Same file also hit pino's `Level` type excluding `'silent'` (it's a special opt-out value passed separately to `pino({ level: 'silent' })` but not assignable to multistream entries). Fix: narrow our internal `LogLevel` → `PinoLevel` at the boundary, treating `'silent'` as `'fatal'` since both effectively disable record emission below the threshold. **Why:** Type debt accumulates across .d.ts files. A 200-line ambient module declaration that's used in one place is worse than a one-line cast that's used in one place — both lose the same type safety, but the cast is colocated and self-documenting. Same logic for boundary-narrowing: when a lib's enum is stricter than your domain enum, convert at the boundary instead of forcing your code to match the lib's vocabulary. **How to apply:** (1) Before writing a `declare module` ambient, check if the cast localization is sufficient. Single-call libs (factory functions, simple loggers) → use the cast. Multi-call API surfaces (full SDKs) → ambient module is justified. (2) Cast shape: name + arg type + return type, nothing more. `(opts: Record<string, unknown>) => Promise<...>` is enough — don't over-spec. (3) When typing the boundary, prefer narrowing OUR types to match the LIB's stricter type. Widening the lib's type via `as Level | 'silent'` is brittle — the lib's typings change, your widening breaks silently. (4) Document the narrowing decision inline (`// 'silent' becomes 'fatal' — both disable record emission below threshold`) so future readers don't think it's a bug. (5) Reference: 2026-05-06 — `D:\PMO_OS\electron\lib\logger.ts:32-35` factory shim + line 88 `'silent' → 'fatal'` narrow. Commit `4e069d1` L074.

752. **First-launch onboarding patterns for desktop apps that send email or contact external humans — three rules: (a) Setup Wizard auto-redirect via a sticky boolean + useEffect on a userConfig flag (NOT unconditional render-time redirect, which traps the user); (b) prefer runtime-derive over static JSON snapshots for onboarding starter data when the dataset is mutable; (c) 4-layer opt-in defenses for email-send features so a fresh install can't dispatch by accident** — Three patterns from May 6 PMO_OS v2.0.7 Setup Wizard build (commits L081-L083, post-L001 P0 generalization). **(a) Setup Wizard auto-redirect:** Pair the gate-reading useEffect with a sticky local boolean: `useEffect(() => { if (!setupComplete && !wizardRedirected) { setActiveTab('setup-wizard'); setWizardRedirected(true); } }, [setupComplete, wizardRedirected])`. Fires exactly once per `setupComplete=false` transition. Second branch resets the boolean when setupComplete flips back to true → "Re-run setup" link in Settings re-triggers the redirect. This lets the user freely navigate mid-wizard (Settings, Portfolio Status, etc.) without being scroll-jacked back. First draft used unconditional `useEffect(() => { if (!setupComplete) setActiveTab('setup-wizard') }, [setupComplete])` — every navigation triggered the effect again, looked like the app was broken. Rules: (1) store the gate in user config (electron-store / equivalent), NOT in component state, so it survives restarts; (2) pair gate-read with sticky local boolean preventing repeated redirects within a single mount; (3) reset boolean on false→true transition for re-run capability; (4) DO NOT use a modal overlay that blocks the underlying app — modals feel coercive; a dedicated tab the user can navigate away from feels respectful; (5) make the wizard tab `hidden: true` in sidebar NAV so it doesn't appear after `setupComplete=true` (only reachable via auto-redirect or "Re-run setup" link); (6) the "Re-run setup" link in Settings → User Config card writes `userConfig.set({ setupComplete: false })` — same hardened path the wizard's Done step uses. **(b) Derive starter data at runtime, not static snapshots:** Setup Wizard Step 4 (IC roster) had two valid implementations: (i) bundle `fixtures/ic-roster-prefilled.json` (21 hand-curated non-PM members) and ship in installer; (ii) derive at runtime from `team_roster` SQLite table via `trpc.teamRoster.list({ status: 'active' })` filtered by `team_code !== 'pm'`. DERIVED is strictly better — self-updating: when Afthab joins QA-QC, when Ayush joins DevOps, when a former member is marked inactive, the wizard reflects current truth without rebuilding static JSON. Static file stays as documented fallback for users who can't / don't want to run the wizard; both routes converge on the same JSON schema (`parseRoster()` validates either source). Pure helpers that do the derivation (filter PMs, drop alias rows, drop invalid emails, map team_code → team slug, sort by team+name) belong in `src/lib/` so they're trivially unit-testable without the DB. Test the derivation helper with synthetic team_roster rows covering PM-filter, alias-filter, email-validation, team-code-mapping, stable sorting, empty-input. Generalizes: any "starter data" flow has the same fork — static JSON wins on no-DB-needed + reproducibility; runtime-derived wins on freshness + zero-maintenance. When you have BOTH a DB and a static file shipping in the installer, prefer derive — the DB IS the source of truth, the static file is documentation. **(c) 4-layer email opt-in defenses (L001 P0 incident pattern):** May 4 incident: Claude auto-wrote `recipient: info@pearlpixels.com` to `briefing-config.json` based on a wrong assumption that CLAUDE.md's `userEmail` value was Mahmoud's personal address. Three test briefings were sent to a global company inbox before catch. The 4-layer fix generalized for the IC weekly digest (Sun 18:00 → 19 ICs): **(1)** `%APPDATA%/<App>/ic-roster.json` is NOT shipped — only `fixtures/ic-roster-template.json`; user must manually copy. No file → `runIcEmailDispatch` returns `{ ok:false, error: 'roster not found' }`. **(2)** Template's `enabled: false` default — even after user copies + fills, dispatch silently skips. **(3)** `parseRoster()` validator REJECTS rows where `name.startsWith('TODO ')` OR `/^ic\d+@/.test(email)` — user must replace every placeholder before validator clears. **(4)** Settings UI Dry-run button renders per-IC HTML + subject for review WITHOUT touching Outlook COM. Send-Now button stays disabled until `enabled:true` AND `status.rosterOk`. ConfirmDialog before Send-Now fires. **Why structural defenses beat process:** "Looks reasonable defaults" is how L001 happened — `info@pearlpixels.com` looked like a personal email; Claude inferred wrong; emails went out. The defense isn't "be careful" (process fails); the defense is structural — every layer would have to fail simultaneously for an unreviewed email to go out. (1)+(2) means the roster doesn't even exist on a fresh install. (3) means filling-but-not-reviewing fails fast. (4) means flipping enabled-but-not-dry-running gives the user a Send-Now-only path which they have to consciously click. **How to apply:** (1) For any "show this surface once on first launch" pattern, store the gate in user config + use sticky boolean + reset on false→true transition for re-runs. (2) For onboarding flows that prefill from existing data, prefer runtime-derive over static JSON snapshot if the dataset is mutable; keep static file as documented fallback. (3) ANY new send-email feature gets the 4-layer treatment from day one — don't bolt on after an incident. Template files SHIP via extraResources or fixtures/; user-data files NEVER ship. Validators reject template placeholders explicitly via regex. Dry-run preview is mandatory UX. Send buttons stay disabled until BOTH enabled-flag AND validator-clean. Document the 4 layers in a walkthrough doc next to the feature. Same pattern applies to: SMS dispatch, Slack messages, Teams messages, calendar invites — anything that contacts external humans at scale. References: `D:\PMO_OS\src\App.tsx` AppShell hooks (~L82-95) + `src/components/Settings.tsx UserConfigCard` "Re-run setup wizard" (v2.0.7); `src/lib/setup-wizard-helpers.ts buildIcRosterRows() + teamCodeToSlug()` (~80 LOC, 26 unit tests, v2.0.7); `fixtures/ic-roster-template.json` + `src/lib/ic-email-helpers.ts parseRoster` (placeholder rejector) + `electron/jobs/ic-emails.ts runIcEmailDispatch` + `src/components/Settings.tsx IcEmailsCard` + `docs/IC_Email_Setup_Walkthrough.md` (5-step opt-in walkthrough). Commits `f505eca` L081 + walkthrough at `ad2e07c` L082 (generalizes L001 incident response). (Folded former #673, #684, #685.)

758. **Living-doc fork-vs-edit + multi-PR audit doc as deliverable — two rules for documentation that survives multi-week refactors and execution-velocity drift: (a) when >50% of phase milestones are DONE (or projected-vs-actual elapsed-time ratio exceeds 4×), fork the doc to `_vNext.md` with a `Supersedes:` header instead of editing in-place — preserves audit trail of which decisions held vs which were made later; (b) for multi-PR / multi-component refactors spanning ≥5 components across ≥2 PRs, ship a top-level audit doc (`docs/<feature>_Migration_Audit.md`) as a first-class deliverable with one row per component, ✅/⚠/✗ status, L#/version closing each row, "Last vulnerable" + "Resolved in vN" running summary** — Two patterns from May 6 PMO_OS documentation discipline. **(a) Fork-don't-edit for living docs:** May 6: user asked to confirm `PMO_Operating_System_Roadmap_v2.md` was latest plan. RAG surfaced the doc dated May 3, internally tagged v2.2 — written when Phase A was projected Mon May 18 and Phase F to ship Mon Aug 24 (16 weeks). Reality: Phases A→F all shipped May 3-6 (4 calendar days). 11 of 11 Roadmap v2.2 residuals closed, bilingual scanner 54→0, 440 tests passing, ship-gate prep complete — all 16 weeks of architecture work. Editing v2.2 in-place would have lost original's audit trail (which decisions held vs which were made later) and made now-irrelevant timeline blur with still-relevant decisions. Created `PMO_Operating_System_Roadmap_v2.3.md` (279 lines) with: explicit `Supersedes: PMO_Operating_System_Roadmap_v2.md` header; section 1 residuals-closed table with commit hashes per item (`eb4293d` for #23, `f672187` for #14, etc.); section 2 explicit "what's left for May 17/18" Mahmoud-driven items; updated risk catalog (8 v2.2 risks retired + 5 new post-build risks); updated decision log (7 v2.2 resolved + D8-D11 added); section 7 post-May-18 deferred backlog. Predecessor v2.2 stays untouched. **Why:** Living docs edited in-place lose their audit trail. A reader six months later sees only current state and can't reconstruct which decisions were made when, which risks materialized vs which were retired, which phases compressed vs which actually took projected time. Fresh-versions preserve history at cost of one extra file. Detection rule: when reading a phased plan, count DONE vs TODO milestones. >50% DONE = candidate for fork; >80% DONE = mandatory fork. Editing in-place is fine for typo fixes and small clarifications; structural drift requires new file. Fresh-version naming: bump patch level (`_v2.md` → `_v2.3.md`), NOT major version unless architecture changed. Required header lines: `**Status:**` (date + tag), `**Supersedes:**` (predecessor filename), one-paragraph "Why vNext exists" section answering "what changed since the predecessor was written." Preserve commit hashes inline for every closed residual or shipped phase — gives reader one-click path to actual diff. Example: `| #14 | electron-updater wired | NOT BUILT | ✅ DONE | f672187 (L073) |`. Predecessor file NEVER deleted — stays in same `_docs/` folder, referenced from new doc's header; future readers trace lineage. Update project memory file (e.g., `pmo_v2_electron_parallel_build_may3.md`) to point at new doc as canonical, with note that predecessor is historical. Same pattern applies to: ADRs (write a new ADR rather than editing the old one when context shifts), bi-weekly retros (write new retro doc rather than appending), migration plans, runbooks. Anti-pattern: single doc with "Last updated: <date>" + revision-tracker table at top — the table grows unbounded, readers skim past it, in-line content drifts from table. Better to fork. **(b) Multi-PR audit doc as deliverable:** May 6 v2.0.4 → v2.0.6 tRPC migration of 19 tabs in PMO_OS landed across 3 PRs (Calendar.tsx in v2.0.4 referencing lesson #657, Meetings.tsx in v2.0.4 as L088, OutlookCategories.tsx in v2.0.6 as L093). Without audit doc, "what's still vulnerable?" required grepping every component for `useState<*State>({ state: 'idle' })` patterns. With the doc — `D:\PMO_OS\docs\Tab_tRPC_Migration_Audit.md` — the answer was always one read away: 19-row table marking each tab ✅ or ⚠. The doc stayed up-to-date because every PR that migrated a tab also bumped its row from ⚠ to ✅ + added L# reference. When OutlookCategories.tsx migrated in v2.0.6, the doc closed: "All 19/19 tabs on the tRPC + React Query pattern. Audit RESOLVED in v2.0.6." **Why this beats just having good commit messages:** (i) commit messages are linear in time and require git-log-archaeology to reconstruct status; audit doc is a snapshot of CURRENT state at HEAD; (ii) commit messages discuss WHAT changed, not what's STILL OUTSTANDING — audit doc inverts that: shows what's outstanding; (iii) commit messages don't have a checkbox UX; audit doc with ✅/⚠ symbols is scannable in 5 seconds; (iv) the doc forces the migrating engineer to think holistically every PR — "what other tabs use this same pattern? are they vulnerable too?" — catches gaps the linear PR-by-PR view misses. **When to NOT bother:** single-PR refactors, atomic changes that finish in one commit, changes touching <3 files. The overhead is real (5 min to write initial table, ~30 sec per PR to update). Worth it only when N >= 5 components across >= 2 PRs. **How to apply:** (1) When reading any phased plan, count DONE vs TODO; >50% DONE → fork; >80% DONE → mandatory fork. (2) When starting multi-PR refactor with ≥5 components, write audit doc on PR1 with all rows marked ⚠. (3) Each subsequent PR bumps row(s) to ✅ + adds L#/lesson reference; bottom line shows running "X/N migrated, Last vulnerable: <component>". When N=N, bump bottom line to "Audit RESOLVED in v<X>" + closing date; don't delete file. (4) Link audit doc from kickoff PR description so reviewers find it immediately. (5) Audit doc + lesson capture pair well: when you close the audit, also write a numbered lesson about the migration pattern itself (e.g., lesson #751's compatibility-shim was extracted FROM the closed Tab_tRPC_Migration_Audit.md). References: `D:\Project Management\Project Management Office\Dashboards\PM Handling\_docs\PMO_Operating_System_Roadmap_v2.3.md` (279 lines, fresh) + `_v2.md` + `_v1.md` (kept as historical) — commit `f979a24` (pattern previously applied: `_v1.md` Apr 30 file:// HTML stack era → `_v2.md` May 3 Electron pull-forward); `D:\PMO_OS\docs\Tab_tRPC_Migration_Audit.md` (born in v2.0.4 batch documentation, RESOLVED in v2.0.6 commit `2e91ab2`, 19 tabs covered). (Folded former #674, #694.)

748. **Embeddable Python is the right way to ship Python sidecars in Electron — NOT bundling the existing dev venv, NOT requiring users to install Python first, NOT PyInstaller. The 6-step recipe: (1) download the official `python-X.Y.Z-embed-amd64.zip` from python.org; (2) extract to `python-runtime/` next to your sidecar source; (3) patch `python*._pth` to enable `import site` (commented out by default — the embeddable distro IS isolated until you flip this); (4) bootstrap pip via `get-pip.py`; (5) `python.exe -m pip install -r sidecar/requirements.txt`; (6) ship as electron-builder `extraResources` (so it lives outside the asar; Python can't `fopen` files inside asar). Total adds ~50-80 MB to installer, fully self-contained, works on any Windows machine without prerequisites** — On 2026-05-06 evening implementing Roadmap v2.3 ship-fix L085. The earlier v2.0.0 build (81 MB) shipped without Python — meaning installed clients couldn't run the sidecar (Outlook COM, Calendar COM, Word COM, send-email) unless they happened to have Python + the dev venv. Considered three options: (A) embeddable Python, (B) PyInstaller-compiled `sidecar.exe`, (C) bundle the dev `.venv/`. Picked A. **Why NOT venv:** venvs are bound to the absolute path they were created at; copying breaks shebang lines + activate scripts. They include the full Python install (~3x size of embeddable). Layout is unpredictable for downstream filtering. **Why NOT PyInstaller:** adds a build-time dependency, requires maintaining a `.spec` file, debugging is harder when Python errors get swallowed by the bundling layer, doesn't compose well with electron-builder. **Why embeddable wins:** designed exactly for this case (Python's "Embedded distribution" is the official cross-language-embedding answer); minimal (~10 MB base + ~70 MB for our 4 deps); standard layout makes filtering predictable; sidecar code stays plain `.py` files (debuggable, hot-editable). The patched `_pth` is the non-obvious step — embeddable Python ships with `import site` commented out so it's isolated from system site-packages, but that ALSO blocks pip-installed local packages until you uncomment the line. The pywin32 post-install script must run too (`Scripts/pywin32_postinstall.py -install`) to register COM DLLs locally. **How to apply:** (1) Always pick embeddable Python over venv copy or PyInstaller for production sidecar bundling. (2) Write the setup as a one-shot PowerShell/bash script (`scripts/setup-python-runtime.ps1`) — idempotent (skip when validator passes), produces a `python-runtime/` folder next to your sidecar source. (3) Always patch `_pth` to enable site-packages — without this, your installed packages are invisible. (4) Always run `pywin32_postinstall.py -install` if pywin32 is in requirements. (5) Validate with an import test before declaring success — pip can succeed but DLLs may still fail to register. (6) Add `python-runtime/` to `.gitignore` — regenerated locally + in CI; never committed. (7) Add `electron-builder.yml extraResources: [{from: python-runtime, to: python-runtime}]` so it ships OUTSIDE the asar (asar is opaque to Python's C-level fopen). (8) Add `extraResources: [{from: sidecar, to: sidecar, filter: ['**/*.py', '!.venv', '!**/__pycache__']}]` so the sidecar source itself ships unrelated to the runtime. (9) In the launcher, use `process.resourcesPath/python-runtime/python.exe` + `process.resourcesPath/sidecar/main.py` for production; keep the dev path (`sidecar/.venv/Scripts/python.exe`) for `!app.isPackaged`. (10) Wire setup into `prebuild` npm script + CI workflow so every fresh checkout produces a working build. (11) Reference: 2026-05-06 — `D:\PMO_OS\scripts\setup-python-runtime.ps1` (~110 lines, idempotent) + `D:\PMO_OS\electron-builder.yml` extraResources entries + `D:\PMO_OS\electron\sidecar\launcher.ts` resolveSidecarCommand + `.github/workflows/release.yml` `npm run setup:python` step. Commits `L084-L086` bundle. (Promoted from former #675; cluster fold with #671 cancelled — topic mismatch since #671 covers TypeScript declaration files, not Python. #671 stays at its original number.)

757. **Cross-machine PowerShell + Task Scheduler discipline — two rules for shipping scripts that ship with apps targeting Windows users at scale: (a) default-target PS 5.1 dialect (Windows PowerShell, ships with every install) — PS 7+ (PSCore) is opt-in install most users don't have; (b) when registering Task Scheduler tasks that touch user-level resources (Outlook COM, keychain, clipboard, user-mode named pipes), use `RunLevel Limited` not `Highest`/`Admin` — Windows COM cannot bridge integrity boundaries (extends lesson #761)** — Two patterns from May 6 PMO_OS v2.0.3+ build/install scripting. **(a) PS 5.1 dialect:** PowerShell 5.1 is NOT the same language as PowerShell 7+. Major syntax differences: (i) `Join-Path` in PS 5.1 takes only TWO arguments — multi-segment path joins must use `[System.IO.Path]::Combine()` or chained `Join-Path` calls; (ii) null-coalescing `??` and ternary `?:` are PS 7+ only; (iii) `&` operator + `2>&1` redirection has parsing quirks in PS 5.1 — use `Start-Process -Wait -PassThru -RedirectStandardOutput`/`-RedirectStandardError` for capturing output reliably. May 6 evening: first three runs of `setup-python-runtime.ps1` failed in succession on PS 5.1 (Windows 10/11 default) with three distinct errors: (1) `Join-Path $repoRoot 'sidecar' 'requirements.txt'` → "A positional parameter cannot be found that accepts argument 'requirements.txt'" (PS 5.1's Join-Path is binary); (2) `(Get-Content ...) ?? ''` → "Unexpected token '??' in expression or statement"; (3) `& $pyExe -c "..." 2>&1` → "The expression after '&' in a pipeline element produced an object that was not valid". Each works fine in PS 7+ where script was originally drafted. Fix: rewrite to PS 5.1-safe equivalents — `[System.IO.Path]::Combine($a, $b, $c)` for multi-arg path joins, explicit `if ($null -eq $x) { $x = '' }` instead of `??`, `Start-Process -RedirectStandardOutput/-RedirectStandardError` to capture stdout/stderr separately into log files. PS 7+ is opt-in install on Windows; most users (and most legacy CI systems) have PS 5.1 only. Build scripts that fail on PS 5.1 are unusable for typical user. Detecting version is doable (`$PSVersionTable.PSVersion`) but writing for the lowest common denominator is simpler. NEVER use `??`/`?:` — substitute with `if/else`. NEVER pass more than 2 args to `Join-Path` — use `[System.IO.Path]::Combine` or chain. Avoid `param($Args)` — `$Args` is an automatic variable; use `$ArgString` or similar. Test on PS 5.1 explicitly: `powershell.exe -ExecutionPolicy Bypass -NoProfile -File script.ps1` (NOT `pwsh.exe` which is PS 7+). Use `#requires -Version 5.1` at the top to make version expectations explicit. Don't trust IDE linters that target PS 7+ — they'll show the script as clean while PS 5.1 chokes on the same code. **(b) Task Scheduler `RunLevel Limited` for user-resource tasks:** May 6 building `Install-PMO-Server-Autostart.ps1` for v2.0.3's PS-server auto-launch (so localhost:5050 PowerShell server backing PMO_OS Outlook Categories + v14 Deep Search starts on every user logon). First instinct: register with `RunLevel Highest` for "more privileges." That would have broken Outlook COM access — exactly the failure mode lesson #761 documented (HRESULT 0x80080005 / CO_E_SERVER_EXEC_FAILURE because user-mode Outlook server can't accept connections from elevated client). Correct pattern: `New-ScheduledTaskPrincipal -UserId $env:USERNAME -LogonType Interactive -RunLevel Limited` + `New-ScheduledTaskTrigger -AtLogOn -User $env:USERNAME` (NOT `-AtStartup`, because boot fires before user session has Outlook available). Action launches via `cmd.exe /c start "PMO Server" /min "$ServerScriptPath"` — `cmd /c start` lets .bat run + keep its embedded PowerShell window alive (`powershell.exe -NoExit`), `/min` opens minimized. Settings: `-AllowStartIfOnBatteries -DontStopIfGoingOnBatteries -StartWhenAvailable -ExecutionTimeLimit Zero -MultipleInstances IgnoreNew` — server is long-running by design; don't let Task Scheduler kill it on schedule. Idempotent: `Unregister-ScheduledTask` runs first (silent on missing), then `Register-ScheduledTask`. Companion uninstall script does reverse. **Why user-integrity rule extends past Outlook:** anything user-mode-only fails the same way — user's keychain (Windows Credential Manager via DPAPI), user's clipboard, user-context COM servers, named pipes with ACLs hardcoded to user-SID. If your scheduled task talks to ANY of these, register at `Limited`. For services that must survive user-session changes (system-wide daemon), use a Windows Service instead of Task Scheduler — different security model, can't bridge integrity either, but lives in the right execution context for system-level work. Test the task BOTH in dev (manual `schtasks /run /tn TaskName`) AND on actual logon (sign out, sign back in, verify task fired via `schtasks /query /tn TaskName /v`). Provide idempotent uninstall script alongside install — if user removes the app, task should be cleanable without original install script. Set `Description` field on task with L# / lesson reference so future maintainers have context. **How to apply:** (1) Default-target PS 5.1 dialect for any script that ships in a project's `scripts/` folder; test explicitly on `powershell.exe`. (2) Audit every Task Scheduler task for `RunLevel`; if it ever reads/writes user resources, set to `Limited`. (3) Use `-AtLogOn -User $env:USERNAME` (not `-AtStartup`) when task depends on logged-in user session. (4) Provide idempotent uninstall script alongside install. References: `D:\PMO_OS\scripts\setup-python-runtime.ps1` rewritten to PS 5.1 dialect after three failed runs (third iteration shipped: 78 MB python-runtime/, validation imports clean, runs idempotent); `D:\Project Management\Outlook\Install-PMO-Server-Autostart.ps1` + `Uninstall-PMO-Server-Autostart.ps1` (paired idempotent registration). Lesson #761 (May 2 2026) is the original integrity-boundary capture; this lesson generalizes it to Task Scheduler context. (Folded former #677, #692.)

751. **tRPC + React Query migration patterns for porting v13/v14 HTML dashboard tabs to Electron + tRPC + SQLite — three rules: (a) compatibility shim `asState(query)` lets you migrate data plumbing in 30 min while keeping 200+ lines of JSX untouched; (b) primary/secondary mutation try/catch isolation prevents new write contracts from making old ones look broken; (c) embedded baselines from the source HTML become bundled JSON imports + runtime merge in the tRPC list resolver — never seed them into SQLite** — Patterns from May 6 PMO_OS tab-port work (commits L088, L089, L091, closing the v13/v14→PMO_OS migration audit doc). **(a) Compatibility shim — port plumbing without touching JSX:** When migrating a 400-line component from local `useState<{state, result?, error?}>` + parallel-fetch to tRPC + React Query, define a tiny mapper that flattens React Query state into the original discriminated-union shape: `asState(q) = { state: q.isLoading ? 'loading' : q.isError ? 'error' : q.isSuccess ? 'done' : 'idle', result: q.data, error: q.error?.message }`. The entire downstream JSX (~30 conditional renders) keeps working unchanged. Why this is correct (not a hack): (1) the original shape was always a discriminated union over the same 4 async states — shim is a literal projection, not a translation; (2) shim is local to the component (no leak into tRPC or rendering layers); (3) migrating data plumbing + rendering in one PR doubles diff review surface and triples regression risk — both network AND visual behavior change, you can't tell from a screenshot which broke. With the shim, visual behavior is byte-identical; only network plumbing changes; bugs isolate to one layer. Shim costs ~12 lines + one type def. **Initial-load gate for user-driven components:** OutlookCategories had Refresh-first behavior (no auto-fetch on mount, unlike Calendar/Meetings). Naive useQuery fires immediately. Fix: `enabled: false` in useQuery options + `const [enabled, setEnabled] = useState(false)`; Refresh handler does `if (!enabled) { setEnabled(true); return; }` — first click flips gate, useQuery fires automatically, subsequent clicks call `.refetch()`. Sticky enabled flag preserves cache across tab switches. Then in `asState`: `if (!enabled && !q.isFetching) return { state: 'idle' }` — preserves "Click Refresh to load" empty state. Always pass `retry: false` to secondary queries (PS server probes that may be offline) so a single failed probe doesn't spam. Pass `staleTime: 5 * 60 * 1000` + `refetchOnWindowFocus: false` to match Calendar/Meetings precedent. Migrate ONLY data plumbing in one PR; defer JSX cleanup to a separate cosmetic pass. **(b) Primary/secondary mutation isolation — additive features must not break the load-bearing contract:** When a UI action involves multiple write paths (e.g., Save Snapshot writes to BOTH `insights` table — Day-9 contract, markdown body — AND `portfolio_snapshots` table — new History tab contract, frozen narratives JSON), wrap the SECONDARY mutation in its own try/catch and let the PRIMARY succeed regardless. User-visible "✓ Saved" state maps to the primary contract; secondary contracts get `console.error` and quiet failure. DO NOT use `Promise.all` — it short-circuits on first failure, killing all parallel writes; use independent `await` calls in separate try/catch blocks. Identify the PRIMARY contract for any UI action — the one the user has muscle memory for. SECONDARY contracts are analytics, mirror writes, derived caches, telemetry — their failure should be invisible in the success path. May 6: first draft chained insight + portfolio-snapshot in sequence; if snapshot save failed (e.g., Drizzle migration 0004 not yet applied on stale DB), user got red error toast even though insight DID save — wrong, since insight is load-bearing. Fix: primary in main try/await flow (sets `setSnapshotResult({ ok: true })`), secondary in own try/catch with `console.error` only. Store both ids in component state (`{ ok: true, id, secondaryId }`) for debug log; UI shows only primary id. **(c) Embedded baselines as static JSON, never SQLite seeds:** v14 used a clever trick — two embedded snapshots (Apr 16 + Apr 20) synthesized at runtime by reading `window.PORTFOLIO_NARRATIVES` (current state) plus a `<script type="application/json" id="portfolio-apr16-diff">` baseline override. They lived IN the HTML file — invisible to localStorage, undeletable, always present. Naive PMO_OS port would seed them as `source='embedded'` rows in `portfolio_snapshots` table. That breaks two ways: (1) Reset DB / Re-seed wipes them and re-creates with new IDs, breaking external references / exported JSON; (2) the `delete` mutation has to special-case them anyway, so storing them in DB adds zero benefit. Correct: extract ONCE via build script (`scripts/extract-v14-portfolio-snapshots.cjs` runs `vm.runInNewContext()` against the JS literal + `JSON.parse()` against the script tag, writes 126 KB JSON to `src/data/embedded-portfolio-snapshots.json`); renderer + main process import as static module (`resolveJsonModule: true`); tRPC list resolver merges `[...embedded, ...userRows]` at read time + sorts by `publishedAt`; `delete` + `importBatch` refuse IDs with `embedded-` prefix. ID convention: embedded → `embedded-YYYY-MM-DD`, user-saved → `snap-{ts}-{rand}`, imported → `imp-{ts}-{rand}`. ID prefix is the dedupe primitive — make `delete` + `importBatch` refuse `embedded-*` early so protection lives at the API layer, not just UI button-disable. Comment in schema file clearly: "embedded baselines do NOT live in this table; they ship as src/data/*.json and merge at read time." Same pattern applies to ANY migration from "HTML file with hardcoded baseline data" to "SQLite-backed app with user data" — design artifact (baseline) and user data are two categories; mixing them in the same persistence layer creates churn at every Reset/Re-seed/Migration boundary. **Why these three cluster:** All are migration-time discipline rules where naive implementations work locally but break at integration boundaries (Reset DB, secondary failure paths, render-vs-plumbing diff isolation). Cost of each mistake: hours-to-days of debugging when a working dashboard "stopped saving" or "shows wrong data after reset." **How to apply:** (1) For any v13/v14 → PMO_OS tab port, identify embedded data BEFORE designing the SQLite schema. (2) For any state-shape migration, write the `asState` shim FIRST and ship it; defer JSX cleanup. (3) For any new mutation layered on existing one, isolate it in its own try/catch — never chain primary + secondary in `Promise.all` or sequential await. (4) ID prefix is the dedupe primitive; protect at API layer, not UI. References: `D:\PMO_OS\src\components\OutlookCategories.tsx:36-110` (asState shim, v2.0.6 — closes Tab_tRPC_Migration_Audit.md); `src/components/PortfolioStatus.tsx saveSnapshot` (primary/secondary isolation, v2.0.4); `src/main/db/schema.ts:140-160` + `src/main/trpc/router.ts:548-720` + `src/data/embedded-portfolio-snapshots.json` (126 KB, 2 baselines × 28 projects) + `scripts/extract-v14-portfolio-snapshots.cjs` (~140 LOC) + 12 unit tests in `portfolio-snapshot-helpers.test.ts` (v2.0.4). (Folded former #679, #680, #683.)

682. **Click telemetry on a desktop app should reuse the existing rotating log file (pino + pino-roll) — DO NOT create a SQLite click_events table, DO NOT add a new dependency, DO NOT design a pivot/rollup table. The aggregator runs in main process, reads the log files at request time, parses them with JSON.parse() per line, filters event === 'click', and computes the pivot in pure functions that live under src/lib/ so they can be unit-tested without spinning up Electron. The renderer talks to it via tRPC clickStats.aggregate({ days: 7 }) — the same channel as everything else. This pattern delivers full telemetry value with zero new infrastructure** — On 2026-05-06 evening adding click telemetry to PMO_OS so the IT PM can see "Refresh from DevOps was clicked 22× this week, mostly on Data Gate". First instinct was a new SQLite click_events table with (id, ts, tab, button, meta_json) schema, a tRPC clickEvents.create mutation called from each onClick, an aggregator query, an index on (ts), retention policy. That's overkill: the data is observational (lossy at 14 days is fine), low-value-per-row (just a counter), and there's already a perfectly good rotating log file (lesson #671 wired pino + pino-roll). Pivoted to the lighter pattern: (1) preload exposes window.api.telemetry.click({ tab, button, meta }), fire-and-forget over ipcRenderer.send (NOT invoke — no round-trip needed for telemetry); (2) main listens via ipcMain.on 'telemetry:click' and calls getLogger().info({ event: 'click', tab, button }, 'click') — single line, drops on error; (3) renderer hook useClickLog(tab) returns { log, wrap } — components do <button onClick={() => { clickLog('refresh-devops'); runFetch(); }}>; (4) aggregator aggregateClickLines(lines) is pure: parse each line as JSON, filter event === 'click', group by button + tab + day, return shape { total, byButton, byTab, byDay, windowStart, windowEnd }; (5) tRPC clickStats.aggregate({ days }) uses readAllLogLines() from electron/lib/logger.ts and calls the pure helper; (6) Settings card renders the result with toggle for 7-day vs 30-day window. Total deliverable: ~330 LOC across 5 files (helpers + tests + hook + router proc + Settings card) + 8 onClick instrumentation sites. 17 unit tests on the aggregator. Zero new dependencies. Zero new tables. **Why this beats SQLite for this specific use case:** (a) the data is fundamentally observational — losing rows older than 14 days is acceptable; (b) the only query is a 7/30-day pivot that fits in memory (logs are ~1 KB/day under typical use); (c) rotation is already solved via pino-roll's frequency: 'daily'; (d) the aggregator is trivially testable as a pure function over string[] — no DB fakes needed; (e) the in-memory tail buffer (lesson #671) AND the live log file BOTH carry the records, so the Settings tab gets free real-time tail + historical aggregation from the same source. **When SQLite WOULD be the right choice:** (a) you need transactional integrity (every event MUST be queryable, lossy is unacceptable); (b) you need long retention (>30 days); (c) you need complex queries beyond "count by N dimensions"; (d) the events are valuable beyond observability (e.g., billing). For "how often was this button clicked," none of those apply. **How to apply:** (1) When designing telemetry on a desktop app, ask "is this observational or transactional?" — observational data goes to logs, transactional to DB. (2) Use ipcRenderer.send (fire-and-forget) for telemetry, NEVER ipcRenderer.invoke (round-trip). The user must never wait on telemetry. (3) Main-process telemetry handlers MUST swallow all errors — try { logger.info(...) } catch {} at the boundary. Telemetry that crashes the app is worse than no telemetry. (4) Aggregator helpers belong in src/lib/ as pure functions over string[] so they're easy to test. The tRPC procedure is just a thin wrapper that resolves file paths + calls the helper. (5) The renderer hook should be tiny — a useCallback + an optional wrap() helper for inline use. Don't build a HOC; React's useCallback is the right primitive. (6) Skip the aggregation in dev mode if you want — but it's so cheap (parse N JSON lines, group, return) that running it always is fine. (7) Reference: 2026-05-06 — D:\PMO_OS\src\lib\click-stats-helpers.ts (~100 LOC) + .test.ts (~140 LOC, 17 tests) + src/lib/use-click-log.ts (~60 LOC) + electron/lib/logger.ts readAllLogLines() helper + tRPC clickStats.aggregate proc + Settings ClickStatsCard. v2.0.5 ships the system end-to-end with 8 instrumented buttons (Refresh DevOps × 3 tabs, Save as Insight × 3, Save Snapshot, Auto-Draft Red, Generate Agenda, Refresh Calendar, View/Export/Delete on Project Status History, sidebar nav, menu nav).

759. **Two anti-bloat rules that prevent shipping work the user didn't ask for: (a) verify external-system state (repo visibility, deployment status, feature flags, ACLs) with a one-shot diagnostic command BEFORE acting on memory — corrective ratio is asymmetric (5 seconds to run `gh repo view --json visibility` vs hours to debug "why doesn't auto-update work" after wrong assumption ships); (b) when the user shows multiple screenshots with a vague description ("3 issues" + "split menu"), ALWAYS use AskUserQuestion with multiSelect to confirm exactly which issues are in scope BEFORE writing the plan — the temptation to "fix everything visible" is a trap that triples PR diff size and trains the user to skim plans rather than read them** — Two patterns from May 6 PMO_OS v2.0.7 → v2.0.8 incident chain. **(a) Verify external state before acting on memory:** May 6 evening: project memory `pmo_os_repo_github_published.md` claimed "PMO_OS repo is now PUBLIC on GitHub". That memory was written after L086 commit which switched electron-builder to `provider: github` and added CI release workflow. Author inferred public-ness from "we're publishing releases via GitHub" — but L086 only changed publish provider and CI workflow; it never flipped repo's visibility setting. Three weeks later the assumption surfaced as a bug: anonymous polls to `https://github.com/melshahedpearl/PMO_OS/releases.atom` returned 404, Settings → Updates card showed cryptic auth error. Diagnosis took ~2 minutes once `gh repo view --json visibility` was run (returned `{isPrivate: true}`); fix took rest of v2.0.8 (~2 hours: GH PAT store + IPC + UpdatesCard UX). The 2-minute verify-first command would have caught this BEFORE shipping v2.0.7. **Why memory drift is hard to catch by inspection:** (i) memories don't expire — they sit in `MEMORY.md` indefinitely unless someone notices contradictions; (ii) the gap between "what was true when written" and "what is true now" is invisible — there's no diff highlight; (iii) memories often summarize multi-step processes ("we did X, then Y, then system became Z") and only the final claim Z is wrong even if X and Y were correct; (iv) memories about EXTERNAL-system state (GitHub repo visibility, AWS bucket policy, S3 ACL, DNS record) are especially prone to drift because those systems are mutated outside the source tree. For ANY memory note that names a remote-resource state, ALWAYS prefix the action that depends on that state with a one-shot verify command: `gh repo view --json visibility,isPrivate` for GitHub repos; `aws s3api get-bucket-policy --bucket NAME` for S3; `kubectl get deployment NAME -o json | jq .status` for k8s deployments. When a memory turns out to be wrong, REWRITE the memory with corrected state + clear note about what was wrong + verify command that catches it. Don't just delete the wrong line — the "what was wrong" record helps the next reader trust the new content. Memories that are pure-domain knowledge ("the IT PM uses Outlook for email") don't need verify-before-recommend; only those that name verifiable external state. Cross-reference: CLAUDE.md "Before recommending from memory" rule already says this in general; this lesson grounds it in concrete repo-visibility example so the principle has a real failure to point at. **(b) AskUserQuestion to confirm scope on multi-screenshot reports:** May 6 evening: Mahmoud sent 4 screenshots with the message `1- Three issue with three photo / 2- split the menu of navigate as we talk to three seperate menu insted of the let Panel`. The first 3 photos showed: (i) Help menu with English-only items, (ii) Updates card with 404 error, (iii) Settings User Config card with Re-run setup wizard button. My instinct read 4 potential issues: Setup Wizard didn't auto-show, Updates 404, native menus English-only, lastSeenVersion stuck at "(never)". Temptation was strong to ship v2.0.8 that fixed all 4 — both because they're all real issues and because shipping more feels like more value delivered. Resisted; called AskUserQuestion with the 4 candidates as multiSelect list. Mahmoud picked ONE: "Updates card shows 404". The other 3 were either (a) not bugs from his perspective (he completed wizard manually + lastSeenVersion is acceptable as "(never)") or (b) acceptable cosmetic gaps (English-only menus). v2.0.8 shipped focused on the one real issue + the menu-split request. Plan tighter, review faster, no dead-end work on issues the user didn't care about. **Why the temptation to "fix everything visible" is a trap:** (i) Visible problems are not always priority problems for the user — your engineering eye sees gaps the user has already accepted as cosmetic. (ii) Fixing 4 things in one PR triples diff size + triples review time. (iii) Each "extra" fix has its own bugs/regressions/edge cases — shipping 4 things means 4× chance of follow-up issue. (iv) The user's mental model of "what's broken" is the load-bearing scope; engineering proposals that exceed it train the user to skim plans rather than read them. When N visible problems map to a vague "fix the issues" instruction, ALWAYS use AskUserQuestion with multiSelect to confirm scope. List each visible problem as a separate option; let the user pick. Phrase each option as 1-sentence problem statement + 1-sentence proposed-fix preview, so the user can pick informed. Do NOT try to upsell the user on adjacent fixes — list them, let them pick, move on. The user-driven scope is sacred. For half-clear half-vague requests (one half clear, one half vague), still ask one clarifying question on the vague half. Use the user's actual words in the question + options when possible. Build the plan AROUND the user's selected scope, not around your engineer-eye proposed scope. **Why these two cluster:** Both prevent the same anti-pattern — Claude doing more work than the user asked for, based on either stale assumed context (a) or visible-but-unwanted findings (b). The cost is the same in both cases: wasted PR cycles + diluted review attention + "the engineer doesn't listen" feeling. **How to apply:** (1) Before any action that depends on memory-claimed external state, run the verify command (`gh repo view --json`, `aws s3api`, `kubectl`, etc.) — 5 seconds vs hours of debugging after wrong assumption ships. (2) When user description includes "fix the issues you see" + multiple visible items, ALWAYS multiSelect-AskUserQuestion to confirm scope. (3) When memory is wrong, rewrite with corrected state + verify command + what-was-wrong note; never silently delete. (4) Plan reflects user-selected scope, not engineer-eye proposed scope. References: 2026-05-06 — Mahmoud's 404 error screenshot in Settings → Updates surfaced the gap; `gh repo view --json visibility` confirmed `isPrivate: true`; memory `pmo_os_repo_github_published.md` rewritten with correct state + 3 fix paths (flip public / paste PAT / generic provider); same evening AskUserQuestion call with 4 options; user picked 1; plan + ship reflected just that 1 + menu-split, saving 50%+ of v2.0.8 implementation time vs "fix all 4" path. (Folded former #688, #690.)

754. **Cross-runtime deploy hazards — two rules for migrations that touch both code and filesystem state: (a) cross-volume data moves MUST use COPY → SHA-256 VERIFY → DELETE sequence (never `Move-Item`/`mv` directly across volumes — those become non-atomic copy+delete with no rollback); (b) when a deploy involves both a code change AND a filesystem change, the running pre-deploy process keeps using or re-creating the old location until restart — explicitly tell user "restart required" with reason, or design migration to be benign if the old location reappears** — Two patterns from May 6 PMO_OS v2.0.10 backup-path relocation (OneDrive → D:\PMO_OS\DB Backup\Prod). **(a) Cross-volume COPY → VERIFY → DELETE:** When migrating user data (DB backups, fixtures, snapshots, exports) from one filesystem location to another, structure as 5 explicit steps: (i) Compute SHA-256 of every source file, print as "Source files (BEFORE)" table. (ii) `Copy-Item -Path $src -Destination $dst -Force` for each. (iii) Compute SHA-256 of every target file, print as "Verification (target)" table. (iv) Visually or programmatically assert hashes match. (v) Only then `Remove-Item -Path $srcDir -Recurse -Force`. Final `Test-Path $srcDir` returns `False` to confirm clean state. Print BOTH hash tables in same script output so audit trail is in terminal scrollback. Why this matters more than it seems: (1) Move operations look atomic in shell mental models but are NOT atomic across volumes — Windows `MOVEFILE_COPY_ALLOWED` flag silently downgrades to copy-and-delete with no rollback; (2) Antivirus on Windows often interferes with both Copy and Remove operations, sometimes silently quarantining files mid-operation — hash check catches this immediately; (3) OneDrive sync can hold file locks for seconds during sync, can cause `Remove-Item` to throw without `-Force` — mid-loop failure leaves split state; (4) For DB backup files specifically, the user is migrating BECAUSE they care about not losing data; silent partial-copy is the worst possible outcome; (5) Hash compare is ~50ms per file; cost is negligible vs peace-of-mind. Use SHA-256 (CRC32 too weak; MD5 fine but SHA-256 strictly better, just as fast in PowerShell — `Get-FileHash -Algorithm SHA256`). When relocating to a folder INSIDE the repo, `.gitignore` the new folder BEFORE copying — otherwise next `git status` flags hundreds of MB of binary blobs as "new files to track" and someone might `git add .` them. When source is OneDrive folder, wait a few seconds after the last copy before `Remove-Item -Recurse` so OneDrive's file watchers release any locks. Script should: succeed-and-exit-zero on clean migration; print errors and refuse to delete source if any hash mismatches; be idempotent (re-run on already-migrated state is no-op). May 6 reference: 3 legacy daily-snapshot files migrated (`pmo-2026-05-03.db` 94 KB, `pmo-2026-05-05.db` 200 KB, `pmo-2026-05-06.db` 221 KB); SHA256 `6267CDE3...` matched source byte-for-byte before OneDrive folder deletion; total elapsed ~10 seconds. **(b) Code+filesystem deploy race:** When deploying a code change that ALSO involves deleting/relocating the old state location, the still-running production process keeps using OR re-creating the old location until the user restarts. There's a race window between code-deploy and process-restart during which the user sees stale or split state. Either (i) explicitly tell user "restart required after install" with the reason, OR (ii) accept old location may temporarily reappear and structure migration to be benign in that case. NEVER assume a deployed-but-not-restarted client is in the new state. May 6 v2.0.10 deploy: (1) push v2.0.10 with backup-path code change `OneDrive\PMO_OS_backups → D:\PMO_OS\DB Backup\Prod`, (2) copy 3 legacy files, (3) delete OneDrive folder. Risk: user's running PMO_OS v2.0.9 still has OLD backup-path in memory. If they click `Settings → Run Now` or 02:00 fires before restart, v2.0.9 code calls `resolveBackupDir()` reading `$OneDrive` env var, returns OneDrive path, calls `ensureDir()` (which CREATES the deleted folder again), writes backup there. Net effect: OneDrive folder reappears with one file in it, splitting state until restart. Mitigation: explicit user message in the ship summary — "Important: Once the new .exe lands and electron-updater installs it on next launch, future backups go to `D:\PMO_OS\DB Backup\Prod\`. Until you relaunch with v2.0.10 installed, the currently-running v2.0.9 process would still write to the old (now-deleted) OneDrive path if a backup fired — quit + reopen the app after the update completes." Converts silent race into explicit user action. State-split is benign even if user forgets — OneDrive folder gets recreated with one new backup file, no data loss, just suboptimal location until restart. **Why this is a category, not a one-off:** any deploy touching BOTH (a) the location code reads/writes AND (b) the actual filesystem/network resource has this race. Examples: relocating log files + deleting old log dir; renaming an env var + clearing old config; moving a DB connection string + dropping old DB. **How to apply:** (1) For ANY data-relocation script, structure as 5 explicit steps; never collapse to single `Move-Item` across volumes. (2) Whenever a deploy involves "code change + filesystem state change," EXPLICITLY think through what the running pre-deploy process does between the two steps. Write it: "Old code reads X, writes to Y. New code reads X, writes to Z. Between deploy and restart, running process runs OLD code with X==either-state-of-the-world." (3) Tell user "restart required after install" with one-sentence reason. Don't assume electron-updater + auto-restart handles this — auto-restart fires only on app close+reopen, not on running session. (4) Structure migrations so old location reappearing is BENIGN — i.e., don't depend on old location staying empty. New code should ignore old-location state when it next runs. (5) If migration is truly atomic-required (old location MUST stay empty), kill running process FIRST, then deploy + cleanup, then re-launch. (6) For Electron apps with auto-update specifically: electron-updater's `quitAndInstall` API supports forcing restart — if migration requires it, set `autoInstallOnAppQuit: true` and surface "Restart now to apply update" button in Settings. (7) Document the pattern in release notes / commit message so future deployers don't re-learn it. References: 2026-05-06 night PMO_OS v2.0.10 backup-path relocation script (5-step COPY-VERIFY-DELETE) + ship summary with explicit "quit + reopen" message; `D:\PMO_OS\DB Backup\Prod\` (3 migrated legacy files); v2.0.10 ships with relocated path baked into `resolveBackupDir()`. Companion to lesson #750 (window title from `app.getVersion()`) — both highlight that "the app reading state at runtime" can be subtly out-of-sync with the developer's mental model. (Folded former #698, #700.)

699. **NEVER tag a release from the HEAD of a feature branch when the branch contains parallel-agent / WIP commits between the previous release and yours. The git tag attaches to a commit hash, and `git push origin <tag>` triggers CI which builds the .exe from THAT commit's tree — so any WIP commits that snuck in via the branch chain ship to end-users via auto-update. Cherry-pick your release-relevant commit ONTO master first, tag from master, push master + tag. The cherry-pick is one command (`git cherry-pick <sha>`) and the resulting master commit is clean — only your changes, no WIP** — On 2026-05-06 night while shipping PMO_OS v2.0.10 (the backup-folder relocation lesson #754), the working branch was `feat/portfolio-narratives-auto-refresh-may6` — a branch ANOTHER agent had been using to develop "L098 Portfolio Status auto-refresh: Day 1 (sidecar fetch + digest parser)". The branch chain was: master `6bb609f` (v2.0.9) → `4a26b2e` (L098 parallel WIP, NOT mine) → `38e38ac` (my v2.0.10 commit). The naive next step would be `git tag v2.0.10 && git push origin v2.0.10` — but that tag would attach to `38e38ac`, whose tree includes `4a26b2e`'s WIP changes via the parent chain. CI would build a v2.0.10 .exe containing the L098 work-in-progress, electron-updater would push it to every installed PMO_OS client, and end-users would silently receive an in-progress feature that wasn't ready for shipping. Detection: `git log master..HEAD --oneline` showed two commits (`4a26b2e L098 ...` + `38e38ac v2.0.10 ...`) — anything beyond just my v2.0.10 commit was an immediate red flag. Correct fix: `git checkout master` (clean tip at `6bb609f`) → `git cherry-pick 38e38ac` (applies just my v2.0.10 diff onto master, becomes new commit `4d30a93` whose tree is master + my changes, NO L098) → `git tag v2.0.10` (tags `4d30a93`, the clean master tip) → `git push origin master` → `git push origin v2.0.10`. CI fired on the tag, built the .exe from `4d30a93`'s tree, published. Then `git checkout feat/portfolio-narratives-auto-refresh-may6` to restore the other agent's working state. **Why "just merge to master then tag" doesn't work here:** a merge would bring `4a26b2e` (L098 WIP) onto master too — same problem. Cherry-pick is the precise tool: it transplants ONE commit's diff onto a different parent, leaving the original commit + the WIP commit in the feature branch where they belong. **Why this matters MORE in auto-update environments:** without auto-update, a stale tag is harmless — users opt-in by downloading the .exe. With electron-updater + GitHub provider, every tag push silently propagates to every installed client. The blast radius of a wrong tag is "every user runs WIP code on next launch." Compare to: a wrong tag in a non-auto-update repo just sits in GitHub Releases as a download nobody clicks. The auto-update channel demands tag discipline. **How to apply:** (1) BEFORE running `git tag <release>`, ALWAYS `git log master..HEAD --oneline` and confirm the output is JUST your release commits. If anything else appears, STOP. (2) Cherry-pick your release commit(s) onto master with `git checkout master && git cherry-pick <sha>` — this gives you a clean release commit on master without dragging in feature-branch siblings. (3) Tag from master, never from a feature branch unless the feature branch IS the release line (rare; most teams release from master/main only). (4) After cherry-picking + pushing the tag, check out the feature branch again so you don't disrupt the other agent's working state. (5) For multi-agent setups specifically: assume the working tree may have commits you didn't make. ALWAYS `git log` before tagging. (6) If you accidentally push a tag with WIP, immediately delete it (`git push origin :refs/tags/<bad-tag>`) and re-tag from a clean commit — but note electron-updater clients that already polled may have cached the bad version's metadata; force a major bump (e.g., v2.0.10 broken → re-release as v2.0.11) rather than reusing the tag, since some clients won't re-poll. (7) Reference: 2026-05-06 night — `git log master..HEAD --oneline` revealed `4a26b2e L098 ... + 38e38ac v2.0.10` on `feat/portfolio-narratives-auto-refresh-may6`; cherry-picked 38e38ac onto master, got clean `4d30a93` master commit, tagged v2.0.10 from there, pushed, CI built clean .exe with NO L098 WIP. Companion to lesson #753 (verify .exe hash before install) — both lessons protect the auto-update channel's integrity.

## PMO_OS Portfolio Status Auto-Refresh from Outlook (May 6, 2026 — late session)

701. **PowerShell 5.1 falls back to Windows-1252 (system ANSI) when reading a `.ps1` file that lacks a UTF-8 BOM. Any non-ASCII char in a STRING LITERAL — em-dash, en-dash, arrow, smart-quote, emoji — is then misread byte-by-byte and the lexer terminates strings mid-sentence on whatever happens to map to a quote character in CP1252. Comments survive (the `#`-to-EOL boundary is unambiguous), so non-ASCII in comments looks fine in QA but ANY new string literal with non-ASCII silently breaks server boot. Save .ps1 files as UTF-8 WITH BOM via `System.Text.UTF8Encoding($true)`, OR keep all string literals strict-ASCII and confine Unicode to comments. Don't trust `Get-Content -Raw` or PSParser to surface the problem — they read the file fine; the runtime lexer is the failing layer** — On 2026-05-06 evening during the L098 PMO_OS narratives auto-refresh build, the new `/api/pmo/narratives-refresh` endpoint included a string literal `message = "skipExtract=true — reusing cached inbox/sent/topic CSVs"` containing an em-dash. The PSParser-based syntax check passed (8,759 tokens parsed cleanly). The file committed and pushed. When the user restarted the server, the boot output was a wall of red parse errors: `Unexpected token 'reusing' in expression or statement`, `The hash literal was incomplete`, `Missing closing '}' in statement block`, `The Try statement is missing its Catch or Finally block`. None of those errors point at the actual cause — the visible mojibake was `â€"` (the bytes 0xE2 0x80 0x94 of an em-dash decoded as Windows-1252), which the parser interpreted as `â`+`€` followed by a closing quote, terminating the string mid-sentence and cascading into garbage tokens for the next 200 lines. The fix was two-pronged: (a) replace `═` with `=`, `—` with `--`, `→` with `->`, `🔄` with `[refresh]`, `✅` with `[ok]` ONLY in the lines I added (existing Phase 4-8 lines were untouched because they used non-ASCII only in comments which already worked); (b) re-save the entire file as UTF-8 WITH BOM via `[System.IO.File]::WriteAllLines($path, $lines, (New-Object System.Text.UTF8Encoding $true))`. After both fixes, server booted cleanly. **Why the failure mode is uniquely sneaky:** (a) PSParser's `Tokenize()` reads with .NET's smart encoding detection (uses BOM if present, else UTF-8 if the byte pattern looks UTF-8, else system default), which is MORE lenient than the runtime PS lexer that powers `& powershell -File foo.ps1`. PSParser passes; the runtime fails. So a syntax-check pass means nothing for non-ASCII literals. (b) Existing em-dashes in the file's comments work because the lexer treats `# ... \n` as one token regardless of byte pattern between `#` and EOL — but ANY non-comment context (string literals, here-strings, even regex literals via `-replace`) routes through the encoding-sensitive sub-lexer. (c) `Get-Content -Raw $path` ALSO reads with smart detection — eyeballing the file content in PowerShell looks correct even when the runtime fails. The only reliable diagnostic is reading the first 3 bytes: `[System.IO.File]::ReadAllBytes($path)[0..2]` returning `0xEF 0xBB 0xBF` means BOM (safe); anything else means CP1252 fallback risk. (d) Git's CRLF/LF normalization can strip BOMs on some setups depending on `.gitattributes` — even if you save with BOM, a clone elsewhere may not have it. Treat BOM presence as a runtime check, not a commit-time guarantee. **How to apply:** (1) For ANY new `.ps1` file that ships HTTP responses, log messages, or any string literal containing non-ASCII, save it with UTF-8 BOM. PowerShell pre-7's `Set-Content -Encoding UTF8` writes BOM by default; PowerShell 7+'s same flag writes BOM-less UTF-8 unless you pass `-Encoding utf8BOM`. Best to use the .NET API directly: `[System.IO.File]::WriteAllLines($path, $lines, (New-Object System.Text.UTF8Encoding $true))`. (2) Add a syntax-AND-lexer sanity check after every PS file edit: `& powershell -ExecutionPolicy Bypass -Command "& { . 'foo.ps1' }"` (sources the script — fails fast if there's a parse error at runtime, not just at PSParser level). For a server file you don't want to actually run, wrap the body in a no-op function: `function _check { [your code] }; Get-Command _check | Out-Null` — defines and immediately discards, exercises the lexer fully. (3) Make a habit of stripping non-ASCII from string literals in PS code on contact: `—` → `--`, `→` → `->`, `…` → `...`, smart quotes → straight quotes. Comments are safe. (4) Add a hook / pre-commit check that flags `.ps1` files without BOM. (5) When debugging "server fails to boot with cascading parse errors," check the encoding FIRST before chasing the line numbers. The cascade always starts at the first non-ASCII string literal byte; the line numbers it cites are downstream noise. (6) Reference: 2026-05-06 — `D:\Project Management\Outlook\Run-Update-Server.ps1` line 1287 had `"skipExtract=true — reusing cached..."`. Fixed by ASCII-scrub of MY added section (lines 1221-1485) + re-save with BOM. Existing Phase 4-8 lines (em-dash in comments only) were untouched and continued working. Companion to lesson #761 (PowerShell COM integrity) — both surface PowerShell sub-system gotchas that don't show up in obvious places.

702. **PowerShell 5.1's `ConvertTo-Json -Compress -Depth 10` hangs the calling thread for tens of seconds (effectively forever for an HTTP response timeout) when given an array of moderately-sized hashtables containing string fields with embedded UTF-8 multi-byte sequences. With a single-threaded `System.Net.HttpListener` accept loop (the standard "while ($listener.IsListening) { GetContext(); handle; }" pattern), one slow `ConvertTo-Json` call blocks ALL subsequent requests indefinitely — even fast endpoints like `/api/status` queue up forever. Workaround: assemble JSON manually per-row. Build the envelope hashtable via `ConvertTo-Json` first (small, fast), strip its trailing `}`, then `ConvertTo-Json` each row in the array INDIVIDUALLY (each is small and fast), string-join with commas, append `]` + closing `}`. The cumulative time scales linearly instead of pathologically, and the listener thread is freed in <1s instead of >30s** — On 2026-05-06 evening the L098 `/api/pmo/narratives-digests` endpoint read 24 markdown files (each ~3 KB) and called `Send-Json $context @{ ..., digests = [object[]]$digests }` which internally does `$obj | ConvertTo-Json -Compress -Depth 10`. The first request hung. The second request (`/api/pmo/status`, which had returned in ms moments earlier) ALSO hung, because the listener thread was still blocked in serialization. After 30 seconds the test client timed out. Server console showed no errors — it was alive, just stuck. Killing the client connection didn't help because PS doesn't see the disconnect until the OutputStream.Write call. Diagnosis: 24 nested hashtables × ~3 KB strings × `-Depth 10` triggers a quadratic-or-worse path in PS 5.1's `ConvertTo-Json` when string content contains multi-byte UTF-8 sequences (em-dashes, Arabic, smart quotes from real Outlook subjects). The fix in `Run-Update-Server.ps1`: `$headerJson = ($headerObj | ConvertTo-Json -Compress -Depth 5).TrimEnd('}'); $digestParts = @(); foreach ($d in $included) { $digestParts += ($d | ConvertTo-Json -Compress -Depth 5) }; $body = "{0},`"digests`":[{1}]}}" -f $headerJson, ($digestParts -join ',')`. Each per-digest `ConvertTo-Json` runs in <50 ms. Total response time dropped from "infinite hang" to ~200 ms for 24 digests. **Why the bug is uniquely lethal in single-threaded HttpListener:** (a) HttpListener.GetContext() blocks until a request arrives, then your handler runs synchronously, then the next GetContext() runs. There's no thread pool, no per-request worker. One slow handler stalls all queued requests. (b) The hang isn't an exception — `ConvertTo-Json` is just slow, not failing — so try/catch doesn't help. (c) Killing the client (Ctrl+C the test script) doesn't kill the server-side serialization. The server is still chewing on the response data structure when you give up. (d) PS 5.1's serializer has known O(n²)-ish behavior on certain inputs: deeply nested + large embedded strings + multi-byte UTF-8 + `-Depth >= 10`. It's not on Microsoft's fix-list because PS 7+ uses Newtonsoft.Json which doesn't have the issue. But Windows PowerShell 5.1 ships with Windows itself and is what most servers run. (e) The endpoint had previously worked for SMALLER digest folders (e.g., during dev with 2-3 files); the bug only manifests at scale. Classic "looked fine until production-shaped data hit it." **How to apply:** (1) For ANY HttpListener-backed PS server returning collections, NEVER pass a top-level array/hashtable with N>5 nested objects directly to `ConvertTo-Json -Depth >= 5`. Build the JSON manually: serialize each row, string-join. (2) The pattern: `$header = $envelope | ConvertTo-Json -Compress -Depth 5; $headerOpen = $header.TrimEnd('}'); $rows = @(); foreach ($r in $list) { $rows += ($r | ConvertTo-Json -Compress -Depth 5) }; $body = "$headerOpen,`"$arrayKey`":[$($rows -join ',')]}"`. Reads ugly, performs well. (3) Alternatively, switch to async HttpListener with `BeginGetContext` / `EndGetContext` so slow handlers don't block other requests — but that's a deeper refactor, and the per-row-assembly fix gets you 95% there with a 3-line change. (4) Migrate to PowerShell 7 if your server can ship with it (PS 7 is sideload-installable, doesn't replace Windows PowerShell 5.1). PS 7's `ConvertTo-Json` is 10-100x faster on the same input. (5) Add a defensive `$sw.Elapsed.TotalSeconds` log line BEFORE and AFTER every `ConvertTo-Json` call in your handler. If you see >1s, the next iteration's hang is just a matter of time. (6) For diagnosis: when an HttpListener server stops responding, check whether it's hung in serialization vs. a real hang. Trick: hit a known-static endpoint (returns hardcoded `{ "status": "ok" }`); if that ALSO times out, the listener thread is stuck somewhere upstream. (7) Reference: 2026-05-06 evening — `Run-Update-Server.ps1` `/api/pmo/narratives-digests` handler at line ~1422. Fixed by per-row assembly + bypassing `Send-Json`. Companion to lesson #701 (encoding) — both shipped as L098 fix commit `2e617e2`. Server boot time dropped from "hangs in 30s" to "all 3 endpoints respond in <500ms."

703. **`better-sqlite3`'s prebuilt binary is built against Electron's Node ABI version, NOT the system Node version that Vitest (and most Node-side tooling) runs on. Importing ANY code path that ultimately requires `better-sqlite3` (the Drizzle client, `:memory:` test setups, schema migration scripts) from a Vitest test under `environment: 'node'` fails with `Error: ERR_DLOPEN_FAILED`. The fix is NOT to rebuild — `npm rebuild better-sqlite3` would compile against Node's ABI and break Electron usage. Instead: do not write router-level / DB-level tests in this codebase. Test pure functions (parsers, serializers, derivers, formatters) and rely on end-to-end Playwright / manual smoke for the DB layer. This is the same constraint that's left every existing tRPC mutation in the codebase untested at the unit level — it's a known cost, not a bug to fix** — On 2026-05-06 during the L098 Day 2 build, I added `projectRegister.bulkSetNarratives` mutation to `src/main/trpc/router.ts` and tried to write a router-level Vitest test (`src/main/trpc/router.test.ts`) using the standard pattern: `new Database(':memory:') → drizzle(sqlite) → migrate(db, ...) → appRouter.createCaller({ db })`. All 7 tests failed in `beforeEach` with `Error: ERR_DLOPEN_FAILED { code: 'ERR_DLOPEN_FAILED' }`. Diagnosis: the `better-sqlite3` package on disk has a `prebuilds/win32-x64/electron.napi.node` file but no `node.napi.node` matching Vitest's runtime. PMO_OS uses `electron-rebuild` post-install to ensure the Electron-targeted binary matches the Electron version, but that same post-install step does NOT also build a Node-targeted variant. So Vitest's `node` environment can't load the addon. Three potential fixes considered: (a) `npm rebuild better-sqlite3 --build-from-source` to target Node — would break Electron until you re-run electron-rebuild, requires VS Build Tools, and you'd flip-flop between targets every test/dev cycle. (b) Switch test environment to Electron via `electron-vite-test` or similar — heavy infrastructure, slows test startup to seconds. (c) Use `sql.js` (pure WASM, no native build) with Drizzle's `drizzle-orm/sql-js` driver — adds a dependency, different SQLite implementation (might diverge subtly from better-sqlite3's behavior), still requires test-side setup that mocks `getDb()`. None are worth the work for one test. The codebase's own pattern confirms this: the existing `setNarratives` mutation (the single-row sibling of `bulkSetNarratives`) has NO router-level test either. Searching `src/main/trpc/` finds zero `*.test.ts` files. Vitest tests in this repo all live under `src/lib/` and exercise pure functions. Decision: delete `router.test.ts`, accept that bulkSetNarratives will be smoke-tested in Day 3 via the modal's "Apply N accepted" button hitting the live mutation. Document the constraint in the commit message so future contributors don't re-attempt. **Why this is worth a lesson rather than a one-off note:** (a) The mistake is tempting to repeat — every new tRPC mutation feels like it deserves a unit test, and the `:memory:` + Drizzle pattern is the obvious approach. Anyone reading the codebase for the first time will hit this wall. (b) The error message `ERR_DLOPEN_FAILED` is unhelpful — it says "failed to load native addon" but doesn't explain WHY. The first instinct is to think "missing dependency" or "rebuild needed," both of which are wrong (and one of which actively breaks the app). (c) The cost of working around this — extracting all DB-touching logic into pure helpers and testing those — is real architectural pressure that this codebase has wisely not taken on. Tests exist for derivers (`portfolio-status-derive`, `auto-draft`, `briefing-helpers`) but not for the database calls that feed them. That's a sustainable trade-off given the small team + heavy Playwright + manual smoke discipline. **How to apply:** (1) When tempted to write a router-level Vitest, FIRST search `src/main/trpc/` for existing `*.test.ts`. If none exist, that's a strong signal — don't fight it. (2) If you still want test coverage on a tRPC mutation, extract the BUSINESS LOGIC (the bit that decides what to write) into a pure function in `src/lib/`, test that exhaustively, and let the 3-line "wire to ctx.db" wrapper in the router go untested. The `bulkSetNarratives` example: the loop-and-skip-and-stamp logic could be a helper `buildBulkPayload(input, existingNames) → { writes, skipped }`, tested in isolation; the router just calls the helper inside `ctx.db.transaction()`. (3) For end-to-end confidence on DB mutations, add a Playwright E2E test under `tests/e2e/` that runs against the real Electron binary (if your project has Playwright + Electron set up). This is heavier than a unit test but actually catches the things you care about. (4) When you see `ERR_DLOPEN_FAILED` on a native module in any Node test environment, check whether the module was built for Electron's Node ABI (look at `node_modules/<pkg>/prebuilds/<platform>/`). If yes, that's the cause — don't rebuild, just stop trying to test through that import boundary. (5) Document this in the project README / CONTRIBUTING.md so the constraint is discoverable: "Database-level tests are not viable due to better-sqlite3 ABI mismatch under Node-environment Vitest. Test pure functions; rely on Playwright + manual smoke for DB paths." (6) Reference: 2026-05-06 — `D:\PMO_OS\src\main\trpc\router.test.ts` deleted after `npx vitest run` returned `Error: ERR_DLOPEN_FAILED { code: 'ERR_DLOPEN_FAILED' }` on all 7 tests. Decision documented in L098 Day 2 commit `60488bc` message: "Router-level vitest skipped: better-sqlite3's prebuilt binary is Electron-ABI not Node-ABI". Companion to the codebase pattern at `src/main/trpc/router.ts` (1400+ lines, zero unit tests) — by design.

704. **Cross-tab modal in a one-tab-rendered-at-a-time SPA: mount the modal once at the App-shell root, NOT inside any individual tab's component tree. Tab components only render when their tab is active (`{activeTab === 'foo' && <Foo />}`), so any cross-cutting overlay mounted inside Foo unmounts when the user switches away. Trigger the modal via a global `window.dispatchEvent(new CustomEvent('app:request-modal-X'))`; the modal listens with `useEffect(() => { window.addEventListener(...); return () => window.removeEventListener(...); }, [])` and opens itself on receipt. Both the original tab's button AND any other tab (Settings, Help, etc.) can dispatch the same event and reach the same modal — single source of truth, zero prop-drilling, zero shared-state plumbing** — On 2026-05-06 during the L098 Day 3 build, the "🔄 Refresh from Outlook" button on the Portfolio Status tab needed to be reachable from BOTH the Portfolio Status header AND the Settings tab's "Outlook Narratives Refresh" card. Since `App.tsx` renders only the active tab (`{activeTab === 'portfolio-status' && <PortfolioStatus />}`, etc.), naive options were: (a) Lift modal state up to AppShell and prop-drill setRefreshOpen down through PortfolioStatus AND Settings — ugly, fragile, requires both consumer tabs to be aware of the modal's existence. (b) Use a context provider — slightly cleaner but still requires every consumer to import the context, and the provider must wrap both tabs. (c) Use a global event bus — single dispatcher line on each consumer side, single listener in the modal, no shared state at all. Option (c) won. Implementation: `<NarrativesReviewModal />` is mounted ONCE in `AppShell` at the bottom of the JSX tree (after `<main>`), so it persists across tab switches. The modal owns its own `open` state and listens for `pmo-os:request-narratives-refresh`. PortfolioStatus's button: `onClick={() => window.dispatchEvent(new CustomEvent('pmo-os:request-narratives-refresh'))}`. Settings card's button: same single line. The modal also dispatches `pmo-os:outlook-refresh-config-changed` after success so the Settings card (which displays `lastRefreshedAt` from localStorage) re-reads its config without a tRPC roundtrip. Total cross-tab plumbing: 2 event names, 4 dispatch lines, 2 listener hooks. Zero context, zero prop-drilling, zero useState lifted to App. **Why CustomEvent beats other inter-component communication in this case:** (a) The relationship is fan-in: many sources can request the modal, one modal handles. CustomEvent is exactly that shape — N senders, M listeners, async. (b) The data being sent is "open the modal NOW" — no payload required. CustomEvent's `detail` field can carry payload if needed (`new CustomEvent('foo', { detail: { ... } })`), but for a pure trigger you don't even need it. (c) No shared types or imports — both sides only know the event name string. The modal can move, the buttons can move, types can change, as long as the event name is stable. (d) No re-render cost — the listener hook subscribes once at mount; dispatching doesn't trigger React re-renders unless the listener calls setState. (e) Built into the platform — no library, no provider, no boilerplate. **When NOT to use CustomEvent:** if you need request/response semantics (button clicks → modal returns data), use a Promise-returning helper or context. If the data being passed is large or structured, prefer Zustand / Redux / context — CustomEvent's detail field is OK for primitives but not great for object trees. **How to apply:** (1) For "Open this modal from anywhere" features in a tab-switched SPA, mount the modal at the AppShell root and use CustomEvent for triggering. Choose a namespaced event name like `app:request-X` to avoid collisions. (2) Always pair `addEventListener` with the matching `removeEventListener` in useEffect cleanup. Forgetting causes leaks across remounts. (3) Use `CustomEvent` not `Event` so future-you can add a `detail` field without breaking existing dispatchers. Even if you don't need detail today, the type signature stays compatible. (4) For TypeScript type-safety on event names, declare a global type augmentation: `declare global { interface WindowEventMap { 'app:request-modal-X': CustomEvent<XPayload> } }`. Then `addEventListener('app:request-modal-X', (e) => e.detail.foo)` is fully typed. (5) For the modal-firing-back-to-other-components pattern (e.g., "I just saved, please refresh your config"), dispatch a SECOND event with a different name. Don't try to multiplex: one event per logical action. (6) Reference: 2026-05-06 — `NarrativesReviewModal.tsx` mounted at `App.tsx:181`. PortfolioStatus button at line 535. Settings card subcomponent at line ~1430. Two events: `pmo-os:request-narratives-refresh` (open) and `pmo-os:outlook-refresh-config-changed` (post-save broadcast). Companion to lesson #766 (use-click-log telemetry pattern) — both leverage window-global helpers to keep cross-component plumbing minimal.

705. **When committing across multiple sibling repos in the same workspace (e.g., `D:\PMO_OS\` is one git repo, `D:\Project Management\` is another, both touched by the same feature build), ALWAYS run `git status` AND `git log --oneline -3` AND `git diff --stat HEAD` in EACH repo before any commit. Discrepancies between "what I think the working tree contains" and "what is actually staged" can be massive when sibling repos have accumulated uncommitted work over weeks. Fixing this after a `git add -A` + commit is painful (forensic line-counting + `git restore --staged` + reconstructing intent). Catching it before is one extra command** — On 2026-05-06 evening during the L098 Day 1 commit, the PMO_OS repo was at HEAD `6bb609f` (v2.0.9) with my new `narratives-fetch.ts` + `parse-digest-md.ts` files staged for commit. Clean. But I ALSO touched `D:\Project Management\Outlook\Run-Update-Server.ps1` to add 3 new HTTP endpoints — that file lives in a SEPARATE git repo (`D:\Project Management\`, the PM workspace repo). Naive `git add Outlook/Run-Update-Server.ps1 && git commit` in that repo would have bundled MY 250-line endpoint addition with ~1,000 lines of Mahmoud's pre-existing uncommitted Phase 4-8 endpoint work that had NEVER been committed since `69ce5f1` (the only prior commit on that file: "workspace: bulk-add sources + deliverables"). HEAD version: 287 lines. Working tree (post-my-edit): 1,494 lines. The `git diff --stat HEAD` showed `1,289 insertions(+), 41 deletions(-)` — way more than my edit could possibly explain (~250 lines). That single discrepancy line caught it: my edit shouldn't have triggered 1,000+ insertions, period. Recovery: `git restore --staged Outlook/Run-Update-Server.ps1`, slice out my added lines via PowerShell line-range deletion (`Set-Content -Value $lines[0..1219] + $lines[1475..]`), commit the pre-existing Phase 4-8 work as a clean "sync" commit (`795b02f` — 984 lines), then `cp` my edit back from the `D:/tmp` backup, commit my 265 lines as L098 (`23f6ffd`). Two clean linear commits where there could have been one ugly bundled mess. **Why `git diff --stat HEAD` is the load-bearing check:** (a) `git status` only shows file-level changes, not magnitude. A file shown as `M` could have 1 line changed or 1,000. (b) `git log --oneline -3` shows what's IN the repo's history, not what's pending — useful for context but not for catch-the-bundle. (c) `git diff --stat HEAD` shows per-file insertion/deletion counts AND the total. For each file you intend to commit, predict the magnitude based on what you actually edited; reality should match. Mismatches by >2× are the early-warning signal. (d) In multi-repo workspaces, this check needs to run in EACH affected repo, not just the one you're "actively working in." Memory of "I only touched X" is not a substitute for the diff. **Why the recovery is harder than the prevention:** (a) Once you've committed a bundle, your options are git-rewrite (interactive rebase to split, requires careful patch picking) or accepting the bundle (history is cluttered, blame is wrong, future bisects find the wrong commit). (b) Splitting a single commit into two CLEAN commits requires understanding which lines belong to which logical change — easy when you wrote it minutes ago, hard a week later, impossible months later. (c) The PM repo had no remote, so an over-bundled commit would have been local-only embarrassment. With a remote, `git push` makes the bundle public — `git reset --hard` + force-push is a much heavier hammer. **How to apply:** (1) Before EVERY commit, run all three: `git status -sb`, `git log --oneline -3`, `git diff --stat HEAD`. Read the diff-stat magnitudes and ask "does this match what I edited?" (2) For multi-repo workspaces (any case where one feature touches files in different `.git` roots), enumerate the affected repos at the START of the work, not at commit time. Quick check: `for d in repo1 repo2; do echo "=== $d ==="; cd $d; git status -sb; done`. (3) When a diff-stat surprises you, STOP. Don't `git add` anything. First understand what's in there. Common cases: (a) someone else's WIP from days/weeks ago that no one committed, (b) auto-generated files that should be `.gitignore`d (build artifacts, lock files, IDE configs), (c) line-ending normalization (CRLF↔LF) flipping every line in a file. (4) If you find pre-existing uncommitted work in a sibling repo, ask the user "should I commit your pending changes first as a sync commit, then add mine separately, OR bundle them together?" — don't decide silently. (5) Use `git stash --keep-index` or branch-and-stash if you need to temporarily isolate your changes from someone else's WIP. Always restore the stash to the original branch when done. (6) For pre-commit hooks specifically: write a hook that warns when a single commit's diff-stat magnitude exceeds a threshold (e.g., >500 lines in a non-merge commit). The threshold catches the "accidentally swept up other work" case. (7) Reference: 2026-05-06 — PM repo `Outlook/Run-Update-Server.ps1` had 1,200 lines of uncommitted Phase 4-8 endpoint work since the May 1-5 build cycle. My L098 endpoint addition would have been bundled with it had I not run `git diff --stat HEAD` and noticed the 1,289 insertion count. Split into two commits: `795b02f` (Phase 4-8 sync, 984 lines) + `23f6ffd` (L098 endpoints, 265 lines). Companion to lesson #699 (cherry-pick-not-merge for clean release) — both are about commit hygiene in multi-agent / multi-repo setups where "what's in your working tree" diverges from "what you actively wrote."

## Weekly Done Closer v2/v3 small-tool patterns (May 7, 2026)

706. **The `security_reminder_hook.py` PreToolUse hook in the user's harness blocks `Edit` calls whose `new_string` contains the inner-HTML assignment idiom (the `.inner` + `HTML =` + opening tag substring) even when the inserted markup is hand-escaped via a custom `escText()` helper. The hook is a regex match on the literal token; it does NOT inspect whether the content is escaped or where the data originates. The fix is to switch to safe DOM construction (`createElement` + `textContent` + `appendChild`) — same end result, zero escape concerns, hook passes silently. For dropdown / option rebuilds the canonical pattern is: `while (sel.firstChild) sel.removeChild(sel.firstChild)` to clear, then a loop that creates each option element via `createElement('option')` and assigns `.value` + `.textContent` before `appendChild`** — On 2026-05-07 while building Weekly_Done_Closer_v3.html (Pearl Pixels weekly closure tool), I needed to populate a new select element (id `close-filter-area`) Area-filter dropdown from `STATE.closeAll`. First attempt was the conventional pattern: build a string of option markup with hand-escaped values, then assign that string to the select's inner-HTML property. PreToolUse blocked the edit with `⚠️ Security Warning: Setting innerHTML with untrusted content can lead to XSS vulnerabilities. Use textContent for plain text or safe DOM methods for HTML content. If you need HTML support, consider using an HTML sanitizer library such as DOMPurify.` Even though the only "untrusted" data was Azure DevOps Area Path strings (server-controlled, alphanumeric + backslash), the hook fired on the literal token. Trying to argue with the hook (e.g., "but I escaped it") is futile because the hook never reads the escape function. Trying to add `// nosec` style comments doesn't work either — the hook is regex-based, not AST-based. The right move is to rewrite using DOM methods: clear via the `while firstChild → removeChild` idiom, then a forEach that creates each option via createElement, assigns `.value` and `.textContent`, and calls `appendChild`. Hook passes, output is identical, and the code is actually safer (textContent never interprets HTML, so even if a future Area Path contained a script tag, it would render as the literal string). **Why the hook is right even when you "know" the data is safe:** (a) "Server-controlled" data can become user-controlled later when someone adds a feature that lets users edit Area Paths in DevOps. The XSS surface moves silently. (b) Defensive coding via DOM methods removes the entire class of bug — no future audit needed. (c) The string-assignment approach carries other costs: it triggers a parse pass, breaks event listeners attached to the old children, and is slower than `appendChild` for small lists. The "fast" perception is wrong below ~50 children. (d) Reviewers reading the string-assignment idiom have to mentally trace whether every interpolated value is escaped; reviewers reading `createElement + textContent` know it's safe by construction. **When the string-assignment idiom is genuinely needed:** complex template rendering (dozens of nested elements per item), inserting trusted server-rendered HTML (CMS-style content), or one-shot static markup with no user-derived data. Even then, prefer `<template>` elements + `cloneNode` over string concatenation. **How to apply:** (1) Default to `createElement` + `textContent` + `appendChild` for any list-rebuild that includes data from anywhere outside the file's own source code. (2) For very large lists where DOM operations become a bottleneck, use a `DocumentFragment`: build all children inside the fragment, then `parent.appendChild(fragment)` once — single reflow, same safety. (3) If you need HTML structure (e.g., an option element with a nested span for a badge), build that with `createElement` too — never reach for the string-assignment idiom just to nest one element. (4) When the hook fires, don't argue or try to bypass — the rewrite is usually 6-8 lines and faster than negotiating. (5) Reference: 2026-05-07 — `D:\DevOps\Weekly tasks Clouser\Weekly_Done_Closer_v3.html` line 691, function `refreshAreaFilterOptions()`. The hook block fired at the exact moment of the Edit call; switching the pattern made the second Edit attempt pass silently. Companion to lesson #587 (paired-span bilingual rendering) — both are "the cleaner DOM pattern is also the safer one."

707. **The "last day of the current month at HH:MM" default for a `datetime-local` input is one line: `new Date(year, monthIndex + 1, 0)`. Day 0 of the next month is, by JavaScript Date arithmetic, the LAST day of the requested month — automatically correct for 28/29/30/31-day months and leap years. Format as `YYYY-MM-DDTHH:MM` with `String(...).padStart(2, '0')` on month/day to satisfy the input's strict format. Set the default in JS at init, NOT as a hardcoded `value=` attribute, so the value re-computes whenever the page is opened in a new month** — On 2026-05-07 the Weekly Done Closer tool's Closure Date input had a hardcoded `value="2026-04-30T16:15"`. The user asked for the default to be "the last day of the current month" with examples: June → 31-6 (loose phrasing — actually June 30), October → 31-10 (October 31). The cleanest implementation is a 10-line helper that takes optional `now` (defaults to `new Date()`), pulls year and month index, computes the last day via `new Date(y, m + 1, 0)`, then template-literals the `YYYY-MM-DDT16:15` string with padStart on the components. Then in the init block: `document.getElementById('close-date').value = lastDayOfCurrentMonthLocal();`. The `new Date(y, m+1, 0)` trick works because the Date constructor's day argument is 1-indexed and accepts overflow/underflow that wraps into adjacent months — passing `0` for the day means "one day before day 1 of (m+1)", which is the last day of month `m`. This is more idiomatic than the manual approach (a 12-element month-length array plus a leap-year `if`), handles February correctly without thinking about it, and is shorter than any library solution. **Why set the default in JS init, not in the HTML attribute:** (a) An HTML `value=` attribute is a static string baked at file-write time. The moment the calendar rolls past that month, the default is wrong and the user sees a stale date every time. (b) JS init runs on every page load, so the default re-computes daily. The user opening the tool on May 15 sees `2026-05-31T16:15`; opening on June 3 sees `2026-06-30T16:15`. Zero file edits needed. (c) The user can still override the default by typing a different date — JS init only runs once and only sets the initial value. (d) For tools with offline/snapshot reproduction needs (this tool's seed dataset re-renders the Apr 30 audit), the date default doesn't affect the seed itself — it only affects new closures going forward. **Why `T16:15` and not end-of-day:** the original hardcoded value was `T16:15`, which matches Pearl Pixels' weekly closure ritual time (end of the workday before the team logs off). Preserving the time portion keeps the workflow consistent — the user picked 16:15 for a reason (it's when the closure email goes out), so the default should respect that. End-of-day (`T23:59`) would be wrong because the tool stamps `Custom.ClosureDate` and that field is read by reporting workflows that expect business-hour timestamps. **How to apply:** (1) For any `datetime-local` input whose default should follow a calendar rule, set the value via JS at init. The HTML attribute should be omitted entirely. (2) For "last day of month" specifically, use `new Date(y, m+1, 0)` — don't hand-roll a month-length lookup table. (3) For "first day of next month" use `new Date(y, m+1, 1)`. For "last day of last month" use `new Date(y, m, 0)`. For "N days from today" use `new Date(d.setDate(d.getDate() + N))` (note: `setDate` mutates and returns the timestamp, so wrap in a fresh Date if you need the original preserved). (4) Always `padStart(2, '0')` on month and day when serializing to `YYYY-MM-DD` — the input parser is strict and rejects single-digit months/days. (5) For `datetime-local` use `T` separator and 24-hour `HH:MM`; do NOT include seconds unless the input has `step="1"` (otherwise the value silently truncates and the input shows empty on round-trip). (6) Time zone: `datetime-local` is naive — no TZ offset. The string represents wall-clock time in the user's local zone. Don't try to inject `Z` or `+03:00` — the input rejects it. If you need UTC stamping for the API call, convert at PATCH time, not at default time. (7) Reference: 2026-05-07 — `D:\DevOps\Weekly tasks Clouser\Weekly_Done_Closer_v3.html` line 487 helper, line 1148 init wiring. v2 had hardcoded `value="2026-04-30T16:15"` (line 247); v3 dropped the attribute and computes via JS. Companion to lesson #225 (auto-populated KR dates from DevOps iterations) — both are "compute defaults from live state, never bake them into source."

708. **Sortable table column header pattern: cycle through three states (none → asc → desc → none) on click, NOT two-state toggle. The third state lets the user return to the original render order without reloading. Use `localeCompare(b, undefined, { numeric: true, sensitivity: 'base' })` so "Sprint 2" sorts before "Sprint 10" (numeric: true) and "alpha" matches "ALPHA" (sensitivity: 'base'). Empty/missing values bubble to the bottom regardless of sort direction (return +1 for missing-on-A, -1 for missing-on-B, 0 for both-missing) so the user always sees real data first** — On 2026-05-07 building the Weekly Done Closer Close-Done tab Area-column sort. The naive two-state cycle (asc/desc only) traps users: once they sort, they can't get back to the original "as-fetched" order without reloading the page or clicking another column to reset. The three-state cycle adds `null` as a real state — clicking a third time clears the sort and re-renders in the underlying array's natural order. Implementation: `STATE.closeSort = STATE.closeSort === 'area-asc' ? 'area-desc' : STATE.closeSort === 'area-desc' ? null : 'area-asc';` then re-render. The render function checks if the sort state is one of the two active values and only sorts in those branches — when null, it skips the `.slice().sort()` call entirely, preserving array order. The header arrow updates: `↕` for none, `▲` for asc, `▼` for desc. The header element gets `.sort-asc` / `.sort-desc` classes via `classList.toggle(name, condition)` so CSS can color the arrow blue when active. **Why localeCompare with numeric: true matters:** plain string compare puts "Sprint 10" before "Sprint 2" because '1' < '2' lexically. `numeric: true` treats embedded digit runs as numbers — "Sprint 2" < "Sprint 10" < "Sprint 100", which is what users expect for sprint names, version numbers, and ID-suffixed strings. `sensitivity: 'base'` makes the compare case-insensitive and accent-insensitive — "Pearl Pixels" sorts next to "pearl pixels" instead of in a separate run. These two options together handle 95% of real-world tabular data without edge-case hacks. **Why missing values bubble to bottom regardless of direction:** if you sort ascending, empty strings would naturally sort first (empty < anything). If you sort descending, they'd sort last. Both behaviors surprise users — they want to see populated rows first when scanning a list. Force missing-to-bottom in BOTH directions by checking emptiness BEFORE delegating to localeCompare: `if (!av && !bv) return 0; if (!av) return 1; if (!bv) return -1; return av.localeCompare(bv, ...) * dir;`. The `* dir` only applies when both values are real, so missing rows always trail. **Why `slice().sort()` not `sort()` directly:** `Array.prototype.sort` mutates the original array. If the source array is the canonical state (e.g., `STATE.closeAll`), in-place sorting corrupts the state and breaks "clear sort" because there's no original order to restore. Always `.slice().sort()` to get a sorted copy and leave the source alone. **How to apply:** (1) For any clickable column header, three-state cycle (asc → desc → none) is the default. Two-state is acceptable only when the natural order has no meaning (e.g., a hash-keyed list with random insertion order). (2) Always `.slice().sort()` — never sort the source array. (3) `localeCompare(b, undefined, { numeric: true, sensitivity: 'base' })` is the right baseline for human-readable text columns. For pure-number columns use `(a-b)`. For date columns use `new Date(a) - new Date(b)`. (4) Empty/null values bubble to bottom in both directions; do this with explicit checks before localeCompare. (5) Reflect the sort state in the header: arrow character (`↕` `▲` `▼`) AND a CSS class (`.sort-asc` / `.sort-desc`) so the styling can show the active column distinctly. (6) Make the header element `cursor: pointer` and add a `:hover` background; pair with `-webkit-user-select: none; user-select: none;` so click-dragging the header doesn't accidentally select text. The `-webkit-` prefix is needed for Safari per IDE diagnostic — don't drop it even if the warning seems pedantic. (7) Reference: 2026-05-07 — `D:\DevOps\Weekly tasks Clouser\Weekly_Done_Closer_v3.html` lines 113-117 (CSS), 266 (sortable th), 705-721 (sort + arrow update in renderCloseTab), 1156-1162 (click handler with three-state cycle). v2 sorted by Iteration; v3 still sorts by Area (the user corrected the column mid-build — leave v2 as the historical "wrong column" build since the rule cycle shipped v2 already). Companion to lesson #587 (paired-span bilingual) — both are "the obvious UX pattern is also the right one once you've thought about edge cases."

---


## PMO_OS Portfolio Status Auto-Refresh — Day 4 e2e + ship (May 7, 2026)

711. **In Electron, a hardcoded `<title>...</title>` element in `index.html` overrides the BrowserWindow constructor's `title:` option as soon as the renderer's HTML loads. The BrowserWindow `title:` only sets the OS-level window title BEFORE navigation; once Chromium loads the page, it auto-updates the window title from whatever `<title>` says. Result: changes to `electron/main.ts createMainWindow({ title: ... })` work for ~50ms then get clobbered by the stale string baked into your HTML. Fix: leave the `<title>` element with a NON-VERSIONED string (e.g., `<title>PMO OS</title>`) and set `document.title = 'App v' + liveVersion` from React useEffect at app mount** — On 2026-05-07 during the L098 ship-gate visual check, the user reported the OS-level window title still showed `PMO OS v2.0` despite my version-fix commit (`f76d3d3`) that added `additionalArguments=['--app-version=' + app.getVersion()]` and made `electron/main.ts` use `title: 'PMO OS v' + app.getVersion()`. The in-app header subtitle correctly read `v2.0.10 — 19/19 tabs` (proving `app.getVersion()` returned '2.0.10' AND the additionalArguments path worked AND the React i18n interpolation worked), but the title bar still showed `v2.0`. Diagnosis: `index.html:7` had `<title>PMO OS v2.0</title>` hardcoded since Phase A. When Chromium loaded `http://localhost:5173/` (Vite-served `index.html` in dev mode), the page's `<title>` element took over the window title — overriding whatever Electron's BrowserWindow had set. Fix: changed `index.html` to `<title>PMO OS</title>` (no version) AND added `useEffect(() => { document.title = 'PMO OS v' + window.api.version; }, [])` in `App.tsx` so the title is set from JS at mount, after `window.api.version` is populated from the additionalArguments-fed preload. **Why this is a category of bug, not a one-off:** ANY Electron app that has BOTH a BrowserWindow `title:` config AND a `<title>` element in its loaded HTML has this collision. The window title behaves differently than every other Electron config option — it's NOT one-shot; it's a continuous binding to the page's `<title>` once HTML loads. Most devs only encounter this when they later add a dynamic version + are surprised the title doesn't update. **How to apply:** (1) For Electron apps where the window title should reflect dynamic state (version, current tab, document name, etc.), DON'T set BrowserWindow `title:` to a versioned string — set it to a generic placeholder like `'My App'`. The renderer will overwrite it within milliseconds anyway. (2) Set `document.title` from JavaScript at the appropriate React lifecycle point. For app version (set once at mount), use a top-level `useEffect` with empty deps. For per-tab titles (Setup wizard / Settings / etc.), update `document.title` in each tab's mount hook. (3) For non-React apps, set `document.title` after page load via a script in `<head>` or `<body>`. (4) The HTML `<title>` element should reflect what you want the title to say BEFORE the JS runs (the brief flash of static text during page load). Make that string match the most-likely-correct value (e.g., `PMO OS` without version is fine — better than the stale `v2.0`). (5) Tools that scrape page metadata for indexing/sharing (browsers' tab list, Windows taskbar tooltip) read whatever document.title currently is — so this fix benefits more than just the title bar. (6) Reference: 2026-05-07 PMO_OS v2.0.11 ship — `D:\PMO_OS\index.html` line 7 changed + `D:\PMO_OS\src\App.tsx` useEffect added in commit `bfe9f55`. Companion to lesson #750 (window title via `app.getVersion()`) and the v2.0.9 / L096 fix that pulled the title from `app.getVersion()` — three surfaces, three different rendering paths, all needing independent wiring to stay in sync with `package.json`.

712. **`npm run dev:all` does NOT recompile electron-side TypeScript. Vite hot-reload covers the renderer (anything under `src/`) but the main process (`electron/`) needs an explicit `npm run build:electron` (which runs `tsc -p electron/tsconfig.json`) BEFORE dev:all picks up the new `dist-electron/` output. Forgetting this is a major debugging time-sink: your renderer changes appear to work but your main-process changes don't, leading you to chase ghosts in fully-correct source code. ALWAYS rebuild electron-side after editing `electron/main.ts`, `electron/preload.ts`, `electron/ipc/*.ts`, `electron/lib/*.ts`. The signal for needing a rebuild: any change in `electron/` directory that you expect to see take effect at runtime** — On 2026-05-07 during the L098 e2e smoke, the user ran `npm run build:electron && npm run dev:all` after my version-fix commit, then reported that the in-app header subtitle worked correctly (showing `v2.0.10`) but the modal STILL showed em-dash mojibake. Diagnosis: by checking `dist-electron/electron/ipc/devops-handlers.js` last-write-time (`5/7/2026 1:41:11 PM`), I saw the rebuild had happened — but BEFORE my UTF-8 fix commit (`335df68`) was made. The rebuild snapshot was the version-fix-only state. After committing the UTF-8 fix, neither `npm run dev:all` nor a Ctrl+R refresh in the Electron window picks up changes to compiled main-process code. The user had to: (1) Ctrl+C the dev:all session, (2) run `npm run build:electron` again, (3) re-launch `npm run dev:all`. **Why this trips experienced devs:** (a) Vite's reputation as 'hot-reload everything' creates an expectation that all .ts files are watched — it isn't true; Vite only watches files imported by the renderer entry. (b) The `concurrently -k` orchestrator in `dev:all` runs `dev:electron` (`wait-on tcp:5173 && electron .`), and `electron .` reads the COMPILED `dist-electron/electron/main.js` — not the source `.ts`. Electron has no native TypeScript runtime. (c) Renderer-side files like `App.tsx`, `index.html`, `i18n/locales/*.json` DO hot-reload via Vite. So mixed feature edits (some files in `src/`, some in `electron/`) lead to a confusing partial-refresh state where the renderer changes are visible but main-process changes aren't. **How to apply:** (1) Memorize: dev:all = renderer hot-reload only. ANY `electron/` edit needs explicit `npm run build:electron` + Electron-process restart. (2) When debugging 'why didn't my fix take effect?', FIRST check the timestamp of the relevant compiled file in `dist-electron/`: `Get-ChildItem dist-electron\electron\foo.js | Select-Object LastWriteTime`. If older than your last edit to `electron/foo.ts`, that's the bug — rebuild. (3) Add a watcher script for the iteration-heavy phases: `tsc -p electron/tsconfig.json --watch` in a third terminal alongside `dev:all`. Auto-rebuilds on save; you still need to restart the Electron app via Ctrl+R inside the window OR a full restart for some main-process changes. (4) Document this in the project README under a `## Iterating on main-process code` heading so new contributors don't lose hours. (5) For the cleanest workflow: bind `npm run dev:all` to ALWAYS pre-build electron via a `prebuild:dev:all` hook or a chained command. Costs ~3 seconds at startup, prevents the entire class of bugs. (6) Reference: 2026-05-07 — `dist-electron/electron/ipc/devops-handlers.js` was at 13:41 timestamp from the version-fix-only build; my UTF-8 fix in commit `335df68` shipped after that build. User had to rebuild + restart to get the encoding fix into the running Electron.

713. **When debugging UTF-8 encoding bugs, NEVER trust what the terminal renders. Windows PowerShell + cmd.exe + most terminal emulators decode UTF-8 bytes as Windows-1252 (CP1252) at DISPLAY time, even when the underlying string in memory or in JSON or in a file is correctly UTF-8. So a string containing the real em-dash character (U+2014, three UTF-8 bytes 0xE2 0x80 0x94) will render as 3-char mojibake in PowerShell stdout — looking IDENTICAL to a string that's actually corrupted. Always pattern-match against the JS string itself (via DevTools console, CDP eval, or a unit test), NOT what the terminal shows. The diagnostic regex against the actual string content gives a definitive yes/no. Terminal output is for humans; pattern-matching is for software** — On 2026-05-07 during the L098 CDP smoke, my automated test script printed sample text from a digest's narrative textarea to PowerShell. The terminal showed `Sprint 2 active (ends Jun 5 [3-char-mojibake] 38 days away)`. My instinct was 'still mojibake — encoding fix didn't land'. But the same script reported `mojibake count: 0` and `em-dashes found: 2` after pattern-matching against the actual JS string content. Resolution: PowerShell's stdout codepage was treating the em-dash UTF-8 bytes as CP1252 for VISUAL rendering only — the application data was correct (single em-dash character), the smoke token round-tripped to SQLite, the regex confirmed no mojibake in memory. The 'visual mojibake' was a terminal display artifact. The user's earlier report of mojibake in the modal textarea was REAL (data layer issue), but my smoke output's mojibake was FALSE (display layer). Without the regex assertion in the smoke script, I would have wasted hours chasing a non-bug. **Why this is one of the trickiest UTF-8 bugs to diagnose:** (a) The visual symptom is identical for the real bug and the false alarm — both look like mojibake to your eyes. (b) Most stack-trace tools / error messages get rendered through the same broken codepage, so even your debug output lies. (c) Console codepage (`chcp` on Windows) defaults to 437 (US English OEM) or 1252 — neither is UTF-8. PowerShell 7+ has better defaults but Windows PowerShell 5.1 (the OS-bundled one) is CP1252. (d) Setting the codepage to UTF-8 (`chcp 65001`) only affects future output; existing terminal buffer stays mis-rendered. (e) Even after `chcp 65001`, font rendering may still fall back to the wrong glyph for some Unicode chars. **How to apply:** (1) When diagnosing UTF-8 issues, ALWAYS write a test that pattern-matches the actual string content. PowerShell: `if ($str -match '—') { 'has em-dash' }`. JavaScript: `str.match(/—/g)`. Python: `'—' in s`. The string literal in the script is your source-of-truth comparison. (2) If you must visually inspect text in a terminal, run `chcp 65001` first AND verify with a known-good UTF-8 sample: `Write-Host '—'` should display ONE em-dash, not three garbage chars. If it still mojibakes, the font is the problem (use Cascadia Code or a font with em-dash support). (3) For HTTP debugging, use `Invoke-WebRequest` then read `.RawContentStream.ToArray()` and check raw bytes against expected UTF-8 sequences. PowerShell: `$bytes = $r.RawContentStream.ToArray(); [System.Text.Encoding]::UTF8.GetString($bytes).Contains([char]0x2014)`. (4) For testing in Node/JS: log the string's `.length` AND its char codes: `[...str].map(c => c.codePointAt(0))`. An em-dash is codepoint 8212 (0x2014); mojibake is 3 separate codepoints (0x00E2, 0x20AC, 0x0022). (5) Don't over-correct: if you've already confirmed the data is clean via pattern-match AND your downstream consumer (browser textarea, SQLite, etc.) shows correct rendering, the terminal's display issue is harmless. Don't invent a 'bug' that isn't there. (6) Reference: 2026-05-07 — `D:\PMO_OS\scripts\smoke-narratives-modal.cjs` reported `mojibake count: 0` while sample stdout showed visual mojibake. The smoke verdict (clean) was correct; the visual artifact was misleading. The real-mojibake → terminal-mojibake distinction saved ~30 minutes of false debugging. Companion to lessons #701 (file encoding), #702 (PS ConvertTo-Json hang), and #709 (undici fetch charset trap) — but THIS lesson is specifically about diagnostic discipline, not the bug itself.

714. **Node's built-in fetch (powered by undici) consults the response Content-Type charset for `res.text()`, but in some server-and-payload combinations slips into latin-1 / CP1252 even when the server explicitly sends `Content-Type: application/json; charset=utf-8`. The result: multi-byte UTF-8 sequences (em-dash, Arabic, smart quotes) come out of `res.text()` as 3-codepoint mojibake strings. Force-decode via `await res.arrayBuffer()` + `new TextDecoder('utf-8').decode(buffer)` to bypass undici's charset detection entirely. Same fix benefits any non-ASCII content from any HTTP source — Azure DevOps responses with Arabic project names, Outlook subject lines with smart quotes, etc.** — On 2026-05-07 during the L098 e2e smoke, the user reported mojibake in every textarea of the NarrativesReviewModal. Wire-side diagnosis confirmed: PowerShell server's response body bytes were correct UTF-8 (real em-dash 0xE2 0x80 0x94), Content-Type header was `application/json; charset=utf-8`. So the bug was on the renderer-side decode path. Code review of `electron/ipc/devops-handlers.ts proxyFetch()` showed `const body = await res.text();` — relying on undici to honor the charset. Fix: replaced with `const buffer = await res.arrayBuffer(); const body = new TextDecoder('utf-8').decode(buffer);`. Post-fix smoke confirmed em-dashes round-trip cleanly through the IPC bridge to the modal. **Why undici slips into latin-1:** (a) The Fetch spec says implementations 'should' use the charset from Content-Type, but 'should' is RFC-2119 weak. Several edge cases trigger fallback: missing/malformed charset parameter, ambiguous parameter casing, multipart responses without per-part charset. (b) Undici's `res.text()` internally calls `Buffer.toString(encoding)` where `encoding` is auto-detected. The fallback chain is utf-8 → latin-1 → ISO-8859-1, with subtle conditions for when each fires. The PowerShell server's ConvertTo-Json output triggered the latin-1 fallback for some PS 5.1 quirk (possibly the order of `Content-Length` vs `Content-Type` in headers, possibly the lack of explicit `charset=utf-8` boundary). (c) The bug is non-deterministic across HTTP servers — works for Azure DevOps API, fails for the local PS server. So it can lurk for months without surfacing. **Why force-decode is the right fix:** (a) `arrayBuffer()` returns raw bytes — no decoding involved. (b) `new TextDecoder('utf-8').decode(...)` ALWAYS uses UTF-8 regardless of Content-Type. (c) For our use case (proxying JSON APIs that we control or trust), UTF-8 is correct 100% of the time. The Content-Type charset hint adds zero value and one bug surface. (d) Performance impact is negligible (~5% slower for large bodies; not measurable for typical API responses). **How to apply:** (1) For ANY Node fetch proxy / IPC bridge / SDK wrapper that handles diverse HTTP sources, default to `arrayBuffer + TextDecoder('utf-8').decode(buffer)`. Don't trust `res.text()`. (2) If you control the server and only ever return UTF-8, this is strictly safer. (3) If you must support non-UTF-8 (rare in modern APIs), branch on actual Content-Type: parse the charset parameter yourself, default to UTF-8 on absence/ambiguity, only use other charsets when explicitly specified. (4) For browsers (not Node), the same pattern works: `await fetch(url).then(r => r.arrayBuffer()).then(buf => new TextDecoder('utf-8').decode(buf))`. Browsers have different charset-detection behavior than Node, but force-decoding works the same. (5) Test for this regression with a unit test that fetches a payload containing non-ASCII (em-dash, Arabic, emoji) and asserts the decoded string contains the original characters via `string.includes(String.fromCharCode(0x2014))`. (6) Reference: 2026-05-07 — `D:\PMO_OS\electron\ipc\devops-handlers.ts proxyFetch()` line 50 changed from `await res.text()` to `arrayBuffer + TextDecoder('utf-8').decode` in commit `335df68`. Companion to lesson #701 (PS encoding) and #708 (terminal vs data) — completes the encoding-discipline arc on both ends of the IPC wire.

715. **Use Chrome DevTools Protocol (CDP) on Electron's `--remote-debugging-port=9222` to drive end-to-end smoke tests programmatically. Pattern: connect via `chrome-remote-interface`, find the renderer page (look for `localhost:5173` or `file://` in target URLs), then `Runtime.evaluate` arbitrary JS in the page context. You can dispatch CustomEvents to trigger UI flows, query the DOM for assertions, or call `window.api.trpc.invoke` to query/mutate via tRPC the same way the user's clicks would. The result is a 30-second automated walk-through that does what would otherwise be 10 manual click-and-verify steps for the user. Especially valuable for multi-screen modal flows where each manual step requires user-side description back to you for verification** — On 2026-05-07 during the L098 build's e2e gate, the manual modal walk-through was getting bogged down in screenshots and round-trips of 'click X → describe what you see → did Y appear?'. The user said 'make your check with yourself' — i.e., automate the verification. I built `D:\PMO_OS\scripts\smoke-narratives-modal.cjs` (~270 lines) using `chrome-remote-interface` to drive the entire flow: (1) connect to CDP on 9222, (2) snapshot pre-state via `window.api.trpc.invoke({ path: 'projectRegister.list', input: undefined, type: 'query' })`, (3) dispatch `new CustomEvent('pmo-os:request-narratives-refresh')` to open the modal (same event the header button fires), (4) poll DOM for `.fixed.inset-0 textarea` to detect modal-loaded state, (5) regex-assert em-dash count + mojibake count in textarea contents (encoding sanity), (6) click Accept checkbox via `card.querySelector('input[type=checkbox]').click()`, (7) edit textarea via React-compatible native setter + InputEvent dispatch (controlled inputs ignore plain `.value=` assignments), (8) click Apply via filtered button text match, (9) re-query pre-state and assert mutation persisted, (10) close modal. Total runtime: 6 seconds. Deterministic. Reusable for every future feature with a multi-step UI flow. **Why CDP beats Playwright for this use case:** (a) Playwright launches its own browser instance — useless for testing the running dev:all session. CDP attaches to the EXISTING Electron process, exercising the real running code. (b) Playwright e2e tests in this codebase live under `tests/e2e/` and run against a separately-launched Electron — useful for CI but heavyweight for one-shot validation. (c) CDP requires no fixture setup, no afterAll teardown, no test runner — just a script you can run while looking at the live app side-by-side. (d) The `Runtime.evaluate` API gives full JS access — read state, write state, dispatch events, query localStorage — which Playwright wraps but CDP exposes directly. **Reusable component patterns:** (1) **Connection boilerplate:** `const targets = await CDP.List({ host: '127.0.0.1', port: 9222 }); const renderer = targets.find(t => t.type === 'page' && t.url.includes('localhost:5173'));`. (2) **Eval-and-extract:** `await client.Runtime.evaluate({ expression, awaitPromise: true, returnByValue: true })` then check `result.result.value` (or `result.exceptionDetails`). (3) **React controlled-input editing:** `const setter = Object.getOwnPropertyDescriptor(HTMLTextAreaElement.prototype, 'value').set; setter.call(el, newVal); el.dispatchEvent(new Event('input', { bubbles: true }));`. (4) **DOM-ready polling:** `for (let i=0; i<60; i++) { if (await checkCondition()) return; await sleep(500); }` for waiting on async UI states. (5) **Cross-tab modal triggering:** dispatch the SAME CustomEvent the production button fires; covers the same code path. (6) **tRPC bridge access:** `window.api.trpc.invoke({ path, input, type })` returns `{ ok, data }` (project-specific shape — verify with a probe script first). **How to apply:** (1) For every new feature with a multi-step UI flow (modal, multi-page wizard, drag-drop builder), write a CDP smoke script alongside the implementation. Pays for itself within the first build cycle by replacing 5-10 manual user clicks. (2) Commit the script to `scripts/` so future regressions are catchable in one command. (3) For features that must work across tabs (cross-cutting modals, language toggles), CDP's ability to switch active tab + inspect state is exactly the right tool. (4) Add a probe script first if you don't know the response shape — e.g., `scripts/smoke-FEATURE-probe.cjs` that just dumps `Object.keys(out)` for the relevant API. Keeps the main smoke clean. (5) Don't run CDP smoke on production-built Electron — `--remote-debugging-port` is dev-only; production builds bind no port for security. Smoke is for `npm run dev:all` only. (6) Reference: 2026-05-07 — `D:\PMO_OS\scripts\smoke-narratives-modal.cjs` ran 7 assertions in 6 seconds end-to-end, replacing the user's manual Steps 4-10 walk-through. Apply mutation round-tripped to SQLite; smoke token recovered intact. Companion to lesson #708 — CDP eval lets you assert against actual JS string content, not terminal-rendered output. Net pattern: CDP smoke = the e2e ship-gate for any feature touching the renderer.

716. **When multiple parallel Claude sessions are working on the same project, L-counter values + version numbers + lesson IDs drift between sessions and CANNOT be reserved in advance. Before finalizing ANY L# tag, version bump, or lesson number, re-query the live source-of-truth: `git log --oneline -5` for the latest L#, `cat package.json | grep version` for the current version, `grep -E "^[0-9]{3}\." global_lessons.md | tail -3` for the latest lesson number. Numbers chosen at the start of a session are only valid until another session commits — treat them as predictions, not reservations. The corollary: NEVER write the L# / version into a lesson body, README, or other long-lived doc BEFORE the actual ship commit lands; either author with TBD placeholders (`L<TBD>` / `v<TBD>`) and patch post-ship, OR delay authoring until the commit hash is fixed** — On 2026-05-06 evening I prepared a per-tab emoji icon fix for PMO_OS sized for "L096 (v2.0.8 follow-up)" — the natural next slot after L095 had landed at v2.0.8. Mahmoud asked me to "wait and bundle with the portfolio-narratives auto-refresh work that's pending on the other branch", so I `git stash`'d the icon fix and stopped. While I waited, two other Claude sessions advanced master: (a) L096+L097 / v2.0.9 (env-separation hook + dynamic title bar), (b) v2.0.10 (backup-root relocate), (c) L098 / v2.0.11 (Portfolio Status auto-refresh from Outlook — multi-day feature spanning 8 commits all tagged L098). When Mahmoud said "bundle now" the next morning, my prepped L096 / v2.0.8 metadata was stale by THREE versions and TWO L-counter slots. The correct ship metadata turned out to be L099 / v2.0.12. Worse: I had ALREADY committed lesson #750 the previous evening with `L096` and `v2.0.8 follow-up` baked into its body — those references were now factually wrong and required a follow-up commit (`2f05564`) to patch. Total drift cost: one "verify before commit" step at the start of bundling (where I caught the drift via `git log master --oneline -5` showing `5c1ca29 L098 (v2.0.11)` as HEAD instead of the expected `6bb609f`) plus one cleanup commit on `D:\Global Lessons\global_lessons.md`. **Why this is a category, not a one-off:** (a) Stashes survive across sessions, branches, and reboots — they're a durable park mechanism. But the world around them moves. The longer you park, the more numbers drift. (b) The L-counter is a project-wide mutable counter; every session sees the same `master` and bumps independently. There's no "reservation" mechanism — first commit wins the slot. (c) Version numbers in `package.json` follow the same rule but with stronger blast radius — a published `v2.0.8` tag triggers CI builds and electron-updater fan-out. Predicting the wrong version doesn't just hurt accuracy, it can cause "phantom version" confusion if a tag is created prematurely. (d) Lessons that pre-reference upcoming commits ARE useful (writing the lesson while the context is fresh, NOT a day later) — but the L# / version metadata inside them is a forward reference that may break. **How to apply:** (1) At the start of any "let me commit and tag" sequence, FIRST run `git log master --oneline -5 && git stash list && cat package.json | grep version`. The first command catches L-counter drift, the second catches lost work, the third catches version drift. Do this even when "you just looked 10 minutes ago" — parallel sessions move faster than your intuition. (2) When authoring a lesson body for a fix that hasn't shipped yet, write the L# and version as `L<TBD>` and `v<TBD>` placeholders. Patch them ONCE the commit lands and the tag is pushed. The patch commit can be tiny (`lesson #N — patch L#/version refs after ship`), keeping history clean. (3) Stashes are durable but not labeled — name them with the branch context (`WIP <feature> pre-<reason>`) so future-you can tell what's parked. (4) When bundling a parked fix into a later release, treat the original prepped metadata as a draft, not a reservation — re-derive L# from `git log` and version from `package.json`. (5) For multi-session projects, consider documenting the "next L# = `git log -1 --format='%s' | grep -oP 'L\d+' | head -1` + 1" rule in CLAUDE.md so every session computes consistently. (6) Don't try to reserve numbers via empty placeholder commits ("L099 reserved for icon fix") — those create more drift problems than they solve. The right pattern is: ship soon → commit captures the live counter → no advance reservation. (7) Reference: 2026-05-07 — PMO_OS icon-restore fix shipped as v2.0.12 / L099 (commit `4e9d2c0`) after parking 1 day at "L096 / v2.0.8 follow-up". Lesson #750 patched in commit `2f05564` to correct the L#/version refs. The drift cost was negligible (one extra verify step + one trivial follow-up commit) — the parking pattern itself was sound and worth the wait for narrative-coherent release ordering. Companion to lessons #683 (audit-doc as cross-PR coordination), #694 (migration-audit as drift detector), and #695 (`app.getVersion()` for version propagation).

717. **Electron renderer windows DISABLE `window.prompt()` by default — calling it raises "prompt() is and will not be supported." even though `typeof window.prompt === 'function'` returns true. Same goes for `window.alert()` and `window.confirm()` in modern Electron releases. NEVER use any of the three in renderer code; build a tiny inline React modal instead. The "feature detection" check is misleading — the function exists on the window object but throws when invoked because Chromium's BrowserWindow context disables them as a security/UX policy** — On 2026-05-07 night Mahmoud installed PMO_OS v2.0.12 and clicked the Save Snapshot button on the Portfolio Status tab. The renderer threw `X prompt() is and will not be supported.` and the snapshot was lost. Root cause: the v2.0.4 ship of the Project Status History tab (lesson #751) added a Save Snapshot flow that asked for the user's snapshot label via `window.prompt(...)`. The code had a defensive `if (typeof window !== 'undefined' && typeof window.prompt === 'function')` guard, but that guard ALWAYS passes — `window.prompt` IS a function in Electron renderers; it's just that calling it triggers Chromium's security shim. Bug shipped silently from v2.0.4 → v2.0.12 (8 versions) because no test exercises it (UI-only path) and the defensive guard hid the issue from code review. Fix in v2.0.13: split saveSnapshot into two functions — `saveSnapshot()` opens an inline `SnapshotLabelModal` React component; the modal's `onConfirm(label)` calls `persistSnapshot(label)` which contains the original save logic. The modal pre-fills with `Bi-Weekly Review — YYYY-MM-DD`, supports Enter to confirm + Escape/click-outside to cancel, has an autoFocus input, full bilingual (3 new i18n keys: `snapshotLabelModalTitle`, `snapshotLabelModalSave`, `snapshotLabelModalCancel`). Existing `portfolioSnapshots` tRPC mutation contract unchanged — only the way the label arrives at the mutation changed. **Why this matters beyond Save Snapshot:** any other place in the codebase that calls `window.prompt/alert/confirm` is a latent crash. Quick audit pattern: `grep -rE "window\.(prompt|alert|confirm)\(" src/` and replace each with a React modal or `window.api.dialog.*` IPC if the modal is unwanted overhead. **Why the typeof check is misleading:** Chromium's renderer process exposes the function on `window` for browser-API compatibility, but the BrowserWindow context attaches a stub that throws. The function passes typeof checks, instanceof checks, length checks, and call-with-no-args checks — only invocation with the prompt-string argument throws. So you can't write a runtime-feature-test that distinguishes a "real" window.prompt from Electron's stub. **How to apply:** (1) Audit any web-app codebase you're porting to Electron for `window.(prompt|alert|confirm)` calls — they ALL break. (2) Replace each with a React-managed inline modal. The pattern is small: `useState<{ defaultValue: string } | null>(null)` for the open/close state, `onConfirm` callback that closes the modal + invokes the real save, `onCancel` callback that closes without saving. (3) Support Esc + Enter + click-outside in the modal — these are user expectations from native prompts. (4) AutoFocus the input so keyboard-only users can immediately type. (5) For bilingual apps, add 3 new i18n keys per modal: title, primary-button label, cancel-button label. (6) DO NOT use `electron.dialog.showMessageBox` from the main process for simple text input — it's heavyweight, blocks the renderer, and doesn't fit React state flow. Native dialogs are right ONLY for file open/save (use the existing `dialog:open-file` IPC bridge for that). (7) Reference (as of v2.0.13): `D:\PMO_OS\src\components\PortfolioStatus.tsx` — `SnapshotLabelModal` component at the bottom of the file, `saveSnapshot` becomes a 5-line modal-opener, `persistSnapshot(label)` contains the original async save body. 568/568 tests still green; no test for the modal itself (UI tests are out of vitest scope). v2.0.13 ships the fix.

741. **For desktop-app DB backups, dual-write to BOTH a local fast-disk path AND an OneDrive (or equivalent personal cloud) mirror path. The local primary write stays authoritative — fast, no network dependency, survives offline. The OneDrive mirror is best-effort offsite redundancy — `fs.copyFileSync` after the primary commit, in a try/catch that NEVER fails the primary backup. Surface the mirror result as separate fields (`mirrorPath`, `mirrorError`) on the BackupResult so a failed mirror is diagnosable but doesn't poison the success of the primary. Reading the OneDrive path from the user's existing `$OneDrive` env var (Windows Microsoft 365 sync sets this automatically) gives zero-config offsite for every user who already has OneDrive enabled. NEVER use a source-control repo (GitHub) as a backup target — pmo.db contains user PII, encrypted credentials, and operational data that has no business in a git repo, even a private one** — On 2026-05-07 night Mahmoud asked if backups should land in GitHub instead of OneDrive. The conversation revealed a confusion between two different cloud targets in the same project: (a) **release artifacts** (`PMO_OS_Setup_v2.0.x.exe`) → GitHub Releases via electron-updater (correct; lesson #753); (b) **DB backups** (`pmo_prod_*.db`) → MUST stay out of git. Reasons GitHub is wrong for backups: (1) `pmo.db` contains the encrypted DevOps PAT (DPAPI-wrapped — opaque to git but still secret), the briefing-recipient email, the IC roster, real Action Log entries with project-specific narratives — none of this should be version-controlled even in a private repo. (2) Git's compression doesn't help binary SQLite files; backup-on-every-commit bloats the repo. (3) Cross-device sync via push/pull is the wrong model for backups (push conflicts on parallel machines, manual operation, no automatic offsite redundancy). (4) `.gitignore` MUST exclude `pmo.db` and `*-pat.bin` — getting backups into git would require disabling that protection. The right design (shipped in v2.0.14): primary backup stays at `D:\PMO_OS\DB Backup\{Prod,Dev}\` (local D: drive, fastest disk, survives offline); add a OneDrive mirror at `$OneDrive\PMO_OS_backups\{prod,dev}\` after the primary write completes. `db-path.ts` exports `resolveBackupMirrorDir(env)` that returns the OneDrive path when env var is set or null when unset — null means skip the mirror silently (no error, just no offsite redundancy for users without OneDrive). `electron/jobs/backup-db.ts` after the primary `sqliteDb.backup(target)` call adds a try/catch with `fs.copyFileSync(target, mirrorTarget)`. Mirror failures (file lock during sync, OneDrive offline, disk full) populate `result.mirrorError` for diagnostics but never fail the primary backup. **Why `fs.copyFileSync` not `fs.symlinkSync`:** symlinks across different drives on Windows require admin elevation; copy is safe at user-level. Also: a symlink would fail if the OneDrive folder is offline, but a copy can be retried. Backup mirroring is fundamentally a "second physical copy" goal, not a "same data through multiple paths" goal — copy is correct. **Why the mirror is best-effort, not load-bearing:** the user's primary recovery path is the local D: drive backup; OneDrive is offsite-redundancy insurance. If OneDrive is offline at backup time, the local backup still ran and the user is recoverable. Failing the entire backup operation because OneDrive was syncing-locked would be silly — it would block the real recovery channel for a tertiary concern. **How to apply:** (1) For ANY desktop-app DB backup flow, design dual-write from the start: local-fast primary + cloud-or-NAS mirror. Don't add the mirror as an afterthought (rotation logic, file naming, status surface all need to handle two locations). (2) The mirror call goes AFTER the primary backup commits successfully — never in parallel (avoids race conditions where mirror succeeds but primary fails). (3) Wrap the mirror in try/catch and capture the error; NEVER let mirror failure short-circuit the primary success. (4) Surface mirror result as separate optional fields on the result object — `mirrorPath?: string | null`, `mirrorError?: string | null` — so callers (Settings UI, status reporters) can distinguish "mirror worked", "mirror failed", "mirror skipped (no env var)". (5) Read the cloud target from a standard env var the user's OS already sets (`$OneDrive` on Windows when M365 is signed in). Don't make the user configure a path manually unless they want a non-OneDrive target — in which case provide an override env var (`$PMO_OS_BACKUP_MIRROR_DIR`). (6) Filename convention MUST match the primary's pattern (`pmo_{env}_{ts}_{kind}.db`) so a forensic restore from the mirror is byte-identical to a primary restore. Don't append "_mirror" or similar — the filename is the source-of-truth identifier; provenance lives in the path, not the name. (7) Test the mirror resolver in isolation: env var set / unset / whitespace-only / override priority / opts.oneDriveEnv injection — 8 test cases covers the contract without spinning up Electron. (8) Reference (as of v2.0.14): `D:\PMO_OS\src\lib\db-path.ts` `resolveBackupMirrorDir(env)` (~25 LOC), `electron/jobs/backup-db.ts` mirror-copy block after primary backup (~25 LOC), `BackupResult` interface gains `mirrorPath` + `mirrorError` fields. 8 unit tests in `db-path.test.ts` lock the contract. Multi-session note: this lands in Session 2's territory (db-path + backup-db) but was explicitly authorized by Mahmoud's request "backup should be in both synced". Companion to lesson #749 (env-separation), lesson #715 (multi-session L# drift), lesson #717 (window.prompt fix in v2.0.13).

745. **For desktop-app dual-write backup pipelines (lesson #741), the test is a FILE-LEVEL byte-equality assertion, NOT a SQL-row roundtrip. The backup contract is "SHA-256 of primary == SHA-256 of mirror == SHA-256 of pre-mutation source"; SQLite row semantics are already covered by SQLite's own tests. Adding better-sqlite3 to a CLI test script introduces ABI fragility — better-sqlite3 native bindings are pinned to Electron's Node version (NODE_MODULE_VERSION 123) but `node scripts/foo.cjs` runs under system Node (137), which fails with `ERR_DLOPEN_FAILED`. The fix is NOT to electron-rebuild before each test run (60s overhead, leaves the dev DB in a wrong-ABI state for Electron afterwards) — it's to drop the SQL layer entirely and assert what actually matters: that fs.copyFileSync produces byte-identical files, and that restoreFromBackup followed by sha256 matches the pre-mutation hash** — On 2026-05-07 night writing the Tier 2 backup roundtrip script for v2.0.16. First attempt used better-sqlite3 to UPDATE a row, then restore, then SELECT to verify the row reverted. Hit `Module compiled against NODE_MODULE_VERSION 123, requires 137` when run under system Node. Considered three workarounds: (a) `npm rebuild better-sqlite3` before, `electron-rebuild -f -w better-sqlite3` after — slow + leaves a tiny race window where a parallel Electron launch finds the wrong ABI; (b) launch the test through Electron itself via `electron . --test-backup` flag — adds Electron startup overhead + test framework integration complexity; (c) drop better-sqlite3 + write a deterministic byte fixture, mutate via direct buffer rewrites, verify via SHA-256. Picked (c). Why this is correct rather than a workaround: the dual-write contract from lesson #741 is fundamentally a file-system claim — "after the primary backup commits, fs.copyFileSync(target, mirrorTarget) produces an identical file at the mirror location, which can be used as a forensic recovery source byte-for-byte equivalent to the primary." Testing that with a SQL UPDATE + SELECT round-trip is testing TWO things at once: the file copy (what we care about) AND the SQLite WAL checkpoint behavior (which SQLite tests for itself, much more thoroughly than we ever could). Untangling those: the test fixture is a 4096-byte file with a stable SQLite-format-3 header + deterministic page contents (`(i * 17 + 41) & 0xff` per byte index). Mutate by rewriting bytes 100-300 in place (size unchanged, hash changes). Restore by `fs.copyFileSync(backup, live)` after deleting any WAL/SHM siblings (matches the production restore sequence in `db-restore.ts`). Verify by SHA-256. Three asserts: PARITY (primary == mirror == baseline), ROUNDTRIP-PRIMARY (baseline → mutate → restore-from-primary → baseline hash), ROUNDTRIP-MIRROR (baseline → mutate → restore-from-mirror → baseline hash). The third is the actual lesson #741 parity proof: the mirror is not just a copy stored offsite, it's a viable forensic recovery source. **Why not just shell out to `sqlite3` CLI:** Windows doesn't ship sqlite3 by default; we'd add a runtime dependency. The pure-JS path (no native bindings, no shell, no SQL parser) runs on any Node version, in any environment, in <100ms. **How to apply:** (1) When writing CLI tests for an Electron app, AVOID native-binding modules that the postinstall hook electron-rebuilt against Electron's ABI. better-sqlite3, sharp, sqlite3, lmdb, all the common Electron native deps. (2) If you really need DB-row-level test coverage in a CLI, run the test through Electron itself via a `--test-mode` CLI flag the main process recognizes. (3) Most "I need to test the DB" goals are actually "I need to test what touches the DB" — file-system pipelines, IPC handlers, REST endpoints — and those can be tested with file fixtures and assertions on the SAME side of the SQL boundary as production code, without a real DB. (4) Reserve real-DB tests for Vitest specs that run inside Electron's sandbox — those have correct ABI by construction. (5) For deterministic test fixtures, use byte patterns derived from index (`(i * P + Q) & 0xff` where P is prime) rather than `Math.random()` — reproducible failures are 10× easier to debug than "it failed on CI but not on my machine." (6) Reference (as of v2.0.16): `D:\PMO_OS\scripts\test-backup-restore.cjs` (~150 LOC, no external deps, 6 asserts, ~50ms runtime). `npm run test:backup-roundtrip` exits 0 on green, 1 on assertion failure (preserves temp dir for inspection), 2 on env error. Companion to lessons #741 (dual-write design rationale) + #744 (Tier 1 regression coverage stratification).

744. **For Electron + Playwright e2e regression coverage, expand BEYOND a single smoke test by stratifying additions into Tier 1 / Tier 2 / Tier 3 by demo-criticality, NOT by feature alphabetization. The Tier 1 set is the regression-gates-must-be-green-before-demo bucket; Tier 2 is should-have-by-next-cadence; Tier 3 is deferred-until-evidence-of-need. For Tier 1 always include: (a) a bilingual visual regression that snapshots EVERY tab in BOTH EN and AR/RTL — RTL layout breaks ship invisibly past EN-only tests, and paired-span bugs only surface visually; (b) a regression gate for any "the renderer disabled this browser API" fix (e.g., the `window.prompt` → React modal swap from lesson #717 — without this gate the API call WILL come back); (c) a cross-tab persistence test that catches the lesson #657 / L088 pattern (data-fetching tabs must use tRPC's React Query cache, NOT local useState that resets on unmount); (d) a tab-routing round-trip that exercises every TabId reachable from the menu, since Electron's native menu accelerators are intercepted at the OS level and Playwright's `page.keyboard.press()` CANNOT fire them — instead expose a `window.__pmoOsSetTab(id)` test hook in App.tsx and call it via `page.evaluate()` so the spec drives the SAME setActiveTab path the menu IPC uses** — On 2026-05-07 evening Mahmoud said "need other testing types not smoke test only" after the L098 CDP smoke for Refresh-from-Outlook passed. The single smoke gave high confidence on ONE feature but had zero coverage of the other 18 tabs. v2.0.15 ships 4 Tier 1 specs (~200 LOC + 38 baselines) addressing the 4 highest-risk regression holes for the Mon May 18 demo: bilingual visual (EN+AR for all 19 tabs), Save Snapshot regression gate (v2.0.13 fix from lesson #717 — proves window.prompt cannot reappear), cross-tab persistence (Data Gate / SPI-CPI / Project Status History round-trip through Action Log without re-fetch), menu accelerator routing (all 19 TabIds reachable via __pmoOsSetTab + getActiveTabId read-back). v2.0.16 follows with 3 Tier 2 additions (refresh-from-outlook Playwright spec porting the L098 CDP smoke, graceful-degradation gate covering LiveBackendBadge + OutlookCategories + Calendar + Settings tabs, backup roundtrip + dual-write parity script). **Why the visual regression for AR is non-negotiable:** EN passes the test means English text renders correctly; it says NOTHING about whether `dir="rtl"` flipped the layout properly, whether paired `<span class="en">/<span class="ar">` siblings render in the right order in AR mode, whether RTL flexbox triggers a horizontal scrollbar, whether Arabic-only fonts have fallback gaps. Capturing 19 EN baselines + 19 AR baselines costs 38 PNGs (~5MB on disk) and ~3 minutes of runtime per full-suite execution; the alternative is shipping AR-broken demos to Dr. Omar and finding out at meeting time. **Why the test hook pattern beats trying to fire menu accelerators:** Electron's native menu (built via `Menu.setApplicationMenu`) registers OS-level keyboard shortcuts. The accelerator fires BEFORE the renderer V8 isolate sees the keypress — it goes through the main-process Menu shortcut handler, then sends an IPC message to the renderer, then the renderer's `setActiveTab` runs. Playwright's `page.keyboard.press('Control+Shift+1')` only dispatches to the renderer-focused frame; the menu accelerator handler never fires. The fix that scales: in App.tsx add a useEffect that exposes `(window as any).__pmoOsSetTab = setActiveTab` and `(window as any).__pmoOsActiveTab = activeTab` — these are test-only side-channels, not part of any production code path. The spec's `clickTab(page, label)` helper now does `page.evaluate(id => window.__pmoOsSetTab(id), TAB_ID_BY_LABEL[label])` which routes through the SAME React state machine the production menu IPC drives. Same code path, same render, same query cache. **Why cross-tab persistence needs an explicit test:** the lesson #657 pattern (auto-fetch on tab visit using local `useState({state:'idle'})` reinit on remount) is a recurring footgun whenever a developer is unfamiliar with the codebase. The regression gate is: open Data Gate, count `tbody tr` rows, switch to Action Log (unmounting Data Gate), switch back to Data Gate, assert (a) zero `text=/Loading/i` elements appear in the 200ms window after switch-back AND (b) row count is identical. If useState was re-introduced, the second visit would show a Loading state, then re-fetch, then re-render. tRPC + React Query cache survives unmount because the cache lives at the `<QueryClientProvider>` wrapper scope (App.tsx top-level) — same as the test hook. **Why the visual setLanguage helper must be button-visibility-based not state-based:** the LanguageToggle button shows the OPPOSITE language as its label ("AR" when current is EN, "EN" when current is AR — it's a "switch to" button). Initial implementation used `document.documentElement.lang` for early-return, which is set by react-i18next on mount but may not be in sync during transitions. Robust fix: `page.getByRole('button', { name: new RegExp('^' + targetLabel + '$') }).first().isVisible()` — if the button labeled with the TARGET language is visible, click it (we're in the OTHER language); if it's NOT visible (because the toggle now shows the OPPOSITE label, meaning we're already in target), early-return. This is more robust than reading any state because it asks the SAME question the user's eyes ask: "is the toggle showing the language I want to switch INTO?" **How to apply:** (1) Before any major demo, audit the e2e spec set for tier coverage: bilingual visual (T1a), recently-fixed-API regression gate (T1b for whatever the latest such fix was), cross-tab-cache regression (T1c), tab-routing round-trip (T1d). If any tier is missing, BUILD IT before declaring demo-ready. (2) For Electron apps, expose test hooks via `useEffect(() => { (window as any).__appTestHook = ...; }, [deps])` in App.tsx — clean up in the return function. Production builds CAN ship these hooks (no harm; they're tiny no-ops to non-test runs) OR be stripped via build-time `if (import.meta.env.DEV)` guards if you're paranoid. (3) Visual baselines MUST live alongside the spec (`tests/visual/snapshot.spec.ts-snapshots/<Tab>[-AR]-win32.png`); commit them to the repo so CI runs against the same pixels. First-run on a new machine generates baselines via `--update-snapshots`; subsequent runs diff against them with `maxDiffPixelRatio: 0.02` (2% tolerance for sub-pixel rendering differences across machines). (4) Mask volatile timestamp + spinner regions before snapshot to reduce false positives: `mask: [page.locator('main >> [class*="font-mono"][class*="text-fg-muted"]')]`. (5) Cross-tab persistence specs MUST assert the absence of a Loading indicator + the equality of row counts — both sides of the contract. Just asserting "no Loading" doesn't prove the data is the same; just asserting "same count" doesn't prove it wasn't re-fetched (a new fetch could return the same count but different data). (6) When tests fail with mysterious 5s timeouts on `getByRole('button', { name: ... })`, suspect state leak from previous tests in the suite. Run a single failing test in isolation (`-g "Portfolio Status pixel snapshot (EN)"`) — if it passes alone but fails in the suite, the previous test contaminated state. Fix the helper to be idempotent (visibility check, not state check). (7) Reference (as of v2.0.15-v2.0.16): `D:\PMO_OS\tests\e2e\_attach.ts` (foundation helpers), `D:\PMO_OS\src\App.tsx` (test hook useEffect), `D:\PMO_OS\tests\e2e\portfolio-snapshot-flow.spec.ts` (T1b), `D:\PMO_OS\tests\e2e\cross-tab-persistence-flow.spec.ts` (T1c), `D:\PMO_OS\tests\e2e\menu-accelerators-flow.spec.ts` (T1d), `D:\PMO_OS\tests\visual\snapshot.spec.ts` (T1a 38-test bilingual matrix), `D:\PMO_OS\tests\e2e\refresh-from-outlook-flow.spec.ts` (T2a Playwright port of L098 smoke), `D:\PMO_OS\tests\e2e\error-paths-flow.spec.ts` (T2b graceful degradation gate, 4 sub-tests). 38/38 visual + 8/8 new e2e specs at v2.0.16 ship. Companion to lessons #657 (cross-tab persistence root cause), #687 (sidebar removed in v2.0.8 — necessitated test hook pattern), #715 (multi-session L# drift), #717 (window.prompt fix that T1b gates against), #741 (dual-write backups), #745 (backup roundtrip script ABI lesson).

743. **When a desktop app reaches feature-parity with the HTML/dashboard version it was meant to replace, FREEZE the HTML at its current published version and switch all forward work to the app. Maintaining BOTH the HTML and the app means double-edits for every refresh — every new project, every revised narrative, every UI tweak has to land in two places. The dual-source-of-truth tax kills velocity. Once the app crosses parity, declare HTML frozen, commit the freeze rule to persistent memory, and propose all future requirements as app features (new tabs, new tRPC procedures, new SQLite tables) — never as new HTML versions. The frozen HTML files stay on disk as historical reference + design source for ports, but no `_v<N+1>` follow-ups** — On 2026-05-07 night Mahmoud declared: "freeze all edit in html, we will use the our new system". Context: PMO_OS v2.0.14 reached feature-parity with v14 HTML on 2026-05-04 and now exceeds it (Setup Wizard, native menu nav, click telemetry, dual-write backups, Project Status History with embedded baselines, GH-PAT auto-update, env-separated dev/prod userData). Maintaining 6 HTML deliverables (PMO Dashboard v14, Sunday Talking Points v3, Outlook Email Analysis v13, OKR Dashboard v10, Weekly Meeting Agenda v7, Project Management Handling v3) IN ADDITION to the app meant: every Outlook Categories refresh required updating both the HTML's `<script id="cat-csv">` data block AND the app's `outlookCategories.tsx` re-fetch path; every project_register edit required updating both the HTML's `PORTFOLIO_KNOWN` map AND the app's SQLite `project_register` table; every new Phase X feature required two builds. The freeze rule eliminates this. **Why frozen-not-deleted:** the HTML files contain design intent that's still useful — when porting v14's Phase 7 Calendar conflict detection into PMO_OS Day 13, the v14 source was the design reference. Frozen HTMLs continue serving as design source for future ports; they just don't receive new versions. **How to apply:** (1) When proposing a new feature, FIRST check the freeze rule (loaded from `feedback_html_deliverables_frozen.md` via auto-memory). If the feature falls into a frozen artifact's domain, propose it as an app feature, NOT a new HTML version. (2) For demos: walk the live app, NOT a static HTML deck. The app IS the demo; talking points become spoken commentary, not a separate published artifact. (3) For bi-weekly reviews (Semir + Dr. Omar Mondays): open PMO_OS, click through tabs, save snapshots live during the review. The Project Status History tab provides the audit trail that the old "Sunday Talking Points HTML" used to serve. (4) When a user references "v14's X feature" or "the v3 STP deck", READ those frozen HTMLs for design context, then implement in the app. Never propose extending the frozen artifact. (5) Frozen-version metadata must live in PERSISTENT MEMORY (feedback memory file + MEMORY.md index), NOT just commit messages — future sessions auto-load memory but don't read commit history. (6) The freeze rule has explicit exceptions: emergency demos for parties who can't install Electron, one-off email-friendly reports, wireframes for future app features. Mark these clearly so the rule itself stays unambiguous. (7) Reference: 2026-05-07 — `C:\Users\DELL\.claude\projects\d--Project-Management\memory\feedback_html_deliverables_frozen.md` lists the 6 frozen artifacts at their current versions + frozen-paths. Indexed in MEMORY.md so every future Project Management session sees this rule on auto-load. Companion to lessons #685 (derive-vs-hardcode: prefer DB over static fixtures), #694 (migration audit doc as cross-PR coordination), #717 (window.prompt fix in v2.0.13 — completed the parity threshold).
